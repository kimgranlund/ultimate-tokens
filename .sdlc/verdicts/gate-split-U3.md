# Verdict gate-split U3 · 🟢 at 27356468

| Field | Value |
|---|---|
| Plan | `.sdlc/plans/gate-split.md` at `plan/gate-split`, revision 9 (the `gap-19 (72)` SAMPLED-log needle moved from pre-land into U3's own verdict) |
| Unit | U3 (`anchor.mjs`: sampled by default, full under `--full`), branch `unit/gs-U3`, head `27356468`, builder pass 2 |
| Verifier runs | `/tmp/gs-u3-verify-p2` (worktree at 27356468, never edited). Nine throwaway clones under `/tmp/gs-u3-verify-ctl-p2` (`ma`, `mc`, `me`, `u34a`, `u34b`, `p3a`, `p3b`, `p3c`, `p8p9`), left in place per the session's `rm -rf` restriction |
| G0 | waived for U2 to U5 (approval question 8); re-observed, no drift from the plan's 36ce7777 baseline (19 pass, 0 FAIL on the unedited FULL leg reproduces at the unit head) |
| Base for P8/P9 | `ebddc55d` (this unit's own fork point), per the correction the pass-1 builder made and the team-lead's note, not `git merge-base origin/main HEAD` |

## Rows

| # | Criterion | State | Evidence | Negative control |
|---|---|---|---|---|
| U3-1 | FULL is the old file: same gates, same verdicts, full corpus | 🟢 | `npm run -s gate:corpus-anchor` in the unit worktree: `exit 0`, `19` pass lines, `0` FAIL lines, `1` occurrence of `(FULL: 343 curated documents, 3780 palettes)`, `1` occurrence of `gap-19 (72), distinct-25 (16) and notch (15`. Matches the plan's 36ce7777 count with no drift | P5's M-A, M-C, M-E rows below all red the same FULL leg |
| U3-2 | SAMPLED is green, says so, and the pass-2 fix holds | 🟢 | `node test/engine/anchor.mjs`: `exit 0`, `1` occurrence of `(SAMPLED seed 0: 35 curated documents, 392 palettes)`, `0` FAIL lines. Revision 9's own needle, independently reproduced: `grep -c 'gap-19 (72)'` prints `0` on this SAMPLED log against `1` on the FULL log above. The PASS line itself reads `PASS (SAMPLED): ... window-clamp (3), gap-19 (8), distinct-25 (3) and notch (2) are the same named allow-lists read as a SAMPLED subset this run (upper bound only, no in-file negative control this run - the exact count and the biting control are the FULL leg's ...)`, matching the handoff's quoted bytes exactly | the FULL log's PASS line carries the frozen `gap-19 (72)` wording; the SAMPLED log never does, in this run or the fictitious-name control below |
| U3-3 | broad regression caught by the sample; sparse one recorded, not graded | 🟢 | M-A clone, SAMPLED: `exit 1`, `lone-spike allow-list: 139 (expected at most 64 ...)`. M-E clone, SAMPLED: `exit 1`, `hueSpace-perceptual-bound: ... max OKLab dE 0.0308 (want <= 0.01 ...)`, its worst witness the always-present default kit as the plan predicts. M-C clone, SAMPLED: `exit 1` (`anchor-ramp monotone: ... FAIL: 1 gate failure(s)`), recorded per the plan's own instruction, not graded | U3-2's unmutated SAMPLED run is the control for M-A and M-E: `0` FAIL lines each |
| U3-4 | subset semantics, both halves, on `gap-19` | 🟢 | (a) deleted `brands "Burger King · The Flame Identity · 2021 rebrand" tertiary-muted #F5EBDC` from `RAMP_GAP_ALLOW` in a fresh clone: SAMPLED `exit 1`, line `,  gap allow-list: unexpected member, brands "Burger King · The Flame Identity · 2021 rebrand" tertiary-muted #F5EBDC`. (b) added `` `zz-not-in-corpus\|primary\|500` `` in a fresh clone: SAMPLED `exit 0`; FULL (`npm run -s gate:corpus-anchor`) `exit 1`, line `,  gap allow-list: expected member missing, zz-not-in-corpus\|primary\|500`. Both byte-for-byte matches to the handoff's quoted lines | each half is the other's control, as the plan states; (a)'s SAMPLED run stays red where (b)'s SAMPLED run passes on the same file shape |
| U3-5 | the file's own share of `npm test` | 🟡 by design | `/usr/bin/time -p node test/engine/anchor.mjs`: `real 34.14`, `exit 0`, 19 pass equivalent (SAMPLED PASS line clean). Load before `18.28 19.61 20.84`, after `21.68 20.43 21.10`. Target is at or under 15 s quiet; the plan's own timing-row rule grades this row only when the 1-minute load is under 5 at start, and it read `18.28`, about 3.7x the core-relative threshold used elsewhere in this pass. Recorded honestly, not waited on, consistent with the builder's own pass-2 🟡 | the same command at the fork point ebddc55d, where `anchor.mjs` carries the FULL corpus, reads far higher; a row that could not separate the two files would be the vacuous case, and it does separate them |
| P1 | `npm test` green, count agrees, tree byte-stable | 🟢 | `npm test` in the unit worktree: `✓ all 50 test files passed`, `exit 0`. `perl` count over `test/run.mjs`'s `TESTS` array: `50`. `git status --short \| wc -l`: `0` | not rerun here (a destructive P1 control needs its own throwaway clone and this unit's own gates already discriminate every moved sweep, see U3-3/U3-4/P5); corroborated instead by nine clones above, each of which reds a registered leg on a one-line edit |
| P3 | coverage not given back: the gate script runs its sweep on everything | 🟢 | real leg: `npm run -s gate:corpus-anchor` `exit 0`, mode-line count `1` (PASS line quoted under U3-1). Control (a), `--full` dropped from the script in a clone: `exit 0`, `(FULL:` count `0` (SAMPLED line printed instead). Control (b), `process.argv.includes("--full")` forced to `false` in a clone: `exit 0`, `(FULL:` count `0`. Control (c), the FULL-loading `if (FULL) {` block (line 154) forced to `if (false) {` in a clone, called via `--full`: `exit 1`, `FAIL` count `10`, `35` count `3` (`FULL loaded 35 curated documents, want 343`), showing the FULL-side vacuity check bites when the flag is honoured but the corpus load is starved | controls (a) to (c) are each other's negative space; none of the three can pass silently, each names a different way the gate could lose coverage |
| P5 (rows 2, 5, 7; anchor's own) | every moved sweep still reds on its mutation, FULL leg | 🟢 | row 2 (M-A): `npm run -s gate:corpus-anchor` `exit 1`, `lone-spike allow-list: 1709 (expected 64)`. row 5 (M-C): `exit 1`, `anchor-ramp monotone: 13 (expected 0 ...)`. row 7 (M-E): `exit 1`, `hueSpace-perceptual-bound: ... dE 0.0308 (want <= 0.01 ...)`. All three `git diff --stat` on the mutated file read `1 file changed, 1 insertion(+), 1 deletion(-)` before running. All three values match the plan's own mutation table exactly | the same seven commands unmutated: U3-1's FULL run reds `0` times on all three needles |
| P7 | baseline agrees with `TESTS.length` | 🟡 recorded, not blocking | `sh .sdlc/checks/baseline-agrees-check.sh`: `STALE tests: baseline 48, test/run.mjs TESTS 50`, `STALE ui.html: ...`, `STALE head: baseline ref 20298cc has the same tree as HEAD outside .sdlc/ and .gitignore`, `stale total: 3`, `exit 1`. Expected: the plan states this stays STALE on unit branches until U6b reruns the baseline, and is a pre-land blocker only, not a unit-level one | not run; the mechanism is the same script U1's verifier already exercised and reproduced the same way |
| P8 | branding clean, no added em dash outside a backtick span, base `ebddc55d` | 🟢 | `node test/repo/branding.mjs \| tail -1`: `branding: clean (515 files scanned)`. `git diff ebddc55d \| grep -v '^+++ ' \| grep '^+' \| perl -CSD -ne 's/\`[^\`]*\`//g; print if /\x{2014}/' \| wc -l`: `0` | in a clone, one added comment line with a literal em dash outside backticks: `branding.mjs` still reads `clean` (it does not scan for em dashes), but the diff-filter count moves from `0` to `1`, proving the filter bites on this diff's own shape |
| P9 | scope wall, base `ebddc55d`, `Q2=yes` | 🟢 | `git diff --name-only ebddc55d`: exactly `.sdlc/handoffs/gate-split-U3.md` and `test/engine/anchor.mjs`, both inside the plan's allow-list. The plan's full P9 filter command: `0`. `git diff --name-only ebddc55d -- src \| wc -l`: `0` | in a clone, a one-line touch to `src/engine/hct.js`: the `-- src` leg prints `1` |

## Findings 4 to 6, carried to pre-land per the team-lead's note

Not graded here, on instruction; noted so the pre-land pass has a pointer. No standalone review artifact for the six pass-1 findings was found on disk (board.md and the plan's revision log record the outcome, not the itemized text), so this list is inferred from the three items the pass-2 handoff itself flags under "Corrections (flagged for the reviewer)" and left unchanged in pass 2 (findings 1 to 3 were pass 2's own fix, described and independently reproduced above):

1. `ORDER_ALLOW`/`DUPE_ALLOW` (anchor-ladder) were extended to the same FULL-exact/SAMPLED-subset treatment as the five ramp allow-lists, though the unit brief names only the five.
2. `controlSubjects`'s SAMPLED floor (46) and `anchored.length`'s SAMPLED floor (not zero) are the builder's own numbers, not plan-pinned ones.
3. The R10 negative controls (`dropCheck`/`swapCheck`) are gated to FULL only; SAMPLED skips them rather than making them mode-aware.

None of the three is a defect in what U3 claims to have done: all three are judgment calls the builder flagged openly, and this unit's own criteria (graded above) hold regardless of how pre-land rules on them.

## Not applicable to a U3-level verify

P2 (the ceiling of record) and P10 (CI wall budget) are pre-land-only rows that read `.sdlc/baseline.md` and a CI run's own timing; U3 changes neither. P4 (CI invokes every gate) is U6a's row (`.github/workflows/ci.yml`, untouched by this diff, already verified 🟢 separately). P6 (`corpus-sample.mjs` purity) is U1's own row (U1-1), unmodified by U3 and already independently verified 🟢 by the U1 verifier. Re-running any of these against U3's diff would just reproduce another unit's own verdict.

## Recorded loads

1-minute load at the start of each timed leg ranged from about 6.3 to 39.9 across the pass; every heavy run except U3-5's timing leg is ungraded on load (P1, U3-1, U3-2, P3, P5 report pass/fail, not wall time), so only U3-5 carries the plan's load-gating rule, and it read 18.28, over the 5-under-cores bar, hence the 🟡.

## Counts

| Rows graded | 🟢 | 🟡 (by design, not blocking) | 🔴 |
|---|---|---|---|
| 11 (U3-1 to U3-5, P1, P3, P5, P7, P8, P9) | 9 | 2 (U3-5, P7) | 0 |
