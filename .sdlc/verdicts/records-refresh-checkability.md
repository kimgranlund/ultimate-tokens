# Criteria review records-refresh · 🟢 mobilize

Reviewed by sdlc-verifier on 2026-09-18 on `plan/records-refresh` @ `8919292` (the plan's front matter still says `746f93d`, 4 commits over `cf8e61a`; the branch is now 6 commits over it, the plan commit included). Plan: `.sdlc/plans/records-refresh.md`.
All 14 criteria are 🟢 checkable: 🟢 14 · 🔴 0. Nothing goes back to the planner.

Method: I ran 11 of the 14 stated commands verbatim at `8919292` and compared the output with the pre-state the plan predicts. Every one reproduced the planner's control figure exactly, so the commands parse, the table-escaped pipes survive, and each criterion separates "unit missing" from "unit done". P1 to P4 were run as gates during the survey grading earlier today in the same worktree.

| # | Checkable | The check I would run, and what it printed at `8919292` |
|---|---|---|
| P1 | 🟢 | `npm test 2>&1 \| tail -1` then `git status --short \| wc -l`. Ran it today with no `node_modules`: `✓ all 46 test files passed`, then `0`. A standing regression gate, so it passes at the pre-state by design; the mutation control (a changed ref in `src/engine/semantic.js`) took `node test/engine/semantic.mjs` to exit 1, so the suite does bite |
| P2 | 🟢 | the stated command, comparing the built size with the one the baseline records. Ran build today: `wrote figma/plugin/ui.html 3753.6 KB` against `3695.6` in `.sdlc/baseline.md`, so the criterion is red at the pre-state and turns green only when U1 rewrites the number. `npm run build` with no `node_modules` exits 127. Note: `$CLAUDE_JOB_DIR` may be unset in a plain shell, which turns the log path into `/tmp/build.log`; harmless, but the clone path in the P1 and P2 controls becomes `/tmp/neg`, so the builder should give it a unique name |
| P3 | 🟢 | `npm run smoke 2>&1 \| grep -c 'SMOKE PASS'`. Ran it today: exit 0 and exactly one `SMOKE PASS` line. Control confirmed separately: `node test/smoke/smoke.mjs` with no `dist/` exits 1 with `smoke: missing dist/ultimate-tokens.html` |
| P4 | 🟢 | `node test/repo/branding.mjs \| tail -1`. Ran it today: `branding: clean (441 files scanned)`, exit 0. It reads `.sdlc/`, so it is the gate that catches a bad word in the very files U1 rewrites |
| P5 | 🟢 | `git diff --name-only $(git merge-base origin/main HEAD) \| grep -vcE '^\.sdlc/\|^\.gitignore$'`. Printed `0`. A scope guard: it cannot fail from the unit being missing, it fails when the unit reaches outside its wall, which is what it is for |
| U1-1 | 🟢 | `sh .sdlc/checks/baseline-agrees-check.sh`. I extracted the script from the plan and ran it: `STALE tests: baseline 44, test/run.mjs TESTS 46`, `STALE ui.html: baseline 3695.6 KB, tree 3753.6 KB`, three `ok` timing lines, `STALE head`, `ok` ancestry, `stale total: 3`, exit 1. Exactly the planner's control. The two figures it compares are computed the same way the build prints them (the script's `length/1024` returns 3753.6, the build line's own number), so U1-1 and P2 cannot contradict each other |
| U1-2 | 🟢 | the awk fence extraction then `diff`. The plan holds exactly one ` ```sh ` fence (`grep -c` prints 1), the octal backtick pattern works in this awk, and the extracted 36 lines are the script I ran for U1-1. Before the unit, `diff` errors on the missing file; one changed character prints a hunk. Note: the check hangs on the plan file's path, which gains `archive/` at landing (the plan says so) and on nobody adding a second `sh` fence to the plan |
| U1-3 | 🟢 | `grep -cE '^[\|] .npm (test\|run build\|run smoke). [\|] 3/3 [\|] 0 [\|]' .sdlc/baseline.md` then the old-figure grep. Printed `3`, then `4`, the planner's control. The second count is what moves: it must reach `0`, and it cannot unless the old head, run id and size are all gone |
| U1-4 | 🟢 | the three-part sha and jobs command. Printed merge base `cf8e61a` against baseline ref `7faf3aa`, so the criterion is red pre-rebase, and `gh run view 35342720776` answers `1ea2f80 build-test=success panda-smoke=success`, so after the rebase the three shas line up. The jobs query reports `build-test=failure` on run 34660177977, so it can say red |
| U1-5 | 🟢 | the `git grep` sweep with the history dirs excluded. Printed three paths: `.sdlc/adapter.md`, `.sdlc/baseline.md`, `.sdlc/debt.md`. It must print one. Checked for a self-trap: the new check script writes the count regex as `(\d+) test files`, which the sweep pattern `[0-9]+ test files` does not match, so adding the script cannot make this criterion fail on itself |
| U1-6 | 🟢 | the four-part adapter command. Printed `0`, `2`, `0`, `0`; expected after the unit is `0`, `0`, `1`, `1`, so values two to four discriminate. Value one is a scope guard that reads `0` either way, which is its job, not a defect |
| U1-7 | 🟢 | the three-part type-fonts command. Printed `0`, `0`, `false false true`. The third value is the useful trap: it proves the same regex shape does see a generator that is in the chain, so a later change adding `gen:type-fonts` to `npm test` would flip it and mark both records false |
| U1-8 | 🟢 | the check-ignore loop and `git ls-files`. Printed this machine's user-global ignore file and this clone's `.git/info/exclude`, then `0`. Both paths must read `.gitignore` after the unit. This is the criterion that closes the survey verdict's C13 concern, and it fails loudly while the rules stay local |
| U1-9 | 🟢 | the eleven-part staleness-note command. Printed `0 0 0 0` for the note and rule counts, `0`, `0`, `1` for the quote counts, `ancestor 1`, `10`, `0`, `0`. Expected after the unit is `1 1 1 1`, `1`, `1`, `1`, `ancestor 1`, `10`, `0`, `0`. Checked the two awk traps: neither `architecture.md` nor `debt.md` has a `## ` heading before its insertion point (first heading at line 16 in both), so the `/^## /{exit}` scan does reach the note. Note: the last two values count deleted lines only, so a stray added line elsewhere in either file would slip through; the reviewer's read of the diff is what covers that. The phrase "quotes the verdict faithfully" is carried by the two number greps against `.sdlc/verdicts/survey.md`, which do match my own C15 line; the rest of that clause is a reading judgment, which is what the l1 reviewer and verifier are for |

Two notes for the Orchestrator, neither blocking. U1-1's expected `stale total: 0` also requires the adapter's three time ranges to equal the rounded minimum and maximum of the new baseline timings, so step 1's quiet-slot rule is load bearing: an inflated run does not just look odd, it sets the adapter ranges. And the plan's own head line is stale by two commits, which is the exact failure mode the plan exists to fix.
