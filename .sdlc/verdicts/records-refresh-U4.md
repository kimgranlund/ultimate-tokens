# Verdict U4 · 🟢

Unit `unit/rr-U4` at `d04b68244c42cdbdacb41364ce2fca0dde0ad63c`. Criteria: `.sdlc/plans/records-refresh.md` §U4 rows 1 to 7 on `plan/records-refresh` at `9145db45afeb0c386d8e63a6318d584524541204` (row 5 as corrected at `aed0caee`). `BASE` = `git merge-base plan/records-refresh HEAD` = `695f13fba158494249d467f45f18b7776a141e8d`. `MB` = `git merge-base origin/main HEAD` = `20298cca9a152e8ff6041c78573293f48628c10f`. Verified in own detached scratch worktrees of `unit/rr-U4` and of `BASE`, plus a `--shared` clone; no hook bypassed, nothing pushed, no source edited. `/usr/bin/grep` has no `-P` and `ggrep` is absent on this host, so rows 3 to 5 ran their PCRE parts through `perl -ne` and `git grep -P` (git here does carry PCRE, checked). `board.py ids` is not yet live in the plugin cache (`grep -c 'board.py" ids' "$HP/pre-commit"` prints `0`), so `B` and `GH` came from `unit/df-ids` in the plugin repo per §Dependency: `python3 "$F/board.py" ids --help` exits 0, the cache `main` copy exits 2.

| # | Criterion | State | Evidence | Negative control |
|---|---|---|---|---|
| 1 | the plugin's check accepts every tracked `.sdlc/*.md` on the unit tree | 🟢 | `python3 "$F/board.py" ids .sdlc` printed nothing, `exit 0` | Same command in the `BASE` worktree printed the thirteen lines `adapter.md:103: C1 defined in a adapter file; owner kind ['plan']` through `adapter.md:115: C13 ...`, `exit 1`. `ids "$F/nope"` printed `board.py: no such directory: ...`, exit 2, so a mistyped path cannot read as green |
| 2 | the hook as git runs it accepts a commit touching `.sdlc/adapter.md` after U4 and refuses it before | 🟢 | `git clone -q --shared . "$F/u4hook"`, `core.hooksPath` set to the `unit/df-ids` `githooks/` extracted by `git archive` into `$F/plug` (its `pre-commit` greps `1` for `board.py" ids`), blank line appended to `.sdlc/adapter.md`, `git commit -qam probe` gave `commit 0` with no refusal line | Same clone at `reset -q --hard 695f13fb`, same probe: the thirteen refusal lines, `board.py ids refused an id defined outside its owning file (plan-rules Id namespaces); rename it or move the definition`, `commit 1`. A probe commit touching `.sdlc/roadmap.md` instead exits 0, so the hook discriminates by file, not by any staged `.sdlc/` path |
| 3 | mapping X1 to X13 back to C1 to C13 reproduces the base adapter except the one appended amendment | 🟢 | The filtered `diff` printed nothing; `diff ... \| grep -c '^<'` printed `0` | A copy with one §4 cell reworded (`\| X7 \|` to `\| X7 REWORDED \|`) printed 4 diff lines; a copy with the X13 row renamed `X14` printed its `<`/`>` pair, since the map covers 1 to 13 only; the base `adapter.md` holds `0` tokens matching `X([1-9]\|1[0-3])`, so the map folds nothing predating U4 |
| 4 | §4 is X1 to X13 in order, no C-headed row, the ten cites renamed, every remaining C1 to C13 token on a survey-cite line | 🟢 | `X1,X2,X3,X4,X5,X6,X7,X8,X9,X10,X11,X12,X13`, then `0`, `0`, `0`, `24`, `1`, matching the expected values exactly | In the `BASE` worktree the same six sub-commands printed an empty list, `13`, `18`, `1`, `0`, `0`. A copy with the line-146 cite reverted to `conflict C9` printed `1` on the third value and `23` on the fifth |
| 5 | live records cite the new ids, the three debt notes name X5 to X7, nothing else in `debt.md` moved, `.claude/` and `README.md` clean, history unchanged | 🟡 | Five of six sub-checks match as written: sweep `0`, debt X cites `3`, debt C cites `0`, `debt.md` non-excluded deletions `0`, `.claude`/`README.md` files `0`. The sixth, `git diff --stat $MB -- .sdlc/verdicts .sdlc/handoffs ... ':!.sdlc/handoffs/records-refresh-U4.md' \| wc -l`, printed `12`, not `0`. All 12 lines are U1, U2 and U3 record writes already on the branch (`records-refresh-U1/U2/U3` handoffs and verdicts, `survey-2026-09-18-approval.md`, `survey.md`, `verdicts/architecture.md`, `verdicts/survey.md`, `verdicts/records-refresh-checkability.md`): the same command against `BASE` prints `0`, so U4's own diff rewrites no history file. The criterion's intent holds; its base choice, changed from `$BASE` to `$MB` by `aed0caee`, cannot read `0` for any unit after U1 | Sweep in the `BASE` worktree printed `13`; base debt counts printed `0` and `3`; a planted `adapter C9 says so` line in `.claude/CLAUDE.md` on the unit tree made the sweep print `1`; appending `conflict X9 (was C9)` to `.sdlc/verdicts/records-refresh-U1.md` made the history count print `2` against `BASE`. All plants reverted, worktree clean |
| 6 | scope: three files, no new em dash, branding clean | 🟢 | `.sdlc/adapter.md,.sdlc/debt.md,.sdlc/handoffs/records-refresh-U4.md`, then `1` and `1`, then `branding: clean (459 files scanned)`, `exit 0` under `set -o pipefail` | A real em dash byte-inserted into the amendment made the counts read `1` then `2`; a staged fourth path (`.sdlc/EXTRA.md`) made the name list read four entries. Both reverted, worktree clean |
| 7 | regression: U1-6, U3-7, U1-9 as amended, and P5 | 🟢 | U1-6's amended first command `0`; U3-7's amended first command (same form) `0`; U1-9's last command (`debt.md` deletions outside P3 and C5 to C7) `0`; P5 `0` | U1-6's pre-amendment form on this tree printed `23`, the thirteen rows plus ten cite lines the map removes; a reworded §4 cell printed `1` through the amended form, so the map does not blind it; a reworded non-excluded `debt.md` row made U1-9's last count print `1`; `echo "// probe" >> src/engine/motion.mjs` made P5 print `1`. All reverted |

## Notes

- The amendment in `.sdlc/adapter.md` §4 sits after the X13 row with one blank line each side and before `## 5.`, and is byte-identical to §Texts with `<date>` as `2026-09-19`. The three `debt.md` §Texts cells are byte-identical to the plan's text.
- Row 5's first value is `0` now, so Note A in the handoff (written before `aed0caee` excluded the board from the sweep) is resolved and needs no Orchestrator action.
- The handoff records rows 1 and 2 in summary form (`(nothing), exit 0` and `same`) rather than the verbatim output step 3 asks for. Cosmetic, outside rows 1 to 7.
- Action for the Orchestrator, not for the builder: row 5's sixth sub-check needs `$BASE` back, or a per-unit base, before pre-land reads it, otherwise it stays unsatisfiable for every unit after U1.

Close-out 2026-09-19 (conductor, pre-land): `$BASE` was restored in fc118ef6, so row 5's sixth sub-check is satisfiable again; the pre-land pass re-grades row 5 at the plan head and `.sdlc/verdicts/records-refresh-prepr.md` carries that grade. The 🟡 above is the unit-pass record and stays as history.
