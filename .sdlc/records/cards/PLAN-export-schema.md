# PLAN-export-schema · PLAN — Align every export surface with the 0.3.0 color system and stamp the export schema · 2026-09-11 · active (ratified 2026-09-11)
| Field | Value |
|---|---|
| Decision | Nine rulings (RP-1..RP-9) over an audit of every export surface at `origin/main` ae621c9: palette group becomes exported METADATA never a name (JSON `palettes[n].group`, DTCG `$extensions["com.ultimate-tokens"].group` on the raw palette node, brand-kit `group` + `list_palettes`, DS bundle `familiesByGroup`, CSS/OKLCH/Tailwind comment lines; nothing on UI3, Figma, ShadCN); group chroma controls ride only JSON `meta.controls` and the brand-kit `controls` (field name `baseChroma`, corrected per #594); ShadCN extends to `chart-1..8` from `data-1..8` with 6..8 OMITTED when absent (H-3, a departure from the proposed default); DS bundle prime parity proven per profile by gate, no prose fork; UI3 keeps `Color Prime` at one `Base` mode; the binder stays unaware of `Color Prime` but its dry-run report says so; consumer plugin names the data families; one `EXPORT_SCHEMA_VERSION = 2` constant stamped on every surface that can carry it (JSON `meta.schemaVersion`, DTCG root `$extensions`, UI3 `$schema …v2`, first-line comments on CSS/OKLCH/Tailwind/ShadCN, DS `$schemaVersion`/`tokensSchema`, brand-kit `$schema …/2` and `SERVER.version 0.2.0`), with version 1 defined retroactively as the unstamped pre-#503 shape; knowledge-04 becomes the shape of record. Steps land concept-major, never format-major, stamp last. |
| Forces | prime system and data palettes already on every surface (plan smaller than the charter feared), #556 ruling that groups are editor-only names/folders, consumers of the zip need one comparable number, R2 mode-independent prime, roles never alias prime tokens, #566 (absolute per-group base chroma) in flight and not touched, byte-diff discipline per step, name freeze (no group word enters a token name) |
| Rejected | per-format version numbers (consumers get several files from one run and need one number); a fallback colour for `chart-6..8` when the data palette is absent ("an invented sixth colour would be exactly what the stock contract lacks"); a Light/Dark mode on `Color Prime`; per-profile prose forks in the DS bundle; moving the DTCG stamp off the root `$extensions` for strict third-party importers (a follow-up, not a move) |
| Consequences | Seven steps: E1 group metadata (big) → E2 controls meta (small, blocked until #566 merges) → E3 DS prime parity gate, E4 binder line + consumer prose, E5 shadcn chart-6..8 (all small, parallel) → E6 schema stamp (small, last code step, with a remove-the-constant negative control) → E7 docs of record (small). New gates named: `hpg-export-group-metadata`, `hpg-export-json-meta`, extended `hpg-export-design-system-prime`, `hpg-export-shadcn-chart-6-8`, `hpg-export-schema-stamp`. Checklist reads INCOMPLETE as written: every step's Status field still says "todo" although in-text corrections record E1 shipped (#578 note: the Group column landed only in Figma Make's grammar table) and E2 shipped as PR #593 (#594 name correction); the status field was never flipped. Living-state note: on completion status flips, learnings promote to knowledge-04 and `adding-export-formats`, file moves to `docs/plan/archive/`. Issues named: #569 (charter), #503, #533, #540, #554, #556, #557, #559, #566, #578, #593, #594. No ADR ids cited. |
| Supersedes / amended by | none stated; amended in place by corrections dated 2026-09-11 on #578 (audit row for MCP `$schema`; E1 Group column scope) and #594 (`baseChroma` field name in RP-2) |
| Source | docs/plan/plan-2026-09-export-schema-revision.md |
