# Verdict pif-u8 · #681 preset-intent-fidelity U8 at `14d9604d` (base `505416d7`)

Seat: verifier, opus high under owner ruling R17 (Fable allowance spent). My own evidence, taken in throwaway `git clone --shared` clones in the session scratchpad (`v8` for gates, `v8b` for controls, `blastctl` for the blast pair). The worktree was not touched. Written to the scratchpad, not the worktree, per dispatch; the orchestrator copies it into `.sdlc/verdicts/` if it keeps it.

## Overall: 🟡 pass with attention (4 🟢, 2 🟡, 0 🔴)

Code and gates are 🟢. The fix bites on four fresh swaps, and the same swaps at `505416d7` headline `pass`, so it is red-then-green. Two items need an orchestrator decision; neither needs builder rework:

1. Commit `.sdlc/verdicts/pif-u8-review.md` so the `pif-u8.md` §7 citation resolves in the tree (U8-3).
2. Rule whether the pre-review amend `a1ad77da` to `d8dd6bdb` counts against "no amend". If it does, the handoff should disclose it (U8-6).

## Rows

| # | State | Command | Evidence (verbatim output) | Negative control |
|---|---|---|---|---|
| U8-1 | 🟢 | `npm test` in `v8` at 14d9604d, then `git status --short` | `✓ all 49 test files passed` · `npm test  332.28s user 4.06s system 104% cpu 5:23.38 total` · exit=0. 49 `▶` lines in the log. `git status --short` printed 0 lines | In `v8b` at 14d9604d, swapped `DUPE_ALLOW` line 351 (Black metal) for the P-Funk entry, full `npm test`: `▶ engine/anchor.mjs        FAIL` · `✗ 1/49 test file(s) failed` · exit=1. Reverted with `git checkout`; status 0 lines after |
| U8-2 | 🟢 | Four swaps, each on the LAST entry of its list (the builder used `ORDER_ALLOW[0]` and `SYM_BY_CONSTRUCTION_ALLOW[0]`; the reviewer used `DUPE_ALLOW[0]` and `SYM_MEASURED_ALLOW[0]`). S1: `ORDER_ALLOW` Tórshavn hex `#221913` to `#221914`. S2: `DUPE_ALLOW` Black metal replaced by the real former member P-Funk. S3: `SYM_BY_CONSTRUCTION_ALLOW` Tórshavn, same hex change. S4: `SYM_MEASURED_ALLOW` Tórshavn replaced by the real by-construction-only member The rave. Each run was `node test/engine/anchor.mjs` or `node test/engine/prime.mjs`, then reverted | Unmodified: `  pass  anchor-ladder order-allow-list: 26 (expected 26)` · `  pass  anchor-ladder dupe-allow-list: 3 (expected 3)` · `  pass  symmetry corpus by-construction: 26 of 3380 (expected 26)` · `  pass  symmetry corpus measured-pixel: 22 of 3380 (expected 22)`, all exit=0. Swapped: `  FAIL  anchor-ladder order-allow-list: 26 (expected 26)` exit=1 · `  FAIL  anchor-ladder dupe-allow-list: 3 (expected 3)` exit=1 · `  FAIL  symmetry corpus by-construction: 26 of 3380 (expected 26)` exit=1 · `  FAIL  symmetry corpus measured-pixel: 22 of 3380 (expected 22)` exit=1 | Same four swaps at 505416d7 (target lines 346, 351, 1042 and 1069 byte-identical there): `  pass  anchor-ladder order-allow-list: 26 (expected 26)` exit=1 · `  pass  anchor-ladder dupe-allow-list: 3 (expected 3)` exit=1 · `  pass  symmetry corpus by-construction: 26 of 3380 (expected 26)` exit=1 · `  pass  symmetry corpus measured-pixel: 22 of 3380 (expected 22)` exit=1. Before the fix, the headline contradicted the exit code |
| U8-3 | 🟡 | (a) `git diff 505416d7..14d9604d \| grep '^+' \| grep -v '^+++' \| grep -c 'scratchpad/u7rev'`. (b) Extracted all 126 path-shaped tokens from the 499 added lines and checked each with `git cat-file -e 14d9604d:<path>`. (c) `blast.mjs` and `ko.mjs` run per the README, with ub `de1bafef` and neg `285f66ec` | (a) `0`. (b) Every real in-tree citation resolves (`.sdlc/records/pif-u7-blast/*`, both gate logs, `pif-u7-review-{1,2}.md`, `pif-u4.md`, `src/engine/tonal.js`, `test/engine/*` and the rest). The remaining misses are explained: relative test-log names resolve under `test/` (checked), some are branch names or prose, `.git-worktrees/lane-a-notes/*` is declared local and gitignored, and `.sdlc/handoffs/pif-preland-fixes.md` is declared never written. One citation does not resolve: `.sdlc/verdicts/pif-u8-review.md` is cited in the `pif-u8.md` §7 header, and in the rework brief, but it is untracked at 14d9604d. (c) `{ total: 344, neutralFlip: 148, primaryMoved: 14, relMoved: 339 }` · `default Relative extend: [0.593,0.206,289.000] -> [0.504,0.187,288.988]` · `architecture/Bankside / Tate Modern · 1947,: primary idx 2 -> 1` · `architecture/SoHo cast-iron loft · 1880s · : primary idx 5 -> 3`, exit=0, identical to the README. `default kit: max \|keyOklch - hexToOklch(key)\| 0.00e+0 palettes 16`, exit=0 | (a) The same grep on removed lines (`^-`) prints `7`, so the needle is findable. (b) The checker printed `MISS .sdlc/handoffs/pif-preland-fixes.md` for a path known not to exist. (c) With ub and neg both at 285f66ec: `{ total: 344, neutralFlip: 0, primaryMoved: 0, relMoved: 0 }`, so the figures come from the diff. The row is 🟡 only for the untracked `pif-u8-review.md` citation |
| U8-4 | 🟢 | `grep -c identity-control scripts/report-preset-fidelity.mjs` · `node test/engine/anchor.mjs \| grep -F dupe-allow-list:` · `grep -c "chromaEnvelope(" src/engine/tonal.js` · `grep -c "export function chromaEnvelope(" src/engine/tonal.js` | `0` · `  pass  anchor-ladder dupe-allow-list: 3 (expected 3)` · `5` · `1`. Sites at `:388` (the definition), `:749`, `:830`, `:1036` and `:1092`, as the plan's C7 names them | C4: a copy with one `--identity-control` line prepended prints `1`. C5: `DUPE_ALLOW` at `4125d965^` holds 4 entries, and the S2 swap reads `FAIL`, so the figure tracks the list. C7: a tonal.js copy with one extra `export function chromaEnvelope(x) { return chromaEnvelope(x); }` prints `6` and `2` |
| U8-5 | 🟢 | Em dashes on added lines, backtick spans stripped with `perl -pe 's/\x60[^\x60]*\x60//g'`, per commit and overall; `git diff --word-diff=porcelain` on the plan; `node test/repo/branding.mjs` | `505416d7..d8dd6bdb stripped_added=1 raw_added=1 raw_removed=2` · `d8dd6bdb..14d9604d stripped_added=0 raw_added=0 raw_removed=0`. The one hit is the carried "ratios explode near 0" clause on plan line 212, part of a whole-line replace. The word diff shows the only changed dash is the removed `-pass) —`, so no dash was added (net -1). `branding: clean (573 files scanned)`, exit=0 | Strip control: a test line with a dash in prose and another inside a backtick span counts `1`. Branding control: added `.sdlc/handoffs/zz-ctl.md` holding the retired domain, and the gate printed `  ✗ .sdlc/handoffs/zz-ctl.md: contains a <retired domain> URL or email` (altered: retired name removed) · `FAIL: 1 branding violation(s) across 574 files`, exit=1. After removing the file: clean, exit=0, status 0 lines |
| U8-6 | 🟡 | `git log --format='%h %p %s' 505416d7..14d9604d` · trailer grep · `git diff --name-only 505416d7..14d9604d \| grep -c '^.sdlc/board.md$'` · `git reflog show unit/pif-u8` | `14d9604d d8dd6bdb fix(records): U8 rework 1, fix-first record findings F1 to F5 (#681 U8)` · `d8dd6bdb 505416d7 fix(records): U8 adopts the orphaned pre-land pass-2 fixes (#681 U8)`. Exact trailer `Co-Authored-By: Claude Opus 5.5 (1M context) <noreply@anthropic.com>`: `1` in d8dd6bdb (and 1 in 14d9604d). board.md: `0`. Reflog: `d8dd6bdb ... commit (amend)` over `a1ad77da ... commit`, 14:46:20 then 14:46:38 | board.md control: the same grep over `505416d7..70c2d5e5` prints `1`. Why 🟡: 14d9604d is a fresh commit, so the rework was not amended. But d8dd6bdb is itself an amend of a1ad77da, made 18 s later: a one-line handoff edit filling the U8-6 row's `<filled in after commit, see below>` placeholder. a1ad77da is cited nowhere in `.sdlc/`, and the handoff does not disclose the amend |

## Orchestrator ruling on the two 🟡 (2026-09-22)

- U8-3: the review record `.sdlc/verdicts/pif-u8-review.md` (PASS at pass 2) is committed in the same commit as this verdict, so the `pif-u8.md` §7 citation now resolves.
- U8-6: the builder's brief (`U8-6` in the dispatch) asked for one commit with the given subject and trailer. It did not forbid an amend; "no amend" was a rule for rework 1 only, and rework 1 obeyed it. `a1ad77da` was never pushed, reviewed, or cited: 18 s after making it, the builder amended it to fill its own sha placeholder. The ruling: this is not a defect against U8-6. This paragraph is the disclosure the handoff lacked. The verifier regrades both rows against this commit.

## Regrade at 96f7d99a (verifier-l2, opus high under R17)

| # | State | Command | Evidence (verbatim output) | Negative control |
|---|---|---|---|---|
| Scope | 🟢 | `git log --format='%h %p %s' 14d9604d..96f7d99a` · `git diff --name-status 14d9604d..96f7d99a` · `git diff --name-only 14d9604d..96f7d99a \| grep -vc '^\.sdlc/verdicts/'` | `96f7d99a 14d9604d sdlc(pif): U8 review record and verdict, orchestrator ruling on the two yellows (#681 U8)` · `A	.sdlc/verdicts/pif-u8-review.md` · `A	.sdlc/verdicts/pif-u8.md` · `0` | The same count over `505416d7..14d9604d` prints `12`, so the filter does report files outside `.sdlc/verdicts/` |
| U8-3 | 🟢 | `git cat-file -e 96f7d99a:.sdlc/verdicts/pif-u8-review.md && echo "review resolves"` · `git grep -n "pif-u8-review.md" 96f7d99a -- .sdlc/handoffs/pif-u8.md` · `git show 96f7d99a:.sdlc/verdicts/pif-u8-review.md \| cmp - <worktree copy>` | `review resolves` · `96f7d99a:.sdlc/handoffs/pif-u8.md:81:## 7. Rework 1, records only (\`.sdlc/verdicts/pif-u8-review.md\`, 🟡 FIX-FIRST)` (altered: inner backticks escaped) · cmp exit 0, so the committed copy is byte-identical to the reviewer's record the U8 review was graded from | At `14d9604d` the same `git cat-file -e` prints `fatal: path '.sdlc/verdicts/pif-u8-review.md' exists on disk, but not in '14d9604d'`, exit 128, so the check fails when the file is absent. The rest of U8-3 (needle 0, the other paths, blast/ko figures) is unchanged from the `14d9604d` grade, because `96f7d99a` adds only these two files |
| U8-6 | 🟢 (against the ruling) | Read the "Orchestrator ruling on the two 🟡" section in `.sdlc/verdicts/pif-u8.md`. `git grep -n -i -E "no amend\|not amend\|do not amend" 96f7d99a -- '.sdlc/handoffs/pif-u8*' .sdlc/plans/preset-intent-fidelity.md` · `git branch -r --contains a1ad77da \| wc -l` · `git grep -c a1ad77da 96f7d99a -- .sdlc` | The only committed no-amend rule is the rework's: `96f7d99a:.sdlc/handoffs/pif-u8.md:83:New commit, no amend, per dispatch.` (and the rework brief's "Do not amend"). `14d9604d` is a fresh commit on `d8dd6bdb`, so that rule held. Remote branches containing `a1ad77da`: `0`. Records citing `a1ad77da`: `96f7d99a:.sdlc/verdicts/pif-u8.md:3`, which is my verdict and the ruling and nothing else. The ruling's paragraph is the disclosure the handoff lacked. Subject, trailer and board.md facts are unchanged from the `14d9604d` grade | The same sha grep for `d8dd6bdb` finds `96f7d99a:.sdlc/verdicts/pif-u8-review.md:7`, so the grep finds a sha that records do cite. Limit: `505416d7` is also on no remote branch, so the zero for `a1ad77da` confirms only that no remote ref carries it. It cannot prove it was never pushed. The original U8 dispatch text is not in the tree, so "the brief did not forbid an amend" rests on the orchestrator's word. The committed records are consistent with it |
