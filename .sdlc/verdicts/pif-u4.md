# Verdict U4 pass 5 (delta) · 🟡

Plan preset-intent-fidelity (#681), unit U4, head `3921f14059ccf8730521c6cffdb9bf1ac3a65da0` on `unit/pif-u4-integration`. Prior verdict at `36ce7777`: `scratchpad/verdict-pif-u4.md`. Verified 2026-09-20, read-only, worktree `.git-worktrees/pif-u4-integration`.

Next action for the Orchestrator: no red remains. Three yellows stay open as owner or follow-up items (rows 13, 20, 25), and three small record lines are stale (Records table). None blocks the unit; row 22 is an owner item by the report-only ruling.

Scope finding: the pass is NOT records-only. `git diff --stat 36ce7777 3921f140` shows 6 files, 4 under `.sdlc/` and 2 tests. `test/engine/semantic.mjs` moves six `FLOORS.even` pins up one decimal (test logic). `test/engine/anchor.mjs` changes a comment and the final PASS string only. No `src/` change, so the engine rows carry forward. I re-ran row 18 fully and ran `npm test` once.

| # | Criterion | State | Evidence | Negative control |
|---|---|---|---|---|
| 1, 4 to 12, 14 to 17, 19, 23, 24, 26, 27 | Unchanged rows | 🟢 | Carried forward by reference from `verdict-pif-u4.md`; `git diff 36ce7777 3921f140 -- src scripts figma mcp` is empty, so every engine measurement stands | As recorded in the prior verdict, row by row |
| 2 | C1 `npm test` exit 0, tree clean | 🟢 | Re-run at `3921f140`: `✓ all 48 test files passed`, `exit 0`, `git status --short` 0 lines after | Prior role table mutant (exit 1) still applies; this pass the floor mutant in row 18 reds the same file |
| 3 | Gate wall time and load | 🟢 | `real 345.89`, load at start `3.14 4.02 4.95`, at end `3.18 4.47 4.93`, with my own envelope, floors and cap sweeps running beside it, so this is an upper reading. A pid at 0% CPU does not hold timings; these loads are from `uptime` at start and end. Handoff section 3 now leads with 293.09 s and keeps 692.42 s as historical | Prior control (load 8.59, about 9 min) shows wall time tracks load |
| 18 | 41 floors and the re-pins, re-run in full because six pins moved | 🟢 | Own 96 cell sweep at `3921f140` is byte identical to the `36ce7777` sweep (`cmp` same). All 16 `FLOORS.even` rows: comment matches my 4dp measure within 0.0005 and floor equals floor(measured, 1dp): `even rows 16 mismatch 0`. Six new pins confirmed: Secondary light 5.6 (5.6078), Success light 7.7 (7.7219), Warning dark 5.3 (5.3334), Data 1 light 6.3 (6.3149), Data 3 light 6.5 (6.5224), Data 5 light 5.8 (5.8350). Gate line: `0 unlisted drops, 0 further erosion (41 cells named "pending U4")` | Secondary light pin raised to 5.7 in a scratch copy: `FAIL  role-contrast  ... even Secondary LIGHT: accent #42715B on #FFFFFF = 5.61:1, below its pinned floor 5.7:1`, exit 1; clean copy exit 0 |
| 21 | Yielded cells report the C6 p90 chroma ratio and reproduce | 🟢 | Handoff 7.3 now strikes the dL* table and reports 146.2% (perceptual 300) and 111.1% (peak 700) against the yielded 93.7 and 96.8. Mine, own pixel path (`projectView(hydrate(preset))`, own CAM16 ratio, n 2,920): perceptual `300: med 94.0 p90 146.2`, peak `700: med 68.8 p90 111.1`. The script agrees to the digit | Anchors stripped on the same head: perceptual 300 p90 97.9, peak 700 p90 69.3, so the probe moves when the construction moves |
| 22 | C6 median and p90 table, before and after, 14 of 24 stated with mechanism | 🟢 | Handoff 7.6 has both tables and says plainly that the rendered path misses 14 of 24, mechanism named (anchored stop 500 is the pinned sample, not the designed peak; Q7 and #701 cover only the above 100% clause), marked an owner item. Reproduced: `node scripts/report-preset-fidelity.mjs --envelope` exit 1, READING (a) FAIL tokens on median and p90 lines: perceptual 6, peak 5, even 3, total 14. Every after cell matches the script output to 0.1. Before table reproduced from my own scratch copy with `anchor: pal.anchor` removed at line 99: all 12 cells match to 0.1, 2 of 24 fail | The stripped copy reads 2 of 24 where the shipped path reads 14 of 24, so the count discriminates |
| 28 | Zero em dashes added in prose | 🟢 | Unit diff `31939051..3921f140`, added lines only, inline backtick spans stripped, occurrences counted with perl: 0. Without the strip: 0. Commit messages in the range: 0. The 4 prior sites are replaced with hyphens | Same counter on `31939051..36ce7777` prints 4, the prior finding |
| 13 | C4 ramp half | 🟡 | Now written down in handoff pass 5 section: 3,780 of 3,780 palettes per mode, max dL* 2.5961, 4.3634, 0.4289, matching my 2.60, 4.36, 0.43. Still no gate and no `--identity-control` ramp mode, and the follow-up the record names has no ticket number | My row 17 blast figures differ from these, so the differ is not echoing constants |
| 20 | Peak-cap trade figures at U4 | 🟡 | The builder's gap statement is honest on the facts: `targetTone` and `preCapOklchHue` are locals in `okhslStops` (`tonal.js:1203`, `1209`) and `paletteStops` does not return them. The conclusion is too strong: no engine change is needed to grade it. I hooked a scratch copy of `src/` (one push after the polish at `tonal.js:1264`, worktree untouched). Shipped path, peak, 3,780 palettes: `capped n 702`, tone error over 0.01 L* on `249 35.5%`, max `0.0848`, hue residual max `24.6211` deg, gap below anchor max `1.6578` C. Anchors stripped: n 1,412, `23.6%`, max 0.0945, 24.6211 deg. The plan's carried 35%, 0.10 and 24.6 deg hold at U4; the anchored path has no cap, so the trade applies only to non anchored palettes. My n differs from U3's 5,814 (scope of that script unknown to me), so this is not a digit reconciliation. Yellow because the U4 record still carries no final numbers | Hook count with anchors kept (702) against stripped (1,412) differs, and an earlier run with a mis-passed flag gave three identical outputs, which I caught and re-ran |
| 25 | Default kit in every sweep | 🟡 | Recorded in `.sdlc/handoffs/pif-u4.md` with today's clean measure (0 on window, gap, distinct, notch, order, dupe), matching my prior sweep. Still asserted by comment, not by a gate; no ticket named | Prior sweep finds the kit's Data 7 spike, so it visits the kit |

Counts: 25 🟢, 3 🟡, 0 🔴.

## Records

| Record | Matches head? | Detail |
|---|---|---|
| Handoff frontmatter `head:` | 🟢 | Now says to resolve from git; `git log -1 --format=%H unit/pif-u4-integration` prints `3921f140...`, the head |
| Gate-time ruling | 🟢 | Verbatim owner ruling dated 2026-09-20 in `.sdlc/questions/pif-u4.md` and handoff section 3, citing #713; `gh issue view 713` is OPEN, title on the 296 s split. U5 is named as the writer of the interim ceiling |
| `FLOORS` even comments | 🟢 | Row 18: 16 of 16 reproduce |
| Timing lead line, profile file count | 🟢 | Leads with 293.09 s; profile record says 48 files in all three places |
| `anchor.mjs` closing lines | 🟢 | Now `C4 (non-anchored construction totally migrated, Q1)` and `notch (15, Q3-resolved)` |
| Item 20 sample wording | 🟢 | Marked superseded by item 21 |
| Handoff pass 5 scope sentence | 🟡 | Says no source change is in scope except the em dashes, and the commit subject says records-only, but six `FLOORS.even` pins moved in `test/engine/semantic.mjs`. The handoff never lists the six re-pins |
| Handoff 41 cell table, `even Success light` | 🟡 | Still `8.0 \| 7.6`; the pin is now 7.7 (measured 7.7219) |
| Handoff 7.6 `cells failing` column | 🟡 | After table says perceptual 7 of 8 and peak 3 of 8; the readings are 6 and 5 (even 3 is right). Before table says 1 of 8 where 2 readings fail. The 14 of 24 and 2 of 24 totals in the prose are correct |
| Row 20 wording | 🟡 | Says reproduction needs an engine change; a scratch copy hook does it (row 20) |

Evidence files: `scratchpad/vu4-p5-npmtest.log`, `scratchpad/vu4/` (envelope-p5.log, rpf-strip.mjs, floors-p5.json, capdrift.mjs, inst/, t/clean.log, t/mut.log).


# Pass 7 (delta 3921f140..67d4df96, main merged in), graded by verifier-l2 because the l3 seat hit its session limit

# Verdict U4 pass 7 (main-merge delta) · 🟡

**verifier-l2 graded this pass because the verifier-l3 seat hit its session limit partway through pass 6.** Started fresh; nothing the dead seat left was read or trusted. Removal of its scratch directory was **refused by the permission system** (`rm -rf /tmp/pif-u4-p6` denied twice, once alone and once compounded), so that directory was left untouched and unread and every run below happened under `/tmp/pif-u4-p7` instead. Someone with the permission needs to remove it by exact name.

Plan `preset-intent-fidelity` (#681), unit U4, head `67d4df964b138ae114852eaf3b0aac1e876188c7` on `unit/pif-u4-integration`. Prior verdict at `3921f140`: `.sdlc/verdicts/pif-u4.md` on `plan/preset-intent-fidelity` (25 🟢, 3 🟡, 0 🔴). Verified 2026-09-20, read-only on the repo. The branch worktree `.git-worktrees/pif-u4-integration` was never touched; work ran in four detached scratch worktrees under `/tmp/pif-u4-p7` (`head`, `replay`, `ctl`, `mainwt`), removed at the end.

**Next action for the Orchestrator:** no red. The one substantive finding is row 7: the handoff's re-pin ledger says "every re-pin below is listed individually" and names 8 citation re-pins plus 1 fixture, but **40** citations across 8 corpus documents sit at values present in neither parent, and there is a **tenth fixture re-pin** (`test/engine/fixtures/shadcn-baseline.css`'s schema stamp) the ledger never names. Every one of the 40 and the tenth reproduces and a gate covers it, so this is a completeness gap in the record, not a correctness one. Rows 13, 18 and 19 are records items; rows 13, 20 and 25 of the prior verdict stay open unchanged.

## Method

The merge was **replayed from scratch** rather than read: a detached worktree at `3921f140` ran `git merge --no-commit --no-ff 3ce50daa`, producing **17 conflicted paths**. Head was then compared against that replay path by path. Outside the 17 conflicts, head deviates from a clean mechanical merge in **exactly 4 files**, each accounted for below. Inside the 17, every conflict hunk was extracted and classified, and the source-bearing resolutions were read by hand.

| # | Criterion | State | Evidence | Negative control |
|---|---|---|---|---|
| 1 | The merge is honest: the delta outside what main brought is exactly the conflict resolutions plus the re-pins | 🟢 | `git diff --name-only 3921f140 67d4df96` = **111** files. **81** are byte-identical to `3ce50daa` (main brought them wholesale). **17** are the replayed merge's conflicted paths. Outside those 17, head differs from my replayed tree in exactly 4 files: `.sdlc/handoffs/pif-u4.md` (the `bf2955f4` ledger commit), `docs/reference/rubrics/acceptance-criteria.md` (re-pins 7 and 8), `test/engine/exports.mjs` (the re-capture comment plus `67d4df96`'s one character), `test/engine/fixtures/radix-baseline.json` (the fixture re-pin). Nothing else. The only delta file main never touched is the handoff | Same path-by-path predicate with `3921f140` substituted for head over the same 39-file main range prints `39 differ, 0 equal`; against head it prints `2 differ, 37 equal`. The predicate separates the two trees rather than reporting equality by construction |
| 2 | `.sdlc/adapter.md` carries main's X1 to X13 ids plus U6's `npm test` budget bullet, no `C<n>` conflict ids remain | 🟢 | `grep -coE '^\| X[0-9]+ \|'` on head's adapter = **13** (X1 through X13, all present); `grep -coE '^\| C[0-9]+ \|'` = **0**. Nine `C<n>` strings survive in prose and are survey claim ids, which the file's own 2026-09-19 amendment states explicitly (`Every C<n> still in this file is a survey claim id`). The U6 bullet is folded onto main's newer row, not dropped: the `test` row reads `56 to 60 s baseline; **revised #681 U6 (review pass 6 N9)**: 90 to 175 s wall observed, host-contention-sensitive` on top of main's Node 24 and `TESTS`-length wording | The identical greps against `3921f140`'s pre-merge adapter print `X rows 0, C rows 13`, the exact inverse, so the greps read the id scheme rather than matching anything |
| 3 | Each of the eight citation re-pins reproduces from the engine at this head | 🟢 | Reproduced with my own `sed -n` against head's tree, never from the handoff. `app.js:1594` = `segmented(items, value, onSelect, opts = {}) {`; `app.js:1610` = the `"aria-pressed": tabs ? undefined : ...` line; `app.js:2319` = `saveToProject() {`; `app.js:2349` = `applyLoadedConfig(config) {`; `app.js:2108` = `_bindRangeDrag() {`; `app.js:2144` = `const end = () => {`; `tonal.mjs:283` = the `hpg-tonal-okhsl-modes` header; `tonal.mjs:687` = the `hpg-tonal-lift-monotonic (#648)` header. The un-repinned half of the `1466/1610` pair also holds: `app.js:1466` = `"aria-pressed": shown ? "true" : "false",`. `node test/repo/citations.mjs` at head: `STALE 0 across 10 discovered docs`, and all six citing documents are in the discovered set, none exempt | Nudging `component-inventory.md:44` from `app.js:1594` to `app.js:1600` in a scratch copy reds the gate: `verdict STALE-WRONG-LINE`, `exit 1`; restored copy `exit 0`. The gate carries a small anchor window, so `1595` still reads `OK` and `exit 0`, which is why my own `sed` reads above are the primary evidence and the gate is the corroborator |
| 4 | Each re-pin's old value is what `3921f140` or main pinned, and the stated mechanism holds | 🟢 | Re-pins 3 to 8 had identical old values on both parents (`app.js:2290-2313`, `2320-2345`, `2096-2142`, `2132-2136`, `tonal.mjs:246-264`, `577-601`), so they are unambiguous. Re-pins 1 and 2 differed per parent: main pinned `1587` and `1466/1603`, the unit pinned `1586` and `1465/1602`; the handoff quotes main's, which is a real parent value. Mechanism confirmed by measurement: `src/ui/app.js` is 2,589 lines at `3921f140`, 2,579 at main, 2,590 at head; `test/engine/tonal.mjs` is 1,891 at `3921f140`, 909 at main, 1,899 at head. Both files grew by different amounts on the two sides, exactly as the ledger states | Row 3's `1600` mutant, plus the fact that the two parents disagree on re-pins 1 and 2 at all: if line numbers had not moved independently per side those two would not have conflicted |
| 5 | The embedded radix re-capture script reproduces the committed fixture byte for byte | 🟢 | The script block was copied verbatim out of the handoff into `/tmp/pif-u4-p7/capture-radix-baseline.mjs` and run against a clean scratch worktree at head. `cmp` against the committed `test/engine/fixtures/radix-baseline.json` (134,104 bytes): **byte-identical**, `git status --short` empty afterwards. Its construction does mirror `test/engine/exports.mjs:24-46` (`C`, `ALL`, `BRAND_ONLY`, `RADIX_MK`, `RADIX_COLLIDING` all match verbatim) | Flipping one digit of one value in the fixture (`cmp` reports `char 141`) reds the gate: `FAIL radix-refs-values-unchanged - exportRadix(ALL) drifted from the pre-#638 values-form baseline`, `exit 1`; restored `exit 0` |
| 6 | The fixture differs from main's for a reason the #681 engine explains | 🟢 | Own leaf walk, main's fixture against head's: **2,763 leaves, 1,724 moved, 1,039 unchanged, 0 key/shape/ordering diffs**, matching the ledger's own figures. Every one of the 1,724 moved leaves is an `oklch(...)` string replaced by another `oklch(...)` string, **zero** name, key, alias or non-color leaves moved; 1,690 of the 1,724 have a changed hue coordinate, which is what #681's anchor and hue work does. Independent corroboration that the move is engine-caused and not a capture artifact: the same script run against a scratch worktree at `3ce50daa` reproduces **main's** fixture on main's engine (`ALL / BRAND_ONLY / COLLIDING` all `MATCH`, lengths `61661 / 34468 / 38182`) | The classification is not vacuous: it partitions 2,763 leaves into 1,724 moved and 1,039 unchanged rather than declaring everything changed, and it would have named any non-color leaf that moved (the `non-oklch moves` list came back empty, from a printer that had 20 slots ready) |
| 7 | The re-pin ledger is complete per owner ruling R2 (a re-pin is accepted only when reproduced and its mechanism cited) | 🟡 | The ledger opens `Every re-pin below is listed individually - name, old value, new value, mechanism` and lists 8 citations plus 1 fixture. Counting every `file:line` citation whose value at head is present in **neither** parent gives **57** across 9 documents; subtracting the 17 inside the handoff's own new ledger prose leaves **40** genuine record re-pins: `04-context-and-messaging.md` 17, `app-shell.md` 6, `component-inventory.md` 4, `02-sections-and-resolvers.md` 4, `03-stores-and-persistence.md` 4, `SKILL.md` 2, `acceptance-criteria.md` 2, `00-synthesis.md` 1. Thirty-two of them sit inside conflict resolutions, which the handoff classes as verified-and-left-untouched rather than enumerating. Separately there is a **tenth re-pin** the ledger never names: `test/engine/fixtures/shadcn-baseline.css`'s three `export schema 2` lines moved to `3` (row 8). Two smaller record nits: the re-capture drops main's trailing newline on `radix-baseline.json` (main 1 newline, head 0) and the ledger's structural account does not mention it; and re-pin 8's `Verify: sed -n '691p'` names 691 where the anchor the citation `687-708` opens on is at 687. **All 40 reproduce and gates cover them**, so this is a record completeness gap, not a correctness one | Spot-checked seven un-enumerated re-pins by hand at head, all landing on their named anchor: `tonal.js:907` = `const _okL = new Map();`, `app.js:1641` = `renderCenter(view) {`, `app.js:1531` = `renderLeftPane(view) {`, `app.js:1927` = `renderRightPane(view) {`, `app.js:2332` = `loadFromProject() {`, `app.js:2356` = `this._loadRequested = false;`, `model.mjs:53` = `export function geometryScale(doc, opts = {}) {` |
| 8 | The tenth re-pin (shadcn schema stamp) reproduces and its mechanism holds | 🟢 | `EXPORT_SCHEMA_VERSION` is **2** at `3921f140` and **3** at main and at head, so main's #638 bump is the cause. Head's `shadcn-baseline.css` carries `export schema 3` three times and `export schema 2` zero times. `git diff 3921f140 67d4df96` on that file is `+6 -4`, and filtering out the three stamp lines and the new `#638:` carve-out comment leaves **0** other changed lines. The re-pin is documented inline in the fixture, just not in the ledger | Reverting one stamp (line 70) to `2` in a scratch copy reds the gate: `FAIL shadcn-baseline - exportShadcn(ALL) drifted from the pre-refactor fixture`, `exit 1`; restored `exit 0` |
| 9 | Nothing main landed is lost: for files main touched that #681 does not own, head equals main | 🟢 | Main touched **39** files in `20298cc..3ce50daa`. **37** are outside the unit's own change set (`bf2aaf65..3921f140`), and head's blob equals `3ce50daa`'s blob for **all 37**, `0` differing. The 2 overlaps are `.sdlc/adapter.md` and `.sdlc/board.md`, both genuine conflicts resolved under rows 2 and 1 | Same predicate with `3921f140` in head's place prints `39 differ, 0 equal`, so it is not reporting equality by construction |
| 10 | Main's #638 reference-form feature survives inside the unit-owned files it conflicted in | 🟢 | This was the real drop risk: both `exports.js` hunks and `model.mjs`'s hunk pitted the unit's new `derived` argument against main's `refs` work. Head keeps both, threaded: `exports.js:1289` `export function exportRadix(state, opts = {}, derived)`, `:1290` `const palettes = derived \|\| derivedAll(state);`, `:1291` `const leaves = opts.refs ? radixRefLeaves(cssPrefixOf(state)) : RADIX_VALUE_LEAVES;`, `:1284` main's `opts.refs (#638)` doc comment, `:1394` `radixRef: exportRadix(state, { refs: true }, derived)`. `model.mjs:1053` keeps main's `radixRefPreset` **with** the `{ geometry: shadGeom, refs: true }` opt main's own comment warns must travel, plus the unit's `derived`, and `:1064` still emits `radixRef` | The twelve `radix-refs-*` gates in `test/engine/exports.mjs` all pass at head (`exit 0`); the row-5 one-byte mutant proves that file's gates fire rather than reporting `pass` unconditionally |
| 11 | Main's #699 `gateReport` conversion survives in every test file it conflicted in | 🟢 | `semantic.mjs`, `categories.mjs`, `prime.mjs` and `tonal.mjs` all take main's `gateReport({ fails, declared: DECLARED, selfUrl: import.meta.url, FAIL })` with a merged `DECLARED`. The one apparent loss is deliberate and documented in the resolution: `categories.mjs:611` states `"lift-anchor" (#648) is not in this list: #681 U2 retired it ... and "ramp-monotone" (#668) is its replacement gate`, and head's file carries zero `lift-anchor` call sites against main's 13 | `report-static` is precisely the gate that reds on a declared name with no call site or a call site with no declaration, and it passes in all four files inside the `npm test` run, so the merged `DECLARED` lists are checked rather than assumed |
| 12 | `npm test` once: exit 0, 49 files, tree clean after | 🟢 | One run in the scratch worktree at head: `✓ all 49 test files passed`, `EXIT 0`, and `git status --short` prints **0 lines** afterwards, so every regenerated asset (including `figma/plugin/ui.html` and `src/ui/describe-mcp-assets.js`, both conflicted and both generator output) is byte-stable. `repo/branding.mjs`, `repo/citations.mjs`, `repo/gate-report.mjs`, `engine/curated-contrast.mjs` all pass inside the same run | The row-5 fixture mutant reds `engine/exports.mjs` inside this same suite, and the row-8 stamp mutant reds it a second, independent way, so a green suite here is not a suite that cannot fail |
| 13 | Gate wall time and load | 🟡 | `real 780.23` (`user 665.63 sys 15.74`). Load from `uptime`: **9.37 / 15.12 / 14.88** at start, peaking at **62.87** mid-run on my own sampling, **26.60 / 33.30 / 25.96** at end. The `ps` sweep before the run found leftover `node` processes but every one sat at **0.0% CPU**, so none of them held these timings; the load came from live concurrent agents on this host. That is **4.5x** the adapter's revised 175 s ceiling and 1.8x the builder's own 430.46 s at load 5.18. Yellow because **one run under a load that peaked near 63 cannot separate contention from a merge-side slowdown**, in either direction. Every one of the 49 files passed, so nothing here suggests a regression; the ceiling itself is an open owner item under #713 with U5 named as the writer of the interim figure. A clean isolated re-run is the outstanding action | The builder's 430.46 s at load 5.18, the prior verdict's 345.89 s at load 3.14 and this 780.23 s at load 9.37-to-62.87 form a monotone series in load, which is what contention looks like and what a fixed regression would not |
| 14 | `node test/repo/branding.mjs` clean | 🟢 | Run standalone at head: `branding: clean (505 files scanned)`, `exit 0`. Also passes as `repo/branding.mjs` inside the `npm test` run | The gate is the repo's own paraphrase enforcement over `.sdlc/`, and it scans 505 files rather than reporting clean over an empty set |
| 15 | `npm run build` once, since the merge brought main's toolchain refresh | 🟢 | The repo root's `node_modules` turned out **stale** against the refreshed lockfile (installed `typescript 6.0.3` and `vite 8.0.16` against declared `~7.0.2` and `^8.3.0`), so it was not reused. `npm ci` in the scratch worktree: `added 18 packages`, `real 1.72`. `npm run build`: `exit 0`, `real 6.81`, zero error lines, `✓ built in 735ms`, `wrote figma/plugin/ui.html 4111.1 KB`, `git status --short` **0 lines** after, and `git diff --quiet -- figma/plugin/ui.html` passes, so the committed bundle is byte-exactly what the generator emits at head | Appending `const __ctl: number = "not a number";` to `src/main.ts` reds it: `error TS2322: Type 'string' is not assignable to type 'number'`, `exit 1`; restored `exit 0` |
| 16 | Zero em dashes added in prose versus both parents | 🟢 | Counted with `perl`, no `grep -P`, inline backtick spans stripped, occurrences counted per line rather than per matching line. The raw added-line counts are misleading and are reported as such: **88** against the unit parent and **661** against main, because a merge "adds" the other parent's existing prose. The discriminating measure is lines present at head and in **neither** parent: **154** such lines carrying **16** em dashes after stripping, and all **16** are on lines whose only difference from a parent line is digits (a moved citation number), leaving **0** em dashes in genuinely new prose. The three delta commit messages carry **0** each (the 1 that `git log 3921f140..67d4df96` reports belongs to one of main's own commits in the range) | The same counter on `31939051..36ce7777` still prints `added_after_backtick_strip=4`, reproducing the prior verdict's finding, and the digit-normalizing split reports `16` versus `0` rather than collapsing both to zero |
| 17 | Prior row 18 (the 41 floors and the six re-pins) is unmoved by this delta | 🟢 | The complete diff of `test/engine/semantic.mjs` across `3921f140..67d4df96` is two hunks: main's `gateReport` import and main's REPORT block. Direct comparison of every `FLOORS` and tone-mode line across the two refs returns identical, and the `even` pin block hashes the same on both (`009e00858d2e018f8434ca19307a3e61`). The merge adopted main's #699 report block and left every pin alone, so the prior verdict's own 96-cell sweep carries forward intact | The file is not untouched, so the comparison had something to find: it correctly reports the import and REPORT-block change while reporting the pins identical |
| 18 | Handoff record currency | 🟡 | `.sdlc/handoffs/pif-u4.md`'s frontmatter still reads `updated: 2026-09-20 (pass 5, records-only, verifier verdict pif-u4)` after the `bf2955f4` commit added the whole Main merge section. Its round history ends at pass 5, described as `this round, the tip as of this commit`, and the frontmatter asserts `This record's own commit is the LAST one in the round history below - nothing here is pending`. Three commits now sit after pass 5 (`9f6783fd`, `bf2955f4`, `67d4df96`) and none is in the history, so the record's own self-description is false on its own branch. The `head:` field itself is fine: it resolves from git by instruction | The same frontmatter's git-resolution instruction does hold (`git log -1 --format=%H unit/pif-u4-integration` prints the head), so the check distinguishes the stale field from the sound one rather than flagging the block wholesale |
| 19 | `sh .sdlc/checks/baseline-agrees-check.sh` | 🟡 (recorded, not graded) | Exit 1, `stale total: 3`, not 1. `STALE tests: baseline 48, test/run.mjs TESTS 49` is U5's, per the dispatch, and `test/run.mjs` does carry **49** entries. Two more: `STALE ui.html: baseline 3780.5 KB, tree 4111.1 KB`, which this merge caused and nobody has been named for; and `STALE head: baseline ref 20298cc ...`, which the script's own header declares expected on a later commit. The three `ok time` rows and the `ok head ... in origin/main's history` row pass. Not graded red per the dispatch | The check reports 3 stale against 4 ok rather than failing everything, and the `time` rows agree exactly, so it discriminates row by row |
| 20 | Prior rows 1, 4 to 12, 14 to 17, 19, 21 to 24, 26, 27 | 🟢 | Carried forward by reference from `.sdlc/verdicts/pif-u4.md`. The delta cannot move them: `git diff 3921f140 67d4df96 -- src scripts figma mcp` is non-empty only where main's own history and the row-10 resolutions land, and every engine measurement those rows rest on is re-confirmed by the green `npm test` at head plus rows 5, 6, 8, 10 and 11 above | As recorded in the prior verdict, row by row; rows 5, 6, 8, 10 and 15 above are this pass's fresh controls over the same engine surface |
| 21 | Prior rows 13, 20, 25 | 🟡 | Carried forward unchanged from `.sdlc/verdicts/pif-u4.md`. Nothing in this delta touches the C4 ramp half, the peak-cap trade figures, or the default-kit sweep, and none of the three has acquired a gate or a ticket number since | As recorded in the prior verdict |

**Counts this pass: 16 🟢, 5 🟡, 0 🔴** across 21 rows. Folding the carried groups out to the prior verdict's own granularity: **35 🟢, 5 🟡, 0 🔴** (prior: 25 🟢, 3 🟡, 0 🔴; the two new yellows are rows 7 and 18, and row 13 moved from 🟢 to 🟡 on the load reading while rows 19 and the prior three yellows account for the rest).

**Re-pins reproduced: 10 of 10** named or found (8 citations, the radix fixture, the shadcn schema stamp), plus **32** further citation re-pins inside conflict resolutions that the ledger does not enumerate, of which 7 were spot-checked by hand and all 40 are covered green by `test/repo/citations.mjs`. **None could not be reproduced.**
verdict: 🟡

## Evidence

Runs and artifacts under `/tmp/pif-u4-p7`: `npmtest.log` (the timed gate plus both `uptime` readings and the post-run `git status`), `build.log` and `build2.log` (`npm ci` plus `npm run build`), `build-mut.log` (the `tsc` control), `exp-clean.log` / `exp-mut.log` / `shad-mut.log` (the two fixture controls), `capture-radix-baseline.mjs` (the handoff's script, copied verbatim), `committed-radix.json` and `main-radix.json`, `hunks.txt` (all 34 conflict hunks with both sides), `conflicted.txt`, `checkres.mjs`, `hunks.mjs`, `novel.mjs`, `novel2.mjs`, `repins.mjs`, `emdash.pl`, `adapter-head.md` and `adapter-pre.md`. Scratch worktrees `head`, `replay`, `ctl`, `mainwt` were removed after the run.
