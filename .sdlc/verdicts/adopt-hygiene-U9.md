# Verdict adopt-hygiene U9 · 🟢
verdict: 🟢

Graded by sdlc-verifier on 2026-09-17 (evidence run: adopt-hygiene-U9-verifier-l3-p1, grade l3, Fable 5.1; rows 1 and 3 regraded twice by the Verifier seat itself, at 7afd45a and again at f39a640). Branch `unit/hygiene-U9` @ 10072b4 (unchanged through both regrades), merge-base 5d4492d, graded against the `sdlc/adopt` plan copy at f39a640. Worktree left at status 0; controls in two detached scratch worktrees (4f30855 and a mutable copy of head), both removed; root checkout untouched. The handoff and `.sdlc/verdicts/adopt-hygiene-U9-review.md` were not used as evidence.
Tally: 16 rows. 🟢 14 · 🟡 2 · 🔴 0. All four pre-land gaps are closed in the tree, and at f39a640 both previously false plan cells now match what the commands actually print, measured by me at head and at the control. The two remaining 🟡 are record parity on the branch and the self-declared checker family, neither a unit defect.

| # | Criterion | State | Evidence | Negative control |
|---|---|---|---|---|
| U9-1 | OD-004 names the repointed record; every directory-qualified path any `.sdlc/records` file names resolves from the repo root or from `.sdlc/records/` | 🟢 | run by the Verifier seat at f39a640 wording, in the unit worktree: `0`, then exactly the three lines Expected names and no others: `.claude/ops/plan.md` (untracked by D1, and the index row says so), `data/role-table.json` and `figma-aliased/palette.tokens.json` (paths inside an exported zip). The earlier 41-line and 77-line results came from the pre-f39a640 wording | at 4f30855, run by me: `1` and a fourth line, `missing docs/spec/CHANGELOG.md` |
| U9-2 | `decisions.md` does not contradict itself on ADR-004 and gap G2 | 🟢 | `0` then `3`; line 21 now names ADR-023 and the G2 closure, matching `:40`, `:66`, `index.md:19`, and both cards | at 4f30855: `1`, `2`; planting the old sentence: `1` |
| U9-3 | no live record claims CI installs with `npm install`; `ci.yml` runs `npm ci` | 🟢 | run by the Verifier seat at f39a640 wording: no line at head, `npm ci` count 3, and `ci.yml:28` carries the "not install" comment. The `.` window plus the `ci.yml` alternative now does what the cell claims | at 4f30855, run by me: all three pre-fix lines print (`shipping-changes/SKILL.md:32`, `references/foundations.md:21`, `:71`). The earlier `[^\n]` form printed one, because `git grep -E` reads that class as the literal characters backslash and `n`, so `runs` and `workflows` ended the match |
| U9-4 | debt C6 lists every `.sdlc` home-path file the revised loop sees | 🟢 | no `missing` line; the loop sees `architecture.md`, `plans/adopt-hygiene.md`, `tickets/T-0001.md`, all in C6 | without the exclusions: `missing .sdlc/plans/adopt-hygiene-U8-p2.md`; a staged probe file with a home path: `missing .sdlc/records/zz-probe.md` |
| U9-5 | the U8 sweep checks stay green | 🟢 | all nine `u8check` lines equal the §U8 Expected values; wording check `0, 0` exit 0 from both origin/main and the merge base | planted full closure on G2: `false-close G2`; committed dash on README:56: `1, 0` |
| U9-6 | gates | 🟢 | `✓ all 44 test files passed`; status 0; `branding: clean (431 files scanned)` | role-table plant: 3 FAIL lines; restored, green |
| G1 | pre-land gap: U7-2 loop flagged the U8 re-diagnosis file | 🟢 | closed by the plan exclusions (row U9-4) | row U9-4 controls |
| G2 | pre-land gap: `OD-004.md:7` dead path | 🟢 | card now names the root `CHANGELOG.md`, which resolves; `docs/spec/CHANGELOG.md` does not; the source doc agrees | row U9-1 controls |
| G3 | pre-land gap: `decisions.md:21` | 🟢 | row U9-2; no other "only as a note" phrase remains except G2's own gap description, whose next cell records the closure | row U9-2 controls |
| G4 | pre-land gap: CI claim, plus a wider sweep | 🟢 | `SKILL.md:32` and `foundations.md:21` and `:71` fixed. Every remaining `npm install` in the live records is true at head (local installs, the MCP server, a pre-fix debt description whose note closes it, a quoted check). Workflow job names in the adapter and CLAUDE.md match `ci.yml`; the `html:` count matches; debt counts 7/7 and 45/45 match | 4f30855 values; planted claims per row U9-3 |
| D | debt rows G2, D2, C4, P1, R8 re-derived | 🟢 | each partial's closed half true and open half still open; live `[true,false,false]`, `delete_branch_on_merge` false, no branch protection; closure check `disagreeing: 0` | planted G2 full closure: `false-close G2` |
| C | no new contradiction across `decisions.md`, `index.md`, and the cards | 🟢 | the ADR-004, ADR-023, and G2 cells agree in all six places; OD-004 reads OPEN everywhere; status cells for ADR-013, 016, 020, the three plan cards, and PRD-0001 agree | the row U9-2 comparison separates old and new text |
| P | P1 to P5 | 🟢 | 44 pass and 0; 0; P3 against the merge base 7faf3aa: 0 (7 against origin/main literally, as the revised plan text says); branding clean 431; 0 deleted lines | motion.mjs probe 2; decision-records line deleted 1; `docs/x.md` copy FAIL 3; origin/main ops tree 7 |
| S | scope and hygiene | 🟢 | 5 files, none behind the wall; no em dash or bold label on added lines | wording check controls |
| R | branch plan copy matches `sdlc/adopt` | 🟡 | it does not: the branch lacks 102c969's P3 merge-base wording, the §U9 mention of `foundations.md`, the criterion 3 pattern, and the revision row. The branch never edited the file, so the merge keeps the `sdlc/adopt` text | file-level fact |
| F | checker family | 🟡 | Fable 5.1, fresh context, self-declared; builders Opus | nothing in the tree can falsify a model name |

## Notes
1. Two plan revisions were needed to make rows 1 and 3 true (7afd45a, then f39a640). Both are measured now; the pre-land rerun grades against f39a640.
2. 🟡 Merge `sdlc/adopt` into the branch (or merge the unit) so the branch stops carrying the pre-advisory plan copy.
3. 🟡 Checker family is self-declared, carried.
