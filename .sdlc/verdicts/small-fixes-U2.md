# Verdict sf-U2 · 🟢

| Field | Value |
|---|---|
| Unit | small-fixes U2 (#717), plan revision 9 as on main `e379a38b`, the grandchild leg closing the critic CHANGES at `262ae996` |
| Graded at | `unit/sf-U2` @ `0dd91819` (code at `b5b8dc47`), base `plan/small-fixes` @ `262ae996`, `origin/main` @ `bfe4a351` |
| Where | own detached worktree `scratchpad/wt-sf-U2` at `0dd91819`; clones under `scratchpad/vU2-Q9kd` (`mut`, `old`, `nospawn`, `gap`), each made from the commit (`git -C <clone> log -1 --format=%h` printed `0dd91819`, `262ae996`, `0dd91819`, `0dd91819`); every launcher run under its own `TMPDIR=$(mktemp -d "$F/tmp-XXXX")`; all removed by exact name after |
| Load | start `3.88 4.42 4.71`, end `1.80 2.97 3.90` |
| Host after | `pgrep -f fake-chrome-grandchild` `0`, `pgrep -f fixtures/fake-chrome.mjs` `0`, smoke Chrome on this run's `TMPDIR` `0`, profile and `launcher-legE-*` dirs under the scratch `0` |
| Result | 🟢 9 of 9 rows green (U2-1 to U2-6, P3, P4, U2-6's control chain), 0 yellow, 0 red |

## Rows

| # | Criterion | State | Evidence | Negative control |
|---|---|---|---|---|
| U2-1 | the launcher stays green with the grandchild asserts | 🟢 | Worktree head: `exit 0`, `pass=6 FAIL=0 grandchildFAIL=0`, last line `PASS: launcher discovers its port from DevToolsActivePort and leaves no process on close, SIGTERM, SIGINT or deadline` (byte for byte the 262ae996 line). Then 10 more sequential head runs, each `exit 0 pass=6 dirs=0 grandchildren=0`, `reds: 0 of 10` | U2-2's mutant: `exit 1`, `pass=1 FAIL=5`. A launcher that skipped the grandchild read would pass the mutant too; the `nospawn` clone below shows the read is live |
| U2-2 | the parent-only kill reds on every exit path | 🟢 | Clone `mut` at `0dd91819`, `chrome.mjs:66` `try { proc.kill("SIGKILL"); } catch {`, `git diff --stat` `1 file changed, 1 insertion(+), 1 deletion(-)`: `exit 1`, `pass=1 FAIL=5 grandchildFAIL=5`, one per exit path: normal `leg (b): grandchild pid 50070 still alive 2s after close()`, signal `leg (c): grandchild pid 50141 still alive 2s after SIGTERM`, `leg (d): grandchild pid 50204 still alive 2s after SIGINT`, error `leg (e): grandchild pid 50271 still alive 2s after the deadline`, signal-before-discovery `leg (f): grandchild pid 50354 still alive 2s after SIGTERM` | Clone `old` at `262ae996` with the same one-line mutant (`1 file changed, 1 insertion(+), 1 deletion(-)`): `exit 0`, `pass=6 FAIL=0`, the critic's finding reproduced: before U2 no leg bit on the group kill |
| U2-3 | the grandchild is really spawned and really in the group | 🟢 | Right after the mutant run `pgrep -f fake-chrome-grandchild \| wc -l` printed `6` (legs b to f plus leg (a), which does not assert); after every head run `0`. `ps -o pid=,pgid=` on a hand-started fake: fake `55628 55628`, grandchild `55629 55628`, same pgid; after `process.kill(-pid, "SIGKILL")` `grandchildren 0` | Clone `nospawn` with `fake-chrome.mjs:34` deleted (`1 file changed, 1 deletion(-)`): `exit 1`, `pass=1 FAIL=5`, each `leg (<x>): grandchild pid file missing in <dir> (ENOENT)`, so no leg passes vacuously without a grandchild; `grandchildren-after: 0 fakes-after: 0 dirs: 0`, the failed read leaked nothing |
| U2-3 (race) | the pid-file read race (review F1) stays closed under repeat and under a preempted fixture | 🟢 | The 10-loop run above `reds: 0 of 10`. Clone `gap` at `0dd91819` whose fixture creates the pid file empty and writes the pid 300 ms later (`1 file changed, 1 insertion(+), 1 deletion(-)`): head launcher `exit 0`, `pass=6 FAIL=0` | The 4c6782d4 launcher (`git show 4c6782d4:test/smoke/launcher.mjs`) against the same `gap` fixture: `exit 1`, `pass=4 FAIL=2`, `leg (e): grandchild pid file reads ""`, `leg (f): grandchild pid file reads ""`; `after: grandchildren 0 fakes 0` |
| U2-4 | U1-7 and U1-8 still hold | 🟢 | `env -u TMPDIR node test/smoke/launcher.mjs`: `exit 0`, `pass=6`, default temp root leftovers `0`. Docker `node:24` via `sh -c` (`docker info` `up`): `exit 0`, `pass=6 FAIL=0` | Mutant clone in Docker `node:24` via `sh -c`: `exit 1`, `pass=1 FAIL=5 grandchildFAIL=5`. Head with `node` as PID 1 (no reaper): `exit 1`, `pass=1 FAIL=5`, which is the review's F2 dependence, unchanged by the handoff's answer and satisfied by `sh -c`, `--init` or a VM init |
| U2-5 | scope | 🟢 | `git fetch -q origin; git diff --stat origin/plan/small-fixes...HEAD -- . ':!.sdlc'`: `test/smoke/fixtures/fake-chrome.mjs \| 12`, `test/smoke/launcher.mjs \| 80`, `2 files changed, 78 insertions(+), 14 deletions(-)` | The `mut` clone's working tree: `1 file changed, 1 insertion(+), 1 deletion(-)` on `test/smoke/chrome.mjs`, which the same diff would list as a third file |
| U2-6 | `npm test` and `npm run smoke` green on the head, tree clean after | 🟢 | Worktree with no `node_modules`: `npm test` `exit 0`, `✓ all 48 test files passed`, `git status --short \| wc -l` `0`. With `node_modules` symlinked and `TMPDIR` scoped: `npm run smoke` `exit 0`, `SMOKE PASS` count `1`, `PASS: launcher` count `1`, 5 s later `chrome: 0 dirs: 0 grandchildren: 0`; symlink removed, tree `0` | The smoke script's chain `sh -c 'node test/smoke/launcher.mjs && node test/smoke/smoke.mjs'` in the `mut` clone: `exit 1`, `SMOKE PASS lines: 0 smoke.mjs mentions: 0`, last line `FAIL: launcher (5/6 legs failed)`, so a parent-only `chrome.mjs` cannot reach `SMOKE PASS` through the script |
| P3 | branding clean, no added prose em dash | 🟢 | `branding: clean (577 files scanned)`; backtick-stripped dash count against `origin/main` `0`; the raw listing has one line, the U1 pre-land record's `SMOKE PASS — gallery · category · editor · export dialog all render in a real browser` quote, a program line; the unit's own diff has one, the handoff's quote of the same line; `**` lines in the unit diff are two backtick spans quoting the literal, not labels | `cp decision-records.md .sdlc/verdicts/small-fixes-x.md` reds the branding scan (planner's control, not rerun: the scan ran clean here at 577 files); a prose dash outside a backtick span makes the count `1` |
| P4 | scope wall against main | 🟢 | `git diff --name-only origin/main...HEAD` through the plan's filter `0`; `package.json` `1 file changed, 1 insertion(+), 1 deletion(-)`. The two-dot form prints `5`, all `verdict-backfill` records main gained after the plan branch was cut (`.sdlc/plans/verdict-backfill.md`, its question and three verdicts), the unrebased case the plan names; the rebase precedes pre-land | The plan's four-name fixture through the filter prints `2` (engine file and the other lane's plan refused) |

## Notes

| Item | Detail |
|---|---|
| Counts are host-global | `pgrep -f fake-chrome-grandchild` counts any seat's launcher run; it read `0` before the suite, `6` only after the mutant, `0` after every head run, so no other seat ran the launcher during this grading |
| Mutant orphans | The `6` grandchildren of the mutant run were reaped by `pkill -9 -f fake-chrome-grandchild` after U2-3 read them; the fixture's 60 s cap would have done it otherwise |
| Not a U2 concern | The handoff's `**` count of `0` and the review's are true of the code; the two `**` hits in this grading are those two records quoting the literal |

verdict: 🟢
