# PLAN-overhaul · Overhaul plan — ultimate-tokens estate (.claude/) · 2026-08-14 · closed 2026-09-16 (Wave 1 executed; Phase 4 items 1, 3, 4 carried as debt D2)
| Field | Value |
|---|---|
| Decision | A 4-move rename plan for the `.claude/` estate generated by `authorkit:overhaul-planning`: MOVE three agents (`marketing-manager`, `palette-researcher`, `ultimate-tokens-reviewer`) and one skill (`migrating-figma-files` → `figma-file-migration`); NO MOVE for six gerund skills (`adding-semantic-roles`, `adding-export-formats`, `building-editor-sections`, `maintaining-brand-kit-mcp`, `maintaining-figma-plugins`, `shipping-changes`), grandfathered under the ADR-0011 D8 ratchet (exemption retained, shrink-only). No merge/split candidates, no species changes, no contested rows. Execution contract per ticket: claim → worktree → `git mv` → supersession note or `renames.json` entry → gates + critics → PR → human merge → verified close, serial through `naming.manifest.json`. |
| Forces | naming-audit errors=0 warnings=0 exemptions=10 (3 agents missing `-agent`, 7 gerund skills), bloat-audit already applied in #436, check-routing and plan-plugin-split unavailable (repo-local estate; no trigger-eval suites), no observed routing misfires, blast radius per member (1 to 16 files), closed ProcessLex/RoleLex lexicons, the five respect invariants (evidence can veto the plan; history preserved; consumers degrade gracefully; old intent read before judged; nothing semantic rides in a move), human-confirmed verdicts 2026-08-14 upholding the two high-radius kills |
| Rejected | Renaming `adding-*`/`building-*`/`maintaining-*`/`shipping-changes` (nearest ProcessLex terminal misdescribes the procedure or no terminal exists; radius buys no routing improvement); editing the closed lexicon for one name ("inverts the tail and the dog"); the plan's own original targets `marketing-agent`, `palette-research-agent`, `change-review-agent` were superseded at execution because they fail the agent grammar (invariant 1: evidence vetoed the plan) |
| Consequences | Wave 1 executed 2026-08-14 as PRs #439 (`marketing-manager-agent`), #440 (`palette-researcher-agent`), #441 (`change-reviewer-agent`), #442 (`figma-file-migration`); RoleLex gained `manager`/`researcher`/`reviewer`, vocab gained `marketing`/`palette`/`change`/`figma`/`file`. Checklist reads INCOMPLETE: Wave 1 W1-A..D all ticked; Phase 4 closeout has four unticked items (routing/session smoke, `fix-old-names` sweep incl. the user memory index, dated supersession notes on records, naming-audit re-run to exemptions 10 → 6). "Next step" says ticket seeds are generated only and must be minted through `file-task` per ADR-017 → GitHub Issues. ADR ids cited: ADR-0011 (D8 ratchet), ADR-017. |
| Supersedes / amended by | none stated; the four W1 target names carry in-line "Superseded target 2026-08-14" notes |
| Source | docs/plan/archive/overhaul-plan-2026-08-14.md |
