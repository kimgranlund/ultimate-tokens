# Verdict A2 architecture · pass 4 · 🟢

Graded by sdlc-verifier on 2026-09-16 against `.sdlc/architecture.md` (152 lines) at `sdlc/adopt` @ f9e20c5. Every control rerun in a throwaway git worktree of HEAD (since removed), plants reset between runs. Pass 1 gaps listed at the bottom with their disposition.

| # | Criterion | State | Evidence | Negative control |
|---|---|---|---|---|
| 1 | Each convention has a negative control run and exceptions listed | 🟡 | 18/18 K rows now carry HEAD result + recorded bite. Extracted and ran the §6.1 scripts verbatim: K9 0, K13 0, K14 3 (`lc-applied`, `lc-ceiling`, `lc-toneline`, as listed), K18 0. Reran the changed filters: K1 0, K6 0, K12 inventory 4, K17 0. Gap: K9's exceptions say `figma/plugin/ui.html` "is still covered by `gen:figma-ui` in `npm test`", but the K9 control itself leaves it out. The script's file list omits ui.html (and the adia artifacts), so a hand edit there passes K9. It should be listed as an exception to the control, or the script should cover it | my own plants, each caught: K1 `document.body` in tonal.js 1 (a comment line mentioning `document.body` added 0); K6 `fetch(` 1; K9 `// hand edited` in mcp-assets.js 1 `CHANGED BY REGEN`; K13 `font-family:${family}` 1; K14 `ty-line` renamed `zz-line`: +1 `MISSING .an-svg .zz-line`; K17 unlisted tracked `test/engine/zzz.mjs` 1; K18 CURRENT 5: 1. Gap proof: `x` appended to `figma/plugin/ui.html`, K9 script: 0 hits |
| 2 | Two evidence files per claim | 🟢 | 35/35 rows still have two non-empty evidence cells. P3 now lists 10 types; the doc's own grep yields exactly `apply, list-fonts, load-config, load-sets, read-float-variables, read-variables, save-config, save-sets, sweep-delete, sweep-scan` (13 raw matches, 10 unique). P7 evidence 2 is now repo files (`package.json` test script, `.claude/CLAUDE.md` Commands). X2 idempotence held: `gen-figma-binder-code.mjs` run twice, `git status` empty both times. Cited refs verified in pass 1 are unchanged | X2: junk inside `GENERATED:ROLE_TABLE` markers was removed by regen (status empty after); junk appended outside the markers survived regen, which is what "rewrite only between markers" predicts |
| 3 | No source edits | 🟢 | `git diff --stat HEAD`: only `.claude/settings.json`, the key reorder already in this session's starting status; no untracked files outside `.sdlc/` | pass 1: an index-backed diff against a planted copy listed all 9 planted files + 1 untracked |

## Pass 1 gaps

| Gap | Disposition |
|---|---|
| bites recorded for 4/18 | 🟢 closed: 18/18, and 8 re-planted here all caught |
| K12 count 4 vs command 10 | 🟢 closed: raw 10 and filtered 4 both stated; filtered command returns 4 |
| K13 count 9 vs 10 | 🟢 closed: generated asset excluded by name; script returns 0 at HEAD |
| P3 partial | 🟢 closed: 10 types |
| P7 evidence not in repo | 🟢 closed |
| X2 idempotence unevidenced | 🟢 closed, verified above |
| (new) K9 control omits `figma/plugin/ui.html` without listing it as an exception | 🟡 open |

## Pass 3 (criterion 1, K9 only)

| # | Criterion | State | Evidence | Negative control |
|---|---|---|---|---|
| 1 | K9 control and exceptions | 🟡 | Ran the revised §6.1 K9 script verbatim in a throwaway worktree of HEAD: 0 lines, tree clean after. `figma/plugin/ui.html` is now watched and regenerated. Still open: the two `gen:adia-exports` outputs (`docs/reference/data/adia-oklch-export.css`, `adia-radix-export.mjs`) are in the `npm test` chain but neither watched by K9 nor run by it (`gen-adia-derived-exports.mjs` is absent from the script), and the exceptions cell does not list them. All other `npm test` generator writes are watched (figma-plugin-assets, mcp-assets, describe-mcp-assets, categories, binder code.js, ui.html); `dist/` is ignored | `<!-- hand edited -->` appended to ui.html: 1 `CHANGED BY REGEN: figma/plugin/ui.html`. Gap proof: `x` appended to `adia-oklch-export.css`, K9 script: 0 lines; `node scripts/gen-adia-derived-exports.mjs` then restored it (status empty), so adding it to the script would catch the plant |

Pass 2 gap "K9 omits ui.html": 🟢 closed. Remaining: adia outputs unwatched and unlisted, 🟡 open.

## Pass 4 (criterion 1, K9 only)

| # | Criterion | State | Evidence | Negative control |
|---|---|---|---|---|
| 1 | Each convention has a negative control run and exceptions listed (K9 was the last open row) | 🟢 | Ran the §6.1 K9 script verbatim in a throwaway worktree of HEAD: 0 lines, tree clean. It watches 16 files and runs 8 generator scripts, including `gen-adia-derived-exports.mjs`. Independent inventory: marker file, then the 6 `npm run` generator steps of `test`, then `find -newer`: 17 files written = the 16 watched + `dist/ultimate-tokens.html` (gitignored, listed as an exception). `git status` empty after | plants, each caught with 1 `CHANGED BY REGEN` line: `adia-oklch-export.css`, `adia-radix-export.mjs`, `src/ui/categories/travel.js` |

Pass 3 gap "adia outputs unwatched and unlisted": 🟢 closed. Final state: criteria 1, 2, 3 all 🟢.

## Pass 5 (U2 of plan records-refresh, per convention)

Graded 2026-09-19 at `d46ae48` (branch `plan/records-refresh`), whose merge base with `origin/main` is at `20298cc`. Every control and every plant below is this verifier's own run: the 15 read-only controls in a detached scratch worktree at `d46ae48`, and K7, K9 plus all 18 plants in a throwaway `git clone --shared` of it, reset with `git reset --hard && git clean -fdx` between plants. The builder handoff (`.sdlc/handoffs/records-refresh-U2.md`) is the claim under test, not evidence; it measured at `d814500`, an earlier merge base, and the branch has since been rebased onto `20298cc`, so its numbers and mine are read at different heads and every difference is quoted. `npm test` was run exactly once (one single run, in the clone): exit 0, `all 48 test files passed`, 63 s, `git status --short` empty after.

| # | Convention | State | Evidence | Negative control |
|---|---|---|---|---|
| K1 | Engines touch no DOM or browser globals | 🟢 | own run of the §6 grep over `src/engine/*` with the comment filter at `d46ae48`: 0 hits. Map cell 0; handoff 0. Agrees | own plant: `const x = document.body;` appended to `src/engine/tonal.js`, control printed 1 hit `src/engine/tonal.js:439:const x = document.body;` |
| K2 | Engines import nothing outside `src/engine/` | 🟢 | own run of `grep -nE 'from "\.\./\|import\("\.\./' src/engine/*`: 0 hits. Map cell 0; handoff 0. Agrees | own plant: `import { h } from "../ui/app-helpers.mjs";` appended to `src/engine/hct.js`, control printed 1 hit at `src/engine/hct.js:331` |
| K3 | Engines use no `node:` builtins; MCP uses no npm packages | 🟢 | own run of both halves (engine `node:`/`require(` grep; `mcp/*.mjs` non-relative import grep): 0 hits. Map cell 0; handoff 0. Agrees | own plant: `import fs from "node:fs";` in `src/engine/hct.js` plus `import x from "lodash";` in `mcp/describe-eval.mjs`, control printed 2 hits, one per half |
| K4 | Sections, overlays, model, persist never import `app.js` | 🟢 | own run of the `from "../app.js"` grep over `src/ui/sections`, `src/ui/overlays`, `src/ui/*.mjs`, `src/ui/persist.js`: 0 hits. Map cell 0; handoff 0. Agrees | own plant: `import { HctApp } from "../app.js";` appended to `src/ui/sections/color.js`, control printed 1 hit at `src/ui/sections/color.js:2164` |
| K5 | Binder planners are pure (no `figma.` global) | 🟢 | own run of `grep -nE "\bfigma\.[a-zA-Z]" figma/binder/*.mjs` with the comment filter: 0 hits. Map cell 0; handoff 0. Agrees | own plant: `const v = figma.variables;` appended to `figma/binder/live-diff.mjs`, control printed 1 hit at `figma/binder/live-diff.mjs:47` |
| K6 | Sandbox `code.js` files have no ES imports and no network I/O | 🟢 | own run over `figma/plugin/code.js` and `figma/binder/figma-semantic-binder/code.js` with the comment filter: 0 hits (the map's 2 raw pre-filter hits are the same header comments stating the rule). Map cell 0; handoff 0. Agrees | own plant: `fetch("https://x");` appended to `figma/plugin/code.js`, control printed 1 hit at `figma/plugin/code.js:1838` |
| K7 | Every module reachable from the app is registered in `bundle.mjs` MODS/KEY | 🟢 | own run in the clone: `node scripts/bundle.mjs` exit 0, `wrote dist/ultimate-tokens.html 3777.5 KB`. The convention (registration) holds. Size differs from both the map cell (3692.6 KB) and the handoff (3774.8 KB at `d814500`); the bundle grew with the commits the rebase brought in, and size is not what this control asserts | own plant: new `src/engine/planted.mjs` imported at the top of `src/engine/exports.js`, control exited 1 with `bundle.mjs preflight found 1 registry problem` and `src/engine/exports.js: import path "./planted.mjs" is not registered in KEY` |
| K8 | The three role tables stay identical (53 roles) | 🟢 | own run in the clone: `node test/engine/semantic.mjs` exit 0 `PASS`, `node test/figma/binder.mjs` exit 0 `PASS`, and `semanticRoles("brand").length` 53 equals `roleTable.length` 53. Map cell and handoff both 53 = 53. Agrees | two own plants: (a) binder `SCRIM_STRENGTH_STEPS` last step 600 changed to 999 in `figma/binder/figma-semantic-binder/code.js`, `test/figma/binder.mjs` exit 1 with `FAIL parity ... code.js "500-999" != semantic.js "500-600"`; (b) one row popped from `role-table.json` `roleTable`, `test/engine/semantic.mjs` exit 1 `FAIL: 1 gate failure(s)` |
| K9 | Generated artifacts are not hand-edited: regeneration reproduces them byte for byte | 🟢 | own run in the clone of the §6.1 K9 script (16 watched files, all 8 generators): 0 `CHANGED BY REGEN` lines, `git status --short` empty after. Map cell 0 and clean; handoff 0 and clean. Agrees | own plant: a hand-edit marker appended to (or prepended to) each of `docs/reference/data/adia-oklch-export.css`, `docs/reference/data/adia-radix-export.mjs`, `figma/plugin/ui.html`, `src/ui/mcp-assets.js`; the script printed exactly 4 `CHANGED BY REGEN:` lines, one per file |
| K10 | `localStorage` access is always inside try/catch | 🟢 | own run of the §6 grep: 6 raw hits, `app-helpers.mjs:89,91,92,114,134` and `app.js:2273`. Own manual trace at `d46ae48` puts all 6 inside multi-line `try {}` blocks (`migrateStorageKeys`, `loadSets`, `saveSets`, `_loadAppPrefs`), matching the convention's stated mechanical limit. Count equals the map cell's 6; the map's `app.js:2296` has shifted to `2273`, which is what the handoff also read | own plant: unguarded `function f(){ localStorage.setItem("a","b"); }` appended to `src/ui/app-helpers.mjs`, count rose 6 to 7 |
| K11 | No UI framework, no JSX, no shadow DOM; markup is built with `h()` | 🟢 | own run of all three legs: tracked `.jsx/.tsx/.vue/.svelte` 0, framework imports 0, `attachShadow` 0. The listed exception re-measured: `git grep -cE "html:"` gives `color.js 6`, `geometry.js 3`, `typography.js 3`, total 12, exactly the ratified count. Map cell 0 and 12; handoff 0 and 12. Agrees | own plant: tracked `src/ui/planted.jsx` plus `import { html } from "lit";` appended to `src/ui/zip.mjs`, control printed 2 hits (`src/ui/planted.jsx`, `src/ui/zip.mjs:107`) |
| K12 | Modals are native `<dialog>` opened with `showModal()`; no ARIA-faked modals | 🟢 | own run: violation grep (`role: "dialog"` / `role="dialog"` / `aria-modal`) 0 hits. Inventory: 4 comment-filtered `showModal(` calls (`app.js:613`, `overlays/apply-gate.js:345`, `overlays/settings.js:19`, `sections/color.js:415`) against 4 `"dialog"` tags, the same 4 files as the map. Line numbers have drifted from the map (`app.js:610` to `613`, `apply-gate.js:317` to `345`); the handoff recorded the `app.js` shift but did not name the `apply-gate.js` one. Counts and files identical, so the convention holds | own plant: `h("div", { role: "dialog", "aria-modal": "true" });` appended to `src/ui/overlays/settings.js`, violation grep printed 1 hit at `src/ui/overlays/settings.js:520` |
| K13 | Interpolated `font-family` values are quoted | 🟢 | own run of the §6.1 K13 script: 0 hits. Raw `font-family:${` count over `src figma/binder scripts mcp` is 10 (`ds-export.js` 7, generated `describe-mcp-assets.js` 1 excluded, `sections/typography.js` 2), matching the map's recorded 10 and 9-after-exclusion. Map cell 0; handoff 0. Agrees | own plant: `` const zz = `font-family:${family}`; `` appended to `src/ui/sections/typography.js`, script printed 1 hit `src/ui/sections/typography.js:1098:font-family:${family` |
| K14 | SVG line paths set `fill: none` in a rule qualified under `.an-svg` | 🟢 | own run of the §6.1 K14 script: exactly 3 hits, `lc-applied`, `lc-ceiling`, `lc-toneline`, which are the three recorded exceptions and nothing else. Map cell 3, same three; handoff 3, same three. Agrees | own plant: `<path class="ty-line` renamed to `zz-line` in `src/ui/sections/typography.js`, script printed a 4th line `MISSING .an-svg .zz-line fill:none` alongside the 3 known ones |
| K15 | `node_modules` and `.claude/docs/other/` are never tracked | 🟢 | own run: `git ls-files \| grep node_modules/` 0, `git ls-files .claude/docs/other` 0, `git log --all --oneline -- .claude/docs/other` no commits, `node .claude/hooks/git-precommit-privatedocs-guard.mjs --selftest` PASS. Map cell and handoff the same. Agrees | own plant in the clone: force-added `node_modules/p/i.js` and `.claude/docs/other/n.md`, the tracking control printed both paths, 2 hits |
| K16 | MCP stdio servers keep stdout for protocol only | 🟢 | own run of `grep -nE "console\.log" mcp/*-server.mjs`: 0 hits. Map cell 0; handoff 0. Agrees | own plant: `console.log("hi");` appended to `mcp/brand-kit-server.mjs`, control printed 1 hit at `mcp/brand-kit-server.mjs:49` |
| K17 | Tests use no framework and every test file is in `run.mjs` TESTS | 🔴 | own run at `d46ae48`: the framework-import half is 0, but the registration half printed 4 hits after the control's own exception filter (`run.mjs\|smoke/smoke.mjs\|ui/counts.mjs`): `gate-report.mjs`, `repo/fixtures/gate-report-clean.mjs`, `repo/fixtures/gate-report-mismatch.mjs`, `repo/fixtures/gate-report-singlequote.mjs`. All four arrived with `9a44f68 test: shared gate-report helper, so an unlisted failing gate always prints (#699) (#702)`, which is on `origin/main` at `20298cc` but not at `d814500`. `test/gate-report.mjs` is a shared printer module and the three `repo/fixtures/*` are fixtures, so they are the same class as the listed `ui/counts.mjs` helper, but they are not in the listed exceptions and the control prints them. The handoff claims `0 unlisted .mjs test files` for K17; that was true at its head `d814500` (`git ls-tree d814500 test/` has no `gate-report.mjs` and no `repo/fixtures`) and is false at this head. Recorded, not fixed: the gap is that K17's exception list is stale against the rebased head | own plant: `import test from "node:test";` appended to `test/engine/hct.mjs` plus a new tracked `test/engine/zzz.mjs`; the control printed 6 hits, the 4 above plus `test/engine/hct.mjs:172:import test from "node:test"` and `engine/zzz.mjs`. The control still discriminates (4 to 6); its baseline, not its bite, is what fails |
| K18 | Every schema version has a documented rule, and no migration outruns `CURRENT_SCHEMA_VERSION` | 🟢 | own run of the §6.1 K18 script: silent (0 hits). `CURRENT_SCHEMA_VERSION` is 4 and the `schema-rename v4` snapshot case is at `test/ui/persist.mjs:183`. Map cell 0 at CURRENT 4, same line; handoff silent at 4. Agrees | own plant: `CURRENT_SCHEMA_VERSION` bumped to 5 in `src/ui/persist.js`, script printed 1 hit `no test/ui/persist.mjs snapshot case for CURRENT_SCHEMA_VERSION 5` |

Result: 17 of 18 🟢, 0 🟡, 1 🔴 (K17). No 🟡 row was needed: every other own-run number either equals its map cell or differs only in a way the convention does not assert (bundle byte size under K7, line-number drift under K10 and K12), and each of those is quoted in its row.

One further observation for the Orchestrator, outside the per-convention grading: `.sdlc/architecture.md` §8, the rerun note and the §7 Counts bullet all name `d814500`, while the merge base of this head with `origin/main` is at `20298cc`. That is criterion 2's subject, not a K row, and is left to whoever grades the unit's criteria.

Note (2026-09-19, plan records-followup U6, #709): the closing observation of pass 5 above was resolved by U3 of plan records-refresh on 2026-09-19, which re-measured §8, the rerun note and the Counts bullet at `20298cc`. It is not an open finding.
