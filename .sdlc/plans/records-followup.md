---
status: approved
ticket: #709
priority: P1
lane: docs
size: S + S + M + S + S + S + S + S + S (U1 S = 1 point, U2 S = 1, U3 M = 2, U4 S = 1, U5 S = 1, U6 S = 1, U7 S = 1, U8 S = 1, U9 S = 1; 10 points; a U10 for the pre-land review's findings is sized in its checklist row)
labels: kind:chore · size:big · lane:docs · P1 (as minted on #709)
written: 2026-09-19
amended: 2026-09-20 (revision 13, the newest; revision 10: the plan file made true against itself after the pre-land review at 887e3eb2; see Revisions)
approval: .sdlc/questions/records-followup-approval.md (Q1 to Q4)
head: d34b4fb1 (`origin/main` when the branch was cut; since U9 merged `origin/main` 3ce50daa in, `BASE` = `git merge-base origin/main HEAD` reads 3ce50daa; the branch is local only, not pushed)
branch: plan/records-followup
inputs: ticket #709; ticket #712 (records-tidy, folded here and closed, owner ruling 2026-09-20; its plan on `plan/records-tidy`); the pre-land review of records-refresh (reviewer-l3, graded at 0dabfd1a; sha256 6a3a285e91555f7b545a828969402d227e35c58626ec47e473bf273f041c1481, 5297 bytes, still in the Lane B scratchpad); .sdlc/plans/archive/records-refresh.md; .sdlc/adapter.md; .sdlc/baseline.md; owner rulings of 2026-09-19 (Conductor channel)
---

# Repair the records that landed untrue with PR #708: nine surviving pre-land findings, one falsified quotation, and the stale roadmap

PR #708 (#691) landed at 28c2e8cc before its pre-land review was answered. The review was FIX-FIRST with ten findings. Nine survive on main at d34b4fb1 (F9 is fixed), and a parallel pre-land pass added an eleventh: a verdict rewrote a line of real program output so it would pass the no-em-dash rule.

Owner rulings, 2026-09-19. Fix everything, F5 and F7 included. `.sdlc/roadmap.md` is its own unit and its own commit, apart from the record repairs. This conductor session and Lane B drive.

Owner answers at approval, 2026-09-19 (`.sdlc/questions/records-followup-approval.md`). Q1: approved, with the 11 gaps of the checkability review folded before mobilizing (revision 2 does that). Q2: two PRs, so the roadmap is its own commit on main. Q3: the `P1` to `P4` collision between `debt.md` and `architecture.md` is a seventh unit of this plan (U7). Q4: yes to a one-line `.claude/CLAUDE.md` edit inside U3, which is the owner's approval adapter conflict row X9 requires.

Root cause, not only the symptoms. Three of the findings (F3, F7, F11) have one shape: a record holds a second copy of something a program prints, and nothing compares the copy with the program. records-refresh fixed that shape for the test count and the three timed gates. This plan finishes it for the other two gates, for the negative control's count, and for quoted output, and it makes the comparison a command each time.

Scope wall. Only paths under `.sdlc/` change, with one approved exception: U3 edits one line of `.claude/CLAUDE.md` (approval Q4). `.sdlc/roadmap.md` changes in U5 only and lands as its own PR (see Landing). `src/`, `test/`, `scripts/`, `mcp/`, `figma/` are untouched.

Prose rules for every line this plan adds. No em dash outside an inline backtick span that quotes program output (U4 writes that exemption down; until U4 merges, U1 to U3 add no quoted output that carries one). The retired maker brand and the pre-rename element identifier are paraphrased, never quoted. No bold inline labels, except the adapter's own amendment opener, which is that file's shape.

Host note. `grep -P` is absent on this host. Every PCRE below runs through `perl` or `git grep -P` (git carries its own PCRE; measured working at d34b4fb1).

Criteria ids follow the archived plan: P rows for the plan, numbered rows per unit, cited as U3-2.

## Measured by the planner on 2026-09-19 at d34b4fb1

Every "measured" value in the tables below comes from this list. Host load was 118 to 311 on 10 cores throughout, so no timing here is a baseline figure; counts and strings are load-independent.

| Fact | Value |
|---|---|
| P1 negative control, throwaway clone, `"scrim` to `"scrimX` in `role-table.json`, then `npm test` | exit 1; `grep -c FAIL` prints `3` (the runner's file line, the gate line, the `FAIL: 1 gate failure(s)` line); last line reports 1 of 48 test files failed; 175.88 s at load 293 to 118, not a timing |
| The gate line that control prints, from `test/gate-report.mjs:82` | two spaces, `FAIL`, two spaces, `refs-canonical`, two spaces, a U+2014 dash, one space, `ordered key set != canonical` |
| `.sdlc/verdicts/records-refresh-U1.md:37` quotes it as | `refs-canonical, ordered key set != canonical` (a comma where the program prints the dash): the falsified quotation |
| `test/smoke/smoke.mjs:293` prints | `SMOKE PASS`, a U+2014 dash, then the four surfaces; `.sdlc/baseline.md:20` and four lines of `.sdlc/handoffs/records-refresh-U3.md` write a colon there, and `.sdlc/verdicts/records-refresh-U3.md:78` spells the glyph out in words |
| `npm run gate:corpus-contrast` | exit 0, tree clean after; prints `(343 curated documents, 3780 palettes (3420 carrying a non-zero lift or skew), 22680 accent/on-color cells; 504 named per cell, 0 carried below 4.5)` and three lines of `7560 cells`, one per tone mode; 48.55 s at load 190, not a timing |
| `npm run gen:type-fonts` | exit 0, tree clean after, 1.00 s at load 50, not a timing |
| CI run 35455937943 on 20298cc, `gh run view --json jobs` | four jobs, all success: `build-test`, `panda-smoke`, `corpus-contrast`, `deploy` |
| `sh .sdlc/checks/baseline-agrees-check.sh` | seven `ok`, `stale total: 0`, exit 0 (d34b4fb1 equals 20298cc outside `.sdlc/`) |
| Shas in `.sdlc/baseline.md` that are not in `origin/main`'s history | one: `b50a4b9b` |
| `git log --oneline d814500..20298cc -- test/run.mjs` | one commit, 9a44f685, subject ends `(#699) (#702)` |
| Ids defined (first table cell) in more than one live record, over adapter, architecture, baseline, debt, survey, roadmap, board | 22: `C1` to `C7` (debt, survey), `K9 K11 K14 K17 K18` (architecture, debt), `P1` to `P4` (architecture, debt), `X1` to `X6` (adapter, architecture) |
| Prefix `XC` followed by digits anywhere under `.sdlc`, `.claude`, `README.md` | 0 hits. Also 0: `CP`, `XP`, `Y`, `Z`, `J`. Not free: `N` 1, `W` 2, `V` 3, `E` 28, `M` 34 |
| The plugin's `board.py ids` (from the plugin repo's `plan/drill-findings` @ 08f3e1b) over `.sdlc` | exit 0 at d34b4fb1 and exit 0 after the simulated U2 rename; its id shape is one uppercase letter, so a two-letter prefix is outside what it reads |
| Roadmap against live facts | worktree table lists 4 that are gone (`638-dual-radix`, `672-citation-predicate`, `673-color-prune-librarymode`, `674-adia-warning`) and omits 2 that exist (`pif-u2-ramp`, `pif-u4-integration`); "Nine worktrees" over 8 rows; ranked rows for 6 closed tickets (#638 #672 #673 #674 #676 #691) and none for 3 open ones (#686 #701 #709); one `Your move` line |
| The `P` series, measured for U7 | `architecture.md` §3 defines `P1` to `P7`; `debt.md` §Process defines `P1` to `P4`. Live cites of the debt rows: `adapter.md` lines 52 and 91 (`ruling P1`, `(P1)`, both the squash-only ruling, which the archived adopt-hygiene plan line 92 ties to debt P1), `debt.md:125` (`P2 prune`), `roadmap.md:91` (`P3 (debt id, ...)`, already disambiguated by hand). Live cite of the architecture rows: `adapter.md:22` (`architecture P7`). `debt.md` lines 49 and 68 say `P3 forbids` and `the P3 wall`: that is criterion P3 of the archived adopt-hygiene plan, a third meaning. `DP` followed by digits: 0 hits under `.sdlc`, `.claude`, `README.md` (`PC` 0, `PR` 2). Simulated U7 in a clone: the seven-file sweep prints `C1,C2,C3,C4,C5,C6,C7,K9,K11,K14,K17,K18` with U2 also applied; mapping `DP` back to `P` reproduces the base `debt.md` with `0` differing lines; `DP` tokens per file `adapter.md:2`, `debt.md:5` before the note |
| The length of `TESTS`, printed by command | `48` (the perl one-liner of P1) |
| `CLAUDE_JOB_DIR` and `CLAUDE_PLUGIN_ROOT` in an agent Bash shell on this host | both empty (checkability review, 2026-09-19), so neither may appear in a command |
| Branding gate | `branding: clean (461 files scanned)`, exit 0. With `decision-records.md` copied to `.sdlc/verdicts/x.md` in the clone: `FAIL: 3 branding violation(s) across 462 files`, so the gate scans `.sdlc/verdicts/` and exempts nothing there |
| Em-dash enforcement | no test, script, hook or workflow enforces it. It is the owner's prose rule, applied by plan criteria that count added lines |

## Criteria (plan-level, run by each builder, again by each verifier, again at pre-land)

Every negative control that edits a file runs in a throwaway clone (`git clone -q --shared . "$F/neg"`), never in a unit worktree. `F` is a directory the seat makes for itself under its own scratchpad, with a name no other seat would pick (`mkdir "$SCRATCH/<unit>-<seat>-tmp"`). `CLAUDE_JOB_DIR` is empty in agent shells here, so the archived plan's `$CLAUDE_JOB_DIR/tmp` resolves to `/tmp`. The scratchpad is shared between seats: create only under `$F`, delete only `$F`, never by glob. In every table cell `\|` is the table's escape for a plain `|`: type it unescaped. A literal pipe inside a regex is written `[|]`. `BASE` is `$(git merge-base origin/main HEAD)`, which on every branch cut from `plan/records-followup` is `d34b4fb1`, so a name list against `BASE` always includes this plan file. `UB` is `$(git merge-base plan/records-followup HEAD)`, where the unit branch left the plan branch; a row that lists a unit's own files uses `UB`. `EM` is `$'\xe2\x80\x94'` (bash or zsh).

| # | Criterion | Command | Expected | Negative control |
|---|---|---|---|---|
| P1 | `npm test` green with no `node_modules`, tree byte-stable | `npm test 2>&1 \| tail -1; perl -0ne 'my ($b) = /const TESTS = \[(.*?)\];/s; my @m = $b =~ /"[^"]+\.mjs"/g; print scalar(@m), "\n"' test/run.mjs; git status --short \| wc -l` | the runner's pass line, then a number equal to the N in that line (`48` at d34b4fb1, measured), then `0` | in the clone: `sed -i '' 's/"scrim/"scrimX/' docs/reference/data/role-table.json && npm test > "$F/neg.log" 2>&1; echo "exit $?"; grep -c FAIL "$F/neg.log"` prints `exit 1` then `3` (measured today). U1-3 reuses `$F/neg.log`; U4-2 reuses its gate line |
| P2 | `npm run build` green and prints the size the live baseline row records (pre-land only; this is F8's corrected form, scoped to the live row) | `npm ci >/dev/null 2>&1; npm run build > "$F/build.log" 2>&1; echo "exit $?"; grep -o 'ui.html [0-9.]* KB' "$F/build.log" \| head -1; grep '^[|] `npm run build` [|]' .sdlc/baseline.md \| grep -o 'ui.html [0-9.]* KB'; git status --short \| wc -l` | `exit 0`, the same `ui.html NNNN.N KB` string twice, `0` | the uncorrected grep over the whole file prints two different strings at d34b4fb1 (`3780.5` live, `3777.8` prior set), which is why the archived form could not pass. No `node_modules`: exit 127 |
| P3 | `npm run smoke` green (pre-land only, and only if U3 took the full re-measure path) | `npm run smoke 2>&1 \| grep -c 'SMOKE PASS'` | `1` | in the clone with no `dist/`: `node test/smoke/smoke.mjs` exits 1 with `smoke: missing <ROOT>/dist/ultimate-tokens.html` (`altered: absolute path written as <ROOT>, cut before the build hint`) |
| P4 | branding gate clean | `bash -c 'set -o pipefail; node test/repo/branding.mjs \| tail -1; echo "exit $?"'` | `branding: clean (N files scanned)`, `exit 0` | in the clone: `cp docs/reference/references/decision-records.md .sdlc/verdicts/x.md && node test/repo/branding.mjs \| tail -1` prints `FAIL: 3 branding violation(s) across 462 files` (measured today) |
| P5 | scope wall: nothing outside `.sdlc/` differs from the merge base except the one approved `.claude/CLAUDE.md` line (approval Q4, graded by U3-8), and on `plan/records-followup` the roadmap does not differ either | `git diff --name-only $BASE \| grep -vcE '^\.sdlc/\|^\.claude/CLAUDE\.md$'; git diff --name-only $BASE \| grep -c '^\.sdlc/roadmap\.md$'; git diff --numstat $BASE -- .claude/CLAUDE.md \| cut -f1,2` | `0`, `0`, then nothing before U3 merges and `1	1` after (on the U5 branch the second line is `1`, the first stays `0`, the third prints nothing) | `echo "// probe" >> src/engine/motion.mjs` in the clone: first line `1`. `echo >> .sdlc/roadmap.md`: second line `1`. A second edited line in `CLAUDE.md`: third line `2	2` |
| P6 | no em dash added in prose, by the measure U4 writes down | `git diff -U0 $BASE -- .sdlc \| grep '^+' \| perl -pe 's/\x60[^\x60]*\x60//g' \| LC_ALL=C grep -o "$EM" \| wc -l \| tr -d ' '` | `0` | fixture measured today (revision 5, occurrences): a `+` line with the dash in prose counts `1`; the same dash inside a backtick span counts `0`; a line with two prose dashes counts `2`, where the line count of revision 4 counted `1`, which is the smuggling hole this row closes; the three lines together count `3`, and `4` without the `perl` strip |
| P7 | the landing rule ruled for every plan that lands after #691: the baseline's test-file figure equals `TESTS.length`, checked by script at the pre-land head | `sh .sdlc/checks/baseline-agrees-check.sh; echo "exit $?"` | every counted line `ok`, `stale total: 0`, `exit 0` (seven lines before U3, nine after); after U3 a `note head:` line may appear when the tree moved outside `.sdlc/` since the baseline ran, it is printed, never counted, and the pre-land record quotes it | records-refresh measured the plants: a baseline figure of 47 against 48 in `TESTS` prints `STALE tests:` and exits 1. If another plan lands first and raises `TESTS`, U3 takes its full path and this row is how pre-land sees it |

## Units

- [x] U1 (S) the five one-cell repairs: F1 `debt.md` K17 note, F2 `architecture.md` rerun note, F3 `adapter.md` negative-control line, F6 and F10 `baseline.md` · grade l1 · reviewer-l1 · verifier-l1
- [x] U2 (S) the id collision F4: `architecture.md` §4 rows X1 to X6 become XC1 to XC6, one dated note carries the map, the three `debt.md` notes say what is true · grade l1 · reviewer-l1 · verifier-l1 · after U1 merges
- [x] U7 (S) the second id collision (approval Q3): `debt.md` §Process rows P1 to P4 become DP1 to DP4, the three live cites follow, one dated note carries the map; `architecture.md` keeps P1 to P7 · grade l1 · reviewer-l1 · verifier-l1 · after U2 merges (same file, `debt.md`)
- [x] U4 (S) the verbatim-quote rule F11, written once in `adapter.md` §3, and the seven altered quotes restored from program output (five from the pre-land ruling, two from review 1 F2) · grade l2 · reviewer-l1 · verifier-l1 · after U7 merges
- [x] U6 (S) F8 as a dated corrections file beside the archive, the review copied into `verdicts/`, one dated note under pass 5, and the seven notes of U4 review 2 (two restorations, five wording and record fixes) · grade l1 · reviewer-l1 · verifier-l1 · after U4 merges
- [x] U3 (M) F7: `baseline.md` becomes the one home for all five gates, measured on a quiet host; the check script reads all five; the adapter loses its copied arithmetic and job count; #712's adapter §2.1 grade line (reviewer-l4 with verifier-l3, the plugin skill's pair) and the cited-quote clause of the verbatim-quote rule; one approved line of `.claude/CLAUDE.md` names the three PR jobs · grade l2 · reviewer-l1 · verifier-l1 · after U6 merges, and only in a quiet slot
- [x] U8 (S) pre-land fix from the U3 verdict: the `note head:` line in `.sdlc/checks/baseline-agrees-check.sh` said the ref has the same tree as HEAD exactly when the tree moved; reword that one line so the note states the tree moved. Line 32 only, so U3-1's numstat stays `3 3`. Criteria: (1) on a tree that moved outside `.sdlc/` the script prints one `note head:` line whose text says the tree moved, `stale total: 0`, exit 0; (2) on an unmoved tree the head line prints `ok`; (3) a baseline ref that is not in `origin/main`'s history still prints `STALE` and exits 1; (4) `git diff --numstat $BASE -- .sdlc/checks/baseline-agrees-check.sh` still reads `3 3`. The 233-character header comment stays unwrapped and is ruled debt, since wrapping it adds a line and breaks (4) · grade l5 · reviewer-l2 · verifier-l2
- [x] U9 (S) bring `origin/main` (3ce50daa, with records-tidy #714 and k17-rerun #711 landed) into `plan/records-followup` by a merge, never a rebase, since the board, verdicts and handoffs cite this branch's shas. Five files conflict, one hunk each: `.sdlc/adapter.md`, `.sdlc/architecture.md`, `.sdlc/debt.md`, `.sdlc/verdicts/architecture.md`, `.sdlc/board.md`. Each hunk is settled by re-measuring the fact it states at the merged head, never by picking a side, and the handoff lists per hunk: both sides, the command run, what it printed, the text kept. It also carries the U8 verdict's yellow: the U3 handoff Correction line says the quoted retired sentence is what ran at ceb471b0. Criteria: (1) `git merge-base --is-ancestor origin/main HEAD` exits 0 and `git grep -c '^<<<<<<<\|^>>>>>>>' -- .sdlc` prints nothing; (2) nothing main landed is lost: `git diff origin/main HEAD -- .sdlc/plans/archive/ .sdlc/verdicts/k17-rerun*.md` is empty for files this plan does not own; (3) P1 to P7 of this plan re-run green at the merged head, U1's F1 and F2 re-measured against main's text; (4) `sh .sdlc/checks/baseline-agrees-check.sh` exits 0 with `stale total: 0`; (5) `npm test` green, tree clean after · grade l5 · reviewer-l2 · verifier-l2
- [x] U10 (S) the pre-land review's findings at 887e3eb2 that are record repairs (the review file is copied into `.sdlc/verdicts/` by this unit, as U6 did for its review). Finding 1, blocking: the records this PR adds obey the verbatim-quote rule it writes: no fenced program dump remains in a record this PR adds or edits, every quoted program line sits in its own inline span with its check mark intact (`baseline.md` lines 19 and 20 among them), and a quote named as the subject of a finding carries `cited:`. Finding 2: `debt.md` K17's plan-branch sha is annotated as dropped by the squash, as `architecture.md:18` does. Finding 4: the three `baseline.md` prose sentences U3's rows made false are made true by re-measuring. One exemption, written by this unit as one sentence appended to the cited-quote amendment in `.sdlc/adapter.md` §3: a byte-pinned verbatim copy of another seat's record (a review copied into `.sdlc/verdicts/` whose copy states the source's hash) is kept byte for byte and is not swept or restored, because it cannot both keep its hash and be rewritten; the copy's header names the hash and says so. Criteria: (1) except for byte-pinned copies under that exemption, whose hash the verifier recomputes, the builder's handoff enumerates every edited line, and for each quoted program line gives the command that reprints it byte for byte; (2) a sweep for fenced blocks holding program output in files this PR touches prints 0, with the pre-fix head printing the review's count; (3) U4-1, U4-3, U4-5 and U3-10 re-run green; (4) `sh .sdlc/checks/baseline-agrees-check.sh` exit 0, branding clean, `npm test` green; (5) no file outside `.sdlc/` moves · grade l5 · reviewer-l2 · verifier-l2
- [x] U5 (S) F5: `.sdlc/roadmap.md` regenerated from live facts, on its own branch, its own commit, its own PR · grade l2 · reviewer-l1 · verifier-l1 · after the first PR lands
- [~] U11 (S) pre-land fixes from the roadmap PR's review, both roadmap-only commits admitted by U5-6 as reworded. R1: `roadmap.md:32` and `:49` say #713 is gated on #681 landing with nothing ready to start, which was false when written, not live drift (`plan/gate-split` had G0 waived 20:52Z, U6a merged 19:55Z, U2 to U5 dispatched 21:35:09Z, all before the 21:36:16Z regeneration); both cells are re-derived from that branch's own state at the fix head. R2: `roadmap.md:7` and `:25` say 10 issues and total 10 over a 13-row table, because the Orchestrator's landing refresh added three rows without moving the header counts; both are recomputed from the table itself. Root cause, and the reason this is a unit and not two typos: U5's rows 1 to 7 check the roadmap's SHAPE (one head sha, the worktree set, the issue set, no leftover prompt, the debt ids, the alone rule) and never its ASSERTIONS about other work, so neither red was catchable by any existing row. U11 adds one criterion that does: every claim the roadmap makes about another plan's readiness or a count it states is re-derived from the thing it describes, with the derivation command in the handoff · grade l5 · reviewer-l2 · verifier-l2

Order: U1, U2, U7, U4, U6, U3, then U5 after the first PR lands. The units touch overlapping files (`adapter.md` in U1, U7, U3, U4; `baseline.md` in U1, U3, U4; `architecture.md` in U1, U2; `debt.md` in U1, U2, U7), so they run in series, one merge at a time. U3 is late because it waits for a quiet host and nothing else should wait with it. U5 is last because a roadmap is a snapshot and the first PR's landing changes what it must say.

Grades. The Orchestrator's table puts docs at l1. U1, U2, U7 and U6 stay there: each edit is given below as exact text or as a mechanical map, and each has a criterion that catches a transcription slip by construction (U1-6 counts changed lines, U2-3 and U7-3 map the rename back to the base file, U6-2 compares a hash). U3 goes one up to l2 for the reason the archived plan gave for its measuring units: it transcribes six or fifteen timed runs and edits a script, and a plausible number nobody measured is the defect this ticket exists for. U4 goes to l2 because it authors a rule other plans will be graded against. U5 goes to l2 because it reads about forty live facts from `gh` and `git` and ranks nothing; the failure mode is a row copied from the old file. The checker pair stays reviewer-l1 and verifier-l1 (opus, outside the sonnet builder's family). Every reviewer dispatch names `.claude/agents/change-reviewer-agent.md` §What to check (adapter conflict row X2).

### U1 (S, grade l1): five one-cell repairs

Each edit is in place, one line changed per finding. No other line in the four files moves.

#### Texts

- F1, `.sdlc/debt.md`, the K17 row. `the control filters 3 files by name` becomes `the control filters 7 files by name (at 20298cc; the list is the exception cell of architecture K17, which is the copy to trust)`.
- F2, `.sdlc/architecture.md`, the rerun note. `pass 5 stays graded at `d814500`` becomes `pass 5 stays graded at `d46ae48` (its own header; a plan-branch sha the squash dropped, whose merge base with `origin/main` was `20298cc`)`. The earlier clause of the same note, that U2 ran the controls at `d814500`, is true and stays.
- F3, `.sdlc/adapter.md` §1, the second rule bullet. The parenthesis `(corrupt `docs/reference/data/role-table.json`, expect 17 FAIL)` becomes:

```text
(in a throwaway clone: `sed -i '' 's/"scrim/"scrimX/' docs/reference/data/role-table.json`, then `npm test`. It exits 1, `engine/semantic.mjs` is the one failing file, and the `refs-canonical` gate is the one failing gate. `grep -c FAIL` printed 3 for this corruption at `d34b4fb1`; a different corruption or a different grep gives a different count, so grade on the exit, the file and the gate, and record the count you measured)
```

The builder runs the control first (P1's control cell) and writes the count it measured. If it is not 3, it writes its own number and sha and tells the Orchestrator.

- F6, `.sdlc/baseline.md`, the prior-set paragraph. `Kept because #706 (`d814500..20298cc`: TypeScript 6 to 7, vite 8.0 to 8.3, Node 24 in CI, one test file added to `TESTS`) changed the toolchain` becomes `Kept because the seven commits of `d814500..20298cc` changed what the live table describes: #706 moved the toolchain (TypeScript 6 to 7, vite 8.0 to 8.3, Node 24 in CI) and #699 (PR #702, 9a44f685) added one test file to `TESTS`. They changed the toolchain`. Read the sentence after the edit and keep it grammatical; the facts above are the fixed part.
- F10, `.sdlc/baseline.md`, the intro sentence. `in the unit worktree at `plan/records-refresh` @ `b50a4b9b`, whose tree equals` becomes `in the U3 unit worktree of plan records-refresh, whose tree equals`. No plan-branch sha replaces it: the sentence already says the tree equals `ref`, and `ref` is a main sha.

#### Criteria

| # | Criterion | Command | Expected | Negative control |
|---|---|---|---|---|
| 1 | F1: the number in debt K17 equals what the K17 listing finds in the tree | `git ls-files test \| grep "\.mjs$" \| sed "s#^test/##" \| while read f; do grep -q "\"$f\"" test/run.mjs \|\| echo "$f"; done \| wc -l \| tr -d ' '; grep -o 'filters [0-9]* files by name' .sdlc/debt.md` | `7`, `filters 7 files by name` | at d34b4fb1: `7`, `filters 3 files by name` (measured) |
| 2 | F2: the sha the map names for pass 5 is the sha pass 5's own header names | `grep -o 'pass 5 stays graded at `[0-9a-f]*`' .sdlc/architecture.md \| grep -o '`.*`'; sed -n '/^## Pass 5/,$p' .sdlc/verdicts/architecture.md \| grep -m1 -o '^Graded [0-9-]* at `[0-9a-f]*`' \| grep -o '`.*`'` | the same backticked sha twice (`d46ae48`) | at d34b4fb1 the two lines differ: `d814500`, `d46ae48` (measured) |
| 3 | F3: the adapter states what the control reproduces, and the verifier's own run agrees with it | P1's control, then `grep -c 'expect 17 FAIL' .sdlc/adapter.md; grep -o 'printed [0-9]* for this corruption' .sdlc/adapter.md; grep -c FAIL "$F/neg.log"; grep -c 'engine/semantic.mjs *FAIL' "$F/neg.log"; grep -c 'FAIL  refs-canonical' "$F/neg.log"` | `0`, `printed N for this corruption`, `N`, `1`, `1`, where the N of line two equals the number on line three, which is the verifier's own count. The planner measured 3. A builder who measured another number wrote that number, and the row still holds only if the verifier's run agrees with it; a mismatch is 🔴 | at d34b4fb1 the first line is `1` and the second is empty (measured). A clone with no corruption: `grep -c FAIL` prints `0`, so the count is the control's and not noise |
| 4 | F6: the baseline credits the test file to the commit that added it | `git log --format=%s d814500..20298cc -- test/run.mjs \| grep -o '(#[0-9]*) (#[0-9]*)$'; grep -c 'Node 24 in CI, one test file added' .sdlc/baseline.md; grep -c '#699 (PR #702, 9a44f685) touched only files under' .sdlc/baseline.md` | `(#699) (#702)`, `0`, `1` | at d34b4fb1: `(#699) (#702)`, `1`, `0` (measured) |
| 5 | F10: every sha the baseline cites is in `origin/main`'s history | `for s in $(perl -ne 'print "$1\n" while /(?<![0-9a-f])([0-9a-f]{7,40})(?![0-9a-f])/g' .sdlc/baseline.md \| grep '[a-f]' \| grep '[0-9]' \| sort -u); do git merge-base --is-ancestor $s origin/main 2>/dev/null \|\| echo "NOT $s"; done \| wc -l \| tr -d ' '` | `0` | at d34b4fb1: `1`, the line is `NOT b50a4b9b` (measured) |
| 6 | the unit changed five lines and nothing else, and the check script still agrees | `git diff -U0 $BASE -- .sdlc/debt.md .sdlc/architecture.md .sdlc/adapter.md .sdlc/baseline.md \| grep -v '^--- ' \| grep -c '^-'; git diff -U0 $BASE -- .sdlc/debt.md .sdlc/architecture.md .sdlc/adapter.md .sdlc/baseline.md \| grep -v '^+++ ' \| grep -c '^+'; git diff --name-only $UB \| LC_ALL=C sort \| paste -sd, -; sh .sdlc/checks/baseline-agrees-check.sh \| tail -1` | `5`, `5`, `.sdlc/adapter.md,.sdlc/architecture.md,.sdlc/baseline.md,.sdlc/debt.md,.sdlc/handoffs/records-followup-U1.md` (against `UB`, so the plan file is not in the list; the four files plus the handoff), `stale total: 0` | a sixth reworded line prints `6`, `6`; a fifth file in the list is the wall |

### U2 (S, grade l1): one id, one thing (F4)

Decision. `architecture.md` §4 gives way, not the adapter. Reasons: the plugin's plan-rules (its `plan/drill-findings` branch) reserves `X<n>` for ids the adapter mints, so moving the adapter again would break that reservation one day after U4 of records-refresh adopted it; the adapter side is 24 lines plus 3 debt notes and a map that would need a second hop, the architecture side is 6 rows and 1 header sentence; and nothing outside `architecture.md` cites its X rows except its own verdict, which is history. New prefix: `XC` (cross-cutting). Measured free across `.sdlc`, `.claude` and `README.md`, and outside the one-letter shape the plugin's id check reads.

#### Texts

- `.sdlc/architecture.md` §4: the first cell of the six rows `X1` to `X6` becomes `XC1` to `XC6`. The header sentence `(A-, T-, P-, X-, L- ids)` becomes `(A-, T-, P-, XC-, L- ids)`.
- One note appended directly under the §4 table: `Renamed <date> (plan records-followup U2, #709): these rows were X1 to X6 until this date, same numbers, same rows. The adapter's conflict rows took X1 to X13 on 2026-09-19, so X1 to X6 named two things in two live records. `.sdlc/verdicts/architecture.md` grades these rows under their old ids and was not rewritten: X<n> there means XC<n> here.`
- `.sdlc/debt.md`, the last cell of rows C5 and C6: the clause `the shared prefix that made this a reading hazard is gone` becomes `that pair no longer shares a prefix. The rename moved the collision to architecture §4 (X1 to X6) until records-followup U2 renamed those rows XC1 to XC6 on <date>. This debt id still shares its id with a 2026-09-16 survey claim`. Row C7's `same history as C5 and C6` stays: it points at the corrected cells.
- The sweep's other 16 shared ids (debt and survey `C1` to `C7`, debt and architecture `P1` to `P4`, the five K rows debt keys to their convention on purpose) are recorded under Not in scope. U2 does not touch them.

#### Criteria

| # | Criterion | Command | Expected | Negative control |
|---|---|---|---|---|
| 1 | no X id is defined in two live records, and the sweep reads `architecture.md` | `for f in adapter architecture baseline debt survey roadmap board; do perl -ne 'print "$1 $ARGV\n" if /^[|] ([A-Z]{1,3}-?\d+[a-z]?) [|]/' .sdlc/$f.md; done \| sort -u \| awk '{n[$1]++} END{for(i in n) if(n[i]>1) print i}' \| sort -V \| paste -sd, -` | `C1,C2,C3,C4,C5,C6,C7,K9,K11,K14,K17,K18,P1,P2,P3,P4` at U2's verdict (measured on the simulated rename); the same list without the four `P` ids once U7 has merged, which is what pre-land sees | at d34b4fb1 the same list ends `,X1,X2,X3,X4,X5,X6` (measured). Dropping `architecture` from the file list at d34b4fb1 prints only `C1` to `C7`, no X at all, which is the omission F4 names: the list must name all seven files |
| 2 | the new prefix was owned by nobody before this unit | `git grep -P '(?<![A-Za-z0-9-])XC[0-9]+(?![0-9A-Za-z])' $BASE -- .sdlc .claude README.md \| wc -l \| tr -d ' '` | `0` | the same command with `X` for `XC` prints `104` at d34b4fb1 (measured), so the sweep can see a taken prefix |
| 3 | the rename is the only change to the map: mapping XC back to X reproduces the base file except the appended note | `UB=$(git merge-base plan/records-followup HEAD); diff <(git show $UB:.sdlc/architecture.md) <(perl -pe 's/(?<![A-Za-z0-9-])XC([1-6])(?![0-9a-z])/X$1/g; s/XC-, L- ids/X-, L- ids/' .sdlc/architecture.md) \| grep '^[<>]' \| grep -vc '^> Renamed 20\|^> $'; awk '/^## 4\. /{s=1} /^## 5\. /{s=0} !s' .sdlc/architecture.md \| grep -cE 'XC[0-9]'` (bash). `UB` is where the unit branch left the plan branch, so U1's merged line is in the base | `0`, `0`. The second number closes a hole the first cannot see: the inverse map also undoes a rename applied outside §4, so `XC` tokens outside §4 are counted directly (the note sits inside §4's range) | simulated today without the note: `0` changed lines of any kind, and `0` outside §4. A cite `XC3` planted in §1 prints `1` on the second line while the first stays `0` (measured). At d34b4fb1 even `s/X([1-6])/XC$1/g` with no lookarounds changes only the 6 rows, so the planner could not reproduce the review's 14-line case; the plant is the control. A reworded evidence cell prints `2`; a row renamed `XC7` prints `2`, because the map covers 1 to 6 |
| 4 | the six rows and the note are there; no X-headed row is left in §4 | `awk '/^## 4\. /,/^## 5\. /' .sdlc/architecture.md \| grep -oE '^[|] XC[0-9]+ [|]' \| tr -d '\| ' \| paste -sd, -; awk '/^## 4\. /,/^## 5\. /' .sdlc/architecture.md \| grep -cE '^[|] X[0-9]+ [|]'; grep -c '^Renamed 20[0-9-]* (plan records-followup U2, #709)' .sdlc/architecture.md` | `XC1,XC2,XC3,XC4,XC5,XC6`, `0`, `1` | at d34b4fb1: an empty line, `6`, `0` |
| 5 | the debt notes stopped claiming the hazard is gone | `grep -c 'reading hazard is gone' .sdlc/debt.md; grep -c 'renamed those rows XC1 to XC6' .sdlc/debt.md; git diff -U0 $BASE -- .sdlc/debt.md \| grep -v '^--- ' \| grep '^-' \| grep -vcE '^-[|] (C5\|C6\|K17) [|]'` | `0`, `2`, `0` | at d34b4fb1: `2`, `0`, `0` |
| 6 | the plugin's id check still accepts the tree (regression on records-refresh U4) | `git -C "$PLUGIN" show 08f3e1b:plugins/sdlc/scripts/board.py > "$F/board.py"; python3 "$F/board.py" ids .sdlc; echo "exit $?"` with `PLUGIN` the sdlc plugin's repo (the target of the `core.hooksPath` symlink, two levels up) | nothing, `exit 0` | records-refresh U4-1 measured the bite: an adapter row headed `C13` prints one refusal line and exits 1 |

### U7 (S, grade l1): the second collision, debt P against architecture P (approval Q3)

Same method as U2. Decision: `architecture.md` keeps `P1` to `P7`, and the four `debt.md` §Process rows move. Reasons, all measured: the debt rows are 4 against 7; the architecture rows are graded under their ids by `.sdlc/verdicts/architecture.md` and cited by the adapter as `architecture P7`; and in `debt.md` a bare `P<n>` already has two other meanings, a priority label (`P0` to `P3`, which is why the roadmap had to write `P3 (debt id, ...)` by hand) and a plan-level criterion (`debt.md` lines 49 and 68 mean criterion P3 of the archived adopt-hygiene plan). New prefix: `DP` (debt, process). Measured free across `.sdlc`, `.claude` and `README.md`, and outside the one-letter shape the plugin's id check reads, so the plugin's reservation of `D<n>` for `debt.md` is not touched.

Dependency with U2. Both units edit `debt.md` (U2 the last cell of rows C5 and C6, U7 the four Process rows, line 125 and one note) and U1 edits its K17 row, so U7 is cut after U2 merges and its map-back row uses `UB`. U7 does not edit `architecture.md`. After U7 merges, U2-1's sweep prints the list without the four `P` ids; U2-1's Expected says so. U5 writes `DP3` in the roadmap's debt table (U5-7).

#### Texts

- `.sdlc/debt.md` §Process: the first cell of rows `P1` to `P4` becomes `DP1` to `DP4`. In the numbered list at the end, `16. P2 prune` becomes `16. DP2 prune`.
- One note appended directly under the §Process table: `Renamed <date> (plan records-followup U7, #709): these rows were P1 to P4 until this date, same numbers, same rows. `.sdlc/architecture.md` §3 also defines P1 to P7, so P1 to P4 named two things in two live records. A record written before this date that cites debt P<n> (the archived plans, `.sdlc/tickets/T-0001.md`, the adopt-hygiene verdicts) means DP<n>; those files were not rewritten. The `P3` on the R12 and G2 rows above is criterion P3 of the archived adopt-hygiene plan, not a debt id.`
- `.sdlc/adapter.md`, two cites inside dated amendments, token edits only, as records-refresh U4 did for the conflict ids: line 52 `ruling P1)` becomes `ruling DP1, debt P1 until records-followup U7)`, and line 91 `squash-merge only (P1).` becomes `squash-merge only (DP1, debt P1 until records-followup U7).` Line 22's `architecture P7` stays.
- `debt.md` lines 49 and 68 stay as they are; the note explains them.

#### Criteria

| # | Criterion | Command | Expected | Negative control |
|---|---|---|---|---|
| 1 | no P id is defined in two live records, by the same seven-file sweep as U2-1, which reads both `debt.md` and `architecture.md` | U2-1's command | `C1,C2,C3,C4,C5,C6,C7,K9,K11,K14,K17,K18` (measured with U2 and U7 both simulated) | before U7 the list also carries `P1,P2,P3,P4` (measured). Dropping either `debt` or `architecture` from the file list at d34b4fb1 removes the four `P` ids from the output, so a sweep that omits one of the two files cannot see this collision |
| 2 | the new prefix was owned by nobody before this unit | `git grep -P '(?<![A-Za-z0-9-])DP[0-9]+(?![0-9A-Za-z])' $BASE -- .sdlc .claude README.md \| wc -l \| tr -d ' '` | `0` (measured at d34b4fb1) | the same command with `PR` for `DP` prints `2`, so the sweep sees a taken prefix |
| 3 | the rename is the only change to the four rows and the list line: mapping DP back to P reproduces the unit's base `debt.md` except the appended note | `UB=$(git merge-base plan/records-followup HEAD); diff <(git show $UB:.sdlc/debt.md) <(perl -pe 's/(?<![A-Za-z0-9-])DP([1-4])(?![0-9a-z])/P$1/g' .sdlc/debt.md) \| grep '^[<>]' \| grep -vc '^> Renamed 20\|^> $'` (bash) | `0` | simulated today without the note: `0` differing lines. A reworded `why that grade` cell prints `2`; a row renamed `DP5` prints `2`, because the map covers 1 to 4 |
| 4 | every cite moved, counted directly and not through the inverse map: the `DP` tokens sit where the texts put them, and no bare debt `P` is left | `git grep -cP '(?<![A-Za-z0-9-])DP[1-4](?![0-9a-z])' -- .sdlc/adapter.md .sdlc/debt.md; perl -ne 'print if /(?<![A-Za-z0-9-])P[1-4](?![0-9a-z])/' .sdlc/debt.md \| grep -vcE 'P3 (wall\|forbids)\|^Renamed 20'; perl -ne 'print "$.," if /(?<![A-Za-z0-9-])P[1-7](?![0-9a-z])/' .sdlc/adapter.md` | `.sdlc/adapter.md:2`, `.sdlc/debt.md:5` (four rows and line 125; the note writes `DP<n>` with no digit); `0`; `22,52,95,` where 52 and 91 carry `debt P1 until` beside the new id and 22 is `architecture P7` | at d34b4fb1: no `DP` line at all; `5` (four rows and line 125); `22,52,95,`. Simulated with line 125 left as `P2 prune`: the second number is `1` |
| 5 | the four rows and the note are there, no P-headed row is left in §Process, and U1's and U2's `debt.md` edits survived | `awk '/^## Process/,0' .sdlc/debt.md \| grep -oE '^[|] DP[0-9]+ [|]' \| tr -d '\| ' \| paste -sd, -; awk '/^## Process/,0' .sdlc/debt.md \| grep -cE '^[|] P[0-9]+ [|]'; grep -c '^Renamed 20[0-9-]* (plan records-followup U7, #709)' .sdlc/debt.md; grep -c 'filters 7 files by name' .sdlc/debt.md; grep -c 'renamed those rows XC1 to XC6' .sdlc/debt.md` | `DP1,DP2,DP3,DP4`, `0`, `1`, `1`, `2` | at d34b4fb1: an empty line, `4`, `0`, `0`, `0` |
| 6 | scope and regression: three files, the plugin's id check still accepts the tree, history untouched | `git diff --name-only $UB \| LC_ALL=C sort \| paste -sd, -; git -C "$PLUGIN" show 08f3e1b:plugins/sdlc/scripts/board.py > "$F/board.py"; python3 "$F/board.py" ids .sdlc; echo "exit $?"; git diff --stat $UB -- .sdlc/verdicts .sdlc/tickets .sdlc/plans/archive .sdlc/questions \| wc -l \| tr -d ' '` with `PLUGIN` as in U2-6 | `.sdlc/adapter.md,.sdlc/debt.md,.sdlc/handoffs/records-followup-U7.md`; nothing, `exit 0`; `0` | measured today: the plugin's check does not read `debt.md` rows at all, because that file's tables are headed `id` in lower case and the check reads only `#`, `Id`, `ID`; a Process row renamed `B1` still exits 0. So this row guards the adapter edit and the history files only, and its bite is the `C13` refusal records-refresh U4-1 measured. Rows 3, 4 and 5 are what catch a wrong debt id. A fourth path in the name list is the wall |

### U4 (S, grade l2): quote program output verbatim (F11)

What is enforced today, measured. `test/repo/branding.mjs` bans three strings and scans every `.sdlc/` file with no exemption for quotes (adapter conflict row X12 says so on purpose). Nothing enforces the no-em-dash rule: it is the owner's prose rule, applied by plan criteria that count added lines. So the branding gate needs no change and gets none, and the dash rule needs its measure written down so a verbatim quote stops tripping it.

#### Texts

The rule, written once, as an amendment appended at the end of `.sdlc/adapter.md` §3 in the file's own shape. The builder types the opener as the file does (`**Amendment (<date>).**`) and then:

```text
Verbatim-quote rule (ticket #709, pre-land ruling 2 on records-refresh). Text a program printed is quoted byte for byte, glyphs included, inside an inline backtick span, one span per line of output. The no-em-dash rule is a prose rule and measures prose: a plan's added-line count strips backtick spans first (`perl -pe 's/\x60[^\x60]*\x60//g'`), so a verbatim quote never trips it and is never reworded to pass it. The strip exempts every backtick span, a command or a path as much as a quote, so it is not the only control: each plan also counts the added dashes raw against the unit's merge base, the unit's own handoff excluded, and states the expected number as one per restored line, re-derived for every unit; a dash a span smuggled into prose shows there as one above the count. If a quote has to change (cut short, a banned string removed, or a glyph the file cannot hold such as an ANSI escape or a control character, written in its place as its name or hex code), the cell says `altered:` and what changed, next to the quote. Leading whitespace the program printed is kept byte for byte, or its loss is marked the same way (`altered: two leading spaces dropped`). The branding gate (`test/repo/branding.mjs`, row X12) has no quote exemption and gets none: a quote that would carry one of its three banned strings is cut before that string and marked `altered: retired name removed`. Copy a quote from the program's output or its source line, never from another record.
```

Restoring the seven altered quotes (five from the pre-land ruling, two more that U4's review 1 found, F2). Each restored quote is copied from the program, not from this plan: the gate line from `grep -o 'FAIL  refs-canonical.*' "$F/neg.log"` (P1's control), the smoke line from `grep -o 'SMOKE PASS[^"]*' test/smoke/smoke.mjs`.

- `.sdlc/verdicts/records-refresh-U1.md:37`: the span `refs-canonical, ordered key set != canonical` becomes the gate line as printed. The program prints two leading spaces before `FAIL` and the `grep -o` drops them, so the cell says `altered: two leading spaces dropped` next to the span (rule sentence on leading whitespace).
- `.sdlc/baseline.md:20`: the smoke summary cell becomes the smoke line as printed, in a backtick span.
- `.sdlc/handoffs/records-refresh-U3.md`, the four lines that write `SMOKE PASS:`: the same.
- `.sdlc/verdicts/records-refresh-U3.md:78`: the span that spells the glyph out becomes the smoke line as printed.
- `.sdlc/verdicts/survey.md:11`: the C3 evidence cell's span writes the dash as an ASCII hyphen-minus (`SMOKE PASS - gallery`); it becomes the smoke line as printed.
- `.sdlc/handoffs/records-refresh-U1.md:26`: run 8's summary cell writes `SMOKE PASS` in its own span followed by a colon and commas for the middots; the cell becomes the smoke line as printed, in one backtick span. Lines 27 and 28 say `same SMOKE PASS line` in prose and stay.
- Each of the five history files (the U1, U3 and survey verdicts, the U1 and U3 handoffs of records-refresh) gains one appended last line: `Correction (<date>, plan records-followup U4, #709): a quotation of program output in this file had been reworded to avoid an em dash. It now reads as the program prints it. Rule: `.sdlc/adapter.md` §3, Verbatim-quote rule.` In `records-refresh-U3.md` the line adds: `Note 2 above no longer holds on its face: the baseline row and the handoff were restored to the printed line on the same date, and the rule it names is the owner's prose rule, which the amendment separates from the branding gate.` (review 1 F6: the note is prose, so it is flagged, not rewritten.)

#### Criteria

| # | Criterion | Command | Expected | Negative control |
|---|---|---|---|---|
| 1 | the rule is written once, in the adapter, and says the three things | `git grep -l 'Verbatim-quote rule (ticket #709' -- .sdlc ':!.sdlc/plans' ':!.sdlc/verdicts' ':!.sdlc/handoffs/records-followup-U4.md' \| paste -sd, -; grep -c 'Verbatim-quote rule (ticket #709' .sdlc/adapter.md; awk '/^## 3\. /,/^## 4\. /' .sdlc/adapter.md \| grep 'Verbatim-quote rule (ticket' \| grep -c 'byte for byte.*strips backtick spans.*has no quote exemption'` | `.sdlc/adapter.md`, `1`, `1` (measured at ba0a66d6 with the exclusion) | at d34b4fb1: an empty line, `0`, `0`. The five corrections cite the rule by the words `§3, Verbatim-quote rule.`, which the first pattern does not match, so a cite is not a second home. U4's own handoff quotes the pattern and is excluded: a unit's handoff is not a record under repair (review 1 F1). Revision 9: `.sdlc/verdicts` is excluded too, since a verdict that grades this row must cite the pattern, and the cited-quote clause rules a cited span is not a home (U9 verifier, 2026-09-20) |
| 2 | the U1 verdict quotes the gate line as the verifier's own control prints it | P1's control, then `grep -o 'FAIL  refs-canonical.*' "$F/neg.log" > "$F/line.txt"; wc -l < "$F/line.txt" \| tr -d ' '; grep -cFf "$F/line.txt" .sdlc/verdicts/records-refresh-U1.md; grep -c 'refs-canonical, ordered' .sdlc/verdicts/records-refresh-U1.md` | `1`, `1`, `0` | at d34b4fb1: `1`, `0`, `1` (measured). The first line guards the vacuous case: an empty `line.txt` makes `grep -Ff` match nothing |
| 3 | every `.sdlc` quote of smoke's summary outside `plans/` and U4's own handoff is the line the source prints, in every altered form seen so far (colon, backtick-then-colon, hyphen-minus, glyph spelled out) | `grep -o 'SMOKE PASS[^"]*' test/smoke/smoke.mjs > "$F/smoke.txt"; wc -l < "$F/smoke.txt" \| tr -d ' '; git grep -cFf "$F/smoke.txt" -- .sdlc/baseline.md .sdlc/handoffs/records-refresh-U1.md .sdlc/handoffs/records-refresh-U3.md .sdlc/verdicts/records-refresh-U3.md .sdlc/verdicts/survey.md; git grep -c -e 'SMOKE PASS: gallery' -e 'SMOKE PASS.: gallery' -e 'SMOKE PASS - gallery' -e 'SMOKE PASS (em dash)' -- .sdlc ':!.sdlc/plans' ':!.sdlc/handoffs/records-followup-U4.md' \| wc -l \| tr -d ' '` | `1`, then `.sdlc/baseline.md:1`, `.sdlc/handoffs/records-refresh-U1.md:1`, `.sdlc/handoffs/records-refresh-U3.md:4`, `.sdlc/verdicts/records-refresh-U3.md:1`, `.sdlc/verdicts/survey.md:1`, then `0` (the fold simulated at ba0a66d6 in a clone: both values measured) | at d34b4fb1 the widened grep prints `5` files (measured); at ba0a66d6 with the exclusion it prints `2`, `.sdlc/handoffs/records-refresh-U1.md:1` and `.sdlc/verdicts/survey.md:1` (measured), and the verbatim grep has no hit in those two files. The patterns are `-e` alternatives on purpose: revision 2's single pattern joined its two forms with a pipe, and once the table's `\|` is typed unescaped, a plain `[|]` in a basic regex is a literal, so that pattern printed `0` at d34b4fb1 too (measured today) and was vacuous. `plans/` is excluded because this plan names the altered forms; U4's handoff is excluded because it quotes the pattern (F1) |
| 4 | each corrected history file says it was corrected | `grep -c '^Correction (20[0-9-]*, plan records-followup U4, #709)' .sdlc/verdicts/records-refresh-U1.md .sdlc/verdicts/records-refresh-U3.md .sdlc/handoffs/records-refresh-U3.md .sdlc/verdicts/survey.md .sdlc/handoffs/records-refresh-U1.md` | `1` on each of the five lines | at d34b4fb1: `0` five times; at ba0a66d6: `1`, `1`, `1`, `0`, `0` |
| 5 | the dash measure exempts the restored quotes and nothing else; branding unchanged | P6's command; then `git diff -U0 07ecb44c b9e70950 -- .sdlc ':!.sdlc/handoffs/records-followup-U4.md' \| grep '^+' \| LC_ALL=C grep -o "$EM" \| wc -l \| tr -d ' '`; then P4's command | `0`; then `9` dash occurrences, the restored quote lines and nothing else (baseline 1, records-refresh-U1 handoff 1, records-refresh-U3 handoff 4, U1 verdict 1, U3 verdict 1, survey verdict 1; re-measured as occurrences at b9e70950 with the exclusion: `9`, the same breakdown, since each restored line carries exactly one); then clean, `exit 0` (`branding: clean (471 files scanned)` at ba0a66d6) | P6's fixture (measured). A dash typed into the amendment's prose makes the first number `1`. A tenth dash inside a backtick span, on a fresh line or on one already counted, leaves the first number `0` and makes the second `10`, which is why the second is an equality. Revision 12 freezes the second leg at U4's graded span (its base 07ecb44c to its graded head b9e70950), since `$UB` re-derived on a later head also counts the dashes later units restored and reads 15. The unit's own handoff is excluded because the rule obliges it to quote both lines (F1) |
| 6 | scope: eight files, and history files changed only on the quote lines and the appended correction | `git diff --name-only $UB \| LC_ALL=C sort \| paste -sd, -; for f in .sdlc/verdicts/records-refresh-U1.md .sdlc/verdicts/records-refresh-U3.md .sdlc/handoffs/records-refresh-U3.md .sdlc/verdicts/survey.md .sdlc/handoffs/records-refresh-U1.md; do git diff -U0 $BASE -- $f \| grep -v '^--- ' \| grep -c '^-'; done \| paste -sd, -; sh .sdlc/checks/baseline-agrees-check.sh \| tail -1` | `.sdlc/adapter.md,.sdlc/baseline.md,.sdlc/handoffs/records-followup-U4.md,.sdlc/handoffs/records-refresh-U1.md,.sdlc/handoffs/records-refresh-U3.md,.sdlc/verdicts/records-refresh-U1.md,.sdlc/verdicts/records-refresh-U3.md,.sdlc/verdicts/survey.md` (eight paths, against `UB`); `1,2,4,1,1` (the U3 verdict's quote wraps over lines 78 and 79 today and the restored span sits on one line, as the rule asks; any other number and the verifier reads the diff; the fold simulated at ba0a66d6 prints exactly this); `stale total: 0` | a reworded finding in a verdict raises its count. At ba0a66d6 as committed: six paths and `1,2,4,0,0` |

### U6 (S, grade l1): the archive's two red criteria, corrected beside it (F8), and U4 review 2's seven notes

The archived plan is history and its criterion text stays as graded. The correction goes where the archive is read. Since revision 4 the unit also carries the seven 🟡 notes of U4's review pass 2 (reviewer-l2 at b9e70950): notes 1 and 2 are restorations under the verbatim-quote rule, the other five are wording and record fixes; all seven are enumerated below by path and line and recorded as rows in the corrections file, so the builder never guesses.

#### Texts

- New file `.sdlc/plans/archive/records-refresh-corrections.md`, front matter `kind: corrections`, `corrects: .sdlc/plans/archive/records-refresh.md`, `written: <date>`, `source: .sdlc/verdicts/records-refresh-prepr-review.md F2, F8 and deferred-note ruling 1`. Body: one table, columns `Where`, `As archived`, `What it prints at d34b4fb1`, `Corrected form`, four rows:
  - P2: the whole-file size grep prints two strings since the prior set was added; corrected form is P2 of this plan (scoped to the live row).
  - U2-7, last sub-check: a presence count that expects `1` and prints `3`, because pass 5 names `20298cc` on three lines; corrected form `sed -n '/^## Pass 5/,$p' .sdlc/verdicts/architecture.md | grep -c '^Graded .*20298cc'`, which prints `1` (measured).
  - Lines 306 and 342: both say pass 5 stays graded at `d814500`; pass 5's header says `d46ae48`, merge base `20298cc`. Corrected in the live map by U1 of this plan.
  - The closing sentence: a plan that copies a criterion from the archive copies the corrected form.
- One row appended to the archived plan's `## Revisions` table, nothing else in that file: `| <date> | corrections recorded beside this file in `records-refresh-corrections.md` (P2, U2-7, the pass 5 sha on lines 306 and 342); the text above is unchanged | pre-land review F2 and F8, ticket #709 |`
- `.sdlc/verdicts/records-refresh-prepr-review.md`: the pre-land review, copied byte for byte from the Lane B scratchpad (hash in this plan's front matter). It is a verbatim record, so it is not edited to fit any prose rule; it carries no em dash and passes the branding gate (measured).
- `.sdlc/verdicts/architecture.md`, one line appended at the end of the file: `Note (<date>, plan records-followup U6, #709): the closing observation of pass 5 above was resolved by U3 of plan records-refresh on 2026-09-19, which re-measured §8, the rerun note and the Counts bullet at `20298cc`. It is not an open finding.`

U4 review 2's seven notes (revision 4). The corrections file gains a second section, `## U4 review 2 notes (unit/rf-U4 @ b9e70950)`, one table, columns `Note`, `Where`, `As written`, `What the program prints or the rule needs`, `Corrected form`, seven rows headed `N1` to `N7`, each row's `Where` a path and line as listed here. The two restorations are copied from the program, never from this plan: the runner's per-file line from `test/run.mjs:23` and `:29` (`▶ `, the test name padded to 24, one space, `FAIL`; derived with `perl -CS -e 'printf "\x{25B6} %-24s FAIL\n", "engine/semantic.mjs"'`, which is the source-line reading the rule allows), and smoke's missing-artifact line from a run, `node test/smoke/smoke.mjs 2>&1 \| sed "s#$(pwd)#<ROOT>#"` in a clone with no `dist/`, which prints `` `smoke: missing <ROOT>/dist/ultimate-tokens.html — run `npm run build` first` `` (the program prints the clone's absolute path; writing it as `<ROOT>` is the one alteration and is marked).

- N1, `.sdlc/verdicts/records-refresh-U1.md:37`: the span `engine/semantic.mjs FAIL` becomes the runner line as derived above, in its own span (restoration). On the same line the marker `(altered: two leading spaces dropped)` is rewritten in the form the rule illustrates, a backtick span `altered: two leading spaces dropped` in place of the parentheses.
- N2, `.sdlc/verdicts/survey.md:11`, and the same string at `.sdlc/verdicts/records-refresh-checkability.md:12` and `.sdlc/verdicts/records-refresh-prepr.md:25` (the review calls it systemic, so all three go): the span `smoke: missing dist/ultimate-tokens.html` (prepr writes `.../dist/`) becomes the missing-artifact line as printed with `<ROOT>` for the path, in a double-backtick span padded with one space on each side so the prose strip reads it as one span, followed by the marker span `altered: absolute path written as <ROOT>` (restoration). The row's N2 entry names all three files. This plan's own P3 control cell is corrected in revision 4 the same way.
- N3 and N4, `.sdlc/adapter.md:97`, the amendment U4 wrote, edited in place (one paragraph, the same opener, so U4-1 still counts one): the clause `and states the expected number as one per restored line, re-derived for every unit;` becomes `and states the expected number as the dashes the enumerated quoted lines carry, counted from the program's output and never from the diff, re-derived for every unit;`.
- N5, the same paragraph, two sentences appended after `never from another record.`: `The strip reads inline spans only, so a quote goes in a span, one per line of output, never in a fenced block; a plan that must fence output strips the fence in its count command and says so. The rule binds lines added from 2026-09-19 on; an earlier quote is restored when a plan touches its record, and records-followup U6 lists the ones known.`
- N6, `.sdlc/handoffs/records-refresh-U3.md:170`: `a quotation of program output in this file had been reworded` becomes `four quotations of program output in this file had been reworded`. `.sdlc/verdicts/records-refresh-U3.md:83`: `had been reworded to avoid an em dash` becomes `had spelled a glyph out in words to avoid an em dash`. The `Correction (<date>, plan records-followup U4, #709):` prefix stays, so U4-4 still counts one per file.
- N7, `.sdlc/handoffs/records-followup-U4.md`, one appended last line: `Correction (<date>, plan records-followup U6, #709): the P1 row above is not P1 evidence, since `node_modules` was present and the tree carried five modified files during that run. P1 was measured by review pass 2 at b9e70950 in a clean detached worktree with no `node_modules`: `✓ all 48 test files passed`, 48 in `TESTS`, tree clean.`
- U4's §Texts block above stays as U4 wrote it; the amendment's live text is the adapter's, and criterion 7 below reads it there.

#### Criteria

| # | Criterion | Command | Expected | Negative control |
|---|---|---|---|---|
| 1 | the corrected commands do what the corrections file says | `grep '^[|] `npm run build` [|]' .sdlc/baseline.md \| grep -o 'ui.html [0-9.]* KB'; sed -n '/^## Pass 5/,$p' .sdlc/verdicts/architecture.md \| grep -c '^Graded .*20298cc'; grep -cF "grep -c '^Graded .*20298cc'" .sdlc/plans/archive/records-refresh-corrections.md; grep -cF "grep -o 'ui.html [0-9.]* KB'" .sdlc/plans/archive/records-refresh-corrections.md` | one line, `ui.html 3780.5 KB` (or the live figure after U3's full path); `1`; then `1` or more twice, which proves the file carries these two commands and not a paraphrase | the archived forms at d34b4fb1: two lines; `3` (measured) |
| 2 | the review copy is the review | `shasum -a 256 .sdlc/verdicts/records-refresh-prepr-review.md \| cut -d' ' -f1` | `6a3a285e91555f7b545a828969402d227e35c58626ec47e473bf273f041c1481` | one byte changed gives another hash. If the scratchpad copy is gone, the unit stops and asks; it does not retype the review |
| 3 | the archive gained one revisions row and lost nothing | `git diff --numstat $BASE -- .sdlc/plans/archive/records-refresh.md; git diff -U0 $BASE -- .sdlc/plans/archive/records-refresh.md \| grep '^+[^+]' \| grep -c 'records-refresh-corrections.md'` | `1	0	.sdlc/plans/archive/records-refresh.md`, `1` | an in-place fix to line 342 prints `2	1` |
| 4 | the corrections file has its rows (three archive rows plus the closing sentence, seven note rows) and names its source | `F2=.sdlc/plans/archive/records-refresh-corrections.md; grep -c '^corrects: .sdlc/plans/archive/records-refresh.md' $F2; grep -cE '^[|] (P2\|U2-7\|Lines 306 and 342\|The closing sentence) ' $F2; grep -c 'd46ae48' $F2; grep -c '^[|] N[1-7] ' $F2; grep -c '^## U4 review 2 notes (unit/rf-U4 @ b9e70950)' $F2` | `1`, `4`, `1` or more, `7`, `1` (a stand-in file with these rows measured today) | file absent: every `grep` errors and prints no count, which is not `1`. Six note rows print `6` |
| 5 | pass 5 note appended, nothing in the verdict deleted; board and plan parsers unaffected | `git diff --numstat $BASE -- .sdlc/verdicts/architecture.md; B="$(dirname "$(realpath "$(git config core.hooksPath)")")/scripts/board.py"; test -f "$B"; echo "found $?"; python3 "$B" check >/dev/null; echo "exit $?"` (`CLAUDE_PLUGIN_ROOT` is empty in agent shells; the hooks path is the plugin's `githooks/`, measured) | `1	0	.sdlc/verdicts/architecture.md` (or `2	0` with a blank separator line), then `found 0`, `exit 0` | a deleted line shows in the second numstat column |
| 6 | N1 and N2: the four restored spans are the program's lines, the old forms are gone, the markers are in the rule's form | `perl -CS -e 'printf "\x{25B6} %-24s FAIL\n", "engine/semantic.mjs"' > "$F/runner.txt"; grep -cF -f "$F/runner.txt" .sdlc/verdicts/records-refresh-U1.md; (cd "$F/neg" && node test/smoke/smoke.mjs 2>&1 \| sed "s#$(pwd)#<ROOT>#") > "$F/missing.txt"; wc -l < "$F/missing.txt" \| tr -d ' '; for f in .sdlc/verdicts/survey.md .sdlc/verdicts/records-refresh-checkability.md .sdlc/verdicts/records-refresh-prepr.md; do grep -cF -f "$F/missing.txt" $f; done \| paste -sd, -; grep -c $'engine/semantic.mjs FAIL\x60' .sdlc/verdicts/records-refresh-U1.md; git grep -c -e 'smoke: missing dist/' -e 'smoke: missing \.\.\./' -- .sdlc ':!.sdlc/plans' ':!.sdlc/verdicts/records-followup-*.md' \| wc -l \| tr -d ' '; grep -c '(altered:' .sdlc/verdicts/records-refresh-U1.md; git grep -c $'\x60altered: absolute path written as <ROOT>\x60' -- .sdlc/verdicts \| cut -d: -f2 \| paste -sd, -` (the clone at `$F/neg` has no `dist/`, so the smoke script exits 1 on its line 24 without a build; `$'\x60'` is the shell's spelling of the backtick this table cannot carry) | `1`; `1`; `1,1,1`; `0`; `0`; `0`; `1,1,1` (the fold simulated at b9e70950 in a clone prints exactly this) | at b9e70950 as committed: `0`; `1`; `0,0,0`; `1`; `3`; `1`; nothing (measured). An empty `runner.txt` or `missing.txt` would make `grep -Ff` match nothing, which the `wc -l` line guards for the second and the negative `0` guards for the first |
| 7 | N3 to N7: the rule says the five new things in the one amendment, and the three Correction lines read as §Texts says | `grep -c 'Verbatim-quote rule (ticket #709' .sdlc/adapter.md; awk '/^## 3\. /,/^## 4\. /' .sdlc/adapter.md \| grep 'Verbatim-quote rule (ticket' \| grep -c 'byte for byte.*strips backtick spans.*counted from the program.*never from the diff.*has no quote exemption.*inline spans only.*binds lines added from 2026-09-19'; grep -c 'one per restored line' .sdlc/adapter.md; grep -c '^Correction (20[0-9-]*, plan records-followup U4, #709): four quotations' .sdlc/handoffs/records-refresh-U3.md; grep -c '^Correction (20[0-9-]*, plan records-followup U4, #709): a quotation of program output in this file had spelled a glyph out' .sdlc/verdicts/records-refresh-U3.md; grep -c '^Correction (20[0-9-]*, plan records-followup U6, #709): the P1 row above is not P1 evidence' .sdlc/handoffs/records-followup-U4.md` | `1`, `1`, `0`, `1`, `1`, `1` (simulated at b9e70950) | at b9e70950 as committed: `1`, `0`, `1`, `0`, `0`, `0` (measured). A second amendment paragraph carrying the rule name makes the first number `2` |
| 8 | the dash count is the one the enumerated lines carry, counted from the program, not from the diff; prose adds none; branding clean; U4's own rows still hold | `git diff -U0 $UB -- .sdlc ':!.sdlc/handoffs/records-followup-U6.md' \| grep '^+' \| LC_ALL=C grep -o "$EM" \| wc -l \| tr -d ' '`; P6's command; P4's command; U4-3's last command; U4-4's command | `5` dash occurrences, counted from the enumerated lines and the program: the three restored missing-artifact spans (one each, in `survey.md:11`, `records-refresh-checkability.md:12`, `records-refresh-prepr.md:25`), plus one each on the two re-added lines that already carry a restored quote, `records-refresh-U1.md:37` (the gate line) and `survey.md:11` (the smoke line), so `survey.md:11` counts `2` (the runner line carries none; measured at unit/rf-U6 @ 3ff96951, where the line count of revision 4 printed `4`); `0`; `branding: clean (N files scanned)`, `exit 0` (`473` simulated); `0`; `1` five times | at 3ff96951: `5`, `0`, clean, `0`, `1,1,1,1,1` (measured). A sixth dash inside any span, even on a line already counted, makes the first number `6` and leaves the second `0`, which is why the first is an equality and its number comes from the line list above, not from rerunning the diff |
| 9 | scope: thirteen files, history files changed only on the enumerated lines | `git diff --name-only $UB \| LC_ALL=C sort \| paste -sd, -; for f in .sdlc/verdicts/records-refresh-U1.md .sdlc/verdicts/survey.md .sdlc/verdicts/records-refresh-checkability.md .sdlc/verdicts/records-refresh-prepr.md .sdlc/adapter.md .sdlc/handoffs/records-refresh-U3.md .sdlc/verdicts/records-refresh-U3.md .sdlc/handoffs/records-followup-U4.md; do git diff -U0 $UB -- $f \| grep -v '^--- ' \| grep -c '^-'; done \| paste -sd, -` | `.sdlc/adapter.md,.sdlc/handoffs/records-followup-U4.md,.sdlc/handoffs/records-followup-U6.md,.sdlc/handoffs/records-refresh-U3.md,.sdlc/plans/archive/records-refresh-corrections.md,.sdlc/plans/archive/records-refresh.md,.sdlc/verdicts/architecture.md,.sdlc/verdicts/records-refresh-U1.md,.sdlc/verdicts/records-refresh-U3.md,.sdlc/verdicts/records-refresh-checkability.md,.sdlc/verdicts/records-refresh-prepr-review.md,.sdlc/verdicts/records-refresh-prepr.md,.sdlc/verdicts/survey.md` (thirteen paths, against `UB`); `1,1,1,1,1,1,1,0` (simulated at b9e70950: one changed line per restored or reworded file, none removed from the U4 handoff, which only gains a line) | a reworded finding anywhere else raises its file's count; a fourteenth path is the wall |

### U3 (M, grade l2): one home for all five gates (F7)

Adapter §1 lists five gates: test, build, smoke, corpus-contrast, fonts. The baseline measures three. The adapter carries a time for corpus-contrast that no record measured, arithmetic that does not hold (343 x 3 is 1029; the chain is 343 documents, 3780 palettes, 7560 cells per tone mode, 22680 over three modes), and the baseline says one CI run reports two jobs when it reports four.

#### Steps

1. Quiet host or no run. `uptime` and `sysctl -n hw.ncpu`; the 1-minute load must be under the core count, and `pgrep -fl 'test/run.mjs|curated-contrast|vite|headless'` must show nothing from this repo. The planner could not get this today (load 118 to 311 on 10 cores), which is why no timing in this plan is a figure. Record load before and after every run and the `pgrep` result before it. A run started at load 10 or above is recorded and not used.
2. `git fetch origin && sh .sdlc/checks/baseline-agrees-check.sh`. Two paths:
   - Short path, both `head:` lines `ok`: the tree still equals `ref` outside `.sdlc/`, so the three measured rows stand. Run `npm run gate:corpus-contrast` three times and `npm run gen:type-fonts` three times (neither needs `node_modules`), recording exit, wall seconds (`/usr/bin/time -p`), the last line, `git status --short | wc -l`. Add two rows to the live `## Pass` table under the same `ref`, and one front matter line `extended: <date>, rows corpus-contrast and fonts, host load <figures>`.
   - Full path, a `STALE head` or `STALE tests` line (another plan landed first): re-measure all five gates as records-refresh U3 did (three runs each, `npm ci` once before build), rewrite the live table at the new `origin/main` sha, and move today's live rows under a second labelled prior set. The Orchestrator is told before the first run.
3. Every run made is recorded in the handoff's Runs table. A red run stops the unit: triage with `flaky-gates`, write it under Fail or Flaky, ask.
4. The corpus row's summary cell carries the gate's PASS line, and a four-item list under the table carries the counts line and the three per-mode lines as printed, one list item per line of output, each in its own backtick span (U4's rule; several carry glyphs). Four lines, so U3-4 counts one way only.
5. `## Not run here`: `gh run list --branch main --workflow ci.yml --limit 5 --json databaseId,headSha,conclusion`, take the run on `ref`, then `gh run view <id> --json jobs`. The sentence names every job and its conclusion. At 20298cc that is run 35455937943 and four jobs.
6. `.sdlc/checks/baseline-agrees-check.sh`: one line changes. The gate list in the `for` loop gains `["npm run gate:corpus-contrast", "corpus-contrast"], ["npm run gen:type-fonts", "fonts"]` after the smoke pair. Nothing else in the script moves (measured today: with that one line and simulated rows the script prints nine `ok`).
7. `.sdlc/adapter.md` §1, in place, inside the gate table only: the corpus-contrast Time cell and the fonts Time cell become `A to B s`, the rounded minimum and maximum of the three baseline timings (the script compares them). In the corpus-contrast Green-means cell, `over all 343 curated documents x 3 tone modes = 22680 accent-on-color cells (#674)` becomes `over the full curated corpus in all three tone modes (#674; the measured document, palette and cell counts are in `.sdlc/baseline.md`)`. The adapter carries no corpus count afterwards.
8. `.sdlc/adapter.md` §2, one appended amendment in the file's shape: since #674 a PR runs three jobs, `build-test`, `panda-smoke` and `corpus-contrast`, all of which must report `success`; `deploy` runs on a main push only. The "Two jobs" row above it is history and stays.
9. `.claude/CLAUDE.md`, one line, approved by the owner under adapter conflict row X9 (approval Q4). In §SDLC, `green CI (`build-test` + `panda-smoke`)` becomes `green CI (`build-test`, `panda-smoke`, `corpus-contrast`)`. Nothing else in the file moves. Measured in a clone: `1	1` by numstat, `doc-drift-rows-check.sh` prints the same `rows 56 drifted 11 holds 45 undetermined 0 bad 0` before and after (no §8 row quotes that line), branding clean.
10. `.sdlc/adapter.md` §2.1, the pre-land grade line (#712, folded into this plan and closed by owner ruling of 2026-09-20; its other two lines, `architecture.md:18` and `debt.md:92`, U1 already repaired on this branch). Line 58, item 1, in place: `(reviewer-l3) and `<plan>-prepr-verifier` (verifier-l3)` becomes `(reviewer-l4) and `<plan>-prepr-verifier` (verifier-l3)`, nothing else on the line. Then one paragraph inserted after item 4 (the `**Ticket close.**` line) and its blank line, before `### 2.2`, followed by one blank line, exactly the text plan records-tidy wrote (`git show plan/records-tidy:.sdlc/plans/records-tidy.md`, §Texts, the adapter amendment), with `<date>` the day of the edit: `**Amendment (<date>).** Item 1 said `reviewer-l3` until this date and says `reviewer-l4` now, edited in place. The plugin's `pre-land-review` skill names `reviewer-l4` (fable) beside `verifier-l3` (fable) so that the pre-land pair never shares a model family with any builder grade (l1 to l4 sonnet, l5 to l7 opus; `reviewer-l3` is opus and would share a family with l5 to l7), and l4 is what ran at the records-refresh and k17-rerun pre-lands (`.sdlc/verdicts/records-refresh-prepr.md`, `.sdlc/verdicts/k17-rerun-prepr.md`). This file wins over plugin defaults, so the stricter pair is now the ruled one, not an accident of dispatch. Records written before this date that say `reviewer-l3` for a pre-land are history and were not rewritten.` The k17 record it cites is on `origin/main` at 730ff941, not on this branch, which was cut at d34b4fb1; the cite resolves at landing. Sources: the plugin's `pre-land-review` skill lines 12, 13 and 40; `records-refresh-prepr.md` line 5; `k17-rerun-prepr.md` line 17 on main.
11. `.sdlc/adapter.md` §3, one paragraph appended at the end of the section, after the verbatim-quote paragraph, in the file's shape (U6 review 1 note 1: a reviewer or a corrections row must quote a defective string to name it, and the rule as written reads as forbidding that): `**Amendment (<date>).** Cited-quote clause of the verbatim-quote rule (records-followup U3, from U6 review 1). A quote cited as the subject of a finding is not a carrier: a verdict, review or corrections row that names a defective quotation writes it as the record holds it, marks the span `cited:` next to it, and no count, sweep or restoration reads that span as a quote of the program. The rule sentence on copying from the program governs a quote offered as evidence of what a program prints, not a quote offered as evidence of what a record says.` The paragraph does not carry the words `Verbatim-quote rule (ticket #709`, so U4-1 still counts one.

#### Criteria

| # | Criterion | Command | Expected | Negative control |
|---|---|---|---|---|
| 1 | the script reads five gates and everything agrees | `sh .sdlc/checks/baseline-agrees-check.sh; echo "exit $?"; git diff --numstat $BASE -- .sdlc/checks/baseline-agrees-check.sh` | eight `ok` lines plus one `note head:` line when the tree moved outside `.sdlc/` since the baseline ran (nine `ok` when it has not), among them `time corpus-contrast:` and `time fonts:`, `stale total: 0`, `exit 0`, then `3	3	.sdlc/checks/baseline-agrees-check.sh` (three changed lines: the header comment that described a STALE head line the script can no longer print, the five-gate read, and the head same-tree row printed as `note head:` without counting toward `stale total` or the exit code. The script's own header says a STALE head on a later commit is expected and means the numbers are unproven at this head, not wrong; counting it made the script exit 1 for every plan that changes a file outside `.sdlc`, which would red the post-#691 landing rule on every code plan, #681 first. The `origin/main` ancestry row still counts. Supersedes the by-name exclusion ruled at 7ea9fdda) | measured today in the clone: the script line alone at d34b4fb1 prints `STALE time corpus-contrast: baseline 0 to 0 s, adapter none` and the same for fonts, `stale total: 2`; with rows present and the adapter cell left at another range, one `STALE time corpus-contrast:` line |
| 2 | the adapter's gates and the baseline's live rows are the same five commands | `diff <(awk '/^## 1\. Gates/,/^Rules the gates/' .sdlc/adapter.md \| grep -oE '^[|] [a-z-]+ [|] `[^`]+`' \| grep -o '`[^`]*`' \| sort) <(awk '/^## Pass/,/^## Fail/' .sdlc/baseline.md \| grep -oE '^[|] `[^`]+`' \| grep -o '`[^`]*`' \| sort); echo "diff $?"` (bash) | no diff lines, `diff 0` | at d34b4fb1: two `<` lines, the corpus-contrast and fonts commands, `diff 1` (measured) |
| 3 | the timings were taken on a quiet host and are the builder's own | in `.sdlc/handoffs/records-followup-U3.md`, the Runs table: one row per run (6 on the short path, 15 on the full path), columns #, command, load before, load after, pgrep before, exit, seconds, last line, status lines. `awk -F'[|]' '/^[|] [0-9]+ [|]/{n++; if($4+0>=10 \|\| $5+0>=10) hot++; if($7+0!=0) red++} END{print n+0, hot+0, red+0}'` on it; then the verifier's own single run of each new gate, taken under the same precondition as U3 step 1 (`uptime` first: 1-minute load under the core count and the `pgrep` empty, else the verifier waits and does not run) with its own load before and after written in the verdict | `6 0 0` or `15 0 0`; the verifier's seconds fall within half to double the recorded range (outside it on a quiet host is a 🟡 note; a verifier run started or ended at load 10 or above is not evidence and is repeated in a quiet slot) | a row with load before or load after `11.46` prints `hot` 1 (the before case measured on a fixture: `2 1 0`). The planner's own runs today would fail this row (load 190 and 50), which is why they are not in the baseline |
| 4 | the corpus counts in the baseline are what the gate prints, and the adapter carries none | `npm run gate:corpus-contrast 2>&1 \| grep -o '([0-9]* curated documents[^;]*' > "$F/cc.txt"; wc -l < "$F/cc.txt" \| tr -d ' '; grep -cFf "$F/cc.txt" .sdlc/baseline.md; grep -c '7560 cells' .sdlc/baseline.md; grep -c 'x 3 tone modes\|22680' .sdlc/adapter.md` | `1`, `1`, `3`, `0` (step 4 fixes the layout: one per-mode quote per line) | at d34b4fb1: `1`, `0`, `0`, `1` (measured; the gate's line reads 343 documents, 3780 palettes, 22680 cells) |
| 5 | the CI sentence names the jobs the run reports | `ID=$(grep -o 'CI run [0-9]*' .sdlc/baseline.md \| head -1 \| grep -o '[0-9]*$'); gh run view $ID --json jobs --jq '.jobs[].name' \| sort > "$F/jobs.txt"; wc -l < "$F/jobs.txt" \| tr -d ' '; L=$(awk '/^## Not run here/,/^## Prior/' .sdlc/baseline.md); while read j; do echo "$L" \| grep -c "\`$j\`"; done < "$F/jobs.txt" \| paste -sd, -; grep -c 'both build-test and panda-smoke' .sdlc/baseline.md` | `4`, `1,1,1,1`, `0` | at d34b4fb1: `4`, `0,0,0,0`, `1` (job names measured; the live sentence names two jobs without backticks) |
| 6 | adapter edits stay inside the §1 gate rows, the §2.1 grade line (step 10) and appended amendments (§2, §2.1, §3) | `git diff -U0 $BASE -- .sdlc/adapter.md \| grep -v '^--- ' \| grep '^-' \| grep -vE '^-[\|] (test\|build\|smoke\|corpus-contrast\|fonts) [\|]' \| grep -vc $'(reviewer-l3) and \x60<plan>-prepr-verifier\x60'; awk '/^## 2\. /,/^### 2\.1/' .sdlc/adapter.md \| grep -c 'corpus-contrast'` with `BASE` the plan branch head the unit was cut from (`$'\x60'` is the shell's spelling of the backtick this table cannot carry) | `0`, `1` or more | a reworded rule bullet prints `1` on the first line; at d34b4fb1 the second line is `0`. Steps 10 and 11 simulated at acfd7298: the widened first line prints `0`, the revision 5 form printed `1` (the grade line is a deletion), one line deleted and three non-blank lines added in all |
| 9 | #712's line: adapter §2.1 item 1 names `reviewer-l4` with `verifier-l3`, no `reviewer-l3` remains in §2.1 outside the amendment that records the rename, and that amendment names the skill | `awk '/^### 2\.1/,/^### 2\.2/' .sdlc/adapter.md \| grep -v '^\*\*Amendment (' \| grep -c 'reviewer-l3'; awk '/^### 2\.1/,/^### 2\.2/' .sdlc/adapter.md \| grep -c '(reviewer-l4) and'; awk '/^### 2\.1/,/^### 2\.2/' .sdlc/adapter.md \| grep -cE '^\*\*Amendment \(20[0-9]{2}-[0-9]{2}-[0-9]{2}\)\.\*\* Item 1 said .reviewer-l3. until this date and says .reviewer-l4. now.*pre-land-review'` | `0`, `1`, `1` (step 10 simulated at acfd7298: measured) | at main 730ff941 and at acfd7298 alike: `1`, `0`, `0` (measured; the one hit is line 58). A copy that inserts the amendment and keeps line 58 at l3 prints `1`, `0`, `1` (measured), so the amendment alone does not pass |
| 10 | the cited-quote clause is in §3, once, and the verbatim-quote paragraph is still the only home of the rule | `awk '/^## 3\. /,/^## 4\. /' .sdlc/adapter.md \| grep -c 'cited as the subject of a finding is not a carrier'; grep -c 'cited as the subject of a finding is not a carrier' .sdlc/adapter.md; grep -c 'Verbatim-quote rule (ticket #709' .sdlc/adapter.md` | `1`, `1`, `1` (step 11 simulated at acfd7298: measured) | at acfd7298: `0`, `0`, `1` (measured). The clause pasted into §2 instead prints `0`, `1`, `1` |
| 8 | the approved `CLAUDE.md` line names the jobs `ci.yml` runs on a PR, and it is the only line that moved | `perl -ne 'print "$1\n" if /^  ([a-z][a-z-]+):\s*$/' .github/workflows/ci.yml \| grep -vx 'push\|pull_request\|deploy' \| LC_ALL=C sort \| paste -sd, -; grep -c 'green CI (`build-test`, `panda-smoke`, `corpus-contrast`)' .claude/CLAUDE.md; git diff --numstat $BASE -- .claude/CLAUDE.md \| cut -f1,2; sh .sdlc/checks/doc-drift-rows-check.sh \| tail -1` | `build-test,corpus-contrast,panda-smoke`, `1`, `1	1`, `rows 56 drifted 11 holds 45 undetermined 0 bad 0` | at d34b4fb1: the same job list, `0`, nothing, the same rows line (measured). A second edited line prints `2	2` |
| 7 | regression: U1's and U4's repairs survived the baseline edit | U1-4, U1-5, U4-3 | as in those rows | the full path rewrites `baseline.md` whole, which is how records-refresh lost a host line once; these three rows are the catch |

### U5 (S, grade l2): the roadmap, regenerated, alone (F5)

Built on `plan/records-followup-roadmap`, cut from `origin/main` after the first PR lands. One commit touches `.sdlc/roadmap.md` and that commit touches nothing else; the handoff is a second commit.

#### Texts

Regenerate, do not patch. Every row is read from `gh issue list --state open`, `gh pr list --state open`, `git worktree list`, `.sdlc/board.md` and the plans on main, at one `origin/main` sha, and the front matter's `head:` names that one sha and nothing else. Keep the file's sections and its cell-mark legend. Specifically:

- The ranked table has one row per open issue and none for a closed one. Landed work moves to a short `## Landed since the last revision` list (ticket, PR, squash sha).
- `## In flight outside sdlc` lists exactly the worktrees under `.git-worktrees/` that `git worktree list` prints, and its lead sentence gives the count as a digit.
- The `Your move` line goes. Open owner questions move to `.sdlc/questions/roadmap-<date>.md` only if the Conductor confirms they are still open; the roadmap then carries one pointer line per question. The Conductor owns this file (its own front matter says so), so the builder's handoff lists each question with its proposed state and the Conductor rules before the verifier runs.
- Priorities the owner has not ranked stay marked `(p)`. The builder ranks nothing new.
- The `## Unticketed debt` table names the Process rows by their ids after U7, `DP3` and not `P3 (debt id, ...)`.
- A revision row records the regeneration, its sha, and ticket #709.

#### Criteria

Live facts move while the other session works. A difference the verifier finds is 🔴 unless the differing worktree or issue was created after the unit's commit time (`stat -f %SB .git/worktrees/<name>`, the issue's `createdAt`); that case is a 🟡 note and the Orchestrator refreshes the row at landing.

| # | Criterion | Command | Expected | Negative control |
|---|---|---|---|---|
| 1 | one head sha, and it is on main | `sed -n '/^head:/p' .sdlc/roadmap.md \| perl -ne 'print "$1\n" while /(?<![0-9a-f])([0-9a-f]{7,40})(?![0-9a-f])/g' \| sort -u > "$F/h.txt"; wc -l < "$F/h.txt" \| tr -d ' '; git merge-base --is-ancestor $(cat "$F/h.txt") origin/main; echo "anc $?"` | `1`, `anc 0` | at d34b4fb1 the head line names two shas: `2`, and the ancestor test errors on two arguments |
| 2 | the worktree table is the live set | `diff <(git worktree list \| grep -oE '\.git-worktrees/[^ ]+' \| sort) <(grep -oE '^[\|] `\.git-worktrees/[^`]+`' .sdlc/roadmap.md \| tr -d '\|` ' \| sort); echo "diff $?"` (bash, from the root checkout) | no lines, `diff 0` | at d34b4fb1: four `>` lines and two `<` lines, `diff 1` (measured, names in the facts table) |
| 3 | the stated count is the row count | `grep -oE '^[0-9]+ worktrees under' .sdlc/roadmap.md \| grep -o '^[0-9]*'; grep -cE '^[\|] `\.git-worktrees/' .sdlc/roadmap.md` | the same number twice | at d34b4fb1: an empty line (the count is the word Nine) and `8` |
| 4 | one ranked row per open issue, none for a closed one | `diff <(gh issue list --state open --limit 200 --json number --jq '.[].number' \| sort) <(grep -oE '^[\|] [0-9]+ [\|] #[0-9]+' .sdlc/roadmap.md \| grep -oE '#[0-9]+' \| tr -d '#' \| sort); echo "diff $?"` | no lines, `diff 0` | at d34b4fb1: six `>` lines (#638 #672 #673 #674 #676 #691) and three `<` lines (#686 #701 #709), `diff 1` (measured) |
| 5 | no unanswered prompt is left in the file | `grep -c '^Your move' .sdlc/roadmap.md; awk '/^## Questions for the owner/,/^## Revisions/' .sdlc/roadmap.md \| grep -E '^[0-9]+\. ' \| grep -vcE 'answered 20[0-9-]+\|open: \.sdlc/questions/'` | `0`, `0` | at d34b4fb1: `1`, `2` |
| 7 | the roadmap cites debt ids as `debt.md` defines them after U7 | `grep -c 'debt id' .sdlc/roadmap.md; awk '/^## Unticketed debt/,/^## Label/' .sdlc/roadmap.md \| grep -c 'DP[1-4]'` | `0`, `1` or more | at d34b4fb1: `1`, `0` |
| 6 | alone: every commit that touches the roadmap touches only the roadmap, and the branch touches nothing outside `.sdlc/` (revision 14, owner ruling R5: the count is of roadmap-only commits, not of commits, so the landing refresh for #718 is admitted by the rule rather than exempted from it; revision 15: run this row under bash, as rows 2 and 3 already say, because `for c in $C` word-splits in bash and not in zsh, where the decisive leg prints nothing and errors) | `C=$(git log --format=%H $BASE..HEAD -- .sdlc/roadmap.md); test -n "$C"; echo "nonempty $?"; for c in $C; do git show --name-only --format= $c; done \| LC_ALL=C sort -u \| paste -sd, -; git diff --name-only $BASE \| LC_ALL=C sort \| paste -sd, -; bash -c 'set -o pipefail; node test/repo/branding.mjs \| tail -1; echo "exit $?"'; git diff -U0 $BASE -- .sdlc \| grep '^+' \| perl -pe 's/\x60[^\x60]*\x60//g' \| LC_ALL=C grep -o "$EM" \| wc -l \| tr -d ' '` | `nonempty 0`; `.sdlc/roadmap.md` and nothing else on that line; the roadmap, `.sdlc/handoffs/records-followup-U5.md` and at most one `.sdlc/questions/roadmap-<date>.md`; `branding: clean (N files scanned)`, `exit 0`; `0` | with no roadmap commit `C` is empty, `wc -l` still prints `1`, and `nonempty 1` is what reds the row. A second file staged with the roadmap shows in the second output; PR #708 is the case this row exists for |

## Risks and assumptions

| Risk | Handling |
|---|---|
| No quiet slot comes (load was 118 to 311 on 10 cores at plan time) | U3 is last of the first PR's units and the only one that waits. If no slot comes in the session, the Orchestrator asks the owner whether to land U1, U2, U4, U6 and carry U3 into its own PR; nothing is measured under load to get done |
| Another plan lands first and raises `TESTS` or moves the tree (adia-library-uplift goes 48 to 49) | that plan repairs the baseline figure itself by the ruled landing rule. U3 step 2 then reads `STALE head` and takes the full path; P7 at pre-land is the second catch |
| The review's scratchpad copy is cleaned up before U6 runs | the Orchestrator copies the file into the unit worktree when it mobilizes the plan, not when U6 starts; U6-2's hash is the proof either way. Without the file U6 stops and asks |
| The fonts gate rounds to `0 to 1 s` or `1 to 1 s` and reads oddly | accepted: the script compares rounded extremes, and an odd cell that is measured beats a tidy one that is not |
| A restored quote carries a glyph the owner's prose rule bans | that is U4's rule working; P6 strips backtick spans. A quote outside a backtick span still counts, which keeps the exemption narrow |
| A plan or skill outside `.sdlc/` still counts raw em dashes and reds a verbatim quote | none found today (no test, script, hook or workflow counts them). If one appears it cites the adapter rule or is fixed; not this plan's to pre-empt |
| The plugin later reserves a two-letter prefix, or reads two-letter ids | `XC` and `DP` are unowned in the plugin's `ID_OWNERS` on both `main` and `plan/drill-findings` (measured); U2-6 and U7-6 rerun the plugin's own check |
| Corrections appended to verdicts and a handoff break a parser | `board.py check` reads the board and `plans/*.md` only (measured in its source); U6-5 runs it |
| The in-place adapter edits (U1's bullet, U3's cells) break the append-only habit | the archived plan ruled the habit was a criterion, not an adapter rule; U1-6 and U3-6 confine deletions to the named lines |
| Plan-branch shas cited by this plan's verdicts die at the squash | accepted as before; every record this plan writes into a live file cites a main sha (U1-5 enforces it for the baseline) |
| Two conductor sessions drive one branch again, which is how #708 landed unanswered | the ticket's process finding. This plan names one driver (this conductor session with Lane B). The adapter's gate already refuses a stale pre-land sha; the missing piece was that nobody ran it. Landing below makes the dry run a named step with its output pasted into the PR body |

## Not in scope, and why

| Item | Why |
|---|---|
| The 12 shared ids left after U2 and U7: debt and survey `C1` to `C7`, five K rows | the K rows are keyed to their convention on purpose. The C rows were ruled the owner's call in the archived plan, and approval Q3 took only the P rows into this plan |
| Any other line of `.claude/CLAUDE.md` | approval Q4 covers one line; row X9 stands for the rest |
| Rewriting the archived plan's criterion text or its lines 306 and 342 | history; U6 records the correction beside it and appends one revisions row |
| Rewriting X1 to X6 in `.sdlc/verdicts/architecture.md`, or debt P cites in the archived plans, `.sdlc/tickets/T-0001.md` and the adopt-hygiene verdicts | history; the notes under architecture §4 and debt §Process carry the maps |
| Extending `scripts/audit-citations.mjs` to scan `.sdlc/` (the review noted the citation audit is vacuous there) | a `scripts/` change behind the scope wall and a gate-design decision; worth its own ticket |
| Re-deriving `architecture.md` and `debt.md` | owner ruling Q2 of 2026-09-18 stands |
| A hook or test that enforces the verbatim-quote rule | the rule is one day old; a gate for it is a `test/` change. P6's measure is a plan criterion until the owner asks for more |
| The five stale `/private/tmp` critic worktrees `git worktree list` shows | repo hygiene, the repo-cleaner seat's proposal to make |

## Landing

Two PRs (approval Q2), because the owner ruled the roadmap lands alone and this repo squash-merges: one PR is one commit on main.

PR 1, `plan/records-followup` to `main`, carrying this plan, the approval doc, the owner's standing rulings of the same day (`.sdlc/questions/standing-rulings-2026-09-20.md`, R1 to R4, which govern how this PR and #681 land), U1, U2, U7, U4, U6, U3, U8, U9, U10 and their handoffs and verdicts. Title `chore(sdlc): repair the records-refresh pre-land findings (#709)`. Pre-land per adapter §2.1 as U3 amends it (#712): reviewer-l4 and verifier-l3 write `.sdlc/verdicts/records-followup-prepr.md` with `verdict: 🟢` and the branch head sha. Pre-land runs P1 to P7 (P3 only on U3's full path), and reruns U2-1, U7-1, U7-4, U3-1, U3-8 and U4-2 at the branch head. P7 is the landing rule the Conductor ruled on 2026-09-19 for every plan landing after #691: the baseline's test-file figure equals `TESTS.length` at the pre-land head, and `baseline-agrees-check.sh` is a pre-land gate; a plan that lands in between and changes the count repairs the figure itself. Then `adapter.py land --branch plan/records-followup --gate .sdlc/verdicts/records-followup-prepr.md --dry-run`, its output pasted into the PR body, so an unanswered pre-land gate cannot pass unseen again. CI watched to `success` for `build-test`, `panda-smoke` and `corpus-contrast`. Squash per `shipping-changes` step 7, sync per step 8. The owner approved this plan on 2026-09-19 (approval Q1); the first push is the Orchestrator's draft PR step, not the planner's.

PR 2, `plan/records-followup-roadmap` to `main`, carrying U5 only. Title `chore(sdlc): regenerate the roadmap (#709)`. Its pre-land record is `.sdlc/verdicts/records-followup-roadmap-prepr.md`; its gates are U5-1 to U5-7, P4, P5 (second line `1`), P6 and P7. `npm test` is not rerun for a one-file `.sdlc/` change unless the branding gate reds.

Close per adapter §5 after PR 2: status `done`, revision row, file moved to `.sdlc/plans/archive/`, ticket #709 closed, board rows 🟢 under `Seat: orchestrator`.

## Revisions

| Date | Change | Why |
|---|---|---|
| 2026-09-19 | plan written (proposed) from ticket #709, the pre-land review and its four rulings, and the owner rulings of 2026-09-19. Every value marked measured was run by the planner at d34b4fb1 in a scratch worktree or a throwaway clone; simulated values had the unit's edit applied by script in the clone. No timing was taken: host load was 118 to 311 on 10 cores. `npm run build` and `npm run smoke` were not run | records-followup dispatch, team lead |
| 2026-09-19 | revision 2, approved. Status `approved` with the approval doc copied byte for byte to `.sdlc/questions/records-followup-approval.md` in the same commit. The 11 🟡 gaps of the checkability review (graded at 407d33b9: 32 🟢, 11 🟡, 0 🔴 of 43) folded into P1, U1-3, U1-6, U2-3, U4-5, U4-6, U6-1, U6-5, U3-3, U3-4 and U5-6, every measured value kept; its cross-cutting notes 1 and 2 folded into the criteria preamble (`F` under the seat's own scratchpad, `UB` for a unit's file list). U7 added (approval Q3): debt P1 to P4 become DP1 to DP4, six criteria, after U2. Owner answers recorded in the body and the Open questions section removed: two PRs (Q2), one approved `.claude/CLAUDE.md` line inside U3 with new row U3-8 and P5 widened to allow exactly that line (Q4). U5-7 added so the regenerated roadmap cites `DP3`. Size 8 points. Planner measured the U7 simulation, the `CLAUDE.md` edit against `doc-drift-rows-check.sh`, the `TESTS` length command, the `XC` plant and the `board.py` path at d34b4fb1; no `npm test`, build or smoke was run (host load) | approval Q1 to Q4, checkability review, revision 2 brief |
| 2026-09-19 | revision 3, after U4 review 1 (reviewer-l2 at ba0a66d6, FIX-FIRST; rulings by the Conductor, all three as recommended). F1: U4-1, U4-3 and U4-5 exclude the unit's own handoff, which the rule obliges to quote both program lines; U4-5's expected count re-derived with the exclusion. F2: the two altered smoke quotes the review found outside `plans/` (`verdicts/survey.md:11`, hyphen-minus; `handoffs/records-refresh-U1.md:26`, backtick-then-colon) folded into U4 as restorations six and seven with their Correction lines; U4-3 widened to four `-e` forms, which also repairs revision 2's pattern (its pipe, typed unescaped, is a literal in a basic regex, so it printed `0` even at d34b4fb1); U4-4 and U4-6 count five history files and eight paths. F3, F4, F5: three sentences added to the rule text (raw count re-derived per unit, a third `altered:` case for an unrepresentable glyph, leading whitespace kept or its loss marked), and the U1 verdict's gate quote gains `altered: two leading spaces dropped`. F6: the U3 verdict's Correction line now says Note 2 no longer holds and why. Every changed expected value measured by the planner at ba0a66d6 in a throwaway clone with the exclusion applied, the two extra restorations simulated by script: U4-1 `.sdlc/adapter.md`; U4-3 `2` files before, `0` after; U4-5 `7` before the fold, `9` after; U4-6 `1,2,4,1,1`; branding clean at 471 files. No `npm test`, build or smoke was run | U4 review 1 F1 to F6, Conductor rulings 1 to 3 |
| 2026-09-19 | revision 4, after U4 review 2 (reviewer-l2 at b9e70950, PASS with seven 🟡 notes; brief from the Conductor). U6 carries all seven: §Texts enumerates each by path and line, and the corrections file gains a second table of seven `N1` to `N7` rows. Notes 1 and 2 are restorations under the verbatim-quote rule: the runner line at `verdicts/records-refresh-U1.md:37` (derived from `test/run.mjs:23` and `:29`) and smoke's missing-artifact line at `verdicts/survey.md:11`, `verdicts/records-refresh-checkability.md:12` and `verdicts/records-refresh-prepr.md:25` (from a run, the absolute path written as `<ROOT>` and marked); this plan's own P3 control cell marked the same way. Notes 3 to 5 edit the amendment in place (the count comes from the enumerated lines and the program, never the diff; inline spans only; the rule binds lines added from 2026-09-19 on); note 6 rewords two Correction lines; note 7 appends a Correction to U4's handoff. U6-4 counts the seven rows; U6-6 to U6-9 added. Every changed expected value measured by the planner at b9e70950 in a throwaway clone with the U6 edits applied by script: verbatim hits `1` and `1,1,1`, old forms `0`, markers `1,1,1`, amendment phrase grep `1`, raw dashes `4` (three restored spans and the re-added line 37), stripped `0`, branding clean at 473, U4-3 `0`, U4-4 five ones, U6-9 counts `1,1,1,1,1,1,1,0`. The pre-land review sha256 row is unchanged. No `npm test`, build or smoke was run | U4 review 2 notes 1 to 7, revision 4 brief |
| 2026-09-19 | revision 5 (Conductor ruling from U6 review 1). The rule clause counts dashes and the four count commands counted lines, so a dash added to a line already counted moved neither number. P6, U4-5, U6-8 and U5-6 now count occurrences (`grep -o "$EM" \| wc -l \| tr -d ' '`). Re-measured in a throwaway clone: U4-5 at b9e70950 with the U4 handoff excluded `9` occurrences, breakdown unchanged (baseline 1, records-refresh-U1 handoff 1, records-refresh-U3 handoff 4, U1 verdict 1, U3 verdict 1, survey 1); U6-8 at 3ff96951 with the U6 handoff excluded `5` occurrences on `4` lines (U1 verdict 1, checkability 1, prepr 1, survey 2, since `survey.md:11` carries the smoke line U4 restored and the missing-artifact line U6 restores), P6 stripped `0`; the P6 fixture gains a two-dash prose line (`2` as occurrences, `1` as lines; three lines `3`, `4` unstripped); U5-6 pre-state at e0e5b551 `0`. No `npm test`, build or smoke was run | U6 review 1, revision 5 brief |
| 2026-09-20 | revision 6 (owner ruling: #712 records-tidy folded into #709 and closed; its `architecture.md:18` and `debt.md:92` lines U1 already repaired here). U3 gains steps 10 and 11: adapter §2.1 item 1 says `reviewer-l4` with `verifier-l3` (the plugin skill's pair, what ran at the records-refresh and k17-rerun pre-lands) with records-tidy's amendment paragraph verbatim, and a §3 amendment adds the cited-quote clause U6 review 1 asked for (a quote cited as the subject of a finding is not a carrier). U3-6's wall admits the grade line; U3-9 and U3-10 added; the Landing paragraph names the l4 pair. Measured by the planner at acfd7298 in a throwaway clone with both edits applied by script: U3-9 `1`, `0`, `0` before (the same at main 730ff941) and `0`, `1`, `1` after, the keep-l3 copy `1`, `0`, `1`; U3-10 `0`, `0`, `1` before and `1`, `1`, `1` after; U3-6 widened `0` (revision 5 form `1`), one deleted and three added lines; U4-1 third leg still `1`; prose dashes `0`; branding clean at 477. No `npm test`, build or smoke was run | owner ruling on #712, U6 review 1 note 1, revision 6 brief |
| 2026-09-20 | U3-1 design change (commits bc34b5fb, 31b53ea9; not numbered at the time): the check script's head same-tree row prints as `note head:` and never counts; the ancestry row still counts; U3-1's numstat is `3 3`. Supersedes the by-name exclusion of `.claude/CLAUDE.md` ruled at 7ea9fdda | U3 review: the script's own header calls a STALE head on a later commit expected, and counting it would red the post-#691 landing rule on every code plan |
| 2026-09-20 | revision 7 (3e4ea5e4): U8 checklist row, the pre-land fix for the inverted `note head:` sentence, four inline criteria; the long header comment ruled debt | the U3 verdict's must-fix note; wrapping the header would break U3-1's `3 3` |
| 2026-09-20 | revision 8 (2e7a4c66, corrected to P1 to P7 at 2c622ad2): U9 checklist row, `origin/main` merged in, never rebased, five record hunks each settled by re-measuring | main moved to 3ce50daa (#711, #714); the board, verdicts and handoffs cite this branch's shas, which a rebase would stale |
| 2026-09-20 | revision 9: U4-1's path list excludes `.sdlc/verdicts`; the U3 verdict's false `still counts one` clause is corrected in the same commit | the U9 verdict: the U3 verdict cites the rule's opener while grading U3-10, so the row printed two paths, and the cited-quote clause rules a cited span is not a home |
| 2026-09-20 | revision 10: frontmatter `size:` and `amended:` brought up to nine units; Revisions rows written for the U3-1 design change and revisions 7 and 8; row 9 given its Why cell; `.sdlc/handoffs/conductor-resume-2026-09-20.md` removed from the branch (a session note, not a record: it said rebase where U9 rules merge and cited the superseded by-name exclusion); Landing names `.sdlc/questions/standing-rulings-2026-09-20.md` as a record this PR carries | pre-land review at 887e3eb2, findings 3 and 5 |
| 2026-09-20 | revision 11: U7-4's expected third figure is `22,52,95,` (it was 91 until U3 appended its adapter amendments, four lines above the cite); `head:` says what `BASE` reads since U9; U6-6's marker leg prints `1,1,1,1` at the head because revision 9's cited span adds a fourth match, the fact it grades holds | pre-land verifier-l3 at 887e3eb2, findings 1 and 2 |
| 2026-09-20 | revision 12: U4-5's second leg is frozen at U4's graded span, 07ecb44c to b9e70950; U10 writes one §3 sentence exempting a byte-pinned verbatim copy of another seat's record from the sweep, and criterion 1 excepts such copies with the hash recomputed | U10 review 1 at 702356e7: `$UB` on a later head reads 15 and can never read 9 again; the copied pre-land review is sha-pinned and quotes defective forms, so it cannot obey the rule and keep its hash |
| 2026-09-20 | revision 13: U1-4's third needle follows U10's reworded baseline sentence (`#699 (PR #702, 9a44f685) touched only files under`), still expecting `1`; noted that since revision 12 U4-5's second leg compares two fixed shas and is a record of U4's grade, not a live control: the live dash control is P6 | U10 verdict at 23a206b4: U3-7 names U1-4 as a regression row and its third leg printed `0`; U10 made the sentence true (finding 4), so the needle moves with it |
| 2026-09-20 | PR 1 landed: #716, squash `e9850935` on `main`, under standing ruling R1 (pre-land 🟢 at 3c755098, CI green and critic ACCEPT at 2430fd43). U5 is still open, so the plan stays active and closes per adapter §5 after PR 2 | Orchestrator close-out, board rows to landed |
| 2026-09-20 | revision 14, owner ruling R5 (question `.sdlc/questions/records-followup-U5-refresh.md`, options offered: U5-6 counts roadmap-only commits (recommended), grade U5-6 at the unit head, no refresh; chosen: the first). Issue #718 opened `2026-09-20T21:45:38Z`, after U5's roadmap commit, so U5-4 wants a ranked row for it while U5-6 as written demanded exactly one roadmap commit, and at the branch head one of the two had to fail. U5-6 now counts roadmap-only commits: every commit touching `.sdlc/roadmap.md` must touch only it, and there may be more than one. The landing refresh that adds #718 is therefore admitted by the criterion, not exempted from it. U5-6's own negative control still bites, since a second file staged with the roadmap still shows | the U5 verdict's 🟡 row U5-4 and the live-facts rule |
| 2026-09-20 | revision 15, from the U5 pass 2 verdict's 🟡 on U5-6: the row is met but was shell-dependent. `for c in $C` word-splits under bash and not under zsh, the login shell here, where the decisive leg prints nothing and exits with an ambiguous-argument error; the Orchestrator hit exactly that before rerunning it with a `while read` loop. Row 6 now carries the same `(bash)` annotation rows 2 and 3 already carried, so the row is read the way it was measured. What the row measures is unchanged | U5 pass 2 verdict, row U5-6 |
| 2026-09-20 | revision 16, U11 added after the roadmap PR's pre-land review returned 🔴 on two rows. R1 is a false readiness claim about #713 that was already false when the roadmap was written; R2 is the header counts left at 10 over a 13-row table by the Orchestrator's own landing refresh, which added three rows and moved no count. The root cause is that U5's criteria grade the roadmap's shape and never its assertions about other work, so U11 carries a criterion that re-derives every stated count and every readiness claim from its subject. Both fixes are roadmap-only commits, which U5-6 as reworded in revision 14 admits | the roadmap PR's pre-land review |
