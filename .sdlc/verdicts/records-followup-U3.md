# Verdict U3 · 🟢
verdict: 🟢

Head graded: `ceb471b0d89c0cbfc8a13386473a975064172124` (unit/rf-U3). Criteria: `.sdlc/plans/records-followup.md` at `31b53ea9ca8fe8be728649d860b6a35dce79b023`, U3 rows 1 to 10 plus P1, P4, P5, P6, P7. `BASE` is `$(git merge-base origin/main HEAD)` = `d34b4fb1beefff11c9be53ec039d4265c925da4a`. `UB` is `$(git merge-base plan/records-followup HEAD)` = `31b53ea9ca8fe8be728649d860b6a35dce79b023`. Worktree detached at the head under this seat's scratchpad; every file-editing control ran in a throwaway clone.

| # | Criterion | State | Evidence | Negative control |
|---|---|---|---|---|
| U3-1 | the script reads five gates and everything agrees | 🟢 | eight `ok` lines, among them `ok    time corpus-contrast: baseline 20 to 23 s, adapter 20 to 23 s` and `ok    time fonts: baseline 1 to 1 s, adapter 1 to 1 s`, then `note  head: baseline ref 20298cc has the same tree as HEAD outside .sdlc/ and .gitignore`, `stale total: 0`, `exit 0`; `git diff --numstat $BASE` prints `3	3	.sdlc/checks/baseline-agrees-check.sh`, and the diff is exactly the header line, the five-gate `for` list and the head row turned into a `console.log` | clone with `ref:` set to `b50a4b9b`: `STALE head: baseline ref b50a4b9b is in origin/main's history`, `stale total: 1`, `exit 1`. Clone with `.claude/CLAUDE.md` restored to the `20298cc` text and committed: the head row prints `ok` and the run has nine `ok` lines, `exit 0`, so the note leg discriminates |
| U3-2 | the adapter's gates and the baseline's live rows are the same five commands | 🟢 | the `diff` prints no lines and `diff 0`; the sorted lists are the five commands `npm test`, `npm run build`, `npm run smoke`, `npm run gate:corpus-contrast`, `npm run gen:type-fonts` on both sides | same command in a clone reset to `d34b4fb1`: `2,3d1`, `< \`npm run gate:corpus-contrast\``, `< \`npm run gen:type-fonts\``, `diff 1` |
| U3-3 | the timings are the builder's own, taken quiet | 🟢 | the awk prints `6 0 0`, and it reads the Runs table: the six matching lines are handoff lines 31 to 36, field 4 is `3.97`, field 5 is `4.84`, field 7 is `0` on row 1, so load and exit are really read. The Steps table's eleven rows are lettered `S1` to `S11` and match nothing. My own runs, one per gate, all exit 0, host 10 cores, 1-minute load 6.90 to 7.82 throughout and the only repo process in `pgrep` the same idle vite server pid 47201 the builder recorded: see the timing table below | a clone with handoff line 31's load-before set to `11.46` prints `6 1 0`; line 32's exit cell set to `1` prints `6 0 1`. Both legs bite |
| U3-4 | the corpus counts in the baseline are what the gate prints, and the adapter carries none | 🟢 | from my own gate run: `1`, `1`, `3`, `0`. The line the gate printed is `(343 curated documents, 3780 palettes (3420 carrying a non-zero lift or skew), 22680 accent/on-color cells` and it is in `.sdlc/baseline.md` verbatim | the same four commands against a clone at `d34b4fb1`, reusing my own gate output: `1`, `0`, `0`, `1` |
| U3-5 | the CI sentence names the jobs the run reports | 🟢 | `4`, `1,1,1,1`, `0`. `gh run view 35455937943` reports `build-test`, `corpus-contrast`, `deploy`, `panda-smoke`, each `success`, and the baseline's Not-run-here sentence names all four with that wording | clone at `d34b4fb1`: `0,0,0,0` and `1` |
| U3-6 | adapter edits stay inside the §1 gate rows, the §2.1 grade line and the appended amendments | 🟢 | `0`, `1`, with `BASE` read as the row defines it, the plan-branch head the unit was cut from, which is `UB` = `31b53ea9`. Against `d34b4fb1` the first leg prints `3`, and all three lines are U1's and U7's earlier deletions on the plan branch, not U3's | the revision 5 form without the grade-line exclusion prints `1` on this tree, so the widening is load-bearing. A clone with one §1 rule bullet reworded prints `1`. The second leg at `d34b4fb1` prints `0` |
| U3-9 | #712's line: §2.1 item 1 names `reviewer-l4` with `verifier-l3` | 🟢 | `0`, `1`, `1`. The amendment appended after item 4 is byte-identical to the text records-tidy wrote: `git show origin/plan/records-tidy:.sdlc/plans/records-tidy.md` §Texts with `<date>` filled in diffs clean against the adapter's line, both 746 bytes. The commits that made the edit are dated `2026-09-19`, so the amendment's date is the day of the edit | clone at `d34b4fb1` and at `origin/main` 730ff941 alike: `1,0,0`. Clone with the amendment kept and item 1 reverted to `(reviewer-l3) and`: `1,0,1`, so the amendment alone does not pass the row |
| U3-10 | the cited-quote clause is in §3, once | 🟢 | `1`, `1`, `1`. The clause is appended after the verbatim-quote paragraph inside §3 and does not carry the words `Verbatim-quote rule (ticket #709`, so the adapter still holds the rule once (U4-1's second and third figures) | clone at `d34b4fb1`: `0,0,0`. Clone with the clause moved out of §3 and pasted above `### 2.1`: `0`, `1`, `1`, exactly the stated control |
| U3-8 | the approved `CLAUDE.md` line names the jobs `ci.yml` runs on a PR, and it is the only line that moved | 🟢 | `build-test,corpus-contrast,panda-smoke`, `1`, `1	1`, `rows 56 drifted 11 holds 45 undetermined 0 bad 0`. The diff is the single line `green CI (\`build-test\`, \`panda-smoke\`, \`corpus-contrast\`)`. `ci.yml:110` gates `deploy` on `github.event_name == 'push' && github.ref == 'refs/heads/main'`, so the three named jobs are exactly the PR set | clone at `ceb471b0` with a second `CLAUDE.md` line edited: `2	2`. Clone at `d34b4fb1`: the grep prints `0` and the drift line is the same `rows 56 drifted 11 holds 45 undetermined 0 bad 0`, so that leg is not what carries the row |
| U3-7 | regression: U1's and U4's repairs survived the baseline edit | 🟢 | U1-4 `(#699) (#702)`, `0`, `1`; U1-5 `0`; U4-3 `1`, then `.sdlc/baseline.md:1`, `.sdlc/handoffs/records-refresh-U1.md:1`, `.sdlc/handoffs/records-refresh-U3.md:4`, `.sdlc/verdicts/records-refresh-U3.md:1`, `.sdlc/verdicts/survey.md:1`, then `0` | clone at `d34b4fb1`: U1-4 prints `1`, `0`; U1-5 prints `NOT b50a4b9b`; the widened U4-3 grep names `5` files |

## Plan-level rows

| # | Criterion | State | Evidence | Negative control |
|---|---|---|---|---|
| P1 | `npm test` green with no `node_modules`, tree byte-stable | 🟢 | run with no `node_modules` present in the worktree: `✓ all 48 test files passed`, the perl count of `TESTS` prints `48`, `git status --short \| wc -l` prints `0` | the plan's measured control, `"scrim` to `"scrimX` in the role table, is recorded at `exit 1` and `3`; not re-run here, since the falsifier for this row is the pass line and the count agreeing, both taken fresh |
| P2 | `npm run build` green and prints the size the live baseline row records | 🟢 | pre-land only, run anyway: after `npm ci` (exit 0, untimed), `exit 0`, `ui.html 3780.5 KB` from the build log and `ui.html 3780.5 KB` from the baseline row, `git status --short` prints `0` | the uncorrected whole-file grep still prints two different strings, which is the archived form's defect; no `node_modules` gives exit 127 |
| P3 | `npm run smoke` green | 🟢 | pre-land only, run anyway: `exit 0`, `grep -c 'SMOKE PASS'` prints `1`, and the printed line is `SMOKE PASS — gallery · category · editor · export dialog all render in a real browser`, byte for byte the string `.sdlc/baseline.md:21` holds | not re-run; the plan's control is a missing `dist/` |
| P4 | branding gate clean | 🟢 | `branding: clean (479 files scanned)`, `exit 0` | clone at `ceb471b0` with `decision-records.md` copied to `.sdlc/verdicts/x.md`: `FAIL: 3 branding violation(s) across 480 files`, so the gate still scans `.sdlc/verdicts/` and exempts nothing there |
| P5 | scope wall | 🟢 | `0`, `0`, `1	1`. Against `UB` the unit touches five paths: `.claude/CLAUDE.md`, `.sdlc/adapter.md`, `.sdlc/baseline.md`, `.sdlc/checks/baseline-agrees-check.sh`, `.sdlc/handoffs/records-followup-U3.md` | clone with a line appended to `src/engine/geometry.mjs`: first leg `1`. Clone with a line appended to `.sdlc/roadmap.md`: second leg `1`. Clone with a second `CLAUDE.md` line edited: third leg `2	2` |
| P6 | no em dash added in prose | 🟢 | `0` against `BASE` and `0` against `UB` | the plan's three-line fixture, rebuilt here: with the backtick strip it counts `3`, without it `4`, so the strip and the occurrence count both bite |
| P7 | the baseline's test-file figure equals `TESTS.length`, checked by script at the head | 🟢 | `stale total: 0`, `exit 0`, and the first line reads `ok    tests: baseline 48, test/run.mjs TESTS 48` | same as U3-1: a `ref` off `origin/main` gives `stale total: 1` and `exit 1` |

## My own gate runs

One run of each of the five gates in a detached scratch worktree at `ceb471b0`, timed with `/usr/bin/time -p`. Host 10 cores. `npm test` ran before `npm ci`, so with no `node_modules`; build and smoke after it. The only process from this repo in `pgrep -fl 'test/run.mjs|curated-contrast|[v]ite|headless'` throughout was pid 47201, the same idle vite dev server the builder recorded. No run started or ended at load 8 or above, so none was repeated.

| gate | baseline's three | window, half to double | mine | load before | load after | exit |
|---|---|---|---|---|---|---|
| `npm test` | 56.27 · 56.43 · 59.83 | 28.14 to 119.66 | 115.26 | 6.90 | 7.37 | 0 |
| `npm run build` | 3.06 · 1.34 · 1.36 | 0.67 to 6.12 | 3.74 | 5.40 | 5.21 | 0 |
| `npm run smoke` | 18.20 · 18.28 · 18.25 | 9.10 to 36.56 | 18.81 | 4.87 | 4.51 | 0 |
| `npm run gate:corpus-contrast` | 20.12 · 22.89 · 22.29 | 10.06 to 45.78 | 34.53 | 7.48 | 7.82 | 0 |
| `npm run gen:type-fonts` | 0.77 · 0.78 · 0.70 | 0.35 to 1.56 | 0.92 | 7.63 | 7.63 | 0 |

Every wall time falls inside its window, the two new gates comfortably. `npm test` at 115.26 s sits near the ceiling of 119.66 s, which is the recorded 56 to 60 s band meeting a host at load 7 rather than the load 4 the baseline was taken at. It passes, and the figure is not U3's to defend: the test row is a records-refresh measurement this unit did not touch.

## Notes, none of them a red

1. 🟡 `.sdlc/checks/baseline-agrees-check.sh:32` prints `note  head: baseline ref 20298cc has the same tree as HEAD outside .sdlc/ and .gitignore` in the case where the tree is not the same. The prefix carries the whole meaning and the sentence contradicts it. The `say` rows avoid this by stating both values rather than asserting, as in `ok    tests: baseline 48, test/run.mjs TESTS 48`. A record that reads untrue is the defect this ticket exists for, so it is worth a line, but the fix changes what the script prints and is a plan change, not this unit's to make. U3-1's expected value names a `note head:` line and one is printed, so the row stands green.
2. 🟡 The same file's header, line 4, is 233 characters against 87, 93, 72 and 68 on the lines around it, one sentence carrying a semicolon and three comma clauses and running on into line 5. It is accurate against the code: `say(onMain, …)` is the only head row that can go STALE and the same-tree row is a bare `console.log`, exactly as the header now says. So the header does describe what the script does; it reads as a repair bolted onto a sentence rather than a rewritten one, and the word `now` will age.
3. 🟡 `.sdlc/handoffs/records-followup-U3.md` frontmatter says `pass: 2` while the body's own row reads `| Pass 3 | this pass.`. One of the two is stale.
4. 🟡 The same handoff, line 77, says `BASE` is `acfd7298`, the plan-branch head the unit was cut from, `which is also UB`. After the plan merge at a129843c, `UB` at the graded head is `31b53ea9`. I ran U3-6 under both readings and it prints `0` either way, so nothing measured moves; the sentence is simply stale.

Correction, 2026-09-20 (conductor, from the U9 verdict): row U3-10 said U4-1 still counts one. That was false for U4-1's path list: this cell cites the rule's opener, so the list printed two paths, this file being the second. The cell now claims only what holds, and plan revision 9 excludes `.sdlc/verdicts` from that list under the cited-quote clause.
