---
status: approved
ticket: "#681"
priority: P1
lane: color-engine
written: 2026-09-18
head: bf2aaf6 (plan branch based on cf8e61a; #662 and #674 landed after it, see Risks)
inputs: owner rulings 2026-09-18 (prime fidelity + muted ramps + ladder symmetry, then the nine question rulings in scratchpad/plan-rulings-preset-intent.md), scripts/gen-categories.mjs, src/engine/{prime.mjs,tonal.js,exports.js}, test/engine/{categories,prime,semantic,tonal}.mjs, issues #641 #647 #648 #656 #657 #662 #668 #674, PRs #663 #678 #679 #682 #683
measurements: scratchpad baseline-preset-fidelity.mjs, baseline-2-feasibility.mjs, baseline-3-prime-ladder.mjs, baseline-4-ruled-ladder.mjs, baseline-5-stop500.mjs, baseline-6-668.mjs, baseline-7-668-export.mjs (read-only; anchor, chroma and stop-500 rows re-run on main @ 279dc74 after #657 and #656 landed, outputs baseline-out-279dc74.txt, baseline-2-out-279dc74.txt, baseline-5-out.txt; ladder rows on 988420d, which #657/#656 do not touch)
---

# Every preset's `prime.DEFAULT` is its sampled source colour byte-for-byte, the ramp passes through that anchor at the prime stop, and every ramp falls away from the anchor in chroma more than today

## Ruling this plan implements (owner, 2026-09-18)

1. `prime.DEFAULT` (the `prime.*` ladder's anchor token, `prime.DEFAULT = prime.prime` in `src/engine/exports.js:967`) renders the preset's authored or sampled source colour exactly, byte-for-byte. Skew and lift shape the ramp around that anchor and never move it. Applies to all 343 curated presets (3,780 palettes) and to the default kit.
2. Tonal palettes are more muted: global ramp shaping in all three tone modes (perceptual, peak, even). The prime stop keeps full intent chroma; chroma falls away from the anchor faster than today.
3. The prime ladder brightest → dimmest steps in perceived lightness, equal on both sides of `prime` (ruled 2026-09-18 on the owner's screenshot finding that brightest and dimmest do not read 1:1 around prime). Metric, step and wall rule ruled with Q9: CIE L*, 9 L* per rung, equal-compress, hold CAM16 chroma.
4. The anchor stop is 500 (Q1, ruled against the plan's 550 recommendation): the ramp passes through the source at stop 500, so both scheme accents (550 light, 450 dark) sit one step off the source.

## Two things are called "prime" today, and the ruling names the first

| Name | Where | What it is today |
|---|---|---|
| `prime.DEFAULT`, `prime.*` | `src/engine/prime.mjs` `primeSwatches`, exported as `--{n}-prime-{step}` and `{n}.prime.{step}` | the seven-swatch identity ladder. Its `prime` step is `deriveKeyColor`: the palette's integer `hue` and integer `chroma` (% of peak) rendered at the hue's cusp tone. It has no lightness relation to the source colour at all. |
| the prime accent role, "Prime fill" | `src/engine/semantic.js:124`, stop 550 light / 450 dark | the ramp stop the generator's `liftForTone` fits `lift` for, in `even` mode only (`toneAt(550, 0, lift)`), while the shipped default mode is `perceptual`. |

So the fit in `scripts/gen-categories.mjs` targets a stop that is not the ruled token, on a tone path that is not the shipped one. The baseline below measures both.

## Baseline (measured 2026-09-18; sections (1), (2) and the prototype on main @ 279dc74, section (3) on 988420d)

Corpus: 343 presets, 3,780 palettes: 62 direct/authored (brands), 340 derived neutrals (no source), 3,380 fitted palettes with a source hex (2,028 sampled + 1,352 status, the four status palettes repeat per preset). Anchor error is `prime.DEFAULT` versus the spec swatch hex the generator fits against. Default kit: `prime.DEFAULT` equals its intent by construction (the intent is the `hue`/`chroma` pair itself), so its anchor error is 0 and only the chroma profile applies.

### (1) Anchor error today

| Token compared to source | exact | > 0.5 L* | > 2 L* | > 5 L* | dL* med / p90 / max | dE2000 med / p90 / max |
|---|---|---|---|---|---|---|
| `prime.DEFAULT` (the ruled token), all 3,380 | 0 | 3,356 | 3,248 | 3,062 | 17.16 / 41.30 / 82.87 | 15.07 / 33.21 / 78.96 |
| stop 550, `perceptual` (shipped default) | 0 | 3,252 | 2,121 | 629 | 2.60 / 6.91 / 29.57 | 7.21 / 21.30 / 31.68 |
| stop 550, `peak` | 0 | 2,969 | 2,811 | 2,220 | 11.23 / 36.49 / 43.53 | 17.87 / 32.24 / 41.26 |
| stop 550, `even` (the path the fit runs on) | 2 | 712 | 285 | 191 | 0.32 / 1.05 / 23.40 | 6.41 / 20.82 / 30.43 |

- Generator fit target `toneAt(550, 0, lift, DEFAULT_CONTROLS)` versus source L*: reachable band 17.77 to 76.52 L*; in-band worst 0.40 L*; overall worst 23.48 L*; 358 palettes over 0.5 L*.
- Clamped at the lift domain edge: 373 palettes carry lift ±40 (87 at −40, 286 at +40) in 245 presets; 365 of them are strictly outside the band (the generator's own `liftUnreachable` count; 368 before #656 repaired travel), 8 more round to the edge.
- Even where the L* fit is exact, the token is still not the colour: dE2000 median 6.4 in `even` comes from the integer OKLCH hue (`|dHue|` CAM16 median 2.11°, p90 6.2°) and the integer %-of-peak chroma rendered at a different tone (dChroma median −6.8, p10 −19.0).
- Worst `prime.DEFAULT` cases are dark low-chroma sources whose hue's cusp sits near white, e.g. travel "Hidaka coast" tertiary-muted: source `#2C2C1F`, `prime.DEFAULT` `#F7F4E5`, 78.4 L* apart; the worst after the travel repair is 82.9 L*.
- Byte-exactness constraints measured: the stored `keyColors[0].oklch` (4 decimals) reproduces the source hex for 2,020 of 2,028 swatches after #656 (1,732 before), still not all, so the anchor must be stored as the hex itself. `okhslToRgb(rgbToOkhsl(rgb))` is byte-exact on all 2,028 source hexes, so the ladder can be built in OKHSL from the anchor, but the `prime` step should still emit the anchor's rgb directly so identity never depends on that round trip.

### (2) Chroma profile today

CAM16 chroma at a stop divided by the source colour's CAM16 chroma, median over the 2,836 fitted palettes with source chroma ≥ 10 (presets render with `VIVID_MIDS`: damp 70, dampCurve 1.5, dampAmp 55).

| mode | 100 | 300 | 500 | 700 | 900 | vs ramp max: 100 / 300 / 500 / 700 / 900 | median peak-chroma stop |
|---|---|---|---|---|---|---|---|
| perceptual | 18% | 129% | 144% | 107% | 39% | 9 / 73 / 89 / 67 / 21% | 400 |
| peak | 13% | 85% | 141% | 139% | 41% | 5 / 46 / 92 / 76 / 24% | 550 |
| even | 16% | 95% | 138% | 92% | 58% | 8 / 56 / 89 / 62 / 34% | 450 |

The mids overshoot the source's own chroma by about 40% (dampAmp 55 pushes the centre past the intent), and the shoulders (300/700) sit at or above it. "The prime stop keeps full intent chroma, chroma falls away from it" is the opposite shape: 100% at the anchor, everything else below it.

Default kit (DEFAULT_CONTROLS, dampAmp 0), chroma relative to `prime.DEFAULT`'s chroma, perceptual: Primary 15 / 69 / 101 / 61 / 21%, Secondary 13 / 74 / 74 / 46 / 16%, Tertiary 29 / 181 / 286 / 175 / 49%, Info 49 / 205 / 201 / 137 / 54%, Neutral 36 / 143 / 197 / 130 / 43%. The low-chroma families (Tertiary 33, Info 40, Neutral 29) render their mids at two to three times their key colour's chroma because the ramp's OKHSL saturation is `% of gamut`, not the key colour's own saturation.

Accent-versus-on-color today (the `hpg-role-contrast` gate's own numbers, reproduced by the scratch script to two decimals): perceptual Neutral 5.89/4.21, Primary 6.06/4.31, Secondary 4.40/3.05, Tertiary 6.80/4.86, Info 5.81/4.07, Success 6.18/4.31, Warning 7.60/4.65, Danger 7.17/5.13.

### Feasibility prototype (scratch, not source)

A pinned OKHSL ladder through the anchor (piecewise linear in OKHSL `l` on each side of the anchor stop, `s` normalised so the anchor stop keeps its own saturation, hue = the anchor's own OKHSL hue) over the 3,380 fitted palettes, at the ruled anchor stop 500 and, for comparison, at 550 (scratch `baseline-5-stop500.mjs`, main @ 279dc74):

| anchor stop | byte-exact at the anchor | dup-hex ramps | non-monotone L* | light accent 550 vs source, dL* med (p10 / p90) | dark accent 450 vs source | accent/050 under 4.5:1 (today 2,943) | max \|dL*\| vs today med / p90 / max |
|---|---|---|---|---|---|---|---|
| 500 (ruled) | 3,380 | 2 | 1 (the L* 100 source) | −4.43 (−7.41 / −2.79) | +6.16 (+2.87 / +7.62) | 2,330 (pre-#662 proxy, historical) | 6.6 / 10.1 / 23.2 |
| 550 | 3,380 | 1 | 0 | 0.00 | +10.74 (+5.12 / +13.67) | 2,884 | 3.0 / 6.9 / 29.6 |

At 500, the light-scheme accent reads about 4 L* darker than the sampled colour and the dark-scheme accent about 6 L* lighter; the two accents straddle the source instead of one of them being it. The accent/050 proxy improves (the light accent is darker) and the corpus moves more than at 550 (median 6.6 L* at the worst stop). Source L* distribution: p5 23.6, p95 81.6, min 7.32, max 100.0; with nine stops on each side of 500 and a 0.55 L* minimum gap, the ramp needs the source inside [9.95, 95.05] L*: 9 sources are darker, 1 (pure white) is lighter, 10 in all. The ladder window [12.25, 96.88] L* excludes 20 dark sources and the same white; the union is the 21-name allow-list in C5.

### (3) Prime ladder span and symmetry today

`primeSwatches` at PRIME_STEP 0.09, window [0.14, 0.97] OKHSL `l`, over the 3,780 corpus palettes and the 16 default-kit palettes (scratch `baseline-3-prime-ladder.mjs`, output `baseline-3-out.txt`).

| Measure | corpus 3,780 | default kit 16 |
|---|---|---|
| achieved span, OKHSL `l` (brightest − dimmest) | 0.540 on every ladder (#641's redistribution guarantees it) | 0.540 on every ladder |
| ladders hitting the 0.97 wall (light side clipped) | 2,076 (55%); clipped side compressed by median 45%, max 97% of its 0.27 | 8 of 16; median 56%, max 87% |
| ladders hitting the 0.14 wall (dark side clipped) | 29; median 11%, max 25% | 1 (Data 1); 11% |
| up vs down in OKHSL `l` | 0.27 / 0.27 on the 1,675 unclipped ladders; on a clipped ladder the clipped side is 3 × room (median 0.149 light) and the other side takes the remainder (median 0.391), by #641's rule | 0.27 / 0.27 on 7 of 16; Secondary 0.117 / 0.423, Data 5 0.036 / 0.504 |
| up vs down in CIE L* (brightest − prime vs prime − dimmest) | up median 23.0, down median 31.2; up − down median −8.2, p10 −34.9; 2,034 ladders with \|up − down\| > 3 L* | up 26.8, down 27.9; 10 of 16 over 3 L* |
| dE2000 brightest→prime vs dimmest→prime | 19.7 vs 28.0 | 24.0 vs 26.5 |
| chroma retained (CAM16, rung / prime; prime C ≥ 5) | brightest median 32%, dimmest 92%; 2,982 of 2,990 ladders have a rung under 70% | brightest 28%, dimmest 70%; 15 of 16 |

Two separate causes, both visible in the default kit:

- The wall redistribution (#641). An unclipped ladder is already close to symmetric in CIE L*: Primary up 27.9 / down 27.4, Neutral 27.1 / 27.6, Danger 28.2 / 26.9. The asymmetry the owner saw on Secondary is the clipped case: `prime` sits at OKHSL `l` 0.853, the light side has 9.5 L* of room and the dark side is handed the rest, 43.7 L*. Same on Success (9.5 / 43.7), Data 5 (3.3 / 50.6), Data 6, Data 7. Because today's `prime` is the cusp colour, 55% of the corpus sits in this clipped regime; the source colours the anchor moves to in U1 have median L* 46.7, so under U1 only 464 light-side and 554 dark-side clips remain at a 9 L* step.
- Flat OKHSL saturation. The six other rungs reuse `prime`'s OKHSL `s` (REQ-052), and OKHSL `s` is relative to the gamut at that lightness, so absolute chroma collapses toward white: brightest keeps 32% of prime's CAM16 chroma (Warning 8%, Data 4 7%, Info 25%) while dimmest keeps 92%. That is the "washed" brightest on Primary (53% kept) and it is independent of the step size or the metric.

Seven rung values at PRIME_STEP 0.09 / 0.11 / 0.13 for six representative palettes (OKHSL `l`, then CIE L*; `*` marks a rung on a clipped side, so its value does not change with the step):

| palette | step | l brightest … dimmest | L* brightest … dimmest |
|---|---|---|---|
| light key: literature "The Sound and the Fury" success, prime `#B2EAC0` l 0.869 L* 88.0 C 32 | 0.09 | 0.970* 0.936* 0.903* 0.869 0.723 0.576 0.430 | 97.2 94.1 91.1 88.0 73.7 59.0 44.0 |
| | 0.11 | 0.970* 0.936* 0.903* 0.869 0.683 0.496 0.310 | 97.2 94.1 91.1 88.0 69.8 50.7 31.4 |
| | 0.13 | 0.970* 0.936* 0.903* 0.869 0.643 0.416 0.190 | 97.2 94.1 91.1 88.0 65.6 42.6 18.7 |
| dark key: brands "Maison" Primary, prime `#361DF1` l 0.379 L* 34.0 C 83 | 0.09 | 0.680 0.580 0.479 0.379 0.299* 0.220* 0.140* | 67.4 56.7 45.7 34.0 26.4 18.5 10.3 |
| | 0.11 | 0.800 0.660 0.519 0.379 0.299* 0.220* 0.140* | 79.8 65.1 50.3 34.0 26.4 18.5 10.3 |
| | 0.13 | 0.920 0.740 0.559 0.379 0.299* 0.220* 0.140* | 92.0 73.7 54.6 34.0 26.4 18.5 10.3 |
| mid key: literature "Dracula" danger, prime `#E43D38` l 0.548 L* 52.0 C 83 | 0.09 | 0.818 0.728 0.638 0.548 0.458 0.368 0.278 | 80.9 71.2 61.4 52.0 43.2 34.4 25.4 |
| | 0.11 | 0.878 0.768 0.658 0.548 0.438 0.328 0.218 | 87.1 75.6 63.6 52.0 41.1 30.4 19.4 |
| | 0.13 | 0.938 0.808 0.678 0.548 0.418 0.288 0.158 | 93.4 79.8 65.7 52.0 39.2 26.4 12.9 |
| high-chroma key: brands "Adia" Warning, prime `#FAA30E` l 0.748 L* 73.9 C 59, chroma 98 | 0.09 | 0.970* 0.896* 0.822* 0.748 0.642 0.536 0.430 | 97.0 89.4 81.7 73.9 63.3 53.0 42.3 |
| | 0.11 | 0.970* 0.896* 0.822* 0.748 0.602 0.456 0.310 | 97.0 89.4 81.7 73.9 59.4 44.9 30.2 |
| | 0.13 | 0.970* 0.896* 0.822* 0.748 0.562 0.376 0.190 | 97.0 89.4 81.7 73.9 55.6 36.8 17.4 |
| neutral: default Neutral, prime `#767C8C` l 0.521 L* 52.0 C 13 | 0.09 | 0.791 0.701 0.611 0.521 0.431 0.341 0.251 | 79.1 70.3 61.2 52.0 43.1 33.6 24.4 |
| | 0.11 | 0.851 0.741 0.631 0.521 0.411 0.301 0.191 | 85.2 74.1 63.1 52.0 41.1 29.7 18.0 |
| | 0.13 | 0.920 0.787 0.654 0.521 0.394* 0.267* 0.140* | 92.0 78.7 65.5 52.0 39.1 26.1 12.2 |
| default Primary, prime `#2177F5` l 0.528 L* 52.0 C 68 | 0.09 | 0.798 0.708 0.618 0.528 0.438 0.348 0.258 | 79.9 70.6 61.4 52.0 42.8 33.7 24.5 |
| | 0.11 | 0.858 0.748 0.638 0.528 0.418 0.308 0.198 | 85.7 74.8 63.6 52.0 40.7 29.7 18.3 |
| | 0.13 | 0.920 0.789 0.658 0.528 0.398* 0.269* 0.140* | 92.0 78.9 65.7 52.0 38.9 25.9 11.8 |

Corpus wall counts by step, today's cusp anchors: 0.09 → 2,076 light / 29 dark clipped; 0.11 → 2,451 / 88; 0.13 → 3,003 / 132 (no ladder clips on both sides at any step, so #641's full-span guarantee still holds at all three).

Ruled-ladder simulation (equal steps both sides; `prime` fixed; scratch `baseline-4-ruled-ladder.mjs`, output `baseline-4-out.txt`). "equal-compress" = at a wall both sides take the clipped side's room; "redistribute" = today's #641 rule; "flat s" = today's chroma rule; "hold C" = keep prime's CAM16 chroma on every rung, desaturate only where the gamut at that tone cannot carry it.

| anchor set | metric, step, wall, chroma | clipped light / dark | span L* min / median | spans < 30 L* | dup-hex ladders | chroma kept brightest / dimmest | rungs < 70% (ladders) | \|up − down\| > 3 |
|---|---|---|---|---|---|---|---|---|
| cusp (today, 3,780) | L* 9, equal, flat s | 2,104 / 25 | 1.4 / 45.7 | 1,118 | 0 | 36% / 101% | 2,977 | 0 |
| cusp | L* 9, equal, hold C | 2,104 / 25 | 1.4 / 45.8 | 1,121 | 5 | 67% / 100% | 1,879 | 0 |
| cusp | L* 9, redistribute, hold C | 2,104 / 25 | 53.7 / 54.0 | 0 | 5 | 66% / 100% | 1,879 | 2,024 |
| cusp | OKLab L 9, equal, flat s | 2,398 / 35 | 1.4 / 39.4 | 1,230 | 0 | 28% / 102% | 2,985 | 0 |
| source (post-U1, 3,380) | L* 9, equal, flat s | 464 / 554 | 0.0 / 53.9 | 372 | 16 | 81% / 65% | 2,910 | 0 |
| source | L* 9, equal, hold C | 464 / 554 | 0.0 / 54.0 | 373 | 19 | 99% / 91% | 1,148 | 0 |
| source | L* 9, redistribute, hold C | 464 / 554 | 53.7 / 54.1 | 0 | 19 | 99% / 89% | 1,148 | 927 |
| source | L* 11, equal, hold C | 656 / 1,092 | 0.0 / 64.1 | 373 | 19 | 76% / 84% | 2,025 | 0 |
| source | L* 13, equal, hold C | 852 / 2,326 | 0.0 / 64.1 | 373 | 19 | 76% / 80% | 2,119 | 0 |
| source | OKLab L 9, equal, flat s | 595 / 1,037 | 0.0 / 53.8 | 461 | 16 | 70% / 58% | 3,028 | 0 |
| default kit (key colour, 16) | L* 9, equal, hold C | 7 / 1 | 5.7 / 51.8 | 5 (Secondary 17.7, Success 17.9, Data 5 5.7, Data 6 17.9, Data 7 13.8) | 0 | 41% / 95% | 14 | 0 |
| default kit | L* 9, redistribute, hold C | 7 / 1 | 53.9 / 54.0 | 0 | 0 | 41% / 72% | 14 | 8 |

Window in CIE L*: [12.25, 96.88] (the greys at OKHSL `l` 0.14 and 0.97). Today's 0.27 OKHSL `l` per side equals 26.9 L* up / 27.6 L* down at a mid grey, so a 9 L* rung keeps today's span (54 L*) for an unclipped ladder; 0.66 and 0.78 OKHSL correspond to 11 and 13 L* rungs.

## Mechanism for (3): the ruled ladder

Ruled: seven rungs, equal perceived-lightness steps on both sides of `prime`, `prime` never moves. What the plan recommends for the open parts, from the table above:

All four ruled with Q9 as recommended.

- Metric: CIE L*. The ramp's tone axis (`toneAt`), the analysis plots, the contrast maths and every pixel-measured gate already read L*; OKLab L would be a third lightness in the product, clips more (2,398 vs 2,104 light clips on the cusp set) and keeps less chroma (28% vs 36% at brightest). OKHSL `l` was already near-symmetric in L* when unclipped, so the ruling's real content is the wall rule and the chroma rule, not the metric.
- Step: 9 L* per rung (54 L* span), the equivalent of today's 0.54. Q8 ruled: keep 54.
- Wall: equal-compress (both sides take the clipped side's room), which is what "equal on both sides" means at a wall. Cost: light cusp anchors collapse (Data 5 to a 5.7 L* span, Secondary/Success/Data 6/7 to 14 to 18 L*). Under U1's source anchors only 373 of 3,380 ladders fall under 30 L*, mostly pale sources where a short ladder is the honest picture. For the default kit the fix is the anchor, not the ladder: Q2 (b), ruled, mints default-kit anchors from today's stop-550 hexes (L* 35 to 51, listed in `baseline-5-out.txt`: Neutral `#576485`, Primary `#0C5DCC`, Secondary `#108960`, Tertiary `#920CC6`, Info `#046C9B`, Success `#21701A`, Warning `#774902`, Danger `#AD1A0D`, Data 1 to 8 `#4C5BF8 #B90CC1 #D6153B #A86004 #7E7806 #1A8B43 #088585 #067CB5`), so no default family drops under C11's 30 L* line. Three of the sixteen do clip on the dark side at STEP_L 9 (27 L* of room needed above the 12.25 L* window floor): Tertiary at L* 38.64 has 26.39, Danger at 37.21 has 24.96, Warning at 35.38 has 23.13, so under equal-compress their spans are 52.8, 49.9 and 46.3 L*; the other 13 families take the full 54.
- Chroma: hold `prime`'s CAM16 chroma on every rung and desaturate only where the gamut forces it (addendum option b). Brightest keeps 99% instead of 81% on source anchors and 67% instead of 36% on cusp anchors; it removes the washed brightest independent of the step. This changes gate (g) (s linear in `primeChroma` becomes chroma linear in `primeChroma`) and re-freezes (d5).

Gates in `test/engine/prime.mjs` and how each choice moves them:

| gate | today | ruled ladder, step 9 L*, equal-compress | if the span had widened (Q8 ruled: keep 54; column kept for the record) |
|---|---|---|---|
| d1 span floor | brightest − dimmest = 6 × 0.09 OKHSL `l` exactly when in bounds | = 6 × 9 L* exactly when neither side clips (13 of the 16 defaults); = 6 × min(room) when one does (Tertiary 52.8, Danger 49.9, Warning 46.3 L*); never more | constant changes to 66 or 78 L*; the "when neither side clips" set shrinks to 40% / 30% of the source corpus |
| d2a anchor identity | `prime.l` = key.l | unchanged (prime = the anchor, U1) | unchanged |
| d3 even per side | three equal steps per side; sides may differ | three equal steps per side AND the two sides equal (stronger) | unchanged in form |
| d4 bounds and order | every rung in [0.14, 0.97], strictly decreasing | window expressed in L* [12.25, 96.88]; strictly decreasing in L* | unchanged |
| d5 frozen unclipped defaults | six pre-#641 hex snapshots (Neutral, Primary, Tertiary, Danger, Data 2, Data 3) | re-frozen from the new construction over the 13 families that do not clip under Q2 (b) anchors (Neutral, Primary, Secondary, Info, Success, Data 1 to 8); Tertiary and Danger leave the set because they clip; the capture commit is named in the gate comment | re-frozen again |
| d6 clipped-side fill | clipped side runs to the bound, other side takes the owed travel | replaced: at a wall both sides equal the clipped room; the gate asserts up == down and the clipped side touches the bound | unchanged in form |
| e, h prime pixel identity | prime == deriveKeyColor hex | anchored palettes: prime == anchor (U1); non-anchored: unchanged | unchanged |
| g s linear in primeChroma | OKHSL s ratio 0.5 at 50 | CAM16 chroma ratio 0.5 at 50 (hold C) | unchanged |

## Mechanism for (1): options

| Option | What changes | Anchor error after | Trade-offs |
|---|---|---|---|
| A. real-valued lift | `liftForTone` returns a float, schema keeps floats | stop-550 L* in `even` 0.4 → 0.0; `prime.DEFAULT` unchanged (17 L* median) | fixes the wrong token on the wrong path; hue/chroma quantisation stays (dE 6.7); the 368 clamped stay clamped |
| B. widen the lift domain | `DOMAINS.palette.lift` ±40 → wider; `LIFT_GAIN`/`LIFT_SHIFT_MAX` bound (#648) is 243.5 stops, so lift beyond ±40.6 needs a smaller gain or breaks the monotonicity guarantee | clamped 368 → fewer; `prime.DEFAULT` unchanged | trades #648's guarantee for reach; still not the ruled token; every stored lift moves |
| C. pin the anchor (recommended, ruled) | the palette stores the source as `anchor` (hex). `prime.mjs` reads the anchor: the `prime` step emits the anchor rgb verbatim, the six other steps ladder from it (U6). The ramp passes through the anchor at stop 500 (Q1 ruled) in all three modes; skew/lift warp each side of 500 with 500 a fixed point; the generator stops fitting lift (stores 0) | 0 on all 3,380 (`prime.DEFAULT`) and 0 at stop 500 in every mode; accents 550/450 one step off (−4.4 / +6.2 L* median) | new palette field; the fit, `liftForTone`, and the `lift-anchor` gate retire; non-anchored palettes (the default kit, authored brands) are byte-identical by construction; sources outside the ladder's workable window need a rule (open question Q3) |

Recommendation: C, ruled. A and B cannot reach the ruled token at all, since `prime.DEFAULT` is the cusp colour and no lift touches it. C makes the anchor a stored fact instead of a fitted approximation, which is what "never moves it" means. The plan recommended stop 550 so the light accent would be the source itself; the owner ruled 500 (glossary "PEAK / prime", the `accentRef: single` accent), accepting that both scheme accents sit one step off the source.

How C interacts with the standing guarantees:

- #648 monotonicity: the pinned ladder is monotone per side by construction (a lerp on each side of the anchor, then skew's gamma on the per-side normalised position, exactly `prime.mjs`'s `w` construction). Anchored palettes no longer rely on `LIFT_SHIFT_MAX`; non-anchored palettes keep `liftStop`/`effStop` untouched, so the #648 bound and its gates stay as they are. The "no duplicate swatch, ≥ 0.55 L* gap" property has to be re-asserted over the anchored corpus: with the anchor at 500 there are nine stops on each side, so the gap holds for anchors inside [9.95, 95.05] L*; 10 sources fall outside (9 dark, 1 white) and take Q3's ruled rule (the ramp lands at the nearest workable L*, the token stays exact, the gate names them).
- #641 span floor: `primeSteps(lPrime)` stays. The anchor's `lPrime` becomes `rgbToOkhsl(anchor).l` instead of the cusp `l`; 14 sources sit outside [PRIME_L_MIN, PRIME_L_MAX], where the #655/F1 floor at 0 prevents inverted travel but the ladder collapses on that side. Rule (Q3): the `prime` step is exact regardless; the six other steps clamp their ladder anchor into the window.
- #657 (hue solver): anchored palettes take the anchor's own OKHSL hue for the perceptual/peak paths and need no Newton solve; the `even` path renders 550 verbatim and solves the CAM16 hue for the other stops from the anchor's measured CAM16 hue. Non-anchored palettes keep `solveOkhslHue`. #657's fix still matters for them and for the byte-identity control.

## Mechanism for (2): options

| Option | What changes | Effect | Trade-offs |
|---|---|---|---|
| D. default/knob change | `VIVID_MIDS.dampAmp` 55 → 0 for presets, `dampCurve` 1.5 → 1.0, `damp` 70/80 → higher | removes the 44% mid overshoot; shoulders drop to about 64% (stop 300) and 29% (stop 100) of the centre | still centred on stop 500, not the anchor, so the anchor stop sits at 0.97 of full chroma; the default kit's low-chroma families still render mids at 2 to 3x their key colour because `s = chroma% of gamut`; `chromaFloor` on `even` still lifts the ends back up |
| E. anchor-centred chroma envelope (recommended, ruled) | one `chromaEnvelope(stop, anchorStop, controls)` in `tonal.js` replaces the two identical copies of the damping multiplier `m` (the skill's "computed identically in both paths" invariant becomes one function). `env(500) = 1` exactly (one centre for every palette now that Q1 rules the anchor stop 500); falls to `edge` at 050/950 with `damp` as depth, `dampCurve` as exponent, `dampBias` as asymmetry; `dampAmp` keeps its slot (Q7) but the envelope is normalised at 500, so it reshapes the shoulders and never lifts the anchor. Anchored palettes: saturation at 500 = the anchor's own OKHSL `s`. Non-anchored palettes: saturation at 500 = the key colour's own OKHSL `s` (the same quantity `prime.mjs` already reads, REQ-052) instead of `chroma% of gamut`. `even` path: `chromaFloor` becomes a floor on the envelope, never above `env` at the anchor | prime stop at 100% of intent chroma in every mode; shoulders below it by construction; the default kit's Tertiary/Info/Neutral mids stop overshooting their key colour | moves every ramp in the product (blast radius is the whole corpus plus the default kit); `intensity-legacy` fixture (`test/engine/fixtures/tonal-legacy.json`) must be regenerated with a recorded rationale; `hpg-role-contrast` ratchet floors move and must be re-pinned |

Target (Q4 ruled), measured as CAM16 chroma at the stop over CAM16 chroma at stop 500, per mode, over the fitted corpus and the default kit:

| stop | today (perceptual, median) | target |
|---|---|---|
| 500 (the anchor stop) | 144% of source | 100% ± 1% (exact at the anchor for anchored palettes) |
| 300 and 700 | 129% / 107% | median ≤ 75%, p90 ≤ 90% |
| 100 and 900 | 18% / 39% | median ≤ 25%, p90 ≤ 35% |
| any stop | up to 144% | none above 100% of the anchor's chroma (peak mode may equal it at the cusp) |

Effect on the 53 roles and on the on-color gates: the accents (550/450) sit one envelope step from the anchor and keep close to its chroma, so their lightness moves little; the tints (050 to 200, the on-colors and containers) lose chroma and move toward white, which raises accent-versus-on-color ratios slightly; hover/active (650/750, 350/250) and outline/scrim bases (500 alpha ramp) get less chroma. Since #662 the default `onColorMode` is `contrast`: the on-color is whichever ramp end clears AA against the accent, else the achromatic white/black constant, so the tints' chroma loss cannot drop a pair under AA by itself; what can is the accent's own L* shift (Helmholtz-Kohlrausch: CIELAB L* rises when chroma falls at fixed OKHSL `l`, #668), which the 16-family ratchet and the #674 corpus sweep both catch. `hpg-role-contrast` is a ratchet floored to one decimal; U3 must print each family's new ratio and re-pin only upward, or hand any downward move to #662's policy. Prototype proxy over the curated corpus: 2,943 fitted palettes already sit under 4.5:1 at the accent/050 pair today (they are muted sampled colours, not accents), 2,330 after the pinned ladder at 500; #674's curated-corpus gate must be measured against the landed U3, not against today.

## Criteria (verifier-checkable)

Rendered-path rule (revision 14, stated once, cited from C3/C5/C6/C8/C11 below): a criterion that
measures product colour reads the RENDERED path — the emitted pixel, or the resolved chroma the
product actually renders (`rampChromaOf` plus `hueShift`/`hueSameDir`/`cuspPull`, matching
`src/ui/model.mjs`'s `projectView(hydrate(preset))`) — never a raw stored `palette.chroma`/`palette.hue`
fed straight into an engine function with `DEFAULT_CONTROLS`. U3 and U2 each built an early gate on that
raw-value proxy and had to retract its numbers (revisions 11-12; U2's `preset-intent-fidelity-u2-rediagnosis.md`
Finding 0); this rule exists so a third unit does not repeat it.

- C1 `npm test` green on the branch head: exit 0 and the runner's last line reads `all N test files passed` with N = `TESTS.length` in `test/run.mjs` on the recorded base sha plus one for `test/engine/anchor.mjs` (47 + 1 = 48 on bf2aaf6, after #674 registered `engine/curated-contrast.mjs`); then `git status --short` prints nothing. Negative control: corrupt one `docs/reference/data/role-table.json` value (for example set `roleTable[0].light` to `999`), expect `npm test` to exit 1 with at least one FAIL line naming the mismatch, in the shape the U2 verifier recorded: `▶ engine/semantic.mjs FAIL` then `FAIL refs-canonical - primary = 550/450 != canonical 999/450` then `✗ 1/48 test file(s) failed`. It is one FAIL line, not 17: `refs-canonical` reports the first mismatch and stops, so a one-field and a whole-table corruption both produce a single line; the 17-line figure in `.sdlc/adapter.md` §1 is not what a role-table corruption reproduces. Interim ceiling (owner ruling 2026-09-20, #713: "Interim ceiling now, split sweeps into gate scripts as a new ticket"): `npm test` is expected between 280 and 550 s on a host at load under about 10, re-measured, not renewed, when #713 splits the corpus sweeps into gate scripts. The readings behind the range, the four heavy files (`engine/tonal.mjs` 100.2 s, `engine/anchor.mjs` 80.0 s, `ui/headless-boot.mjs` 61.0 s, `engine/prime.mjs` 54.9 s at `bf62ee30`, load 5.16) and the contention rule (a reading past the ceiling is a contention question first, per `flaky-gates`, since the series is monotone in host load) live in `.sdlc/baseline.md` §Interim gate-time ceiling, the one home. A run at load 10 or above is recorded with its load and not graded against the ceiling.
- C2 Anchor identity: for every palette that carries `anchor`, `primeSwatches(...)[3].hex === anchor` and `exports.js`'s `prime.DEFAULT` OKLCH string equals `oklchStr(rgbToOklch(hexToRgb(anchor)))`; count = 3,380 on the regenerated corpus (2,028 sampled + 1,352 status), 0 mismatches. Command: `node test/engine/anchor.mjs` prints `anchor-identity: 3380 exact, 0 off`. Negative control: patch one spec swatch hex by one byte in a scratch copy and rerun the generator into a temp dir, the gate names that preset and palette.
- C3 Ramp pass-through (rendered path): scoped to the 3,370 anchored palettes INSIDE the ramp window [9.95, 95.05] L* — the 10 sources outside it are C5's own named window-clamp allow-list, not this criterion's (U2's Q1 reading is correct: the TOKEN, `prime.DEFAULT`, is exact for all 3,380 per C2 unconditionally; the RAMP's stop 500 is exact only in-window, and clamps to the window edge nearest the source for the 10 named exceptions, per Q3(b)). For every in-window anchored palette, `paletteStops(p, controls, STOPS)` stop 500 hex equals `anchor` in each of `perceptual`, `peak`, `even` (3 × 3,370 checks, 0 misses), and the 25-stop export ramp agrees at 500. Command: `node test/engine/anchor.mjs` prints `anchor-ramp: 10110 exact, 0 off (in-window sources only, 3370 of 3380)`. Negative control: set `lift: 40` on an in-window anchored palette, stop 500 unchanged; on a non-anchored copy of the same palette stop 500 moves.
- C4 Non-anchored byte identity (the engine-level negative control): with every `anchor` stripped from the regenerated presets and the envelope defaults reverted to today's values, `paletteStops` and `primeSwatches` output is byte-identical to `origin/main` at branch creation (the sha recorded in the unit handoff's `base:` row, never a fixed commit in this plan) for all 3,780 palettes in all three modes, and the default kit is byte-identical. Command: `node scripts/report-preset-fidelity.mjs --identity-control --base <sha>` prints `0 differing cells`; the verifier reads `<sha>` from the handoff and confirms `git merge-base plan/<slug> origin/main` equals it.
- C5 Monotone and distinct (rendered path): every anchored ramp has non-increasing OKHSL `l` 050 → 950, a ≥ 0.55 L* gap between neighbouring stops, and no two stops share a hex. The Q3 rule (ruled b) applies to exactly the sources outside the ramp window [9.95, 95.05] L*: their `prime.DEFAULT` is still the source byte-for-byte (C2), the ramp's stop 500 lands at the window edge nearest the source, and the gate prints the allow-list by name with its expected count, 10 (9 dark sources at 7.32 to 9.84 L* plus the one pure-white source), failing on any other count or any other name. Measured CIELAB L* non-increasing in all three modes: the #668 class (11 palettes in perceptual and 46 in peak on the 25-stop ramp, 43 on the 19-stop, on bf2aaf6 before R1, 0 in even) must be 0 in every mode, per C6's named cases. Command: `node test/engine/anchor.mjs` prints `anchor-ramp allow-list: 10 (expected 10)` then the ten r-tagged lines below (Nike secondary, Nike tertiary-muted white, Double Indemnity, The Night of the Hunter, 2001, TRON: Legacy, Tórshavn, Khumbu, Rub' al Khali, Atchafalaya); an eleventh line or a changed name is a FAIL. The ladder window [12.25, 96.88] L* (U6) excludes 21 sources including these 10; U6's `ladder-window` gate in `test/engine/prime.mjs` prints that list the same way (`ladder-window allow-list: 21 (expected 21)`). The 21 names, measured on 279dc74 (`baseline-5-out.txt`), tagged r = outside the ramp window, l = outside the ladder window:
  brands "Nike · The Swoosh · Since 1971" secondary `#101820` L* 7.84 (r, l) · brands "Nike · The Swoosh · Since 1971" tertiary-muted `#FFFFFF` L* 100.00 (r, l) · film "Double Indemnity" primary `#1B1B1D` 9.84 (r, l) · film "The Night of the Hunter" primary `#161618` 7.32 (r, l) · film "Apocalypse Now" primary `#241E1A` 11.81 (l) · film "2001: A Space Odyssey" tertiary-muted `#1A1B1E` 9.78 (r, l) · film "TRON: Legacy" secondary `#181B1F` 9.62 (r, l) · film "Suspiria" tertiary-muted `#201F25` 12.09 (l) · film "The Matrix" tertiary-muted `#1F1F24` 11.95 (l) · music "The late-night club" primary-muted `#1F1F23` 11.91 (l) · music "UK '77" secondary `#1F1F23` 11.91 (l) · music "P-Funk" secondary-muted `#211E27` 11.95 (l) · music "Black metal" secondary `#1E2024` 12.20 (l) · nature "32° N Carlsbad Caverns" secondary `#1D1D20` 10.88 (l) · travel "62° N Tórshavn" tertiary-muted `#221913` 9.66 (r, l) · travel "48° N Saint-Malo" primary-muted `#251B14` 10.76 (l) · travel "27° N Khumbu teahouse" tertiary-muted `#1F1A16` 9.70 (r, l) · travel "41° N Eminönü" tertiary-muted `#251B12` 10.71 (l) · travel "20° N Rub' al Khali" primary-muted `#1F1A16` 9.70 (r, l) · travel "30° N Wadi Rum" primary `#1E1D1B` 10.80 (l) · travel "30° N Atchafalaya" primary `#221913` 9.66 (r, l). Negative control: lower one allow-listed source's L* in a scratch spec copy so it re-enters the window, the count line reads 9 and the gate fails. U1's own provisional, pre-U6 OKHSL-domain ladder fix (`test/engine/anchor.mjs`'s `anchor-ladder` gate, a widening search over the prime ladder's 7 rungs, not a plan criterion in its own right) separately names 4 exact-duplicate exceptions where `prime` and an extreme rung render byte-identical hex even after widening (owner ruled: hex-inequality is the goal, but not reachable for these 4 given the window geometry; name and count them rather than silently pass) — `anchor-ladder dupe-allow-list: 4 (expected 4)`. Three are already among the 21 above: film "Suspiria" tertiary-muted `#201F25`, music "Black metal" secondary `#1E2024`, music "P-Funk" secondary-muted `#211E27`; the fourth is film "The Night of the Hunter" TERTIARY `#1E211E` (a different swatch of the same preset than the primary `#161618` already listed). Near-grey notch (revision 20, team-lead ruling, Q-C, `test/engine/anchor.mjs`'s `NOTCH_ALLOW`): stop 500's CAM16 chroma under 70% of BOTH its 450 and 550 neighbours is the wrong instrument alone for a near-grey (or very low-chroma) anchor inside a group whose resolved `rampChroma` target is well above it — ratios explode near 0. The gate reads ratio PLUS an absolute dip of at least 3 CAM16 C (both conditions must hold to flag a cell); the definition's 70%-ratio bar itself is unchanged, not loosened. The remaining residual population, whatever it measures under the combined ratio+absolute-dip reading, is named by preset in `NOTCH_ALLOW` as "pending U4" and re-measured after U3's `dampAmp 0` retune integrates; the default kit stays at 0 notched cells in every mode, before and after. If any residual survives U4's re-measurement it comes to the owner then, with final numbers, not before.
- C6 Chroma envelope (Q4 ruled; rendered path): `node scripts/report-preset-fidelity.mjs --envelope` prints, per mode, the median and p90 of CAM16 chroma at stops 100/300/700/900 over stop 500, over the 2,836 fitted palettes with source chroma ≥ 10 and over the 8 default-kit semantic families; pass is median ≤ 75% and p90 ≤ 90% at 300 and 700, median ≤ 25% and p90 ≤ 35% at 100 and 900, and, in even and peak modes, 0 GENERATED palettes (`dampAmp 0`) with any stop above 100% of stop 500 (peak's richest stop coincides with 500 by construction, confirmed by the pre-existing `okhsl-modes` gate, so peak carries no exemption); `env(500) = 1` within 1e-9 for every controls combination in the sweep. Owner held the bar (2026-09-18): these targets do NOT rescope; U3 continues against the named root cause (lift sign × hue-cusp tone, not `dampAmp` or `skew`) until the table passes for generated palettes. Owner ruled (e) (AskUserQuestion, 2026-09-19), on a ratified-vs-ratified conflict U3 surfaced (the "0 above 100%" ceiling forced 12 of the 16 default-kit palettes' richest stop to exactly 500 in perceptual mode, erasing the hue-cusp-following richness the pre-existing, ratified `hpg-tonal-cusp-pull` gate, #55, requires): the "0 above 100%" ceiling stays as written for even and peak (peak's own cap, restored scoped to `mode === "peak"` only at `a28fa18`, closes peak's count to exactly the 16 named Adia palettes). Superseded at revision 20 (owner ruling (f), AskUserQuestion, 2026-09-19), once U3 measured that 75.6% of perceptual's above-anchor palettes carry 2-5 ADJACENT above-anchor stops (a natural cusp shoulder, not a single point), so a strict one-stop exemption produces a spike (capping every neighbour but one lone stop): perceptual instead exempts exactly ONE CONTIGUOUS RUN of above-100% stops per ramp (the shoulder, however wide), not one stop; a second, non-adjacent run anywhere in the same ramp is a FAIL. Every stop inside the exempted run must sit at or under a frozen bound of 189.3005% of stop 500's chroma (the fresh, cusp-scoped corpus-wide max excess, measured on the current rendered path at U3's head `a28fa18`/`d5c09c3`, witness "cuisine/Sushi & sashimi · the cypress counter/primary-muted", cusp stop 650 at 189.3% of anchor `.sdlc/questions/pif-u3.md`); any stop past that bound, inside or outside the run, is a FAIL. `hpg-tonal-cusp-pull` (#55) ships exactly as it does today, untouched; no ramp construction changes for this ruling, only the gate's shape. The earlier one-stop framing and the unscoped 89.3pp/93.8pp/241.6pp background figures (revision 19) are retired by this more precise, run-shaped ruling; the 89.3005pp/189.3005% figure is the fresh, confirmed replacement. Median ≤ 75%/p90 ≤ 90% at 300/700 and median ≤ 25%/p90 ≤ 35% at 100/900 still apply to perceptual unchanged; the exemption touches only the "0 above 100%" clause. Named carve-out: an AUTHORED palette with `dampAmp > 0` is exempt from the "0 above 100%" clause only (never from the median/p90 targets), named in the gate by preset — today exactly the Adia brand kit (`dampAmp 70`, 16 palettes), frozen by name/count the same shape as every other allow-list in this plan. Negative control: rerun with `--damp-amp 55`, expect the `above 100%` count to be non-zero and the line marked FAIL; separately, add a second authored `dampAmp > 0` preset in a scratch copy, expect the gate to name it as unlisted. Negative control (perceptual cusp-run exemption): a scratch palette with a second, non-adjacent above-100% run, or with any stop (inside or outside the run) pushed past 189.3005%, must FAIL the gate — the exemption names exactly one contiguous run per palette and freezes a ceiling on every stop inside it, not a blanket pass for perceptual. The four ramp-shape gates (zero upticks, zero duplicate hexes, docs/ moves by named exception only, the owner-accepted movement table) are specified under U3 and are part of this criterion.
- C7 Both paths share one envelope: `grep -c "export function chromaEnvelope(" src/engine/tonal.js` prints 1, `grep -c "chromaEnvelope(" src/engine/tonal.js` prints exactly 3 (the definition plus the even-path and OKHSL-path calls), and `grep -c "1 + ((controls.dampAmp" src/engine/tonal.js` prints 0 (today 2, lines 318 and 427). Damping never perturbs tone: the `damping-curve (f)` gate stays green at `|Δ| ≤ 1e-9`.
- C8 Contrast (rendered path): `hpg-role-contrast` (`test/engine/semantic.mjs`, widened at #662 to all 16 families, both schemes, AA floor) green with FLOORS re-pinned to the new measured ratios; no family's new ratio is below its current floor (perceptual and even tables as of bf2aaf6, e.g. Neutral 5.8 / 4.9, Secondary 4.7 / 6.1, Data 5 4.5 / 5.8). Team-lead ruling (revision 20, not the owner's, Q-B): 41 of the 96 measured entries sit below their PRE-#681 (`bf2aaf6`) value while every entry still clears AA (`.sdlc/questions/pif-u2.md`); this is not a C8 fail this pass — the numbers move again once U3's `dampAmp 0` retune integrates, so re-pinning now spends a ruling on interim figures. The 41 are pinned by name as "pending U4" in U2's gate; U4 re-measures on the integrated tree and the owner rules once, there, with final numbers, the same treatment the Neutral-dark re-pin already carries. MCP `contrastLint` over the default kit (`contrastLint(brandKit(defaultDocument()))` per mode): 0 entries in perceptual, peak and even on bf2aaf6 (0 / 0 / 9 before #662); pass is 0 / 0 / 0. `npm run gate:corpus-contrast` (the #674 full sweep) reports 0 of 7,560 cells under 4.5 with no new carve-out row. Negative control: lower one FLOORS entry by 0.1 below the measured ratio in a scratch copy of the gate, expect it to stay green; raise it 0.1 above, expect that family's line to FAIL.
- C9 Regenerated assets: after `npm run gen:categories && npm run gen:adia-exports && npm run gen:mcp-assets && npm run bundle && npm run gen:figma-ui`, `git status --short` prints nothing; `git diff --stat origin/main -- figma/binder/figma-semantic-binder/code.js` prints nothing; `docs/reference/data/role-table.json` changes only by the 16 `defaults[].anchor` fields U1 adds: `git diff origin/main -- docs/reference/data/role-table.json | grep -c '^[-+]' ` prints 18 (16 added lines plus the two file-header lines) and `jq '.defaults | map(has("anchor")) | all' docs/reference/data/role-table.json` prints `true`, while `jq '.roles | length'` and the 53-role deep-equal gate are unchanged; `git tag --list 'adia-*@1.1.0'` prints exactly three lines (`adia-brand-document@1.1.0`, `adia-oklch-export@1.1.0`, `adia-radix-export@1.1.0`), cut on the squash commit per the #631 procedure, and `git tag --list 'adia-*'` prints six.
- C10 Records, one grep each, all non-zero on the branch and zero on `origin/main`: `grep -c "^## ADR-026" docs/reference/references/decision-records.md` = 1 and it precedes `## Quick map`; `grep -c "^## 1.64" docs/reference/CHANGELOG.md` = 1; `grep -c "^| \*\*Anchor\*\*" docs/reference/references/glossary.md` = 1; `grep -c "^## 9\. Anchored palettes" docs/reference/references/knowledge-02-tonal-scale.md` = 1; `grep -ci "anchor" .claude/skills/color-math/SKILL.md` ≥ 3; `grep -ci "anchored palette" docs/reference/rubrics/acceptance-criteria.md docs/reference/rubrics/quality-rubric.md` ≥ 1 in each file (today 0 in both: the rubrics state lift's displacement mechanism at `quality-rubric.md:39-43` and `acceptance-criteria.md:22-25` and carry no anchor predicate; the new text must state that an anchored palette's `prime.DEFAULT` and stop 500 equal `anchor` byte-for-byte and that skew/lift never move them). `node test/repo/branding.mjs` and the #664 citation gate green.
- C11 Ladder symmetry (Q8, Q9 ruled; rendered path — already pixel-measured per its own text below) over the full sweep (both hue spaces, hue 0..359 step 5 × chroma {0, 50, 100}) plus the 16 defaults plus the 3,380 anchored palettes, `L*(brightest) − L*(prime) == L*(prime) − L*(dimmest)` within 1e-9 by construction and within 3 L* measured from the emitted pixels, 0 exceptions; every rung keeps ≥ 70% of prime's CAM16 chroma or equals `maxChromaInGamut(hue, L)` at its tone within 0.5 (the gate prints which); the corpus report prints the ladders with span < 30 L*: expected 373 ± 5 on source anchors (the prototype's count under the ruled rules; the tolerance covers the U6 window constants being derived rather than retyped) and exactly 0 on the default kit under Q2 (b). Negative control: run the gate against `origin/main`'s `prime.mjs`, expect the symmetry line to FAIL with 2,034 ± 10 corpus ladders over 3 L*. (Revision 9's gamut-ceiling clause is retired, revision 11: the measured 1,288/302,400 out-of-gamut figure was an artifact of a shared gamut-cache determinism bug, not a property of the ruled ladder; on the fixed engine the true count is 0, so a plan criterion pinning a ceiling near 1,288 would be tautological. U6's own test suite keeps a dedicated, non-tautological determinism regression gate for this — see its handoff — but it is not a plan-level criterion.)

- C12 Reset action (Q6, ruled): on an opened anchored preset copy, editing `hue` or `chroma` removes `anchor` from the palette (the palette becomes ordinary and `prime.DEFAULT` reverts to the key colour) while the generator-written `sourceAnchor` field (U1 schema: the same hex, never edited by the UI, persisted through serialize/hydrate) stays; the inspector shows a "Reset" action, visible only when `sourceAnchor` is present and `anchor` is absent, that sets `anchor = sourceAnchor` and re-derives hue/chroma/lift from it, after which `paletteStops` and `primeSwatches` output is byte-identical to the untouched preset. Command: the headless shim run (`test/ui/headless-boot.mjs`, a new lettered group) opens a curated preset, drags Hue by +10, asserts `anchor` absent, `sourceAnchor` still equal to the preset's source hex, and the prime hex changed, clicks Reset, asserts `anchor` restored and the 19 ramp hexes plus the 7 prime hexes deep-equal the pre-edit capture; skew and lift edits keep `anchor` (asserted in the same group). Negative control: with the Reset handler stubbed to a no-op, the group fails on the `anchor` restored assertion.
## Units

- [x] U1 (M) anchor field + `prime.DEFAULT` byte-exact · grade l3
  `src/ui/model.mjs` (`DEFAULT_PALETTES` gain `anchor` = today's stop-550 hex per Q2 (b), the 16 values listed under mechanism (3)), `docs/reference/data/role-table.json` (`defaults[]` gains the same 16 `anchor` fields, and `hpg-role-contrast`'s two-source parity loop in `test/engine/semantic.mjs` extends its compared fields from chroma/skew/lift to chroma/skew/lift/anchor, `compared` = 4 × 16), `src/ui/persist.js` (`DOMAINS.palette.anchor` and `DOMAINS.palette.sourceAnchor`, `clampPalette`: each a 6-hex string or absent, `sourceAnchor` written only by the generator and by `defaultDocument()`, so Reset (C12) has a field to read after a detach; schema version bump with a RENAME_MAPS no-op entry per the TKT-0016 rule), `scripts/gen-categories.mjs` (store `anchor: hex` for sampled + status palettes; `direct` palettes untouched unless the JSON authors `anchor`), `src/engine/prime.mjs` (anchor branch: `prime` step emits the anchor rgb verbatim, ladder from `rgbToOkhsl(anchor)`, window clamp for the six other steps), `src/engine/exports.js` unchanged (`prime.DEFAULT = prime.prime` already), new `test/engine/anchor.mjs` registered in `test/run.mjs` (C2 + the identity control of C4 for `primeSwatches`). Criteria: C1, C2, C4 (prime part), C10 for the schema note.
- [x] U2 (L) the ramp passes through the anchor at stop 500 in all three modes, with the Reset action · grade l4
  `src/engine/tonal.js`: anchored branch in `okhslStops` (piecewise ladder through `(500, anchor.l)`, skew/lift as per-side warps with 500 fixed, hue = anchor OKHSL hue, s at 500 = anchor.s) and in `paletteStops`' even path (`toneAt` piecewise through `(500, anchor L*)`, chroma at 500 = anchor CAM16 chroma, hue solved from the anchor's CAM16 hue, 500 emitted verbatim); sources outside [9.95, 95.05] L* land at the nearest window edge (Q3 b). `scripts/gen-categories.mjs`: `liftForTone` retired, `lift: 0` stored, the summary line reports anchors and the allow-list instead of the band. `test/engine/categories.mjs`: `lift-anchor` gate replaced by the `anchor-ramp` gate in `test/engine/anchor.mjs` (C3, C5); #656 is landed, so `EXPECTED_DRIFT` retires with it. `src/ui/sections/color.js` + `src/ui/app.js`: hue/chroma edits drop `anchor`, a Reset action restores it (Q6, C12), with a headless-shim group in `test/ui/headless-boot.mjs`. Owner ruling (2026-09-18, revision 14, on the re-diagnosis's Finding 2/F4): reading (a) — anchored palettes keep `toneAt`'s curve, tension, vibrancy and hue space composed with the anchor pivot on every path; these controls stay live for an anchored palette exactly as this paragraph already specifies ("`toneAt` piecewise through (500, anchor L*)"). The shipped construction's straight lerp to `lmin`/`lmax` (which made Curve/Tension inert, Vibrancy inert in perceptual, hue space inert everywhere, and peak/perceptual byte-identical for all 3,396 anchored palettes) is a bug against this text, which stands as written — no straight-lerp shortcut on any anchored path, in any mode. Check (revision 15): peak and perceptual must NOT render byte-identical for any anchored palette — a regression back to the straight-lerp construction reads as exactly this predicate turning true again, so it is the cheapest possible tripwire for it. Team-lead ruling (revision 17, not the owner's, on Q-U2-5): the re-diagnosis's Finding 1 fix (route the anchored branches through `chromaEnvelope` using the anchor's own OKHSL `s` / CAM16 chroma as its LITERAL, unconditional pivot basis) broke REQ-002 (a group's Base chroma becomes a no-op for every anchored ramp, since chroma no longer reads `palette.chroma`/`rampChroma` at all — `(gid3)`/`(gid8)`/`(gid8b)` in `test/ui/headless-boot.mjs` red). Ruled (b), applying the owner's F4 principle (keep the ratified spec text, fix the construction to satisfy it): keep REQ-002 as ratified, keep `chromaEnvelope` itself verbatim (still called, not forked), but the anchored branches' BASIS input to it is a BLEND — the anchor's own chroma/saturation exactly at stop 500, shading to `rampChroma`/`palette.chroma` at the ramp's ends — not the anchor's value read unconditionally at every stop. Check: Base chroma moves an anchored ramp's ends while stop 500 stays byte-exact to the anchor (both halves of REQ-002 and the anchor pass-through hold at once; a regression to the literal, unconditional anchor basis reads as Base chroma becoming inert for an anchored ramp again — `(gid3)`/`(gid8)`/`(gid8b)` are the existing tripwire). Team-lead rulings (revision 20, not the owner's): Q-B (C8) — 41 of 96 `hpg-role-contrast` entries sit below their pre-#681 value with AA held everywhere, pinned by name as "pending U4", not re-pinned or failed this pass; Q-C (C5) — the near-grey notch gate reads ratio PLUS an absolute dip of at least 3 CAM16 C (the 70%-ratio bar itself unchanged), its residual named "pending U4" in `NOTCH_ALLOW`. Both re-measure once U3's `dampAmp 0` integrates, and the owner rules once at U4's report with final numbers, not on interim figures. Team-lead ruling (revision 21, not the owner's, Q-D, under the owner's F4): two independent sources (U2 review 3, `pif-u2-review-3.md`; a scout probe, `U2-scout-huespace` at `e426c74`) agree that flipping `hueSpace` on an anchored palette moves 3,393 perceptual and 3,390 peak ramps at the hex level (`tonal.js:702`, `:764`), but every one of those moves is 8-bit rounding noise (max dE_OK ≤ 0.01, per-channel diff mostly 1 code) — OKHSL hue IS the OKLab hue, so the per-stop solve in those two modes only re-picks the nearest 8-bit neighbour, nothing perceptual. In even mode the same flip is real (up to 20 codes, the Abney correction). Ruled: the engine is unchanged (F4's fix stays as shipped, no revert); `hueSpace` stays live for anchored palettes in even; for anchored perceptual and peak the inspector hides or disables the control with a one-line reason ("hue follows the anchor; OKHSL and OKLCH hue coincide here") — F4 forbids a dead slider, and hiding a control with no visible effect is the sanctioned shape, not presenting one that renders identically either way. Gate (rendered path, restated at revision 23 on U2 review 4's corpus measurement): the flip yields max dE_OK ≤ 0.01 over every anchored ramp in the corpus in perceptual and peak (proving the hide is justified; the earlier "≤ 2 codes" bound holds on the default kit but 56 of 3,396 corpus ramps sitting on the gamut face move 3-12 codes at max dE_OK 0.0053, still rounding, so the perceptual bound is the criterion and the code bound is a default-kit check only) and a max dE_OK above the JND in even (proving it stays live there); a headless-shim check asserts the control is absent/disabled for anchored perceptual/peak and present for even. Negative control: force the perceptual solve into a wrong hue space in a scratch copy, the ≤ 2 bound reds. Criteria: C1, C3, C4 (ramp part), C5, C8, C12. Depends on U1.
- [x] U3 (M) anchor-centred chroma envelope, all modes · grade l4
  `src/engine/tonal.js` (`chromaEnvelope`, both paths, keyed on `liftStop(stop, lift)` (the #648 displacement helper, the same call `effStop` makes before skew's gamma), never on the nominal stop and never on a separately re-derived effective stop: Lane A's #668 R1 grid evidence shows a re-derived `effStop` breaks the lift-0 control, drops Danger below its floor, moves Panda literals, and still leaves 4 grid cells rising, while `liftStop()` avoids all four; `chromaFloor` as an envelope floor, saturation basis = the key colour's own OKHSL `s` for non-anchored palettes), `scripts/gen-categories.mjs` (`VIVID_MIDS.dampAmp` 55 → 0, Q7 ruled: the envelope is normalised at 500 so a non-zero `dampAmp` can only raise the shoulders, which C6 forbids above 75%; the builder reports the C6 table at both values and ships 0), `test/engine/tonal.mjs` (envelope gate C6/C7; `hpg-tonal-lift-monotonic` widened from the 16 defaults to the full corpus on both stop sets, or a sibling no-duplicate gate over the curated presets, C6 ii; regenerate `fixtures/tonal-legacy.json` via `scripts/gen-tonal-fixture.mjs` with the rationale in the fixture header), `test/engine/semantic.mjs` (`hpg-role-contrast` floors re-pinned, C8). Depends on U1 (anchor stop) and on #668 R1 (Lane A, landing now); can be built in parallel with U2 behind the `anchorStop` parameter. Baseline the owner held against, ruling "hold the bar, keep fixing" (2026-09-18, revision 14): 52-79% of non-Adia curated palettes carry at least one stop above 100% of the anchor's chroma, median excess 3.8 to 17.1 points, worst 241.6, uniform across all 8 categories; the driver is lift sign combined with hue-cusp tone, not `dampAmp` or `skew`. C6's median/p90/100% targets are not rescoped; U3 continues against this named root cause until the envelope table passes for generated (`dampAmp 0`) palettes, with the Adia carve-out (C6) covering the one authored `dampAmp > 0` exception. Landing condition: the owner's 🟢 on the movement table (C6 iv), so U3 merges into the plan branch after U4's report exists, not before. Team-lead ruling (revision 18, not the owner's): U3's own measurement proved several median/p90 misses (`perceptual|300` p90, `peak|700` p90, `even|100` p90, `even|300` p90, `even|900` median/p90) pre-exist on `origin/main` and are not lift-driven (a `bf2aaf6` re-measurement shows them essentially unchanged, and a lift=0 vs lift≠0 split rules lift out as the driver on `even|900`); the actual cause is the shipped `damp`/`dampCurve` defaults. Closing C6's held targets (revision 14: do not rescope) therefore REQUIRES retuning those defaults — ruled: this retune is INSIDE U3's own scope, the 75%/25% targets ARE the retune, not a follow-up unit or ticket. Owner ruled (e) (revision 19, AskUserQuestion, 2026-09-19) on the separate ratified-vs-ratified conflict this unit surfaced against `hpg-tonal-cusp-pull` (#55, pass-4 addendum, `.sdlc/questions/pif-u3.md`): the "0 above 100%" ceiling (C6) stays for even and peak; perceptual gets one named exemption for its own richest (cusp) stop only, bounded by a measured max excess U3 freezes in the gate. Pass 5 (`f21f589`/`a28fa18`): step 1 restored the OKHSL joint (s,l) cap scoped to `mode === "peak"` only, closing peak's "0 above 100%" count to exactly the 16 named Adia palettes with C8's full 96-cell sweep showing 8 cells moved, max |delta| 0.0091, none crossing the 4.5 floor; perceptual stays untouched, byte-identical to pass 3's head (`986c032`). Step 2 measured before building anything (per the second-workaround rule) and found the one-stop premise does not survive contact with the corpus: only 24.4% of perceptual's 2,207 above-anchor palettes have exactly one above-anchor stop, the other 75.6% have 2-5 ADJACENT stops (a natural cusp shoulder), so a strict one-stop cap produces a lone chroma spike, not a graceful exception. Owner ruled (f) (revision 20, AskUserQuestion, 2026-09-19): perceptual exempts ONE CONTIGUOUS RUN of above-100% stops per ramp instead of one stop, bounded by 189.3005% of stop 500's chroma (`d5c09c3`'s fresh, cusp-scoped re-measurement, confirming the earlier 89.3pp figure was not stale, only under-labeled — witness "cuisine/Sushi & sashimi · the cypress counter/primary-muted", cusp stop 650); a second, non-adjacent run, or any stop past the bound, FAILs. No ramp construction change; `hpg-tonal-cusp-pull` (#55) ships as today. Median/p90 targets are unchanged for perceptual. The retune (revision 18) proceeds on top once this gate shape lands. Ramp-shape gates for C6, replacing any magnitude bar on #668 (a chroma cliff repair moves more than one 8-bit channel, so a sub-pixel bar is unsatisfiable): (i) zero measured-L* upticks over the corpus: for every one of the 3,780 palettes in each of perceptual, peak and even, measured CIELAB L* is non-increasing 050 → 950 on the 19-stop display ramp and the 25-stop export ramp; the report prints the per-mode count of palettes with any rise and the worst rise; pass is 0 / 0 / 0 on both stop sets (today on bf2aaf6, `baseline-7-668-export.mjs`: 25-stop export ramp 11 / 46 / 0, 19-stop display ramp 11 / 43 / 0). Two named sub-cases, distinct mechanisms: (i-a) the dark-side upticks at stop 800 under lift ≤ −30, 11 in perceptual (worst +0.5105, travel "34° S San Telmo" secondary-muted) and 38 in peak (worst +0.8312, travel "59° N Lake Baikal corridor" primary-muted), witnesses including travel "59° N Lake Baikal corridor" primary-muted and travel "16° N Bogyoke Aung San Market" secondary in both modes; (i-b) the light-side mirror at stops 200 to 250 under lift ≥ 32, peak only, 8 palettes on the 25-stop ramp (5 of them visible on the 19-stop ramp), worst +0.1543 (travel "41° N La Boqueria" secondary-muted, lift +40, stop 250). #668 R1 takes (i-a) and (i-b) to 0 first and U3 keeps them there. (ii) zero duplicate hexes anywhere in the ramp: no two stops of any palette's 19-stop or 25-stop ramp share a hex, in any mode; the report prints the per-mode count; pass is 0 / 0 / 0 on both stop sets, measured on the RENDERED chroma path (`rampChromaOf` plus hueShift/hueSameDir/cuspPull, matching `src/ui/model.mjs`'s `projectView`), not raw `palette.chroma` (revision 11's "21 pre-existing, 7 residual" figures were an artifact of that raw-chroma proxy and are retracted; on the rendered path the pre-U3 baseline is already 0, and the shipped engine keeps it at 0). Negative control: the raw-chroma proxy pointed at the pre-U3 base reads a vacuous 0/0/0 even where the real (#668) defect exists; the rendered-path gate pointed at the same base correctly reads 11 perceptual / 46 peak, matching the plan's own #668 figures exactly, proving the corrected gate is non-vacuous; separately, patching the shipped engine back to its own first-attempted chroma-centering formula reproduces 2 named duplicates, proving the gate discriminates a real regression. Boundary, not a gate (U3's handoff, not a plan criterion): 21 of a 10,080-cell synthetic hue × chroma × skew × lift sweep still rise in CIELAB L* under the shipped construction (worst +0.1314, near white), unused by any shipped preset or default (the cells sit inside user-settable hue/chroma/lift domains, so a user CAN reach them; no curated preset happens to) and named in U3's own fixture rather than a corpus-facing allow-list. (iii) no `docs/` literal moves without a named exception: `git diff --stat origin/main -- docs/` lists only the paths U3's handoff names with a one-line reason each (expected: `docs/reference/data/adia-oklch-export.css` and `adia-radix-export.mjs`, regenerated by `gen:adia-exports` and versioned 1.1.0 under C9; the records U5 owns); any other `docs/` path in the diff is a FAIL. (iv) a per-preset movement table, not a magnitude gate: `node scripts/report-preset-fidelity.mjs --movement` prints, per preset and per mode, the palettes moved, max |dL*| at any stop and max dChroma at any stop, plus the corpus totals (count moved / 343, max |dL*|, max dChroma); it ships as U4's blast-radius report and U3 lands only after the owner accepts it, recorded as a 🟢 answer in `.sdlc/questions/preset-intent-fidelity-u3-movement.md`; the verifier checks that the record exists, names the report's sha, and carries the owner's acceptance line, and does not grade the magnitudes. Team-lead ruling (revision 22, not the owner's), on the planner's pass-6 re-diagnosis (`preset-intent-fidelity-u3-rediagnosis.md`, `046044a`): pass 7 proceeds on its recommended two-part construction, not another plain parameter retune. Named mechanism: the two reverted pass-6 retunes each reopened a real CIELAB-L* uptick via Helmholtz-Kohlrausch (CIELAB L* rises when chroma falls at fixed OKHSL `l`, `#668`) — a lower `dampCurve` concentrates damping near the anchor, producing an uneven chroma drop between adjacent stops large enough to move measured L*; this fires only in the OKHSL-domain modes (perceptual, peak), never even, since even's `toneAt` sets CIELAB L* directly and chroma damping there cannot move it. Construction: (1) an even-only `dampCurve` retune, no coupling risk, closing `even|100`/`even|300`/`even|900`; (2) a tone-held joint `(s, l)` solve for `perceptual|300` and `peak|700` only, holding CIELAB L* fixed per stop while damping chroma, reusing the already-shipped pass-4/5 peak-cusp-cap technique rather than a third bare retune. Stop rule: if (2) cannot close both `perceptual|300` and `peak|700` at 0 upticks, U3 stops there — no pass 8 — and reports "which target yields, by how much" with real measured numbers for the owner to rule on, the same shape as every other measure-before-claiming practice in this unit.
- [x] U4 (M) integration (U1 → U2 → U3 → U6), corpus regeneration and blast-radius report · grade l3
  Integration step added revision 14, team-lead-ruled (2026-09-18, not an owner ruling) on U2's re-diagnosis's proposal (Finding 9, `preset-intent-fidelity-u2-rediagnosis.md`): U2, U3 and U6 were built in parallel off the plan branch and all touch `tonal.js` or its callers; `git merge-tree` auto-merges the files textually but the result is semantically broken (anchored palettes skip `chromaEnvelope` entirely, C7's greps fail on the merged tree, U6 branches without U1 and conflicts with `prime.mjs`). Sequence: U1 (common ancestor) → U2's own repair pass lands first (it fixes the exact seam here, routing the anchored branches through `chromaEnvelope` keyed on `liftStop`) → U3 merges on top (now textually and semantically compatible) → U6 rebases onto the result (resolving the `prime.mjs`/U1 conflict once, at the integration point). After each merge: re-run C7's three greps, and re-run every rendered-path gate (C3, C5, C6, C8, C11) on the INTEGRATED tree — a unit's own isolated green is not sufficient once its neighbours have merged. Only once the integrated tree is green does the rest of this unit run: every generator, the moved assets committed, and `scripts/report-preset-fidelity.mjs` (lifted from the two scratch scripts) whose output goes into the PR body and `.sdlc/handoffs/`: presets moved / 343, palettes moved / 3,780, max |dL*| per stop and per mode, max dChroma, per-family default-kit ratios before and after, exports that moved (the 10 colour formats, the DS bundle, `figma/plugin/ui.html`, `src/ui/categories/*.js`, `mcp` assets, `docs/reference/data/adia-*` at 1.1.0), C4's identity-control line, C5's allow-list count, #668's count. Team-lead ruling (revision 18, not the owner's): since U3's `damp`/`dampCurve` retune (its own paragraph) is a visible change to every ramp, this report gains a `damp`/`dampCurve` before/after row (the shipped defaults versus U3's retuned values, and the corpus-wide movement that retune causes on its own, separated from the anchor/envelope movement already reported) and the full per-mode median/p90 table (all stops, all three modes, before and after) so the owner rules on the retune's magnitude with real numbers at the C6(iv) gate, not just the pass/fail line. Criteria: C1, C4, C7, C9. Depends on U1 to U3 and on U6, and on U2's repair pass per its re-diagnosis (not the pre-repair `3cfa605` head).
- [x] U6 (M) prime ladder steps equally in perceived lightness · grade l4
  `src/engine/prime.mjs`: the ladder is built in CIE L* around the anchor's L* (rung k = L*prime ± k × STEP_L, STEP_L 9 by default, Q8), both sides take `min(roomUp, roomDown)` when either side hits the window (equal-compress), each rung rendered by `hctToRgb(anchorHue, min(C_prime, maxChromaInGamut(hue, L)), L)` so chroma is held and only the gamut desaturates; `primeSteps` becomes the L*-domain equivalent; `skew` keeps its per-side gamma on the normalised position; `PRIME_L_MIN/MAX` are re-expressed as the L* window [12.25, 96.88] (constants derived from the greys, not retyped). `test/engine/prime.mjs`: d1/d3/d4/d6 rewritten per the gate table, d5 re-frozen, g re-based on chroma, a new `symmetry` gate (|up − down| ≤ 1e-9 L* on every case, and ≤ 3 L* measured from pixels), a `ladder-window` gate that prints the 21-name allow-list with its expected count (C5), and the three clipped defaults (Tertiary, Danger, Warning) asserted at their equal-compress spans 52.8 / 49.9 / 46.3 L* ± 0.05. Criteria: C1, C5 (ladder half), C11. Depends on U1 (anchor = prime); independent of U2/U3.
- [ ] U5 (S) records · grade l2
  `docs/reference/references/knowledge-02-tonal-scale.md` (§2 controls: `anchor`; §4 `toneAt` pinned form; §5 the envelope replaces the `m` formula; new §9 "Anchored palettes"; §8.3 the anchor branch of the ladder), `glossary.md` (rows `Lift`, `Prime fill`, `dampAmp`; new `Anchor`), `docs/reference/rubrics/{acceptance-criteria,quality-rubric}.md` (the predicates that bless `liftForTone` and the 1.5 L* anchor tolerance become the exact-anchor predicate), `docs/reference/CHANGELOG.md` 1.62, `.claude/skills/color-math/SKILL.md` and `references/foundations.md` (the two-path warning gains the anchor rule and the single envelope), `docs/reference/references/decision-records.md` ADR-025 "A palette's anchor is stored, not fitted" appended before Quick map, `docs/reference/data/adia-*` tag notes. U6's ladder change also amends the approved SPEC and LLD it was ratified under: `docs/spec/spec-muted-base-key-spikes.md` REQ-050 (the OKHSL-`l`, `PRIME_STEP` construction, lines 181-184), REQ-051/REQ-051a (the redistribute-wall rule and the `[PRIME_L_MIN, PRIME_L_MAX]` window, lines 185-228, replaced by U6's L* window and equal-compress), REQ-052's flat-OKHSL-`s` chroma rule (lines 229-236, replaced by hold-CAM16-chroma), EX-4/EX-4b/EX-5's literal ladder values (lines 307-329, regenerated), AC-050 (b)/(d)/(d1)/(d3)/(d4)/(d5)/(d6) (lines 413-450, restated to U6's gate table), and R1 (lines 504-512, amended a third time); `docs/spec/spec-panda-park-ui-exports.md` EX-1's normative `tokens.colors.primary.prime.{prime,brightest,dimmest}` OKLCH literals (lines 422-428 on the plan's bf2aaf6 head; U6's builder re-pinned the `test/engine/exports.mjs` mirror but left this spec doc unedited per lane rules, so it is stale on the branch, not a mechanical-regeneration exception like #657/#662); its companion `docs/lld/lld-muted-base-key-spikes.md` (the `prime.mjs` pseudocode block naming `PRIME_STEPS`/`PRIME_STEP`/`PRIME_L_MIN`/`PRIME_L_MAX`, lines 68-89, and the Yellow-compression finding's redistribution narrative, lines 196-225); both version-bumped past 0.3.2/0.3.0 with a dated amendment note, per the SPEC's own correction convention (REQ-051 has already been amended twice this way). Criteria: C10. Depends on U1 to U3 for the final numbers and on U6 for the ladder SPEC/LLD repair. Landing (Conductor ruling, 2026-09-19): records-refresh (#691) lands first; this plan lands after it and changes the test-file count, so its pre-land commit also updates `.sdlc/baseline.md`'s test-file figure, and the pre-land pass (reviewer-l3 and verifier-l3) runs `baseline-agrees-check.sh` as a gate, since that script compares the recorded figure against `TESTS.length` and a landing that adds a test file without repairing the figure leaves main with the stale record records-refresh exists to remove.

## Blast radius (what U4 must report; the per-preset movement table is what the owner accepts before U3 lands, C6 iv)

| Measure | Expected direction |
|---|---|
| presets with any moved byte | 343 / 343 (the envelope is global); with the envelope reverted, 338 (the 5 direct Adia presets are untouched by U1/U2) |
| palettes with `prime.DEFAULT` moved | 3,380 (all fitted) plus the 16 default-kit palettes (Q2 b); 0 direct unless a JSON opts in (Q5) |
| max dL* at stop 500, anchored | up to 23.2 at the worst stop (median 6.6); the clamped presets landing on their source |
| max dChroma at any stop | up to about 99 CAM16 units at stop 500 on vivid sources (the 44% overshoot removed) |
| default kit ramps | move under U2 ALONE first (all 16 default palettes carry `anchor` per Q2(b); C2/C3 name no default-kit carve-out and none exists in the anchored branch), then again under U3's envelope — corrected from the plan's earlier "via U3 only" row (U2's Q-U2-2 finding). U2's own measured movement, proxy, not yet re-measured on the rendered path: max \|dL*\| 4.7 to 16.3 (perceptual) / 5.9 to 20.5 (even) across the 16 families, worst Warning at stop 250 |
| default kit `prime.*` | move via U6 (metric, wall and chroma rule) for all 16 families; and via U1 under Q2 (b), ruled (the anchor becomes today's stop-550 hex, so `prime.DEFAULT` moves for all 16 families, e.g. Secondary `#00FBB1` to `#108960`); the d5 snapshot is re-frozen once |
| corpus `prime.*` other six rungs | all 3,780 move via U6; the `prime` rung moves only via U1 (3,380 to their source) |
| exports | all colour formats, DS bundle, Figma plugin bundle, categories modules, MCP assets, Adia artifacts; `role-table.json`, binder `code.js`, type and geometry exports unchanged |

## Risks and sequencing

| Risk | Handling |
|---|---|
| #662 (on-color policy) | Landed (PR #682, `1ea2f80`): `onColorMode` defaults to `contrast` with an achromatic fall-through, no stop moved; `hpg-role-contrast` now pins all 16 families at AA in both schemes. C8 re-measured on bf2aaf6. The on-color is no longer stop 050, so the plan's earlier accent/050 proxy columns are historical. |
| #674 (curated-corpus contrast gate) | Landed (PR #683, `bf2aaf6`): `test/engine/curated-contrast.mjs` is a flat AA floor over all 343 documents, 7,560 accent/on-color cells, expectation `under: 0, worst: 4.50` per category, sampled in `npm test` and full in `npm run gate:corpus-contrast`. U3 moves every ramp, so U4 runs the full sweep and any miss must be written down by name in the gate's carve-out table, never normalised away. |
| #656 (travel hex/oklch) | Landed (PR #679, `e3ef1d6`): hex and oklch agree; the anchor is minted from the hex. |
| #657 (hue solver best iterate) | Landed (PR #678, `bda9584`); the baseline rows above are re-measured on 279dc74 and the C4 identity control compares against that head. |
| #668 (measured L* upticks: 11 perceptual and 46 peak on the 25-stop ramp on bf2aaf6, 38 of the peak cases dark-side at 800 under lift ≤ −30 and 8 light-side at 200 to 250 under lift ≥ 32) | Lane A lands R1 now, keying the damping term on `liftStop()`. U3's envelope must stay keyed on `liftStop()` (not a re-derived effective stop) or the cases return; C6's named cases check all three modes plus the no-new-duplicate line. Anchored presets also store lift 0, so U4's corpus count is expected 0 either way. Land #668 R1 before U3 is dispatched. |
| `intensity-legacy` fixture (frozen at 83756bb) reds on any envelope change | U3 regenerates it with the rationale; the fixture's purpose (proving `baseIntensity` retirement) is preserved by keeping the two-mode comparison. |
| Sources outside the workable window (10 for the ramp, 21 for the ladder, named in C5) | Q3 (b) ruled: token exact, ramp lands at the nearest window edge, both gates print the named allow-list with its expected count. |
| U6 moves every `prime.*` token in every export (3,780 corpus ladders, 16 default families) and re-freezes the d5 snapshot; a light cusp anchor collapses to a short ladder under equal-compress | The corpus report (U4) prints the short-ladder list; Q2 (b) removes the default-kit cases; the d5 re-freeze names its capture commit so the gate stays independent of the new construction. |
| Users editing an anchored preset copy | Q6 ruled: hue/chroma edits detach, a Reset action re-attaches; in U2's scope with a shim test (C12). |
| With the anchor at 500 (Q1) neither scheme accent is the source: the light accent reads about 4 L* darker and the dark accent about 6 L* lighter (medians); sources near white (105 above L* 85) still give pale accents | That is the ruling; the on-color policy (#662) is the tool for readability, not the anchor. U4 reports both accents' dL* from the source per preset. |

Sequence: #668 R1 lands (#657, #656, #662 and #674 have); then U1 → U2, U3 and U6 in parallel → U4 → U5 and pre-land review.

## Open questions: all ruled (owner via the conductor, 2026-09-18, `scratchpad/plan-rulings-preset-intent.md`)

| Q | Question | Plan recommended | Ruled | Where it lands |
|---|---|---|---|---|
| Q1 | which ramp stop carries the anchor | 550 | 500 | mechanism (1) C, envelope centre, U2, C3, C5, C6, blast radius; re-measured above |
| Q2 | default kit anchors | (b) after the ladder measurement | (b) mint from today's stop-550 hexes | U1, mechanism (3), C11, blast radius |
| Q3 | sources the window cannot hold | (b) | (b) token exact, ramp at the nearest workable L*, gate names them | C5 (9 ramp / 21 ladder names), U2 |
| Q4 | envelope numbers | 300/700 ≤ 75%, 100/900 ≤ 25%, none above 100% | as recommended | mechanism (2) target table, C6, U3 |
| Q5 | authored brand presets | opt-in `anchor` | opt-in | U1, blast radius |
| Q6 | edits on an anchored copy | (a) detach on hue/chroma | detach on hue/chroma, plus a Reset action that re-attaches (owner: "we should have a Reset action to re-attach") | U2, C12 |
| Q7 | `dampAmp` | keep the slot, default 0, document | as recommended | mechanism (2) E, U3, U5 |
| Q8 | ladder span | keep 54 L* | keep 54 L* | U6, d1 constant |
| Q9 | ladder metric, step, wall, chroma | CIE L*, 9 L*, equal-compress, hold CAM16 chroma | as recommended | U6, C11, gate table |

The pre-ruling option text for each question is kept in the plan's git history (revision 2 below) and in `scratchpad/plan-rulings-preset-intent.md`.

## Revisions

| Date | Change | Why |
|---|---|---|
| 2026-09-19 | revision 26: C1's negative control reworded to what reproduces. The U2 verifier (`verdict-pif-u2.md` row 8) corrupted `role-table.json` two ways, one field and every `light` ref, and got exactly one FAIL line each time, because `refs-canonical` reports the first mismatch and stops; the "17 FAIL lines" figure the control borrowed from `.sdlc/adapter.md` §1 does not reproduce from a role-table corruption. The control now expects exit 1 and at least one FAIL line naming the mismatch, with the recorded one-line shape | team-lead, on the U2 verifier's plan-side attention row |
| 2026-09-19 | revision 25: U5 (records) gains the landing line recording the Conductor ruling that this plan lands after records-refresh (#691) and, if its test-file count differs from the recorded figure, updates `.sdlc/baseline.md`'s figure in its pre-land commit with `baseline-agrees-check.sh` run as a pre-land gate | the Conductor ruled that every plan landing after records-refresh that changes the count repairs the figure itself, so main never carries a stale record |
| 2026-09-18 | drafted from the owner's ruling with a measured baseline on 988420d | planner-preset-intent, read-only; no ticket yet, priority unranked |
| 2026-09-18 | added baseline (3) prime ladder span and symmetry, the PRIME_STEP 0.09/0.11/0.13 rung table, the ruled-ladder simulation, mechanism (3), U6, C11, Q8, Q9; Q2 recommendation revised to (b); blast radius rows for `prime.*` | owner finding (ladder not 1:1 around prime) ruled as equal perceived-lightness steps; span widening still open |
| 2026-09-18 | revision 1 (team-lead request plan-revision-1.md): confirmed U6, C11, the symmetry and span tables, Q8/Q9 are present; added the OKHSL up/down row and a U6 risk row | checkability review pre-pass |
| 2026-09-18 | revision 2: nine rulings applied (Q1 anchor stop 500 against the 550 recommendation, re-measured on 279dc74: 3,380 exact at 500, accents −4.4 / +6.2 L*; Q2 b; Q3 b with the 21-name allow-list in C5; Q6 Reset action into U2 with C12); head set to 279dc74 after #657 and #656 landed; baseline (1), (2) and the prototype re-run; open questions replaced by the ruled table | checkability review reds C5 and U2 resolved by the rulings |
| 2026-09-18 | revision 3 (checkability yellows): `src/ui/model.mjs` added to U1 for the Q2 (b) anchors; C1, C6, C7, C8, C9, C10, C11 rewritten with a command, a numeric threshold and a control each (contrastLint today 0 / 0 / 9 per mode measured on 279dc74); C4 pins the identity control to `origin/main` at branch creation with the sha in the handoff; U3's `dampAmp` conditional replaced by a rule | checkability record `scratchpad/plan-preset-intent-checkability.md` |
| 2026-09-18 | revision 4 (re-check): C5 allow-list count corrected to 10 in all three places and the ten names listed; C9 states the `role-table.json` change (16 `defaults[].anchor` fields, parity loop extended) instead of "unchanged"; C10's vacuous `liftForTone` grep replaced by an `anchored palette` grep that is 0 on main; C12 names `sourceAnchor` as the field Reset reads; U6 names Tertiary, Danger, Warning as the defaults that clip at STEP_L 9 with their spans, d1/d5 rows and the `ladder-window` gate | re-check in `scratchpad/plan-preset-intent-checkability.md` |
| 2026-09-18 | revision 5: U3's envelope keyed on the effective (lift-displaced) stop, the nine #668 palettes named as a C6 case with expected 0 upticks, #668 R1 added as a dependency in Risks and the sequence | team-lead, #668 R1 landing in Lane A |
| 2026-09-18 | revision 6 (`scratchpad/plan-revision-6-u3.md`): U3 keyed on `liftStop()` by name, not a re-derived effective stop; C6's #668 case corrected to 11 palettes worst 0.5105 in perceptual, the 43-palette peak mirror added, and a no-new-duplicate-hex line (0 / 0 / 0 today); C5 and the #668 risk row aligned. Head moved to bf2aaf6: #662 (PR #682) and #674 (PR #683) recorded as landed, C8 re-measured (contrastLint 0 / 0 / 0, 16-family floors, corpus sweep), C1's test count re-derived from `TESTS.length` | Lane A's #668 R1 grid evidence; main advanced under the plan |
| 2026-09-18 | revision 7: C6's #668 handling reshaped to four ramp-shape gates (zero measured-L* upticks, zero duplicate hexes, docs/ literal moves by named exception only, a per-preset movement table that ships as U4's report and needs owner acceptance before U3 lands); no magnitude bar; U3 gains that landing condition | team-lead: a sub-pixel bar on a chroma-cliff repair is unsatisfiable |
| 2026-09-18 | revision 8 (`scratchpad/plan-revision-7-u3-facts.md`): C6 (i) peak upticks are 46 on the 25-stop export ramp (43 on the 19-stop), split into the dark-side case at 800 under lift ≤ −30 and a light-side mirror at 200 to 250 under lift ≥ 32; C6 (ii) names the duplicate-swatch witness (nature "70° N Varanger" tertiary, peak, stops 150/175 `#FDFDFA` on the R1 residue) and widens `hpg-tonal-lift-monotonic` from the 16 defaults to the corpus; C5, the #668 risk row and U3's file list aligned | Lane A's #668 investigation, re-measured on bf2aaf6 |
| 2026-09-18 | revision 9: C11 gains a gamut-ceiling clause (U6's hold-CAM16-chroma ladder per Q9 pushes 1,288 of 302,400 rungs, 0.43%, out of sRGB gamut on the full sweep, surfacing as the product's existing `inGamut` hover-footer flag; owner accepted, ceiling ≤ 0.5%/1,350 so a silent widening is caught); U5's file list gains the approved SPEC and LLD sections U6's ladder change amends (`docs/spec/spec-muted-base-key-spikes.md` REQ-050/051/051a/052, EX-4/EX-4b/EX-5, AC-050 (b)/(d)/(d1)/(d3)/(d4)/(d5)/(d6), R1; `docs/lld/lld-muted-base-key-spikes.md`'s `prime.mjs` pseudocode and Yellow-compression finding), both version-bumped past 0.3.2/0.3.0 | team-lead, owner ruling 2026-09-18 |
| 2026-09-18 | revision 10: U5's file list also gains `docs/spec/spec-panda-park-ui-exports.md`'s EX-1 normative `tokens.colors.primary.prime.{prime,brightest,dimmest}` OKLCH literals (lines 422-428 on the plan's bf2aaf6 head, corrected from the ruling's remembered 419-424), stale on the branch once U6 lands since U6's builder re-pinned the `test/engine/exports.mjs` mirror but left the spec doc itself unedited per lane rules | team-lead, owner ruling confirmed in chat with Lane A but never previously written into the plan |
| 2026-09-18 | revision 11 (two of three corrections; the third is held for a factual re-check — see the handoff to team-lead): (1) C11's revision-9 gamut-ceiling clause retired — the 1,288/302,400 figure was a shared gamut-cache determinism-bug artifact, not a ladder property; the fixed engine measures 0, so a plan criterion pinning a ceiling near 1,288 would be tautological; U6 keeps its own non-tautological determinism regression gate at the unit level, not as a plan criterion. (3) U3's C6(ii) zero-duplicate-hex gate rescoped to "no worse than the measured pre-U3 baseline": the peak-mode near-white duplicate-hex class pre-dates this plan (21 ramps on `362cc48` with no #668 fix applied at all — revision 8's premise that the baseline was 0 was wrong); U3's shipped engine cuts it to 7 named ramps (10 stop-pairs), listed by exact hue/chroma/lift/stop-pair as a load-bearing named-exception allow-list; revision 8's Varanger witness corrected (still duplicates, but at stops 200&250 `#FAFAF9`, not 150&175 `#FDFDFA` — the collision point is formula-dependent) | team-lead, owner rulings relayed from the U6 and U3 builders' review folds |
| 2026-09-18 | revision 11 (2), completing the set: C5 gains U1's own `anchor-ladder` dupe-allow-list (4 named exact-duplicate exceptions where `prime` and an extreme rung render byte-identical hex, not the 1-unit-apart near-miss a peer reviewer's report had described — a planner direct-verification against the U1 worktree found the two hexes identical, and the owner's original ruling — accept exact duplicates, name them — stands unchanged) | team-lead, owner ruling reaffirmed after planner verification |
| 2026-09-18 | revision 12: revision 11's C6(ii) rescope retracted. The "21 pre-existing, cut to 7" figures were an artifact of measuring raw `palette.chroma` instead of the rendered `rampChromaOf` path the product actually uses; on the corrected rendered-path measurement the pre-U3 baseline is 0, U3's first-attempted mechanism (Design A) introduced 2 duplicates, and the shipped mechanism (Design B) closes both, so C6(ii) is restored to a clean "0 duplicate hexes, both stop sets" gate. The 7 named ramps (Varanger, the cuisine and other nature witnesses) are retracted as artefacts of the broken proxy, not real defects. A boundary note (21 of a 10,080-cell synthetic sweep still rise, unreachable by any real preset) is kept as U3 handoff/fixture documentation, not a corpus-facing gate | team-lead, Lane A's corrected rendered-path measurement |
| 2026-09-18 | revision 13: wording fix, the 21-cell boundary note in U3's ramp-shape gate (ii) now says "unused by any shipped preset" rather than "unreachable" — the cells sit inside user-settable hue/chroma/lift domains, so a user CAN reach them, no curated preset happens to | team-lead, Lane A's reviewer |
| 2026-09-18 | revision 14: a rendered-path rule stated once in the criteria preamble and cited from C3/C5/C6/C8/C11 (two units, U2 and U3, independently built early gates on a raw-`palette.chroma` proxy and had to retract numbers). C3 rescoped to the 3,370 in-window sources plus C5's 10 named clamps (U2's Q1 reading confirmed correct). C6 gains a named carve-out for authored `dampAmp > 0` (today: Adia, 16 palettes) on the "0 above 100%" clause only; the median/p90/100% targets themselves are NOT rescoped (owner: hold the bar, keep fixing) and U3's paragraph records the measured baseline (52-79% of non-Adia curated palettes above 100%, driver lift sign × hue-cusp tone) it must close. Blast-radius table corrected: default-kit ramps move under U2 alone, not via U3 only (U2's Q-U2-2 finding), with U2's own proxy movement numbers recorded. U4 gains an explicit integration step (U1→U2→U3→U6, C7 and every rendered-path gate re-run on the merged tree), team-lead's own ruling (not the owner's) on U2's re-diagnosis's proposal, sized up to M/l3 and gains C7 as a criterion | team-lead: owner rulings 2026-09-18 for the C6 items; team-lead's own ruling, not the owner's, for the U4 integration step (per U2's re-diagnosis, `preset-intent-fidelity-u2-rediagnosis.md`) |
| 2026-09-18 | revision 14 (2): owner ruled the re-diagnosis's Finding 2 (F4) as reading (a) — anchored palettes keep `toneAt`'s curve, tension, vibrancy and hue space composed with the anchor pivot; U2's own paragraph already specified this, so no mechanism change, only the ruling's confirmation and an explicit "no straight-lerp shortcut" clause recorded there. Finding 9's integration step (U1 → repaired U2 → U3 → U6, rendered-path gates re-run on the integrated tree) is confirmed as team-lead's OWN ruling in U4's paragraph (not the owner's — the human never ruled on it), not merely a planner recommendation | F4: owner ruling 2026-09-18; Finding 9's integration step: team-lead's own ruling, not the owner's |
| 2026-09-18 | revision 15: a crossed-message check confirmed revision 14 (2) (`3c27879`) already recorded the F4 ruling and the "no straight-lerp shortcut" clause team-lead's follow-up asked for; the one genuinely new piece — naming "peak and perceptual must not render byte-identical for an anchored palette" as an explicit check, not just a symptom description — added to U2's paragraph | team-lead, owner ruling 2026-09-18 |
| 2026-09-18 | revision 16 (provenance fix): U4's integration step and its two revision-log mentions (14 and 14 (2)) wrongly attributed the U1→U2→U3→U6 integration ruling to the owner; it was team-lead's/the conductor's own ruling, never put to the human. Corrected U4's paragraph and both rows to attribute it to team-lead. F4's attribution to the owner is unchanged, it was theirs | team-lead |
| 2026-09-18 | revision 17: Q-U2-5 ruled — the re-diagnosis's Finding 1 fix (anchor's own chroma as the LITERAL, unconditional `chromaEnvelope` basis) broke REQ-002 (a ratified spec predating this ticket: group Base chroma went dead for every anchored ramp). Team-lead ruling, applying the owner's F4 principle (keep the ratified text, fix the construction): keep REQ-002 as ratified, keep `chromaEnvelope` verbatim, blend the anchored branches' basis input from the anchor's own chroma at stop 500 to `rampChroma` at the ends. Recorded in U2's paragraph with the check "Base chroma moves an anchored ramp's ends while stop 500 stays byte-exact" | team-lead (not the owner) |
| 2026-09-18 | revision 18: U3 proved several C6 median/p90 misses pre-exist on `origin/main`, unrelated to lift (a `bf2aaf6` re-measurement is essentially unchanged, and a lift=0 split rules lift out); the real cause is the shipped `damp`/`dampCurve` defaults. Team-lead ruled the resulting retune stays inside U3's scope (the 75%/25% targets ARE the retune, not a follow-up unit or ticket) — recorded in U3's paragraph. U4's report gains a `damp`/`dampCurve` before/after row and the full per-mode median/p90 table so the owner rules on the retune's magnitude with real numbers at the C6(iv) gate — recorded in U4's paragraph | team-lead (not the owner) |
| 2026-09-19 | revision 19: U3's pass-4 addendum surfaced a ratified-vs-ratified conflict: C6's "0 above 100%" ceiling, capped literally, forced 12 of 16 default-kit palettes' richest stop to exactly 500 in perceptual mode, erasing the hue-cusp-following richness `hpg-tonal-cusp-pull` (#55, pre-existing) requires; the pass-4 OKHSL cap that produced this was already reverted byte-for-byte to `986c032`. Owner ruled (e): the ceiling stays for even and peak (peak's richest stop sits at 500 by construction per `okhsl-modes`, so no exemption there); perceptual gets one named exemption for its own richest (cusp) stop only, bounded by a measured max excess U3 freezes in the gate, with a negative control (a second above-100% stop, or excess past the frozen bound, must FAIL); median/p90 targets are unchanged for perceptual. Recorded in C6 and U3's paragraph. Noted the bound is not yet measured against the reverted construction — the 89.3pp/93.8pp/241.6pp figures already in U3's paragraph predate this exemption's scoping and must not be copied in as the frozen number without a fresh, cusp-stop-scoped measurement | owner (AskUserQuestion, 2026-09-19) |
| 2026-09-19 | revision 20, three items. (1) Owner ruled (f): U3 measured that revision 19's one-stop premise fails on 75.6% of perceptual's above-anchor palettes (2-5 adjacent stops, a natural cusp shoulder, not one point); perceptual now exempts one CONTIGUOUS RUN per ramp instead, bounded by a fresh, confirmed measurement of 189.3005% of stop 500's chroma, gated both ways (a second run, or any stop past the bound, FAILs); no ramp change; `hpg-tonal-cusp-pull` (#55) unaffected. Recorded in C6 and U3's paragraph. (2) Team-lead: U2's near-grey notch gate reads ratio PLUS an absolute dip of at least 3 CAM16 C, since ratio alone explodes near a near-grey anchor; the 70%-ratio bar itself is unchanged; the residual is named "pending U4" in `NOTCH_ALLOW`, re-measured on the integrated tree. Recorded in C5 and U2's paragraph. (3) Team-lead: U2's 41 (of 96) `hpg-role-contrast` entries reading below their pre-#681 value (AA held) are pinned by name as "pending U4" rather than re-pinned or failed now; the owner rules once at U4's report with final, integrated numbers. Recorded in C8 and U2's paragraph. Held: the brief's specific residual count for item (2), "76 (15/9/52)", could not be verified against the U2 worktree at its current head and was not written into the plan; flagged to team-lead | item 1: owner (AskUserQuestion, 2026-09-19); items 2-3: team-lead (not the owner) |
| 2026-09-19 | revision 21 (Q-D, under F4): held for verification first — the planner's own read of the shipped F4 fix showed `hueSpace` live and hex-moving in all three modes for anchored palettes, contradicting a first draft of this ruling that called it a no-op in perceptual/peak. Two independent sources (U2 review 3; a scout probe at `e426c74`) then confirmed the hex moves in perceptual/peak (3,393/3,390 ramps) are 8-bit rounding only (dE_OK ≤ 0.01), while even's move (up to 20 codes) is real (Abney). Ruled: engine unchanged; `hueSpace` stays live for anchored even; the inspector hides/disables it for anchored perceptual/peak with a one-line reason, per F4's no-dead-sliders rule. Gate: ≤ 2-code diff bound in perceptual/peak, an above-JND dE_OK bound in even, a headless-shim presence/absence check, and a negative control. Recorded in U2's paragraph | team-lead (not the owner) |
| 2026-09-19 | revision 22: on the planner's pass-6 re-diagnosis (`preset-intent-fidelity-u3-rediagnosis.md`, `046044a`), team-lead ruled pass 7 proceeds on its recommended two-part construction rather than a third plain retune. Named mechanism: pass 6's two reverted retunes each reopened a CIELAB-L* uptick via Helmholtz-Kohlrausch (`#668`, chroma damping moves measured L* at fixed OKHSL `l`), firing only in the OKHSL-domain modes (perceptual, peak); even's `toneAt` sets L* directly and is immune. Construction: an even-only `dampCurve` retune (closes `even|100`/`even|300`/`even|900`, no coupling risk) plus a tone-held joint `(s, l)` solve for `perceptual|300`/`peak|700` only, reusing the pass-4/5 peak-cusp-cap technique. Stop rule: if the joint solve cannot close both coupled-mode misses at 0 upticks, U3 stops (no pass 8) and reports which target yields, by how much, measured, for the owner. Recorded in U3's paragraph | team-lead (not the owner) |
| 2026-09-19 | revision 23 (Q-D gate restated, under revision 21's ruling): U2 review 4 (`pif-u2-review-4.md`) measured the whole anchored corpus: "≤ 2 codes" holds on the default kit, but 56 of 3,396 ramps move 3-12 codes on the gamut face at max dE_OK 0.0053, 0 ramps above 0.01. The premise (no visible effect) holds, the code-count proxy does not, so the gate is now dE_OK ≤ 0.01 over the corpus; the 2-code bound stays as a default-kit check. Ruling unchanged: engine as shipped, hide/disable for anchored perceptual/peak, live in even | team-lead (not the owner) |
| 2026-09-19 | revision 24 (U3 yield, three owner rulings, `.sdlc/questions/pif-u3-yield-answer.md`): (1) step 2 reverted, perceptual\|300 (p90 93.7%) and peak\|700 (p90 96.8%) named as yielded under C7's zero-uptick hard stop, carried to U4's blast-radius table; (2) the 53 even-mode 3-4 C dips from step 1 stay, listed by name with a <= 53 count gate, chromaFloor redesign to a follow-up ticket; (3) F1's peak-cap trade (dips 221 to 6; tone drift 35% > 0.01 L*, max 0.10; hue residual <= 24.6 deg) accepted for U3, ruled at U4 with final numbers. U3 at da351505 goes to the verifier on these terms | owner |
| 2026-09-20 | revision 27 (U5 handoff §5): C10's ids move to ADR-026 and CHANGELOG 1.64, since ADR-025 (#662) and 1.62 (#656) landed on main after C10 was written, so those two greps were non-zero on `origin/main` and could not discriminate; C1 carries the interim gate-time ceiling ruled for #713, with `.sdlc/baseline.md` as its home. |
