node_modules
dist/
_site/
smoke-out/
.DS_Store

# Reference drop for the Color Categories: the category data was distilled into
# docs/spec/colors/categories/*.json (the gen's source of truth). The full reference
# project is bulky + redundant, so it stays local-only.
test/plugin/.hosted-pack-scratch/
