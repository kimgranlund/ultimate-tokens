---
status: draft
ticket: "#741 (anchor; the plan also closes #742, #745, #747)"
priority: P2
lane: records and gates (`.sdlc/adapter.md`, `.sdlc/checks/card-source-range-check.sh`, `.sdlc/checks/card-amendment-check.sh`, eleven `.sdlc/verdicts/*.md` records, `test/repo/verdict-frontmatter.mjs` (new), `test/run.mjs`, `test/repo/em-dash.mjs`, `.sdlc/baseline.md`, `docs/reference/references/decision-records.md` under Q2)
size: S + S + M + S (U1 S = 1 point, U2 S = 1, U3 M = 2, U4 S = 1; 5 points)
labels: as minted: #741 size:M · status:backlog; #742 size:M · status:backlog; #745 kind:bug · size:S · status:backlog; #747 kind:feature · size:S · status:backlog
written: 2026-09-25
depends: rule-gates (#730) landing on `origin/main`. Every file this plan edits is in rule-gates' diff or is born there: `test/repo/em-dash.mjs` (U4's subject), `test/run.mjs` (U3 registers a file beside the two rule-gates registers), the two card scripts (rule-gates U4 widened their heading regex to `[: ]`), `.sdlc/adapter.md` (rule-gates U5 edits §1's test row; U1 and U2 edit §1, §2.1 and §6), `.sdlc/baseline.md` (rule-gates U5 sets N to 52; U3 moves it again). #741's own ticket says "start after #713 and rule-gates land"; #713 landed as PR #743. Gate G0 below decides it by command; no unit starts before G0 prints green, and the Orchestrator merges `origin/main` into `plan/records-gates` before cutting the first worktree
head: 61225d0c (`origin/main`; `plan/records-gates` is cut from it and carries this file only)
measured-at: 7d325b32 (`origin/main` earlier the same day; `git diff --stat 7d325b32 61225d0c -- . ':(exclude).sdlc'` prints nothing, so every figure below holds at 61225d0c) and `unit/rg-U5` e8a56d7e (rule-gates' last unit in flight, read with `git show`), 2026-09-25, in the main checkout without editing it
branch: plan/records-gates
inputs: tickets #741, #742, #745, #747; `.sdlc/plans/archive/verdict-backfill.md` (Q4, the deferral of #741; U1 and U2's placement rules for a `verdict:` line); `.sdlc/plans/archive/verdict-frontmatter.md`; `.sdlc/plans/rule-gates-U4-rediagnosis.md` §1 (the E1 to E4 predicates) and §4 (the two tickets, owner ruling R39) on `plan/rule-gates`; `.sdlc/verdicts/records-policy-U2.md` (row U2-6, the planted-range control that exited 0) and `.sdlc/verdicts/small-fixes-prepr.md` (N4, the finding); `.sdlc/checks/*.sh` at 7d325b32; `test/repo/em-dash.mjs` at e8a56d7e (`selftest()`, the rule table, `decide`, `runFix`); `.sdlc/adapter.md` §1, §2.1, §6; owner ruling R46 (2026-09-25, plan the backlog in one batch)
---

# Records and gates: the vacuous exits, the stale §6, the verdict check in `npm test`, and colons inside strings (#741, #742, #745, #747)

Four small records-and-gates tickets, grouped because they share files with each other and with rule-gates, and none can start before it lands.

#745 (U1). `card-source-range-check.sh` and `card-amendment-check.sh` end in an `echo` and exit 0 whatever their count. Measured: at 7d325b32 both print a zero count (`range mismatches: 0`, `stale total: 0`) and exit 0; `records-policy-U2` planted a wrong range and read `range mismatches: 2`, exit `0`; `small-fixes-prepr` N4 planted a stale card and read `stale total: 1`, exit `0`; rule-gates U4 pass 3 read `range mismatches: 24`, exit 0. The other four checks already exit on their count. U1 adds `exit $((n > 0))` to both and writes the rule into the adapter: every check under `.sdlc/checks/` is read by its last line and its exit code, and a non-zero count is a non-zero exit.

#742 (U2). Adapter §6 says the ADR heading is `## ADR-NNN` followed by the em dash glyph and a title, and that new ADRs append after ADR-022. The file runs to ADR-027; at 7d325b32 all 27 headings carry the glyph, and at rule-gates' `unit/rg-U5` the sweep has turned ADR-001 to ADR-025 into `## ADR-NNN: title` while ADR-026 and ADR-027, written after the sweep's base, read `## ADR-NNN - title`. §6 itself still carries the glyph three times and `after ADR-022` twice (the rule sentence and the ADR-023 stub row). U2 rewrites the rule sentence to the colon shape and "after the last ADR, before `## Quick map`", rewords the stub row so the grep prints 0, adds a dated amendment paragraph, and (Q2) normalises the two hyphen headings so the file has one shape.

#741 (U3). `verdict-frontmatter-check.sh` is run at pre-land only. Measured at 7d325b32 it is red on `origin/main`: `verdicts 141 graded 141 bad 11`, exit 1. The eleven are records written after #734's backfill: six gate-split verdicts (`gate-split-U1` to `U5`, `U6a`) whose state sits in a `| Verdict |` table row, four review records with a prose `Verdict:` line (`gate-split-U6c-review`, `gate-split-U7-review`, `gate-split-prepr-review`, `okl-memo-prepr-review`), and `gate-split-U6b-review`, whose front matter says `verdict: FIX-FIRST` (a value, not a token) under a later `Verdict: PASS`. The set grows with every plan that lands records without the line (rule-gates' `unit/rg-U5` adds five, docs-repair's U4 review one), so U3's set is whatever the check names at G0 on the merged base, the eleven being today's reading. U3 backfills that set with the #734 placement rules, then wires the check into `npm test` as `test/repo/verdict-frontmatter.mjs`: the file runs the shell check twice, once in the repo root (green) and once in a planted directory holding one MISSING and one VALUE record (red, both lines), so the test bites on its own without a clone. N moves by one and the baseline row with it.

#747 (U4). `em-dash.mjs --fix` picks a replacement by habitat, so a label separator inside a string literal (a Markdown heading or bold-label bullet a program emits, E1 and E2 of the U4 re-diagnosis) gets the clause-join comma. The sweep has already hand-fixed the tree (at e8a56d7e the E1 and E2 predicates print `0` and `0`; at 7d325b32, before the sweep, `17` and `35`), so this protects late branches: U4 applies R2's and R3's colon by construct inside string literals for E1 and E2, refuses E3 (short UI titles, toasts) and E4 (blockquoted and bullet-glyph label lines in Markdown) to the residual list, and adds one self-test fixture per case. The gate's verdict on the tree does not change.

Scope wall. Paths this plan may change: `.sdlc/checks/card-source-range-check.sh` and `.sdlc/checks/card-amendment-check.sh` (U1: the exit line each), `.sdlc/adapter.md` (U1: §1 and §2.1 sentences; U2: §6), `docs/reference/references/decision-records.md` (U2, under Q2: the ADR-026 and ADR-027 heading lines only, no other line), the eleven verdict files named above (U3: one `verdict:` line each, one replaced in `gate-split-U6b-review.md`), `test/repo/verdict-frontmatter.mjs` (U3, new), `test/run.mjs` (U3: one `TESTS` entry), `.sdlc/baseline.md` (U3: the `npm test` row's N and one correction paragraph), `test/repo/em-dash.mjs` (U4), this plan, its own handoffs, verdicts and questions (`.sdlc/{plans,handoffs,verdicts,questions}/records-gates*`), and `.sdlc/board.md` (Orchestrator). Nothing under `src/`, `scripts/`, `figma/`, `mcp/` (no bundled source changes, so `npm run build` is owed at pre-land only), no other `.sdlc/checks/` script (`doc-drift-rows-check.sh` already exits on its count), no other verdict file, no card, no `docs/marketing/` line (#752 owns the Markdown bold-label construct and stays out), no `CHANGELOG.md`.

Prose rules for every line this plan adds. No em dash anywhere: once rule-gates lands, `test/repo/em-dash.mjs` is inside `npm test` and grades every tracked file, this one and every verdict U3 touches included; the adapter's inline-span exemption holds for a verbatim program quote, one span per output line. No bold inline labels. The retired maker brand and the pre-rename element identifier are paraphrased, never written (`test/repo/branding.mjs` scans `.sdlc/`). Criteria needles are function names, ids, strings and counts, never line numbers (R10). `grep -P` is absent on this host: PCRE runs through `perl`. The shell is zsh, where `${PIPESTATUS[0]}` is empty and `$VAR:path` is a history modifier: a command that needs a pipe's exit code runs under `bash -c 'set -o pipefail; ...'`, and `git show "${REF}:path"` braces the ref. R8 (light process for a `.sdlc/`-only change) applies per unit: U1, and U2 if Q2 is answered no, get one reviewer and no verifier pass; U3 and U4 change `test/`, so they get the full pass, and the plan as a whole gets the pre-land pair.

Diff bases. Every row diffs against `B=$(git merge-base origin/main HEAD)`, never against the plan branch's own tip; after G0's merge that is the `origin/main` tip rule-gates landed on, and it sees the unit whole. Every negative control that edits a file runs in a throwaway clone (`git clone -q --shared . "$F/neg"`), made from the unit's commit, never in a unit worktree (`git -C "$F/neg" log -1 --format=%h` prints the unit head). `F` is a directory the seat makes under its own scratchpad with a name no other seat would pick, and removes by that exact name. In table cells `\|` is the escape for a plain `|`: type it unescaped. `N` is the length of `TESTS` on the branch, read by command, never by number: `50` at 7d325b32, `52` at e8a56d7e (`repo/svg-rules.mjs` and `repo/em-dash.mjs`), one more after U3.

Criteria ids: P rows for the plan, numbered rows per unit, cited as U3-2. G0 is the start gate.

## Measured by the planner on 2026-09-25

| Fact | Where | Value |
|---|---|---|
| the two card checks, on main | 7d325b32, `sh .sdlc/checks/card-source-range-check.sh; echo $?` and the amendment check | `range mismatches: 0` then `0`; `stale total: 0` then `0`. Each script's last two lines are `done` and an `echo` of the count; no `exit` anywhere |
| the same with a planted finding | `.sdlc/verdicts/records-policy-U2.md` row U2-6 (the ADR-025 range set to `682-694`); `.sdlc/verdicts/small-fixes-prepr.md` N4 | `range mismatches: 2`, exit `0`; `stale total: 1`, exit `0`. Rule-gates U4 pass 3 at 5091460f: `range mismatches: 24`, exit 0 |
| the other checks' exits | 7d325b32 | `verdict-frontmatter-check.sh`: `process.exit(bad ? 1 : 0)`; `doc-drift-rows-check.sh`: `process.exit(rows.length && !bad ? 0 : 1)`; `baseline-agrees-check.sh`: `process.exit(stale ? 1 : 0)`; `ceiling-counts-check.mjs`: exits on its failure count. So #745's two are the only output-only scripts |
| rule-gates' change to the two card scripts | `unit/rg-U5` e8a56d7e against `origin/main` | one line each: the heading match `^## $id ` becomes `^## $id[: ]` (source-range) and the awk range `/^## ADR-$num /` becomes `/^## ADR-$num[: ]/` (amendment). U1's `exit` line is a different hunk; the plan starts after rule-gates lands, so there is no merge to reason about |
| records that cite `exit 0` for these checks | `grep -rn 'card-(source-range\|amendment)-check' .sdlc --include='*.md'` filtered on `exit 0` | `.sdlc/plans/archive/preset-intent-fidelity.md` (U9-P5, "each `exit 0` as at 33ab0942"), `.sdlc/handoffs/pif-u9.md`, `.sdlc/verdicts/pif-u9-review.md` (three rows), `.sdlc/verdicts/pif-u10.md` (U10-P3), `.sdlc/verdicts/records-policy-U2.md` (U2-6, "exit `0`, so the printed figure is the reading"), `.sdlc/verdicts/small-fixes-prepr.md` (N4, the finding itself). All are dated readings of what the scripts did then; adapter §2.1 item 1 ("records each in the pre-land table") is the one live sentence, and it names no exit code |
| `doc-drift-rows-check.sh` on main | 7d325b32 | `QUOTE DD9: not found at .claude/CLAUDE.md:95`, `rows 56 drifted 11 holds 45 undetermined 0 bad 1`, exit 1: red today, on a DD row rule-gates U5 and docs-repair both edit near. Not this plan's; reported to the lead |
| adapter §6 | 7d325b32 `.sdlc/adapter.md`, `sed -n '/^## 6\./,/^## 7/p'` | `grep -c 'after ADR-022'` prints `2` (the rule sentence and the ADR-023 stub row `new section after ADR-022`); the glyph appears on `3` lines (the rule sentence's `## ADR-NNN` shape and the two stub headings); `(line 654 at f9e20c5)` still cited for the Quick map. Byte-identical at e8a56d7e: the sweep does not reach §6 (its dashes sit inside backtick spans) |
| ADR heading shapes | `docs/reference/references/decision-records.md` | at 7d325b32: `27` headings `^## ADR-`, all `27` with the glyph, Quick map at line 818. At e8a56d7e: `25` read `## ADR-NNN: title` (001 to 025, the sweep's R2), `2` read `## ADR-NNN - title` (026 and 027, hand-written after the sweep's base), `0` with the glyph |
| the verdict check on main | 7d325b32, `sh .sdlc/checks/verdict-frontmatter-check.sh` | `verdicts 141 graded 141 bad 11`, exit 1. `git ls-files .sdlc/verdicts \| wc -l` prints `141`, all tracked, tree clean |
| the eleven | the check's own lines | `MISSING gate-split-U1.md`, `-U2`, `-U3`, `-U4`, `-U5`, `-U6a`, `gate-split-U6c-review.md`, `gate-split-U7-review.md`, `gate-split-prepr-review.md`, `okl-memo-prepr-review.md` (ten `MISSING`); `VALUE gate-split-U6b-review.md: last verdict: FIX-FIRST is not 🟢, 🟡 or 🔴` |
| their shapes and last states | the files' first lines and state rows | title-first with a `\| Verdict \| 🟢 \|` row: U1 (`🟢`), U6a (`🟢 with one waived red and two ...`); title-first with the state in the title: U2, U3, U4 (`🟢 at <sha>`), U5 (`🟡`); title-first review records with a `\| Verdict \|` cell: U6c-review (`🟡 FIX-FIRST. ...`), U7-review (`PASS`), prepr-review (`FIX-FIRST. Runtime is right ...`); `okl-memo-prepr-review.md` whose line 1 is `PASS`; and U6b-review, front matter at line 1 with `verdict: FIX-FIRST` at its line 9 and a later prose `Verdict: PASS. Every finding in U6b's scope is fixed.` The builder derives each token from the file's last state line, as #734's U2 did, and writes the derivation table before editing |
| the #734 placement rules | `.sdlc/plans/archive/verdict-backfill.md` (the paragraph "Where the line goes") | the adapter reads the LAST `verdict:` line, so it goes where it is last and where the record's shape puts state: the last line of the front matter block when the file has one; line 2, directly under the title, when it does not; the end of the file when the final state is a closing block. Convention of the backfilled review records: `verdict: 🟢 PASS` (2 files), `verdict: 🟡 FIX-FIRST` (3 files), bare `verdict: 🟢` (39) |
| `TESTS` and the baseline row | 7d325b32 `test/run.mjs`; e8a56d7e; `.sdlc/baseline.md` | `50`; `52`; the baseline's `npm test` row reads `✓ all 50 test files passed` at 61225d0c and `baseline-agrees-check.sh` compares that N with `TESTS` (`tests: baseline 50, test/run.mjs TESTS 50`). Rule-gates U5 moves it to 52 with its own correction; U3 moves it to 53 |
| `test/run.mjs` names the verdict check | 7d325b32, e8a56d7e | `0`, `0` |
| how a repo test is shaped | 7d325b32 `test/repo/gate-report.mjs`, `test/repo/branding.mjs` | a `#!/usr/bin/env node` module, a local `FAIL`, prints `✓ ...` and exits 0, or the FAIL lines and exits 1; `test/run.mjs` runs each with `execFileSync("node", [path])`, cwd inherited (the repo root under `npm test`) |
| the em-dash tool at e8a56d7e | `unit/rg-U5:test/repo/em-dash.mjs` | 902 lines; `selftest()` runs `32` fixtures (`expectRule:` count), each `{ name, md, line[, prevLine], expectRule[, expectFix] }` through `fixLines()`; `--fix` runs `runFix()` then the gate; R2 (heading label, colon) and R3 (bullet label, colon) fire on `.md` lines only; a non-Markdown `#` line falls to R8 (fixture `R2 does not fire outside Markdown`, `expectRule: "R8"`); a quoted `"## Which variant? ... decision tree"` string is refused by the guard (`expectRule: "R0"`); no fixture has a heading or a bold label inside a string literal expecting a colon |
| E1 to E4 as the re-diagnosis defines them | `.sdlc/plans/rule-gates-U4-rediagnosis.md` §1 | E1: a string opening `#{1,6} ` and carrying the dash, `["\`]#{1,6} [^"\`]*\x{2014}`, replacement `: `; E2: `\*\*[^*]+\*\* \x{2014} ` outside a `//` comment and outside the generated `"data":` mirrors, replacement `: `; E3: a `title:`, `ariaLabel:`, `label:`, `note:`, `description:`, `hint:`, `card(`, `toast(`, `notify(` line whose text before the dash is one to four words with no `. , ; : ! ?`, a colon or a sentence break by hand; E4: `^\s*(>\s*[-*]?\s*\*\*[^*]+\*\* \x{2014}\|• [^\x{2014}]{1,40} \x{2014})` in `.md`, R3's colon by hand. Paths for E1 to E3: `src/ui`, `src/engine`, `mcp`, `figma/plugin/code.js`, the binder `code.js` |
| E1 and E2 counts on the tree | the predicates over those paths, `assets.js` and `categories/` mirrors excluded | 7d325b32: E1 `17`, E2 `35`; e8a56d7e: `0`, `0` (the sweep and U4's hand pass) |
| `gh issue view 730 --json state` | 2026-09-25 | `OPEN`; rule-gates U1 to U4 and U6 ticked, U5 in flight at e8a56d7e; no PR open from `plan/rule-gates` yet |

## The design, stated once

U1. Each card script ends `echo "...: $n"` then `exit $((n > 0))`, so the last line stays the reading and the exit agrees with it. Adapter §1 gains one sentence after its gate table's rules list: every script under `.sdlc/checks/` is read by its last line and its exit code, a non-zero count is a non-zero exit, and a seat records both (`range mismatches: 0`, exit 0). Adapter §2.1 item 1's "records each in the pre-land table" becomes "records each one's last line and exit code in the pre-land table". The dated records that read `exit 0` from the old scripts stay as they are: they quote what a program printed at a named ref, and the verbatim-quote rule and R9 both say a record is not rewritten to match a later change (Q1 confirms).

U2. §6's first paragraph is edited in place (the adapter's own amendment convention allows a direct edit when the sentence is a rule, and this one sends a builder to the wrong place): headings are `## ADR-NNN: title`, the shape ADR-001 to ADR-025 carry since the em-dash sweep; a new ADR appends after the last ADR and before the closing `## Quick map` section, with no fixed number; the `(line 654 at f9e20c5)` cite goes. The ADR-023 and ADR-024 stub rows in the table are history (they landed 2026-09-17, adapter §8's amendment) and are reworded to say so without the phrase `after ADR-022` (for example "the section following ADR-022, landed 2026-09-17") and with their heading text in the colon shape; the ADR-023 and ADR-024 headings in the file already carry the colon at e8a56d7e. A dated amendment paragraph under the table records the edit and why (#742, records-policy U2 review, the pre-land record's L4). Under Q2 (recommended yes), the ADR-026 and ADR-027 heading lines in `decision-records.md` change ` - ` to `: ` so `grep -c -E '^## ADR-[0-9]{3}: '` equals the heading count; the line count does not move, so every card's Source range holds and `card-source-range-check.sh` (whose regex admits both shapes after rule-gates) stays at `0`.

U3. First the backfill: eleven `verdict:` lines, each token derived from the file's last state (`🟢` for a `🟢` table row or title; `🟡` for U5's title and U6c-review's `🟡 FIX-FIRST` cell; `🟢 PASS` for U7-review, okl-memo-prepr-review and U6b-review's later prose PASS; `🔴 FIX-FIRST` or `🟡 FIX-FIRST` for gate-split-prepr-review by its own closing block, the builder reads it and says which), placed at line 2 under the title, or, for U6b-review, replacing the front-matter `verdict: FIX-FIRST` line in place so the front matter's last `verdict:` line carries the token. Then the test: `test/repo/verdict-frontmatter.mjs` spawns `sh .sdlc/checks/verdict-frontmatter-check.sh` with `execFileSync` (a) in the repo root, expecting exit 0 and a last line matching `^verdicts (\d+) graded \1 bad 0$`, and (b) in a temporary directory under `os.tmpdir()` holding `.sdlc/verdicts/missing.md` (no `verdict:` line) and `.sdlc/verdicts/value.md` (`verdict: PASS`), expecting exit 1, one `MISSING missing.md` line, one `VALUE value.md` line and `bad 2`; (b) is what makes the test bite without a clone. Registered in `TESTS` after `repo/gate-report.mjs`; the baseline's `npm test` row cell moves from `all 52` to `all 53 test files passed` with a correction paragraph stating the added file runs the shell check twice and costs under a second (the builder measures it; timings carry forward, the gate-split U1 precedent).

U4. In `em-dash.mjs`, `classifyLine()` gains two whole-line shape rules that fire before R8 on a non-Markdown line: E1, the dash sits inside a string literal (the `insideStringAt` helper the residual list already uses) whose content opens with `#{1,6} `, replacement `: ` (R2's form, rule id `R2s`); E2, the dash follows `**label**` inside a string literal, outside a `//` comment, replacement `: ` (R3's form, `R3s`). Two refusals join R0 with their own sub-label: E3, a string on a `title:`, `ariaLabel:`, `labelTitle:`, `label:`, `note:`, `description:`, `hint:`, `card(`, `toast(`, `notify(` line whose text before the dash is one to four words with no sentence punctuation; E4, a `.md` line matching the re-diagnosis's blockquote or bullet-glyph predicate. Both print in the residual list with the habitat label so a builder hand-edits them. Two builder notes from the checkability review: the rule function is `classifyLine({ line, prevLine, md, skipStructural })`, and `runFix()` keys its counters on a fixed `{ R1: ..., R8: ... }` object, so each new rule id needs an entry there or `linesByRule[e.rule].add` throws. Four fixtures: `E1 heading in a string` (`expectFix` with the colon), `E2 bold label in a string` (colon), `E3 short title string` (`expectRule: "R0"`), `E4 blockquoted label bullet` (`R0`). The existing `R2 does not fire outside Markdown` fixture (a `#` comment, not a string) keeps `R8`. The gate's count on the tree is unchanged (nothing left to match), which is why the fixtures are the control, and `--fix --sample` on a planted file is the end-to-end proof.

## G0: has rule-gates landed (each unit's step 1, and the Orchestrator's before it cuts the first worktree)

```sh
git fetch -q origin
gh issue view 730 --json state,stateReason --jq '.state + " " + .stateReason'
git show origin/main:test/run.mjs | grep -c '"repo/em-dash.mjs"'
git show origin/main:.sdlc/checks/card-source-range-check.sh | grep -c '\[: \]'
git show origin/main:docs/reference/references/decision-records.md | grep -c -E '^## ADR-[0-9]{3}: '
git show origin/main:test/run.mjs | perl -0ne 'my ($b) = /const TESTS = \[(.*?)\];/s; my @m = $b =~ /"[^"]+\.mjs"/g; print scalar(@m), "\n"'
mkdir -p "$F/g0" && git archive origin/main .sdlc/verdicts | tar -x -C "$F/g0" && (cd "$F/g0" && sh "$OLDPWD/.sdlc/checks/verdict-frontmatter-check.sh" | tail -1)
```

Expected `CLOSED COMPLETED` (a `NOT_PLANNED` close is not a landing), `1`, `1`, `25` or more, the N the plan's rows then read (`52` if rule-gates alone landed), and a last line of the shape `verdicts M graded M bad K`. Today: `OPEN` followed by a space, `0`, `0`, `0`, `50`, `verdicts 142 graded 142 bad 11`. Line 4 below `25` once line 1 is green means someone re-shaped the headings again; the builder of U2 re-reads and cites. Line 6 reads `origin/main`'s verdict set through `git archive` into a scratch directory (`F` as in Diff bases) and runs main's own check over it, so it needs no checkout: K is the size of U3's backfill set, and the check's K lines above the last one are that set by name. K is `11` on `origin/main` today and grows with every plan that lands records without the line: `unit/rg-U5` adds five (`MISSING rule-gates-U1.md` through `rule-gates-U6.md`: U1, U2, U3, U4-review, U6) and `plan/docs-repair` adds one (`VALUE docs-repair-U4-review.md: last verdict: PASS is not 🟢, 🟡 or 🔴`). U3 backfills whatever the merged base lists, not the eleven this plan names today; the U3 handoff copies the G0 list, and P4's verdict pattern admits exactly those names. The lead tells rule-gates and docs-repair that their records red the check before they land.

Post-merge check, the Orchestrator's own step once G0 is green: merge `origin/main` into `plan/records-gates`, then `git merge-base --is-ancestor origin/main plan/records-gates; echo $?` prints `0`, and only then is a worktree cut. U1, U2 and U4 are independent of each other; U3 follows U1 (both edit `.sdlc/adapter.md`, and U3's pre-land row reads the exit rule U1 writes). U2's decision-records edit (Q2) and U3's eleven verdict files share nothing.

## Units

- [ ] U1 (S) `exit $((n > 0))` in both card scripts; adapter §1 and §2.1 state the last-line-and-exit rule · builder-l1 · reviewer-l1 · no verifier (R8) · starts at G0 green
- [ ] U2 (S) adapter §6: the colon heading shape, append after the last ADR, the stub rows reworded, a dated amendment; ADR-026 and ADR-027 headings normalised under Q2 · builder-l1 · reviewer-l1 · verifier-l1 only if Q2 is yes (a file outside `.sdlc/` changes) · starts at G0 green
- [ ] U3 (M) eleven `verdict:` lines backfilled; `test/repo/verdict-frontmatter.mjs` registered; baseline N moved · builder-l2 · reviewer-l1 · verifier-l1 · after U1
- [ ] U4 (S) `em-dash.mjs --fix`: E1 and E2 take the colon inside string literals, E3 and E4 are refused, four fixtures · builder-l3 · reviewer-l2 · verifier-l2 · starts at G0 green

| Unit | Size | Builder | Reviewer | Verifier | Touches |
|---|---|---|---|---|---|
| U1 exits | S | builder-l1 | reviewer-l1 | none (R8) | `.sdlc/checks/card-source-range-check.sh`, `.sdlc/checks/card-amendment-check.sh`, `.sdlc/adapter.md` (§1, §2.1) |
| U2 §6 | S | builder-l1 | reviewer-l1 | verifier-l1 under Q2 | `.sdlc/adapter.md` (§6), `docs/reference/references/decision-records.md` (two heading lines, Q2) |
| U3 verdict check | M | builder-l2 | reviewer-l1 | verifier-l1 | eleven `.sdlc/verdicts/*.md`, `test/repo/verdict-frontmatter.mjs`, `test/run.mjs`, `.sdlc/baseline.md` |
| U4 colons in strings | S | builder-l3 | reviewer-l2 | verifier-l2 | `test/repo/em-dash.mjs` |

Grades. U1 and U2 are l1: one-line script edits and prose with grep needles. U3 is l2: a new test file with a spawned shell check and a temp-dir fixture, plus a records backfill whose eleven tokens must be derived, not guessed (K17 for the register). U4 is l3 with reviewer-l2 and verifier-l2, the grades rule-gates U3 used for the same file: a 900-line rule engine whose replay determinism a late branch depends on.

## Plan-level criteria

`B=$(git merge-base origin/main HEAD)` at the top of every row.

| Id | Criterion | Command | Expected | Negative control | Today |
|---|---|---|---|---|---|
| P1 | `npm test` green with no `node_modules`, the count agrees with the baseline, the tree is byte-stable | `npm test 2>&1 \| tail -1; perl -0ne 'my ($b) = /const TESTS = \[(.*?)\];/s; my @m = $b =~ /"[^"]+\.mjs"/g; print scalar(@m), "\n"' test/run.mjs; sh .sdlc/checks/baseline-agrees-check.sh \| grep 'tests:'; git status --short \| wc -l` | `✓ all N test files passed`, then N (the base's N plus one after U3), then `ok    tests: baseline N, test/run.mjs TESTS N`, then `0` | in the clone: `sed -i '' 's/"scrim/"scrimX/' docs/reference/data/role-table.json && npm test > "$F/neg.log" 2>&1; echo "exit $?"` prints `exit 1` (adapter §1's own control); the baseline row left at the base's N after U3 prints `STALE tests:` | `50` at 7d325b32 and `ok    tests: baseline 50, test/run.mjs TESTS 50`; the suite was not run by the planner (host under load) |
| P2 | no bundled or executable product source changes; build is owed at pre-land only | `git diff --name-only "$B" -- src scripts figma mcp plugin package.json \| wc -l` | `0`; the pre-land record runs `npm run build` once per adapter §1 (verifier at pre-land always) and says smoke is not owed | a fixture of two names (`src/ui/app.js`, `test/repo/em-dash.mjs`) piped through `grep -c -E '^(src\|scripts\|figma\|mcp\|plugin)/'` prints `1` | `0` on the plan branch (this file only) |
| P3 | branding clean, no added line carries an em dash (U+2014) outside a backtick span, the raw count matches the enumerated quotes, the em-dash gate is green | `bash -c 'set -o pipefail; node test/repo/branding.mjs \| tail -1'; git diff "$B" \| grep -v '^+++ ' \| grep '^+' \| perl -CSD -ne 's/\x60[^\x60]*\x60//g; print if /\x{2014}/' \| wc -l; git diff "$B" -- . ':(exclude).sdlc/handoffs' \| grep -v '^+++ ' \| grep '^+' \| perl -CSD -ne 'print if /\x{2014}/' \| wc -l; bash -c 'set -o pipefail; node test/repo/em-dash.mjs \| tail -1'; echo "exit $?"` | `branding: clean (N files scanned)`, `0`, a number equal to the count the handoff states with each quoted line listed word for word (U4's fixtures write the glyph as `${DASH}`, never literally, so they add none; U2 adds none: the old heading shape is described, not quoted), the em-dash gate's pass line, `exit 0` | in the clone: `cp docs/reference/references/decision-records.md .sdlc/verdicts/x.md` reds the first; one added prose line with the glyph makes the second `1` and the gate `exit 1` | clean at 61225d0c (`792 files scanned` in the root checkout, which holds untracked files; a clean checkout of 65f8171c scans `723`, the checkability review's figure); this plan's own added lines: `0` and `0` (planner); the em-dash gate is graded from G0 on |
| P4 | scope wall, and no verdict record deleted or renamed | `git diff --name-only "$B" \| grep -v -E -e '^\.sdlc/checks/card-(source-range\|amendment)-check\.sh$' -e '^\.sdlc/adapter\.md$' -e '^docs/reference/references/decision-records\.md$' -e '^\.sdlc/verdicts/[A-Za-z0-9-]+\.md$' -e '^test/repo/verdict-frontmatter\.mjs$' -e '^test/run\.mjs$' -e '^\.sdlc/baseline\.md$' -e '^test/repo/em-dash\.mjs$' -e '^\.sdlc/(plans\|handoffs\|verdicts\|questions)/records-gates' -e '^\.sdlc/board\.md$' \| wc -l; git diff --name-only --diff-filter=DR "$B" -- .sdlc/verdicts \| wc -l; git diff --numstat "$B" -- docs/reference/references/decision-records.md \| cut -f1,2 \| tr '\t' ' '; git diff --name-only --diff-filter=M "$B" -- .sdlc/verdicts \| sed 's#.*/##' \| grep -v -c -F -f "$F/g0-list.txt"` | `0`, `0`, `2 2` or nothing (Q2 yes or no; any other numstat reds the row), and `0`, where `$F/g0-list.txt` holds the file names G0 line 6 printed (one per line, copied from the check's own `MISSING`/`VALUE` lines by the Orchestrator at the merge and quoted in the U3 handoff): every modified verdict record is one the check named, and the pattern in the first filter is only as wide as that list makes it | a fixture of four names (`.sdlc/checks/doc-drift-rows-check.sh`, `docs/marketing/store-copy.md`, `.sdlc/adapter.md`, `.sdlc/verdicts/records-gates-U1-review.md`) piped through the first filter prints `2` (run by the planner: `2`); `git mv` of a verdict in the clone makes the second `1`; a third line changed in `decision-records.md` makes the numstat `3 3`; a `verdict:` line added to `achromatic-anchor-U1.md` (not on the G0 list) makes the fourth `1` | `0`, `0`, nothing, `0` on the plan branch |

## U1: exits (#745), starts at G0 green

Steps. (1) G0. (2) Append `exit $((n > 0))` after the final `echo` in each card script; the comment header gains one line saying the exit agrees with the count (#745). (3) Adapter §1 sentence and §2.1 item 1 wording. (4) Run U1-1 to U1-3 in the worktree, the controls in a clone; the handoff quotes both scripts' last two lines.

| Id | Criterion | Command | Expected | Negative control | Today |
|---|---|---|---|---|---|
| U1-1 | both checks read green on the tree and their exit agrees with a zero count | `sh .sdlc/checks/card-source-range-check.sh; echo "exit $?"; sh .sdlc/checks/card-amendment-check.sh; echo "exit $?"; tail -1 .sdlc/checks/card-source-range-check.sh; tail -1 .sdlc/checks/card-amendment-check.sh` | `range mismatches: 0`, `exit 0`, `stale total: 0`, `exit 0`, then `exit $((n > 0))` twice | in the clone, set the ADR-025 card's Source range to `682-694` (records-policy-U2's own plant, both ends wrong): `start ADR-025 says 682, heading at 696`, `end ADR-025 says 694, section ends at 728`, `range mismatches: 2` then `exit 1` (an end-only edit to `694` prints one line and `range mismatches: 1`, the review's measurement); in the same clone, drop `2026-09-16` from the `Supersedes / amended by` row of ADR-010's card (its source carries the amendment; the small-fixes-prepr N4 plant): `stale card ADR-010`, `stale total: 1` then `exit 1` | `range mismatches: 0` `exit 0`, `stale total: 0` `exit 0` at 7d325b32; the planted forms exited `0` in both cited records |
| U1-2 | the adapter states the rule, in §1 and at the pre-land step | `grep -c 'read by its last line and its exit code' .sdlc/adapter.md; sed -n '/^### 2.1/,/^### 2.2/p' .sdlc/adapter.md \| grep -c 'last line and exit code'; grep -c '#745' .sdlc/adapter.md` | `1` or more, `1`, `1` or more | the tree at G0 prints `0`, `0`, `0` | `0`, `0`, `0` at 7d325b32 |
| U1-3 | the other four checks are untouched and still exit on their count | `git diff --name-only "$B" -- .sdlc/checks \| grep -v -c -E 'card-(source-range\|amendment)-check\.sh$'; grep -c 'process.exit(bad ? 1 : 0)' .sdlc/checks/verdict-frontmatter-check.sh; grep -c 'process.exit(rows.length && !bad ? 0 : 1)' .sdlc/checks/doc-drift-rows-check.sh` | `0`, `1`, `1` | an `exit` line added to `doc-drift-rows-check.sh` makes the first `1` | `0`, `1`, `1` at 7d325b32 |

## U2: §6 (#742), starts at G0 green

Steps. (1) G0; read line 4's count and the two hyphen headings. (2) Rewrite §6's first paragraph and the two stub rows per the design; append the dated amendment paragraph under the table. (3) Under Q2 yes, change the two heading lines in `decision-records.md`. (4) Run U2-1 to U2-3 and `sh .sdlc/checks/card-source-range-check.sh` (it must still print `range mismatches: 0`); the handoff quotes the old and new rule sentence.

| Id | Criterion | Command | Expected | Negative control | Today |
|---|---|---|---|---|---|
| U2-1 | §6 no longer says `after ADR-022`, carries no glyph, names the colon shape and the append rule | `S=$(sed -n '/^## 6\./,/^## 7/p' .sdlc/adapter.md); printf '%s\n' "$S" \| grep -c 'after ADR-022'; printf '%s\n' "$S" \| perl -CSD -ne 'print if /\x{2014}/' \| wc -l; printf '%s\n' "$S" \| grep -c -F '## ADR-NNN: title'; printf '%s\n' "$S" \| grep -c 'after the last ADR'; printf '%s\n' "$S" \| grep -c 'line 654'; printf '%s\n' "$S" \| grep -c '#742'` | `0`, `0`, `1` or more, `1` or more, `0`, `1` or more | the tree at G0 prints `2`, `3`, `0`, `0`, `1`, `0` | `2`, `3`, `0`, `0`, `1`, `0` at 7d325b32 |
| U2-2 | the shape §6 names is the shape the file uses, for every heading | `grep -c -E '^## ADR-[0-9]{3}' docs/reference/references/decision-records.md; grep -c -E '^## ADR-[0-9]{3}: ' docs/reference/references/decision-records.md; grep -c -E '^## ADR-[0-9]{3} - ' docs/reference/references/decision-records.md` | under Q2 yes: `27`, `27`, `0` (or the count at G0 if an ADR was appended since, equal in the first two); under Q2 no: `27`, `25`, `2`, and §6 says the last two carry the hyphen form until their next amendment | in the clone, revert ADR-026's heading to the hyphen: the second prints `26` and the third `1` | at 7d325b32: `27`, `0`, `2`; at e8a56d7e: `27`, `25`, `2` |
| U2-3 | the two heading edits move no card range and the amendment check stays green | `sh .sdlc/checks/card-source-range-check.sh; echo "exit $?"; sh .sdlc/checks/card-amendment-check.sh; echo "exit $?"; git diff --numstat "$B" -- docs/reference/references/decision-records.md \| cut -f1,2 \| tr '\t' ' '` | `range mismatches: 0`, `exit 0`, `stale total: 0`, `exit 0`, `2 2` (Q2 yes) or nothing (Q2 no) | in the clone, add a blank line above the ADR-026 heading: four lines (`start ADR-026 says 730, heading at 731` through `end ADR-027 says 816, section ends at 817`), `range mismatches: 4` (both cards, start and end each; the review measured `4`) and, after U1, `exit 1` | `0`, `0` at 7d325b32; no diff |

## U3: the verdict check in `npm test` (#741), after U1

Steps. (1) G0 and U1 merged into the plan branch. (2) The derivation table in the handoff: eleven rows (file, the line the token is derived from quoted as the file holds it, the token, the placement). (3) The eleven lines. (4) `sh .sdlc/checks/verdict-frontmatter-check.sh` prints `bad 0`. (5) `test/repo/verdict-frontmatter.mjs` per the design; register it in `TESTS`. (6) The baseline row and its correction paragraph. (7) `npm test`; U3-1 to U3-5 in the worktree, the controls in a clone.

| Id | Criterion | Command | Command's expected output | Negative control | Today |
|---|---|---|---|---|---|
| U3-1 | the check is green on the tree, every record graded, none skipped | `sh .sdlc/checks/verdict-frontmatter-check.sh \| tail -1; sh .sdlc/checks/verdict-frontmatter-check.sh > /dev/null; echo "exit $?"; git ls-files .sdlc/verdicts \| grep -c '\.md$'` | `verdicts M graded M bad 0` where M equals the third number (every tracked top-level `.md` graded), `exit 0` | in the clone, delete the new line from `gate-split-U1.md`: `... bad 1`, `exit 1`, and the first output line reads `MISSING gate-split-U1.md: no verdict: line` | `verdicts 141 graded 141 bad 11`, `exit 1`, `141` at 7d325b32; `verdicts 142 graded 142 bad 11` at 65f8171c. `bad 0` is reachable only because U3's set is the G0 list, not the eleven named today: over `unit/rg-U5`'s records main's check prints `bad 16` and over `plan/docs-repair`'s `bad 12` |
| U3-2 | the eleven lines are placed per the rules and each token is the file's last state | `for f in gate-split-U1 gate-split-U2 gate-split-U3 gate-split-U4 gate-split-U5 gate-split-U6a gate-split-U6c-review gate-split-U7-review gate-split-prepr-review okl-memo-prepr-review; do sed -n 2p ".sdlc/verdicts/$f.md" \| grep -c -E '^verdict: [🟢🟡🔴]'; done \| sort \| uniq -c; awk '/^---$/{n++; next} n==1 && /^verdict:/{v=$0} n==2{exit} END{print v}' .sdlc/verdicts/gate-split-U6b-review.md; git diff --numstat "$B" -- $(sed 's#^#.sdlc/verdicts/#' "$F/g0-list.txt") \| cut -f1,2 \| sort \| uniq -c` | `     10 1` (the ten title-first files of today's list carry the token at line 2; a file the G0 list adds is placed by the same rules and joins the loop, the handoff says where), `verdict: 🟢 PASS` (U6b-review's front matter, the token derived from its later `Verdict: PASS.` line), and, over the G0 list's paths only (this plan's own review records are outside the pathspec), the numstat set K minus one lines of `1 0` plus one line of `1 1` (one added line per file, one replaced; today `10` and `1`) | a token placed at line 3 makes a `0` appear in the first tally; a `verdict:` line appended at the end of a title-first file leaves line 2 unchanged and the tally short | `0` lines at line 2 carry the token in any of the ten at 7d325b32; U6b-review's front matter reads `verdict: FIX-FIRST` |
| U3-3 | the test is registered and green, and it bites in its planted leg | `grep -c '"repo/verdict-frontmatter.mjs"' test/run.mjs; node test/repo/verdict-frontmatter.mjs > "$F/v.log" 2>&1; echo "exit $?"; tail -1 "$F/v.log"; grep -c -e 'MISSING' -e 'VALUE' -e 'tmpdir' test/repo/verdict-frontmatter.mjs` | `1`, `exit 0`, a line starting `✓ verdict-frontmatter:` and containing `bad 0` and `planted 2` (the repo leg's count and the planted leg's), `3` or more (the planted leg names both failure kinds and uses a temp dir) | in the clone, (a) plant `.sdlc/verdicts/zz-planted.md` with no `verdict:` line: `exit 1` with a FAIL line containing `MISSING zz-planted.md`; (b) flip the shell check's `process.exit(bad ? 1 : 0)` to `process.exit(0)`: `exit 1` with a FAIL line saying the planted leg exited `0` (the test reads the exit code, not only the lines); `repo/verdict-frontmatter.mjs` is in `TESTS`, so both reds are `npm test`'s | `0` at 7d325b32; no such file |
| U3-4 | N and the baseline agree, with the correction | `sh .sdlc/checks/baseline-agrees-check.sh \| grep -E 'tests:\|stale total'; grep -c '#741' .sdlc/baseline.md` | `ok    tests: baseline N, test/run.mjs TESTS N` with N the base's plus one, `stale total: 0` (a `note  head:` line is expected and counts toward nothing), `1` or more | the row left at the base's N: `STALE tests: baseline 52, test/run.mjs TESTS 53` and `stale total: 1` | `ok    tests: baseline 50, test/run.mjs TESTS 50` at 61225d0c; `0` |
| U3-5 | the test resolves the repo root from its own path, not from cwd, and costs under a second | `bash -c 'cd "$(git rev-parse --show-toplevel)" && s=$(date +%s.%N); node test/repo/verdict-frontmatter.mjs > "$F/v1.log"; rc=$?; e=$(date +%s.%N); echo "exit $rc $(echo "$e - $s" \| bc) s"; tail -1 "$F/v1.log"'` three times; then `bash -c 'cd test && node repo/verdict-frontmatter.mjs > "$F/v2.log"; echo "exit $?"; tail -1 "$F/v2.log"'` | `exit 0` and a figure under `1.00` each time (two `sh` spawns over about 150 files), then the same `✓ verdict-frontmatter:` line; from `test/`: `exit 0` and the identical last line (the test spawns the check with `cwd` set to the root it derives from `fileURLToPath(import.meta.url)`, so where it is run from does not matter). The `rc=$?` is captured right after the node call; revision 1 read `$?` after a `date` and always saw `0` | in the clone, replace the derived root with `process.cwd()` in the test: the run from `test/` reds with a FAIL naming a missing `.sdlc/verdicts` (the shell check's `readdirSync` throws there), while the root run stays green, which is the difference the row exists to see | not run by the planner; the shell check alone runs in under a second on 141 files |

## U4: colons inside strings (#747), starts at G0 green

Steps. (1) G0. (2) The two shape rules and two refusals in `classifyLine()` per the design, with a comment naming #747 and the E1 to E4 predicates' source. (3) Four fixtures in `selftest()`. (4) `node test/repo/em-dash.mjs` (self-test then gate) green; `node test/repo/em-dash.mjs --fix --sample` on a planted scratch copy per U4-3. (5) U4-1 to U4-3 in the worktree, the controls in a clone; the handoff lists the four fixtures' before and after.

| Id | Criterion | Command | Expected | Negative control | Today |
|---|---|---|---|---|---|
| U4-1 | the fixtures exist and the self-test and gate are green | `grep -c 'expectRule:' test/repo/em-dash.mjs; grep -c -E 'name: "E[1-4] ' test/repo/em-dash.mjs; bash -c 'set -o pipefail; node test/repo/em-dash.mjs \| tail -1'; echo "exit $?"` | `36` or more (the base's `32` plus four), `4`, the gate's pass line, `exit 0` | in the clone, delete the `R2s` rule: `node test/repo/em-dash.mjs` exits `1` at the self-test with a line naming the `E1` fixture (`expected R2s, got R8`, or the tool's own wording, which the builder quotes) | `32`, `0` at e8a56d7e; the tool does not exist at 7d325b32 |
| U4-2 | the tree's verdict is unchanged: nothing on main matches E1 or E2 any more, and the refusal count is what the sweep left | in two throwaway shared clones, one at `$B` and one at the unit head (`--fix` rewrites a tree, so never in the worktree): `cd "$F/base" && node test/repo/em-dash.mjs --fix \| grep -E '^R0 [0-9]+$'; cd "$F/head" && node test/repo/em-dash.mjs --fix \| grep -E '^R0 [0-9]+$'` (the `R0 <n>` line is printed by `runFix()` only; gate mode prints `em-dash: clean (N files scanned)` or the FAIL block, so revision 1's gate-mode grep printed nothing on any tree); then, in the worktree, `for f in $(git ls-files src/ui src/engine mcp figma/plugin/code.js figma/binder/figma-semantic-binder/code.js \| grep -E '\.(js\|mjs\|ts)$' \| grep -v -E 'assets\.js$\|categories/'); do perl -CSD -ne 'print if /["\x60]#{1,6} [^"\x60]*\x{2014}/' "$f"; done \| wc -l` | the two `R0 <n>` lines equal (the base's and the head's refusal counts, both cited in the handoff; `git status --short` in each clone lists the same files afterwards, or none), then `0` | in the head clone, plant one E1 line in a tracked file (`git add` it) before `--fix`: the two `R0` lines still agree (the line is rewritten, not refused) and the file's diff shows the colon; a planted E3 line raises the head's `R0` by one | `R0` line not measurable at 7d325b32 (no tool); E1 `17` at 7d325b32, `0` at e8a56d7e |
| U4-3 | `--fix` applies the colon by construct inside a string and refuses E3 and E4 to the list, end to end | in a scratch copy of the repo (`git clone -q --shared . "$F/fix"`), plant `src/ui/zz-e.js` with four lines: `const h = "## Hard rules U+2014 IMPORTANT";`, `const b = "- **Pro** U+2014 the paid tier, a, b";`, `const t = { title: "Export kit U+2014 all formats" };` and `docs/zz-e.md` with `> - **Label** U+2014 one, two`; then `cd "$F/fix" && git add src/ui/zz-e.js docs/zz-e.md && node test/repo/em-dash.mjs --fix > "$F/fix.log" 2>&1; grep -c 'Hard rules: IMPORTANT' src/ui/zz-e.js; grep -c 'Pro\*\*: the paid' src/ui/zz-e.js; awk '/residual \(left in place, fix by hand\):/{f=1} f' "$F/fix.log" \| grep -c -e 'zz-e.js' -e 'zz-e.md'` (the `U+2014` escapes are written as the glyph in the planted files; the `git add` is what puts them in the walk: `walkTracked()` reads `git ls-files -z`, so an untracked plant is never scanned and every grep read `0` on revision 1's form whatever the rules did, the review's finding; no commit is needed) | `1`, `1`, `2` (the E3 line of `zz-e.js` and the E4 line of `zz-e.md` both sit in the residual block, listed, not rewritten); the planted files still carry the two refused dashes, `grep -c` of the glyph over both prints `2` | the same plant, `git add` included, in a clone at the base (G0's tree): the first two print `0` and the two strings read `Hard rules, IMPORTANT` and `Pro**, the paid` (R8's comma, the ticket's defect); without the `git add`, every grep prints `0` on both trees, the vacuous form | at e8a56d7e the tool rewrites both with the comma (fixture `R2 does not fire outside Markdown` expects `R8` on a non-Markdown `#` line, and a quoted heading has no rule of its own) |

## Not in scope

| Item | Why | Where it goes |
|---|---|---|
| rewriting the dated records that read `exit 0` from the old card scripts | a record quotes what a program printed at a named ref; the verbatim-quote rule and R9 keep it; the adapter's live sentence is the one that changes | Q1 |
| `doc-drift-rows-check.sh`, red on main today (`QUOTE DD9: not found at .claude/CLAUDE.md:95`) | it already exits on its count; the red is a DD row in `.sdlc/architecture.md` whose quote moved with gate-split U7b's CLAUDE.md figure, a records repair rule-gates U5 or docs-repair U5 is nearer to | the lead, in this plan's report; whichever open plan edits that row next |
| #752, paragraph-start bold labels in Markdown | its own ticket, lands after rule-gates; U4's E4 predicate is the blockquote and bullet-glyph forms only and must not grow a fifth | #752 |
| a `verdict:` line for records under `.sdlc/verdicts/` subdirectories or for `.sdlc/reviews/` | the check grades top-level `.md` files under `.sdlc/verdicts/` by #723's rule; widening it is a records-policy decision | a ticket if wanted |
| making `adapter.py land --gate` read the card checks | the gate reads one record's `verdict:`; the pre-land table is where every check is recorded | untouched |
| the E3 rewrite by hand on any live line | the tree has none; U4 refuses, never rewrites, E3 and E4 | the residual list of whichever branch adds one |

## Risks

| Risk | What this plan does about it |
|---|---|
| rule-gates U5 moves the baseline N to 52 and this plan's U3 moves it to 53 on a stale base | G0 line 5 reads N by command; U3-4 compares the baseline against `TESTS`, not against a number in this plan |
| a verdict token derived wrongly (a review's later state contradicts its cell) | U3-2's derivation table quotes the line each token comes from, and the reviewer reads all eleven; the check itself only grades the token's presence |
| the temp-dir leg of the new test leaves files behind | the test removes its directory in a `finally`; U3-5's three runs leave `os.tmpdir()` without a `verdict-frontmatter-` entry (the handoff shows the `ls`) |
| `--fix`'s replay determinism (a late branch reruns `--fix` and gets main's bytes) changes with new rules | on the tree there is nothing left for E1 or E2 to match (U4-2), so the replay over main's history is unchanged; the fixtures fix the new rules' behaviour for what comes later |
| U2's heading normalisation under Q2 is read as an ADR rewrite | it is punctuation the sweep already applied to 25 of 27 headings, no content changes, and the amendment paragraph in §6 says so; Q2 is the owner's call |
| the pre-land pair runs the card checks by exit code before U1 lands | U1 is first; after it the two agree with their last line, which is what the pre-land record now writes |

## Landing

One PR from `plan/records-gates` to `main`, title `chore(sdlc): card checks exit on findings, adapter §6 current, verdict check in npm test, colons inside strings (#741, #742, #745, #747)`, body with `Closes #741`, `Closes #742`, `Closes #745`, `Closes #747`. Draft at the first verified unit. Pre-land per adapter §2.1: P1 to P4 and every unit row at the branch head, every `sh .sdlc/checks/*.sh` recorded by last line and exit code (U1's rule, which this plan is the first to be graded under), `npm run build` once (P2), record at `.sdlc/verdicts/records-gates-prepr.md` with `verdict: 🟢` as its last `verdict:` line and `sha:` at the head; smoke not owed (no `src/ui/` change, stated in the record). Then the `shipping-changes` squash and sync steps, and the plan-closing rule of adapter §5 (status `done`, four units ticked, the file to `.sdlc/plans/archive/`, the four tickets closed by the PR's `Closes` lines and `adapter.py close`, the baseline `ref` re-pointed per §5 step 5 since `test/` changed).

## Owner questions

| Id | Question | Recommendation |
|---|---|---|
| Q1 | The dated records that read `exit 0` from the old card scripts (pif-u9 review and handoff, pif-u10, records-policy-U2, small-fixes-prepr N4, the archived preset-intent-fidelity plan): leave them as the readings they were, or add a one-line note to each? | Leave them. Each quotes a program's output at a named ref, which is exactly what the verbatim-quote rule protects, and small-fixes-prepr N4 is the finding that became #745. The adapter's live sentence (U1-2) is the record that changes |
| Q2 | Normalise the ADR-026 and ADR-027 headings from ` - ` to `: ` so `decision-records.md` has one heading shape, in U2? | Yes: two lines, no content change, no card range moves (U2-3), and §6 then states one shape instead of two. If no, §6 names both and U2-2's second expectation reads `25` |
| Q3 | For the six gate-split verdicts whose state lives in a `\| Verdict \|` table row, is the backfilled `verdict:` line the bare token (`verdict: 🟢`, the 39-file convention) or the row's full text? | The bare token, or the token plus the row's first clause where the row qualifies it (`🟢 with one waived red` for U6a): the adapter reads the first token only, and the row stays the record |
| Q4 | Should U3's test also grade `.sdlc/reviews/` or subdirectories, since the check's rule names top-level `.sdlc/verdicts/` only? | No: #723 ruled the set; a wider set is a records-policy change with its own backfill, not a wiring ticket |
| Q5 | Grades: U4 at l3 with reviewer-l2 and verifier-l2 (rule-gates U3's grades for the same file), the rest as listed? | Yes |

## Revisions

| Date | Change | Why |
|---|---|---|
| 2026-09-25 | written, status draft. Every value measured at 7d325b32 in the main checkout (unedited; the two card checks and the verdict check run there, read-only) and at `unit/rg-U5` e8a56d7e with `git show`; the tree outside `.sdlc/` is identical between 7d325b32 and the plan's head 61225d0c. P3's two counts on this file's own lines: `0` and `0` | tickets #741 (verdict-backfill Q4), #742 (records-policy U2 review, pre-land L4), #745 and #747 (rule-gates U4 re-diagnosis §4, owner ruling R39); owner ruling R46 (2026-09-25) plans the backlog in one batch |
| 2026-09-25 | revision 2, from the checkability review at 65f8171c (12 green, 5 yellow, 4 red). Reds: U3-1's `bad 0` was unreachable because the backfill set was fixed at today's eleven while rule-gates and docs-repair add six more; G0 gains line 6 (main's check over `origin/main`'s verdicts through `git archive`, `bad K`), U3's set is that list, P4's verdict pattern is widened and pinned to the list by a fourth command, U3-2's numstat is scoped to the list's paths; U3-5 captured `date`'s exit, now `rc=$?` right after node, and its control is the `process.cwd()` mutant with the `test/` run green as the positive case; U4-2 read `R0 <n>` from gate mode, which never prints it, now from `--fix` in two throwaway clones; U4-3's planted files were untracked and never walked, now `git add`ed, with the residual block asserted. Yellows: U1-1's range control is the whole `682-694` plant (`2`) and names the `Supersedes / amended by` row; U2-3's control expects `4`; P3's Today names its checkout; the design names `classifyLine()` and `runFix()`'s counter object; the eleven-file sentences say "today's reading" | checkability review, 2026-09-25 |
