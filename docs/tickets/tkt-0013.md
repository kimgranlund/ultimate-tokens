---
doc-type: ticket
kind: feature
size: big
id: tkt-0013
status: open
date: 2026-07-17
owner:
---
# TKT-0013 — THE kebab/taxonomy migration — ADR-016 executed end-to-end in one wave

## Summary
Execute the ratified grammar (ADR-016) everywhere at once, riding TKT-0012's rename capability: kebab every emitted segment on every surface (CSS already kebab; DTCG/JSON/UI3 keys, Figma paths incl. voice/step segments), unify semantic-role names (suffix-derived kebab in ALL formats), fix the size-row casing, rename homonyms (icon-gap, pill-radius), nest scrims ({n}/scrim/{step}), JSON keys by slug, one collection-name constant, collection renames Geometry→Breakpoints + Color Modes→Color Semantic with registry migration, BZZR migrated in-place via MCP, consumer skills + design-systems plugin re-synced, docs/memory updated.

## Acceptance
- Every emitter green under the grammar; export-drift matrix re-run shows zero unratified divergence
- BZZR: all renames id-preserving (readback: zero orphaned bindings), collections renamed
- Consumer plugin skills + design-systems plugin re-synced in the same wave
- ADR-016 fences respected (Tailwind/ShadCN/all-pixel untouched)

## Links
- docs/reference/references/decision-records.md#adr-016
- docs/reference/reviews/2026-07-17-librarian.md
- docs/reference/reviews/2026-07-17-export-drift.md
- docs/tickets/tkt-0012.md

## Findings

### 2026-07-17 — ENGINE LEG built + gated (BZZR leg pending the Figma MCP reconnect)
Every emitter speaks ADR-016: role leaves suffix-derived kebab on all surfaces (`roleLeaf`), scrims
nested via `refPath`/`refSlug` (500 base implicit), JSON slug-keyed with kebab constants, DTCG/UI3
under the canonical collection constants (`src/engine/collections.js` — the C1 three-way split is
dead), type paths `type/{kebab-voice}/{lower-step}/{kebab-prop}`, Font Primitives
`font|weight|weight-style/{kebab-voice}` + `override/{voice}` (was family/voice), geometry size
rows kebab with the `icon-gap`/`pill-radius` homonym renames, `Breakpoints` collection (was
Geometry), `Color Semantic` (was Color Modes). Style planner binds the new grammar (styles-panel
DISPLAY names untouched — TKT-0025's seam); both sandboxes migrated (binder role leaf + refPath
mirror; flagship recursive DTCG walk for the nested scrims + metric pool on "Breakpoints").
MIGRATION MAPS frozen in migrations.mjs: `kebabWaveVarRenames` (live-plan-derived reverse
transform — 330/362 float vars) + `kebabWaveColorRenames` (46 roles + 7 scrims per palette) +
both collection renameFrom maps, all riding the TKT-0012 channel (renamecap gates re-proven with
synthetic old-era fixtures). DTCG composite $value inner keys stay W3C camel (spec-fenced);
Tailwind/ShadCN/CSS-vocabulary fences respected. Docs + skills re-synced in the same change
(knowledge-03/04/05, adding-semantic-roles, adding-export-formats, maintaining-figma-plugins,
consumer color-tokens skill + its parity script). 27/27 green; build clean.
