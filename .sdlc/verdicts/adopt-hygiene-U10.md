# Verdict adopt-hygiene U10 · 🔴

Graded by sdlc-verifier on 2026-09-17 (evidence run: adopt-hygiene-U10-verifier-l3-p1, grade l3, Fable 5.1; the branch-sweep row re-measured by the Verifier seat). Branch `unit/hygiene-U10` @ a6e32b2, merge-base 6c83520, graded against the `sdlc/adopt` plan copy at 6636b74 (the branch copy differs only in a 162-vs-161 prose count, which the head proves wrong, and the last revision row). Worktree left at status 0; controls at 279ae0f and on a mutable scratch copy of head, both removed; root checkout untouched. The handoff and `.sdlc/verdicts/adopt-hygiene-U10-review.md` were not used as evidence.
Tally: 9 rows (6 criteria + 3 gap rows). 🟢 7 · 🟡 2 · 🔴 1. The unit's own files are correct: the card and index amendments are complete, and the new `Source` range check holds across all 24 ADR cards. One pre-land gap is still open, in the plan's Landing sentence, which the Orchestrator owns.

| # | Criterion | State | Evidence | Negative control |
|---|---|---|---|---|
| U10-1 | every card whose source carries U1's amendment names it, index cell too, no "none stated" contradiction | 🟢 | `card-amendment-check.sh`: `stale total: 0`. Independent sweep of all 38 cards against every dated amendment in their sources: 0 omissions; the source carries exactly 6 dated amendments and all 6 cells name them; `ADR-016` and `SITE-runbook` now agree with `decisions.md:33` and `:50` | at 279ae0f the same sweep reports the six omissions the pre-land record named |
| U10-2 | the check is not vacuous: card set derived from Source rows, covers a new amendment anywhere | 🟡 | a planted amendment under `## ADR-011` prints `stale card ADR-011`, `stale index ADR-011`, `stale total: 2`, as the plan says; the card set is derived (38 of 38 resolve). Concern: the same plant under `## ADR-023` prints `0` because the card title and index row already carry that date (lines 20-21 grep the whole card, not the amendment cell), and a plant dated 2026-09-18 never fires. The check is narrower than the criterion text; a pass at head still implies the fact, because the independent sweep found 0 omissions | restored copy: `stale total: 0`; the two probes above are the finding |
| U10-3 | no other live record contradicts `decisions.md` on the six records | 🟢 | the six `index.md` cells each carry the amendment with content; `none` count 0 | at 279ae0f: `2` |
| U10-4 | the U8 and U9 sweeps stay green | 🟢 | all nine `u8check` lines at their §U8 expected values with live `gh`; wording check `0, 0` exit 0; U9-1 prints exactly its three named misses; U9-3 silent, `npm ci` count 3 | at 61a3f90: `disagreeing: 20` with 20 `no-note` lines, and the other lines at their pre-fix values |
| U10-5 | gates green, tree clean, branding clean | 🟢 | `✓ all 44 test files passed`; status 0; `branding: clean (435 files scanned)`; P3 wall 0 (2 without the branding-gate exclusion); the diff against `sdlc/adopt` touches only `.sdlc/` files | role-table plant at 279ae0f: 3 FAIL lines; restored, green, tree clean |
| U10-6 | every ADR card's `Source` line range matches its section, start and end | 🟢 | `card-source-range-check.sh`: `range mismatches: 0`; independent recompute over all 24 ADR cards: 0 disagreements. ADR-010 cites 145-161 and 161 is the last non-blank line, so the `sdlc/adopt` prose is right and the branch copy's 162 is wrong | at 279ae0f: `range mismatches: 26` (12 starts, 14 ends); planted end and start shifts each print their exact mismatch line |
| G1 | pre-land gap: the quoted `/Users/` loop in the plan's U7 row | 🟢 | the loop carries its `-- .sdlc` scope again and prints nothing at head | without the scope it prints `missing .claude/skills/shipping-changes/references/best-practices.md` |
| G2 | pre-land gap: the Landing branch sweep | 🔴 | the command is now a glob and is right, but the sentence around it is false at this head: it says the sweep removes `sdlc/adopt` plus "the nine `unit/hygiene-U1` to `U9` branches" and that the count then prints 28. The root checkout has 40 local branches and 10 `unit/hygiene-*` (U10 exists), plus the new `feat/646-poster-strip-chroma` worktree branch, so the glob deletes 11 and leaves 29. U3-4's "at most 28" is still not met by the written procedure. Re-measured by the Verifier seat | `git branch \| wc -l` = 40; `git branch --list 'unit/hygiene-*' \| wc -l` = 10 |
| G3 | pre-land gap: the six cards and index cells | 🟢 | rows U10-1 and U10-3 | at 279ae0f both fail |

## Gaps for the next pass
1. 🔴 `.sdlc/plans/adopt-hygiene.md` Landing: the sweep sentence names nine unit branches and a post-sweep count of 28; at this head there are ten unit branches and 40 local, so the glob leaves 29. Restate both from a measurement taken at the landing head, and say what happens to the unrelated `feat/` branch. Orchestrator-owned; the unit's own files need no change.
2. 🟡 `card-amendment-check.sh` greps the whole card rather than the amendment cell, so a card that names the date elsewhere passes, and it keys on known dates. Tighten it to the `Supersedes / amended by` cell before it is reused, or the pass proves less than the criterion claims.
3. 🟡 Criterion 6's row is written into the §U6 table, not §U10's; §U10's own table has five rows.
