# Verdict U2 · 🟢
verdict: 🟢

| Field | Value |
|---|---|
| Unit | U2, plan `preset-intent-fidelity`, ticket #681, "the ramp passes through the anchor at stop 500 in all three modes, with the Reset action" |
| Head verified | `unit/pif-u2-ramp` @ `959c43dd60e18a634956c9affbc401b1ba8bd060` (Lane A's `959c43d0` is a typo). Plan tip `b9f2e0ee` is NOT an ancestor of head; head sits on `552eafcd`, and `b9f2e0ee` is a single plan-doc commit on top of it, so nothing in the plan branch's code is missing from head |
| Base | `git merge-base 959c43dd origin/main` = `bf2aaf659fde4db3bddaed8dfa23e2f485ab2c46`, equal to the handoff's Base row and to `git merge-base plan/preset-intent-fidelity origin/main` |
| Method | Read-only. My own scratch worktrees under the scratchpad: `vhead` (`959c43dd`), `vbase` (`954675c6`, the owner's parity baseline), `vpre` (`552eafcd`, the pre-U2 plan tip), `vneg` and `vneg2` (head plus one deliberate fault each). Every colour number measured on the rendered path, `projectView(hydrate(preset))`, over 344 subjects (343 curated presets plus the default kit) times 3 modes. My CIELAB L*, OKLab, OKLCH and dE implementations are my own, written in the probe, never the engine's. All worktrees removed after the run |
| Corpus size, my own count | 1,032 documents, 11,388 palette-renders, 284,700 ramp cells, 3,396 anchored palettes (3,380 corpus plus 16 default kit) |
| Gates at head | `npm test` 🟢 48/48, exit 0, **146.80 s** wall, tree clean after. `node test/engine/anchor.mjs` 🟢 exit 0, 65.6 s. `node test/engine/semantic.mjs` 🟢 exit 0. `npm run gate:corpus-contrast` 🟢 PASS, worst cell 4.500:1. `node scripts/audit-citations.mjs` 🟢 exit 0, STALE 0 in all 20 sections. `node test/repo/branding.mjs` 🟢 clean, 455 files. `node test/ui/headless-boot.mjs` 🟢 exit 0 |

## Criteria

| # | Criterion | State | Evidence | Negative control |
|---|---|---|---|---|
| C1 | `npm test` green on the branch head, last line `all 48 test files passed`, then `git status --short` prints nothing | 🟢 | `/usr/bin/time -p npm test` in `vhead`: `✓ all 48 test files passed`, exit 0, 146.80 s wall (155.64 user, 2.77 sys), under the 175 s ceiling. `git status --short` empty immediately after | 🟢 I ran it. `vneg`, one `roleTable[0].light` set to `999`: `npm test` exits 1, `▶ engine/semantic.mjs FAIL`, `FAIL refs-canonical - primary = 550/450 != canonical 999/450`, `✗ 1/48 test file(s) failed`. I also ran the whole-table variant (every `light` ref set to `999`): still exit 1 with one FAIL line, because `refs-canonical` reports the first mismatch. The plan's "17 FAIL lines" figure does not reproduce from either corruption shape (records note 3 below); the control bites regardless |
| C3 | Ramp pass-through: for every in-window anchored palette, stop 500 hex equals `anchor` in all three modes, and the 25-stop export ramp agrees at 500 | 🟢 | My own sweep, own CIELAB L* for the window test: 10,158 in-window anchored renders (3 modes x 3,386 = 3,370 corpus plus 16 default kit), **stop 500 exact 10,158, off 0**, with the 19-stop and 25-stop sets agreeing at 500 in every one. 30 out-of-window renders resolving to exactly 10 distinct names. The gate's own line at head reads `anchor-ramp: 10110 exact, 0 off (in-window sources only, 3370 of 3380)`, the corpus-only subset of my count | 🟢 I ran it. `vneg`, both anchored stop-500 emissions shifted by one 8-bit code on the red channel: my sweep reports **exact 9, off 10,149**. Separately, removing the stop-500 pin entirely (`&& false`) moves only 3 of 10,158 cells, which is a finding about the construction, not a control: the piecewise build already converges to the anchor at 500 and the verbatim emission is a rounding guard. The in-suite `lift: 40` control the plan names runs every time at `test/engine/anchor.mjs:677-689` |
| C4 | Non-anchored byte identity (ramp part): with every `anchor` stripped, `paletteStops` output byte-identical to the pre-unit tree for all 3,780 palettes in all three modes, and the default kit byte-identical | 🟢 | C4 is a CODE-PATH criterion, so I fed ONE dataset (the pre-U2 corpus at `552eafcd`, which carries no `anchor` and the pre-unit `lift` values) through BOTH engines, with `anchor`/`sourceAnchor` stripped on every palette. **284,700 cells, 11,388 rows, 0 differing ramps, 0 differing cells.** Rendering each tree's own stored data instead reports 206,933 diffs, because U2's own scope also rewrites the corpus (`liftForTone` retired, `lift: 0` stored, `anchor`/`sourceAnchor` added) - that is the data changing, not the non-anchored code path. The prime half is gated in-suite: `prime-identity-control: 3796 exact, 0 off` | 🟢 I ran it. Same harness, head engine, same data, `hue` bumped by one degree: **204,180 differing cells in 11,383 ramps**. A `chroma + 1` mutation moves only 3 cells, which is itself REQ-002 working (`rampChromaOf` replaces `p.chroma`), so I used `hue` |
| C5 | Monotone, gap, distinct, window-clamp and notch on every anchored ramp; CIELAB L* non-increasing in all three modes with the #668 class at 0 | 🟢 | All measured by me on the rendered path with my own L*, OKLCH and the product's CAM16 chroma. **Monotone: 0 violations** over all 3,396 anchored palettes x 3 modes x both stop sets (the #668 class is 0 in every mode, not only even). Allow-lists re-derived by NAME from my own sweep and compared to the gate's frozen arrays: window-clamp **10 measured / 10 constant, 0 missing, 0 unexpected**; gap-19 **91 / 91, 0 / 0**; distinct-25 **14 / 14, 0 / 0**; notch **78 / 78, 0 / 0**, splitting **15 perceptual / 9 peak / 54 even**. Lone-spike 0 with no allow-list. Gate lines at head agree | 🟢 I ran two. (a) The one-code shift in `vneg` also drives the C3 miss above, proving the same sweep discriminates a single byte. (b) A same-length one-name substitution into each of the four allow-lists compares unequal to the measured set for all four, so the name comparison cannot pass a substitution. In-suite: `dropCheck`/`swapCheck` at `anchor.mjs:841-857` call the real `allowListMatches` against real measured data, plus synthetic predicate controls at `:641-666` |
| C8 | Contrast: `hpg-role-contrast` green with the Q-B 41 pinned by name as pending U4; MCP `contrastLint` 0/0/0 over the default kit; `gate:corpus-contrast` 0 of 7,560 cells under 4.5 | 🟢 as ruled | `node test/engine/semantic.mjs` exit 0: `pass role-contrast Q-B floor gate: 0 unlisted drops, 0 further erosion (41 cells named "pending U4")`. My own `contrastLint(brandKit(defaultDocument()))` per mode: **perceptual 0, peak 0, even 0**. `npm run gate:corpus-contrast` PASS: 343 documents, 3,780 palettes, 7,560 cells, 0 carried below 4.5, worst 4.500:1 | 🟢 In-suite and running every time: the Q-B controls at `test/engine/semantic.mjs:395-403` run the same `checkFloors` predicate against a mutated copy in both directions, an unlisted floor lowered below its `bf2aaf6` value and a listed floor eroded further, and fail if either passes |
| C12 | Reset action: a Hue or Chroma edit drops `anchor` and keeps `sourceAnchor`; the Reset control restores `anchor` and re-attaches so the 19 ramp hexes and 7 prime rungs deep-equal the pre-edit capture; skew and lift edits keep `anchor` | 🟢 | `node test/ui/headless-boot.mjs` exit 0, `HEADLESS BOOT PASS`. The `(rst)` group drives the real Hue slider and the real Reset button, then asserts `anchor` restored, the exact pre-detach hue/chroma/lift snapshot restored (stamped to a non-zero lift first, so re-derivation cannot coincidentally pass), the snapshot fields cleared, and both the 19 ramp hexes and the 7 prime rungs deep-equal. `(rst5)`/`(rst5b)` drive the real Skew and Lift sliders in even mode and assert `anchor` survives. `(rst-corpus)` extends the round trip to all 3,396 anchored palettes across all eight categories plus the default kit, at the field level and on the full 25-stop rendered ramp | 🟢 I ran the plan's own stated control. `vneg2`, `resetAnchor` in `src/ui/sections/color.js` replaced by a no-op: the shim exits 1 with `✗ (rst3) Reset restores anchor (got undefined, want #1B45A2)` plus `(rst3b)`, `(rst3d)`, `(rst4)` and `(rst4b)`. The in-suite control `(rst6b)` is stronger than the plan's text: it stubs the OLD re-derive-and-zero-lift behaviour rather than a tautological no-op, and proves `(rst3b)`/`(rst3c)` catch the real pre-fix regression. A documented improvement on the criterion, not a shortfall |
| F4 | Peak and perceptual must NOT render byte-identical for any anchored palette (the ruled tripwire against a straight-lerp regression) | 🟢 | From my own head dump: over all 3,796 palette-renders, **3,794 differ, 2 identical**, and I checked both identical ones in the source: they carry no `anchor` (one architecture `neutral` at chroma 6, one direct design-system preset's `Primary`), so **0 anchored palettes are identical**. The gate reads `anchor-f4 peak-vs-perceptual: 3380 of 3380 differ (0 identical required)` | 🟢 In-suite and running every time: the F4 curve, tension, vibrancy and hueSpace rows each assert "moved 16 default-kit anchored ramps, stop 500 moved 0", which reds if any of those four controls goes inert on an anchored path. The handoff records a scratch run with `anchorLerp` replaced by the straight lerp: all four move counts drop to 0 |
| Q-U2-5 / REQ-002 | A group's Base chroma moves an anchored ramp's ends while stop 500 stays byte-exact to the anchor | 🟢 | My own probe over the first 60 presets x 3 modes, 1,650 anchored renders, Base chroma forced to 40 on all four groups: **at least one non-500 stop moves in 1,650 of 1,650**, and stop 500 stays byte-exact to the anchor in **all 1,644 in-window renders, 0 broken** (the 6 excluded are the window-clamped sources, which clamp by Q3(b) design). Both halves hold at once | 🟢 In-suite and running every time: `(gid3)`/`(gid8)`/`(gid8b)` in `test/ui/headless-boot.mjs` are the ruled tripwire and are green inside the passing shim run; a regression to the literal unconditional anchor basis makes Base chroma inert for an anchored ramp and reds them |
| Q-D | The hueSpace flip yields max dE_OK <= 0.01 over every anchored ramp in perceptual and peak, a max dE_OK above the JND in even, a default-kit 2-code check, and a shim presence/absence check | 🟢 | My own OKLab dE over all 3,396 anchored ramps, flipping `hueSpace`: **perceptual max dE 0.0047** (worst music "Liquid light show" tertiary stop 925), **peak max dE 0.0053** (worst music "Studio 54" secondary stop 950), both <= 0.01. **Even max dE 0.1645** corpus-wide and 0.0399 on the default kit, both above the 0.01 floor. Default-kit max code diff **1** in perceptual and **1** in peak, within the <= 2 check. Full-corpus code diffs reach 10 and 12, reported and not gated, exactly as revision 23 ruled. My numbers and worst-case names reproduce the gate's two bound lines exactly. Shim side: `(hs1)`-`(hs6)` assert the doc-level control is disabled for anchored perceptual and peak with its reason visible, enabled in even, re-enabled when one palette detaches, and the per-palette note present in perceptual and absent in even; `(hs9)` covers a mixed doc | 🟢 I ran two. (a) Engine: `vneg2`, the anchored OKHSL hue solve aimed 6 degrees off target, my probe reports perceptual max dE **0.0806** and peak **0.0892**, both breaking the 0.01 bound, and default-kit codes 27 and 29, breaking the 2-code check. (b) UI: `vneg2`, `hueSpaceForced` forced to `false`, the shim exits 1 on `✗ (hs1)`, `✗ (hs1b)` and `✗ (hs2)`. In-suite: `(hs8)` proves the disabled-predicate tells a disabled segmented control from an enabled one, and `(hs9)`'s own control proves the per-palette rule is not a copy of the doc-level one |
| Parity | 0 changed hex cells against the owner's baseline `954675c6` over the full corpus, 3 modes, 343 presets plus the default kit | 🟢 | My own dump-and-diff, `vhead` against `vbase`: **1,032 documents, 11,388 palettes, 284,700 cells, 0 differing ramps, 0 differing cells, 0 missing rows.** Both stop sets are covered: the 19-stop display ramp is `fullRamp` filtered (`src/ui/model.mjs:949`), so 25-stop identity implies 19-stop identity | 🟢 I ran it. The same harness pointed at `vpre` (`552eafcd`) reports **236,985 differing cells in 10,189 ramps**, so the 0 is a measurement, not a vacuous compare |

## Records

**Do the records match the measured numbers at head?** Mostly yes, and every one of review pass 6's four records findings is fixed at head. Three items remain.

| # | Item | State | What I measured |
|---|---|---|---|
| 1 | Review 6 Finding 1, the stale sha column | 🟢 fixed | `eafd16b8` dropped the sha column and renamed the table to a "Live subject list" of subject lines only. I resolved every one of the 9 subject lines against head with `git log --oneline --fixed-strings --grep`: **each matches exactly one commit on the branch**. The R8 row, the review-pass-3 "Final state" paragraph and the Q-D addendum all point at the new list |
| 2 | Review 6 Finding 2, review 4's disproved claims | 🟢 fixed | The three lines are corrected in place, not only superseded. The `:433` line now reads "as measured at review pass 4" with a dated correction naming review 5's 865 missed roots and 152 regressed stops. The `:577-583` paragraph carries a correction block giving the state at head, 0 stops in 0 ramps worse than cam16 by more than 5 degrees and an even-mode max residual of 4.55 degrees. The `48/48 green (49 with (hs9))` line now reads 48 test files |
| 3 | Review 6 Finding 3, the 79 that did not reproduce | 🟢 fixed | `:673` now reads 66 at head, labelled with the tree it was measured on and why the original 79 predates the fast path |
| 4 | Review 6 Finding 4, Q-U2-7's by-name notch diff | 🟢 fixed | `.sdlc/questions/pif-u2.md` carries the full by-name addendum, 9 added and 7 removed, all even, net +2, a cause per name, plus the owner's ruling |
| 5 | Review 6 fix item 5, the residual percentile table | 🟢 present and it reproduces | The table is in the handoff's new records-fix section. I re-derived it independently. Every maximum matches to the digit: perceptual oklch 5.47 and cam16 6.14, peak oklch 5.55 and cam16 6.67, even oklch **4.55** and cam16 52.07. Over-10-degree oklch is **0 in all three modes**. Medians and p90/p99 agree within 0.02 to 0.6 of a degree, the expected spread from a slightly different stop-exclusion rule and percentile convention. The recorded even-mode cam16 over-10 count of 5,272 reads 5,405 on my filter, same order, same conclusion |
| 6 | One sentence in the new records-fix section contradicts its own table | 🟡 attention | The closing line reads "the count that changed between the two even-mode rows is the max, not the over-10-degree column, which was already 0 at review 5". The table two lines above it records review 5's even-mode over-10-degree oklch count as **151**, and review 6's own body gives 151 as well. So the over-10-degree column DID change, 151 to 0, and was not 0 at review 5. One clause, introduced by `eafd16b8`, false against the document's own numbers. Fix: strike the clause after "in either reading" |
| 7 | The board's U2 row is stale on the owner's ruling and one commit behind | 🟡 attention | At plan tip `b9f2e0ee` the row reads `unit/pif-u2-ramp @ eafd16b8` and ends "NOTCH_ALLOW 78 (15/9/54) **pending the owner** against the ruled 76". Head is `959c43dd`, whose whole purpose is to record that the owner ruled 78. The row also carries `npm test 138.96 s`, the builder's own figure; review 6 measured 161.41 s and I measured 146.80 s on the same code, so that single number is load-dependent and not reproducible, though every reading is under the 175 s ceiling. The row's status emoji 🔵 and pass count 7 are correct. The board copy inside the unit worktree is older still (`a75733d4`, 🔁 5, "halted by owner pass cap"), and `a75733d4` is not on the branch after the rebase; that copy simply predates the Orchestrator's board commits, so it is not U2's defect |
| 8 | The plan's C1 negative-control figure | 🟡 attention, plan-side | C1's text expects "the 17 FAIL lines `.sdlc/adapter.md` §1 names". I corrupted `role-table.json` two ways, one field and every `light` ref, and got one FAIL line each time, because `refs-canonical` reports the first mismatch and stops. The control bites either way, but the 17 does not reproduce from a role-table corruption on this tree. Belongs to the plan or the adapter, not to U2's work |
| 9 | Does the notch set render 78 by name? | 🟢 yes | I rebuilt the notch predicate in my own probe, swept the rendered corpus, and got **78 names, 15 perceptual / 9 peak / 54 even, 0 missing from the measured set and 0 unexpected** against the frozen `NOTCH_ALLOW`. The gate's own line reads `notch allow-list (ruled Q-C, named pending U4): 78 (expected 78)`. The owner's ruling at `959c43dd` sets the constant to 78 and supersedes the 76. The gate compares by name, so an unexpected name still reds. Negative control: a same-length one-name substitution compares unequal |
| 10 | Em dashes | 🟢 | 0 in `.sdlc/handoffs/pif-u2.md` and 0 in `.sdlc/questions/pif-u2.md`. Across `552eafcd..959c43dd` exactly 12 em dashes survive in commit messages, all five of them in the historical commits the handoff's R8 row names by subject (4, 3, 2, 2, 1). Every commit from `8206e369` onward is clean, which is what R8 promises |
| 11 | Retired maker brand | 🟢 | `node test/repo/branding.mjs` exit 0, clean, 455 files scanned, and it scans `.sdlc/` too |
| 12 | U3 is not in U2's tree | 🟢 consistent | Neither `78a9018f` nor `da351505` is an ancestor of head, so U2's numbers are pre-integration, as the record says. The U3 yield rulings doc names nothing that constrains U2: no mention of U2 or of the notch gate. C8's Q-B 41 and C5's notch 78 both re-measure at U4 by their own ruled terms |

**Verdict.** Every U2 criterion passes on my own measurements, and every one has a negative control I ran that reds. The construction is 🟢: stop 500 exact in 10,158 of 10,158 in-window anchored renders in all three modes, monotone a true 0 in every mode on both stop sets, all four allow-lists reproducing by name, the non-anchored code path byte-identical over 284,700 cells, parity 0 against `954675c6` with a probe that shows 236,985 diffs when pointed at a tree that really differs, Q-D inside its bounds with the ruled hide in place, REQ-002 holding both halves, and the F4 tripwire clear. The three 🟡 records items are record-only and none of them changes a measured number: one false clause to strike in the handoff, one board row to refresh onto `959c43dd` with the ruling recorded rather than pending, and one plan-side control figure that does not reproduce.
