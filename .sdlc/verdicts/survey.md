# Verdict survey · 🟡

Graded by sdlc-verifier on 2026-09-18 on `sdlc/survey-refresh` @ `8120ab4` (that commit touches `.sdlc/survey.md` only, so the tree is `cf8e61a` for every gate). Gates ran in `.worktrees/survey-refresh`; every negative control ran in a throwaway local clone at `cf8e61a` (`/tmp/v-neg1`), never in a seat's worktree.
Supersedes the 2026-09-16 grading at `f9e20c5`.
Tally: 🟢 11 · 🟡 3 · 🔴 1. The one 🔴 and both survey-side 🟡 are wrong counts in the record, not repo defects.

| # | Claim | State | Evidence | Negative control |
|---|---|---|---|---|
| C1 | `npm test` with no `node_modules`, exit 0, `all N test files passed` | 🟢 | no `node_modules` in the worktree before the run; exit 0; last line `✓ all 46 test files passed`. N is 46, not the 44 the records carry (see C9) | mutated one ref in `src/engine/semantic.js` (`'550'` to `'540'`) in the clone: `node test/engine/semantic.mjs` exit 1; reverted, exit 0 |
| C2 | `npm run build` exit 0 after `npm ci` | 🟢 | `npm ci` exit 0; `npm run build` exit 0, `wrote figma/plugin/ui.html 3753.6 KB`; `git status --short` empty after | `npm run build` in the clone with no `node_modules`: exit 127, `sh: tsc: command not found` |
| C3 | `npm run smoke` exit 0 | 🟢 | exit 0, `SMOKE PASS — gallery · category · editor · export dialog all render in a real browser`, screenshots written to `smoke-out/` | `node test/smoke/smoke.mjs` in the clone with no `dist/`: exit 1, `smoke: missing dist/ultimate-tokens.html` |
| C4 | CI `build-test` and `panda-smoke` green on `cf8e61a` | 🟢 | run 35340367753, headSha `cf8e61a`: build-test success, panda-smoke success, deploy success | run 34660177977 reports build-test failure with panda-smoke success, so the per-job query does surface red |
| C5 | main has no branch protection | 🟢 | `branches/main/protection` 404 `Branch not protected`; `branches/main -q .protected` is false | the same endpoint on a non-existent branch answers `Branch not found`, a different 404, so the two states are distinguishable |
| C6 | squash-merge only | 🟢 | `allow_squash_merge` true, `allow_merge_commit` false, `allow_rebase_merge` false (`delete_branch_on_merge` false) | two sibling repos of the same owner answer merge true and rebase true on the same fields, and the same object mixes true and false values |
| C7 | 49 of the last 50 subjects end `(#NNN)`, 1 merge commit, oldest `bef0bfd` 2026-09-11 | 🟢 | `git log -50` at `cf8e61a`: 50 commits, 49 match `\(#[0-9]+\)$`, `--merges` count 1, oldest `bef0bfd 2026-09-11` | the one non-matching subject is named (`5035189 sdlc(adopt-hygiene): close-out after landing PR #653`); widened to 60 commits the count is 58, so the regex is not matching everything |
| C8 | `.claude/` tracks 57 files with the stated split | 🟢 | `git ls-files .claude` 57: agents 3, skills 49 (14 `SKILL.md` + 35 support), hooks 1, `CLAUDE.md`, `settings.json`, `workflow.json`, `naming.manifest.json` | the same count at `bef0bfd` and at `7faf3aa` is 71, so the count tracks the tree rather than being fixed |
| C9 | 49 tracked files under `test/`; adapter §1 baseline N = 44 | 🔴 | `git ls-files test \| wc -l` is 52, not 49: engine 17, ui 8, mcp 11, figma 6, plugin 5, repo 3, smoke 1, `run.mjs` 1. The survey's per-directory split is the non-fixture count (3 fixtures are tracked too). `test/run.mjs` registers 46 entries, so `.sdlc/adapter.md:24` and `.sdlc/baseline.md:17` both carry a stale N = 44. `test/ui/counts.mjs` is tracked and registered nowhere | at `180eca0` the same TESTS array held 45 entries and `.claude` held a different count, so both counters move with the tree |
| C10 | 24 ADRs, highest ADR-024; `preset-intent-fidelity` reserves ADR-025, untaken on main | 🟢 | `grep -c '^## ADR-'` is 24, last three headings ADR-022/023/024; `ADR-025` appears nowhere on `cf8e61a` outside `.sdlc/survey.md` itself | the same grep on `origin/plan/preset-intent-fidelity` finds ADR-025 twice (plan step C10 and the approval question), so the string is findable where it exists |
| C11 | generated assets byte-identical after `npm test`, `git status --short` empty | 🟡 | tree clean after `npm test` and again after `npm run build`. Concern: the claim overstates its scope. `npm test` runs five generators plus `bundle`, never `gen:type-fonts`, so `src/ui/type-fonts.js` is clean only because nothing rewrites it | in the clone, appending a comment to `src/ui/mcp-assets.js` shows in `git status`, and rerunning its generator restores the exact bytes (the check is neither blind nor vacuous); the same probe on `src/ui/type-fonts.js` survives the whole test-chain regeneration and leaks into `figma/plugin/ui.html` |
| C12 | no secrets in tracked files | 🟢 | anchored greps over all tracked files for `AKIA[0-9A-Z]{16}`, `ghp_`+20, `sk-`+20, PEM private-key header, `api_key = "..."`: 0 hits. The loose `sk-` scan only hits the word `dusk-` in palette prose and the two `.sdlc` docs that name the patterns. History not scanned | a planted `ghp_`-shaped token in a tracked file in the clone: 1 hit, 0 again once removed |
| C13 | `.claude/settings.local.json` and `.sdlc/launcher.env` cannot reach a commit from this checkout | 🟡 | `git check-ignore -v` matches both, in the root checkout and in this worktree; neither file currently exists. Concern: neither rule is in the repo. `settings.local.json` is covered only by `~/.config/git/ignore` (this machine's user-global file) and `launcher.env` only by `.git/info/exclude` (this clone), so a fresh clone anywhere else has no protection for either | `git check-ignore -v README.md` exits 1 with no match, so the command is not matching everything |
| C14 | five `.git-worktrees/` worktrees with no board row; `unit/pif-u1-anchor` = `origin/plan/preset-intent-fidelity` @ `fb3ad33` | 🟢 | `git worktree list` shows the five (638-dual-radix, 662-contrast-policy, 668-stop800-uptick, 674-adia-warning, pif-u1-anchor); `grep -cE 'pif-u1\|638\|662\|668\|674' .sdlc/board.md` is 0; both refs resolve to `fb3ad33`. Note: the list already drifted from the survey - the two `/private/tmp/pr682-*` entries are gone, two scratchpad worktrees replaced them, and `fix/674-adia-warning-lift` moved to `9667ac3` | `grep -c hygiene .sdlc/board.md` is 11, so the board grep does find rows that exist; `origin/main` resolves to a different sha (`1ea2f80`), so the rev-parse comparison can disagree |
| C15 | baseline, architecture and debt cite a head older than `cf8e61a`; name the facts the commits since `180eca0` touch | 🟡 | Heads confirmed: `baseline.md` `7faf3aa` (20 commits behind `cf8e61a`), `architecture.md` and `debt.md` `f9e20c5`, which is not in main's history at all (it landed squashed as `180eca0`). Concern: the range is 16 commits, not the 8 the claim states. Facts they touch: `baseline.md` is stale in three of its four numbers (`all 44 test files passed` is now 46, `ui.html 3695.6 KB` is now 3753.6 KB, CI run 34804556354 on `7faf3aa` is superseded by 35340367753 on `cf8e61a`); its tree-clean and no-lint-script rows still hold. 18 of the 72 paths cited by `architecture.md` and 16 of the 96 cited by `debt.md` were edited in the range, headed by `src/engine/exports.js`, `src/engine/tonal.js`, `src/ui/sections/color.js`, `src/ui/model.mjs`, `figma/plugin/ui.html`, `figma/binder/.../code.js`, `docs/reference/data/role-table.json`, `test/run.mjs` and `test/ui/headless-boot.mjs`, so their line-anchored citations are unverified at this head. The 53-role contract they rest on is intact (`npm test` green) | `git log --oneline 180eca0..180eca0` is 0 commits and the empty diff yields no paths, so the range query is not reporting everything by construction |

Corrections the record needs, for the Conductor: `test/` tracks 52 files, not 49 (C9); `test/run.mjs` registers 46 tests, so `.sdlc/adapter.md:24` and `.sdlc/baseline.md:17` must say 46, not 44 (C9); the range since `180eca0` is 16 commits, not 8 (C15); `baseline.md`'s test count, bundle size and CI run id are stale (C15); C11's byte-stability holds for every asset `npm test` regenerates, which excludes `src/ui/type-fonts.js` (C11); the survey's worktree table and `origin/main` both moved during grading (C14).

Correction (2026-09-19, plan records-followup U4, #709): a quotation of program output in this file had been reworded to avoid an em dash. It now reads as the program prints it. Rule: `.sdlc/adapter.md` §3, Verbatim-quote rule.
