---
plan: preset-intent-fidelity
unit: U10
ticket: 681
seat: verifier
sha: 3c2667a764d5e1e19b0000a3f7f4f4ef5fdbaf38
graded_clone_sha: 4bfd09d83be38cba1fab808b9b21ef71949fc109
code_sha: 872e0b7a100c333ffcc35f264ffdbff76e215750
base: 8f0e08d65db288dd0e112b8152bca50d3d9bbd13
date: 2026-09-23
result: 🟢
verdict: 🟢
---

# Verdict pif-U10 · 🟢

Next action for the Orchestrator: merge U10; add `test/engine/exports.mjs` to the plan's off-list table (row 11) in the next revision, no rework.

Verified read-only in `git clone -q --shared` at `4bfd09d8` under the scratchpad, no `node_modules`; comparisons against a second shared clone at `origin/main` `dae67a3a`. Rulings applied: R26 (cap at plus or minus 1, F5 to #738, hct.js:276 reworded), R27 (U10-5 graded on hue, half chroma accepted). Head moved to `3c2667a7` after the clone run: `git diff --stat 4bfd09d8..3c2667a7` names only `.sdlc/verdicts/pif-u10-review.md | 13 +++++++------` (one commit, `review regraded against R27, PASS 🟢 at 65b72746`), and `git diff --name-only 8f0e08d6...3c2667a7 | wc -l` still reads `16`, so every row below holds at `3c2667a7`. Counts: 14 🟢, 1 🟡, 0 🔴.

| # | Criterion | State | Evidence | Negative control |
|---|---|---|---|---|
| U10-1 (F1) | `#000000` anchor renders real hex in perceptual, peak, even | 🟢 | probe on the engine at head: `#000000 perceptual nan=0 100:#E9E1E3 150:#D6C2C7 500:#1B1B1B`, `peak nan=0 ... 500:#1B1B1B`, `even nan=0 ... 500:#1B1B1B`; `#FFFFFF` and `#010101` `nan=0` in all three; `rgbToOkhsl(black).s=0`; stop 500 is the clamped window edge for all three (their L* sits outside 9.95..95.05), the in-window `#808080` case is covered by U10-3 | `src/engine/okhsl.js` reverted to `8f0e08d6` in the clone, same probe: `#000000 perceptual nan=19 100:#NANNANNAN 150:#NANNANNAN 500:#NANNANNAN`, peak `nan=19`, even `nan=0`; `rgbToOkhsl(black).s=NaN` |
| U10-2 | guard lives in the engine | 🟢 | `git diff --name-only 8f0e08d6...4bfd09d8 -- src/engine/ src/ui/persist.js` names `src/engine/hct.js src/engine/okhsl.js src/engine/tonal.js`, no `persist.js`; the guard is `if (L <= 0) return { h: ..., s: 0, l: 0 }` at `okhsl.js:194` | U10-1's probe calls `paletteStops` directly, bypassing `persist.js`; the reverted-guard control above reds through that same path |
| U10-3 | a test pins it | 🟢 | `node test/engine/anchor.mjs` at head exit `0`: `pass  anchor-achromatic: 24 of 24 ramps real (#000000, #FFFFFF, #010101, #808080 x 3 tone modes x 2 hue spaces), planted-NaN control caught` | same file with `okhsl.js` from `8f0e08d6`: exit `1`, `FAIL  anchor-achromatic: 20 of 24 ramps real (...)` |
| U10-4 (F2) | fact sheet current | 🟢 | `grep -n -e 'OKHSL lightness ladder' -e 'unchanged from the previous release' docs/marketing/fact-sheet.md \| wc -l` prints `0`; line 18 now reads `derived on a **CIE L\*** lightness ladder` and `The ladder moved from OKHSL to CIE L\* in the same release (#681) that moved the ramps` | the same grep on `8f0e08d6:docs/marketing/fact-sheet.md` prints `18:\| Prime swatches per palette \| **7** raw identity swatche...` |
| U10-5 (F3) | envelope never reads 0 across a band; capped stops carry main's hue | 🟢 | `chromaEnvelope(stop, 500, -36, DEFAULT_CONTROLS)` at 50/100/150/200/300/500: `0.20 0.20 0.20 0.20 0.59 1.00`; rendered Warning (`projectView(defaultDocument())`, lift `-36`) `100:#FCFBFA 150:#F5F1ED`; hue 250 chroma 60 lift +40 perceptual non-anchored `850:#2C3137 900:#1C2024 950:#0F1114`. OKLCH hue head vs `origin/main`: W100 `67.8` vs `67.8`, W150 `67.7` vs `67.7`, 850 `253.0` vs `250.7`, 900 `248.2` vs `252.0`, 950 `258.4` vs `255.6`; none reads grey. Chroma ratio head/main (information, R27): W100 `0.0017/0.0034`, W150 `0.0069/0.0137`, 850 `0.0128/0.0308`, 900 `0.0097/0.0185`, 950 `0.0070/0.0096`. `origin/main` renders `100:#FDFBF9 150:#F9F1E9` and `850:#253240 900:#192028 950:#0E1115` | cap removed in the clone (`sd` back to the raw quotient): envelope `0.00 0.00 0.00 0.17 0.59 1.00`, Warning `100:#FBFBFB 150:#F1F1F1`, `850:#313131 900:#202020 950:#111111` (neutral greys); `node test/engine/exports.mjs` then exits `1` with `FAIL  radix-refs-values-unchanged  — exportRadix(ALL) drifted from the pre-#638 values-form baseline` |
| U10-6 | `hct.js:276` names the hct.js caches only and cites #738 | 🟢 | `sed -n '270,282p' src/engine/hct.js` lines 276 to 279: `so this cache and the other two exact-keyed caches in this file (`peakC`, `oklchToCam16Hue`) are pure again. That claim covers hct.js only: tonal.js still keeps a bucketed `_okL` memo on `lstar.toFixed(2)`, the same defect class, tracked as #738.`; `grep -c '#738'` on that window `1`, `grep -c 'genuinely pure'` `0`; `gh issue view 738` is `OPEN tonal.js _okL memo is order-dependent, the same class as #686`; #686 still `OPEN` (closes at landing, per R26) | `8f0e08d6:src/engine/hct.js` line 276 ends `returns exactly what a fresh computation would — genuinely pure again.` with no scope and no #738 |
| U10-7 | zero em dashes in added tonal.js comment lines | 🟢 | `git diff origin/main...HEAD -- src/engine/tonal.js \| grep '^+' \| perl -CSD -ne 'print if /\x{2014}/' \| wc -l` prints `0` | same count at `8f0e08d6` prints `58`; one planted `+// planted — dash` line appended to the head diff makes the count `1` |
| U10-P1 (C1) | `npm test` green, no `node_modules`, tree clean | 🟢 | load at start `4.29 5.27 5.08` (under 5, gradable), `ls node_modules` `No such file or directory`; `npm test` exit `0`, `✓ all 49 test files passed`, `347` s (inside the 280 to 550 s ceiling), `git status --short \| wc -l` `0`; load at end `3.79 4.48 4.78` | guard reverted: `node test/engine/anchor.mjs` exit `1` (U10-3 row); cap removed: `node test/engine/exports.mjs` exit `1` (U10-5 row) |
| U10-P2 | scope wall | 🟢 | `git diff --name-only 8f0e08d6...4bfd09d8` names 16 paths: the three engine files, `test/engine/anchor.mjs`, `docs/marketing/fact-sheet.md`, the two generated assets, the handoff, plus seven the handoff's Left out names (three fixtures, `test/engine/tonal.mjs`, `test/engine/exports.mjs`, `.sdlc/baseline.md`) and the plan and review records; no path outside those | a stray edit would print a 17th name; the F1/F3 controls above each showed `1 file changed` in `git diff --stat` and the tree read `0` after restore |
| U10-P3 | landing checks, branding, no em dash added | 🟢 | `baseline-agrees-check.sh`, `card-amendment-check.sh`, `card-source-range-check.sh`, `doc-drift-rows-check.sh`, `verdict-frontmatter-check.sh` each `exit 0`; `ceiling-counts: clean`; `branding: clean (641 files scanned)`; `git diff 8f0e08d6 \| grep '^+' \| perl -CSD -ne 'print if /\x{2014}/' \| wc -l` `0` | planted dash line makes the count `1` (U10-7 row); `.sdlc/baseline.md`'s U10 correction says `baseline-agrees-check.sh` read `STALE ui.html: baseline 4117.5 KB, tree 4118.2 KB` before the repair |
| C5 | gap-19 at 72, distinct-25 at 16, allow-list 10 | 🟢 | plan revision 38 under C5: `anchor-ramp gap (19-stop) allow-list: 72` and `anchor-ramp distinct (25-stop) allow-list: 16`; `node test/engine/anchor.mjs` at head prints `pass  anchor-ramp allow-list: 10 (expected 10)`, `pass  anchor-ramp gap (19-stop) allow-list: 72 (expected 72)`, `pass  anchor-ramp distinct (25-stop) allow-list: 16 (expected 16)` | the gate reds on any other count by its own `(expected N)` shape; with the okhsl guard reverted the same two lines still print `72` and `16`, so the counts do not depend on the F1 change |
| Off-list | the plan's off-list section names every changed path outside the unit lists | 🟡 | `## Paths off the unit lists (revision 38)` lists `tonal-legacy.json`, `default-doc-ramps.json`, `radix-baseline.json`, `test/engine/tonal.mjs`, `figma/plugin/ui.html`, `src/ui/describe-mcp-assets.js`, `.sdlc/baseline.md` for U10; `test/engine/exports.mjs` (3 comment lines, the radix re-capture citation) is in the diff and in the handoff's Files table but on no plan list: `grep -n 'test/engine/exports.mjs' .sdlc/plans/preset-intent-fidelity.md` hits only revision narrative at lines 236 and 402. The plan and review records themselves are sdlc records, not product paths | the section's own table row for `test/engine/tonal.mjs` shows the same class of path is expected there; a plan revision adding the one row closes this |
| Fixtures | moves explained, regenerate identically | 🟢 | `node scripts/gen-ramp-fixture.mjs "#681 U10 pre-land F3: chromaEnvelope sd capped at +/-1 (2026-09-23)"` rewrote `default-doc-ramps.json` and `git diff \| wc -l` read `0`; `radix-baseline.json`: `radix leaves moved=30 nonWarning=0` against `8f0e08d6` and the `radix-refs-values-unchanged` gate is green in `npm test`; `tonal-legacy.json`: `8` cells moved, all Warning (`#FBFBFB #F8F8F8 #F3F3F3 #EDECEB #EAE4DE` to `#FCFBFA #F9F7F5 #F5F2EF #F0ECE6 #EBE4DC`, `#FBFBFB #FCF6F3 #FAF2ED` to `#FFFAF8 #FFF6F0 #FAF2EC`), matching the tonal.mjs comment, and `engine/tonal.mjs pass` | cap removed reds the radix gate (U10-5 row); the regenerated ramp fixture would diff if the engine and fixture disagreed |
| Head move | `3c2667a7` differs from the graded `4bfd09d8` only in the review record | 🟢 | `git diff --stat 4bfd09d8..3c2667a7`: `.sdlc/verdicts/pif-u10-review.md | 13 +++++++------`, `1 file changed`; no product, test or plan path | any second name in that stat would void the clone run and force a rerun at the new head |
| Generated assets | ui.html and describe-mcp-assets regenerate identically | 🟢 | `npm test` printed `wrote figma/plugin/ui.html 4119.1 KB` and `wrote src/ui/describe-mcp-assets.js 787.0 KB (26 files)` and the tree read `0` after, so the committed bytes equal the generator output; `.sdlc/baseline.md` KB cell reads `4119.1 KB` and `baseline-agrees-check.sh` `exit 0` | the baseline correction paragraph records `STALE ui.html: baseline 4117.5 KB, tree 4118.2 KB` from before the repair, the check's red shape |

## Notes

- Leftover clones: `rm` was denied twice. `pif-u10-verify` (at `4bfd09d8`, tree clean) and `pif-u10-main` (at `dae67a3a`) remain under the scratchpad. Logs: `pif-u10-npmtest.log`, `pif-u10-anchor-head.log`, `pif-u10-nc-anchor.log`, `pif-u10-nc-exports.log`, `pif-u10-probe.mjs`.
- `#739` (achromatic anchors take an arbitrary hue after the clamp) and `#738` (`_okL` memo) are `OPEN`; the U10 `#000000` ramps carry hue 30's tint at stops 100/150 (`#E9E1E3`), the #739 class, outside this unit.
- `#725` `OPEN`, the chroma follow-up R27 names.

verdict: 🟢
sha: 3c2667a764d5e1e19b0000a3f7f4f4ef5fdbaf38
