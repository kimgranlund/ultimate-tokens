# Verdict pif-U9 · 🟡

verdict: 🟡

Plan `preset-intent-fidelity` (#681), unit `unit/pif-u9` @ `1fe7976e14be4b74df42dbdd5422a67d340dcec5`, UB `96b93f45` (`git merge-base origin/plan/preset-intent-fidelity HEAD` in my clone prints `96b93f45`). Plan read at `96b93f45`, revisions 34 and 35. Verifier-l2, read-only, all runs in my own `git clone -q --shared` at `scratchpad/v9`, detached at the unit head, `git checkout -q .` after every plant, `git status --short | wc -l` printed `0` after the last one. My U9-8 tokens were derived from the records at UB before I read the handoff's table. The review record was read last, for the decoy shape only.

Next action for the Orchestrator: the unit's checks, controls and eleven tokens are all green. One 🟡: the eleven `verdict:` lines sit at line 2 (or in `pif-u5`'s pass-1 front matter) and name no sha, where the plan's ruling asks for a closing line naming the summarized sha whenever there is no front matter or a later pass changes the state. Rule on it (accept as placed, or a record-only rework); nothing machine-read depends on it.

Load: `12:57 ... load averages: 3.70 3.79 3.28` at `npm test` start, `13:02 ... load averages: 3.70 3.63 3.33` at its end, `13:05 ... load averages: 2.42 3.13 3.18` at the end of all controls. `npm test` wall `292 s`, started under load 5 (R13), recorded and not graded.

| # | Criterion | State | Evidence | Negative control |
|---|---|---|---|---|
| U9-P1 | `npm test` green, no `node_modules`, tree clean | 🟢 | `ls: node_modules: No such file or directory`; last line `✓ all 49 test files passed`, exit `0`, `0` lines matching `FAIL` in the log; perl count over `test/run.mjs` prints `49`; `git status --short \| wc -l` prints `0` | `roleTable[0].light` set to `999` in the clone: `node test/engine/semantic.mjs` prints `  FAIL  refs-canonical  — primary =  550/450 != canonical  999/450`, `exit 1` (single-file run of the file `npm test` would fail, not a second full suite) |
| U9-P2 | branding clean, `.sdlc/` scanned | 🟢 | `branding: clean (617 files scanned)`, `exit 0` | a one-line file carrying the retired maker's uppercase run written to `.sdlc/verdicts/zz-probe.md`: `FAIL: 1 branding violation(s) across 618 files`, `exit 1` (my first probe used the wrong spelling and printed `branding: clean (618 files scanned)`, so the count moving to 618 shows the file was scanned) |
| U9-P3 | no em dash on added lines, raw and stripped | 🟢 | the plan's pipe over `96b93f45`: raw `0`, after the backtick strip `0`; commit bodies `96b93f45..HEAD` through the same filter: `0` | `printf '+ a \xe2\x80\x94 b\n'` through the filter prints `1` |
| U9-P4 | scope wall | 🟢 | `git diff --name-only 96b93f45` (LC_ALL=C sorted): `.sdlc/adapter.md .sdlc/baseline.md .sdlc/checks/baseline-agrees-check.sh .sdlc/handoffs/pif-u9.md` plus the eleven `pif-*` verdicts, all on the declared list; `-- src test scripts figma mcp docs \| wc -l` prints `0` | `// probe` appended to `src/engine/hct.js` in the clone: the same count prints `1` |
| U9-P5 | every other landing check as at `33ab0942` | 🟢 | head: all five `.sdlc/checks/*.sh` `exit 0`; `ceiling-counts: clean` with `12` `ok` lines, equal to the `12 ok lines` in `pif-u7.md` U7-11; at UB `96b93f45`: `baseline-agrees-check.sh exit 1`, `verdict-frontmatter-check.sh exit 1`, the other three `exit 0`, the planner reading | `284 s` to `285 s` in `.sdlc/adapter.md` (one target, `grep -c` printed `1`): `ceiling-counts: 1 failure(s)`, `exit 1` |
| U9-1 | baseline check green, labelled line printed | 🟢 | `ok    ceiling test: baseline 280 to 550 s, adapter 280 to 550 s`, `ok    time test: baseline 56 to 60 s, adapter 56 to 60 s` (one adapter range), `stale total: 0`, `exit 0` | U9-2, U9-3, U9-4 and X1 to X3 each red this same run |
| U9-2 | label disagrees with baseline | 🟢 | `STALE ceiling test: baseline 280 to 550 s, adapter 280 to 560 s`, `ok    time test: baseline 56 to 60 s, adapter 56 to 60 s`, `stale total: 1`, `exit 1` | the row is the control; `grep -c 'ceiling 280 to 550 s' .sdlc/adapter.md` prints `1` at head and `0` at UB, so the sed has no target before the unit |
| U9-3 | label removed, #718's red returns | 🟢 | `STALE ceiling test: baseline 280 to 550 s, adapter none`, `STALE time test: baseline 56 to 60 s, adapter 56 to 60 s, 280 to 550 s`, `stale total: 2`, `exit 1`. The `time test` line is byte for byte revision 35's; the check prints the ceiling line first where the row says "then". Order is not graded by any consumer (the total and exit do not depend on it), so no 🟡 | the row is the control; the same `time test` line is what UB printed before the label existed |
| U9-4 | baseline ceiling sentence drifts | 🟢 | 560 in `.sdlc/baseline.md`: `STALE ceiling test: baseline 280 to 560 s, adapter 280 to 550 s`, `stale total: 1`, `exit 1`; the `Interim ceiling: **` line deleted (`grep -c` then `0`): `STALE ceiling test: baseline none, adapter 280 to 550 s`, `exit 1` | the row is the control; `grep -c 'expected between 280 and 550 s' .sdlc/baseline.md` prints `1` at head, one target |
| U9-5 | #718 behaviour unchanged | 🟢 | (i) `STALE time test: baseline 56 to 60 s, adapter 56 to 60 s, 56 to 61 s` `exit 1`; (ii) `STALE time test: baseline 56 to 60 s, adapter none` `exit 1`; (iii) `ok    time test: baseline 56 to 60 s, adapter 56 to 60 s, 56 to 60 s` `exit 0` with `ok    ceiling test: baseline 280 to 550 s, adapter 280 to 550 s`; `grep -cF 'matchAll('` prints `2` | the three plants are the controls; (i) and (ii) red on the whole `time test` line, not the exit alone |
| U9-6 | pointer sentence credits #681 U9 | 🟢 | `211:labelled figure to check against, per PR #729's out-of-scope note (#718 made the check read`; the sentence reads `#681 U9 gives this script its own labelled figure`; `git show 96b93f45:.sdlc/baseline.md \| grep -c '#718 is ruled to give this script'` prints `1`, head `0`; `ceiling-counts: clean` (U9-P5) | UB's `baseline.md` copied over the head file in the clone: the same grep prints `1` |
| U9-7 | verdict-frontmatter green, grandfather list untouched | 🟢 | `verdicts 90 graded 43 grandfathered 47 bad 0`, `exit 0`, no `MISSING`/`VALUE`/`GROWN`/`STALE`/`CLEARED` line; `ls .sdlc/verdicts/*.md \| wc -l` prints `90`, and 90 minus 47 is 43; `git diff 96b93f45 --stat -- .sdlc/checks/verdict-frontmatter-grandfather.txt \| wc -l` prints `0` | `pif-u2.md`'s `verdict:` line deleted: `MISSING pif-u2.md: no verdict: line`, `bad 1`, `exit 1`; set to `verdict: PASS`: `VALUE pif-u2.md: last verdict: PASS is not 🟢, 🟡 or 🔴`, `exit 1` |
| U9-8 | eleven values match each record's own final state | 🟢 | my table below, built from the UB bytes before reading the handoff: 11 of 11 agree with the file's only `verdict:` line, and with the handoff's own table afterwards | `pif-u2.md` set to `verdict: 🔴`: the check prints `verdicts 90 graded 43 grandfathered 47 bad 0`, `exit 0`, while my derived `🟢` (from `# Verdict U2 · 🟢`) disagrees, so the row reds on the derivation, not the check |
| U9-9 | no other verdict byte moved | 🟢 | per file `git diff 96b93f45 -- <f> \| grep -c '^[-+][^-+]'` prints `1` for all eleven; deleted lines over `.sdlc/verdicts`: `0`; `grep -c '^verdict:'` is `1` in each | `extra line` appended to `pif-u2.md`: per-file count `2`; content line 4 of `pif-u3.md` deleted: deleted count `1` (my first try deleted a blank line and printed `0`, which the `[^-]` filter cannot see) |
| X1 | a second planted label reds | 🟢 | `revised ceiling 90 to 999 s` added after the real label (`grep -c` `1`): `STALE ceiling test: baseline 280 to 550 s, adapter 280 to 550 s, 90 to 999 s`, `ok    time test: baseline 56 to 60 s, adapter 56 to 60 s`, `stale total: 1`, `exit 1` | the unplanted head run is `ok    ceiling test: ...` `stale total: 0`, so the plant alone moves it |
| X2 | a labelled `ceiling 1 to 2 s` in the build time cell reds (R2-1) | 🟢 | build cell (field 5) became `1 to 3 s warm; ceiling 1 to 2 s`: `STALE time build: baseline 1 to 3 s, adapter 1 to 3 s, 1 to 2 s`, `stale total: 1`, `exit 1`. The red comes through the `time build` line, not a `baseline none` ceiling line as the ruling paragraph first described; no row requires that wording | the same plant against rework 1's script (`git show 1dd3a106:.sdlc/checks/baseline-agrees-check.sh`): `ok    time build: baseline 1 to 3 s, adapter 1 to 3 s`, `stale total: 0`, so red-then-green on R2-1. Placing the label in field 6 instead of the time cell stays `stale total: 0`, as it should |
| X3 | the ceiling clause deleted reds `adapter none` | 🟢 | `; **INTERIM ceiling** ... about 10 - ` removed from the test cell: `STALE ceiling test: baseline 280 to 550 s, adapter none`, `ok    time test: baseline 56 to 60 s, adapter 56 to 60 s`, `stale total: 1`, `exit 1` | the head run with the clause present prints `ok    ceiling test: ...`, `stale total: 0` |
| X4 | an earlier `expected between` decoy does not fool the anchor | 🟢 | the head baseline has one `expected between` line (`97:`); planted `Old note: the suite is expected between 280 and 550 s under any load.` at line 2 and moved the real line to 600: `STALE ceiling test: baseline 280 to 600 s, adapter 280 to 550 s`, `exit 1`. A second plant of two earlier decoys (`100 and 200`, and `Interim ceiling: draft, ... 111 and 222 s` with no `**`) printed `ok    ceiling test: baseline 280 to 550 s, adapter 280 to 550 s`, `exit 0` | the unanchored regex `/expected between (\d+) and (\d+) s/` over that planted file reads `unanchored reads 100 to 200`, so the decoy would have won without the `^Interim ceiling: \*\*` anchor |
| X5 | placement follows the ruling (the plan's `verdict:` paragraph) | 🟡 | the ruling: front matter only where no later pass changes the state, "otherwise a closing line after the final block, naming the sha it summarizes". Ten files have no front matter and carry the line at `2:`; `pif-u5` carries it at `10:` in its pass-1 front matter though passes 3 (🟡) and 4 (🟢) follow. `grep -c '^verdict:.*[0-9a-f]\{7\}'` prints `0` in all 11. In `pif-u8.md`, `verdict: 🟢` at line 2 sits right above `## Overall: 🟡 pass with attention` with nothing naming `96f7d99a` | the machine read is unaffected: the line is each file's only and so last `verdict:` line (U9-7, U9-9); a closing `verdict: 🟢 at 96f7d99a` appended in the clone would make the sha grep print `1`, which is the shape the ruling asks for |

## U9-8 derivation (mine, from UB bytes, before the handoff)

| File | Line derived from | My token | File's last `verdict:` |
|---|---|---|---|
| pif-u1.md | `\| Verdict \| **PASS** (2 🟡 concerns, both plan-text / process, neither a code defect) \|` | 🟢 (precedent `verdict: 🟢 PASS` in `verdict-frontmatter-U1-review.md` for a PASS with non-blocking findings) | `verdict: 🟢` |
| pif-u2.md | `# Verdict U2 · 🟢` | 🟢 | `verdict: 🟢` |
| pif-u3.md | `# Verdict U3 · 🟢` (re-verified at `78a9018f`) | 🟢 | `verdict: 🟢` |
| pif-u4.md | `# Verdict U4 pass 7 (main-merge delta) · 🟡`, `Counts this pass: 16 🟢, 5 🟡, 0 🔴` | 🟡 | `verdict: 🟡` |
| pif-u5.md | `# Verdict pif-U5 pass 4 · 🟢 at 6d0755b9`, `The unit is 🟢.` | 🟢 | `verdict: 🟢` |
| pif-u6.md | `**PASS** , with two 🟡 carry-forwards, neither fix-first.` | 🟢 | `verdict: 🟢` |
| pif-u7.md | `🟢 **U7 is green.** 19 of 19 criteria 🟢, 0 🟡, 0 🔴` | 🟢 | `verdict: 🟢` |
| pif-u7-review-1.md | `**FIX-FIRST.** F1 blocks` | 🟡 | `verdict: 🟡` |
| pif-u7-review-2.md | `**PASS.** F1, F2 and F3 are closed` | 🟢 | `verdict: 🟢` |
| pif-u8.md | `## Regrade at 96f7d99a`: rows `Scope`, `U8-3`, `U8-6` all `🟢`, U8-1/2/4/5 already `🟢` | 🟢 | `verdict: 🟢` |
| pif-u8-review.md | `## Pass 2 · 🟢 PASS`, `No new finding.` | 🟢 | `verdict: 🟢` |

Counts: 18 🟢, 1 🟡, 0 🔴 across 19 rows (U9-P1 to U9-P5, U9-1 to U9-9, X1 to X5).

Clone: `scratchpad/v9`, removed by exact name after this record was written.
