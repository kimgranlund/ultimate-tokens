# Criteria review records-refresh · 🟢 mobilize

Reviewed by sdlc-verifier on 2026-09-18 on `plan/records-refresh` @ `8919292` (the plan's front matter still says `746f93d`, 4 commits over `cf8e61a`; the branch is now 6 commits over it, the plan commit included). Plan: `.sdlc/plans/records-refresh.md`.
All 14 criteria are 🟢 checkable: 🟢 14 · 🔴 0. Nothing goes back to the planner.

Method: I ran 11 of the 14 stated commands verbatim at `8919292` and compared the output with the pre-state the plan predicts. Every one reproduced the planner's control figure exactly, so the commands parse, the table-escaped pipes survive, and each criterion separates "unit missing" from "unit done". P1 to P4 were run as gates during the survey grading earlier today in the same worktree.

| # | Checkable | The check I would run, and what it printed at `8919292` |
|---|---|---|
| P1 | 🟢 | `npm test 2>&1 \| tail -1` then `git status --short \| wc -l`. Ran it today with no `node_modules`: `✓ all 46 test files passed`, then `0`. A standing regression gate, so it passes at the pre-state by design; the mutation control (a changed ref in `src/engine/semantic.js`) took `node test/engine/semantic.mjs` to exit 1, so the suite does bite |
| P2 | 🟢 | the stated command, comparing the built size with the one the baseline records. Ran build today: `wrote figma/plugin/ui.html 3753.6 KB` against `3695.6` in `.sdlc/baseline.md`, so the criterion is red at the pre-state and turns green only when U1 rewrites the number. `npm run build` with no `node_modules` exits 127. Note: `$CLAUDE_JOB_DIR` may be unset in a plain shell, which turns the log path into `/tmp/build.log`; harmless, but the clone path in the P1 and P2 controls becomes `/tmp/neg`, so the builder should give it a unique name |
| P3 | 🟢 | `npm run smoke 2>&1 \| grep -c 'SMOKE PASS'`. Ran it today: exit 0 and exactly one `SMOKE PASS` line. Control confirmed separately: `node test/smoke/smoke.mjs` with no `dist/` exits 1 with `` `smoke: missing <ROOT>/dist/ultimate-tokens.html — run `npm run build` first` `` `altered: absolute path written as <ROOT>` |
| P4 | 🟢 | `node test/repo/branding.mjs \| tail -1`. Ran it today: `branding: clean (441 files scanned)`, exit 0. It reads `.sdlc/`, so it is the gate that catches a bad word in the very files U1 rewrites |
| P5 | 🟢 | `git diff --name-only $(git merge-base origin/main HEAD) \| grep -vcE '^\.sdlc/\|^\.gitignore$'`. Printed `0`. A scope guard: it cannot fail from the unit being missing, it fails when the unit reaches outside its wall, which is what it is for |
| U1-1 | 🟢 | `sh .sdlc/checks/baseline-agrees-check.sh`. I extracted the script from the plan and ran it: `STALE tests: baseline 44, test/run.mjs TESTS 46`, `STALE ui.html: baseline 3695.6 KB, tree 3753.6 KB`, three `ok` timing lines, `STALE head`, `ok` ancestry, `stale total: 3`, exit 1. Exactly the planner's control. The two figures it compares are computed the same way the build prints them (the script's `length/1024` returns 3753.6, the build line's own number), so U1-1 and P2 cannot contradict each other |
| U1-2 | 🟢 | the awk fence extraction then `diff`. The plan holds exactly one ` ```sh ` fence (`grep -c` prints 1), the octal backtick pattern works in this awk, and the extracted 36 lines are the script I ran for U1-1. Before the unit, `diff` errors on the missing file; one changed character prints a hunk. Note: the check hangs on the plan file's path, which gains `archive/` at landing (the plan says so) and on nobody adding a second `sh` fence to the plan |
| U1-3 | 🟢 | `grep -cE '^[\|] .npm (test\|run build\|run smoke). [\|] 3/3 [\|] 0 [\|]' .sdlc/baseline.md` then the old-figure grep. Printed `3`, then `4`, the planner's control. The second count is what moves: it must reach `0`, and it cannot unless the old head, run id and size are all gone |
| U1-4 | 🟢 | the three-part sha and jobs command. Printed merge base `cf8e61a` against baseline ref `7faf3aa`, so the criterion is red pre-rebase, and `gh run view 35342720776` answers `1ea2f80 build-test=success panda-smoke=success`, so after the rebase the three shas line up. The jobs query reports `build-test=failure` on run 34660177977, so it can say red |
| U1-5 | 🟢 | the `git grep` sweep with the history dirs excluded. Printed three paths: `.sdlc/adapter.md`, `.sdlc/baseline.md`, `.sdlc/debt.md`. It must print one. Checked for a self-trap: the new check script writes the count regex as `(\d+) test files`, which the sweep pattern `[0-9]+ test files` does not match, so adding the script cannot make this criterion fail on itself |
| U1-6 | 🟢 | the four-part adapter command. Printed `0`, `2`, `0`, `0`; expected after the unit is `0`, `0`, `1`, `1`, so values two to four discriminate. Value one is a scope guard that reads `0` either way, which is its job, not a defect |
| U1-7 | 🟢 | the three-part type-fonts command. Printed `0`, `0`, `false false true`. The third value is the useful trap: it proves the same regex shape does see a generator that is in the chain, so a later change adding `gen:type-fonts` to `npm test` would flip it and mark both records false |
| U1-8 | 🟢 | the check-ignore loop and `git ls-files`. Printed this machine's user-global ignore file and this clone's `.git/info/exclude`, then `0`. Both paths must read `.gitignore` after the unit. This is the criterion that closes the survey verdict's C13 concern, and it fails loudly while the rules stay local |
| U1-9 | 🟢 | the eleven-part staleness-note command. Printed `0 0 0 0` for the note and rule counts, `0`, `0`, `1` for the quote counts, `ancestor 1`, `10`, `0`, `0`. Expected after the unit is `1 1 1 1`, `1`, `1`, `1`, `ancestor 1`, `10`, `0`, `0`. Checked the two awk traps: neither `architecture.md` nor `debt.md` has a `## ` heading before its insertion point (first heading at line 16 in both), so the `/^## /{exit}` scan does reach the note. Note: the last two values count deleted lines only, so a stray added line elsewhere in either file would slip through; the reviewer's read of the diff is what covers that. The phrase "quotes the verdict faithfully" is carried by the two number greps against `.sdlc/verdicts/survey.md`, which do match my own C15 line; the rest of that clause is a reading judgment, which is what the l1 reviewer and verifier are for |

Two notes for the Orchestrator, neither blocking. U1-1's expected `stale total: 0` also requires the adapter's three time ranges to equal the rounded minimum and maximum of the new baseline timings, so step 1's quiet-slot rule is load bearing: an inflated run does not just look odd, it sets the adapter ranges. And the plan's own head line is stale by two commits, which is the exact failure mode the plan exists to fix.

## U2 · 🟢 mobilize

Reviewed by sdlc-verifier on 2026-09-18 on `plan/records-refresh` @ `2c82ff2` (amendment for U2 under owner rulings Q7 and Q8). Scope is U2-1 to U2-8 only; U1 and P1 to P5 stand as graded above.
All 8 criteria are 🟢 checkable: 🟢 8 · 🔴 0. Nothing goes back to the planner.

Method: I ran all 8 stated commands verbatim at `2c82ff2`, with table-escaped pipes removed. All 8 reproduced the planner's cfd0ff4 pre-state exactly. Then I planted the post-state shapes in a throwaway `--shared` clone, which showed that every command can print its failing value and also the expected value.

| # | Checkable | The check I would run, and what it printed |
|---|---|---|
| U2-1 | 🟢 | the three-part section command. At `2c82ff2`: `0`, nothing, `5`. In the clone, after I appended a `## 8. Doc drift` it printed `1`. After I also appended `## 9. Notes`, it printed `NOT LAST`. `6` is the ceiling while §1 keeps its name, and the plan records that substitution |
| U2-2 | 🟢 | the five-line sha command. At `2c82ff2`: `841e185`, `7faf3aa`, then three empty lines. The baseline `ref` is still `7faf3aa` because U1 is not merged. So line 2 turns green only after U1 lands, which matches U2 step 1's gate. Lines 3 to 5 extract the sha from fixed phrases, so a wrong sha prints a different line |
| U2-3 | 🟢 | I extracted the `sh doc-drift` fence (exactly one in the plan, 27 lines) and ran it. At `2c82ff2`: `rows 0 drifted 0 holds 0 undetermined 0 bad 0`, `exit 1`, seeds `0`. I planted six rows in the clone: a quote one line off (`QUOTE DD1 ... CLAUDE.md:22`), an untracked `src/engine/nope.mjs:3` (`PATH DD3`), a changed character (`QUOTE DD4`) and a blank state (`STATE DD5`). The correct row, `README.md:11` "derives a **53-role semantic layer**", printed nothing. Totals were `bad 4`, `exit 1`. My plant differs from the planner's six rows, so its counts differ; each failure class fired as described |
| U2-4 | 🟢 | the fence-vs-file `diff`. At `2c82ff2`, `diff` errors on the missing file. In the clone, the extracted copy printed `same`. After I changed one token (`bad = 0` to `bad = 1`) it printed a hunk. The awk pattern does not collide with U1-2's bare `sh` fence (one of each) |
| U2-5 | 🟢 | the five-part note, deletions and pointer command. At `2c82ff2`: `0 0 0 0 0`. Values 1, 2 and 5 are presence counts and move on the unit. Values 3 and 4 are zero-deletion guards that read `0` either way, which is their job |
| U2-6 | 🟢 | the three-part handoff command. At `2c82ff2` the file is absent: `0`, `0`, grep warns. I planted 18 K rows with K7's last cell blank and one `measured at 841e185` line: `18`, `1`, `1` |
| U2-7 | 🟢 | the five-part pass 5 command. At `2c82ff2`: `0 0 0 0 0`. I planted pass 5 with 18 K rows, K9's emoji replaced and K3's control cell blanked: `1`, `18`, `1`, `1`, `1`. Each defect moved its own line |
| U2-8 | 🟢 | C31's block with the recorded substitutions. At `2c82ff2`: `5`, `18 18`, `0`, `1`, `17 18`, the planner's figures. Line 3 is scoped to pass 5, so it is `0` now and must reach `18`. The mutation, moved to the K1 row, bites (`17`) |

Three notes, none blocking.
- U2-3's seed count greps the whole §8 block. A seed phrase that appears only in a code or state cell would count, so whether each seed has its own row is a reading check for the U2 verifier.
- U2-6 and U2-7 expect exactly `1` for the `measured at` or `at <sha>` line. A handoff or pass 5 that repeats the sha on a second line reads `2`. Builder and verifier should write the sha phrase once.
- U2-2 depends on U1's merge. A U2 cut before U1 fails line 2, not only step 1.

## U4 · 🟢 mobilize

Reviewed by sdlc-verifier on 2026-09-19 on `plan/records-refresh` @ `265b870` (the U4 amendment plus its Q11 cite fix). Scope is U4-1 to U4-7 only; U1, U2 and P1 to P5 stand as graded above.
All 7 criteria are 🟢 checkable: 🟢 7 · 🔴 0. Nothing goes back to the planner.

Method: `.sdlc/adapter.md` and `.sdlc/debt.md` are byte-identical at `265b870` and at the plan's measurement point `114061a2` (the only files that moved between them are the plan, the board and the approval doc), so every "at 114061a2" figure is testable at the current head. I ran all 7 commands verbatim at that head with the table-escaped pipes removed, then applied the §Texts edits by script in a throwaway `git clone -q --shared` copy under `$CLAUDE_JOB_DIR/tmp` and ran all 7 again plus every stated control. No command touched this worktree or any other.

Preconditions I confirmed first, because rows 1 and 2 read the plugin's code rather than the tree:
- `git config core.hooksPath` resolves through the plugin cache into the plugin repo, so the plan's `PR` derivation lands on it and `unit/df-ids` is present there.
- The branch copy of `board.py` answers `ids --help` with exit 0; the installed copy and the plugin's `main` copy both exit 2. The plan's `B` fallback chain therefore picks the branch copy today, as written.
- `grep -c 'board.py" ids' "$(git config core.hooksPath)/pre-commit"` prints `0`. The check is not live yet, so the plan's §Dependency exception is the branch that fires: U4 runs first from `plan/records-refresh` as it stands, and `GH` is the extracted `githooks/`, not the installed one.

| # | Checkable | The check I would run, and what it printed |
|---|---|---|
| 1 | 🟢 | `python3 "$B" ids .sdlc; echo "exit $?"`. At `265b870`: the thirteen lines from `adapter.md:103: C1 defined in a adapter file; owner kind ['plan']` to `adapter.md:115: C13 ...`, `exit 1`, the planner's control exactly. On the simulated tree: nothing, `exit 0`. With row X13 left as `C13`: one line, `exit 1`. A mistyped directory prints `board.py: no such directory: <path>` on stderr and exits 2, so a typo cannot read as green |
| 2 | 🟢 | the clone, `core.hooksPath`, probe and commit. Against the base commit: the same thirteen lines, then `board.py ids refused an id defined outside its owning file (plan-rules Id namespaces); rename it or move the definition`, `commit 1`. On the simulated tree: `commit 0`, no refusal line. The file discrimination holds in both directions at the pre-state: an `echo >> .sdlc/roadmap.md` commit exits 0, and a non-ff `git merge` staging the unrenamed `adapter.md` exits 1 through `pre-merge-commit`. So the row separates "U4 missing" from "U4 done" and cannot pass by staging nothing |
| 3 | 🟢 | the two `diff` commands with the reverse map. On the simulated tree: nothing, then `0`. Controls: a reworded X7 resolution cell prints its `<` and `>` pair and `1`; a row renamed `X14` prints its pair, because the map covers 1 to 13 only. `grep -cP` for an X token in `adapter.md` at `265b870` is `0`, so the map folds nothing that predates U4 and the row cannot pass vacuously |
| 4 | 🟢 | the six-part §4 command. At `265b870`: an empty line, `13`, `18`, `1`, `0`, `0`. On the simulated tree: `X1,X2,X3,X4,X5,X6,X7,X8,X9,X10,X11,X12,X13`, `0`, `0`, `0`, `24`, `1`. Both are the planner's figures to the digit. The `24` is 13 rows plus 10 cite lines plus the amendment line, and I confirmed it is a line count, not a token count: line 89 carries two tokens and contributes one. Control: line 144 left as `conflict C9` prints `1` on the third value and `23` on the fifth, as stated. I also confirmed why the survey filter alone is not enough: lines 60 and 89 both carry a conflict cite and an emoji or the word survey on the same line, which is why the fourth value exists |
| 5 | 🟢 | the six-part sweep. At `265b870` the sweep prints `13` on exactly the lines the plan names (`adapter.md` 49, 60, 65, 89, 91, 130, 144, 179, 180, 181 and `debt.md` 59 to 61), then `0`, `3`, `0`, `0`, `0`. On the simulated tree: `0`, `3`, `0`, `0`, `0`, `0`. Controls: a planted `adapter C9 says so` line in `.claude/CLAUDE.md` moves the first value to `1` and the fifth to `1`; rewriting one cite in `.sdlc/questions/adopt-a5-conflicts.md` makes the last value non-zero. I also checked each of the eleven old cite strings occurs exactly once in `adapter.md`, so the ten substitutions are unambiguous |
| 6 | 🟢 | the four-part scope command. On the simulated tree, with a stub handoff in place: `.sdlc/adapter.md,.sdlc/debt.md,.sdlc/handoffs/records-refresh-U4.md`, then `1` and `1`, then `branding: clean (457 files scanned)` and `exit 0`. The em-dash pair is `1` and `1` because adapter line 130 already carries one and moves with its rename, exactly as the plan says. The path list is the wall and the second count is the bite |
| 7 | 🟢 | U1-6's first command, U3-7's first command, U1-9's last command, P5's command. On the simulated tree: `0`, `0`, `0`, `0`. Both stated controls fire: U1-6's pre-amendment form prints `23` on the same tree, which is precisely what the amended form maps away, and a reworded §4 cell still prints `1` under the amended form, so the map does not blunt U1-6's own measured control |

The plan's claim about which files `board.py ids` would refuse today: 🟢 correct, and correct for the reason it gives. Run against the whole `.sdlc/` tree at `265b870`, the only refused file is `adapter.md`, thirteen lines, rows C1 to C13 at lines 103 to 115. I checked the mechanism rather than only the outcome: the sole table in a defining-kind file whose header's first cell is `#`, `Id` or `ID` is `adapter.md` line 101; `debt.md`, `architecture.md` and `adapter.md` §6 head theirs with lower-case `id`, and `survey.md` and `baseline.md` head theirs with words, so the scanner never enters a defining state there. This plan and the four `adopt-hygiene-*` sidecars are plan-kind files, where `U`, `C` and `A` are owned anyway, and their criterion rows are bare numbers or shapes like `U1-6` that the row regex does not match. `verdicts/`, `handoffs/`, `questions/`, `tickets/` and `board.md` are never read, so appending this section cannot make the check bite on itself.

Five notes for the Orchestrator, none blocking.
- Rows 3, 4 and 5 need a `grep` that accepts `-P`. This machine's `/usr/bin/grep` does not: it prints a usage block on stderr and exits 2, and a `-c` form then writes nothing to stdout, which reads like a quiet zero. They reproduced here because an agent's shell resolves `grep` to the harness shim, which has PCRE. A `bash -c` wrapper does not inherit that shim, so rows 3 to 5 must be run directly in the agent shell, unlike row 6, which is explicitly wrapped. `git grep -P` in row 5 is independent of this and works either way.
- Rows 3 and 4 hardcode `2026-09` in the amendment pattern. The §Texts block writes the date as a placeholder, so a U4 that builds in October passes §Texts and fails rows 3 and 4 on the date alone. Worth pinning the date in step 2 or widening the pattern.
- The plan's §The form paragraph states that no `X1` to `X13` token exists in this plan, measured `0`. That was true at `114061a2` and is no longer: the U4 amendment that made the claim also wrote 32 of them into the plan. No criterion reads that number, so nothing is graded wrong, but the plan exists to fight exactly this kind of stale line.
- Row 2's `GH` comes from `git -C "$PR" archive unit/df-ids plugins/sdlc | tar -x -C "$F/plug"`. `$F/plug` has to exist before `tar -x -C` runs, and the archive has to carry `plugins/sdlc` whole rather than `githooks` alone, because the hook resolves `scripts/board.py` relative to its own real path. Both are implied by the plan; neither is spelled out in step form.
- Step 3's real commit stages the handoff as well as the two records, so the branch hook's block 2 (`verdict.py check` on staged `.sdlc/handoffs/*.md`) also runs on it. That block is outside U4's criteria and the plan does not mention it, so a handoff that fails it would refuse the commit for a reason no row predicts.
