# SITE-runbook · Ultimate Tokens: Monetization go-live runbook · 2026-07-12 · none stated
| Field | Value |
|---|---|
| Decision | The soft launch (built, nothing withheld) becomes a hard launch by flipping `TIERS_ENFORCED = true` in `src/engine/flags.js` in a single merge of a prepared draft PR, after: step 0 wires the Pro gates in code (`maxSets`, `proExport`, `advancedTreatments`, `hostedMcp`) since the flag resolver is complete but no feature surface consumes `flagOf()` yet; step 1 configures the Lemon Squeezy dashboard (enable license keys on Pro and Studio, Pro activation limit high e.g. 25 per the 2026-07-02 licensing decision, Studio activation limit 5 with +1 per additional seat, confirm the variant belongs to store `420293`); step 2 confirms CORS from the deployed origin's browser console against `api.lemonsqueezy.com/v1/licenses/validate`, with a same-origin proxy pointed at by `LEMON_LICENSE_API` in `src/main.ts` as the fallback if blocked. Rollback is flipping `TIERS_ENFORCED` back to `false` and redeploying. |
| Forces | The gates can be wired and shipped now with zero user impact because `flagOf()` returns unlocked values while `TIERS_ENFORCED` is `false`, license validation only works if license keys are enabled per product and activation limits match the seat model, the app validates/activates from the browser directly against Lemon Squeezy so missing CORS breaks activation for everyone, `curl` proves the endpoint but not CORS, the Figma plugin stays free/offline regardless (license UI hidden when `inFigma`), nothing changes stored data |
| Rejected | none named |
| Consequences | Post-flip: free users capped at 2 brand kits with `proExport`/`advancedTreatments`/`hostedMcp` off, Pro/Studio unlocked per seat, lapsed subscription auto-downgrades to Free. Decisions still needed: which export formats are Free vs Pro, which treatments count as "advanced"; `hostedMcp` is blocked because no hosted MCP server is deployed. Device-seats remain the shipped model (#131) until Phase 2's named-email seats. Post-flip verification checklist covers a fresh browser at Free limits, key paste in Settings → Account → Validate, seat-limit message at N+1 devices, Remove freeing the seat, and the Figma plugin unaffected. Engine seam: `resolveFlags`/`resolveTier`/`entitlementActive` in `src/engine/flags.js`. Issue named: #131; go-live referenced as #158 in `licensing-identity-spec.md`. |
| Supersedes / amended by | supersedes none stated; amended by Amendment (2026-09-16) (step 0's premise is now stale: `maxSets`, `proExport`, `advancedTreatments`, and `describePalette` are wired `flagOf()` consumers; `hostedMcp` remains the only unwired flag, since no hosted MCP server is deployed) and by activation-limit guidance updated per `licensing-identity-spec.md`, decided 2026-07-02 |
| Source | docs/site/go-live-runbook.md |
