# Verdict adopt-hygiene U10 · 🟢

Graded by sdlc-verifier on 2026-09-17 (evidence run: adopt-hygiene-U10-verifier-l3-p1, grade l3, Fable 5.1; the branch-sweep row and the regrade re-measured by the Verifier seat at plan 1d48971). Branch `unit/hygiene-U10` @ a6e32b2, merge-base 6c83520, graded against the `sdlc/adopt` plan copy at 6636b74 (the branch copy differs only in a 162-vs-161 prose count, which the head proves wrong, and the last revision row). Worktree left at status 0; controls at 279ae0f and on a mutable scratch copy of head, both removed; root checkout untouched. The handoff and `.sdlc/verdicts/adopt-hygiene-U10-review.md` were not used as evidence.
Tally: 9 rows (6 criteria + 3 gap rows). 🟢 9 · 🟡 0 · 🔴 0 after the 1d48971 plan revision. The unit's own files are correct: the card and index amendments are complete, and the `Source` range check holds across all 24 ADR cards. The one open gap and both concerns were plan text or check text, and all three are now measured true by me.

| # | Criterion | State | Evidence | Negative control |
|---|---|---|---|---|
| U10-1 | every card whose source carries U1's amendment names it, index cell too, no "none stated" contradiction | 🟢 | `card-amendment-check.sh`: `stale total: 0`. Independent sweep of all 38 cards against every dated amendment in their sources: 0 omissions; the source carries exactly 6 dated amendments and all 6 cells name them; `ADR-016` and `SITE-runbook` now agree with `decisions.md:33` and `:50` | at 279ae0f the same sweep reports the six omissions the pre-land record named |
| U10-2 | the check is not vacuous: card set derived from Source rows, covers a new amendment anywhere | 🟢 | at 1d48971 the check reads the lineage cell only (`^\| (Supersedes\|Amended\|Lineage)`), so a date elsewhere in the card no longer satisfies it. Run by the Verifier seat: `stale total: 0` in the U10 worktree, `12` on `sdlc/adopt` where the unit is not yet merged, `12` at 279ae0f. The card set is derived from Source rows (38 of 38 resolve); the independent sweep of all 38 cards found 0 omissions | `12` at both control commits, naming the same cards the pre-land record listed; a planted amendment under `## ADR-011` prints its two stale lines |
| U10-3 | no other live record contradicts `decisions.md` on the six records | 🟢 | the six `index.md` cells each carry the amendment with content; `none` count 0 | at 279ae0f: `2` |
| U10-4 | the U8 and U9 sweeps stay green | 🟢 | all nine `u8check` lines at their §U8 expected values with live `gh`; wording check `0, 0` exit 0; U9-1 prints exactly its three named misses; U9-3 silent, `npm ci` count 3 | at 61a3f90: `disagreeing: 20` with 20 `no-note` lines, and the other lines at their pre-fix values |
| U10-5 | gates green, tree clean, branding clean | 🟢 | `✓ all 44 test files passed`; status 0; `branding: clean (435 files scanned)`; P3 wall 0 (2 without the branding-gate exclusion); the diff against `sdlc/adopt` touches only `.sdlc/` files | role-table plant at 279ae0f: 3 FAIL lines; restored, green, tree clean |
| U10-6 | every ADR card's `Source` line range matches its section, start and end | 🟢 | `card-source-range-check.sh`: `range mismatches: 0`; independent recompute over all 24 ADR cards: 0 disagreements. ADR-010 cites 145-161 and 161 is the last non-blank line, so the `sdlc/adopt` prose is right and the branch copy's 162 is wrong | at 279ae0f: `range mismatches: 26` (12 starts, 14 ends); planted end and start shifts each print their exact mismatch line |
| G1 | pre-land gap: the quoted `/Users/` loop in the plan's U7 row | 🟢 | the loop carries its `-- .sdlc` scope again and prints nothing at head | without the scope it prints `missing .claude/skills/shipping-changes/references/best-practices.md` |
| G2 | pre-land gap: the Landing branch sweep | 🟢 | at 1d48971 the sentence no longer names a branch list or a count: the glob removes `sdlc/adopt` and every `unit/hygiene-*` branch however many exist, and U3-4 is met against the pre-plan 28 plus whatever other work opened, with the close-out recording the number it measures. Read at head by the Verifier seat, and the counts it cites (40 local, 10 unit branches, `feat/646-poster-strip-chroma` belonging to other work) match my own `git branch` runs | at 279ae0f the same sentence named nine branches and asserted 28, which the head refuted |
| G3 | pre-land gap: the six cards and index cells | 🟢 | rows U10-1 and U10-3 | at 279ae0f both fail |

## Notes
1. All three items from the first grade are closed at plan 1d48971, each re-measured by me: the Landing sweep sentence, the lineage-cell-only check, and criterion 6 now sitting in the §U10 table.
2. The check still keys on the 2026-09-16 date rather than on any dated amendment; it is a landing-time sweep, not a standing gate, and the independent 38-card sweep is what certifies the fact here.
3. U10 is the last unit: the final pre-land round runs `.sdlc/checks/*` as part of its record.
