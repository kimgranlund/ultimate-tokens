# Verdict U3 · 🟢
verdict: 🟢

Unit `unit/pif-u3-envelope` @ `78a9018f` (re-verified over `da351505`, first verdict below re-checked at the new sha) (merge-base with `plan/preset-intent-fidelity` @ `2be8f742`: `3c9630bf`), verified in a detached scratch worktree, removed afterwards. Every measurement below is my own run on the rendered path (`rampChromaOf` + `paletteStops`, CAM16 chroma from the emitted pixel), never raw `palette.chroma`. All 12 rows 🟢 at `78a9018f`. Re-verify scope at the new sha: `78a9018f` over `da351505` is 13 files, 168 insertions / 168 deletions, every removed line carries an em dash and no removed line exists verbatim on the base `3c9630bf` (0 of 168), so it is character replacements on the unit's own lines only, no logic change, main's pre-existing em dashes untouched (the two extra files beyond the 11 are the regenerated bundles `figma/plugin/ui.html` and `src/ui/describe-mcp-assets.js`). Gates re-run at `78a9018f`: `node test/engine/tonal.mjs` PASS (all gates incl. `chroma-envelope`); `--envelope` reading (a) identical to the first run (only perceptual\|300 p90 93.7% and peak\|700 p90 96.8% FAIL, 0 above 100% in even/peak, 0 cusp-run violations); `npm test` `✓ all 47 test files passed`, tree clean after; `branding: clean (452 files scanned)`. Rows 1-11 therefore hold unchanged.

Next action for the Orchestrator: none on U3; carry the two named yields and the peak-cap trade figures into U4's blast-radius table per revision 24.

| # | Criterion | State | Evidence | Negative control |
|---|---|---|---|---|
| 1 | C6 median/p90 table, reading (a), all modes and stops | 🟢 | `node scripts/report-preset-fidelity.mjs --envelope` at head: every median cell passes in all three modes; p90 passes everywhere except the two named yields. perceptual: 100 11.1/19.7, 300 68.9/**93.7**, 700 59.6/68.5, 900 19.2/30.3. peak: 100 6.6/13.5, 300 46.4/58.0, 700 65.5/**96.8**, 900 19.0/28.8. even: 100 8.8/19.8, 300 40.3/51.7, 700 36.5/50.6, 900 16.3/27.1. Only perceptual\|300 p90 and peak\|700 p90 read FAIL, exactly the two cells revision 24 / ruling 1 names, at exactly the recorded 93.7% and 96.8%. `env(500) = 1 sweep: PASS`. Two disclosed caveats, neither a criterion miss: the report's n is 2,920 (plan text says 2,836; the unit records the ~3% discrepancy as unresolved in `pif-u3.md`), and the report's alternate reading (b) (the bare multiplier) fails 8 perceptual/peak cells, recorded in `pif-u3-retune.md`; the ruled bar is reading (a). | Tightened the stop-700 p90 bar 90 → 60 in a scratch copy: perceptual\|700 (68.5%) and peak\|700 (96.8%) flipped to FAIL, even\|700 (50.6%) stayed OK, so the report discriminates per cell and the OK cells are real readings, not vacuous. Reverted. |
| 2 | C6 "0 above 100%" in even and peak, generated palettes | 🟢 | Report: `above 100% of stop 500: 0 OK` in peak and even, with `16 additional instance(s) from the named Adia carve-out`. Gate `chroma-envelope` (C6 iii) green in `node test/engine/tonal.mjs`. | `--envelope --damp-amp 55`: peak `above 100% of stop 500: 2391 FAIL`, perceptual `rule violations: 35 FAIL`, exit 1. |
| 3 | Adia `dampAmp > 0` carve-out, named and counted | 🟢 | `ADIA_CARVEOUT = new Set(["Adia · The product's own design system"])` in `test/engine/tonal.mjs`; the gate also reds if the carve-out produces zero instances (staleness check). Report shows exactly 16 carved-out instances per mode. | Renamed the set's one entry to `Adia · renamed`: `FAIL chroma-envelope (C6 iii) even: 1 above-100% instance(s) from an UNLISTED preset ... Adia · The product's own design system/Neutral`, exit 1. Reverted. |
| 4 | C6 perceptual cusp-run exemption, one contiguous run, bound 189.3005% | 🟢 | Gate `CUSP_RUN_BOUND = 1.893005`, second-run and past-bound clauses both present and green. Independent re-derivation (my own script over 3,764 generated non-Adia perceptual ramps, CAM16 chroma from the emitted pixel): worst ratio 189.3005% at cuisine "Sushi & sashimi · the cypress counter"/primary-muted, stop 650; 0 palettes with more than one above-anchor run. Matches the frozen bound and witness exactly. | Bound lowered to 1.89: `FAIL chroma-envelope (C6 iii-b) perceptual: 1 palette(s) with a stop past the frozen CUSP_RUN_BOUND 189.0000% ... Sushi & sashimi · the cypress counter/primary-muted (189.30%)`, exit 1. (A first try at 1.893004 stayed green because the gate carries a 1e-6 tolerance; that is consistent with the bound being frozen at the measured value.) Reverted. |
| 5 | C7 zero L* upticks, full corpus, both stop sets, three modes | 🟢 | `chroma-envelope` (C6 i) green: 0 / 0 / 0. Independent scan (my script, 3,780 palettes × 3 modes × 19-stop and 25-stop): `{ perceptual: 0, peak: 0, even: 0 }`, duplicate hexes 0. | Re-keyed `chromaEnvelope` on the nominal stop instead of `liftStop()` (the wrong path the plan names): `FAIL chroma-envelope (C6 i) peak: 15 rise(s), e.g. 19-stop hue 110 chroma 100.00 skew 0 lift 22: stop 350->400 (98.1600 -> 98.1641)`, plus `skew-lift-okhsl`, `damping-curve`, `intensity-legacy` reds, exit 1. Reverted. |
| 6 | C7 one shared envelope (greps) and damping never perturbs tone | 🟢 | `grep -c "export function chromaEnvelope("` = 1; `grep -c "chromaEnvelope("` = 3 (definition at `src/engine/tonal.js:247`, calls at :382 and :522); `grep -c "1 + ((controls.dampAmp"` = 0. `damping-curve` gate green. | Appended one stray `// chromaEnvelope(` comment: `FAIL chroma-envelope (C7) chromaEnvelope must appear exactly 3 times total ... found 4`, exit 1. Reverted. |
| 7 | 53 even-mode 3-4 C dips listed by name, count gate ≤ 53 (ruling 2) | 🟢 | `EVEN_DIP_BASELINE` in `test/engine/tonal.mjs` holds exactly 53 entries (51 at stop 350, 2 at stop 650); `.sdlc/questions/pif-u3-yield.md` lists the same 53 by name. The gate reds on any unlisted dip (count can never exceed 53) and also on any listed dip not observed, so it is an exact-match gate, stricter than ≤ 53; a fixed dip forces the list to tighten, which the FAIL text says. Perceptual has no baseline (any dip reds); peak keeps its separate 6-entry `DIP_BASELINE`. | Removed "Katsura Imperial Villa · 17th c · Kyoto\|primary\|350": `FAIL (iv dip gate) even: 1 dip instance(s) beyond the cited baseline, e.g. Katsura ...\|primary\|350`. Added a bogus entry: `FAIL (iv dip gate) even: 1 of the 54 cited baseline dips were not observed this run (Bogus preset\|primary\|350)`. Both exit 1. Reverted. |
| 8 | C8 AA floors incl. Neutral dark 4.5 re-pin; contrastLint 0/0/0; corpus sweep | 🟢 | `node test/engine/semantic.mjs`: `role-contrast` pass. FLOORS diff base → head: the only floor lowered is perceptual Neutral dark 4.9 → 4.5 (measured 4.5327), the disclosed re-pin; Neutral light 5.8 → 5.9, even Neutral/Success/Warning dark raised; all other 92 floors unchanged. `contrastLint(brandKit({...defaultDocument(), toneMode}))`: 0 / 0 / 0 entries in perceptual / peak / even. `npm run gate:corpus-contrast`: 7,560 cells, 0 below 4.5, worst 4.503 (Adia data-3 light); `test/engine/curated-contrast.mjs` is untouched by the unit (0 diff lines), so no new carve-out row. | Raised perceptual Neutral dark floor 4.5 → 4.6: `FAIL role-contrast — perceptual Neutral DARK: accent #717C98 on #101113 = 4.53:1, below its pinned floor 4.6:1`, exit 1. Reverted. |
| 9 | Peak-cap trade figures recorded honestly (ruling 3, not a criterion) | 🟢 | `pif-u3.md` :853-857 and :878-887 and `pif-u3-yield.md` carry dips 221 → 6, fallback rate 93.5%, tone drift 34.5% of capped stops > 0.01 L* (max 0.1009), hue residual max 24.62°, and an explicit withdrawal of "better in every other respect". `src/engine/tonal.js:675` now says the fallback fires on most capped stops, "not rarely". Grep for "better in every" finds only the two withdrawal sentences. | n/a (prose audit; the only "better in every" hits are the retractions themselves). |
| 10 | `npm test` green, tree clean after | 🟢 | `npm test` at head: `✓ all 47 test files passed`, exit 0, `git status --short` empty afterwards. (47, not C1's 48: `test/engine/anchor.mjs` is U1's registration and is not on this unit branch; C1 is a plan-level criterion, not U3's.) | Covered by rows 5-8: each scratch edit turned the same runner's gates red. |
| 11 | `test/repo/branding.mjs` clean | 🟢 | `branding: clean (452 files scanned)`, and it passes inside `npm test`. | Appended a retired-brand support email line to `scripts/report-preset-fidelity.mjs`: `✗ ... contains a retired-brand URL or email`, `FAIL: 1 branding violation(s)`. Reverted. |
| 12 | No em dash in files this unit authored | 🟢 | At `78a9018f`: em dashes on the unit's added lines over `git diff 3c9630bf..78a9018f` = 0 in every file (was 12 in the new `scripts/report-preset-fidelity.mjs`, 53 in `src/engine/tonal.js`, 78 in `test/engine/tonal.mjs`, 1-8 in eight others at `da351505`). The five `.sdlc` records stay at 0. Base-line em-dash removals are the same 12 at both shas, i.e. only lines the unit had already rewritten; `src/engine/tonal.js` file total 61 → 56 and `test/engine/tonal.mjs` 97 → 93 are those already-rewritten base lines, not new touches of main's text. | The check is a literal grep for the character on `+` lines; at `da351505` the same grep read the non-zero counts above. |

Runs recorded in the scratchpad: `envelope-head.txt`, `npmtest-head.txt`, `nc-out.txt`, `nc2-out.txt`, `cusp.mjs` (independent derivation), `lint.mjs`.
