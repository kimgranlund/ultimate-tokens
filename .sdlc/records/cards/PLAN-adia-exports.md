# PLAN-adia-exports · PLAN: Publish sha256-pinned derived OKLCH + Radix export artifacts for the Adia brand document · 2026-09-13 (frontmatter date; brief said 2026-09-12) · active
| Field | Value |
|---|---|
| Decision | Seven rulings: R-1 scope is OKLCH + Radix only, Panda is NOT added; R-2 both artifacts live at `docs/reference/data/adia-oklch-export.css` and `docs/reference/data/adia-radix-export.mjs` with tags `adia-oklch-export@1.0.0` / `adia-radix-export@1.0.0` (basename == tag family), each carrying a provenance comment block AFTER the exporter's own `/* ultimate-tokens export schema N */` line 1, with a patch/minor/major bump policy and independent cadence per file; R-3 the pipeline is `hydrate(preset)` then `projectView(doc).exports` (the drawer's own bytes, never bare `exportRadix`, because Adia's `geometry: { ramp: "linear4" }` adds a `tokens.radii` block only via `projectView`), body hashes measured at `770297b` (`95235b6a…` OKLCH, `2f03074d…` Radix); R-4 a repeatable generator `scripts/gen-adia-derived-exports.mjs` wired as `gen:adia-exports` into both `test` and `build` chains immediately after `gen:categories`, failing loudly on a missing preset, the no-driver sentinel, or any of Adia's 16 slugs colliding with `exportRadix`'s 7 reserved alias keys; R-5 annotated tags on the squash-merge commit; R-6 no ADR and no LLD owed for this ticket; R-7 assert at two grains (CI drift gate plus whole-file sha256 published in the PR body and on #618) and a new `test/engine/adia-derived-exports.mjs` with a one-byte-mutation negative control. |
| Forces | #616's per-artifact tag scheme (comments 5647614190, 5647640551), #618's committed Adia document (`770297b`, `adia-brand-document@1.0.0`), adia-ui-kit's #251 unreachable from this machine so #631's seed is the only in-reach consumer statement, CI's existing `git diff --exit-code` drift gate, `gen:*` convention (`gen-categories.mjs`), 4-decimal OKLCH rounding for Node-version drift (TKT-0011), the repo has no `"type": "module"` so the ESM artifact must be `.mjs`, tags never moved or deleted once pushed |
| Rejected | publishing a Panda artifact nobody asked for (deferring costs near zero; a third pinned surface must be bumped forever); a one-off script (no owner once the engine moves); bare `exportOKLCH`/`exportRadix` calls; hashing only the exporter body (the consumer cannot strip the header); an ADR now ("an ADR whose Context reads fine with no Decision above it is not an ADR"; trigger is a second derived-export request); hand-setting a `--md-sys-color-*` prefix (would mutate state or require re-tagging the document) |
| Consequences | Eight steps: generator → artifacts (stop if body hashes differ from R-3) → gate → repo gates (`npm test` + `npm run build`) → hashes → PR via `shipping-changes` → tags → record on #618 and #631's Findings. Checklist reads INCOMPLETE as written: every step's Status field says "todo" (though git log at `14c4260` "Generate derived Adia OKLCH + Radix export artifacts (#631)" shows the work landed; the plan's status fields were never flipped and the living-state note says R-3's hashes are to be replaced by committed whole-file hashes on completion). Two discovered-design-fork escalations posted to #631 on 2026-09-13: R-A the CSS emits the default `--c-*` prefix, not the ticket's `--md-sys-color-*`; R-F the ticket says eight palette names but the committed document has 16. R-E: the Radix preset `name` is `ultimate-tokens-radix-brand-kit` because `stateOf` does not carry `doc.name` (faithful output, not a defect). Issues named: #631 (intent), #616, #618, #452, #630 (adjacent, open), adia-ui-kit #251; TKT-0011. ADR ids cited: adia-ui-kit's ADR-0010 (its CI pin) only; explicitly rules no ADR of this repo's own. |
| Supersedes / amended by | none stated |
| Source | docs/plan/archive/plan-2026-09-adia-derived-export-artifacts.md |
