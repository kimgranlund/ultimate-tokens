---
kind: verdict
plan: preset-intent-fidelity
seat: verifier
pass: 2
passes: 1 at 30bfaeeb 🟡, 2 at 899173f5 🟢
pr: 737
ticket: "#681"
written: 2026-09-23
---

verdict: 🟢
sha: 899173f5d70d02326c95fea4ccb4aac6a31415e9

# Pre-land verdict, plan preset-intent-fidelity (#681), PR #737

Current finding: 🟢 at `899173f5`, in `## Pass 2` below, the block the landing check reads. Pass 1 graded `30bfaeeb` and is history: its review returned FIX-FIRST (F1 NaN on a black anchor, F2 fact sheet), and the owner folded F3 into fix unit U10 (R26, R27). F4 and F5 are tickets #739 and #738.

## Pass 1, at `30bfaeeb`

verdict: 🟡
sha: 30bfaeebaac8b1ed4d25da37dc30c1fbaa9a1e9b

Method. Read-only. Own shared clone `scratchpad/pif-clone` at `30bfaeeb`, no `node_modules`; controls in a second clone `scratchpad/pif-neg` restored with `git checkout .` between edits; origin/main controls in `scratchpad/pif-main-clone` at `b0fc3c65`. Diff base `origin/main...30bfaeeb` (merge-base `6bb7796e`), 125 paths. Load at start `13:24 load averages: 3.02 3.08 3.02`, at end `13:49 load averages: 6.48 4.87 4.26`. Sampling, where the plan allows it: the C8 full corpus sweep (`gate:corpus-contrast`) is read from CI, not re-run; `test/engine/tonal.mjs` (C7 gates, 100 s) is read from the `npm test` run, its greps re-run by hand; U7-9's consumer one-liner is sampled through the `key-anchor rendered path` gate line; U7-10's and C12's stubbed-handler controls are the shim's own built-in `rst3` control (`test/ui/headless-boot.mjs:3954`), not re-run by me. Every other row below was run here.

| # | Criterion | State | Evidence | Negative control |
|---|---|---|---|---|
| C1 | `npm test` green, no `node_modules`, tree clean, within the interim ceiling | 🟢 | clone at `30bfaeeb`, `ls: node_modules: No such file or directory`; start `13:24 load averages: 3.02`; `✓ all 49 test files passed`, exit `0`, `wall 325 s`, end `13:30 load averages: 4.31`; perl one-liner prints `49`; `git status --short \| wc -l` prints `0`. 325 s sits inside `ceiling 280 to 550 s` at start load 3.02 (under R13's 5) | `roleTable[0].light = 999` in the clone: `node test/engine/semantic.mjs` prints one FAIL line, `FAIL  refs-canonical` then `primary =  550/450 != canonical  999/450` (the glyph between the two halves is the runner's own), `semantic exit 1` (one FAIL line, as C1 says) |
| C2 | anchor identity, 3,380 exact | 🟢 | `node test/engine/anchor.mjs` exit `0`: `pass  anchor-identity: 3380 exact, 0 off`; `pass  key-anchor corpus: 3380 of 3380 ... 0/0 off` | in the clone, `src/engine/prime.mjs:142` nudged so the anchor branch's rgb flips the source hex's last digit: `FAIL  anchor-identity: 0 exact, 3380 off`, `C2 control exit 1` |
| C3 | ramp passes through the anchor at 500, three modes, in-window | 🟢 | `pass  anchor-ramp: 10110 exact, 0 off (in-window sources only, 3370 of 3380)` | the gate's own lift-40 control is live in the run (its FAIL text `C3 negative control DID NOT bite` never printed); the C2 control above moves every anchored rung's rgb and reds the sibling identity gate in the same file |
| C4 | non-anchored byte identity control | 🟡 carried | `grep -c identity-control scripts/report-preset-fidelity.mjs` prints `0`; no implementing command exists, K2 since `pif-u4.md`, no ticket; the anchor file's `pass  prime-identity-control: 0 exact, 3796 off (want 0 exact, 3796 off ...)` is the Q1-ruled replacement reading | the C2 control run prints the same `prime-identity-control` line unchanged while `anchor-identity` reds, so the two are independent readings |
| C5 | monotone, distinct, window allow-list 10, ladder window 21, dupe 3, notch | 🟡 | `pass  anchor-ramp monotone: 0 (expected 0, pixel L*, all three modes, both stop sets, all 3,380 sources including the window-clamped ones)`; `pass  anchor-ramp allow-list: 10 (expected 10)` with the ten r-tagged names; `pass  anchor-ladder dupe-allow-list: 3 (expected 3)`; `ladder-window allow-list: 21 (expected 21)`; `pass  anchor-ramp notch allow-list (ruled Q-C, named pending U4): 15 (expected 15)`. Attention: the gate also carries `pass  anchor-ramp gap (19-stop) allow-list: 72 (expected 72)` and `pass  anchor-ramp distinct (25-stop) allow-list: 16 (expected 16)`, ruled in `.sdlc/questions/pif-u2.md` (`gap-19 62 -> 69, distinct-25 12 -> 10`, re-frozen at U4), while the C5 bullet still reads as an unconditional 0.55 L* gap and no shared hex: `grep -c 'gap-19\|distinct-25\|lone-spike'` on the plan prints `0`. Text drift, no product defect | first `RAMP_WINDOW_ALLOW` name removed in the clone: `FAIL  anchor-ramp allow-list: 10 (expected 9)`, `C5 control exit 1` |
| C6 | chroma envelope | 🟡 carried open (owner Q2) | `node scripts/report-preset-fidelity.mjs --envelope` prints `envelope exit 1`; `env(500) = 1 sweep: PASS`; 14 median/p90 readings miss (perceptual `stop 300: median 94.0% (<=75 FAIL) / p90 146.2% (<=90 FAIL)`, peak `stop 300: median 84.0% (<=75 FAIL) / p90 137.7%`, even `stop 300: median 48.4% (<=75 OK) / p90 113.7% (<=90 FAIL)`); reading (b) `above 100% of stop 500: 0 OK` in all three modes; reading (a) `rule violations (second run or past-bound stop): 446 FAIL` (perceptual), `above 100% of stop 500: 2592 FAIL` (peak), `670 FAIL` (even), which is the anchored population `test/engine/tonal.mjs:1756` scopes out of the 0-of-384 bar and pins as the Q7 ratchet (`PEAK_VIOLATOR_PIN = 3119`, report `peak ... 3119/3764 violator(s), max 15.132599x`). `.sdlc/questions/preset-intent-fidelity-preland.md:9` Q2 `File a ticket for perceptual and peak, re-cite, accept C6 open (Recommended)`; `gh issue view 725`: `OPEN Chroma envelope misses its muted targets in perceptual and peak mode, and nothing gates the direction kind:bug,size:big` | `--damp-amp 55`: reading (b) `above 100% of stop 500: 2904 FAIL` in every mode (from `0 OK`), peak reading (a) `2893 FAIL` (from 2592), `damp55 exit 1` |
| C7 | one envelope, four call sites, no damp literal | 🟢 | `grep -c "export function chromaEnvelope("` prints `1`; `grep -c "chromaEnvelope("` prints `5` (`:388` definition, `:749`, `:830`, `:1036`, `:1092`); `grep -c "1 + ((controls.dampAmp"` prints `0`; `▶ engine/tonal.mjs` passed inside `npm test` | `git show bf2aaf6:src/engine/tonal.js \| grep -c "1 + ((controls.dampAmp"` prints `2` |
| C8 | contrast floors, contrastLint 0/0/0, corpus sweep | 🟢 | `node test/engine/semantic.mjs` exit `0`: `pass  role-contrast Q-B floor gate: 0 unlisted drops, 0 further erosion (41 cells named "pending U4")`; `contrastLint(brandKit(defaultDocument()))` per mode prints `perceptual 0`, `peak 0`, `even 0`; CI `corpus-contrast \| completed \| success` at `30bfaeeb` | `["Primary", 7.1, 4.9]` raised to `7.2` in the clone: `FAIL  role-contrast` then `perceptual Primary LIGHT: accent #1054B5 on #FFFFFF = 7.10:1, below its pinned floor 7.2:1`, `raised exit 1`; lowered to `7.0`: `lowered exit 0` |
| C9 | regenerated assets, role-table by 16 anchors only, binder and brands.json untouched, Adia versions | 🟢 | tree clean after `npm test` (`0`); `git diff origin/main 30bfaeeb -- docs/reference/data/role-table.json \| grep -c '^[-+]'` prints `18`; `defaults` all carry `anchor` (`true`), `roleTable` length `53`; `git diff --stat origin/main 30bfaeeb -- figma/binder/figma-semantic-binder/code.js docs/reference/colors/categories/brands.json \| wc -l` prints `0`; `scripts/gen-adia-derived-exports.mjs:65` `// 1.2.0 (#681, plan revision 28): the #681 engine ...` above `version: "1.2.0"` for both rows. `git tag --list 'adia-*'` prints three `@1.0.0` tags today; the two `@1.2.0` tags are cut on the squash commit per #631, a landing step, not a pre-land reading | `git show origin/main:docs/reference/data/role-table.json` with the same `every(has anchor)` prints `false` |
| C10 | records, non-zero on branch, zero on main | 🟢 | head/main counts: `^## ADR-026` `1`/`0` (line `730`, before `## Quick map` at `770`); `^## 1.64` `1`/`0`; `^\| \*\*Anchor\*\*` `1`/`0`; `^## 9\. Anchored palettes` `1`/`0`; `palette.anchor` in the color-math SKILL `3`/`0`; `anchored palette` in `acceptance-criteria.md` `3`/`0`, `quality-rubric.md` `1`/`0`; `node test/repo/branding.mjs` prints `branding: clean (639 files scanned)`; `repo/citations` passed inside `npm test` | the `origin/main` column above is the control: every needle reads `0` there |
| C11 | ladder symmetry, 26/22 by name, span report 364/363/0 | 🟢 | `node test/engine/prime.mjs` exit `0`: `symmetry (this branch): by-construction fails 0, measured exceed-3L* 0/464`; `symmetry (anchored corpus): 3380 palettes, by-construction exceptions 26 (max \|up-down\| 54.3116 L*), measured exceed-3L* 22 (max 54.2383 L*)`; `pass  symmetry corpus by-construction set == ORDER_ALLOW (test/engine/anchor.mjs): 26 vs 26`; `pass  ladder-span under 30 L*: 364 ... (expected 364), 363 ... (expected 363), 0 of 16 default-kit families (expected 0)`; `pass  gamut-ceiling` | the gate's own frozen-fixture legs, printed in the same run: `symmetry negative control (frozen pre-#681 fixture, same sweep): exceed-3L* 295/464, max asymmetry 52.0149 L*` and `... anchored corpus): measured exceed-3L* 1816/3380, max 52.4868 L*, pixel span under 30 L* 0` |
| C12 | Reset action, detach on hue/chroma, shim group | 🟢 | `node test/ui/headless-boot.mjs` prints `HEADLESS BOOT PASS` then `all Phase-3 interaction assertions hold` on its last line, `headless exit 0`; the file carries `37` `(rst` assertions and the hueSpace hide checks `(hs1)`, `(hs2)`, `(hs4)`, `(hs5)`, `(hs9)`; the file also passed inside `npm test` | sampled: the shim's own built-in stub control at `test/ui/headless-boot.mjs:3954` (`Stub resetAnchor with the OLD, PRE-FIX` body, `rst3` must red) runs inside the same green pass; not re-stubbed by me |
| U7-P1 | npm test, 49 files, clean | 🟢 | as C1: `✓ all 49 test files passed`, perl `49`, status `0` | as C1 |
| U7-P2 | branding clean | 🟢 | `branding: clean (639 files scanned)`, exit `0` | the gate scans `.sdlc/` (639 files, up from the planner's 551); U9's own brand-word control is recorded in `pif-u9.md` U9-P2, not re-run |
| U7-P3 | no em dash on U7's added lines | 🟢 | `git diff de1bafef 30bfaeeb -- <the ten U7 paths>` through the `-CSD` filter prints `0` | `printf '+ a \xe2\x80\x94 b\n'` through the same filter prints `1` |
| U7-P4 | U7 scope wall | 🟢 | every U7 path in the diff is on the declared list or named in `.sdlc/handoffs/pif-u7.md` (`component-inventory.md`, `02-sections-and-resolvers.md`, `test/ui/shell.mjs`, `test/ui/poster-strip.mjs` traced to commits `826dd090`, `d6700c49`, `f315eaf9`) | a path in neither list reds the global scope row below, which found ten such paths for the whole plan and traced each to a handoff |
| U7-P5 | landing checks green | 🟢 | `stale total: 0`; `ceiling-counts: clean`, `12` `ok` lines, both exit `0` | U9-2 to U9-4 and the U9-P5 control below each red one of the two scripts |
| U7-1 | corpus symmetry gate, 26 by name | 🟢 | `pass  symmetry corpus by-construction: 26 of 3380 (expected 26)` | the C5 drop-one-name control shows the allow-list shape reds on a count mismatch (`10 (expected 9)`); the fixture leg reads `1816/3380` |
| U7-2 | measured-pixel leg, 22 by name | 🟢 | `pass  symmetry corpus measured-pixel: 22 of 3380 (expected 22)` | `symmetry/ladder-span negative control (frozen pre-#681 fixture, anchored corpus): measured exceed-3L* 1816/3380, max 52.4868 L*` |
| U7-3 | mechanism, set equality with ORDER_ALLOW | 🟢 | `pass  symmetry corpus by-construction set == ORDER_ALLOW (test/engine/anchor.mjs): 26 vs 26`; `pass  anchor-ladder order-allow-list: 26 (expected 26)` | the set-equality assertion is the gate's own cross-check; a one-sided edit was proven to red by `pif-u7-review-2.md` F3 and is not re-run here |
| U7-4 | span-under-30 report | 🟢 | `364 ... 363 ... 0 of 16` line quoted under C11 | `pixel span under 30 L* 0` on the frozen fixture, same run |
| U7-5 | C11 text names 364 as the gate's own | 🟢 | `awk '/^- C11 /,/^- C12 /' ... \| grep -c 364` prints `1`; the bullet reads `364 is the gate's OWN measurement at U7's head; the earlier 373 ± 5 was the PROTOTYPE simulation's count` and cites Q3 (b) | the plan text at `0391f045` printed `0` per the planner; re-read, the bullet carries both labels |
| U7-6 | #725 cited, #701 kept | 🟢 | `grep -c '#725'` prints `2` in the ADR file and `2` in the CHANGELOG; `grep -c '#701'` prints `2` and `3`; `gh issue view 725` reads `OPEN` | `#725` reads `0` in both files on `origin/main` (C10 control column) |
| U7-7 | C6 carries the owner's acceptance | 🟢 | `awk '/^- C6 /,/^- C7 /' ... \| grep -c '#725'` prints `1`, in the sentence citing `.sdlc/questions/preset-intent-fidelity-preland.md` | same grep at `0391f045` printed `0` per the planner; the C6 sub-bullet is present at the head |
| U7-8 | key follows anchor, two producers | 🟢 | `pass  key-anchor corpus: 3380 of 3380 anchored palettes where the identity swatch equals the stored anchor, 3380 of 3380 where it equals primeSwatches(...)[3].hex, 0/0 off` | the gate's own one-digit anchor-corruption control (`negative control DID NOT bite` never printed); my C2 nudge of the rgb left the hex leg at 3380 equal because the prime hex is emitted verbatim from the stored string, confirming the two legs read different things |
| U7-9 | consumer surfaces agree | 🟢 sampled | `pass  key-anchor rendered path: 46 of 46 anchored palettes over projectView(hydrate(doc)), 4 subjects (the 16 default-kit families plus 3 named corpus presets), 0 off`; `src/ui/mcp-assets.js` is absent from the diff and the tree is clean after `npm test` | the U7 verdict's pre-fix reading `0 of 16` is derived, not re-run here |
| U7-10 | seedFromKey detaches | 🟢 sampled | `headless exit 0` with the `rst` groups present (`37` assertions) | built-in `rst3` stub control, see C12 |
| U7-11 | ceiling check assertions can fail | 🟢 | `ceiling-counts: clean`, `12` `ok` lines; `grep -n 'PARTITION\|above + inside'` hits only comment lines `69` and `111` describing the retired assertions | `284 s` to `285 s` in the adapter's series note: `ceiling-counts: 1 failure(s)`, exit `1` |
| U7-12 | schema version stated | 🟢 | document `CURRENT_SCHEMA_VERSION\` is 6`, code `CURRENT_SCHEMA_VERSION = 6` | constant bumped in the clone: code reads `CURRENT_SCHEMA_VERSION = 7`, document still `6`, the two differ |
| U7-13 | preview regenerated | 🟢 | `npm run gen:preview` exit `0`, then `git status --short \| wc -l` prints `0` | `docs/img/palette-preview.svg` is in the plan diff (`d6700c49` era), so the file did move once and is now stable |
| U7-14 | carried K items recorded | 🟢 | `grep -c identity-control` prints `0`; `grep -c "pending U4" test/engine/semantic.mjs` prints `38`; both named as carried in `pif-u7.md` | either reading changing would red this row; both match the planner's `0` and `38` |
| U9-P1 | npm test | 🟢 | as C1: `✓ all 49 test files passed`, exit `0`, `git status --short \| wc -l` prints `0` | as C1 |
| U9-P2 | branding | 🟢 | as U7-P2: `branding: clean (639 files scanned)`, exit `0` | as U7-P2 |
| U9-P3 | no em dash on U9's added lines, raw and stripped | 🟢 | over U9's declared files vs `33ab0942`: raw `2`, stripped `0`; the two raw hits are inside backtick spans in `pif-u9.md` and `pif-u9-review.md` quoting the runner's `✓ all 49 test files passed` line; `.sdlc/adapter.md`, `.sdlc/baseline.md`, `baseline-agrees-check.sh`, both U9 handoffs read `0` | `printf '+ a \xe2\x80\x94 b\n'` through the filter prints `1` |
| U9-P4 | U9 scope wall | 🟢 | U9's files vs `33ab0942`: the check script, adapter, baseline, the eleven `pif-u*` records (`added 1 deleted 0` each), the plan, `pif-u9*.md` handoffs and verdicts, `board.md` | `git diff --name-only 33ab0942 30bfaeeb -- src test scripts figma mcp docs` is non-empty only through the `origin/main` sync merge U9 carries, none from U9's own commits |
| U9-P5 | other landing checks | 🟢 | `for f in .sdlc/checks/*.sh` prints `exit 0` for all five; `ceiling-counts: clean` with `12` ok lines, equal to `pif-u7.md` U7-11 | `284 s` to `285 s`: `ceiling-counts: 1 failure(s)`, exit `1` |
| U9-1 | labelled ceiling line green | 🟢 | `ok    ceiling test: baseline 280 to 550 s, adapter 280 to 550 s`, `ok    time test: baseline 56 to 60 s, adapter 56 to 60 s`, `stale total: 0`, exit `0` | U9-2 to U9-4 below |
| U9-2 | control (a) | 🟢 | `grep -c 'ceiling 280 to 550 s' .sdlc/adapter.md` prints `1` at head, `0` at `33ab0942` | after the sed: `STALE ceiling test: baseline 280 to 550 s, adapter 280 to 560 s`, `ok    time test: ...`, `stale total: 1`, exit `1` |
| U9-3 | control (b) | 🟢 | label removed: `STALE ceiling test: baseline 280 to 550 s, adapter none`, `STALE time test: baseline 56 to 60 s, adapter 56 to 60 s, 280 to 550 s`, `stale total: 2`, exit `1` | this row is the control |
| U9-4 | control (c) | 🟢 | `STALE ceiling test: baseline 280 to 560 s, adapter 280 to 550 s`, `stale total: 1`, exit `1`; line deleted: `STALE ceiling test: baseline none, adapter 280 to 550 s`, exit `1` | this row is the control |
| U9-5 | #718 behaviour unchanged | 🟢 | (i) `STALE time test: baseline 56 to 60 s, adapter 56 to 60 s, 56 to 61 s` exit `1`; (ii) `STALE time test: baseline 56 to 60 s, adapter none` exit `1`; (iii) `ok    time test: baseline 56 to 60 s, adapter 56 to 60 s, 56 to 60 s` with `ok    ceiling test: ...`, exit `0`; `grep -cF 'matchAll('` prints `2` | this row is three controls |
| U9-6 | baseline pointer sentence | 🟢 | `#718 is ruled to give this script` reads `1` at `33ab0942`, `0` at head; `.sdlc/baseline.md:211` now reads `labelled figure to check against, per PR #729's out-of-scope note` | the `33ab0942` reading is the control |
| U9-7 | verdict-frontmatter check green | 🟢 | `verdicts 101 graded 54 grandfathered 47 bad 0`, exit `0`; grandfather list untouched | `pif-u2.md` line deleted: `MISSING pif-u2.md: no verdict: line`, `bad 1`, exit `1`; token `PASS`: `VALUE pif-u2.md: last verdict: PASS is not 🟢, 🟡 or 🔴`, exit `1` |
| U9-8 | eleven tokens derived from the records | 🟢 | my derivation before reading any handoff: `pif-u1` `Verdict \| **PASS** (2 🟡 concerns ... neither a code defect)` 🟢; `pif-u2` `# Verdict U2 · 🟢`; `pif-u3` `# Verdict U3 · 🟢`; `pif-u4` `# Verdict U4 pass 7 (main-merge delta) · 🟡`; `pif-u5` `# Verdict pif-U5 pass 4 · 🟢 at 6d0755b9` and `The unit is 🟢.`; `pif-u6` `**PASS** , with two 🟡 carry-forwards, neither fix-first` 🟢; `pif-u7` `🟢 **U7 is green.** 19 of 19 criteria 🟢`; `pif-u7-review-1` `**FIX-FIRST.** F1 blocks` 🟡; `pif-u7-review-2` `## F3, the set-difference assertion: 🟢 closed` 🟢; `pif-u8` `## Regrade at 96f7d99a` all 🟢; `pif-u8-review` `## Pass 2 · 🟢 PASS`. File tokens: `🟢 🟢 🟢 🟡 🟢 🟢 🟢 🟡 🟢 🟢 🟢`, eleven agree | a `🔴` token would pass the check and fail only this derivation, which is why the row is a derivation |
| U9-9 | one added line per record, no deletions | 🟢 | each of the eleven: `added 1 deleted 0` vs `33ab0942` | a second added or any deleted line reds the row; none found |
| G1 | #686, exportPanda independent of call order | 🟢 | own script `scratchpad/panda-order2.mjs`: cold process vs a process that first renders every preset at hue ± 0.0049 deg (same `toFixed(2)` bucket): head `n 343 cold-vs-poison differing presets 0`; the shipped gate: `determinism (cold process vs poisoned-before-any-render process, real-palette poison, hex comparison): 0/2000 palettes shifted hex by call order` | same script on `origin/main` (`b0fc3c65`): `n 343 cold-vs-poison differing presets 16`; `git show origin/main:src/engine/hct.js \| grep -c '#686'` prints `0` |
| G2 | #668, L* non-increasing at stop 800 on the witnesses | 🟢 | own script `scratchpad/uptick-668.mjs` over the eleven `ramp-monotone` witnesses on the rendered 25-stop ramp: head `witnesses 11: non-increasing 11, rose 0`; `anchor-ramp monotone: 0` over all 3,380 sources (C5); `ramp-monotone` passed inside `npm test` | same script on `origin/main`: `witnesses 11: non-increasing 0, rose 11`, e.g. `travel "San Telmo" secondary-muted lift -40 anchored=false: +0.5105 at 750->800`; `git show origin/main:test/engine/categories.mjs \| grep -c 'ramp-monotone'` prints `0` |
| G3 | owner ruling Q6, labelled ceiling figure | 🟢 | adapter `test` cell carries `ceiling 280 to 550 s` (`grep -c` `1`); `.sdlc/baseline.md:97` `Interim ceiling: **\`npm test\` is expected between 280 and 550 s ...`; all five `.sdlc/checks/*.sh` `exit 0`; `ceiling-counts: clean` exit `0` | U9-2, U9-3, U9-4 |
| G4 | X5, the eleven `verdict:` lines sit at line 2 rather than as closing lines naming a sha | 🟡 carried | `pif-u9.md` X5; ten files carry the line at `2:`, `pif-u5.md` at `10:`; owner moved it to #734 under R25: `gh issue view 734` reads `OPEN Backfill verdict: front matter on the 47 grandfathered verdict records (#723 follow-up)` | the machine read is unaffected (U9-7 `bad 0`); U9-7's controls prove the check reads the line |
| G5 | PR #737 head and CI | 🟢 | `gh pr view 737`: `"headRefOid":"30bfaeebaac8b1ed4d25da37dc30c1fbaa9a1e9b"`, `"baseRefName":"main"`, `"isDraft":true`, `"mergeable":"UNKNOWN"`, `"mergeStateStatus":"UNKNOWN"`; check-runs at the sha: `build-test \| completed \| success`, `panda-smoke \| completed \| success`, `corpus-contrast \| completed \| success` (`deploy \| completed \| skipped`), run `https://github.com/kimgranlund/ultimate-tokens/actions/runs/35915576626` | `build-test` read `in_progress \| null` at 13:24 and `completed \| success` by 13:44, so the reading is the finished state, not a cached one |
| G6 | `git merge-tree --write-tree origin/main 30bfaeeb` clean | 🔴 | `CONFLICT (content): Merge conflict in .sdlc/board.md`, exit `1`; `origin/main` is `b0fc3c65` (a board commit landed after the plan's sync merge-base `6bb7796e`); only `.sdlc/board.md` conflicts, no product file | `git merge-tree --write-tree 6bb7796e 30bfaeeb \| grep -c CONFLICT` prints `0` while the same against `origin/main` prints `1`, so the conflict is the one board commit main gained after the sync, not the plan's own content |
| G7 | scope: no private docs, no `node_modules`, changed files on the plan's lists | 🟡 | `grep -c 'claude/docs/other\|node_modules'` over the 125 paths prints `0`. Ten paths are on no unit's declared list but each is named in a unit handoff with its cause: `docs/lld/app-shell.md`, `docs/reference/references/component-inventory.md`, four `docs/reference/reviews/2026-08-20-reactivity/*.md` (citation repairs, `d6700c49`, `826dd090`), `test/mcp/png-swatch-board.mjs` and `test/ui/persist.mjs` (fixture re-measures, `0a2c6f2e`, `a4fc9467`), `test/engine/fixtures/radix-baseline.json` and `shadcn-baseline.css` (`pif-u4.md`, `pif-u2.md`); U7-P4 revision 32 opened this half for repairs the unit's own edits force | any path in neither the plan lists nor a handoff would red the row; `grep -l` over `.sdlc/handoffs/pif-*.md` finds every one of the ten |
| G8 | em dashes in the whole plan diff | 🟡 | over `6bb7796e..30bfaeeb` on hand-edited paths (generated bundles, categories, fixtures and `docs/img` excluded) the `-CSD` filter prints `648` added lines carrying the glyph, in code comments and gate strings (`test/engine/anchor.mjs`, `src/engine/tonal.js`, `src/ui/sections/color.js` among them). The plan scopes its no-em-dash rows to U7 and U9, both `0`; the standing rule says anywhere. Attention, not a plan criterion | `printf '+ a \xe2\x80\x94 b\n'` prints `1`; the same filter without `-CSD` prints `0` on that input, so the flagged reading is the non-vacuous one |

Counts: 53 rows, 45 🟢, 7 🟡 (C4, C5, C6, G4, G7, G8, and the overall), 1 🔴 (G6). Carried 🟡 from earlier passes: C4 (K2), C6 (owner Q2, #725 and #701), G4 (X5, #734). New this pass: C5's text drift on the gap and distinct allow-lists, G7's ten off-list paths, G8's glyph count. Red this pass: G6 only, a `.sdlc/board.md` sync conflict.

verdict: 🟡
sha: 30bfaeebaac8b1ed4d25da37dc30c1fbaa9a1e9b

## Pass 2, at `899173f5`

verdict: 🟢
sha: 899173f5d70d02326c95fea4ccb4aac6a31415e9

Next action, orchestrator: land. Nothing in the product, the gates or the plan criteria is red at `899173f5`; the merge-tree against `origin/main` (`1838e364`) is clean and CI is green at the head. The 🟡 rows are the carried, owner-ruled items (C4, C6, G4, G8, X5, R27, F4, F5); none blocks.

Method. Read-only. The head moved from `c3849089` to `899173f5` while this pass ran; `git diff --stat c3849089..899173f5` prints `.sdlc/board.md | 2 ++` and nothing else (two added table rows, `grep -c '^+|'` prints `2`), so every product, test, gate and record reading below, taken on the clone at `c3849089`, holds byte for byte at `899173f5`; G5 and G6 were read at `899173f5` directly. Own shared clone `scratchpad/pif-p2-clone` at `c3849089`, no `node_modules`; controls in a second clone `scratchpad/pif-p2-neg` restored with `git checkout .` between edits; origin/main controls in `scratchpad/pif-p2-main` at `1838e364`. Diff base `origin/main...899173f5` (merge-base `1838e364`), 130 paths, five more than pass 1 (`.sdlc/handoffs/pif-u10.md`, `.sdlc/verdicts/pif-u10.md`, `.sdlc/verdicts/pif-u10-review.md`, `docs/marketing/fact-sheet.md`, `src/engine/okhsl.js`; `comm -13` over the sorted path lists, none dropped). Load at start `15:34 load averages: 3.73 4.16 4.12`, at end `15:58 load averages: 4.44 3.96 4.30`. Rows pass 1 graded 🟢 and the delta does not touch (C8, C10, C11, C12, U7-*, U9-*, G1 to G3 apart from their re-runs below) stand on pass 1's readings; the gate files that carry them (`engine/semantic.mjs`, `engine/prime.mjs`, `ui/headless-boot.mjs`, `engine/categories.mjs`) each print `pass` inside this pass's `npm test`. All three clones read `git status --short | wc -l` `0` before removal and were removed by name; none left.

| # | Criterion | State | Evidence | Negative control |
|---|---|---|---|---|
| C1 | `npm test` green, no `node_modules`, tree clean, within the interim ceiling | 🟢 | clone at `c3849089`, `ls: node_modules: No such file or directory`; start `15:34 load averages: 3.73`; `✓ all 49 test files passed`, `npm test exit 0`, `wall 393 s`, end `15:41 load averages: 4.90`; `grep -c '^▶.*pass'` prints `49`; `git status --short \| wc -l` prints `0`. 393 s sits inside `ceiling 280 to 550 s` | the F1 control below (one line deleted from `src/engine/okhsl.js`) turns `node test/engine/anchor.mjs`, one of the 49, to `F1 control exit 1` |
| C2 | anchor identity, 3,380 exact | 🟢 | `node test/engine/anchor.mjs` `anchor exit 0`: `pass  anchor-identity: 3380 exact, 0 off`; `pass  key-anchor corpus: 3380 of 3380 ... 0/0 off` | the gate's own built-in corruption controls are live in the run: `grep -c 'DID NOT bite'` on the saved output prints `0`; pass 1's `prime.mjs:142` nudge (`0 exact, 3380 off`, exit 1) is the by-hand control and `prime.mjs` is unchanged in the delta (`git diff --stat 30bfaeeb c3849089` does not name it) |
| C3 | ramp passes through the anchor at 500, three modes, in-window | 🟢 | `pass  anchor-ramp: 10110 exact, 0 off (in-window sources only, 3370 of 3380)` | the new `anchor-achromatic` gate in the same file asserts stop 500 equals the in-window `#808080` anchor; with the F1 guard deleted the file reds (`F1 control exit 1`), proving the file's FAIL path fires |
| C4 | non-anchored byte identity control | 🟡 carried | as pass 1, K2 with no implementing command; the head run still prints `pass  prime-identity-control: 0 exact, 3796 off (want 0 exact, 3796 off ...)` | the F1 control run prints the same `prime-identity-control` line unchanged (`0 exact, 3796 off`) while `anchor-achromatic` reds, so the two are independent readings |
| C5 | monotone, distinct, window allow-list 10, ladder window 21, dupe 3, notch; the two frozen lists now named | 🟢 | `pass  anchor-ramp monotone: 0 (expected 0, ...)`; `pass  anchor-ramp allow-list: 10 (expected 10)`; `pass  anchor-ladder dupe-allow-list: 3 (expected 3)`; `pass  anchor-ramp notch allow-list ...: 15 (expected 15)`; `pass  anchor-ramp gap (19-stop) allow-list: 72 (expected 72)`; `pass  anchor-ramp distinct (25-stop) allow-list: 16 (expected 16)`; the plan's C5 bullet now carries `Revision 38: two more allow-lists sit under this bullet ... gap (19-stop) allow-list: 72 ... distinct (25-stop) allow-list: 16`, `grep -c 'gap (19-stop)\|distinct (25-stop)'` prints `2` at head. Pass 1's text drift is closed | the same grep on the plan at `30bfaeeb` prints `0`; pass 1's drop-one-name control (`10 (expected 9)`, exit 1) stands, the allow-list code is unchanged in the delta |
| C6 | chroma envelope | 🟡 carried open (owner Q2, #725) | `node scripts/report-preset-fidelity.mjs --envelope` at head: `envelope exit 1`, `env(500) = 1 sweep: PASS`, reading (b) `above 100% of stop 500: 0 OK` prints `3` times (one per mode), reading (a) `2592 FAIL` (peak), `670 FAIL` (even), `rule violations (second run or past-bound stop): 446 FAIL` (perceptual), `stop 300: median 94.0% (<=75 FAIL) / p90 146.2% (<=90 FAIL)`; every figure equal to pass 1, so the U10 cap did not move the envelope report | pass 1's `--damp-amp 55` control (`2904 FAIL` from `0 OK`, `damp55 exit 1`) stands; the F3 cap-removed control below moves the rendered Warning stops (`C=0.0000 h=grey`), so the cap is live even though this report's medians do not read it |
| C7 | one envelope, four call sites, no damp literal | 🟢 | `grep -c "export function chromaEnvelope("` prints `1`; `grep -c "chromaEnvelope("` prints `5` (lines `388 751 832 1038 1094`, the four call sites moved two lines down under the cap's comment); `grep -c "1 + ((controls.dampAmp"` prints `0`; `▶ engine/tonal.mjs         pass` | the F3 control's one-line edit at `tonal.js:390` keeps the count at `5` and greys Warning, so the count reads the seam, not the formula; `git show bf2aaf6:src/engine/tonal.js \| grep -c "1 + ((controls.dampAmp"` printed `2` in pass 1 |
| C9 | regenerated assets, role-table by 16 anchors only, binder and brands.json untouched | 🟢 | tree clean after `npm test` (`0`) with `figma/plugin/ui.html` and `src/ui/describe-mcp-assets.js` regenerated in the delta; `git diff origin/main c3849089 -- docs/reference/data/role-table.json \| grep -c '^[-+]'` prints `18`; `git diff --stat origin/main c3849089 -- figma/binder/figma-semantic-binder/code.js docs/reference/colors/categories/brands.json \| wc -l` prints `0`; `.sdlc/baseline.md` names the new ui.html figure `4119.1 KB` with its cause | `baseline-agrees-check.sh` prints `stale total: 0` at head, and read `STALE ui.html: baseline 4117.5 KB, tree 4118.2 KB` before U10's correction per `.sdlc/baseline.md`'s own 2026-09-23 paragraph |
| U10-1 | a `#000000` anchor renders real colours in all three modes | 🟢 | own probe on the head clone: `F1 #000000 perceptual nan=0 100:#E9E1E3 150:#D6C2C7 500:#1B1B1B`, `peak nan=0`, `even nan=0`; same `nan=0` for `#FFFFFF` and `#010101` in every mode; `F1 rgbToOkhsl(black).s=0`; `#FFFFFF` stop 500 clamps to `#F8F1DE` and `#000000` to `#1B1B1B` (out-of-window sources, Q3 b) | guard deleted in the control clone: `F1 #000000 perceptual nan=19 100:#NANNANNAN 150:#NANNANNAN 500:#NANNANNAN`, `F1 rgbToOkhsl(black).s=NaN`; the origin/main clone also prints `rgbToOkhsl(black).s=NaN` |
| U10-2 | the guard is in the engine, not the caller | 🟢 | `git diff --name-only 8f0e08d6 c3849089 -- src/engine/ src/ui/persist.js` prints `src/engine/hct.js`, `src/engine/okhsl.js`, `src/engine/tonal.js`, no `persist.js`; the guard is `okhsl.js:191` `if (L <= 0) return { h: ..., s: 0, l: 0 };` | the U10-1 probe calls `paletteStops` on the engine directly, never through `persist.js`; a caller-side filter would leave that probe at `nan=19`, which is what the control clone prints with the engine line removed |
| U10-3 | a test pins it | 🟢 | `test/engine/anchor.mjs` gains `anchor-achromatic`: `pass  anchor-achromatic: 24 of 24 ramps real (#000000, #FFFFFF, #010101, #808080 x 3 tone modes x 2 hue spaces), planted-NaN control caught`, and the gate first asserts its own predicate on a planted `#NANNANNAN` stop | guard deleted: `FAIL  anchor-achromatic: 20 of 24 ramps real ...`, `F1 control exit 1` (the four `#000000` ramps in perceptual and peak, both hue spaces) |
| U10-4 | the fact sheet is current | 🟢 | `grep -n -e 'OKHSL lightness ladder' -e 'unchanged from the previous release' docs/marketing/fact-sheet.md \| wc -l` prints `0`; line 18 now reads `derived on a **CIE L\*** lightness ladder` and `moved from OKHSL to CIE L\* in the same release (#681) that moved the ramps: an anchored palette's ramp now passes through its source colour at stop 500, so ramp and role values differ from the release before it`; the line carries `0` em dashes | the same grep at `30bfaeeb` prints `18:` |
| U10-5 | F3, the envelope never reads 0 across a band because lift moved the anchor | 🟢 (R27) | head: `F3 env lift-36 stops 50/100/150/200/300/500: 0.20 0.20 0.20 0.20 0.59 1.00`; `F3 Warning(lift -36) 100:#FCFBFA 150:#F5F1ED` (`OKLCH W100 L=0.989 C=0.0017 h=67.8`, `W150 C=0.0069 h=67.7`); `F3 hue250 c60 lift+40 850:#2C3137 900:#1C2024 950:#0F1114` (`h=253.0`, `h=248.2`, `h=258.4`, `C=0.0128 / 0.0097 / 0.0070`). origin/main for the same inputs: Warning `100:#FDFBF9 150:#F9F1E9` (`h=67.8 C=0.0034`, `h=67.7 C=0.0137`), `850:#253240 900:#192028 950:#0E1115` (`h=250.7 C=0.0308`, `h=252.0 C=0.0185`, `h=255.6 C=0.0096`). Hue matches main within 5 degrees at every probed stop, chroma sits at about half, as R27 rules | cap removed at `tonal.js:390` in the control clone: `0.00 0.00 0.00 0.17 0.59 1.00`, Warning `100:#FBFBFB 150:#F1F1F1`, `850:#313131 900:#202020 950:#111111`, all five `C=0.0000 h=grey` |
| U10-6 | the `hct.js:276` comment claims only what #686 fixed | 🟢 | `sed -n 270,282p src/engine/hct.js \| grep -c 'genuinely pure again'` prints `0`; lines 276 to 279 read `so this cache and the other two exact-keyed caches in this file (\`peakC\`, \`oklchToCam16Hue\`) are pure again. That claim covers hct.js only: tonal.js still keeps a bucketed \`_okL\` memo on \`lstar.toFixed(2)\`, the same defect class, tracked as #738`; `grep -c '#738' src/engine/hct.js` prints `1` | the same `sed \| grep -c 'genuinely pure again'` at `30bfaeeb` prints `1`; `#738` reads `0` in `origin/main`'s `hct.js` |
| U10-7 | no em dash in any comment this plan adds to `tonal.js` | 🟢 | `git diff origin/main...c3849089 -- src/engine/tonal.js \| grep '^+' \| perl -CSD -ne 'print if /\x{2014}/' \| wc -l` prints `0` | the same pipeline at `30bfaeeb` prints `58`; `printf '+ a \xe2\x80\x94 b\n'` through the filter prints `1` |
| U10-P1 | `npm test` green, tree clean | 🟢 | as C1: `✓ all 49 test files passed`, `0` | as C1 |
| U10-P2 | U10 scope wall | 🟢 | `git diff --name-only 30bfaeeb c3849089` outside `.sdlc/`: `docs/marketing/fact-sheet.md`, `figma/plugin/ui.html`, `src/engine/{hct,okhsl,tonal}.js`, `src/ui/describe-mcp-assets.js`, `test/engine/{anchor,exports,tonal}.mjs`, `test/engine/fixtures/{radix-baseline.json,tonal-legacy.json}`, `test/ui/fixtures/default-doc-ramps.json`; each is on U10-P2's list or in the plan's off-list section (`grep -c '^\| \`test/engine/exports.mjs\`'` on the plan prints `1`, which closes the U10 verdict's one 🟡) | a path in neither list reds G7 below; the U10 verdict's own 🟡 at `3c2667a7` was exactly such a path (`exports.mjs`) before revision 39 added its row |
| U10-P3 | landing checks and branding clean, no em dash added | 🟢 | the five `.sdlc/checks/*.sh` each `exit 0` (`stale total: 0`, `stale total: 0`, `range mismatches: 0`, `rows 56 drifted 11 holds 45 undetermined 0 bad 0`, `verdicts 103 graded 56 grandfathered 47 bad 0`); `ceiling-counts: clean`, `12` ok lines, exit `0`; `branding: clean (642 files scanned)`; the delta's added lines carry `3` em dashes, all in `.sdlc/verdicts/pif-u10.md` rows U10-5 to U10-7 quoting the plan's own cells, `0` in product or test files | `284 s` to `285 s` at `.sdlc/adapter.md:51`: `FAIL  adapter pointer found  (no 'a N-reading series from 284 s to M s')`, `ceiling-counts: 1 failure(s)`, `ceiling control exit 1`; `verdict:` line deleted from `pif-u10.md`: `MISSING pif-u10.md: no verdict: line`, `bad 1`, `vf control exit 1` |
| G1 | #686, exportPanda independent of call order | 🟢 | own script `panda-order2.mjs` on the head clone: `n 343 cold-vs-poison differing presets 0`; `grep -c '#686' src/engine/hct.js` prints `8` | same script on the origin/main clone at `1838e364`: `n 343 cold-vs-poison differing presets 16`; `#686` reads `0` in main's `hct.js` |
| G2 | #668, L* non-increasing at stop 800 on the witnesses | 🟢 | own script `uptick-668.mjs` on the head clone: `witnesses 11: non-increasing 11, rose 0; stops 25`; `grep -c 'ramp-monotone' test/engine/categories.mjs` prints `11`; `▶ engine/categories.mjs    pass` | same script on the origin/main clone: `witnesses 11: non-increasing 0, rose 11`, e.g. `travel "San Telmo" secondary-muted lift -40 anchored=false: +0.5105 at 750->800`; `ramp-monotone` reads `0` in main's `categories.mjs` |
| G3 | owner ruling Q6, labelled ceiling figure | 🟢 | `ok    ceiling test: baseline 280 to 550 s, adapter 280 to 550 s` inside `ceiling-counts: clean`; `baseline-agrees-check.sh` `stale total: 0` | the U10-P3 ceiling control (`284 s` to `285 s`, exit 1) |
| G4 | X5, `verdict:` lines at line 2 on ten records | 🟡 carried (#734, R25) | `verdicts 103 graded 56 grandfathered 47 bad 0`; the new `pif-u10.md` closes with `verdict: 🟢` then `sha: 3c2667a7...` as its last two lines, the ruled shape | the `verdict:` deletion control above (`bad 1`, exit 1) proves the check reads the line |
| G5 | PR #737 head is `899173f5`, CI green at that sha | 🟢 | `gh pr view 737`: `"headRefOid":"899173f5d70d02326c95fea4ccb4aac6a31415e9"`, `"mergeable":"MERGEABLE"`, `"mergeStateStatus":"CLEAN"`; check-runs at the sha: `build-test \| completed \| success`, `panda-smoke \| completed \| success`, `corpus-contrast \| completed \| success` (`deploy \| completed \| skipped`), run `https://github.com/kimgranlund/ultimate-tokens/actions/runs/35930145263` | `build-test \| in_progress \| null` at 15:49 and `completed \| success` by 15:57, so the reading is the finished state, not a cached one; at 15:34 the same query read `"headRefOid":"c3849089..."`, `"mergeable":"CONFLICTING"`, `"mergeStateStatus":"DIRTY"` with `total_count` `0` check-runs (a conflicting PR has no merge ref to run), so the green is this head's own |
| G6 | `git merge-tree --write-tree origin/main 899173f5` clean | 🟢 | prints a tree id `fbb0245b...` and `grep -c CONFLICT` prints `0`; `origin/main` is `1838e364` and `899173f5`'s second parent is `1838e364` (`git log --format='%h %p'` reads `899173f5 c3849089 1838e364`) | `git merge-tree --write-tree origin/main c3849089` prints `CONFLICT (content): Merge conflict in .sdlc/board.md`, `mt exit 1`, so the clean read is the board merge's doing, not a no-op query |
| G7 | scope: no private docs, no `node_modules`, changed files on the plan's lists or its off-list section | 🟢 | `grep -c 'claude/docs/other\|node_modules'` over the 130 paths prints `0`; the plan's `## Paths off the unit lists (revision 38)` table names every one of pass 1's ten paths plus U10's fixture, test, generated-asset and baseline paths; the five paths new since pass 1 are three `.sdlc/` U10 records and two U10-P2 list entries | a path in neither a unit list nor the off-list table reds this row; at `30bfaeeb` the table did not exist (`grep -c 'Paths off the unit lists'` printed `0` there per pass 1's G7) and `test/engine/exports.mjs` was on no list at `3c2667a7` per the U10 verdict |
| G8 | em dashes in the whole plan diff | 🟡 carried (rule-gates U4 sweep) | the delta `30bfaeeb..c3849089` on hand-edited paths adds `3` lines with the glyph, all in `.sdlc/verdicts/pif-u10.md`; `src/engine/tonal.js` is now at `0` (U10-7); pass 1's `648` over the full plan diff is otherwise unchanged and routed to the rule-gates U4 sweep by the owner | `printf '+ a \xe2\x80\x94 b\n'` through the `-CSD` filter prints `1` |
| X5 | eleven `verdict:` lines at line 2 | 🟡 carried | `git show 899173f5:.sdlc/verdicts/pif-u1.md \| grep -n "^verdict:"` prints `2:verdict: 🟢`, the same for `pif-u2`, `u3`, `u4`, `u6`, `u7`, `u7-review-1`, `u7-review-2`, `u8`, `u8-review`; `pif-u5.md` prints `10:verdict: 🟢`; owner moved it to #734 under R25 | as G4 |
| R27 | half-chroma tint on the capped stops | 🟡 carried, accepted | U10-5 above: head chroma at 850 to 950 `0.0128 / 0.0097 / 0.0070` against main's `0.0308 / 0.0185 / 0.0096`; the full-chroma follow-up is noted on #725 per revision 39 | the cap-removed control renders `C=0.0000` at the same stops |
| F4 | filed as #739 | 🟡 carried | `1838e364`'s subject names `achromatic-anchor (#739) queued on G0` | not re-run; a filed ticket is the owner's disposition, the F1 gate above covers the black and white anchors this pass |
| F5 | `_okL` memo, filed as #738, #686 still closing | 🟡 carried (R26) | `hct.js:277` `tracked as #738`; PR #737 body `Closes #668`, `Closes #681`, `Closes #686`; G1's `0` differing presets shows #686's own defect is closed at head | G1's origin/main reading `16` |

Counts: 30 rows, 22 🟢, 8 🟡 (C4, C6, G4, G8 and the carried rulings X5, R27, F4, F5), 0 🔴. Re-graded from pass 1: C5 🟡 to 🟢 (revision 38 names the 72 and 16 lists), G7 🟡 to 🟢 (the off-list section, with `exports.mjs` added at revision 39), G6 🔴 to 🟢 (the board merge at `899173f5`). No new attention items this pass; every 🟡 is a carried, owner-ruled item with its ticket or ruling named in its row.

The review side of this pass: `reviewer-l4` (pif-prepr-reviewer-l4-p2), fresh context at `c3849089`, PASS with carried yellows, `npm test` 49 of 49, all checks exit 0; record `pif-prepr-review-p2.md` in the orchestrator's temp dir. The delta `c3849089..899173f5` is `.sdlc/board.md` only. CI run `35930145263` is green on build-test, panda-smoke and corpus-contrast at `899173f5`.

verdict: 🟢
sha: 899173f5d70d02326c95fea4ccb4aac6a31415e9
