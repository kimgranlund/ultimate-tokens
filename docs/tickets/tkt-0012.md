---
doc-type: ticket
kind: feature
size: big
id: tkt-0012
status: open
date: 2026-07-17
owner:
---
# TKT-0012 — Rename capability: id-preserving renames{} maps through planners + executors (+ registry-key migration)

## Summary
All four apply-loops reconcile by NAME (create-or-reuse, prune the rest) — any engine rename orphans every consumer binding on a normal re-apply; the id-preserving path was a bespoke BZZR script twice (TKT-0009/0010). Ship a first-class renames map: planners thread `renames{old→new}`, each executor runs a rename-first pass (`variable.name =` keeps ids) before reconcile/prune; collection renames migrate the provenance-registry KEY the same way. (docs/reference/reviews/2026-07-17-collections-arch.md CRITICAL-1.)

## Acceptance
- mode-apply/bind/style planners accept + emit rename maps; executors rename-first in all four loops
- Collection rename migrates the float/style registry keys (old key adopted by id, re-keyed)
- Gates: a renamed field re-applied over an old-shape mock keeps variable ids; parity gate between flagship + binder copies holds
- The per-ticket convention (every rename ships its map) recorded in the shipping skill

## Links
- docs/reference/reviews/2026-07-17-collections-arch.md
- docs/tickets/tkt-0013.md

## Findings
