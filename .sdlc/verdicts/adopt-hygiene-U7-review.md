---
kind: review
plan: adopt-hygiene
unit: U7
pass: 2
diff: 65bbda3..unit/hygiene-U7 (head 3fbeb5c); pass 2 delta ac52be8..3fbeb5c
reviewer: reviewer (fresh context)
date: 2026-09-17
verdict: 🟢 pass
---

# U7 review pass 2: declare the nonoun marketplace

Fresh-context review against the root `.sdlc/plans/adopt-hygiene.md` §U7 (row 2 revised), the pass 1 verdict `.sdlc/verdicts/adopt-hygiene-U7.md`, and the re-diagnosis `.sdlc/plans/adopt-hygiene-U7-p2.md` (its clash-and-wording block, copied verbatim into a scratch script). Every command was run by me in `.worktrees/hygiene-U7` @ 3fbeb5c, or in detached scratch worktrees at 65bbda3, ac52be8 and 3fbeb5c (removed after). The handoff was not used as evidence. The unit worktree ends at porcelain `0`.

Tally: 3 criteria + 4 independent checks. 🟢 7 · 🟡 0 · 🔴 0. Blocking findings: 0.

## Criteria

| # | Criterion | State | Evidence | Negative control |
|---|---|---|---|---|
| 1 | `nonoun` declared with the `nonoun-plugins` shape; nothing else in settings changes | 🟢 | `github kimgranlund/sdlc-orchestration true true`; diff vs 39db0a9 counts `6`, all added, 0 removed (read by hand: one 6 line entry between `nonoun-plugins` and `adia`); settings at 3fbeb5c minus `extraKnownMarketplaces.nonoun` deep-equals 39db0a9: `true`. Pass 2 does not touch the file | pass 1 control stands (file unchanged since ac52be8) |
| 2 | debt records the repo, C5 resolved, C6 complete, clashing ids noted, no em dash or bold label, row count true | 🟢 | pass 1 commands: `2`, no `missing` line. Block: no `clash` line, `0`, `0`, `7 / 7`, `45 / 45`. Root and worktree `.sdlc/adapter.md` are identical; adapter §4 C5 (no hooks line), C6 (`.claude/ops/`), C7 (trailers) match the referents each debt note names | 65bbda3: `0`, `missing .sdlc/debt.md`, `clash C5 without note`, `clash C6 without note`, `0`, `0`, `4 / 6`, `41 / 44`. ac52be8: `2`, `clash C13 without note`, `2`, `1`, `4 / 7`, `41 / 45`. Scratch 3fbeb5c with C7 renamed C8: only change is `clash C8 without note`. Planted U+2014 in the C7 note: em dash count `1`. Re-bolded `resolved by U7`: bold count `1`. All match the plan |
| 3 | JSON valid, `npm test` green, tree clean, branding clean | 🟢 | `npm test` exit 0, `✓ all 44 test files passed`, `repo/branding.mjs pass`; porcelain `0` | scratch 3fbeb5c: an uppercased brand token generated by `tr` (never typed) appended to `.sdlc/debt.md`: branding exit 1, `FAIL: 1 branding violation(s) across 421 files`; restored: `clean (421 files scanned)`, porcelain `0` |

## Independent checks

| Check | State | Evidence |
|---|---|---|
| No em dash or bold inline label in the unit diff or pass 2 commit message | 🟢 | added lines of `git diff 65bbda3..3fbeb5c` (debt.md, settings.json, handoff): 0 hits for U+2014 or a bold span. Whole files at 3fbeb5c: debt.md 0/0, settings.json 0/0, handoff 0/0 (the pass 1 handoff's em dash and bold ids are gone). Messages of 3fbeb5c and ac52be8: 0 |
| Every debt C id this plan added that exists in adapter §4 carries a note | 🟢 | ids absent at b885e67: C5, C6, C7; all three exist in adapter §4 and each row names `adapter conflict Cn in .sdlc/adapter.md §4` with the correct referent |
| No debt rows deleted | 🟢 | sorted row id sets 65bbda3 vs 3fbeb5c differ by one added line, `C7`; nothing removed. C13 existed only at ac52be8 on this branch |
| Skipped pre-merge of `sdlc/adopt` | 🟢 not required | `git merge-tree --write-tree sdlc/adopt 3fbeb5c` exits 0 with a tree and no conflicts. `sdlc/adopt` @ 4f735dd changed only board, plan, p2, verdict and review files since 65bbda3, none of which the unit touches. The pass 1 gap 3 (stale branch plan copy) resolves on the orchestrator's merge, since the plan's current text lives on `sdlc/adopt` |

## Findings

None blocking. One note, not a defect: pass 1 verdict gap 4 (fresh clones cannot resolve `sdlc@nonoun` until the human pushes `kimgranlund/sdlc-orchestration`) is still open by design and tracked as debt C7, owner human.
