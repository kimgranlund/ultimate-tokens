# issue-sorter sweep — kimgranlund/ultimate-tokens — 2026-07-25T19:02:38Z

Dispatch: interactive, on-demand ("run your standard issue-sorting sweep"). First firing on this
repo — no prior `.claude/ops/` state existed.

## Discovery

- Sources: `gh issue list` and `gh pr list` (GitHub-only; no Linear MCP connector configured, so
  Linear stays `not_configured`, not polled).
- No prior checkpoint entry existed, so this firing's window was the full open set (all 34
  all-time issues / 366 all-time PRs enumerated for classification context; 2 open issues + 1 open
  PR are the live surface). Both sources reached successfully — checkpoint now advanced for both.

## Open items found (2 issues, 1 PR — full open set)

| # | Title | Labels | Author | Status |
|---|---|---|---|---|
| #379 | describe-a-palette generator MCP (tracking issue) | `kind:feature`, `size:big` | kimgranlund | correctly labeled, no action |
| #377 | hosted describe-palette MCP surface (blocked: domains, accounts) | `task` | kimgranlund | correctly labeled (task, unsized — legal per `file-task`'s default); already known/blocked externally per your context |
| PR #158 | flip TIERS_ENFORCED → true (HELD for go-live) | *(none)* | kimgranlund | draft, held; already known per your context |

**Finding: nothing new beyond the two threads you already named.** #379 is the describe-palette
program's own tracking issue (parent to the now-closed #369–#376, #393–#398 children) — open,
correctly `kind:feature`/`size:big`, self-documenting via its own comment thread, no triage gap.
#377 and PR #158 are the same two open threads you flagged; neither is missing ADR-017 labeling in
a way that needs fixing (PR #158 carries no `kind:`/`size:` labels, but those labels are the
ticket/issue convention — PRs implement tickets, they don't carry the label themselves; #158 is
already tracked as the monetization go-live PR, not an untriaged filing).

No unlabeled issues, no author-unknown filings, no stale/forgotten threads, no duplicates found.
All 34 issues and all PRs closed to date are consistently labeled (`kind:bug`/`kind:feature` +
`size:small`/`size:big`, or `task`). Nothing landed in `held-items.md` this firing — every item
seen was authored by the sole evidenced trusted author (see below), and nothing needed a fresh
record minted (both open issues already exist as the record itself, per the resolved git-native
backend).

## REQ-011 bootstrap (first firing)

No `friendlies.json` existed yet. Seeded evidence-only:
- **kimgranlund** — repo owner/admin (`gh repo view`), sole collaborator (`gh api
  .../collaborators`), and sole author across all 34 issues + 366 PRs all-time. No second author
  guessed.
- Repo is **public**, so both the private-repo lens (approved collaborators) and the public-repo
  lens (historical authors + owners) land on the same single candidate. Surfaced for your
  confirmation — no roster decision has been recorded; this seat doesn't ask, it only surfaces.

**Open for you:** confirm the roster as `[kimgranlund]` (or name anyone else who should be
trusted), so a later dispatch can record the standing rule in `friendlies.json`'s `policy` block.

## REQ-013 MCP offer

Resolved backend is Option B (git-native/GitHub), and no `github_mcp_offer` decision is on record
→ gate is met, offer surfaced, outcome this firing: **offered-and-pending-a-carrying-dispatch**.

Offer: declare a project-scoped GitHub MCP server
(`https://api.githubcopilot.com/mcp/`) for richer interactive-session access (PR review, code
search) alongside the existing `gh`-CLI skills, which stay the only write path regardless.
Recommended default: a read-only-scoped fine-grained PAT (Issues/PRs/contents-read only — you'd
generate it on GitHub's token page; this agent never generates one), referenced from `.mcp.json`
only as `${GITHUB_MCP_PAT}` (never a literal secret).

**Open for you:** accept, decline, or accept-with-different-scope. No `.mcp.json` write and no
`policy.github_mcp_offer` record happens until a dispatch carries your choice — until then this
stays the named surfaced-but-pending state and will re-surface (not re-ask) on the next interactive
firing.

## State written this firing

- `.claude/ops/watch-checkpoint.json` — created; `github_issues`/`github_prs` advanced to
  2026-07-25T19:02:38Z (both sources reachable); `linear: not_configured`.
- `.claude/ops/friendlies.json` — created; evidence-seeded `trusted_authors: [kimgranlund]`;
  `policy.bootstrap` records the seed basis and surfaced (unconfirmed) roster candidate;
  `policy.github_mcp_offer: null` (surfaced-but-pending, not yet recorded either way).
- `.claude/ops/held-items.md` — created; empty ledger (no untrusted-author items this firing).

## Boundaries respected

No source file touched, no PR merged, no issue closed, no label changed on any existing issue/PR
(none needed changing), no held item self-approved, no `.mcp.json` written (offer not yet
confirmed).
