# Handoff pif-u10 · #681 preset-intent-fidelity, pre-land F1, F2, F3 and the comment rows

Builder, grade l5, pass 1 plus the revision 37 scope growth (owner R26). Written 2026-09-23.

## Branch

`unit/pif-u10`, worktree `.worktrees/pif-u10`. First built on `8f0e08d6`; team-lead merged revisions 37 and 38 in at `68750e20` and the growth commits sit on top, no rebase. UB now = `git merge-base plan/preset-intent-fidelity HEAD` = `d1f2704c1ba2dc194709bd7ffdcb8ccf8b2348be`. Controls ran in `git clone -q --shared` scratch clones under `$TMPDIR` (`pif-u10-scratch` at `30bfaeeb`, `pif-u10-main` at `origin/main` `dae67a3a`, `pif-u10-head` at `47f361e7`), never in the repo.

## Files

| File | Change |
|---|---|
| `src/engine/okhsl.js` | F1: `rgbToOkhsl` returns `s = 0` at `L <= 0`. Pure black has a zero-width gamut, so `getCs` gave zero chroma bounds and the saturation formula divided 0 by 0. No other input changes path. |
| `src/engine/tonal.js` | F3 (U10-5): `chromaEnvelope` clamps `sd` to [-1, 1]. U10-7: the 61 em dashes on comment lines the plan added (58 lines) are hyphens. The cap comment is two lines on purpose: `docs/reference/reviews/2026-08-20-reactivity/00-synthesis.md:89` cites `tonal.js:907` for `_okL`, and the citation gate allows a window of 3 lines (a four-line comment read STALE). |
| `src/engine/hct.js` | U10-6: the purity sentence now covers the three exact-keyed `hct.js` caches only and names #738 for `tonal.js`'s bucketed `_okL` memo. |
| `test/engine/anchor.mjs` | new `anchor-achromatic` gate (F1): `#000000`, `#FFFFFF`, `#010101`, `#808080` x 3 tone modes x 2 hue spaces through `paletteStops`; real 6-hex, finite tone, stop 500 equals an in-window anchor; a planted NaN stop proves the predicate bites. |
| `test/engine/tonal.mjs` | damping-curve (a)'s independent legacy formula caps `uLeg` at 1, as the engine now caps `sd`. intensity-legacy gains a cited re-pin paragraph. |
| `test/engine/fixtures/tonal-legacy.json` | 8 Warning cells hand-patched (perceptual 100/125/150/175/200, even 125/150/175), all neutral grey to Warning tint. |
| `test/ui/fixtures/default-doc-ramps.json` | regenerated by `scripts/gen-ramp-fixture.mjs` with a `generatedFrom` label naming U10 F3: 6 cells (Success 75 `#ECF0EB`->`#ECF1EB`, Warning 100 to 200). |
| `test/engine/fixtures/radix-baseline.json` + `test/engine/exports.mjs` | re-captured by script from the engine (a temporary copy of the gate that wrote the file, deleted after): 30 leaves, all Warning steps 1 to 5 and a1 to a5 in ALL / BRAND_ONLY / COLLIDING, `oklch(... 0 0)` to Warning's tint. Re-capture cited in the gate comment. |
| `docs/marketing/fact-sheet.md` | F2 on line 18 (CIE L* ladder; #681 moved the ramps), and that line's 4 em dashes now colons and commas. |
| `figma/plugin/ui.html`, `src/ui/describe-mcp-assets.js` | regenerated by `npm test` (both inline engine source). |
| `.sdlc/baseline.md` | ui.html KB cell 4117.5 to 4119.1, two named correction paragraphs. |

## Ran

| # | Output | Control output |
|---|---|---|
| U10-1 | 🟢 `#000000`/`#FFFFFF`/`#010101` x perceptual/peak/even: `nan=0` in all 9; `#000000 perceptual 500=#1B1B1B`. All three sit outside the 9.95..95.05 L* window, so 500 is the clamped edge by the anchor rule; in-window `#808080` hits 500 byte for byte in the U10-3 gate | at `30bfaeeb`: `#000000 perceptual nan=19 500=#NANNANNAN 100:#NANNANNAN ...`, peak likewise |
| U10-2 | 🟢 `git diff --name-only $UB -- src/engine/ src/ui/persist.js` names `src/engine/hct.js`, `src/engine/okhsl.js`, `src/engine/tonal.js`, not `persist.js` | U10-1 calls the engine directly |
| U10-3 | 🟢 `pass  anchor-achromatic: 24 of 24 ramps real (...), planted-NaN control caught` | the new `anchor.mjs` at `30bfaeeb`: `FAIL  anchor-achromatic: 20 of 24 ramps real`, exit 1 |
| U10-4 | 🟢 grep count `0` | at `30bfaeeb` it prints line 18 |
| U10-5 | 🟡 `env lift-36: 0.20 0.20 0.20 0.20 0.59 1.00` (stops 50/100/150/200/300/500); Warning `100:#FCFBFA 150:#F5F1ED`; hue 250 c60 lift +40 `850:#2C3137 900:#1C2024 950:#0F1114`. Tinted, same hue as `origin/main`, about half its chroma; see Left out | `30bfaeeb`: `0.00 0.00 0.00 0.17 0.59 1.00`, `100:#FBFBFB 150:#F1F1F1`, `850:#313131 900:#202020 950:#111111`. `origin/main`: `100:#FDFBF9 150:#F9F1E9`, `850:#253240 900:#192028 950:#0E1115`. Cap removed in the head clone: tonal.mjs (damping-curve, intensity-legacy), exports.mjs (radix-refs-values-unchanged), shell.mjs (ac003b) each exit 1 |
| U10-6 | 🟢 lines 276 to 279 read "this cache and the other two exact-keyed caches in this file (`peakC`, `oklchToCam16Hue`) are pure again. That claim covers hct.js only: tonal.js still keeps a bucketed `_okL` memo ..., tracked as #738." | `30bfaeeb` line 276 ends `genuinely pure again.` with no scope |
| U10-7 | 🟢 `0` | `30bfaeeb`: `58` |
| U10-P1 | 🟢 fresh clone at `47f361e7`, no `node_modules`, load `3.32 4.14 4.18`: `✓ all 49 test files passed`, `npm test  308.26s user 5.19s system 103% cpu 5:03.00 total`, `git status --short \| wc -l` = `0` | same clone, `okhsl.js` from UB, `node test/run.mjs`: `▶ engine/anchor.mjs        FAIL`, `✗ 1/49 test file(s) failed`, exit 1 |
| U10-P2 | 🟡 names: `.sdlc/baseline.md`, `.sdlc/handoffs/pif-u10.md`, `docs/marketing/fact-sheet.md`, `figma/plugin/ui.html`, `src/engine/{hct,okhsl,tonal}.js`, `src/ui/describe-mcp-assets.js`, `test/engine/anchor.mjs`, `test/engine/exports.mjs`, `test/engine/fixtures/radix-baseline.json`, `test/engine/fixtures/tonal-legacy.json`, `test/engine/tonal.mjs`, `test/ui/fixtures/default-doc-ramps.json`; see Left out | a stray edit prints an extra name |
| U10-P3 | 🟢 five `.sdlc/checks/*.sh` exit 0, `ceiling-counts: clean`, `branding: clean (640 files scanned)`, dash count `0` | one planted added line carrying U+2014 prints `1`; before the baseline repair `baseline-agrees-check.sh` read `STALE ui.html`, exit 1 |

## Left out

- U10-5 "matching the tint `origin/main` renders": the hue matches (OKLCH h 68 for Warning 100/150 on both; 248 to 258 for the navy stops at the head, 251 to 256 on `origin/main`), the chroma does not. Capped stops sit at the 50/950 edge floor (env 0.20), while `origin/main` read them at sd 0.78 to 0.89 (env about 0.33 to 0.45). Measured OKLCH C, head vs `origin/main`: Warning 100 `0.0017` vs `0.0034`, 150 `0.0069` vs `0.0137`, 850 `0.0128` vs `0.0308`, 900 `0.0097` vs `0.0185`, 950 `0.0070` vs `0.0096`. Matching `origin/main`'s chroma would need `sd` normalized per side by the lifted span, which is the post-hoc-normalized draft U3 rejected (33 rises, `tonal.js` R2 comment). I took the ruled cap.
- The cap moves three pinned fixtures and one test formula outside U10-P2's list. All of them are the F3 class only (neutral to tint on lifted ramps), each cited where it is pinned, and each reds with the cap removed.
- `.sdlc/baseline.md` is also outside the list. The ui.html figure moved twice (the okhsl guard, then the tonal/hct edits), and U10-P3 needs `baseline-agrees-check.sh` green. Repaired in this unit on the U7 precedent, from `gen:figma-ui`'s own `wrote figma/plugin/ui.html 4119.1 KB`.
- `#FFFFFF` still reads `rgbToOkhsl` s = 0.558 at h 89.9 (F4, #739); `_okL` untouched (F5, #738). `npm run build` and `npm run smoke` not run (no build-chain file touched).
