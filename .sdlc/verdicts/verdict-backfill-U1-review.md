# Verdict U1 review · 🟢

Reviewed `f69ce16a` (`unit/bf-U1`) against `plan/verdict-backfill` base `a7321fa0`, reading `.sdlc/plans/verdict-backfill.md` §U1 and `.sdlc/handoffs/verdict-backfill-U1.md`. Every row below reran independently in a fresh `git clone -q --shared` of `f69ce16a` under this seat's own scratchpad (`.claude/jobs/8c58a81c/tmp/bfu1/clone`), never in the worktree. `B` is `b0bc8343` (`git merge-base origin/main HEAD` in the clone).

| # | Criterion | State | Evidence | Negative control |
|---|---|---|---|---|
| 1 | U1-1: each of the 31 carries the line, token equals the title's first emoji | 🟢 | own run: `31`, `0` MISMATCH lines | own plant, `sed -i '' '2s/🟢/🟡/' .sdlc/verdicts/adopt-hygiene-U1.md`: `30 ok` and `MISMATCH adopt-hygiene-U1.md title=🟢 line=🟡`, matching the plan's stated control |
| 2 | U1-2: placement, 28 at line 2, 3 in front matter | 🟢 | `28 1`, then three lines each `verdict: 🟢` | own plant, moved `adopt-hygiene-U1.md`'s line to line 3: `27 1` and `1 0`, matching the plan's stated control exactly |
| 3 | U1-3: list lost exactly the 31, kept the 16, header intact | 🟢 | `cf9e4407ced1d74d6e1b8007fb0a7a3cbcf0657860e5bf799abb3dcb82723e42`, `ee97008bb4def1516de7765c7e0145768217a4df8ca3c3bb001247e7a0aad8e8`, `#` | matches the plan's precomputed U1 and U2 hashes exactly; a name moved wrong prints a different pair per the plan's own row, not replanted since the positive hashes already pin the exact set |
| 4 | U1-4: no later block in any of the 31 re-grades the title | 🟢 | read all 31 files' closing blocks myself (headings + tail); every file whose last text carries a `🟡`/`🔴`-flavored note (`adopt-hygiene-U5.md`, `adopt-hygiene-U9.md`) states the note is non-blocking in its own tally line, so the title stands in every case | `k17-rerun-checkability.md` (a U2 file, read for contrast): title 🔴, `## Pass 2` overturns it to 🟢, confirming the loop can detect a real overturn; none of the 31 has one |
| 5 | U1-5 / P4: check green at unit head, list holds 16 | 🟢 | `verdicts 79 graded 63 grandfathered 16 bad 0`, exit 0 | P4's own controls in the measured table (`CLEARED`/`MISSING`) are the check's, not replanted here since nothing in this unit's edits touches the check script |
| 6 | U1-6: one commit carries both the 31 files and the list | 🟢 | `1`, and `e2bd2329` for both `adopt-hygiene-U1.md` and the list | same command against a second file (`survey.md`) also prints `e2bd2329`, confirming the whole set, not just one file, is in that commit |
| 7 | P1: `npm test` green, tree clean after | 🟢 | ran in foreground in the clone, 600000 ms timeout: `✓ all 48 test files passed`, `git status --short` `0` lines after | regression guard per the plan; nothing under `src/`, `test/`, `scripts/` is touched by this unit, confirmed by the file list below |
| 8 | P2: branding clean, no added em dash | 🟢 | `branding: clean (569 files scanned)`, `0`, `0` | not replanted: every added line in this unit is a bare `verdict: <emoji>` line, confirmed by reading the diff itself (own run, not the plan's canned control) |
| 9 | P3: scope wall, only the named files move | 🟢 | `git diff --name-only "$B"` sorted: exactly the 31 named files, the grandfather list, and the U1 handoff (33 total); `0`, `0`, `0` on P3's three counting commands | no `pif-*` name and no name outside the plan's 47-file set appears in the diff, confirmed by listing every changed path myself, not by sampling |
| 10 | P5: each backfilled file one added line, none deleted | 🟢 | all 31 files print `1 0` on `--numstat` | own plant, a second clone of the unit head, `echo "extra line" >> .sdlc/verdicts/adopt-hygiene-U1.md` then `git diff --numstat "$B" -- .sdlc/verdicts/adopt-hygiene-U1.md`: `2 0`, proving the loop reads a real per-file count and would flag a file carrying more than the one backfilled line |
| 11 | P6: added line is the file's last `verdict:` line and matches the unit's token table | 🟢 | 27 files `🟢`, `adopt-hygiene-plan.md`/`k17-rerun-U1.md`/`records.md`/`survey.md` `🟡`, matching the plan's title-emoji table exactly | today's tree (pre-unit) prints no `verdict:` line for any of the 31, confirmed against the plan's stated "Today" row |
| 12 | P7: every other `.sdlc/checks/*.sh` exits as at `$B` | 🟢 | all five scripts `exit 0` at the unit head | matches the plan's stated "Today" baseline (all five `exit 0` at `3e1483e4`); not replanted since the timing-sensitive control belongs to `baseline-agrees-check.sh`'s own plan, not this unit |
| 13 | Does the added line change what `adapter.py`'s `read_gate` reads for any file that is also a landing gate (`*-prepr.md`)? | no | none of the 31 U1 filenames end in `-prepr.md`: `k17-rerun-prepr-review.md` ends `-review.md`, not `-prepr.md`, and no other U1 name contains `prepr` at all (checked by pattern match against the full 31-name list, `0` matches) | the U2 file `records-followup-prepr-review.md` is also a `-review.md`, not a `-prepr.md` gate file itself, so this question is moot for both U1 and U2's set; the actual `*-prepr.md` landing-gate records (e.g. `verdict-frontmatter-prepr.md`) are untouched by this plan's scope wall (P3, row 9), so `read_gate` reads the same bytes before and after for every real gate record |

File-by-file check (not sampled): all 31 named files gained exactly one line and lost none (row 10); the token equals each title's own first emoji, read by me directly from each file, not taken from the handoff's table (row 1, row 4); the line sits at line 2 (28 files) or as the last front-matter line (3 files), matching the plan's placement rule exactly (row 2); the 31 names removed from the list are exactly the 31 named files and no others, proven by the hash match against the plan's precomputed set (row 3) and by `comm -3` producing the same pair independently in this seat's own clone. No `pif-*` record and no record outside the plan's 47-file set moved (row 9, full file listing, not a sample).

`sh .sdlc/checks/verdict-frontmatter-check.sh` exits 0 at the head (row 5).

Nothing here contradicts the builder's handoff; every figure it claims reproduced independently.

verdict: 🟢
