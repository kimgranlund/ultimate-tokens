---
doc-type: ticket
kind: feature
size: big
id: tkt-0010
status: done
date: 2026-07-16
owner:
---
# TKT-0010: Control padding corrections: paddingWide/paddingNarrow (+compact variants) and the calibrated gap-unit

## Summary
Reporter-supplied correction to control-internal geometry (2026-07-16):

1. **Rename + reformulate the pads.** `padding` → **paddingNarrow** `= (height − icon)/2`
   (formula unchanged, the centering law); `edgePadding` → **paddingWide** `= (height − caret)/2`
   (NEW formula, was `height/2`; a bare-text control's edge now keys on the caret glyph, not the
   half-height, landing much tighter: md 14 → 7.5).
2. **Add compact variants:** **paddingWideCompact** `= (height − gap − caret)/2` and
   **paddingNarrowCompact** `= (height − gap − icon)/2`.
3. **Half-pixels stay EXACT** (ratified): the laws emit true halves (7.5 · 5.5 · 2.5 …), valid
   CSS px and Figma floats; no rounding layer.
4. **gap is no longer `font/2`.** New hand-calibrated **GAP_UNIT** table (Desktop, canonical
   baseHeight 28): xs 3 · sm 3 · md 4 · lg 6 · xl 6 · 2xl 8, scaled by `baseHeight/28` × density;
   per-mode hand columns override (ratified matrix): Tablet frozen at Desktop; Mobile 3/3/4/5/5/6
   (reporter anchors xs 3 · 2xl 6); Desktop Lg 4/4/5/7/7/9; Desktop Xl 4/5/6/8/8/10; TV (BZZR)
   5/5/6/9/9/12.

Scope ruling: **engine + BZZR** (rename at the source, then migrate the live file, variables
rename in place, so node/style bindings survive by id).

## Acceptance
- `buildSize` returns `paddingNarrow`/`paddingWide`/`paddingNarrowCompact`/`paddingWideCompact`
  (no `padding`/`edgePadding`), `gap = max(1, round(GAP_UNIT[name] × factor × density))`, with
  `opts.gapOverrides` (final per-size values, mirrors `fontOverrides`) winning.
- All emitters carry the four pads: CSS `-padding-narrow/-padding-wide/-padding-narrow-compact/`
  `-padding-wide-compact` (the `.control-{size}` utility rides `-padding-wide`), DTCG + Figma
  `paddingNarrow`/… keys (Geometry moded collection 8 → 10 size fields).
- `_geomModeScales` passes the ratified per-tier gap columns via `gapOverrides`.
- Tests: engine REF table (new gap column + four pad rows, exact halves), headless/smoke/figma
  count literals, mcp asserts.
- BZZR: `size/*/padding` → `size/*/paddingNarrow` renamed in place, `size/*/edgePadding` →
  `size/*/paddingWide` renamed + revalued per the new formula, 12 new compact vars × 6 modes,
  gap recalibrated across all 6 modes; readback-verified.
- `npm test` + `npm run build` green; CI green; squash-merged.

## Scope / Open
- Non-goal: the container tier (insets/gaps/borders/focus) and the space ladder, untouched.
- Follow-up (flagged, not in this ticket): the design-systems plugin's
  material-design-geometry-tokens skill documents `-pad`/`-pad-edge`, needs a re-sync pass after
  this ships (same motion as the 0.9.0 typography re-sync).

## Links
- Builds on TKT-0009 (merged moded collection) and the TV mode (memory: bzzr-tv-breakpoint).

## Findings

### 2026-07-16: built (engine + BZZR), both verified
**Engine.** `buildSize` returns the four pads (exact halves, the base ramp lands pn/pw/pnc/pwc =
5/7.5/3/5.5 at MD); `GAP_UNIT` {3,3,4,6,6,8} × bh/28 × density replaces `font/2` (gap no longer
tracks the font, gated); `opts.gapOverrides` = final per-size values; `_geomModeScales` carries
the ratified tier columns. All emitters renamed (`-padding-narrow/-wide/-narrow-compact/`
`-wide-compact`; `.control-{size}` rides `-padding-wide`); GEOM_SIZE_FIELDS 8 → 10. The consumer
skill's own `dimension-parity` gate caught the first doc wording (an unknown "padding" field
parsed from a wildcard phrase), the parity gate bites on prose too. 27/27 green; build clean.

**BZZR.** 12 vars renamed IN PLACE (ids keep every node/style binding, zero re-binds needed),
12 compact vars minted, 180 values set (gap matrix + four pads × 6 modes), formula-verified
0/180 mismatches in the same call. Geometry group: 350 → 362 vars.
