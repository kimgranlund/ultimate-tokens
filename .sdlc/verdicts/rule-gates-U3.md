# Verdict rg-U3 · 🔴

Unit `unit/rg-U3` at `397e69ea`, base `b3961aa9` (predates U6's merge, so R0 (e) still carries 20 source lines). Plan `plan/rule-gates` revision 9 at `b2b79fcb`. Verifier-l2, read-only: a detached worktree at `397e69ea` and a `git clone --shared` at the same commit, both under this seat's scratchpad and removed by name afterwards. Load `3.00 9.35 16.06` at start, `5.43 7.73 11.92` at end.

| Next action | Owner |
|---|---|
| Fix two reds: route the self-test's temp-file read through `readText()` (row 6), and treat a dash that follows a line-leading comment or quote marker (`//`, `#`, `>`) as a line-start dash so it goes to R7 or R0 (d), never R8 (row 19). Then re-verify | builder, via the Orchestrator |

27 rows: 🟢 22 · 🟡 3 · 🔴 2.

| # | Criterion | State | Evidence | Negative control |
|---|---|---|---|---|
| 1 | U3-1: the gate reds on the unswept tree, and the self-test runs first | 🟢 | worktree: `exit 1`, `1`, `self-test: PASS`, then `FAIL: 16746 em dashes outside inline code spans in 343 files` | R1's fixture changed to expect `\| a \| - \| b \|` in the clone: `exit 1`, `self-test: FAIL 1 case(s)`, `✗ R1 empty md cell: fix produced "\| a \| none \| b \|", expected "\| a \| - \| b \|"`, and `grep -c 'em dashes'` printed `0` (stopped before the scan) |
| 2 | P2 greps: the gate names the glyph only as the escape | 🟢 | `grep -c 'u2014'` `4`, `grep -c` for the literal glyph `0` | a literal glyph written into the source would make the second grep `1` and the gate count its own file |
| 3 | P2 (a): a plain dash appended to `README.md` counts | 🟢 | total `16746` to `16747`, `exit 1` | if README.md's tail paragraph masked the new line, the total would stay at `16746` |
| 4 | P2 (b): the same dash inside a Markdown span is exempt | 🟢 | clean rerun: `FAIL: 16746 ...` (my first attempt leaked (a)'s staged line into (b); rerun on a restored README gave `16746`) | a masker that skips spans would print `16747` |
| 5 | P2 (c): template literal, lone token and escaped token all count; `--fix` leaves all three and lists them as R0 (e) | 🟢 | each form alone: `16747`; after `--fix` the three lines diff empty against the saved copy; listed as `test/run.mjs:35 (e)`, `:36 (e)`, `:37 (e)` | a mask on non-Markdown backticks (mutant M10) or a missing escaped-form match (M7) drops one of these to `16746` |
| 6 | P2 (d): a byte-mode reader fails the self-test before the scan | 🔴 | the self-test reads its temp file with its own `readFileSync(tmp, "utf8")` and never calls the gate's `readText()`. Mutant d1, the self-test's own read set to `latin1`: `exit 1`, `✗ reader: UTF-8 read found 0 dashes, expected 1`. Mutant d2, `readText()` set to `latin1`: `self-test: PASS`, `em-dash: clean (586 files scanned)`, `exit 0`. Mutant d3, a `Buffer` read with `.toString("binary")`: the same `clean`, `exit 0`. The design says the temp file is "read back through the same reader" and P2 (d) names "a `Buffer` read" | d2 and d3 are the controls: each should stop at `self-test: FAIL`, but both go vacuously green over 16,746 real dashes |
| 7 | Planted prose dashes outside spans count (a `.md` sentence with a later span, a `.js` comment, a Markdown table cell, a fenced `#` line, a dash after a stray backtick) | 🟢 | each plant alone: `FAIL: 16747 ...`, `exit 1` | a masker that hid any of these would leave the total at `16746` |
| 8 | 16746 against the plan's 16744 | 🟢 | an independent naive mask (per line, strip backtick pairs in `.md`) on the same tree: `TOTAL 16744 in 343`, so the tree has not drifted. The per-line diff is exactly 10 lines. 6 are dashes outside a span that the naive mask hid by mis-pairing a cross-line span's closer (`geometry-system/SKILL.md:58`, `:181`, `lemon-squeezy-api/references/endpoints.md:238`, `maintaining-brand-kit-mcp/references/foundations.md:114`, `knowledge-02-tonal-scale.md:261`, `2026-07-17-cto-app.md:165`); 4 are dashes inside a real cross-line span that the naive mask counted (`geometry-system/SKILL.md:180`, `maintaining-figma-plugins/references/best-practices.md:19`, `tkt-0031.md:37`, `:82`). I read all 10 lines with context: the gate is right on each. Net `+6 -4 = +2` | if the gate over-masked, the diff would carry lines where the gate is below naive on a dash that sits outside every span; none of the 4 negative lines is such a case |
| 9 | P3: two `--fix` runs, identical, line counts kept | 🟢 | both `329 files changed, 9324 insertions(+), 9324 deletions(-)`; `cmp` on the two `git diff --text` patches: identical; numstat mismatches `0` | a rule that edits its own output again would change the second stat |
| 10 | P3: rule lines and counts | 🟢 | `14` lines: `R0 a 13 R0 b 3 R0 c 1 R0 d 2 R0 e 20 R0 f 1 R1 27 R2 565 R3 441 R4 0 R5 0 R6 281 R7 29 R8 8260`; each R0 count equals its residual list (40 lines) | dropping R0 (f) (mutant M4) moves `export-drift.md:212` into R8 |
| 11 | P3: no range wording, no new double punctuation, lone tokens unchanged | 🟢 | `4` and `4`; double punctuation `100` before and after (base `b3961aa9` `96`, plus `3` in `em-dash.mjs` and `1` in the handoff); lone tokens `39` before and after (base `39`) | a revision-1 style range rule would make the second `to` figure exceed the first |
| 12 | P3: the post-fix FAIL names exactly the R0 lines plus the generated files | 🟢 | `FAIL: 6616 em dashes ... in 28 files`; hits outside the `GENERATED` set are 40 lines, and `diff` against the residual list is empty | a rule that silently skipped a line would add a hit that is not in the R0 list |
| 13 | R0 (a) to (f) are refused, left in place and named, including (f) | 🟢 | residual list carries every construct: `(a)` 13 (the citations, the two store-copy sign-offs, `decomposition.md:77`, `cto-core.md:44`), `(b)` `ds-export.js:1373`, `:1461`, `:1462`, `(c)` `foundations.md:96`, `(d)` `figma-styles-hard-constraints.md:19`, `mcp-hosting-spec.md:259`, `(e)` 20 source tokens, `(f)` `docs/reference/reviews/2026-07-17-export-drift.md:212 (f) \| Color stops (raw) \| ... \| [dash] (mapped indirectly) \|`, byte-identical after `--fix` | mutants M3 (no R0 (a)) and M4 (no R0 (f)) both red the self-test: `matched R8, expected R0` |
| 14 | R1, all 27 hits read | 🟢 | every pair reads as an empty cell said in words, `\| none \|`; the two-cell rows (`component-inventory.md:100`, `export-drift.md:213`, `containers.md:50`) read `\| none \| none \|` with no `\|, \|`; patch scan for `\|, ` added `0` | a first-cell-only R1 would leave `\|, \|` in those rows |
| 15 | R7, all 29 hits read, meaning | 🟢 | each line before ends in a word, span or bracket and gains the comma at the wrapped pause, for example at `color-math/references/best-practices.md:37` the line before gains its comma after the `okhslStops` span and line 37 reads `with the IDENTICAL formula.` | mutant M8 (no comma on the line before) reds the self-test |
| 16 | R7 also strips the continuation line's indentation | 🟡 | `raw.slice(idx + 1).replace(/^\s+/, "")` drops every leading space, so an indented list continuation moves to column 0 (`decision-records.md:235`, five spaces, becomes `editorial voices use ...`; `type-rubric.md:45`, six spaces). None of the 29 starts with a list or heading marker, so rendered Markdown is unchanged (lazy continuation), but the source loses its alignment; the plan says the dash goes, not the indent | a line whose text after the dash began `1. ` or `# ` would render as a new list item or heading at column 0 |
| 17 | R2, 34 of 565 read (seeded sample) | 🟢 | every heading keeps its label and title, for example `## Vol IV: Coastal & Maritime`, `# TKT-0031: Migrate the 18 open file tickets ...`; the only oddity is a doubled colon where the title already had one (`## Matrix 2: TYPE: same concept ...`), which keeps the meaning | a period rule would end the label |
| 18 | R3, 34 of 441 read | 🟢 | labels keep their definitions, for example `- \`expires_at\`: ISO 8601 date-time ...`, `6. **\`src/ui/app.js\`**: the Roles inspector label ...`; R3 also fires on bullets inside program strings (`mcp/describe-rubric.mjs:99`, `:285`), meaning kept | a comma for a label would read as a list |
| 19 | R8 after a line-leading comment or quote marker | 🔴 | 14 lines where the dash follows `//`, `#` or `>` and the rest of the line is blank before it. The line-start test (`line.slice(0, idx).trim() === ""`) sees the marker, so neither R7 nor R0 (d) fires and R8 writes `, ` right after the marker. `docs/marketing/store-copy.md:486`, the customer email sign-off `> [dash] Ultimate Tokens`, becomes `>, Ultimate Tokens`: the sign-off R0 says must keep its author now reads as a dangling clause. The other 13 keep their meaning but open with a stray comma: `.github/workflows/pages.yml` `#, an automatic deploy in flight.` and 12 `//, ...` comment lines (for example `//, see applyFloatPlans below).`) | patch scan `grep -cE '^\+\s*(//\|/\*+\|\*\|#+\|>\|-\|\d+\.)\s*, '` printed `14`; with the marker treated as line-start it would print `0` |
| 20 | R6 and R8 by random sample, 34 each | 🟢 | R6 puts a comma where the sentence wraps (`CLAUDE.md:111` `never reaches a commit,`); R8 gives the ratified comma splice (`motion.mjs:58` `is a CLS defect, animate`); patch scans for `(, `, `, )`, `,:`, `:,`, `, ,`, `,,`, `[, ` each added `0` | mutant M2 (R6 to a period) reds the self-test |
| 21 | `npm test` green after `--fix`, and `--fix` idempotent after regeneration | 🟢 | swept clone, no `node_modules`: `✓ all 48 test files passed`, `exit 0`, `git status --short` `343` (329 swept plus 14 regenerated); the gate then `FAIL: 77 em dashes ... in 17 files` (R0 lines plus their mirrored copies); a third `--fix` prints every rule `0` and the patch is unchanged | a rule that corrupted a pinned string would red `engine/exports.mjs` as revision 1 did |
| 22 | U3-3: not registered, plan tree green | 🟢 | `grep -c '"repo/em-dash.mjs"' test/run.mjs` `0`; unswept clone `npm test`: `✓ all 48 test files passed`, `exit 0`, tree clean `0` | adding `"repo/em-dash.mjs"` to `TESTS` in a clone makes the first command `1` and `npm test` red on the unswept tree (the runner reports `1/N test file(s) failed`); a unit change that broke another suite would red the second |
| 23 | U3-4: the line-78 span keeps its glyph | 🟢 | `grep -c` for the span `SMOKE PASS [dash] gallery` `1` before, `1` after; tree-wide naive in-span tally `82` to `76`, and the 6 lines that moved are exactly row 8's 6 lines, where the dash was outside the real span; the 4 cross-line span lines keep their glyph (`best-practices.md:19`, `tkt-0031.md:82` unchanged) | a `--fix` that stripped spans would print `0` after |
| 24 | Self-test bites: 10 mutants | 🟡 | killed: M1 (R5 without the space rule), M2, M3, M4, M7, M8. Survived: M6 and M9 are equivalent (R0 (b) and R0 (f) fire first). M5, R2 fired outside Markdown: `self-test: PASS`, so R2's Markdown-only condition has no fixture. M10, backticks masked in non-Markdown files: `self-test: PASS` (P2 (c) still catches it on the tree) | a fixture with a non-Markdown `# heading [dash] x` expecting R8 would kill M5 |
| 25 | P8: branding clean, no added line carries the dash | 🟢 | `branding: clean (569 files scanned)`; added lines `b3961aa9..397e69ea` with spans stripped: `0` | one planted `+x [dash] y` line through the same perl prints `1` |
| 26 | P9: the U3 scope wall | 🟢 | `0`; files changed: `.sdlc/handoffs/rule-gates-U3.md`, `test/repo/em-dash.mjs` | `src/ui/styles.css` through the same filter prints `1` |
| 27 | The handoff matches the head | 🟡 | header `HEAD: \`b9f159f2\`` while the branch is `397e69ea`; the fold-in says `13` rule lines with `R0 f 1`, the run prints `14`; the P2 bullet cites the handoff's own pre-pass-3 total `16713` | a reader citing the header's head would grade the wrong commit |

## Notes

- `--fix` rewrites `voice-check.mjs:89`'s pivot regex to `/, (?:not|never|no)\b/`, as the plan predicts; U4 owns the hand edit, so it is not graded here.
- Nothing in this unit touches `src/ui/`, so no smoke run is required.

# Verdict rg-U3 pass 2 · 🟢

Unit `unit/rg-U3` at `f300ac87` (code at `2ebde63a`, plan branch merged in at `40e21900`, so U1 and U6 are on the tree), plan revision 11 (owner ruling B, a positive guard). Verifier-l2, read-only: one `git clone --shared` at `f300ac87` plus a detached worktree at `397e69ea` for the delta, both under this seat's scratchpad and removed by name. Load `2.64 3.44 4.30` at start, `4.84 4.57 4.53` at end.

| Next action | Owner |
|---|---|
| None blocking. U4 reads the 92 refused lines by hand; rows P2-11 to P2-14 are notes for it | Orchestrator |

20 rows: 🟢 16 · 🟡 4 · 🔴 0. The two reds and three yellows from the first pass are all 🟢.

| # | Criterion | State | Evidence | Negative control |
|---|---|---|---|---|
| P2-1 | Was row 6: a byte-mode reader fails the self-test | 🟢 | the self-test now reads the temp file through `readText()`. `readText()` set to `latin1`: `exit 1`, `✗ reader: readText() found 0 dashes, expected 1`. Set to `readFileSync(abs).toString("binary")`: the same FAIL, `exit 1`, and no tree scan | at `397e69ea` both mutants printed `em-dash: clean (586 files scanned)`, `exit 0` |
| P2-2 | Was row 19: a dash after a leading `//`, `#` or `>` is refused | 🟢 | all 14 lines are in the refused list and unchanged after `--fix`, including `docs/marketing/store-copy.md:486 (quote) > [dash] Ultimate Tokens`, `.github/workflows/pages.yml:12 (comment)`, and the 12 `//` lines (`code.js:82`, `:719`, `mode-apply-plan.mjs:361`, `:466`, `exports.js:185`, `prime.mjs:12`, `tonal.js:122`, `model.mjs:709`, `geometry.js:866`, `test/engine/exports.mjs:2123`, `test/figma/binder.mjs:106`, `brand-kit-merged.mjs:4`); patch scan for a marker followed by `, ` printed `0` new | fixtures `R0(g) JS comment marker`, `R0(g) YAML/shell comment marker` and `r0g-refused` red under the guard-off mutant (P2-9) |
| P2-3 | Was row 16: R7 keeps the continuation's indentation | 🟢 | `decision-records.md:235` keeps its five spaces (`     editorial voices use one-or-two registers.`); `type-rubric.md:45` keeps its six | a mutant back to `raw.slice(idx + 1)` gives `✗ R7 keeps the continuation's indentation`, `exit 1` |
| P2-4 | Was row 24: R2 fires only in Markdown | 🟢 | a new fixture, `R2 does not fire outside Markdown` | mutant M5 (the `md &&` dropped): `self-test: FAIL 2 case(s)`, `✗ R2 does not fire outside Markdown: matched R2, expected R8` |
| P2-5 | Was row 27: the handoff matches the head | 🟢 | header `HEAD: \`2ebde63a\``, the last commit that touches code (`f300ac87` changes only the handoff); its pass-5 figures match my run: `FAIL: 16688`, `329 files changed, 9256 insertions(+), 9256 deletions(-)`, `R0 92` | a stale header would name a head that my run does not reproduce |
| P2-6 | U3-1 and the 16688 total | 🟢 | `exit 1`, `1`, `self-test: PASS`, `FAIL: 16688 em dashes outside inline code spans in 343 files`. Per-file diff of the gate's own counts at `397e69ea` and `f300ac87`: net `-58`, all in U6's files (`ui.html -28`, `color.js -7`, `describe-mcp-assets.js -5`, `typography.js -5`, `ds-export.js -4`, `geometry.js -4`, and 1 each in `foundations.md`, `brand-kit-core.mjs`, `app-helpers.mjs`, `app.js`, `mcp-assets.js`); `16746 - 58 = 16688` | R1's fixture changed to expect `\| a \| - \| b \|`: `self-test: FAIL 1 case(s)`, `exit 1`, `grep -c 'em dashes'` `0` |
| P2-7 | P2 (a) to (c) and planted dashes | 🟢 | (a) `16689`; (b) span `16688`; (c) template literal, lone token and escaped token each `16689`, and `--fix` leaves all three, listed as `test/run.mjs:35 (e)`, `:36 (e)`, `:37 (e)`; plants in `.md` prose, a `.js` comment and a table cell each `16689` | a masker that hid any plant would leave `16688` |
| P2-8 | P3: identical runs, counts, invariants | 🟢 | both runs `329 files changed, 9256 insertions(+), 9256 deletions(-)`, patch `cmp` identical; numstat mismatches `0`; `R0 92 R1 27 R2 565 R3 439 R4 0 R5 0 R6 280 R7 29 R8 8192`; `to` pairs `4` and `4`; double punctuation `100` before and after; lone tokens `0` | a second run that edited its own output would change the stat |
| P2-9 | Mutants: guard off, before side, `\p{L}` | 🟢 | all three guard functions forced true: `self-test: FAIL 17 case(s)`. `guardHolds` alone forced true: `FAIL 15`. Before-side test removed (U3-5): `exit 1`, `grep -c '^self-test: FAIL'` `1`, with the legend, question, aligned-column and chart fixtures among the 13 fails. `guardBeforeHolds` forced true: `FAIL 1` (the `<slug> [dash]` R6 fixture). After-side test removed: `FAIL 1`. `guardAfterHolds` forced true: `FAIL 1`. Space check removed: `FAIL 1` (the glued name). `\p{L}\p{N}` back to `A-Za-z0-9`: `FAIL 2`, `✗ guard: Unicode letter before the dash: matched R0, expected R8` | each line is a mutant; every one reds before the tree scan |
| P2-10 | U3-5 greps, and the re-diagnosis's 8 constructs refused | 🟢 | after `--fix`: `1`, `1`, `1`. In the refused list and unchanged: `src/ui/app.js:2374 (comment)`, `src/ui/sections/color.js:1330 (comment)`, `ui-plan.md:111 (fence)`, `ds-export.js:1495 (string) "## Which variant? [dash] decision tree"`, `type-scale/SKILL.md:81` and `:82 (list)`, `type.mjs:526 (comment) relTrackEm[dash]`, `style-plan.mjs:42 (comment)`, `README.md:119 (fence)`, `component-inventory.md:461` to `:465 (list)`; also `export-drift.md:212 (table)` and `voice-check.mjs:89 (comment)` | the pass-3b code at `397e69ea` rewrote all of these (first verdict row 19; re-diagnosis §1) |
| P2-11 | The refused list: complete, under 120, one habitat per line, all 92 read | 🟢 | hits after `--fix` outside the `GENERATED` set are 92 lines, and `diff` against the list is empty; habitats `comment 32`, `string 25`, `paragraph 20`, `list 9`, `quote 3`, `fence 2`, `table 1`. Read all 92: each is a dash the guard cannot call a pause (a full stop, a marker, a slash path, `?`, `✅`, `>`, a glued name, a line-end label) | a refused line that the gate did not also count would show up in the `diff` |
| P2-12 | A refused line can still be partly rewritten | 🟡 | `classifyLine` tests the guard only on the line's first remaining dash, so when an earlier dash passes and a later one fails, the earlier one is rewritten and the line is then refused. 5 lines leave `--fix` both edited and listed, with a dash pair split in half: `rubric.md:3` (`\`brand-kit-core.mjs\`, the surface [dash] + ...`), `app.js:690` (`role=button div, NOT a <button> [dash] so`), `ds-export.js:776`, `decomposition.md:77`, `brands.json:1960`. The code comment says `refused, whole line, no guess`; the refused dash does stay in place, as the plan asks. U4 rewrites these 5 by hand either way | a guard check over every dash before any edit would print `0` changed refused lines; my check printed `5` |
| P2-13 | Guard over-refusals (plan: a line the guard should have passed is a 🟡 on the guard) | 🟡 | about 20 of the 92 are real word pauses the guard refuses because the character before the dash is `/` (a path: `drawer.js:387`, `:398`, `:407`, `:411`, `CLAUDE.md:115`, `marketing-manager-agent.md:4`, `project-docs/SKILL.md:44`), `>` of a closing tag (`brands.json:5`, `:1808`, `travel.json:4638`, `code.js:495`, `plugin/code.js:773`), or `%` or `×` after the dash. None is changed, so the only cost is hand edits in U4 | a guard that let `/` and `</tag>` through would move these to R8 |
| P2-14 | Every source auto-fix line (`src mcp figma scripts`, 3026 added lines) | 🟡 | read all 660 lines that are not comment-only (strings, trailing comments, CSS comment bodies), plus a seeded 60 of the 2366 comment lines; `npm test` stays green. No line changes behaviour. Label dashes inside program strings fall to R8 rather than R2 or R3, so user-visible labels become lists: tooltips `Color, palettes, scrims & semantic roles`, card titles `Tone curve, L* per stop`, exported headings `## Hard rules, IMPORTANT` and `# Components, Overview`, the MCP guide `# ${name}, usage`, and the Figma notice `Please try again, if it keeps happening, open an issue`, which now reads less clearly. Meaning is kept but clarity drops; this is the ratified comma set applied to program strings | patch scans for `(, `, `, )`, `,:`, `:,`, `, ,`, `,,`, `[, `, `//, `, `#, `, `>, ` each added `0` |
| P2-15 | 50 random Markdown prose pairs (seeded, aligned hunks) | 🟢 | all 50 keep the sentence's meaning, for example `## Vol VIII: Rail & Transit`, `(a) truly the same concept viewed at two grains, fine, or (b)`, `**B2 [MEDIUM]**, geometry.js has a *third* ...`, `1. **\`exports.js\`: \`exportTailwind(state)\`**, opened ...` | a period rule or a range rule would show up as a pair that ends the sentence early |
| P2-16 | Refused-list habitat labels | 🟡 | `src/ui/styles.css:6` and `:106` are CSS comment lines labelled `(string)` because the line quotes an HTML tag; the label is text only and affects nothing | a CSS-aware label would print `(comment)` |
| P2-17 | `npm test` green after `--fix`, idempotent after regeneration | 🟢 | swept clone, no `node_modules`: `✓ all 49 test files passed`, `exit 0`, `git status --short` `343`; the gate then `FAIL: 206 em dashes ... in 57 files` (the refused lines plus their mirrored copies); a third `--fix` prints `R0 92` and every other rule `0`, patch unchanged | a rule that corrupted a pinned string would red a test file |
| P2-18 | U3-3: not registered, plan tree green | 🟢 | `grep -c '"repo/em-dash.mjs"' test/run.mjs` `0`; unswept clone `npm test`: `✓ all 49 test files passed`, `exit 0`, tree clean `0` | adding `"repo/em-dash.mjs"` to `TESTS` in a clone makes the first command `1` and `npm test` red on the unswept tree (the runner reports `1/N test file(s) failed`); a unit change that broke another suite would red the second |
| P2-19 | U3-4: the line-78 span keeps its glyph | 🟢 | `grep -c` for the span `SMOKE PASS [dash] gallery` `1` before `--fix` and `1` after | a `--fix` that stripped spans would print `0` |
| P2-20 | P8 and P9 against the merge base with the plan branch (`dab214b6`) | 🟢 | files changed: `.sdlc/handoffs/rule-gates-U3.md`, `test/repo/em-dash.mjs`; wall `0`; added lines carrying the dash with spans stripped `0`; `branding: clean (587 files scanned)` | `src/ui/styles.css` through the wall filter prints `1` |

# Verdict rg-U3 pass 3 · 🟢

Head `b41c87ea` (delta from `f300ac87`: `3068fc32` changes 8 lines of `test/repo/em-dash.mjs`, plus the handoff). A throwaway clone at `b41c87ea`, removed afterwards. Load `5.20 4.91 4.67`.

| # | Criterion | State | Evidence | Negative control |
|---|---|---|---|---|
| P3-1 | The delta changes only the R6 before-side fixture text and the habitat fallback; self-test, totals and refusals hold | 🟢 | the fixture line is now `mode-apply-plan.mjs:289`'s real comment; the habitat fallback returns `string`, so `voice-check.mjs:89` now reads `(string)`, not `(comment)` (habitats `comment 31`, `string 26`). The gate prints `self-test: PASS` then `FAIL: 16688 em dashes ... in 343 files`, `exit 1`; the fix prints `R0 92 R1 27 R2 565 R3 439 R4 0 R5 0 R6 280 R7 29 R8 8192` | before-side test removed: `self-test: FAIL 13 case(s)`, `exit 1`; `guardBeforeHolds` forced true: `self-test: FAIL 1 case(s)`, `✗ guard: R6 before-side (placeholder before a line-end dash): matched R6, expected R0`, `exit 1` |

Pass 2's yellows P2-12, P2-13 and P2-14 are unchanged by this delta. P2-16 is unchanged too: `styles.css:6` and `:106` are still labelled `(string)`.
verdict: 🟢
