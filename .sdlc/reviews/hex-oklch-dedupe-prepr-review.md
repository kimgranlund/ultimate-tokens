PASS

# Pre-land review · hex-oklch-dedupe (#731) · plan/hex-oklch-dedupe @ 600763ce

Reviewer: hex-oklch-dedupe-prepr-reviewer (reviewer-l4, fable), fresh context, 2026-09-25.
Scope: `git -C .worktrees/plan-hex-oklch-dedupe diff origin/main...plan/hex-oklch-dedupe` (10 files, 2 of them generated binaries), the 19 commits on the branch, the plan at `.sdlc/plans/hex-oklch-dedupe.md`, the unit verdict, handoff and review. Read only; no npm test or build run (the Verifier's leg). Read-only repo scanners were run in the worktree: `test/repo/citations.mjs`, `test/repo/branding.mjs`, `.sdlc/checks/baseline-agrees-check.sh`.

## Checks

| Check | State | Evidence |
|---|---|---|
| Source change is the ticket and nothing else | 🟢 | `src/ui/model.mjs` 1 added / 20 deleted: `anchorRgbOf`, `rgbToOklchLocal` and their five-line comment gone (`:871`, `:874-878`, `:880-892` at B), the anchored branch of `deriveKeyColor` now `return { keyOklch: hexToOklch(keyHex), keyHex }` (`src/ui/model.mjs:894`). `hexToOklch` (`src/ui/model.mjs:836`) is `rgbToOklchArr(hexToRgb(...))`, exported, declared 58 lines above its new caller; same matrices, the only formal difference the hue wrap, equal for every atan2 output. `ANCHOR_HEX` and its two surviving comment lines (`:871-872`) still read correctly with the lead line gone |
| Integration (one unit, no inter-unit seams) | 🟢 | Single unit. The new test block (`test/ui/model.mjs:367-394`) imports `hexToOklch` from the same module, walks `projectView(defaultDocument())` and deep-equals `keyOklch` against `hexToOklch(anchor)` on the 16 anchored default-kit families; pinned pass line `keyOklch agrees with hexToOklch: subjects <n>` matches plan U1-4's grep |
| Dead code left behind | 🟢 | `grep -rn 'rgbToOklchLocal\|anchorRgbOf\|identically-scoped\|no shared full-triple'` over `src/ test/ docs/ scripts/ mcp/ plugin/ figma/binder CLAUDE.md .claude/`: no hit in live source or docs. Remaining hits are the engine's own private copies (`src/engine/prime.mjs:90,100`, `test/engine/fixtures/prime-pre-681.mjs:97`, out of scope per the plan's Q1 and Not-in-scope) and historical `.sdlc` records (`pif-u7-review-1.md`, `handoffs/pif-u7.md`, `plans/archive/small-fixes.md`), which describe the tree they were written on |
| Stale docs the diff invalidates | 🟢 | The one shifted citation is repaired: `docs/reference/reviews/2026-08-20-reactivity/02-sections-and-resolvers.md:41` `model.mjs:1100` to `:1081`; line 1081 at the head is the `shadcn: exportShadcn(..., radii: shadGeom.radii ...)` line. `node test/repo/citations.mjs`: `STALE 0 across 10 discovered docs (HEAD 600763ce)`. `CLAUDE.md` and `docs/reference/` carry no `model.mjs` line citations touched by the shift. `.sdlc/baseline.md` `npm run build` KB cell 4125.3 to 4122.4 plus a dated correction paragraph; `sh .sdlc/checks/baseline-agrees-check.sh`: `stale total: 0`, `ref: main @ a62ec020` still in origin/main's history |
| Generated artifacts regenerated with the source | 🟢 | `figma/plugin/ui.html` (4260330 to 4257383 bytes) and `src/ui/describe-mcp-assets.js` (822923 to 821440 bytes) both inline `model.mjs` (`scripts/bundle.mjs:59`, `scripts/gen-describe-mcp-assets.mjs`); both shrink, consistent with a 19-line net deletion. Both admitted by P4 |
| Scope wall (P4) | 🟢 | Touched set: `src/ui/model.mjs`, `test/ui/model.mjs`, `figma/plugin/ui.html`, `src/ui/describe-mcp-assets.js`, `.sdlc/{baseline,board}.md`, `.sdlc/{plans,handoffs,verdicts}/hex-oklch-dedupe*`, the one review doc. Every name is in P4's exclude list after its two recorded revisions (plan Revisions table 2026-09-25 and 2026-09-26). Nothing under `src/engine`, `test/run.mjs`, `scripts`, `.github`, `package.json` |
| Dependency changes | 🟢 | `package.json` and `package-lock.json` untouched; no `node_modules` path in the diff |
| Secrets or config smells | 🟢 | No `.env`, key, token or settings file in the touched set; no `.claude/docs/other/` path |
| Em dashes on added lines | 🟢 | Perl U+2014 scan over every added line of the text diff (handoffs included, backtick spans kept): 0 |
| Retired maker brand in `.sdlc` | 🟢 | `node test/repo/branding.mjs`: `branding: clean (729 files scanned)`, `.sdlc/` in scope |
| Commit hygiene | 🟢 | 19 commits, all authored Kim Granlund with a Co-Authored-By trailer; every `.sdlc`-only commit made by the Orchestrator carries `Seat: orchestrator`; unit commits carry `(#731)`; one merge commit (`52ad7ddc`) has the default merge subject and no trailer, acceptable for a branch that will squash |
| Shipped-text claims (`scripts/claims.py`) | 🟡 | The pre-land-review skill says to rerun `scripts/claims.py` on the plan head; the script does not exist in this repo (`ls scripts/claims.py`: no such file). Nothing to run, noted so the record does not imply it ran. The diff adds no user-facing text, so the class is empty here regardless |

## Findings, ranked

1. LOW, records: the plan branch sits two record-only commits behind `origin/main` (`47046684` the pass-2 🟢 verdict, `0b2c3fc1` the board mirror), so `plan/hex-oklch-dedupe:.sdlc/verdicts/hex-oklch-dedupe-U1.md` still carries only pass 1 (🔴 at f972da4f) while `origin/main` holds the pass-2 🟢 record. The branch did not touch that file after the merge base, so a squash lands main's copy unchanged; `.sdlc/board.md` is byte-identical on both sides (`git diff origin/main plan/hex-oklch-dedupe -- .sdlc/board.md` empty). No conflict, no action needed to land; a final `origin/main` merge into the plan branch would make the branch self-consistent but would also move the head past the reviewed sha.
2. LOW, test: `test/ui/model.mjs:375-378`'s fallback (build one anchored palette when the default kit carries none) runs only after `ok(anchored.length > 0, ...)` has already recorded a failure, and it clones `doc.palettes[0]` under the same name, so `view.palettes.find((p) => p.name === src.name)` would hit the original unanchored palette first. Unreachable on the tree it lands on (16 anchored families) and the row still reds on the assertion before it, so this is a comment-vs-code mismatch, not a gap in what the test proves. The comment at `:369-371` says the fallback keeps the subject count from "silently" falling to 0; the `ok()` above it is what does that. Follow-up nit for whoever next touches the block.
3. NOTE: the unit review's nit 5 (handoff Branch row naming an older sha) was overtaken: the handoff on the branch now names `ed5b716a` and the head is `600763ce`; the two later merges and the board commit touch no code, so every result the handoff states still describes the head's `model.mjs`.

## Verdict

PASS. The diff is the ticket: one conversion in `model.mjs`, the false comment gone, an agreement assertion that bites (its control in the unit review and verdict), generated bundles and the two records the shrink invalidated repaired in the same branch. No blocking finding.
