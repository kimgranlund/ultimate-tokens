# verdict-backfill U3 review · reviewer-l2 pass 1 · unit/bf-U3 @ c9c01d41

Diff `4fecc97d..c9c01d41`: four paths (`M .sdlc/adapter.md`, `M .sdlc/checks/verdict-frontmatter-check.sh`, `D .sdlc/checks/verdict-frontmatter-grandfather.txt`, `A .sdlc/handoffs/verdict-backfill-U3.md`). Every control that edits a file ran in `git clone -q --shared` under `$TMPDIR/bfU3-rev-l2.*`, never in the worktree. The worktree's tree was clean after, apart from this file.

## Findings

| Id | State | Evidence | Negative control |
|---|---|---|---|
| U3-1 | 🟢 | `test -e "$L"; echo $?` printed `1`; the non-comment grep for `PIN`/`grandfather`/`GROWN`/`STALE`/`CLEARED`/`cat-file`/`execFileSync` printed `0`; `grep -n -e git -e child_process` hits only header line 5 (`no git read`) | the 4fecc97d script, per the plan's measured table, prints `0` and `15`; the diff shows every removed line (`PIN=f685529f`, `git cat-file`, `execFileSync`, the four red branches) |
| U3-2 | 🟢 | `verdicts 83 graded 83 bad 0`, `exit 0`, then `grep -c grandfathered` printed `0` | the plants in U3-3 turn the same line to `bad 1` |
| U3-3 A (field-less new file) | 🟢 | `MISSING zz-control-U1.md: no verdict: line`, `verdicts 84 graded 84 bad 1`, `exit 1` | clean tree prints `bad 0`, `exit 0` (U3-2) |
| U3-3 C (prose value) | 🟢 | `VALUE zz-control-U1.md: last verdict: pass is not 🟢, 🟡 or 🔴`, `bad 1`, `exit 1`; extra plant `printf 'verdict: pass\n' >> survey.md` printed `VALUE survey.md: last verdict: pass ...` (the last-line rule holds over an earlier valid line) | clean tree `bad 0` |
| U3-3 N1 (backfilled `survey.md` rewritten field-less; its last touch is `sdlc(verdict-backfill): U1, the 31 title-token records carry verdict: (#734)`) | 🟢 | `MISSING survey.md: no verdict: line`, `verdicts 83 graded 83 bad 1`, `exit 1` | the same plant in a checkout of `3e1483e4` with its own script and list: `verdicts 79 graded 32 grandfathered 47 bad 0`, `exit 0` (the hole N1 named, reproduced) |
| U3-4 shallow clone | 🟢 | `git clone -q --depth 1 --no-local`; `git log -1 --format=%h` printed `c9c01d4`, `git rev-parse --is-shallow-repository` printed `true`; the check printed `verdicts 83 graded 83 bad 0`, `exit 0` | the `3e1483e4` script in that clone: `PIN unreadable: cannot read commit f685529f (no git, no repo, or shallow clone)`, `exit 1`. Stronger: a copy of `.sdlc/` alone, no `.git`, `PATH` holding only `sh`/`node`/`tail`: `command -v git` printed nothing (`nogit`), the check printed `verdicts 83 graded 83 bad 0`, `exit 0` |
| U3-5 adapter additive, names #734, no-git, diff-filter | 🟢 | `git diff --numstat "$B" -- .sdlc/adapter.md \| cut -f2` printed `0` (`2 0` against 4fecc97d); §6 counts `#734` `1`, `shallow clone`/`no git` `1`, `diff-filter` `1` | the plan's head adapter prints `0`, `0`, `0` on the three counts; any in-place reword of the #723 paragraph would print a nonzero deletions column |
| U3-5 claims vs script | 🟢 | each script claim in the amendment holds by run: `reads files only` and `needs no git` (the no-git run above), `every top-level .md` (`readdirSync(".sdlc/verdicts").filter(.md)`, no recursion; `ls -d .sdlc/verdicts/*/` printed `no matches found`), `MISSING`/`VALUE` alone (U3-1 grep `0`) | a claim of `STALE` or `CLEARED` surviving would contradict U3-1's `0` |
| F1 adapter overclaims the deletion guard | 🟡 | the amendment's last sentence reads `the scope wall each plan states (a \`--diff-filter=DR\` guard on \`.sdlc/verdicts\`)`; `grep -c 'diff-filter=DR'` over `.sdlc/plans/*.md` prints `2` for `verdict-backfill.md` and `0` for the other eleven, and `grep -l 'diff-filter=DR' .sdlc/plans/archive/*.md \| wc -l` prints `0` | the plan's own wording is the source (`the scope wall of every plan (\`--diff-filter=DR\` on \`.sdlc/verdicts\`, P3 here)`), so the builder followed it; the sentence is still false as a standing contract. Suggested wording: `a plan's scope wall, where it carries a \`--diff-filter=DR\` guard on \`.sdlc/verdicts\` as \`verdict-backfill\`'s P3 does, is what catches that`. Cheap now; after landing the paragraph is append-only |
| U3-6 em dash in script | 🟢 | `perl -CSD -ne 'print if /\x{2014}/' ... \| wc -l` printed `0` | one glyph in the header prints `1` (the row's stated control; the head used `--`) |
| U3-7 N1 to N6 table | 🟢 | the handoff table has six rows `N1` to `N6`; N1 cites the run reproduced above; N3 says `This check was never the deletion guard and still is not` and my run printed `verdicts 82 graded 82 bad 0`, `exit 0` after `git rm -q .sdlc/verdicts/survey.md`, then `git diff --cached --name-only --diff-filter=D -- .sdlc/verdicts \| wc -l` printed `1` | a handoff claiming the check catches N3 would contradict `bad 0`, `exit 0` above |
| F2 handoff wording nits | 🟡 (non-blocking) | the `Ran` row has four cells in a two-column table (`\| Ran \| see rows below \| \`npm test\` ✅ 48/48 ...`); N2 reads `was easy to miss` where the plan's N2 is `moving both pins together passes` (an attack, not an omission) | closure text for N2 (`No pin anywhere in the script`) is correct either way; `grep -c PIN` on non-comment lines printed `0` |
| U3-8 P1 | 🟢 | in the clone at `c9c01d41`: `npm test \| tail -1` printed `✓ all 48 test files passed`, TESTS count `48`, `git status --short \| wc -l` `0` (regression guard: nothing on the test path moved) | adapter §1's control (`scrimX` in role-table) is the stated red; not rerun here |
| U3-8 P2 | 🟢 | `branding: clean (575 files scanned)`, `0`, `0` | a copied `decision-records.md` under `.sdlc/verdicts/` reds branding (the row's stated control) |
| U3-8 P3 scope wall | 🟢 | `0`, `0`, `0` against `B=3e99fdca`; `git diff --name-status 4fecc97d..HEAD` lists only the four unit paths; `git diff --name-only "$B" -- .sdlc/verdicts \| grep -c 'pif-'` printed `0` | the planner's three-name fixture prints `2` through the first filter |
| U3-8 P4 (U3 form) | 🟢 | `verdicts 83 graded 83 bad 0`, `exit 0`, `test -e "$L"` `1` | U3-3 plants |
| U3-8 P7 | 🟢 | all five `.sdlc/checks/*.sh` printed `exit 0` | U3-3 A turns `verdict-frontmatter-check.sh` to `exit 1` |
| Live references to the deleted list | 🟢 | `git grep -l 'verdict-frontmatter-grandfather' -- ':!.sdlc/plans/archive' ':!.sdlc/verdicts' ':!.sdlc/handoffs'` prints only `.sdlc/adapter.md` (the #723 paragraph, now history) and `.sdlc/plans/verdict-backfill.md` | a live script or test naming the file would appear in that list; none does |

## Summary

The check does what the plan asks: no list, pin or git path, every top-level verdict graded, `MISSING` and `VALUE` bite on a new file and on a backfilled one, and it runs in a shallow clone and with no git at all. No `pif-*` or other lane's record is touched. One accuracy defect in the adapter sentence (F1), inherited from the plan's wording; worth a one-clause fix before the paragraph lands on main. F2 is cosmetic.

verdict: 🟡
