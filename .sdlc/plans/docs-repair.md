---
status: draft
ticket: none yet; `adapter.py create --title docs-repair --label kind:chore --label lane:docs` at activation (Q0)
priority: P2
lane: docs (`docs/lld/`, `docs/reference/`, `README.md`, `figma/README.md`, the `building-editor-sections` skill, three source file headers, `test/repo/citations.mjs`)
size: M+M+M+S+S+S+S (U1 M = 2, U2 M = 2, U3 M = 2, U4 S = 1, U5 S = 1, U6 S = 1, U7 S = 1; 10 points)
labels: kind:chore · lane:docs · size:M (to mint on the ticket)
written: 2026-09-25
depends: nothing on another plan. `origin/plan/chroma-floor` (#701) differs from `origin/main` in `.sdlc/` only (`board.md`, its own plan, handoffs and question), so this plan runs in parallel with it; the one shared file is `.sdlc/board.md`, Orchestrator-owned on both
head: 282fca8d (`origin/main`; `plan/docs-repair` is cut from it and carries this file only)
measured-at: 282fca8d in the planning worktree `/private/tmp/claude-501/docs-repair-plan`, 2026-09-25, plus one throwaway `--shared` clone for the citations-gate probe, removed by name
inputs: the docs audit of 2026-09-25 (`/private/tmp/claude-501/docs-audit-2026-09-25.md`, the Explore scout's read-only pass on main); owner ruling R42 of 2026-09-25 approving the repair direction (relayed by the Conductor; no committed question file carries it, so this plan's revision rows are its record); `.sdlc/adapter.md` §1, §2.1, §3, §4 (X9), §5; `.sdlc/architecture.md` §8 (the DD rows that quote `.claude/CLAUDE.md` by line); `.sdlc/baseline.md`; `.sdlc/plans/archive/okl-memo.md` and `small-fixes.md` (shape); `docs/reference/CHANGELOG.md` 1.11 (the ruling that the spec-cell id keeps its HCT name)
---

# Docs repair: the UI record catches up with three sections (R42)

The reference docs describe a one-section colour tool. `docs/reference/references/ui-plan.md`, the spec `docs/lld/app-shell.md` traces to, was last edited 2026-07-30 and knows Color only, with `T8 export: 5 formats`; the app has had Typography and Geometry sections, breakpoint modes, a Compare view, per-section inspectors and ten colour formats for two months. Four claims in the live docs are wrong against the code (colorMode's states and persistence, the voice count, a method name, five helper locations), and a handful of small ones are stale (a line-number citation, a dead link, an old product name in a header, a missing header, a citation into a private folder; the fact sheet's "59" sentence turned out to be accurate history, see the table). The audit graded ten areas; this plan repairs what it found, links the two UI records from the places a reader starts, adds a `figma/README.md`, and pins the four repaired claims so the next drift reds `npm test`.

What re-verification changed. Every audit finding was re-read at 282fca8d before it was planned on (table below). One is stale: `scripts/audit-citations.mjs` is already inside `npm test` (`test/repo/citations.mjs`, in `TESTS` since #658, with a parser self-test and #672's synthetic negative controls). Item 6 of the ask is therefore not "wire it in" but "prove it bites and close the gap it cannot see": the gate checks that a cited line still carries its anchor, not that a claim is true, which is exactly how `11 voices` and `light | dark | both` survived with STALE 0. U6 adds a fact-pin leg for those claims and a plan-level control proves the existing gate reds on a bumped citation (measured: exit 1, `STALE` on the bumped line).

Scope wall. Paths this plan may change: `docs/reference/references/ui-plan.md`; `docs/reference/SKILL.md`; `docs/reference/references/spec-draft.md`, `docs/reference/rubrics/acceptance-criteria.md`, `docs/reference/rubrics/quality-rubric.md` (format-count strings only); `docs/reference/references/glossary.md`; `docs/reference/references/component-inventory.md`; `docs/reference/app-shell-patterns.md`; `docs/lld/app-shell.md`; `.claude/skills/building-editor-sections/SKILL.md`; `figma/README.md` (new); `src/ui/app.js`, `src/ui/persist.js`, `src/engine/geometry.mjs` (comment lines only, P4's middle command counts non-comment changes and must print `0`); `test/repo/citations.mjs`; `.claude/CLAUDE.md` (U5 only, after Q1 yes, line count unchanged); `README.md` (U7, one new section before `## License`); `.sdlc/architecture.md` (U7 only: the line number in the one DD row that quotes a `README.md` line below the insertion point, `DD41` at `README.md:147`, and nothing else in the file); the generated files `npm test` rewrites because a bundled source comment changed (`figma/plugin/ui.html` and `src/ui/describe-mcp-assets.js`, which embeds `geometry.mjs` and `persist.js`; `src/ui/*-assets.js` otherwise, `src/ui/categories/*.js` and `docs/img/` only if the generators move them, which the builder reports); `.sdlc/baseline.md` for the `npm run build` row's `ui.html` KB figure and one correction paragraph (the okl-memo revision 7 precedent: the bundle keeps comments, so a header line grows it); this plan, its own handoffs, verdicts, reviews and questions (`.sdlc/{plans,handoffs,verdicts,reviews,questions}/docs-repair*`); `.sdlc/board.md` (Orchestrator). Nothing else under `src/`, no `test/run.mjs` (no test file is added, N stays 50), nothing under `docs/marketing/`, `docs/site/` or `docs/spec/`, no other line of `.sdlc/architecture.md` (U5 keeps `.claude/CLAUDE.md` at 115 lines so no DD row moves; U7's insertion point moves exactly one), no `scripts/`.

Prose rules for every line this plan adds. No em dash outside an inline backtick span that quotes program output; this plan quotes none, so its raw count is `0`. No bold inline labels. The retired maker brand and the pre-rename element identifier are paraphrased, never written (`test/repo/branding.mjs` scans `.sdlc/`). Criteria needles are function names, ids, strings and counts, never line numbers (R10); the Measured table cites line numbers as "at 282fca8d" for the builder's orientation only. `grep -P` is absent on this host: PCRE runs through `perl`. The shell is zsh, where `${PIPESTATUS[0]}` is empty: a command that needs a pipe's exit code runs under `bash -c 'set -o pipefail; ...'`.

Diff bases. `git fetch -q origin` first, then every row diffs against `B=$(git merge-base origin/main HEAD)`, never against the plan branch's own tip; on a unit branch after the Orchestrator's merge that is the `origin/main` tip the plan was activated on, and it sees the unit whole. Every negative control that edits a file runs in a throwaway clone (`git clone -q --shared . "$F/neg"`) made from the unit's commit, never in a unit worktree (`git -C "$F/neg" rev-parse --short HEAD` prints the unit head; a clone made before the commit exercises the pre-fix file, adapter §1). Inside such a clone `origin` is the local checkout, not GitHub, so a control never computes `B` there: the seat computes `B` once in the unit worktree and passes the sha into the clone (`git -C "$F/neg" diff "$B" ...`). `F` is a directory the seat makes under its own scratchpad with a name no other seat would pick, and removes by that exact name. In table cells `\|` is the escape for a plain `|`: type it unescaped. `N` is the length of `TESTS` in `test/run.mjs`, read by command: `50` at 282fca8d, and this plan registers no test file.

Criteria ids: P rows for the plan, numbered rows for a unit, cited as U3-2. The unit order is U3 first (it owns every `app-shell.md` and section-skill edit, so no two units touch one file), then U1, U2 and U4 in parallel, then U6 and U7 (both read U1's Revision B), U5 when Q1 is answered yes.

## Re-verification of the audit at 282fca8d

| Audit row | Finding | At 282fca8d | Planned in |
|---|---|---|---|
| `docs/lld/app-shell.md` B+ | §3.2 says `colorMode` is `light \| dark \| both` and never persisted; the code adds `system` and persists it | holds, and wider: the §3.2 table also lists `theme` and `canvasTheme` under "never persisted", while `app.js` writes `{ theme, canvasTheme, colorMode, motion, fontMode }` to `localStorage` under `_appPrefsKey()` and reads them back on boot | U3 |
| same | the Typography wireframe says `11 voices`; `type.mjs` has fifteen | holds (`(11 voices × steps`); `makeVoices()` returns 15 keys | U3, U6 pins it |
| same | `mixinInto` cited at `app.js:2570`, defined at `:2557`, called at `:2581` | holds; the audit gate reads it `NEAR` (`src/ui/app.js:2568 has mixinInto (-2)`), so the gate cannot red it | U3 |
| `docs/reference/app-shell-patterns.md` B | links `~/.claude/skills/ui-patterns/...`, which does not exist | holds; the file lives in the `frontend` plugin as `skills/ui-pattern-facts/references/archetype-productivity-shell.md` (plugin cache), no copy in this repo | U3 |
| `component-inventory.md` B | the factory table places `switchControl`, `swatch`, `btn`, `chip`, `field` in `app.js` | holds; all five are `export const` in `src/ui/app-helpers.mjs`; `segmented()` and `slider()` are still `app.js` methods, so those two rows stay | U3 |
| `building-editor-sections` B+ | names `deleteMode`, which does not exist | holds; the methods are `deleteTypeMode(id)` (`typography.js`) and `deleteGeomMode(id)` (`geometry.js`); no `deleteMode` anywhere under `src/ui` | U3 |
| `ui-plan.md` + `docs/reference/SKILL.md` D | ui-plan last edited 2026-07-30, Color only, `T8 export: 5 formats`; the index is still named after the old spec | holds. Also stale in the same family: `docs/reference/SKILL.md` says `five export formats` (intent), `all five exports`, `each of the eight color formats`, `all five exporters` (contract checks), `the eight color formats` (reference map); `spec-draft.md` says eight and five; `acceptance-criteria.md` AC-X1 and `quality-rubric.md` say eight; `app-shell.md` SPEC-R8 says `5 formats` twice. The code: `exports.js` documents ten colour formats, the drawer's Colors group lists ten tabs. On the name: CHANGELOG 1.11 ruled the cell id `spec.system.hct-palette-generator-spec` stays (HCT is the colour model), and `decomposition.md` cites it; the human-facing title and description are what drifted | U1 (ui-plan), U2 (index and counts), U3 (the two `app-shell.md` lines), Q2 (the name) |
| `src/engine` headers A | `geometry.mjs` cites `.claude/docs/references/{geometry.md, ...}`, not in the repo | holds; `find docs .claude -name geometry-sizing-spec.md` finds nothing tracked; the path is the owner's private folder, which no record may cite (adapter §3) | U3 |
| `src/ui` headers B+ | `app.js` line 1 says `the DOM app for the HCT Palette Generator`; `persist.js` opens on an `import` with no header | holds. `model.mjs` and `exports.js` carry the same old product name in their headers and are Not in scope (the ask names `app.js` only; CHANGELOG 1.11 kept engine-side HCT naming on purpose) | U3 |
| `CLAUDE.md` A- | no link to `app-shell.md` or the component catalog | holds (`grep` finds neither in `.claude/CLAUDE.md`, `docs/reference/SKILL.md`, or the section skill) | U5 (CLAUDE.md, Q1), U2 (index), U3 (skill) |
| `README.md` B | no tour of views/sections | holds; the headings are What it does, Quick start, Build, Test, Layout, Figma plugin, License, and no section walks the gallery, the categories or the three editor sections. 31 DD rows in `.sdlc/architecture.md` §8 quote `README.md` by line, the highest at `:147` (DD41, the license line), so a section inserted before `## License` moves that one row and no other | U7 |
| `figma/` C+ | no `figma/README` | holds; `figma/` has `binder/` (six planner modules plus `figma-semantic-binder/`) and `plugin/` (`code.js`, `manifest.json`, generated `ui.html`), documented only in `README.md` §Figma plugin and the `maintaining-figma-plugins` skill | U4 |
| `fact-sheet.md` A- | lines 6 to 7 read as a contradiction with 53 | does not hold (checkability review of revision 1): the roles went 53 to 59 at `bb43e653` (#133, 2026-06-29) and back to 53 at `8083c040` (#219, 2026-07-06, the six `-variant` roles dropped); the sentence landed at `ae18c498` (#173, 2026-07-02) while the product was at 59 and the store copy still said 53. The sentence is accurate history and rule 1's "pre-trim" note agrees with it. Revision 1 planned a rewrite on a wrong premise (it read `role-table.json`'s history from the docs migration onward only) | dropped; no marketing file is touched |
| gap 3 | `audit-citations.mjs` checks cited lines, not claims | holds; the lead's item 8 asks for a claim check on the countable facts the docs state. U6 is that check: six pins (colorMode states, voices, roles, colour formats, the helper home, the two delete methods), each read from the doc by needle and from the code by a small reader, red on either side. It fits this plan as one S unit inside the existing gate; a general number-agreement scanner over every doc is bigger and is Q5 | U6 |
| gap 4 | `audit-citations.mjs` is manual, not in `package.json`, `test/run.mjs` or CI | stale: `test/repo/citations.mjs` is in `TESTS` and runs the parser self-test plus the audit; a bumped citation reds it (Measured table) | P5 proves it; U6 extends it |
| gap 2 | `glossary.md` has no UI terms | holds: 33 rows, none for section, canvas scene, inspector, breakpoint mode, Compare, gallery, drawer, analysis card; its `Mode` row means Light or Dark, which collides with the breakpoint sense and must be flagged | U2 |
| gap 5 | Typography, Geometry and gallery views documented only in the section skill | holds; the skill has the anatomy table, no reference doc names the modes (`specimen`/`tokens`, `controls`/`tokens`, `base`/mode id/`compare`), the Compare view (`colorMode === "both"`, `renderCompareArea`), the inspector tabs (`Ramp`/`Radius`/`Space` for Geometry) or the gallery (hub, category volumes, search, Import, Project). The lead's addendum: the gallery and the categories view (`this.category`, `null` for the hub, a category slug when open; `buildPresetTiles` over `_categoryData[slug]`) get one wireframe each in `app-shell.md` and nothing in the reference docs (`app-shell.md` names `category` once) | U1 (reference), U7 (README tour) |

## Measured by the planner on 2026-09-25 at 282fca8d

| Fact | Where | Value |
|---|---|---|
| `colorMode` states and persistence | `src/ui/app.js` at `this.colorMode = "system"` (:98), `localStorage.setItem(this._appPrefsKey(), JSON.stringify({ theme: ..., canvasTheme: ..., colorMode: ..., motion: ..., fontMode: ... }))` (:2297), the read-back `if (scheme(p.colorMode) \|\| p.colorMode === "both") this.colorMode = p.colorMode;` (:2290) | four states `system \| light \| dark \| both`; persisted as an app pref with `theme`, `canvasTheme`, `motion`, `fontMode`, separate from the document |
| voice count | `node --input-type=module -e 'import { makeVoices } from "./src/engine/type.mjs"; console.log(Object.keys(makeVoices()).length)'` | `15`; `type.mjs` header names the fifteen; `fact-sheet.md` pins 15; `app-shell.md` wireframe says 11; `docs/marketing/web/landing.md` says "eleven voices" (Not in scope, #748) |
| `mixinInto` | `src/ui/app.js` | defined at `function mixinInto(Target, ...Sources) {` (:2557), called at `mixinInto(HctApp, ColorSection, TypeSection, GeomSection, DrawerMixin, ApplyGateMixin, SettingsMixin);` (:2581); `app-shell.md` cites `:2570`; `node scripts/audit-citations.mjs` reads it `NEAR ... src/ui/app.js:2568 has mixinInto (-2)` |
| the five helpers | `src/ui/app-helpers.mjs` | `export const switchControl` (:370), `swatch` (:389), `btn` (:413), `chip` (:545), `field` (:571); `segmented(items, value, onSelect, opts = {}) {` (`app.js:1594`) and `slider(...)` (`app.js:2065`) are methods |
| `deleteMode` | `grep -rn deleteMode src/ui .claude/skills/building-editor-sections` | one hit, `SKILL.md` (:82); the methods are `deleteTypeMode(id) {` (`typography.js:210`) and `deleteGeomMode(id) {` (`geometry.js:265`) |
| colour formats | `src/engine/exports.js` header; `src/ui/overlays/drawer.js` `FORMAT_GROUPS` Colors group | ten documented (CSS hex, CSS OKLCH, JSON, DTCG, UI3, Tailwind v4, shadcn, Panda CSS, Radix, `exportAll`); ten drawer tabs (`css`, `oklch`, `tailwind`, `shadcn`, `panda`, `radix`, `figma`, `ui3`, `dtcg`, `json`); Typography two, Geometry three, Design System two, Project one |
| stale format counts | P6's case-insensitive grep over `docs` and `.claude/skills` minus `docs/tickets`, `docs/plan/archive`, `docs/reference/reviews`, `docs/site`, `docs/spec` and the CHANGELOG | 15 lines: `app-shell.md` ×3 (two `5 formats`, one `11 voices`), `SKILL.md` ×5, `spec-draft.md` ×3 (`:48`, `:158` `Eight color formats`, `:232`), `ui-plan.md` ×2, `acceptance-criteria.md` ×1, `quality-rubric.md` ×1. Outside the wall and excluded on purpose: `docs/spec/spec-panda-park-ui-exports.md` `:423` and `:578`, a spec whose requirement is that the "8 formats" claim moves to 10 (a record of the count, not a claim of it); `docs/site/describe-palette-spec.md:533` `each of the 8 formats` for the MCP `export_tokens` tool, an MCP-surface claim the `maintaining-brand-kit-mcp` skill owns (Not in scope) |
| section modes | `typography.js`, `geometry.js` | canvas segment `specimen \| tokens` (type) and `controls \| tokens` (geometry); breakpoint control `base \| <mode id> \| compare` (`compare` labelled `All`, only when modes exist); Geometry inspector tabs `ramp \| radius \| space`; Type inspector is `renderTypeInspector` with its own `typeSegment` tabs; Color canvas views `palettes \| scrims \| mapping \| radix`, Compare is `colorMode === "both"` via `renderCompareArea` |
| gallery | `app.js` `renderGallery` (:887), `this.category` (:75), `buildTiles` (:671), `this.search` (:153) | brand, Project (load from file or Figma), Import, `+ New`, theme button; a hub of category tiles (`CATEGORY_INDEX`, 8 categories, named four times in `app.js`) opening to volumes; the saved-set tiles filtered by `search` |
| `spec.system.hct-palette-generator-spec` | `docs/reference/CHANGELOG.md` 1.11 and its 1.12 note; `decomposition.md:62` | ruled kept ("HCT" is the colour model); cited by the decomposition |
| citations gate | `test/run.mjs` `TESTS` | `repo/citations.mjs` present; N is `50`; `node scripts/audit-citations.mjs` exits 0 with `canvasView enumerations: 0` and no STALE across the discovered docs; `DOCS_IMPLIED` maps `app-shell.md` and `component-inventory.md` to `src/ui/app.js` |
| citations gate, bumped | a `--shared` clone at 282fca8d with the `app-shell.md` `mixinInto` cite changed from `app.js:2570` to `app.js:1`, then `node test/repo/citations.mjs` | prints `✗ docs/lld/app-shell.md: 1 STALE/NOFILE citation line(s): 13 (run ...)` and `✗ 1 citation gate failure(s)`; the gate's own exit is 1 (the planner's pipe through `tail` masked it, hence the `bash -c` rule above) |
| `.sdlc/checks/*.sh` on main today | each run in the planning worktree | `baseline-agrees-check.sh` `stale total: 0`; `card-amendment-check.sh` `stale total: 0`; `card-source-range-check.sh` `range mismatches: 0`; `doc-drift-rows-check.sh` `rows 56 drifted 11 holds 45 undetermined 0 bad 1` (`QUOTE DD9: not found at .claude/CLAUDE.md:95`, the records lane's, not this plan's); `verdict-frontmatter-check.sh` `verdicts 141 graded 141 bad 11` (`MISSING gate-split-U1.md` and ten more, not this plan's) |
| DD rows that quote `.claude/CLAUDE.md` by line | `.sdlc/architecture.md` §8 | 24 rows cite lines 15 to 115; the file is 115 lines; an added line shifts up to 17 of them, which is why U5 edits in place |
| `figma/plugin/ui.html` | `ls -l` | 4260330 bytes, `4125.3 KB` in `.sdlc/baseline.md`'s build row; `scripts/bundle.mjs` strips exports, not comments |
| `chroma-floor` overlap | `git diff --name-only origin/main...origin/plan/chroma-floor` | `.sdlc/board.md`, `.sdlc/handoffs/chroma-floor-*`, `.sdlc/plans/chroma-floor.md`, `.sdlc/questions/chroma-floor-approval.md`: no path this plan admits except the board |
| the roles count history | `git log -S rolesPerPalette` and the two commit subjects | 53 to 59 at `bb43e653` (#133), 59 to 53 at `8083c040` (#219); read by the checkability reviewer, which is why the fact-sheet row above is dropped |
| `this.colorMode = "system"` | `grep -c` in `src/ui/app.js` | `2`: the constructor line (the one carrying the comment `Color section value-mode control`) and the prefs-reset line |
| host load at planning | `uptime` | 25 to 59 on 10 cores; the planner ran no gate of record |

## The design, stated once

U3 repairs claims and is first because every other unit's text may cite what it fixes. In `app-shell.md`: the §3.2 table row for `colorMode` gains `system` and the heading's "never persisted" gains an exception sentence naming the five app-pref keys `app.js` writes under `_appPrefsKey()` (`theme`, `canvasTheme`, `colorMode`, `motion`, `fontMode`), persisted per app and never with the document; the rule-of-thumb sentence says the same in one clause. The wireframe's `11 voices` becomes `15 voices`. The `mixinInto` cite moves to the call line (the audit then reads `OK` with anchor `mixinInto`). The two `SPEC-R8` cells say ten formats. In `component-inventory.md` the five factory rows say `app-helpers.mjs` (pure); `segmented()` and `slider()` stay `app.js` (method). In `app-shell-patterns.md` the dead `~/.claude/skills/ui-patterns/...` link becomes a name, not a path: the `frontend` plugin's `ui-pattern-facts` skill, reference `archetype-productivity-shell.md`. In the section skill, `deleteMode` becomes `deleteTypeMode`/`deleteGeomMode`, and its References table gains two rows: `docs/lld/app-shell.md` (the frame, regions, render pipeline, state) and `docs/reference/references/component-inventory.md` (the primitives). Headers: `app.js` line 1 names Ultimate Tokens; `persist.js` gains a first line opening `// persist.js` in the `model.mjs` shape (purpose, what it owns: hydrate, serialize, clamp domains, storage keys); `geometry.mjs` drops the private path and names its source as the external agent-ui dimensional spec with no path (its source note spans two comment lines, so up to two lines change there). Three header edits change the bundle, so the builder commits the regenerated `figma/plugin/ui.html` and `src/ui/describe-mcp-assets.js` and re-points the `ui.html` KB figure in `.sdlc/baseline.md`'s build row with a one-line correction paragraph, and P2 checks `baseline-agrees-check.sh` reads clean. U3 is sized M: ten files, two regenerated mirrors, a baseline correction and nine criteria.

U1 writes `ui-plan.md`'s Revision B, after Revision A and before §1, dated, in the same voice as Revision A: the document is a brand kit of three composing systems; `this.section` routes Color, Typography and Geometry through one five-region frame; each section's canvas modes, breakpoint modes and Compare, inspector tabs, and left-pane analysis, named by the ids and method names in the Measured table; the gallery as hub plus categories plus saved sets, with the categories view named as its own state (`this.category`, a slug or `null`, volumes of curated presets loaded per category) since `app-shell.md` gives it one wireframe and no prose; the export drawer's five groups (ten colour tabs, two type, three geometry, two design-system, one project). `T8 export: 5 formats` becomes ten and the export wireframe's `5 format tabs` says ten grouped by system. It is a summary with pointers: `docs/lld/app-shell.md` for the how, the `building-editor-sections` skill for the build pattern, `type-scale` and `geometry-system` for the engines; it copies no procedure from the skill. §1's task list gains the tasks Revision B adds (tune type, tune geometry, manage breakpoint modes, compare) without renumbering T1 to T9. The title keeps `HCT Palette Generator` only if Q2 says keep; otherwise it reads `Ultimate Tokens`.

U2 brings the index up to date. `docs/reference/SKILL.md`: the title and description say Ultimate Tokens and name the three systems and ten colour formats (the `name:` and the `cell` id follow Q2, default keep); the intent paragraph's `five export formats` and the three contract strings (`all five exports`, `each of the eight color formats`, `all five exporters`) say ten; the reference map row for knowledge-04 says ten and gains rows for `references/ui-plan.md`, `references/component-inventory.md` and `../lld/app-shell.md` (with `../` because the LLD sits outside `docs/reference/`). The same count strings in `spec-draft.md` (three lines: the intent's eight, the knowledge-04 sentence `Eight color formats: ...`, the reference map's `5 formats`), `acceptance-criteria.md` AC-X1 and `quality-rubric.md` change in the same commit, so P6's case-insensitive grep prints nothing. `glossary.md` gains nine rows: Section, Canvas scene, Canvas view, Breakpoint mode (flagged against the existing `Mode` row, which keeps its Light/Dark meaning), Compare, Inspector (with `.seg-example`), Analysis card, Gallery, Drawer; each one sentence, each naming its class or state field.

U4 writes `figma/README.md`: what the two plugins are (the generator as a plugin under `plugin/`, the standalone Semantic Binder under `binder/figma-semantic-binder/`), the six pure planner modules under `binder/` by one line each (from their own first-line headers), how `plugin/ui.html` is generated (`gen:figma-ui` from the bundle; never hand-edited), how to import either manifest in Figma, which tests cover it (`test/figma/*.mjs`, six files) and which skill owns changes (`maintaining-figma-plugins`). It cites no line numbers, so the citations gate has nothing to hold it to; U4-2 holds it to the tree by name.

U5 links the two records from `.claude/CLAUDE.md` once Q1 is yes. The edit rewrites the existing `docs/site/`, `docs/lld/`, `docs/img/` sentence in place so that the file stays 115 lines and every DD row in `.sdlc/architecture.md` §8 keeps its line number; the sentence names `docs/lld/app-shell.md` as the shell LLD and `docs/reference/references/component-inventory.md` as its component catalog. The fragment DD55 quotes stays on its line unchanged.

U6 adds a fact-pin leg to `test/repo/citations.mjs`, numbered (4) after the discovery block: a `FACT_PINS` array of `{ doc, needle, source }` rows, each a claim the audit cannot see. The number lives in the doc `needle` (the exact text the doc must carry, `15 voices`), `source()` reads the code and returns the value the code holds (a count, a string, a boolean), and the pin compares the two: the doc must contain the needle, and the needle's number must equal what `source()` returned. There is no `want` field, so a pin cannot compare code with code or carry a typed number beside the needle; a pin whose `source` is a bare literal (`() => 15`) is refused by U6-4. Six pins, the four repaired claims plus the two counts the lead's item 8 names: (a) `app-shell.md`'s `this.colorMode` row carries `system`, and `app.js`'s constructor line (the one carrying the comment `Color section value-mode control`) sets `"system"`; (b) `app-shell.md` says `15 voices` and `makeVoices()` has that many keys; (c) `ui-plan.md`'s `T8 export:` line says `10 formats` and the drawer's Colors group, read from `drawer.js`'s text from `const FORMAT_GROUPS` up to but not including the line carrying `"Typography"`, has that many `["id", "label"]` pairs (the end-inclusive slice reads `12`, the review's finding; the `"Colors"` line alone reads `10`); (d) `ui-plan.md`'s intent line says `a 53-role` (that exact phrase, which sits on the intent line only; `the 53-role table` in Revision A is a second, unpinned mention) and `role-table.json`'s `rolesPerPalette` is that number; (e) `component-inventory.md` places `btn()` in `app-helpers.mjs` and that file exports `btn`; (f) the section skill names `deleteTypeMode` and `deleteGeomMode` and each exists as a method in its section file. The pin reds when either side moves. The pass line stays one line and gains ` + N fact pins`. No new test file, so N stays 50 and `.sdlc/baseline.md`'s `tests:` row is untouched. U6 runs after U1 as well as U3 because pins (c) and (d) read the Revision B lines.

U7 adds one README section, `## Views and sections`, inserted before `## License`: the gallery (hub of category tiles, a category's volumes, the saved-set tiles with search, Project and Import), then the editor's three sections in one paragraph each (what the canvas shows, its modes, the inspector, the left analysis), the Compare view, and the export drawer's five groups, each paragraph ending with the doc that owns the detail (`docs/reference/references/ui-plan.md` Revision B, `docs/lld/app-shell.md`). Under 40 lines, no `file:line` cites (the citations gate discovers `docs/` only, so a README cite would be unguarded). The insertion moves DD41's quoted line from `README.md:147` by the section's length; U7 re-points that one number in `.sdlc/architecture.md` §8 in the same commit (R9: a records fix rides the open PR), and U7-3 reads the check.

## Units

- [ ] U3 (M) the four wrong claims and the six small ones, `app-shell.md`, `component-inventory.md`, `app-shell-patterns.md`, the section skill, three source headers · builder-l2 · reviewer-l1 · verifier-l1 · first
- [ ] U1 (M) `ui-plan.md` Revision B: three sections, modes, Compare, inspectors, gallery, ten formats · builder-l3 · reviewer-l2 · verifier-l2 · after U3
- [ ] U2 (M) `docs/reference/SKILL.md` index and the format counts across four spec files; glossary UI rows · builder-l2 · reviewer-l1 · verifier-l1 · after U3
- [ ] U4 (S) `figma/README.md` · trivial lane · grade l2
- [ ] U6 (S) six fact pins in `test/repo/citations.mjs` · builder-l2 · reviewer-l1 · verifier-l1 · after U3 and U1
- [ ] U7 (S) `README.md` tour of the views and sections, DD41 re-pointed · builder-l2 · reviewer-l1 · verifier-l1 · after U1
- [ ] U5 (S) `.claude/CLAUDE.md` links the two records, line count unchanged · builder-l1 · reviewer-l1 · verifier-l1 · starts at Q1 yes

Grades follow the Orchestrator's rule: an L2 builder gets reviewer-l1 and verifier-l1; U1 is L3 because it authors the one piece of new prose in the plan and must read three section files to get the ids right, so its reviewer and verifier are l2. U4 is the trivial lane (one `S` unit, one new `.md`, nothing under `scripts/`, `githooks/` or `hooks/`): one builder, one reviewer at `.sdlc/reviews/docs-repair-U4-review.md`, no verifier. U5 is L1: a one-sentence rewrite with a mechanical guard. No unit touches `src/engine/semantic.js`, `figma/` code, or `test/ui/headless-boot.mjs`, so X2's reviewer floor does not apply.

| Unit | Size | Builder | Reviewer | Verifier | Touches |
|---|---|---|---|---|---|
| U3 claims and headers | M | builder-l2 | reviewer-l1 | verifier-l1 | `docs/lld/app-shell.md`, `docs/reference/references/component-inventory.md`, `docs/reference/app-shell-patterns.md`, `.claude/skills/building-editor-sections/SKILL.md`, `src/ui/app.js` (line 1), `src/ui/persist.js` (header), `src/engine/geometry.mjs` (up to two comment lines), `figma/plugin/ui.html` and `src/ui/describe-mcp-assets.js` (regenerated), `.sdlc/baseline.md` (build row KB figure) |
| U1 ui-plan Revision B | M | builder-l3 | reviewer-l2 | verifier-l2 | `docs/reference/references/ui-plan.md` |
| U2 index, counts, glossary | M | builder-l2 | reviewer-l1 | verifier-l1 | `docs/reference/SKILL.md`, `docs/reference/references/spec-draft.md`, `docs/reference/rubrics/acceptance-criteria.md`, `docs/reference/rubrics/quality-rubric.md`, `docs/reference/references/glossary.md` |
| U4 figma README | S | builder-l2 | reviewer-l2 (lane pass) | none | `figma/README.md` |
| U6 fact pins | S | builder-l2 | reviewer-l1 | verifier-l1 | `test/repo/citations.mjs` |
| U7 README tour | S | builder-l2 | reviewer-l1 | verifier-l1 | `README.md`, `.sdlc/architecture.md` (the DD41 line number) |
| U5 CLAUDE.md links | S | builder-l1 | reviewer-l1 | verifier-l1 | `.claude/CLAUDE.md` |

## Plan-level criteria

`git fetch -q origin; B=$(git merge-base origin/main HEAD)` at the top of every row.

| Id | Criterion | Command | Expected | Negative control | Today |
|---|---|---|---|---|---|
| P1 | `npm test` green with no `node_modules`, the count agrees, the tree is byte-stable | `npm test 2>&1 \| tail -1; perl -0ne 'my ($b) = /const TESTS = \[(.*?)\];/s; my @m = $b =~ /"[^"]+\.mjs"/g; print scalar(@m), "\n"' test/run.mjs; git status --short \| wc -l` | `✓ all 50 test files passed`, `50`, `0` | in the clone: `sed -i '' 's/"scrim/"scrimX/' docs/reference/data/role-table.json && npm test > "$F/neg.log" 2>&1; echo "exit $?"` prints `exit 1` (adapter §1's own control) | `✓ all 50 test files passed`, `50`, `0` at 2ae50110 (the checkability reviewer's run, 4m37s at load 65; not a set of record) |
| P2 | `npm run build` green, tree clean after, and the baseline agrees with the regenerated bundle (U3 changes three bundled comment lines) | `npm run build > "$F/b.log" 2>&1; echo "exit $?"; tail -1 "$F/b.log"; git status --short \| wc -l; sh .sdlc/checks/baseline-agrees-check.sh \| tail -1` | `exit 0`, a line starting `wrote figma/plugin/ui.html`, `0`, `stale total: 0` | in the clone, revert the `.sdlc/baseline.md` KB figure to `4125.3`: the check prints a `STALE` line naming `ui.html` and a nonzero total. The control bites only when U3's regenerated bundle rounds to a figure other than `4125.3`; the checkability review's simulated U3 edit gave `tree 4125.5 KB`, so it does | `wrote figma/plugin/ui.html 4125.3 KB` recorded; `stale total: 0` at 282fca8d |
| P3 | branding clean, no added prose line carries an em dash (U+2014), and the raw count matches the enumerated quotes | `bash -c 'set -o pipefail; node test/repo/branding.mjs \| tail -1'; git diff "$B" -- . ':(exclude)figma/plugin/ui.html' ':(exclude)src/ui/*-assets.js' ':(exclude)src/ui/categories' \| grep -v '^+++ ' \| grep '^+' \| perl -CSD -ne 's/\x60[^\x60]*\x60//g; print if /\x{2014}/' \| wc -l; git diff "$B" -- . ':(exclude)figma/plugin/ui.html' ':(exclude)src/ui/*-assets.js' ':(exclude)src/ui/categories' ':(exclude).sdlc/handoffs' \| grep -v '^+++ ' \| grep '^+' \| perl -CSD -ne 'print if /\x{2014}/' \| wc -l` | `branding: clean (N files scanned)`, `0`, then a number equal to the count the handoffs state, each handoff listing every quoted program line word for word so the verifier matches them one to one (`0` if no handoff quotes a dashed line). A doc line a unit merely keeps is not counted; a line it rewrites is, so a rewritten `app-shell.md` or `SKILL.md` line drops the glyph it carried, the same rule okl-memo applied to `tonal.js` | in the clone: `cp docs/reference/references/decision-records.md .sdlc/verdicts/docs-repair-x.md` reds the first; one added prose line with the glyph makes the second `1` | clean at 282fca8d; this plan's own added lines: `0` and `0` (planner) |
| P4 | scope wall, and the three source files change comment lines only | `git diff --name-only "$B" \| grep -v -E -e '^docs/reference/references/(ui-plan\|spec-draft\|glossary\|component-inventory)\.md$' -e '^docs/reference/SKILL\.md$' -e '^docs/reference/rubrics/(acceptance-criteria\|quality-rubric)\.md$' -e '^docs/reference/app-shell-patterns\.md$' -e '^docs/lld/app-shell\.md$' -e '^\.claude/skills/building-editor-sections/SKILL\.md$' -e '^figma/README\.md$' -e '^src/ui/(app\|persist)\.js$' -e '^src/engine/geometry\.mjs$' -e '^test/repo/citations\.mjs$' -e '^\.claude/CLAUDE\.md$' -e '^figma/plugin/ui\.html$' -e '^src/ui/[a-z-]+-assets\.js$' -e '^src/ui/categories/' -e '^docs/img/' -e '^\.sdlc/baseline\.md$' -e '^\.sdlc/(plans\|handoffs\|verdicts\|reviews\|questions)/docs-repair' -e '^\.sdlc/board\.md$' -e '^README\.md$' -e '^\.sdlc/architecture\.md$' \| wc -l; git diff "$B" -- src/ui/app.js src/ui/persist.js src/engine/geometry.mjs \| grep -E '^[+-][^+-]' \| grep -v -E '^[+-][[:space:]]*(//\|\*\|/\*)' \| wc -l; git diff --name-only "$B" -- test/run.mjs scripts .github package.json \| wc -l; git diff "$B" -- .sdlc/architecture.md \| grep -E '^[+-]. DD[0-9]+ ' \| grep -v -c '^[+-]. DD41 '` | `0`, `0`, `0`, `0` (the fourth: every changed DD row of `architecture.md` is the DD41 row; the `.` stands for the table's leading pipe so the command carries no pipe character and no cell escape, after the revision 2 form `^[+-]\|` matched every line as an ERE with an empty branch) | a fixture of four names, `printf 'src/engine/tonal.js\nsrc/ui/model.mjs\ndocs/lld/app-shell.md\n.sdlc/plans/docs-repair.md\n'`, piped through the first filter prints `2` (the engine file and `model.mjs` are refused, the LLD and the plan pass; run by the planner: `2`); a non-comment line added to `persist.js` makes the middle command `1` or more; a second DD row edited makes the fourth `1` or more (run by the planner in a clone: DD41 re-pointed alone prints `0`, DD41 plus DD40 prints `2`) | `0`, `0`, `0`, `0` on the plan branch (this file only) |
| P5 | the citations gate is live inside `npm test` and reds on a bumped citation | `grep -c '"repo/citations.mjs"' test/run.mjs; bash -c 'set -o pipefail; node test/repo/citations.mjs \| tail -1'; echo "exit $?"` | `1`, a line starting `✓ citations: parser self-test + STALE 0 across`, `exit 0` | in the clone, `perl -pi -e 's/\x60app\.js:(\d+)\x60/\x60app.js:1\x60/ if /mixinInto/' docs/lld/app-shell.md` (the `mixinInto` cite pointed at a line whose text is a file header), then `bash -c 'set -o pipefail; node test/repo/citations.mjs \| tail -3'; echo "exit $?"` prints a line containing `docs/lld/app-shell.md: 1 STALE/NOFILE citation line(s)`, a line naming the bumped cite, `✗ 1 citation gate failure(s)`, `exit 1` (the failure line sits third from the end, above the per-cite detail line); `engine`-side, `repo/citations.mjs` is in `TESTS`, so the same red is `npm test`'s | measured by the planner at 282fca8d: `1`; the control printed both lines (the gate's exit was masked by a bare `tail`, which is why the row says `bash -c`) |
| P6 | every stale count string is gone from the live docs and the live count agrees with the drawer | `grep -rn -i -E '5 formats\|five export formats\|five exports\|five exporters\|eight colou?r formats\|8 formats\|5 format tabs\|11 voices' docs .claude/skills --include='*.md' \| grep -v -E 'docs/(tickets\|plan/archive\|reference/reviews\|site\|spec)/\|CHANGELOG' \| wc -l; sed -n '/const FORMAT_GROUPS/,/^    \];/p' src/ui/overlays/drawer.js \| grep '"Colors"' \| grep -o '\["[a-z0-9-]*", "' \| wc -l` | `0`, `10` | the tree at `$B`: the first prints `15` (the Measured table lists them by path and line; `docs/site` and `docs/spec` are excluded for the reasons given there) | `15` and `10` at 282fca8d |

## U3: claims and headers (first)

Steps. (1) `app-shell.md`: the §3.2 heading, the `colorMode` row, the rule-of-thumb sentence, the wireframe's voice count, the `mixinInto` cite, the two `SPEC-R8` cells. (2) `component-inventory.md`: five factory rows. (3) `app-shell-patterns.md`: the link sentence. (4) the section skill: `deleteMode`, two References rows. (5) `app.js` line 1, `persist.js` header, `geometry.mjs` source note. (6) `npm test`; commit the regenerated `ui.html` and `describe-mcp-assets.js`; re-point the KB figure in `.sdlc/baseline.md` with a one-line correction paragraph naming this unit. (7) The handoff states the P3 raw count and lists any quoted dashed line.

| Id | Criterion | Command | Expected | Negative control | Today |
|---|---|---|---|---|---|
| U3-1 | `colorMode` has four states and the app-pref persistence is stated | `grep 'this.colorMode' docs/lld/app-shell.md \| head -1 \| grep -c 'system'; grep -c '_appPrefsKey' docs/lld/app-shell.md; grep -c 'fontMode' docs/lld/app-shell.md; grep -c 'colorMode: this.colorMode' src/ui/app.js` | `1`, `1` or more, `1` or more, `1` (the fourth proves the pref key still exists in the source) | the file at `$B` prints `0`, `0`, `0` | `0`, `0`, `0`, `1` at 282fca8d |
| U3-2 | the wireframe's voice count matches the engine | `grep -c '11 voices' docs/lld/app-shell.md; grep -c '15 voices' docs/lld/app-shell.md; node --input-type=module -e 'import { makeVoices } from "./src/engine/type.mjs"; console.log(String(Object.keys(makeVoices()).length))'` | `0`, `1`, `15` (as text; a bare number prints with ANSI colour on a TTY) | the file at `$B` prints `1`, `0` | `1`, `0`, `15` |
| U3-3 | the `mixinInto` cite lands on its call line and the audit reads it `OK` | `L=$(grep -o 'mixinInto\x60, \x60app.js:[0-9]*' docs/lld/app-shell.md \| grep -o '[0-9]*$'); [ -n "$L" ] \|\| echo NO-CITE; sed -n "${L:-0}p" src/ui/app.js \| grep -c '^mixinInto(HctApp,'; node scripts/audit-citations.mjs \| grep 'app-shell.md:[0-9]* cites app.js:[0-9]* -> matched .mixinInto' \| grep -c '^  OK'` | no `NO-CITE` line, `1`, `1` (a reworded cite that the `grep -o` misses prints `NO-CITE` and `0`, never a vacuous `1`) | the file at `$B`: the first prints `0` (the cited line is inside the function body), the second `0` (the audit reads `NEAR`) | `0`, `0` |
| U3-4 | `deleteMode` is gone from the skill and the two real names are present and exist | `grep -c 'deleteMode' .claude/skills/building-editor-sections/SKILL.md; grep -c 'deleteTypeMode' .claude/skills/building-editor-sections/SKILL.md; grep -c 'deleteGeomMode' .claude/skills/building-editor-sections/SKILL.md; grep -c '^  deleteTypeMode(id) {' src/ui/sections/typography.js; grep -c '^  deleteGeomMode(id) {' src/ui/sections/geometry.js; grep -c 'docs/lld/app-shell.md' .claude/skills/building-editor-sections/SKILL.md; grep -c 'component-inventory.md' .claude/skills/building-editor-sections/SKILL.md` | `0`, `1` or more, `1` or more, `1`, `1`, `1` or more, `1` or more | the file at `$B` prints `1`, `0`, `0`, then `1`, `1`, `0`, `0` | `1`, `0`, `0`, `1`, `1`, `0`, `0` |
| U3-5 | the five helper rows name `app-helpers.mjs` and the two method rows keep `app.js`; each named export exists | `for f in switchControl swatch btn chip field; do grep "^| \x60$f()\x60 |" docs/reference/references/component-inventory.md \| grep -c 'app-helpers.mjs'; grep -c "^export const $f = " src/ui/app-helpers.mjs; done; for f in segmented slider; do grep "^| \x60$f()\x60 |" docs/reference/references/component-inventory.md \| grep -c '\x60app.js\x60 (method)'; done` | ten `1`s, then `1`, and `slider()` has no row in that table so the last prints `0` (the builder confirms by reading; if a `slider()` row is added it says `app.js` (method) and the expected becomes `1`) | the file at `$B`: the five doc greps print `0` | five `0`s interleaved with five `1`s, then `1`, `0` |
| U3-6 | the dead link is gone and no path outside the repo remains | `grep -c 'ui-patterns/' docs/reference/app-shell-patterns.md; grep -c 'ui-pattern-facts' docs/reference/app-shell-patterns.md; grep -c '~/' docs/reference/app-shell-patterns.md` | `0`, `1` or more, `0` | the file at `$B` prints `1`, `0`, `1` | `1`, `0`, `1` |
| U3-7 | the three headers | `head -1 src/ui/app.js \| grep -c 'HCT Palette Generator'; head -1 src/ui/app.js \| grep -c 'Ultimate Tokens'; head -1 src/ui/persist.js \| grep -c '^// persist.js'; grep -c '\.claude/docs/' src/engine/geometry.mjs; git grep -c '\.claude/docs/' -- src \| wc -l` | `0`, `1`, `1`, `0`, `0` (after `npm test`: the generated `src/ui/describe-mcp-assets.js` embeds the `geometry.mjs` header and is rewritten by `gen:mcp-assets`, so the count over `src` falls to `0` with the source; before regeneration it reads `1`) | the files at `$B` print `1`, `0`, `0`, `1`, `2` (the source and its generated mirror) | `1`, `0`, `0`, `1`, `2` |
| U3-8 | the fact sheet is untouched (revision 3 dropped the rewrite; the sentence is accurate history) | `git diff --name-only "$B" -- docs/marketing \| wc -l` | `0` | any edit under `docs/marketing` prints `1` or more and P4 reds too | `0` |
| U3-9 | the source diff is comments only, and the baseline moved with the bundle | P4's middle command; `git diff --numstat "$B" -- src/ui/app.js src/ui/persist.js src/engine/geometry.mjs \| cut -f1-3 \| tr '\t' ' '`; `git diff "$B" -- .sdlc/baseline.md \| grep -c '^+.*wrote figma/plugin/ui.html'` | `0`; one row per file in git's path order, `1 1 src/engine/geometry.mjs` or `2 2 src/engine/geometry.mjs` (the source note spans two comment lines), `1 1 src/ui/app.js`, `A 0 src/ui/persist.js` with A the header's line count; `1` or more (the build row plus the correction paragraph, which may quote the `wrote` line as earlier corrections at `baseline.md` do) | a re-keyed or moved code line makes the first `1` or more; a `3 3` on `geometry.mjs` reds the numstat | no diff; the checkability review's simulated edit printed `1 1`, `1 1`, `3 0` in that path order |

## U1: ui-plan Revision B (after U3)

Steps. (1) Read `src/ui/app.js` `renderGallery`, `setSection`, `renderCenter`; `sections/typography.js` and `sections/geometry.js` for the mode and inspector ids; `overlays/drawer.js` `FORMAT_GROUPS`. (2) Write Revision B between Revision A and §1. (3) Update §1's task list and the two count lines. (4) Run `node scripts/audit-citations.mjs` on the file if any `file:line` was added (prefer none: cite by name). (5) The handoff lists the ids the revision names and where each was read.

| Id | Criterion | Command | Expected | Negative control | Today |
|---|---|---|---|---|---|
| U1-1 | Revision B exists, dated, and names the three sections and the routing field | `grep -c '^## Revision B' docs/reference/references/ui-plan.md; grep -c 'this.section' docs/reference/references/ui-plan.md; for s in Color Typography Geometry; do grep -c "$s" docs/reference/references/ui-plan.md; done` | `1`, `1` or more, three numbers each `1` or more (the file writes the three names in lower case today, so all three are `0` at `$B`; the builder cites the counts) | the file at `$B` prints `0`, `0`, `0`, `0`, `0` | `0`, `0`, then `0`, `0`, `0` |
| U1-2 | the section ids the revision names exist in the source, read by needle | `for n in specimen typeSpecMode geomMode compare renderCompareArea renderTypeInspector renderGeomInspector radius; do printf '%s ' "$n"; grep -c "$n" docs/reference/references/ui-plan.md; done; grep -c 'id: "specimen"' src/ui/sections/typography.js; grep -c 'typeSpecMode' src/ui/sections/typography.js; grep -c 'geomMode' src/ui/sections/geometry.js; grep -c 'id: "compare"' src/ui/sections/geometry.js; grep -c 'renderCompareArea' src/ui/sections/color.js; grep -c 'renderTypeInspector' src/ui/sections/typography.js; grep -c 'renderGeomInspector' src/ui/sections/geometry.js; grep -c 'id: "radius"' src/ui/sections/geometry.js` | each of the eight doc counts `1` or more (every needle is `0` at `$B`; the bare words `tokens` and `controls` were dropped because the file already carries them four times each); the eight source greps `1` or more each | the file at `$B`: every doc count `0` | all eight doc counts `0`; the source greps `2`, `3`, `36`, `1`, `3`, `2`, `1`, `1` |
| U1-3 | the gallery is described as hub, categories and saved sets with search | `grep -c -i 'gallery' docs/reference/references/ui-plan.md; grep -c 'CATEGORY_INDEX\|categor' docs/reference/references/ui-plan.md; grep -c 'CATEGORY_INDEX' src/ui/app.js` | `10` or more (nine mentions today, Revision A and §4), `1` or more, `1` or more | the file at `$B` prints `9` and `0` | `9`, `0`, `4` |
| U1-4 | the export counts say ten and the pointer rows exist | `grep -c '5 formats\|5 format tabs' docs/reference/references/ui-plan.md; grep -c 'docs/lld/app-shell.md' docs/reference/references/ui-plan.md; grep -c 'building-editor-sections' docs/reference/references/ui-plan.md` | `0`, `1` or more, `1` or more | the file at `$B` prints `2`, `0`, `0` | `2`, `0`, `0` |
| U1-5 | no new stale citation: the audit reads the file with STALE 0, or the file carries no `file:line` cite | `node scripts/audit-citations.mjs \| grep -A3 '=== docs/reference/references/ui-plan.md' \| grep -c 'STALE 0'; grep -c -E '[a-z-]+\.(js\|mjs):[0-9]+' docs/reference/references/ui-plan.md` | `1` or (if the file is not discovered because it cites nothing) `0` with the second also `0`; the builder states which | one added `app.js:1` cite reds P5's gate | the file is not discovered today (no cite); `0` |
| U1-6 | the summary copies no procedure from the skill | `grep -c 'Flip to' docs/reference/references/ui-plan.md; grep -c 'scrollport' docs/reference/references/ui-plan.md; wc -l < docs/reference/references/ui-plan.md` | `0`, `0`, under `330` (227 today; Revision B is a summary of at most about a hundred lines) | a paste of the skill's table-view section makes the first two `1` or more | `0`, `0`, `227` |

## U2: the index, the counts, the glossary (after U3)

Steps. (1) `docs/reference/SKILL.md`: title, description, intent, the three contract strings, the reference map (knowledge-04 row and three new rows); `name:` and `cell` per Q2. (2) `spec-draft.md` (three lines, including the `Eight color formats:` sentence that opens the knowledge-04 pointer), `acceptance-criteria.md`, `quality-rubric.md`: the count strings. (3) `glossary.md`: the nine UI rows, the `Mode` row's flag. (4) `npm test`. (5) The handoff quotes nothing dashed unless it must, and states the P3 count.

| Id | Criterion | Command | Expected | Negative control | Today |
|---|---|---|---|---|---|
| U2-1 | the index names the product and the three systems, and no stale count remains in it | `grep -c 'Ultimate Tokens' docs/reference/SKILL.md; grep -c -i 'typography' docs/reference/SKILL.md; grep -c -i 'geometry' docs/reference/SKILL.md; grep -c -E 'five export formats\|all five exports\|eight color formats\|all five exporters' docs/reference/SKILL.md; grep -c '^name: ' docs/reference/SKILL.md` | `1` or more, `1` or more, `1` or more, `0`, `1` (the value follows Q2: `hct-palette-generator-spec` if keep) | the file at `$B` prints `0`, `0`, `0`, `5` | `0`, `0`, `0`, `5` (`SKILL.md` lines 35, 112, 113, 117, 236 at 282fca8d), `1` |
| U2-2 | the reference map links the three UI records | `for p in 'references/ui-plan.md' 'references/component-inventory.md' '../lld/app-shell.md'; do grep -c "\x60$p\x60" docs/reference/SKILL.md; done; test -f docs/lld/app-shell.md && echo ok` | three numbers each `1` or more (the non-goals name `references/ui-plan.md` today without backticks, which this needle does not match), `ok` | the file at `$B`: `0`, `0`, `0` | `0`, `0`, `0`, `ok` |
| U2-3 | the other three spec files say ten | `grep -ci 'eight color formats' docs/reference/references/spec-draft.md docs/reference/rubrics/acceptance-criteria.md docs/reference/rubrics/quality-rubric.md; grep -c '5 formats' docs/reference/references/spec-draft.md` | `0` for each of the three, `0` | the files at `$B` print `2`, `1`, `1`, `1` (`spec-draft.md` carries `eight color formats` and `Eight color formats:`) | `2`, `1`, `1`, `1` |
| U2-4 | the glossary has the UI rows and flags the two senses of mode | `for t in 'Section' 'Canvas scene' 'Canvas view' 'Breakpoint mode' 'Compare' 'Inspector' 'Analysis card' 'Gallery' 'Drawer'; do grep -c "^| \*\*$t" docs/reference/references/glossary.md; done; grep '^| \*\*Mode\*\*' docs/reference/references/glossary.md \| grep -c -i 'breakpoint'; grep -c '^| ' docs/reference/references/glossary.md` | nine `1`s, `1`, `42` or more (33 today plus nine rows) | the file at `$B`: nine `0`s, `0`, `33` | nine `0`s, `0`, `33` |
| U2-5 | each glossary UI row names its class or state field | `for n in 'this.section' 'canvas-scene' 'seg-example' 'an-card' 'this.view' 'colorMode'; do grep -c "$n" docs/reference/references/glossary.md; done` | six numbers, each `1` or more | the file at `$B`: six `0`s | six `0`s |

## U4: figma/README.md (trivial lane)

Steps. (1) Read `README.md` §Figma plugin, `figma/plugin/manifest.json`, `figma/binder/figma-semantic-binder/manifest.json`, the first line of each `figma/binder/*.mjs`, `test/figma/`, `scripts/gen-figma-ui.mjs` and `gen-figma-assets.mjs`, the `maintaining-figma-plugins` skill's description. (2) Write the README. (3) `node test/repo/branding.mjs` (the file must not name the retired brand or the pre-rename identifier; the binder's own manifest name is fine).

| Id | Criterion | Command | Expected | Negative control | Today |
|---|---|---|---|---|---|
| U4-1 | the file exists and names both plugins, both manifests, the generator and the skill | `test -f figma/README.md && echo ok; for n in 'plugin/manifest.json' 'figma-semantic-binder' 'gen:figma-ui' 'ui.html' 'maintaining-figma-plugins' 'test/figma'; do grep -c "$n" figma/README.md; done` | `ok`, six numbers each `1` or more | absent at `$B` | absent |
| U4-2 | every module it names exists, and every `binder/*.mjs` is named | `for f in $(grep -o 'binder/[a-z-]*\.mjs' figma/README.md \| sort -u); do test -f "figma/$f" \|\| echo "missing $f"; done; for f in figma/binder/*.mjs; do grep -q "binder/$(basename "$f")" figma/README.md \|\| echo "unnamed $f"; done` | nothing printed | a README naming `binder/splice.mjs` prints `missing binder/splice.mjs` (ran in the checkability review); deleting the `binder/live-diff.mjs` mention prints `unnamed figma/binder/live-diff.mjs` even while `test/figma/live-diff.mjs` is still named, because the needle carries the `binder/` prefix | absent |
| U4-3 | trivial-lane boundary holds | `git diff --name-only $(git merge-base plan/docs-repair HEAD)..HEAD` (the unit's own base on the plan branch, the plan-rules command; `$B` would list every unit merged before it) | exactly `figma/README.md` (plus the unit's own `.sdlc/reviews/` record if committed on the branch) | any other path moves the unit out of the lane | no diff |
| U4-4 | branding and dash rules | P3's three commands on the unit branch | `branding: clean (N files scanned)`, `0`, `0` | as P3 | as P3 |

## U6: fact pins in the citations gate (after U3 and U1)

Steps. (1) Add block (4) with `FACT_PINS` to `test/repo/citations.mjs`; the pass line gains ` + N fact pins`. (2) Each pin's `source()` reads the engine (`makeVoices` via dynamic import), `role-table.json`, or a source file's text with a needle; none spawns a process and none holds the wanted number as a literal. (3) `node test/repo/citations.mjs` green; `npm test` green. (4) In the clone, flip each pinned doc string and prove one FAIL line per pin, naming the doc and the pin. (5) The handoff quotes the new pass line.

| Id | Criterion | Command | Expected | Negative control | Today |
|---|---|---|---|---|---|
| U6-1 | the leg exists, runs, and the pass line counts it | `grep -c 'FACT_PINS' test/repo/citations.mjs; bash -c 'set -o pipefail; node test/repo/citations.mjs \| tail -1'; echo "exit $?"` | `1` or more, a line starting `✓ citations: parser self-test + STALE 0 across` and containing ` + 6 fact pins`, `exit 0` | the file at `$B` prints `0` and a pass line without ` fact pins` | `0`; the pass line has no pin count |
| U6-2 | each pin bites on the doc side | in the clone, six edits one at a time, each `node test/repo/citations.mjs > "$F/p$i.log" 2>&1; echo "exit $?"; grep -c '^  ✗' "$F/p$i.log"`: (a) `app-shell.md`'s `colorMode` row loses `system`; (b) `15 voices` becomes `11 voices`; (c) `ui-plan.md`'s `T8 export:` says `5 formats` again; (d) `ui-plan.md`'s intent line says `a 59-role` while Revision A's `the 53-role table` is left alone (a pin that matched any `53-role` would still pass here, which is why (d) pins the intent phrase); (e) the `btn()` row says `app.js`; (f) the skill says `deleteMode` again | each `exit 1`, `1`, and the ✗ line names the doc path and the pin's needle | the same six edits on U6's parent commit (U3 and U1 merged, U6's own commit absent: `git -C "$F/neg" checkout -q HEAD~1` when U6 is one commit, else the merge that preceded it) leave `node test/repo/citations.mjs` green, which is the gap this unit closes; at `$B` itself the edits are no-ops because the repaired strings do not exist there yet | green on U6's parent under all six edits |
| U6-3 | each pin bites on the source side | in the clone: (a) both `this.colorMode = "system"` lines in `app.js` (the constructor line carrying `Color section value-mode control`, and the prefs-reset line) become `"light"`, and the pin anchors on the constructor line's comment so that changing that one line alone also reds; (b) the `makeVoices` import in the test is pointed at a stub returning 11 keys (edit the test's import line only); (c) one pair removed from the drawer's Colors group; (d) `rolesPerPalette` in `role-table.json` set to `59`; (e) `export const btn` in `app-helpers.mjs` renamed `btn2`; (f) `deleteTypeMode(id) {` renamed | each `exit 1` with a ✗ line naming the source path; (d) also reds `engine/semantic.mjs`, which the builder records as expected | the same edits at `$B` are green in this gate for all six; (c), (d) and (e) may red other tests, which the builder records | green in this gate |
| U6-4 | no typed number on the code side: no `want` field, and no `source` that is a bare literal | `grep -c 'want' test/repo/citations.mjs; grep -c -E 'source: *(\(\) *=> *)?(53\|15\|10\|4)\b' test/repo/citations.mjs; grep -c 'FACT_PINS' test/repo/citations.mjs` | `0`, `0`, `1` or more (the third proves the file was read, so the two `0`s are not vacuous) | a pin written as `source: () => 15` prints `1` on the second; a `want: 15` field prints `1` on the first (the review ran the digit regex on this host: `printf 'want: 15,\nwant: 150\n'` piped through it prints `1`) | `0`, `0`, `0` (no pins yet) |
| U6-5 | no test file added, N unchanged, the pass line is one line | `perl -0ne 'my ($b) = /const TESTS = \[(.*?)\];/s; my @m = $b =~ /"[^"]+\.mjs"/g; print scalar(@m), "\n"' test/run.mjs; git diff --name-only "$B" -- test/run.mjs \| wc -l; git diff "$B" -- .sdlc/baseline.md \| grep -c '^[+-].*tests:'; node test/repo/citations.mjs \| grep -c '^✓'` | `50`, `0`, `0` (U3's KB correction is in that file's diff and is not a `tests:` row), `1` | registering a new file makes the first `51` and `baseline-agrees-check.sh` reds on `tests:` | `50`, `0`, `0`, `1` |

## U7: README tour of the views and sections (after U1)

Steps. (1) Read `ui-plan.md` Revision B (U1's output) and `app-shell.md` §0 and §6. (2) Insert `## Views and sections` before `## License`. (3) Re-point DD41's `README.md:147` in `.sdlc/architecture.md` §8 to the license line's new number; touch no other row. (4) `sh .sdlc/checks/doc-drift-rows-check.sh`; `npm test` (the branding gate walks `README.md`). (5) The handoff states the section's line count and the new DD41 number.

| Id | Criterion | Command | Expected | Negative control | Today |
|---|---|---|---|---|---|
| U7-1 | the section exists, sits before License, and names the views and the three sections | `grep -n '^## ' README.md \| tail -2; for n in gallery categor Typography Geometry Compare drawer 'ui-plan.md' 'app-shell.md'; do printf '%s ' "$n"; grep -c -i "$n" README.md; done` | the two headings printed are `## Views and sections` then `## License`, in that order; eight counts each `1` or more, and the four that are `0` at `$B` (`Compare`, `drawer`, `ui-plan.md`, `app-shell.md`) become `1` or more; the other four already appear in What it does, so the builder cites their counts before and after | the file at `$B`: the last two headings are `## Figma plugin` and `## License`; `Compare` `0`, `drawer` `0`, `ui-plan.md` `0`, `app-shell.md` `0` | `gallery` `2`, `categor` `11`, `Typography` `3`, `Geometry` `4`, `Compare` `0`, `drawer` `0`, `ui-plan.md` `0`, `app-shell.md` `0` |
| U7-2 | it is a tour, not a copy, and carries no unguarded cite | `awk '/^## Views and sections/,/^## License/' README.md \| wc -l; grep -c -E '[a-z-]+\.(js\|mjs):[0-9]+' README.md` | under `42` (the section plus its two heading lines), `0` | a paste of `app-shell.md` §1 (the frame, which carries `app.js:NNN` cites) makes the first larger and the second `1` or more; §6's wireframes carry no cite and would red only the length leg | section absent; `0` |
| U7-3 | DD41 follows the license line and no other DD row moves | `L=$(grep -n 'see \[LICENSE\](LICENSE)' README.md \| cut -d: -f1); grep -c "DD41 \| \x60README.md:$L\x60" .sdlc/architecture.md; sh .sdlc/checks/doc-drift-rows-check.sh \| tail -1; sh .sdlc/checks/doc-drift-rows-check.sh \| grep -c QUOTE; git diff "$B" -- .sdlc/architecture.md \| grep -E '^[+-]. DD[0-9]+ ' \| grep -v -c '^[+-]. DD41 '` | `1`, `rows 56 drifted 11 holds 45 undetermined 0 bad 1`, `1` (DD9, as at `$B`), `0` (P4's pipe-free form) | leave DD41 at `:147`: the first prints `0`, the check prints `bad 2` and a `QUOTE DD41` line (the checkability review ran both: re-pointed to `:153` after a simulated insert it printed `1` and `bad 1`); re-point DD40 as well and the fourth prints `2` | `1` at `:147`, `bad 1`, `1`, no diff |

## U5: CLAUDE.md links the two records (starts at Q1 yes)

Steps. (1) Rewrite the `docs/site/`, `docs/lld/`, `docs/img/` sentence in place across its existing lines so the total stays 115 and the DD55 fragment stays on its line. (2) `sh .sdlc/checks/doc-drift-rows-check.sh`. (3) `npm test` (the `html:` count line and everything else in the file is untouched).

| Id | Criterion | Command | Expected | Negative control | Today |
|---|---|---|---|---|---|
| U5-1 | both records are named | `grep -c 'docs/lld/app-shell.md' .claude/CLAUDE.md; grep -c 'component-inventory.md' .claude/CLAUDE.md` | `1`, `1` | the file at `$B` prints `0`, `0` | `0`, `0` |
| U5-2 | the line count and every DD row's line hold | `wc -l < .claude/CLAUDE.md; sh .sdlc/checks/doc-drift-rows-check.sh \| tail -1; sh .sdlc/checks/doc-drift-rows-check.sh \| grep -c 'QUOTE'` | `115`, `rows 56 drifted 11 holds 45 undetermined 0 bad 1`, `1` (DD9 is red at `$B` for the records lane's reason and stays exactly one; a second `QUOTE` line is this unit's) | in the clone, insert one blank line above the Layout heading: `wc -l` prints `116` and the `QUOTE` count rises above `1` | `115`, `bad 1`, `1` |
| U5-3 | the edit is inside the Layout bullet, nothing else moved | `git diff --numstat "$B" -- .claude/CLAUDE.md \| cut -f1,2 \| tr '\t' ' '; git diff "$B" -- .claude/CLAUDE.md \| grep '^-' \| grep -v '^---' \| grep -c 'docs/lld/'` | `A A` with A at most `3` (the same number added and removed), `1` (the one removed line is the one that named `docs/lld/`) | a four-line rewrite reds the first | no diff |

## Not in scope

| Item | Why | Where it goes |
|---|---|---|
| A general number-agreement scanner over every doc (item 8 at full width: every countable fact in `docs/`, `README.md` and the skills against the code) | the countable facts are not machine-discoverable from prose (`53-role`, `fifteen`, `ten`, `11 voices` are all shapes), so a scanner is a curated pin table anyway; U6's six pins are that table for the facts the audit found wrong, and a pin is added when a fact is repaired, the same rule the citations gate applies to a cite | U6 now; a wider table rides whichever plan next repairs a countable claim (Q5) |
| `docs/marketing/web/landing.md` "eleven voices" and "five treatments from Editorial to Brutalist" (the type treatments are product, luxury, editorial, technical, statement) | marketing corpus, `marketing-manager-agent` owns it; #748 is the open voice reread of the store copy | #748, cited in this plan's landing comment |
| `docs/marketing/fact-sheet.md` "moved to 59 while the copy still said 53" | accurate history (#133 then #219); the audit misread it and revision 1 planned on the misreading | nothing; the re-verification table records why |
| `docs/site/describe-palette-spec.md` "each of the 8 formats" (the MCP `export_tokens` row) | an MCP-surface claim; whether the tool returns 8 or 10 is `mcp/brand-kit-core.mjs`'s to answer, owned by the `maintaining-brand-kit-mcp` skill | a follow-up ticket if the MCP tool's count disagrees; P6 excludes `docs/site` so this plan cannot read green on it by accident |
| `docs/spec/spec-panda-park-ui-exports.md` REQ-080 and AC-009 | a spec that requires the "8 formats" claim to move to 10; it records the count, it does not claim it | none; P6 excludes `docs/spec` |
| `model.mjs` and `exports.js` headers naming the old product | CHANGELOG 1.11 kept engine-side HCT naming on purpose; the ask names `app.js` line 1 only | Q2's answer may widen it to a follow-up |
| Renaming the spec-cell id `spec.system.hct-palette-generator-spec` | ruled kept at 1.11; `decomposition.md` cites it | Q2 |
| `.sdlc/architecture.md` DD9 (`QUOTE DD9: not found at .claude/CLAUDE.md:95`) and the eleven `MISSING` verdict files | red on `origin/main` today for the records lane's reasons; this plan changes neither count | the records lane; the pre-land record cites both as carried exceptions with today's numbers |
| Auditing `.claude/skills/**` with `audit-citations.mjs` | the section skill carries no `file:line` citation (`0` at 282fca8d), so discovery would add nothing | none |
| A `test/repo/doc-facts.mjs` file | raises N to 51 and moves `.sdlc/baseline.md`'s `tests:` row under the post-#691 rule; the leg fits the existing gate | U6 |

## Risks

| Risk | What this plan does about it |
|---|---|
| Two units edit one file and conflict on the plan branch | every `app-shell.md` and section-skill edit is U3's; U2 owns the four spec files; U1 owns `ui-plan.md`; P4 lists the split |
| U1 writes prose that names an id the code does not have | U1-2 greps every named id in both the doc and the source |
| A header line changes the bundle size and `baseline-agrees-check.sh` reds at pre-land | P2 checks the figure in the unit; the scope wall admits the build row (okl-memo revision 7) |
| An added line in `.claude/CLAUDE.md` shifts 17 DD rows | U5 keeps the line count; U5-2 reads the check |
| The owner says no to Q1 | U5 is dropped in a revision row; U2 and U3 still link the records from the index and the skill |
| Q2 answered rename | U2 changes `name:` and the cell id and `decomposition.md:62` joins the scope wall by a revision row; CHANGELOG 1.11's note gains an amendment line |
| The verifier's `npm test` runs on a loaded host | the quiet-host rule of adapter §1; no timing is graded by this plan |
| A quoted program line carries an em dash (the `app.js` header does) | P3's third count; handoffs quote by needle where they can, and enumerate any dashed quote |

## Landing

One PR from `plan/docs-repair` to `main`, title `docs(ui): the reference docs catch up with three sections; fact pins in the citations gate`, body `Closes #<ticket>` once Q0 mints it. Draft at U3's verified state. Pre-land per adapter §2.1: P1 to P6 and every unit row at the branch head, every `sh .sdlc/checks/*.sh` (with DD9 and the eleven `MISSING` verdicts recorded as carried, same counts as `origin/main`), record at `.sdlc/verdicts/docs-repair-prepr.md` with `verdict: 🟢` as its last `verdict:` line and `sha:` at the head; smoke is owed because `src/ui/app.js` and `src/ui/persist.js` change (comment lines, but adapter §1's smoke row keys on the path), so the pre-land verifier runs `npm run smoke` and cites `SMOKE PASS`. Then the `shipping-changes` squash and sync steps and the plan-closing rule of adapter §5. The closing comment names #748 for the landing-page facts.

## Owner questions

| Id | Question | Recommendation |
|---|---|---|
| Q0 | Mint the ticket at activation (`adapter.py create --title docs-repair --label kind:chore --label lane:docs`)? | Yes, at activation; ruling R42 is recorded in this plan's revision rows (a separate questions file would sit outside the scope wall) |
| Q1 | Edit `.claude/CLAUDE.md` (X9) to name `docs/lld/app-shell.md` and `component-inventory.md`, in place, 115 lines kept? | Yes: one sentence, a mechanical guard (U5-2), and it is the file every seat reads first |
| Q2 | Keep the spec-cell id and `name: hct-palette-generator-spec` (CHANGELOG 1.11, "HCT is the colour model") and change only the human-facing title and description, or rename to `ultimate-tokens-spec`? | Keep the id; retitle. A rename touches `decomposition.md` and CHANGELOG 1.11's ruling for no reader gain |
| Q3 | withdrawn in revision 3: the fact-sheet sentence is accurate history (53 to 59 at #133, 59 to 53 at #219, the sentence written between them); no marketing file is touched | none |
| Q4 | U4 in the trivial lane at grade l2 (one builder, one reviewer, no verifier), or a full unit? | Trivial lane: one new `.md`, held to the tree by U4-2 |
| Q5 | Item 8's claim check: is U6's curated pin table (six pins inside the existing citations gate, no new test file) enough for this plan, with a wider table growing pin by pin as facts are repaired, or does the owner want a standalone scanner and its own plan? | U6 as planned. A standalone scanner is its own plan: it needs a needle grammar for prose numbers, a registry the docs keep current, and a `tests:` row move in `.sdlc/baseline.md`; none of that is a docs repair |
| Q6 | U7 edits `README.md` (31 DD rows quote it) and re-points DD41 in `.sdlc/architecture.md`, a records edit inside this PR (R9). Accept, or place the tour in `ui-plan.md` only and leave README at grade B? | Accept: one row moves, U7-3 reads the check both ways, and the README is where a visitor looks first |

## Revisions

| Date | Change | Why |
|---|---|---|
| 2026-09-25 | written, status draft. Every audit finding re-verified at 282fca8d (table above); gap 4 found stale (the citations gate is in `TESTS`), so item 6 became P5 plus U6. Six units, 8 points. P3's two counts on this file's own lines: `0` and `0` | owner ruling R42 of 2026-09-25 on the docs audit |
| 2026-09-25 | revision 3, from the checkability review of revision 1 at 2ae50110 (23 green, 10 yellow, 4 red) and its rerun on revision 2 at 58c88f14 (23 green, 12 yellow, 6 red; `/private/tmp/claude-501/docs-repair-checkability.md`). The two reds the rerun added: P4's and U7-3's DD41 guard was `grep -E '^[+-]\|'`, an ERE with an empty branch that matches every line; both now read `grep -E '^[+-]. DD[0-9]+ '`, pipe-free, measured `0` on a DD41-only edit and `2` with DD40 moved too. The rerun's yellows on U6 and U7: no `want` field (a pin compares the doc needle's number with what `source()` reads, U6-4 guards both shapes); pin (d) reads the intent line's `a 53-role`, not any `53-role`; pin (c)'s slice stops before the `"Typography"` line (end-inclusive reads `12`); U7-2's paste control uses `app-shell.md` §1, which carries cites. Revision 1's reds: U3-8 dropped with Q3, the fact-sheet sentence is accurate history (#133, #219) and the fact sheet leaves the scope wall; U1-2's `tokens` and `controls` needles, already present four times each at `$B`, replaced by `typeSpecMode` and `geomMode` plus two inspector legs, Today corrected; U4-3 diffs against the unit's own base on `plan/docs-repair`; U6-5 (the review's U6-4) drops `.sdlc/baseline.md` from its pathspec and reads the `tests:` row instead, since U3 edits that file. Yellows: P5's control reads `tail -3`; P6 and U2-3 grep case-insensitively and catch `spec-draft.md`'s `Eight color formats:` line, with `docs/site` and `docs/spec` excluded and ruled Not in scope; U3-3 guards an empty `L`; U3-9 keeps the path in the numstat and allows `geometry.mjs` two lines; U1-3's `CATEGORY_INDEX` count is `4`; U2-4 counts nine glossary rows; U4-2's second control matches `binder/<name>`; U6-2's control runs on U6's parent, not `$B`; U6-3 (a) edits both `"system"` lines and anchors the pin; U3 is sized M (10 points), its Touches row names `describe-mcp-assets.js`; Q0 no longer offers a questions file; controls take `B` from the worktree. P1's Today carries the reviewer's green run | checkability review, 2026-09-25 |
| 2026-09-25 | revision 2, before activation, from the lead's two addenda: the gallery and categories views join gap 5 (U1's Revision B names `this.category` and the volumes; U7 tours them); item 7 adds U7 (S), the README tour before `## License`, with DD41 re-pointed in `.sdlc/architecture.md` and P4's fourth command holding every other row still (Q6); item 8 widens U6 from four pins to six (colour formats against the drawer's Colors group, roles against `role-table.json`), U6-4 refuses a typed literal, and Q5 asks whether a standalone scanner is wanted instead. Seven units, 9 points; U6 now waits for U1 as well as U3. Scope wall admits `README.md` and the DD41 line of `.sdlc/architecture.md`; the README row leaves Not in scope | lead messages of 2026-09-25 |
