verdict: 🔴
sha: beb4cf61b58a99a75ff492bdb43b6e847efb5cc8

# Verdict gs-U6-timing · 🔴

Read-only. No `npm test` or gate script run. Mechanical rows ran in a `git clone -q --shared` at `beb4cf61` in this seat's job tmp. Committed records carry no clock times, so run placement comes from the builder's own tool-call timestamps (transcript `subagents/agent-ags-U6b-builder-l3-p1-ef8f20457c26a074.jsonl` of session `b2e196e7`, converted UTC to PDT) cross-checked against its uncommitted raw log `scratchpad/window2-results.txt`. Each bracket below is the tool call's start and end, which contains the before reading, the run and the after reading.

One red, a record defect: the U6b handoff says rows 7 to 15 are all window 2, and rows 7, 10 and 13 are window 1. No counted row overlaps either rg-U4 interval, and every timing figure holds.

| # | Criterion | State | Evidence | Negative control |
|---|---|---|---|---|
| 1 | U6-3 formula over the Runs table: 15 counted, 0 failing, rejected listed | 🟢 | U6-3's three commands at `beb4cf61` print `15`, `0`, `32` (`hw.ncpu` 10) | Moving R20 (`hot after 1`, 77.94 s) into the counted table makes the second figure `1`, and the rule's own awk tests `$8!="0"` |
| 2 | Load under 5 at the start of every counted row (U6-10) | 🟢 | awk over the 15 counted rows: `load>=5: 0`, highest start `4.95` (row 11) | R18 read `5.09` at start and sits in the rejected table; typed into row 9 it would print `load>=5: 1` |
| 3 | Hot after is 0 on every counted row | 🟢 | Column 7 of all 15 rows reads `0`; raw log agrees, e.g. `RESULT\|3\|npm run gate:sweep-prime\|4.95\|0\|none\|6.46\|0\|0\|70.40` | R23 (`hot after 4`, 59.40 s) would fail U6-3 if counted; it is R23 in Rejected |
| 4 | Quiet window, rg-U4 interval 1 (22:00:30 to 22:03 PDT): no counted row overlaps | 🟢 | Only two window-2 attempts fall inside it, both rejected: R15 `21:59:58 to 22:01:23` (`hot after 1`) and R16 `22:01:29`, whose pgrep caught rg-U4 itself (`8076 sh -c npm run gen:figma-assets ...`). First counted window-2 row starts `22:14:57` | A counted row starting at 22:01 would overlap interval 1; R15 is exactly that case and the hot-after reading rejected it |
| 5 | Quiet window, rg-U4 interval 2 (22:07 to 22:14 PDT): no counted row overlaps | 🟢 | R17 `22:07:05 to 22:08:35` overlaps and is rejected (`load after 11.33`, `hot after 1`). Row 8 (99.90 s) starts `22:14:57`, 57 s after the interval's stated end, with pgrep `none` at start, so rg-U4's `sh -c ... test/run.mjs` was no longer alive | A counted row starting at 22:13 would overlap interval 2; had rg-U4 still been running at 22:14:57, row 8's pgrep cell would show its pid as R16's did |
| 6 | Placement of every window-2 counted row | 🟢 | Row 8 `22:14:57-22:16:38`, row 9 `22:17:44-22:19:11`, row 11 `22:27:56-22:29:07`, row 12 `22:30:28-22:31:36`, row 14 `22:40:17-22:41:15`, row 15 `22:51:27-22:52:25`, row 1 `22:58:18-22:59:47`, row 2 `00:14:34-00:15:54`, row 3 `00:17:36-00:18:56`, U6c-7 prime `00:19:32-00:19:49`, headless-boot `00:20:16-00:20:34`. All after 22:14, none inside either interval | Any bracket with an end after 22:00:30 and a start before 22:03, or an end after 22:07 and a start before 22:14, would be red; none has either |
| 7 | The record says which window each counted row came from | 🔴 | U6b handoff line 59: `Rows 1-3 and 7-15 were taken in window 2`. Rows 7, 10, 13 (`78.98`, `86.25`, `83.40`) are byte-identical to window 1's rows 7, 8, 9 at `c87d98fc`, first committed in `288ce31c` at `2026-09-23 19:13:30`, before window 2 opened at 21:58, and absent from `window2-results.txt`. Window 2 supplied rows 1-3, 8, 9, 11, 12, 14, 15. `baseline.md` states it right (`first reading window 1, other two window 2`); the handoff contradicts it. Fix: line 59 reads rows 1-3, 8, 9, 11, 12, 14, 15 window 2; rows 4-7, 10, 13 window 1 | If row 7 had been taken in window 2, `grep -c '78.98' window2-results.txt` would print `1`; it prints `0` |
| 8 | Window 2's date label | 🟡 | Handoff and `baseline.md` label window 2 `(2026-09-24)`. Seven of its nine U6b rows (8, 9, 11, 12, 14, 15 and row 1) ran on 2026-09-23 PDT, `22:14:57` to `22:59:47`; only rows 2, 3 and U6c-7 ran after midnight. Label should read 2026-09-23 to 2026-09-24 | Rows 2 and 3 (`00:14:34`, `00:17:36`) and U6c-7 (`00:19:32`) carry the 09-24 label correctly, so the label is right for those three and wrong for the rest |
| 9 | Overlap check is reproducible from committed records | 🟡 | `git show beb4cf61:.sdlc/handoffs/gate-split-U6b.md \| grep -cE '[0-2][0-9]:[0-5][0-9]'` prints `0`, same for U6c. Rows 4 to 6 above depend on a session transcript and an uncommitted `/private/tmp` log, the phantom-evidence pattern `baseline.md` already rules against | `baseline.md`'s own prior rows carry clock lines such as `UPTIME BEFORE: 17:36`; a Runs table with a start-time column would make rows 4 to 6 gradeable from git alone |
| 10 | Recorded figures match the raw log | 🟢 | All nine window-2 U6b rows match `window2-results.txt` field for field; U6c-7's `17.11` and `17.57` are the log's `real 17.11` and `real 17.57` lines (the wrapper's own wall read `17.12` and `17.58`; the plan names `/usr/bin/time -p` real, so the recorded figure is the right one) | A retyped `99.09` for row 8 would disagree with the log's `99.90` |
| 11 | P2 and the 120 s ceiling on the counted `npm test` runs | 🟢 | P2's command at `beb4cf61` prints `ok 89.1`; the three runs `89.10 / 79.93 / 80.07`, each under 120, load after `6.02 / 3.66 / 4.44` | Changing one baseline figure to `121.00` prints `OVER 121`; window 1's `171.23` printed `OVER 171.23` |
| 12 | U6c-7 timings under U4-4 and U5-4 ceilings, quiet rule held | 🟢 | prime `17.11` (ceiling 25 s), headless-boot `17.57` (ceiling 30 s); load before `3.37` / `3.49`, hot before and after `0`, pgrep `none`; the one prime attempt with `hot before 1` is recorded as rejected | The loud pre-narrowing prime reading `43.39` s (run A) exceeds 25 s and would grade 🟡 |
| 13 | U6-5: `baseline-agrees-check.sh` time lines | 🟡 | `grep -c '^ok    time gate:'` prints `4`; tail prints `stale total: 1`, the plan's expected is `stale total: 0`. The one STALE is `head: baseline ref c87d98fc is in origin/main's history`, P7's documented unit-branch exception; every `time` line reads `ok`. Blocks at pre-land until the close-out re-points `ref` (U6-9) | Changing one adapter gate range by one second prints `STALE time gate:` and raises the total to `2` |
| 14 | 32 rejected attempts recorded, none dropped | 🟢 | `grep -cE '^[\|] R[0-9]+ [\|]'` prints `32`; R15 to R32 (18) equal the 18 non-counted U6b attempts in `window2-results.txt`. U6c-7's one rejected prime attempt sits in the U6c handoff, so the two records hold 33 rejected attempts together | Dropping R17 from the table would print `31` against the log's 18 window-2 rejections, a visible gap |

Counts: 🟢 10 · 🟡 3 · 🔴 1. No counted row overlaps an rg-U4 interval.

verdict: 🔴
sha: beb4cf61b58a99a75ff492bdb43b6e847efb5cc8
