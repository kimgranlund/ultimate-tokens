# Review achromatic-anchor U1 · #739 · builder-l3 pass 1

Reviewer-l2, fresh context, 2026-09-23. Head `32a49d40` on `unit/aa-U1`, diff base `a17fa1ef`. Controls ran in `git clone -q --shared` scratch clones under `$TMPDIR/aaU1-rev-l2-p1` (`head` at `32a49d40`, `base` at `a17fa1ef`, `ctl` for edits, reverted after each control), removed after this review. I reran `test/engine/anchor.mjs`, `test/engine/okhsl.mjs` and `test/engine/tonal.mjs` at head myself (all `exit 0`) and rely on the builder's `npm test` for the rest.

| Criterion | State | Evidence | Negative control |
|---|---|---|---|
| U1-1 grey anchor takes the palette hue | 🟢 | head: `perceptual #808080 d300 5 d700 3`, `peak #808080 d300 0 d700 4`, `even #808080 d300 2 d700 2` | base clone prints `d300 151 d700 156`, `144 157`, `153 158` |
| U1-2 white reads `s = 0`, nothing else moves | 🟢 | head: `0 1.56e-5 254,254,254 255,255,255`; sweep of the `240..255` RGB cube finds `s===0` only at `255,255,255`, so `L >= 1 - 1e-6` swallows no real near-white | base clone prints `0.5582831888483675` first; deleting the guard line in `ctl` makes `okhsl.mjs` print `FAIL  white  ... 0.5582831888483675, want exactly 0` |
| U1-3 Nike tertiary-muted no longer khaki | 🟢 | head: `25 33.21 98.38 7 2.77 500:#F1F1F1 650:#B8969F 900:#3B1E27` (33.21 at most 98.38, 7 at most 12, 2.77 at most 3.50) | base clone: `25 31.47 98.38 105 8.10 500:#F8F1DE 650:#B89E57 900:#2F2815` |
| U1-4 corpus outside the constant byte-identical | 🟢 | `10188` lines each side; differing anchors `#ACADAE`, `#FFFFFF`; `6` lines (Nike tertiary-muted and travel "67° N" secondary-muted, three modes each); kit lines identical | constant set to `0.02` in `ctl`: `342` anchors and `1415` lines differ |
| U1-5 gate bites on the engine | 🟢 | head: `pass  achromatic-anchor: 27 of 27 ... 3 skipped under CAM16 C 5`; the 3 skips are `#FFFFFF` stop 300 in each mode (`#F5F7F9` CAM16 C `4.40`, OKLCH hue `248` against the twin's `251`), so the skip hides no wrong hue | forcing `achromatic` false in `ctl` (`< -1`): `FAIL  achromatic-anchor: 0 of 26`. The builder's base-clone control is not a real control: base lacks the `ACHROMATIC_ANCHOR_C` and `rgbToOklabChroma` exports, so its `exit 1` is an import error |
| U1-5 gate cannot pass vacuously | 🔴 | the skip has no floor: a ramp that renders every checked stop under CAM16 C 5 is skipped, and the gate prints `pass ... 0 of 0 ... 30 skipped` | the Q1 (b) shape the owner rejected (basis pinned to the anchor value for achromatic anchors, `groupIntendedH`/`groupIntendedS` in `ctl`) renders `#808080` as `hex300 #B7B7B7 hex700 #4E4E4E` and the gate line reads `pass  achromatic-anchor: 0 of 0`; the file reds only because the corpus notch list drops to `15 (expected 17)`, not because of this gate |
| U1-6 allow-lists re-measured, each re-freeze caused by the two anchors | 🟢 | Nike counts `anchor.mjs 4`, `prime.mjs 2`, `tonal.mjs 0`; `prime.mjs` diff is `0` lines. With `achromatic` forced false, the new entries go stale: `2 of the 23 cited baseline duplicates were not observed`, `peak: 1 of the 1 cited baseline dips were not observed`, `notch allow-list: expected member missing ... #ACADAE [peak]` | deleting Nike from `RAMP_WINDOW_ALLOW`: `2` FAIL lines; removing `#ACADAE [peak]` from `NOTCH_ALLOW`: `17 (expected 16)`; emptying `PERCEPTUAL_DIP_BASELINE`: `perceptual: 1 dip instance(s) beyond the cited baseline`; dropping dup key `150&175`: `1 duplicate-hex pair(s) beyond the cited list` |
| U1-7 prime ladder unmoved | 🟢 | `3780` lines each, `cmp 0` | `keyChroma = cam.chroma * 1.01` in `ctl`: `differ: char 241, line 2`, `cmp 1` (the builder did not run this control) |
| U1-8 §9 paragraph states the rule | 🟡 | `grep -c ACHROMATIC_ANCHOR_C` prints `1` (doc) and `3` (`tonal.js`); `audit-citations.mjs` last line identical at base and head. The paragraph says "its stored hue is rounding residue" of the anchor, but the anchor stores no hue: its measured hue is residue and the palette's stored hue is what the ramp takes. It also says the palette hue applies directly in all three modes, while even mode seeds from `effHue(palette.hue, hueSpace, ...)` | deleting the paragraph would make the doc count `0`; the paragraph is the only line in the doc naming the constant (`grep -c` `1`) |
| P3 no em dash | 🔴 | second count (backticks stripped) `13`: one in `okhsl.js` (the `rgbToOklabChroma` header comment), one in `00-synthesis.md`, eleven in the handoff. Third count (raw, handoff excluded) `2`, Expected `0` | branding `branding: clean (647 files scanned)`; the base's own lines are not counted, since the diff greps added lines only |
| P4 scope wall | 🟡 | first command prints `.sdlc/baseline.md` and `docs/reference/reviews/2026-08-20-reactivity/00-synthesis.md`, Expected `0`; second command `0` | both files are caused by this unit: `_okL` now sits at `tonal.js:926` (grep), and `baseline-agrees-check.sh` prints `stale total: 0` at head; the wall needs a plan revision or the Orchestrator's ruling, not a builder change |
| baseline figure and citation fix match their causes | 🟢 | `ui.html` grows `4253980` to `4260169` bytes (6.04 KB), the same delta as `4119.1` to `4125.1 KB`; the synthesis line changes only `907` to `926` | `baseline-agrees-check.sh` reads `stale total: 0` at head |
| engine rule itself | 🟢 | both anchored branches gate `targetOklchHue` and the seed on `anchor.achromatic` only; OKHSL branch uses `palette.hue`, CIE branch uses `effHue(palette.hue, controls.hueSpace, hueAnchorFrac(...))`. With `hueSpace: "cam16"` (untested by the gate) `#808080` stays within `11` of the twin at hues 30, 100, 250 and 300 | `#808082` (OKLab C `0.0030`) prints the same hexes at base and head (`hex300 #B5B5C6` perceptual both sides) |

## Findings, by severity

1. 🔴 The `achromatic-anchor` gate can pass vacuously. It needs a floor on checked cells, for example `skipped <= 3` or `bound >= 27`, or a check that stop 700 carries CAM16 C at least 5, so a neutral-ramp regression reds the gate itself. The Q1 (b) control above is the proof.
2. 🔴 P3: remove the em dash from the new `okhsl.js` comment (plan: engine comments carry none) and the eleven outside backticks in the handoff. The `00-synthesis.md` glyph is inherited on a one-number edit: rewording it or a plan revision that allows it clears the third count.
3. 🟡 P4: `.sdlc/baseline.md` and `00-synthesis.md` are outside the scope wall. Both are caused by this unit and correct; the Orchestrator records the allowance as a plan revision.
4. 🟡 §9 wording: "its stored hue is rounding residue" should say the anchor's measured hue; say that even mode seeds through `effHue` and `hueSpace`.
5. 🟡 The handoff marks U1-6 and U1-7 controls "n/a" and "not separately run", and its U1-5 base control reds on missing exports rather than on the rule. This review ran all three for real (table above); the handoff should cite real controls next pass.
6. Info: `origin/main` has moved to `b801b71f` since `a17fa1ef`, and none of the new commits touch `src/engine`, `test/engine`, §9 or `.sdlc/baseline.md`.

verdict: 🟡
