---
status: approved
ticket: "#701"
priority: P1
lane: color-engine
size: M (U1 M = 2, U2 M = 2, U3 S = 1; 5 points)
labels: kind:defect · P1 · size:M · lane:color-engine · mode:multi (issue #701 carries `size:small` from intake; the adapter's `size:M` is this plan's, two label families per adapter X3)
written: 2026-09-19
head: 730ff941 (`plan/chroma-floor` = `origin/main`, nothing pushed); measured against `unit/pif-u4-integration` @ 326592d2 (#681 U4 pass 1); the branch moved to e2df6a42 (U4 pass 2, five commits) during revision 3 and the rows that read it say so; revision 6 revalidated against `plan/preset-intent-fidelity` @ `505bf565` (the U7 checkability commit); revision 7 re-measured against that branch's tip `505416d7` (47 commits later, U7 verified and merged, still unlanded; `test/engine/tonal.mjs` and `scripts/report-preset-fidelity.mjs` are byte-identical between the two, `test/engine/anchor.mjs` differs by one import and one default-kit subject row, none of it in this plan's greps). This is the base every unit builds on until #681 lands (the owner's 2026-09-20 follow-up in `chroma-floor-approval.md`), not a reading of the eventual `origin/main` head; revision 10 re-read every figure at `<base>` = 282fca8d (`origin/main` merged into the plan branch at 4381e860, the start gate green), the section 'Re-read at 282fca8d'
branch: plan/chroma-floor
depends: "#713 (gate-split) landed on `origin/main`, which implies #681 landed (#713's own G0 gate). Revision 8 sequences this plan after #713 because #713 moves the gates C1 to C4 and C12 read (`tonal.mjs` and `anchor.mjs` become sampled by default and full under `--full`, `gate:corpus-tonal` / `gate:corpus-anchor` / `gate:sweeps` appear, the `sweeps` CI matrix appears, `TESTS` reads 50, the adapter's §1 and §2 name every gate and every PR job). The start gate in Units decides it by command. `<base>` is then the `origin/main` commit `git log -1 --format=%h -S'"gate:sweeps"' origin/main -- package.json` prints (the commit that introduced the `gate:sweeps` script; revision 9: a subject grep for `(#713)` could match an sdlc record commit first, since `origin/main` already carries such suffixed record commits, e.g. 88905891 `(#709)`), or a later `origin/main` commit the Orchestrator names, recorded in each unit handoff's `base:` row; the verifier confirms `git merge-base plan/chroma-floor origin/main` equals it. The owner's 2026-09-20 follow-up (`.sdlc/questions/chroma-floor-approval.md`, question 3: start off #681's plan tree) is superseded by this sequencing, proposed by the Orchestrator and put to the owner under item 1 (re-approve), because it delays the plan and reverses a recorded owner answer (revision 9); if the owner wants the earlier start back, the units build off `plan/preset-intent-fidelity` @ 505416d7 and every row that says `gate:corpus-anchor` / `gate:corpus-tonal` reads `node test/engine/anchor.mjs` / `node test/engine/tonal.mjs` (full by default there) and the `sweeps` rows read the `corpus-contrast` job shape instead. Every measured 'Today' figure below is at 505416d7, the freshest #681 tree; it is re-read at `<base>` before U1 starts. Revision 10 (2026-09-24): the start gate printed green (`a62ec020`, `1`, `1`, `50`) and `<base>` = 282fca8d, the `origin/main` tip the Orchestrator MERGED into the plan branch (4381e860) rather than reset it onto: the branch already carried nine pushed revision commits plus the approval and checkability records, a merge keeps that history reachable, and `git merge-base plan/chroma-floor origin/main` still prints 282fca8d, which is the check the verifier runs. Three plans landed between 505416d7 and `<base>` that touch this plan's files: #713 gate-split (a62ec020), #739 achromatic-anchor (9638894c) and #738 okl-memo (cbca0aec); every 'Today at 505416d7' figure is re-read at 282fca8d in the section 'Re-read at 282fca8d' and every line pin is re-pinned there."
inputs: gh issue #701 (body and the 2026-09-20 scope comment), `.sdlc/plans/preset-intent-fidelity.md` (git show plan/preset-intent-fidelity, revision 26; C6, C7, C8, U3, U4, Q-D under U2), `.sdlc/questions/pif-u3-yield-answer.md` (ruling 2), `.sdlc/questions/standing-rulings-2026-09-20.md` (R2, R3, "Lone spike: name the 64 and gate the count; fix joins #701"), U4 review pass 1 §1 and the lone-spike decision memo (owner ruled 2026-09-20: option (b) for #681, option (a) here), `.sdlc/handoffs/pif-u4.md` §7.3 and §7.6, `.claude/skills/color-math/SKILL.md` and `references/foundations.md` §5, `src/engine/tonal.js` (`evenChroma`, `chromaEnvelope`, `anchorChromaBasis`, `paletteStopsAnchored`, `paletteStops`), `test/engine/tonal.mjs` (`chroma-floor`, `chroma-envelope` (iv) dip gate, `EVEN_DIP_BASELINE`), `test/engine/anchor.mjs` (`loneSpikeStop`, `notchOk`, `NOTCH_ALLOW`, the `anchor-f4 hueSpace-*` gates), `scripts/report-preset-fidelity.mjs`; revision 6 additionally reads `plan/preset-intent-fidelity` at its current tip `505bf565` (U7 in flight, not yet landed), `.sdlc/handoffs/pif-prepr-review-p1.md` (the pre-land review's blast-radius section), gh issue #725 (`kind:bug`, `size:big`, owns the perceptual/peak median and p90 miss, explicitly excludes even mode), and `test/engine/anchor.mjs` / `test/engine/tonal.mjs` at that tip directly (grep-verified, no test run); revision 7 reads `plan/preset-intent-fidelity` @ `505416d7` in a detached scratch worktree (`test/engine/anchor.mjs` lines 774 to 1029, `test/engine/tonal.mjs` lines 1395 to 1667 and 1137 to 1138, `scripts/report-preset-fidelity.mjs`, `test/run.mjs`, `.sdlc/checks/baseline-agrees-check.sh` at `505416d7` and at `origin/main` 2836d727, `.sdlc/adapter.md` §1, `.github/workflows/ci.yml`), gh issues #713 and #715, and the owner's 2026-09-20 follow-up in `chroma-floor-approval.md`
measurements: read-only, planner, 2026-09-19, scratch worktree of 326592d2; scratch scripts cf-measure.mjs, cf-probe2.mjs, cf-probe3.mjs (patched copies of `tonal.js` loaded from data URLs, the technique the dip gate's own negative controls use; the engine file was never edited); host 10 cores, load 9.84 10.07 9.62 at the first run, 5.67 to 15.67 across the session, other agents' gates running concurrently. Revision 7, 2026-09-22, scratch worktree of `505416d7`, no source edited, no test file run: `node scripts/report-preset-fidelity.mjs --envelope` (exit 1, 94.35 s wall at load 5.88 6.38 6.30 start, 20.33 11.45 8.28 end) and its `--damp-amp 55` control; scratch `cf-gatepath.mjs` (the shipped `findDips` predicate and corpus with `anchor` omitted, 16.7 s at load 20.33) and its 1.6x-floor data-URL control (17.4 s); the FLOORS comparator run on the `505416d7` file against itself (`changed 0, down 0`, exit 0) and against 730ff941's (48 changed, 33 down, all #681's); every grep count below run on `505416d7` and, as the failing control, on 730ff941 (where `anchor.mjs` does not exist and each count reads 0). Revision 10, 2026-09-24, planner, read-only, a `git clone -q --shared` of 282fca8d under the job's tmp dir (deleted after), no source edited, every heavy run in the foreground on a 10-core host: `npm run -s gate:corpus-anchor` (exit 0, 109.9 s wall, 1-minute load 8.50 at start and 5.91 at end), `npm run -s gate:corpus-tonal` (exit 0, 119.5 s, load 5.91 / 6.55), `node scripts/report-preset-fidelity.mjs --envelope` (exit 1, 31.4 s, load 5.75 / 5.33), a scratch `cf-gatepath.mjs` rebuilt from `findDips`' own shape at `tonal.mjs:1615` (four sweeps, 51.3 s: rendered, gate path, and the 1.6x-floor data-URL control on each), the C8 FLOORS comparator on the 282fca8d `semantic.mjs` against itself, `sh .sdlc/checks/baseline-agrees-check.sh`, `node test/engine/semantic.mjs`, `npm run gate:corpus-contrast`, `node test/ui/headless-boot.mjs`; every grep count on the same tree; `test/engine/semantic.mjs` and `scripts/report-preset-fidelity.mjs` are byte-identical between 505416d7 and 282fca8d (`git diff` empty), so their pins stand
---

# Even ramps carry no chroma artifact at or beside the anchor stop: the even envelope has a shoulder at stop 500, the chroma floor cannot cross it, and both named allow-lists (53 even dips, 64 lone spikes) retire as gates that red if a name survives

## What #701 owns, and what it builds on

#681 lands with two named exceptions on the even tone path, each with a count gate, each ruled by the owner as a carve-out for that plan with the fix routed here:

| List | Where | Names | Ruling that parked it |
|---|---|---|---|
| `EVEN_DIP_BASELINE` | `test/engine/tonal.mjs`, `chroma-envelope` gate (iv), even mode | 53 (51 at stop 350, 2 at 650) | `pif-u3-yield-answer.md` ruling 2, plan revision 24 |
| the lone-spike allow-list | `test/engine/anchor.mjs`, `anchor-ramp lone-spike`, even mode, 25-stop ramp | 64, all at stop 500, each the palette's own anchor hex | standing ruling 2026-09-20 ("name the 64 and gate the count; fix joins #701"); #681 U4 is adding the list on the integration branch, so its identifier is not known at this head (see U1) |

Both lists describe the same construction. After #681 U3's pass 7, the even envelope is `1 - damp' * |sd|^(0.375)` around the anchor (`EVEN_DAMP_FACTOR` 0.25 scales both `damp`'s headroom and `dampCurve`), and the floor is `chromaFloor% * maxc(stop)`, gamut-relative at every stop. Two artifacts follow:

1. The envelope exponent is under 1, so the envelope has infinite slope at the anchor: one stop away (`|sd|` = 50/450) it is already 0.594 at the shipped `damp` 70. With U2's pass-through pinning stop 500 to the anchor's full chroma and U3's `dampAmp` 0 removing the only term that used to prop the shoulders, stops 450 and 550 render at about 60% of stop 500 in CAM16 C, and for muted anchors that lands them under the gate's 0.05 OKLCH bound while 500 sits at 0.07 to 0.09. That is the 64 lone spikes (U4 review pass 1 §1, root-caused by a single-variable swap: the same engine with `dampAmp` 55 reads 0).
2. The floor is shaped by `maxc(stop)`, whose hue-cusp-driven curve is not monotone, so in a narrow band the floor has passed its local peak while the damped value has not caught up: a local minimum of 3 to 4 CAM16 C. That is the 53 named dips, measured on U3's branch at stops 350 and 650.

The witness, architecture "Komsomolskaya Station" tertiary, anchor `#346190`, lift 0, skew 0, group chroma 37, `damp` 70, `dampCurve` 1.5, `chromaFloor` 40, measured at 326592d2 on the rendered path (the numbers the product emits):

| stop | hex | tone | CAM16 C | maxc | floor (40% of maxc) | envelope | basis | envelope × basis | OKLCH C |
|---|---|---|---|---|---|---|---|---|---|
| 400 | #748CA6 | 57.3 | 22.82 | 57.05 | 22.82 | 0.474 | 35.30 | 16.73 | 0.0480 |
| 450 | #5F768F | 48.8 | 22.53 | 51.83 | 20.73 | 0.594 | 36.56 | 21.72 | 0.0475 |
| 500 | #346190 | 40.1 | 37.02 | 48.41 | 19.36 | 1.000 | 37.02 | 37.02 | 0.0911 |
| 550 | #3E546C | 35.0 | 22.53 | 43.03 | 17.21 | 0.594 | 36.56 | 21.72 | 0.0482 |
| 600 | #37485B | 30.0 | 19.11 | 39.25 | 15.70 | 0.474 | 35.30 | 16.73 | 0.0390 |

At 450 and 550 the damped value (21.72) is above the floor (20.73 / 17.21), so the floor is not what makes the spike; the envelope's own shape is. At 400 the floor binds (22.82 = 40% of 57.05) and lifts the stop above 450, which is the second mechanism in the same ramp. So the ticket's two asks are one redesign with two terms: a shoulder (neighbourhood term) in the even envelope at the anchor stop, and a floor that is envelope-relative in the interior and gamut-relative only near the extremes.

## Measured at 326592d2 (the integration head, before #681's U4 rework lands)

Two readings exist for every even-mode number, and they disagree. The gate path renders the palette WITHOUT its anchor (`paletteStops({hue, chroma: rampChromaOf(pal, doc), skew, lift, ...})`, the shape `EVEN_DIP_BASELINE`'s `findDips` and `report-preset-fidelity.mjs --envelope` both use). The rendered path renders it WITH the anchor (`paletteStops` dispatches to `paletteStopsAnchored` when the palette carries one, which is what `projectView` does for 3,380 of the 3,780 curated palettes). #681's rendered-path rule (its criteria preamble, revision 14) says a criterion that measures product colour reads the rendered path. The two gates above were written before U2's anchored branch existed and were never moved; this plan moves them (C3, C4, C5) and keeps the gate-path reading beside them, because the non-anchored path is what a user's own palette renders.

| Reading | Value at 326592d2 |
|---|---|
| corpus | 343 presets, 3,780 palettes, 3,380 anchored; 1 preset (`dampAmp` 70, the Adia kit, 16 palettes) skipped by every `dampAmp`-0 gate, so those gates sweep 3,764 palettes; the default kit (16, `dampAmp` 0) is outside the dip and lone-spike sweeps today (other gates in `anchor.mjs` do include it: `controlSubjects` at lines 172 to 176 asserts 3,796) |
| `EVEN_DIP_BASELINE` names | 53 |
| even dips, gate path (non-anchored, `rampChromaOf`, 19 + 25 stops, `dampAmp`-0 docs) | 0; 0 of the 53 names observed. The gate reds today as "53 of the 53 cited baseline dips were not observed" (U4 handoff §7.3 records the same and left the list in place because deleting it broke the gate's own negative control) |
| even dips, rendered path (anchor passed), 25-stop ramp, at least 3 CAM16 C below both neighbours | 90: 57 at stop 450, 32 at stop 500, 1 at stop 550; 49 of the 90 dip stops sit exactly on the floor (`chroma === chromaFloor% * maxc`); depth 44 in [3, 4), 45 in [4, 6), 1 at 6 or more. With the 19-stop ramp added, 180 instances |
| lone spikes, rendered path (`loneSpikeStop` predicate, 25-stop, even) | 64, all at stop 500, all the palette's own anchor hex; plus 1 in the default kit, Data 7 stop 500 (`#669291` `#088585` `#436E6E`, OKLCH C 0.0480 0.0940 0.0478), ungated today because the gate sweeps `presetsByCat` only |
| even palettes whose 450 or 550 CAM16 C is under 50% of stop 500 | 0 (the spike is a 60% shoulder, not a collapse) |
| `report-preset-fidelity.mjs --envelope`, even, reading (a), gate path | 100: 10.9% / 16.2%, 300: 39.1% / 52.2%, 700: 39.0% / 44.6%, 900: 16.3% / 16.5% (median / p90, bars 25/35 and 75/90), above 100%: 0, `env(500) = 1` sweep PASS. Perceptual 300 reads 79.9% / 97.9% FAIL (the yielded cell, #681 revision 24, not this plan's) |
| the same four cells on the rendered path (anchored, source CAM16 C at least 10, 25-stop) | 100: 15.6% / 36.9%, 300: 48.0% / 113.2%, 700: 42.5% / 79.8%, 900: 22.9% / 51.1%. No gate reads this today; C5 reports it, does not bar it (ruled Q2) |
| perceptual fingerprint (sha256 over the sorted `label + 25 hexes` lines of all 3,780 `projectView` ramps, first 16 hex digits) | `f4ed35e36edd6b9b` |
| peak fingerprint, same method | `6e6d81e23ab10661` |
| `NOTCH_ALLOW` (Q-C, all modes) | 78 names at `test/engine/anchor.mjs:505`, "pending U4"; the gate observes 15 at this head and reds `15 (expected 78)`; at e2df6a42 (U4 pass 2) the list holds 142 names (entries starting with a backtick; revision 3's 165 also counted comment lines carrying one). #681's to settle |
| `TESTS.length` in `test/run.mjs` | 48 on 326592d2 and 48 on `origin/main` 730ff941; `.sdlc/baseline.md` reads `all 48 test files passed` |
| `node test/engine/anchor.mjs` (load 12.80 14.13 11.90 at start) | exit 1, 3 gate failures: `anchor-ramp monotone: 0` pass; `distinct (25-stop) allow-list: 16 (expected 16)` pass; `notch allow-list ... 15 (expected 78)` FAIL (the population moved under U4, #681's); `lone-spike ... 64 (expected 0, no allow-list ...)` FAIL; `anchor-f4 hueSpace` even live at max OKLab dE 0.0486 pass; `hueSpace-perceptual-bound` max dE 0.0048 (codes max 2) pass; `hueSpace-peak-bound` max dE 0.0048 (codes max 1) pass |
| `node test/engine/tonal.mjs` | exit 1, 3 gate failures, 16 passes: `chroma-floor` pass, `skew-lift-okhsl` pass, `okhsl-modes` pass; FAIL `chroma-envelope` "(iv dip gate) peak: 6 of the 6 cited baseline dips were not observed" (the even list reads the same way, 0 of 53, per the planner's own run of `findDips` and U4 handoff §7.3); FAIL `hue-solver-best` (a stop-500 cell re-pin) and FAIL `intensity-legacy` (`#9789FA` vs fixture `#9789FB`), both #681 U4's, not this plan's |

## Re-read at 505416d7 (revision 7, the base the units build on)

#681's U4 pass 2 addenda and U7 moved four of the mechanisms the 326592d2 table describes. What changed and what did not, each row measured or grep-counted at `505416d7` with 730ff941 as the failing control:

| Reading | Value at 505416d7 | Moved since 326592d2? |
|---|---|---|
| corpus | 3,780 palettes, 3,380 anchored; `dampAmp`-0 sweeps cover 3,764 (the Adia kit's 16 skipped by name, `ADIA_CARVEOUT` at `tonal.mjs:1297`); the default kit's 16 are now IN both the lone-spike sweep (`anchor.mjs:977` to 996, a separate loop) and the dip sweep (`dipDocs` at `tonal.mjs:1563`) | the kit joined both sweeps |
| `TESTS.length` | 49 (`anchor.mjs` registered); 48 on `origin/main` until #681 lands; `.sdlc/baseline.md` reads `all 49 test files passed` | yes, 48 to 49 |
| lone-spike gate | TWO allow-lists, two printed lines matching `lone-spike`: `LONE_SPIKE_ALLOW` (64 names, `anchor.mjs:789`, 11 lines mention it) compared by `allowListMatches` at line 998, and `DEFAULT_KIT_SPIKE_FINDING` (1 name, Data 7 `#088585` stop 500, line 989, 6 lines) at line 991; both feed the R10 drop/swap negative controls at lines 1018 and 1028; `loneSpikeSorted.length === 0` is on 0 lines | yes (326592d2 had no list and the `=== 0` assertion) |
| `NOTCH_ALLOW` | 15 entries (the literal at `anchor.mjs:608` is 19 lines; each entry ends in `[perceptual]`, `[peak]` or `[even]`, and the gate prints `notch allow-list ... 15 (expected 15)` at 505416d7, review of revision 7). Revision 7's "79" and revision 6's "102" were miscounts whose awk range ran past the notch list's closing bracket into `LONE_SPIKE_ALLOW` (15 + 64 = 79); revision 4's 142 was likely the same error at e2df6a42 (78 + 64) | the gate's own printed count is the only figure of record; 15 at 505416d7 |
| even dip gate (iv) | `EVEN_DIP_BASELINE` 90 names keyed `preset|palette|stop` (`tonal.mjs:1441` to 1532; 57 at 450, 32 at 500, 1 at 550), `findDips` passes `anchor: pal.anchor` (the RENDERED path, line 1545), both stop sets, `dipDocs` includes the kit, FAILs on an unlisted dip and on a listed dip not observed, mode tripwire at line 1594, no unconditional count line; the even negative control (1.6x floor, line 1650) must exceed 90 | yes: the 53-name gate-path list is gone, the gate reads the path the product renders |
| even dips, gate path (anchor omitted, same predicate, same corpus + kit, 19 + 25 stops) | 0 (scratch `cf-gatepath.mjs`); control with the floor 1.6x: 155 (400: 118, 450: 32, 550: 5), so the reading discriminates | no (0 at 326592d2 too); no gate takes this reading any more |
| `report-preset-fidelity.mjs --envelope` | reads the RENDERED path since fa0264fa (U4 pass 2 round 2, `anchor: pal.anchor` at line 99); READING (a) even: 100: 15.6% / 37.0%, 300: 48.4% / 113.7%, 700: 42.5% / 80.2%, 900: 22.9% / 52.0%, `above 100% of stop 500: 670 FAIL`; C6 (v) even 1,205 / 3,764 violators, max 17.18x (report-only); exit 1; the `--damp-amp 55` control reads 19.6 / 51.5, 92.2 / 158.4, 86.1 / 161.0, 36.7 / 77.6, above 100% 2,897 | yes: the four ruled even cells were measured on the gate path and no instrument prints that reading today; `test/engine/tonal.mjs` gates the gate-path `0 above 100%` clause only (`above100Violators`, deliberately without `anchor`, line 1314), not the median / p90 cells |
| perceptual and peak blocks of READING (a) | perceptual 17.4 / 35.1, 94.0 / 146.2, 74.5 / 119.3, 31.7 / 66.5, 446 cusp-run violations; peak 11.7 / 23.6, 84.0 / 137.7, 68.8 / 111.1, 29.3 / 62.1, 2,592 above 100% | #725's, not this plan's; C5 pins them by md5 |
| `chromaEnvelope(` count | 1 export, 5 mentions; the literal assert is `tonal.mjs:1137` (`callCount !== 5`), the `1 + ((controls.dampAmp` absence assert is line 1138 | line numbers only |
| `SAT_FLOOR_EXCEPT` | 11 stops at `tonal.mjs:350`, stale-list self-red at 359 | no |
| `FLOORS` | `semantic.mjs:252`; comparator on the file against itself `changed 0, down 0` exit 0 | shape unchanged |
| `scripts/report-preset-fidelity.mjs --movement` | does not exist (the file's header says U4 did not build it); only `--envelope` and `--damp-amp N` | the blast-radius section cited it |
| `.sdlc/checks/baseline-agrees-check.sh` | exits `stale ? 1 : 0`, never the stale count; a `head` line for a ref that is an ancestor of `origin/main` is `ok`, the tree-moved line is a `note` that counts toward nothing (same text at `origin/main` 2836d727, 3 removed lines between the two, none in that logic) | C12's "exit 3" was never what the script does |
| CI gate scripts | one, `gate:corpus-contrast` (`package.json:30`, ci.yml job `corpus-contrast`) at 505416d7 and on `origin/main` today; `plan/gate-split` (#713, `e035f841`, U1 to U5 and U6a merged, U6b pending, not landed) adds `gate:corpus-tonal`, `gate:corpus-anchor`, `gate:sweep-prime`, `gate:corpus-reset`, `gate:sweeps` and a `sweeps` matrix job (`ci.yml:111` to 127 there, `- run: npm run ${{ matrix.gate }}`), and its `test/run.mjs` lists 50 files | this plan starts after #713 lands and its two new sweeps join that matrix |
| `hueSpace-<mode>-bound`, `hpg-role-contrast`, `skew-lift-okhsl`, `(hs)` | all present (`anchor.mjs:1188` builds the key per mode; `semantic.mjs` 1 line; `tonal.mjs` 21 lines; `headless-boot.mjs` 4 lines) | no |

## Re-read at 282fca8d (revision 10, `<base>`)

Between 505416d7 and `<base>` three plans landed on `origin/main` that touch this plan's files: #713 gate-split (a62ec020: the FULL/SAMPLED split in `test/engine/anchor.mjs` and `tonal.mjs`, the `gate:*` scripts, the `sweeps` CI matrix, `TESTS` 50), #739 achromatic-anchor (9638894c: `ACHROMATIC_ANCHOR_C` and the achromatic hue branch in `tonal.js`, `NOTCH_ALLOW` +2, `KNOWN_BASELINE_DUP` +2, one-name `DIP_BASELINE` and `PERCEPTUAL_DIP_BASELINE`) and #738 okl-memo (cbca0aec: the `_okL` memo deleted from `tonal.js`, an okl-order gate in `tonal.mjs`). `git diff --stat 505416d7 282fca8d` on this plan's files: `tonal.js` 168 lines, `anchor.mjs` 283, `tonal.mjs` 215, `headless-boot.mjs` 57, `adapter.md` 65, `baseline-agrees-check.sh` 24, `test/run.mjs` 2; `semantic.mjs` and `report-preset-fidelity.mjs` unchanged. #739's engine change is hue-only on the two anchored branches (`tonal.js:729` to 730 for the even path, 1003 to 1005 for OKHSL: an achromatic anchor takes the palette's stored hue); `chromaEnvelope`, `evenChroma` and `anchorChromaBasis` are not in its diff, so U1's shoulder term and U2's floor meet the same chroma code they were planned against. #738's engine change is the memo deletion in `okhslLAt` (OKHSL path only, C6's territory, byte-identical output by its own gate).

Every figure, 505416d7 beside 282fca8d, each from the run named in `measurements:`:

| Reading | 505416d7 | 282fca8d (`<base>`) | Moved? |
|---|---|---|---|
| start gate | nothing, 0, 0, 48 | `a62ec020`, `1`, `1`, `50` | green |
| corpus | 3,780 palettes, 3,380 anchored, `dampAmp`-0 sweeps 3,764 + kit 16 | same: the scratch dip sweep visits 3,780 palettes (3,764 + 16), `anchor-ramp: 10110 exact, 0 off (in-window sources only, 3370 of 3380)` | no |
| `TESTS.length` | 49 | 50 (`engine/corpus-sample.mjs` registered by #713 U1); `.sdlc/baseline.md` reads `all 50 test files passed` | yes, C1 reads 50 |
| gate scripts | `gate:corpus-contrast` only | `gate:corpus-tonal`, `gate:corpus-anchor`, `gate:sweep-prime`, `gate:corpus-reset`, `gate:corpus-contrast`, and `gate:sweeps` running the five; the `sweeps` matrix in `ci.yml` lists the same five, run line `npm run ${{ matrix.gate }}` | yes, as revision 8 planned; C4 and C6 add the sixth and seventh legs |
| lone-spike gate | two lines, 64 and 1 | two lines: `pass  anchor-ramp default-kit lone-spike ... 1 (expected 1)` (line 1061) and `pass  anchor-ramp lone-spike allow-list ... 64 (expected 64)` (line 1068, now through `allowListOk`, #713's FULL/SAMPLED wrapper at 981, which delegates to `allowListMatches` under `--full`); greps 16, 0, 2 | no (line numbers only, and the wrapper name) |
| `NOTCH_ALLOW` | 15 | 17 (`17 (expected 17)` printed; 15 `[even]`, 1 `[peak]`, 1 `[perceptual]`, the two new being the #ACADAE travel palette under peak and perceptual at `anchor.mjs:681` to 682, #739); the even population is the same 15 | yes, +2, neither in even mode |
| even dip gate (iv) | 90 names, no count line | same 90 names (57 at 450, 32 at 500, 1 at 550, re-measured by the scratch sweep on the rendered path), no `dip-gate even` line, 22 gates pass, exit 0; `BASELINE_BY_MODE` now has three entries and `DIP_BASELINE` / `PERCEPTUAL_DIP_BASELINE` hold one name each (`TICKET_739_DIP`, the #ACADAE travel palette at stop 500) | K = 32 unchanged; the peak and perceptual lists moved from empty to one name (#739's, not this plan's) |
| even dips, gate path (anchor omitted) | 0; control 155 (400: 118, 450: 32, 550: 5) | 0; control 155 (400: 118, 450: 32, 550: 5); the same control on the rendered path reads 433 (400: 2, 450: 390, 500: 41) | no |
| `--envelope` READING (a) even (rendered) | 15.6 / 37.0, 48.4 / 113.7, 42.5 / 80.2, 22.9 / 52.0, above 100% 670; C6 (v) even 1,205 / 3,764, max 17.18x | identical, exit 1 | no |
| perceptual and peak blocks | perceptual 17.4 / 35.1, 94.0 / 146.2, 74.5 / 119.3, 31.7 / 66.5, 446; peak 11.7 / 23.6, 84.0 / 137.7, 68.8 / 111.1, 29.3 / 62.1, 2,592 | perceptual identical; peak 900 median 29.4 (was 29.3), the rest identical; md5 of the C5 extraction `6e558839ee9e43217e1e2f7afc898b7b` | yes, by #739's achromatic hue branch (two anchors, #ACADAE and #FFFFFF); C5 and C6 key to `<base>`, so the 505416d7 md5 and the 326592d2 fingerprints are history |
| `chroma-floor`, `skew-lift-okhsl`, `chroma-envelope`, `okhsl-modes` | pass | pass, 22 pass lines, 0 FAIL | no |
| `GRID_R2_EXCEPTIONS` / `KNOWN_BASELINE_DUP` | 21 / 21 | 21 / 23 (#739 added two Nike tertiary-muted peak keys, its comment at `tonal.mjs:1249`) | yes, C7 reads 23 |
| `chromaEnvelope(` | 1 export, 5 mentions | 1 and 5; the asserts moved to `tonal.mjs:1161` (`callCount !== 5`) and 1162 | line numbers only |
| `SAT_FLOOR_EXCEPT` | 11 stops at 350, self-red at 359 | 11 stops at 362, self-red at 371 | line numbers only |
| `FLOORS` | `semantic.mjs:252`, comparator `changed 0, down 0` | same file, same line, `16, 16, 16`, `changed 0, down 0`, exit 0; even Neutral 7.2 / 4.6, even Primary 7.4 / 4.7 (the C8 control's figures still apply) | no |
| `role-contrast` | not run | `pass  role-contrast` (the gate prints `role-contrast`; `hpg-` is only in the source comment at `semantic.mjs:152`, so C8's revision-9 grep was vacuous and is re-pinned) | C8 grep |
| `gate:corpus-contrast` | not run | `PASS: every measured curated preset's accent clears 4.5:1 against its own on-color` | no |
| `hueSpace` bound lines | even live 0.0486, perceptual 0.0048 (codes 2), peak 0.0048 (codes 1) | identical, printed as `full corpus + default kit` (key built at `anchor.mjs:1263`) | no |
| `(hs)` shim group | 4 source lines | 4 source lines; the shim's `ok()` (`headless-boot.mjs:17`) records failures only, so a green run prints no `(hs)` line and C9's grep on the output was vacuous (re-pinned to the PASS line plus the source count); the run ends `HEADLESS BOOT PASS`, exit 0 | C9 grep |
| `baseline-agrees-check.sh` | 10 lines, `stale total: 0`, exit 0 | 14 lines (13 plus `stale total`: four `time gate:*` rows and the `note` head line added by #713 U6b), all `ok`, `stale total: 0`, exit 0; the gate list is a LITERAL array at lines 24 to 33, so a gate the script does not name is never checked | C12: U1 and U2 append their gate rows to that array |
| `adapter.md` | §2 CI-on-a-PR at line 46 named two jobs | line 92 names four (`build-test`, `panda-smoke`, `corpus-contrast`, `sweeps`); "no branch protection" at 91; `.claude/CLAUDE.md`'s SDLC paragraph names the same four | as C4 and C6 assume |

Every line pin the unit text and criteria carried at 505416d7, re-pinned at 282fca8d (the identifier is what a builder searches for; the number is where it was on 2026-09-24):

| File | 505416d7 | 282fca8d | Identifier |
|---|---|---|---|
| `anchor.mjs` | 608 | 659 (closes at 685) | `NOTCH_ALLOW` |
| `anchor.mjs` | 774 to 854 | 834 (comment) and 849 to 914 (literal) | `LONE_SPIKE_ALLOW` |
| `anchor.mjs` | 977 to 988 | 1047 to 1058 | the separate default-kit lone-spike loop (`dkSpikeDoc`, `defaultKitSpikeNames`) |
| `anchor.mjs` | 989, 991 | 1059, 1061 | `DEFAULT_KIT_SPIKE_FINDING` and its `allowListMatches` line |
| `anchor.mjs` | 998 | 1068 to 1071 (echo at 1073) | `allowListOk(loneSpikeSorted, LONE_SPIKE_ALLOW)` (was `allowListMatches`) |
| `anchor.mjs` | 1018, 1028 | 1093, 1103 | `dropCheck("lone-spike", ...)`, `swapCheck("lone-spike", ...)` |
| `anchor.mjs` | 1188 | 1263 | the `hueSpace-${modeName}-bound` print |
| `anchor.mjs` | 172 to 176 | 241 to 248 | `controlSubjects` (3,796) |
| `tonal.mjs` | 32 | 35 | `FAIL` (first message per gate name) |
| `tonal.mjs` | 350, 359 | 362, 371 | `SAT_FLOOR_EXCEPT`, its stale-list self-red |
| `tonal.mjs` | 967 | 986 | `GRID_R2_EXCEPTIONS` |
| `tonal.mjs` | 1137, 1138 | 1161, 1162 | `callCount !== 5`, the `1 + ((controls.dampAmp` absence |
| `tonal.mjs` | 1205 | 1251 | `KNOWN_BASELINE_DUP` |
| `tonal.mjs` | 1276 to 1281 | 1293 (uptick counter), 1302 (dup counter), 1330 to 1332 (the three (C6 ii) asserts) | the uptick and duplicate-hex gate |
| `tonal.mjs` | 1297 | 1353 | `ADIA_CARVEOUT` |
| `tonal.mjs` | 1302, 1314 | 1358, 1370 | `above100Violators`, its deliberate `NOT anchor` line |
| `tonal.mjs` | 1415 to 1532 | 1490 (comment) and 1516 to 1607 (literal); `TICKET_739_DIP` 1479, `DIP_BASELINE` 1488, `PERCEPTUAL_DIP_BASELINE` 1489 | `EVEN_DIP_BASELINE` |
| `tonal.mjs` | 1540, 1545 | 1615, 1620 | `findDips`, its `anchor: pal.anchor` |
| `tonal.mjs` | 1563, 1564 | 1638, 1639 | `dipDocs`, `BASELINE_BY_MODE` |
| `tonal.mjs` | 1594 | 1677 to 1678 | the mode tripwire |
| `tonal.mjs` | 1643 to 1667 | 1724 to 1755 (`evenFloor = FULL ? EVEN_DIP_BASELINE.size : ...` at 1753, the peak control's `peakFloor` at 1720) | the even 1.6x-floor negative control |
| `tonal.js` | 322 | 322 (unchanged text) | `evenChroma`'s floor line, the C3 patch target |
| `tonal.js` | 395 | 397 | `const shoulder` (the `dampAmp` term) |
| `report-preset-fidelity.mjs` | 99, 292 | 99, 292 (file byte-identical) | the two `paletteStops` calls with `anchor: pal.anchor` |
| `semantic.mjs` | 252 | 252 (file byte-identical) | `FLOORS` |
| `adapter.md` | 45, 46, 52, 54 | 91 (Merge row, no branch protection), 92 (CI on a PR, four jobs), 100 (the 2026-09-19 amendment) | §2 rows |
| `baseline-agrees-check.sh` | (10 printed lines) | 24 to 33, the literal gate list; 14 printed lines | the `time <gate>` rows |

Criteria whose Expected, control or feasibility moved: C1 (N = 50), C7 (`KNOWN_BASELINE_DUP` 23), C8 (the `role-contrast` grep), C9 (the `(hs)` grep), C11 (`NOTCH_ALLOW` `17 (expected 17)`), C12 (14 lines; the literal gate list), and C3's description of the peak and perceptual dip lists (one name each, left alone). Nothing blocks U1: the two even-mode populations this plan retires are byte-for-byte what revision 7 measured (64 + 1 spikes, 90 dips with K = 32), the gate-path reading is still 0 with a live 155 control, the four even cells are identical on the rendered path, and every moved count is in a list this plan does not own (#739's two notch names and two dup keys, both outside even mode).

## Feasibility probe (scratch, not source; the builder measures for real)

A patched copy of `tonal.js` with ONE change, inside `chromaEnvelope`'s even branch only: `uG` multiplied by a smoothstep plateau `t² (3 − 2t)`, `t = min(1, |sd| / r)`. Everything else shipped. The unpatched data-URL copy reproduces `projectView`'s 25-stop even ramps byte for byte on the first 40 presets (440 of 440 palettes), so the rows below are the shipped engine plus that one term.

| even envelope | spikes (corpus + default kit) | rendered dips, 25-stop, by stop | gate-path dips | `--envelope` even cells | even 25-stop cells moved / 94,500 | max dC at any moved cell |
|---|---|---|---|---|---|---|
| shipped | 64 + 1 | 90: 450 57 · 500 32 · 550 1 | 0 | as measured above | 0 | 0 |
| plateau r = 0.2 (90 stop units, just under two 50-stop steps) | 0 + 0 | 56: 400 3 · 450 20 · 500 32 · 550 1 | 0 | unchanged to one decimal | 4,498 | 19.14 (brands "Modal jazz" danger, 550) |
| plateau r = 0.3 | 0 + 0 | 68 (500 stays 32) | 0 | unchanged | 8,953 | 30.44 |
| plateau r = 0.4 | 0 + 0 | 64 (500 stays 32) | 0 | unchanged | 13,070 | 35.96 |

What the probe says and does not say. A neighbourhood term of radius about 0.2 closes every spike including the default kit's, leaves the four C6 cells where they are (the term is zero from stop 300 outward, so `--envelope`'s stops never see it), and moves about 5% of even stop cells, all within two steps of the anchor. It does not close the stop-450 dips that sit on the floor (20 remain at r = 0.2, 15 of them floor-bound), because those are mechanism 2, and it never touches the 32 dips AT stop 500, which are the anchor sitting under a higher group basis (the notch class, Q-C, `NOTCH_ALLOW`), a different mechanism this plan does not own. The exact form (radius, blend, whether the plateau reads `dampCurve`) is U1's to derive from the constraints below, not this table's to fix.

## Constraints every unit holds (the color-math invariants, restated for this change)

- `chromaEnvelope` stays one exported function, called from the even path's two sites and the OKHSL path (C7's greps). The even-only branch is the only place a term may be added; perceptual and peak are byte-identical before and after (C6).
- `env(anchorStop) = 1` exactly for every controls combination and every lift (`env(500) = 1` sweep in the report). A shoulder term is a factor on the off-anchor part, never an offset.
- In-gamut by construction: `chroma = min(maxc, max(damped, floorC))` keeps its shape; the floor never exceeds `intended` (a muted palette stays muted, a chroma-0 neutral stays untinted, `chroma-floor` (b) and (c)).
- Damping touches chroma only; `toneAt` sets CIELAB L* on the even path directly, so no even change can move measured L* (the reason #681 revision 22 put the even retune first). The uptick and duplicate-hex gates stay at 0 anyway (C7).
- Both sliders stay live in even mode (`damp`, `dampCurve`, `chromaFloor`): F4, no dead control, no new control. Any new constant is derived from a slider or named and documented.
- Generated palettes (`dampAmp` 0) never emit more chroma than the anchor in even mode (the `Math.min(chroma, anchorChroma)` cap in `paletteStops`, C6's "0 above 100%"). A shoulder that lifts 450/550 toward 1 must respect it on the anchored path too, where no cap exists today (the probe counts 14,979 above-anchor stops there before and after at r = 0.2, all from `anchorChromaBasis` blending toward a group target above the anchor's own chroma; not this plan's, reported under C5).

## Criteria (verifier-checkable; each with the command, the exact Expected, a negative control that reds, and today's value at 326592d2)

Every command runs in the unit worktree on the branch head, with no `node_modules` unless the row says build. "Today" is 326592d2, the integration head, and is a planner reading, not a verdict; rows re-derived in revision 7 say "Today at 505416d7", the freshest #681 tree. `<base>` is the `origin/main` commit that carries #713's landing (the `depends:` row); every "Today" figure is re-read there before U1 starts. Heavy commands are captured once to a file under `$F` (a scratch directory the verifier names) and grepped from it, never run twice. Flags are written literally on the command line (an unquoted `$a` holding `--damp-amp 55` passes one argument under zsh, and `report-preset-fidelity.mjs` ignores an unknown flag silently). Every gate-script timing row this plan adds to `.sdlc/baseline.md` follows #713's quiet-host rule: a run counts only if the 1-minute load is under the core count, read by `uptime` immediately before and again after; a run that starts quiet and ends loud is recorded and not counted.

- C1 `npm test` green. Command: `npm test 2>&1 | tail -1; git status --short | wc -l`. Expected: `✓ all N test files passed` with N equal to `TESTS.length` in `test/run.mjs` at the pre-land head after merging `origin/main` (this plan registers no `TESTS` entry; the two gate scripts it adds, C4 and C6, are matrix legs per #713, so N is inherited: 49 at 505416d7, 50 on `plan/gate-split` @ e035f841, re-read at `<base>` and again at pre-land, 50 at 282fca8d (revision 10); a landing of #713 or of any later plan that changes `TESTS` is a re-read trigger, not a FAIL). After #713 `npm test` runs `tonal.mjs` and `anchor.mjs` SAMPLED; the full legs are C2's and C3's gate scripts. Then `0`. Negative control: in a throwaway clone, `sed -i '' 's/"scrim/"scrimX/' docs/reference/data/role-table.json && npm test 2>&1 | grep -c FAIL` prints a non-zero count and the runner exits 1. Today: not run by the planner (no criterion needs a value from it); U4 review pass 1 records `npm test` exit 1 with exactly `engine/tonal.mjs` and `engine/anchor.mjs` red at this head, which C2 and C3 close.
- C2 Lone spikes are a true 0 with no allow-list, corpus and default kit in one count (re-derived at 505416d7, revision 7). Command: `npm run -s gate:corpus-anchor > "$F/a.log" 2>&1; echo "exit $?"; grep -cE 'lone-spike' "$F/a.log"; grep -E 'anchor-ramp lone-spike' "$F/a.log"; grep -cE 'LONE_SPIKE_ALLOW|DEFAULT_KIT_SPIKE_FINDING' test/engine/anchor.mjs; grep -cE 'loneSpikeSorted\.length === 0' test/engine/anchor.mjs; grep -cE 'dropCheck\("lone-spike"|swapCheck\("lone-spike"' test/engine/anchor.mjs` (the full leg, run once and grepped from the file: `anchor.mjs` took 266.8 s at load 23 to 42 in the revision-7 review; before #713 lands the first command reads `node test/engine/anchor.mjs`, full by default). Expected: `exit 0`, `1` (one line in the whole full run matches `lone-spike`; at 505416d7 two do, lines 991 and 998; at `<base>` 282fca8d the same two at 1061 and 1068), that line being `pass  anchor-ramp lone-spike (even, near-achromatic neighbours <= 0.05, OKLCH C > both by > 0.03): 0 (expected 0, corpus A anchored + default kit 16, no allow-list)` where A is the `anchored.length` the `anchor-ramp:` exact line of the same full run prints as its denominator (`3370 of 3380` at 505416d7: 3,380 anchored, 3,370 in-window; A is 3,380, the sweep visits every anchored palette), then `0`, `1`, `0`. A sampled `npm test` run prints the same line over its sample and is not this criterion's evidence. What U1 does to the gate at `<base>` (pins re-read in revision 10; the 505416d7 numbers were 774 to 854, 989, 1018, 1028, 977 to 988): deletes `LONE_SPIKE_ALLOW` (comment from 834, literal 849 to 914) and `DEFAULT_KIT_SPIKE_FINDING` (line 1059) with their two comparisons (1061 through `allowListMatches`, 1068 to 1071 through `allowListOk`, #713's FULL/SAMPLED wrapper at 981, which stays for the other pinned lists), the two `for ... console.log("    r ...")` echoes (1066, 1073), and the two R10 controls that reference the list (`dropCheck("lone-spike", ...)` at 1093, `swapCheck("lone-spike", ...)` at 1103, which cannot exist without a list); keeps the separate default-kit loop (lines 1047 to 1058, so the big shared loop's other six allow-lists are not widened, #715's job) but unions its names into `loneSpikeNames` before the one sort; asserts `loneSpikeSorted.length === 0` printed as pass or FAIL plus one `FAIL("anchor-ramp", ...)` per surviving name. The gate's replacement negative control is the dip gate's own shape (`tonal.mjs:1724` to 1755 at `<base>`, 1643 to 1667 at 505416d7): a data-URL copy of `tonal.js` with U1's neighbourhood term neutralised (the patch target string is the term's own line, which U1 names in the handoff; a missing target is itself a FAIL) rerun through `loneSpikeStop` over the same corpus + kit must read more than 0 hits, else `FAIL("anchor-ramp", "negative control DID NOT bite ...")`. Negative control for the verifier: that same patched copy reads 65 (64 corpus + Data 7), `FAIL`; a scratch copy of the gate with the default-kit union removed leaves the printed line's `+ default kit 16` unbacked, which the reviewer checks by planting a spike in a scratch kit palette (the C2 print must move to 1). Today at 505416d7: two `lone-spike` lines, `pass ... lone-spike allow-list ... 64 (expected 64)` and `pass ... default-kit lone-spike ... 1 (expected 1)`; the greps read 16 (one line at 991 names both identifiers; revision 7 said 17 by adding the two single counts), 0, 2. Today at 282fca8d (revision 10): unchanged, the two lines print `1 (expected 1)` and `64 (expected 64)` from the full leg (exit 0, 109.9 s), the greps 16 (the both-identifiers line is 1061), 0, 2.
- C3 The even dip gate (iv) keeps #681's rendered-path reading, loses its 90-name baseline, and prints its off-anchor count unconditionally with the stop-500 count beside it (re-derived at 505416d7, revision 7; this is the rework of an existing `npm test` sweep, so it stays in `tonal.mjs` per #713). Command: `grep -c EVEN_DIP_BASELINE test/engine/tonal.mjs; npm run -s gate:corpus-tonal > "$F/t.log" 2>&1; echo "exit $?"; grep -E 'dip-gate even' "$F/t.log"` (the full leg, captured once; `tonal.mjs` took 257.5 s at load 42 in the review; before #713 lands the command is `node test/engine/tonal.mjs`, full by default; the sampled `npm test` run prints the same line over its sample and is not this evidence). Expected: `0`, `exit 0`, then exactly one line that the gate prints on every run, pass or fail, BEFORE any `FAIL` summary: `  dip-gate even rendered (anchor passed): 0 dips at stops other than 500 (19 + 25 stops, 3764 palettes + default kit 16, no baseline); 500: K (notch class, Q-C, not gated here)`, where K is the measured count of at-least-3-C dips exactly at stop 500, printed and never asserted (32 at 505416d7, the owner's Q3 ruling keeps them with #681's notch ruling), and the handoff names the K palettes. The shape is fixed here because `FAIL()` at `test/engine/tonal.mjs:35` (32 at 505416d7) keeps only the first message per gate name, so a red even reading under the shared `chroma-envelope` name can be masked by another sub-check; the FAIL for a non-zero off-anchor count is `FAIL("dip-gate-even", ...)` under its own name with one line per dip. What U2 does to the gate at `<base>` (pins re-read in revision 10; at 505416d7 the list was 1415 to 1532, `BASELINE_BY_MODE` 1564, `findDips` 1540, `dipDocs` 1563): deletes `EVEN_DIP_BASELINE` (comment from 1490, literal 1516 to 1607) and the `even` entry of `BASELINE_BY_MODE` (line 1639); the peak entry `DIP_BASELINE` and the perceptual entry `PERCEPTUAL_DIP_BASELINE` hold one name each since #739 (`TICKET_739_DIP` at 1479, the #ACADAE travel palette at stop 500, lines 1488 to 1489) and stay as #739 left them, as does the peak control's `peakFloor` at 1720; the even control's `evenFloor = FULL ? EVEN_DIP_BASELINE.size : ...` at 1753 goes with the list; `findDips` (line 1615) keeps `anchor: pal.anchor`, both stop sets, `dipDocs` (line 1638, the kit included) and the `dampAmp`-0 filter unchanged; the even branch of the mode loop splits the found names by stop (500 versus the rest) instead of by baseline membership. Negative controls, both inside the gate and both calling the real `findDips` with a patched engine as the existing control at lines 1724 to 1755 does, both sweeping `dipDocs` (the kit included; the `<base>` control sweeps `docs` only, U2 widens it and says so): (revision 14 replaces the pass-1 pair, whose first member, the shipped floor scaled 1.6x, is vacuous by construction: `min(maxc, floorRef)` is non-increasing outward at any scale, so it reads 0 off-anchor at 1.6x and 3x on both paths) (1) the pre-#701 gamut-relative floor, the `<base>` form `Math.min(((chromaFloor ?? 0) / 100) * maxc, intended)` at every stop (`floorRef` dropped), which reads 24 off-anchor rendered dips at U1's head (the revision-13 figure); (2) the same form scaled 1.6x, `Math.min((((chromaFloor ?? 0) * 1.6) / 100) * maxc, intended)`, which reads 381. The patch target for both is U2's floor line, quoted in the handoff (`const floorC = Math.min(((chromaFloor ?? 0) / 100) * Math.min(maxc, floorRef), intended);` at pass 1, unchanged by revision 14, which moves only the `floorRef` definitions); a missing target is itself a FAIL. The bar for both is `> 0`, the gate asserts only `> 0`, and the handoff records both measured values. Today at 505416d7: `grep -c` prints 4; no line matches `dip-gate even`; the gate passes by matching 90 names (57 at 450, 32 at 500, 1 at 550). Today at 282fca8d (revision 10): `grep -c` prints 5 (#739's comment at 1483 adds a mention); no `dip-gate even` line; the full leg passes (exit 0, 119.5 s) on the same 90 names, re-measured on the rendered path by the scratch sweep as 57 / 32 / 1, so K = 32.
- C4 The gate-path reading (the palette without its anchor, what a user's own palette renders) is also 0 dips, as a CI gate script, not an `npm test` sweep (re-derived at 505416d7, revision 7: #681 moved the (iv) gate to the rendered path and nothing reads the gate path any more; #713 rules where a new sweep goes). Command: `npm run -s gate:even-dips > "$F/ed.log" 2>&1; echo "exit $?"; tail -2 "$F/ed.log"; grep -c '"gate:even-dips"' package.json; node -e 'const s=require("./package.json").scripts["gate:sweeps"]; console.log(s.split("&&").some(x=>x.trim()==="npm run gate:even-dips")?1:0)'; awk '/^  sweeps:/{f=1;next} f&&/^  [a-z-]+:$/{f=0} f' .github/workflows/ci.yml | grep -v '^ *#' | grep -c 'gate:even-dips'; grep -c 'gate:even-dips' test/run.mjs; grep -c 'gate:even-dips' .sdlc/adapter.md`. Expected: `exit 0`, `  dip-gate even gate-path (no anchor): 0 dips (19 + 25 stops, 3764 palettes + default kit 16, no baseline)` then `PASS`; `1`; `1` (the script joins `gate:sweeps`, the local pre-land command); `1` (one `- gate:even-dips` entry in the `sweeps` matrix, read by an awk that opens at `  sweeps:` and closes at the next job key, since a plain range would close on its own first line; the run line is `npm run ${{ matrix.gate }}`, so a grep for `npm run gate:even-dips` would read 0 by design; comments stripped so a commented mention cannot count); `0` (not a `TESTS` entry, C1); `2` or more (the §1 gates row AND the §2 "CI on a PR" row, which at `adapter.md:46` names `build-test` and `panda-smoke` only and which #713's U6b rewrites to name every PR job; U2 adds `even-dips` to that list in the same commit as the script, so a red leg is a named landing blocker, not a row in a gates table nobody reads at landing). What blocks landing: `main` has no branch protection (`adapter.md:45`, 52), so the block is procedural and the pre-land line below carries it. The script is `test/engine/even-dips-gate.mjs`, run by `package.json` as `node test/engine/even-dips-gate.mjs --full`; it imports the same `findDips` predicate shape (a copy is acceptable only if `tonal.mjs`'s `findDips` cannot be exported without running that file; U2 says which), sweeps `docs` + the kit with `anchor` omitted, both stop sets, `dampAmp`-0 filter, and carries its own negative control (the pre-#701 floor at 1.6x in a data-URL copy, `floorRef` dropped, must read more than 0 or the script fails with `negative control DID NOT bite`; revision 14: the shipped floor scaled alone reads 0 on this path too, so the control removes what U2 added). `.sdlc/adapter.md` §1 gains an `even-dips` row, §2's CI-on-a-PR list gains the leg, and `.sdlc/baseline.md` a `npm run gate:even-dips` timing row (three runs under #713's quiet-host rule, `uptime` before and after each, the criteria preamble) in the same unit. Negative control for the verifier: `node test/engine/even-dips-gate.mjs --full --floor-scale 1.6` (the script exposes its control's scale as a flag so the verifier can run it by hand) prints a non-zero count and `FAIL`. Today at 505416d7: 0 by the planner's scratch equivalent (`cf-gatepath.mjs`, 16.7 s at load 20.33); its 1.6x control 155 (400: 118, 450: 32, 550: 5); no script, no job. Today at 282fca8d (revision 10): 0 over 3,764 + 16 palettes, both stop sets; the 1.6x control 155 (400: 118, 450: 32, 550: 5), identical; no script, no job. At U1's head (revision 14, measured by U2 pass 1 and the re-diagnosis): the pre-#701 floor at 1.6x reads 120 on this path, since U1's shoulder is in; `--floor-scale 1.6` prints that count and `FAIL`.
- C5 The even envelope cells hold on the gate path, perceptual and peak do not move, and the rendered path is reported (re-derived at 505416d7, revision 7: since fa0264fa the report reads the rendered path only, where even fails three p90 bars and prints `above 100% of stop 500: 670 FAIL`, and no instrument prints the ruled gate-path cells; the owner's Q2 ruling says the rendered path is reported, not barred, so the ruled cells need their reading back). Command: `node scripts/report-preset-fidelity.mjs --envelope --gate-path > /tmp/env-gp.txt; awk '/READING \(a\)/,/READING \(b\)/' /tmp/env-gp.txt | sed -n '/^even:/,/carve-out/p'; node scripts/report-preset-fidelity.mjs --envelope > /tmp/env.txt; awk '/READING \(a\)/,/READING \(b\)/' /tmp/env.txt | sed -n '/^perceptual:/,/^even:/p' | md5; awk '/READING \(a\)/,/READING \(b\)/' /tmp/env.txt | sed -n '/^even:/,/carve-out/p'`. Expected: `--gate-path` (U1 adds it: the one `paletteStops` call at `report-preset-fidelity.mjs:99` and the C6 (v) call at line 292 omit `anchor` under the flag, nothing else changes, and the header line names the flag) prints READING (a) even with the four `stop` lines all `OK` on median and p90 (bars 25/35 at 100 and 900, 75/90 at 300 and 700) and `above 100% of stop 500: 0 OK`, the same clause `test/engine/tonal.mjs`'s `above100Violators` (line 1358 at `<base>`, without `anchor` by design, line 1370; 1302 and 1314 at 505416d7) gates inside C1; the md5 of the perceptual and peak blocks of the default (rendered) run equals the md5 of the same extraction at `<base>` (the verifier runs both; those blocks are #725's and a differing md5 is a FAIL of this criterion because it means perceptual or peak moved); the rendered even block is quoted in the pre-land record under `REPORTED, NOT BARRED` with its `above 100%` count and the C6 (v) even violator count (670 and 1,205 / 3,764 at 505416d7; a rise in either is reported, not a FAIL, per Q2). The revision-5 `--rendered` flag is dropped: the default run is that table. Negative control: `--gate-path --damp-amp 55` prints `above 100% of stop 500: N FAIL` with N non-zero in the even block (on the rendered default run this control does not discriminate, 670 versus 2,897, which is why the flag exists). Today at 505416d7: rendered even 15.6 / 37.0 (p90 FAIL), 48.4 / 113.7 (p90 FAIL), 42.5 / 80.2, 22.9 / 52.0 (p90 FAIL), above 100% 670; gate-path cells not printable by any shipped instrument (10.9 / 16.2, 39.1 / 52.2, 39.0 / 44.6, 16.3 / 16.5 at 326592d2, and the `tonal.mjs` gate that holds the `0 above 100%` clause is green at 505416d7 per #681's U7 verdict, which is the only evidence the cells still hold there until U1 prints them). Today at 282fca8d (revision 10): the rendered even block is identical to 505416d7 (15.6 / 37.0, 48.4 / 113.7, 42.5 / 80.2, 22.9 / 52.0, above 100% 670, C6 (v) even 1,205 / 3,764 max 17.18x, exit 1); the perceptual block is identical and the peak block differs by one cell (900 median 29.4, was 29.3: #739's achromatic hue branch moved the two achromatic-anchor palettes), so the md5 of the perceptual-and-peak extraction at `<base>` is `6e558839ee9e43217e1e2f7afc898b7b` and that, not a 505416d7 value, is what the verifier's own `<base>` run must reproduce; the `tonal.mjs` `chroma-envelope` gate is green at `<base>` in the planner's own full run.
- C6 Perceptual and peak are byte-identical to `<base>`. Command: `npm run -s gate:mode-isolation > "$F/mi.log" 2>&1; echo "exit $?"; tail -1 "$F/mi.log"; grep -c '"gate:mode-isolation"' package.json; node -e 'const s=require("./package.json").scripts["gate:sweeps"]; console.log(s.split("&&").some(x=>x.trim()==="npm run gate:mode-isolation")?1:0)'; awk '/^  sweeps:/{f=1;next} f&&/^  [a-z-]+:$/{f=0} f' .github/workflows/ci.yml | grep -v '^ *#' | grep -c 'gate:mode-isolation'; grep -c 'mode-isolation' test/run.mjs; grep -c 'gate:mode-isolation' .sdlc/adapter.md; head -3 test/engine/fixtures/mode-isolation.json`. Expected: `exit 0`, `pass  mode-isolation: perceptual <hash16> peak <hash16> match fixture (captured at <base sha>, 3780 corpus + 16 default kit, 25-stop, projectView)`, the hashes equal to those in `test/engine/fixtures/mode-isolation.json`; then `1`, `1`, `1`, `0`, `2` or more (§1 row and the §2 CI-on-a-PR list, as C4), and a fixture header that names the capture commit AND the re-capture rule: `owner: the plan that moves perceptual or peak re-captures this fixture in its own change and names the new sha here; #725 is that plan today` (revision 8: a frozen fixture would otherwise collide with #725, which exists to move those two modes; the rule makes #725's re-capture an expected, named change rather than a red it has to argue past, and a hash mismatch with no such re-capture line in the same change is what the gate reds on). Revision 7: this is a new full-corpus sweep, two `projectView` passes over 3,796 palettes, so per #713 it is `test/engine/mode-isolation-gate.mjs` run as `npm run gate:mode-isolation`, a `sweeps` matrix leg and a `gate:sweeps` member, not a `TESTS` entry; `.sdlc/adapter.md` §1 and §2 and `.sdlc/baseline.md` (timing under the quiet-host rule) gain its rows in U1. The verifier recomputes both hashes at `<base>` itself with the gate's own function and they match the fixture. Negative control: a scratch copy of `tonal.js` with the OKHSL path's damping scaled by 1.01 prints two different hashes and `FAIL`; a scratch copy with the even branch's shoulder removed prints the SAME two hashes (proving the gate is blind to even, which is the point). Today: `f4ed35e36edd6b9b` / `6e6d81e23ab10661` by the planner's method at 326592d2; the fixture is captured at the landed head, not from these. Revision 10: those two are history, since #739 moved the perceptual and peak ramps of the two achromatic-anchor palettes (#ACADAE, #FFFFFF) between 326592d2 and `<base>`; U1 captures the fixture at `<base>` = 282fca8d with the gate's own function and the verifier recomputes it there.
- C7 Ramp shape gates stay at zero and the envelope stays one function. Command (revision 8: the `dup` grep of revision 7 matched no printed line on either side and was vacuous; the C2 and C3 captures are reused): `grep -E 'anchor-ramp monotone|distinct' "$F/a.log"; grep -E '^  (pass|FAIL)  (skew-lift-okhsl|chroma-envelope)' "$F/t.log"; sed -n '/const GRID_R2_EXCEPTIONS = new Set(\[/,/\]);/p' test/engine/tonal.mjs | grep -cE '^\s*"'; sed -n '/const KNOWN_BASELINE_DUP = new Set(\[/,/\]);/p' test/engine/tonal.mjs | grep -cE '^\s*"'; grep -c "export function chromaEnvelope(" src/engine/tonal.js; grep -c "chromaEnvelope(" src/engine/tonal.js`. Expected: `anchor-ramp monotone: 0`; the 25-stop distinct-hex allow-list line unchanged from `<base>` (its count and names); `pass  skew-lift-okhsl` and `pass  chroma-envelope` (the second is the gate that asserts 0 upticks and 0 duplicate-hex pairs beyond `KNOWN_BASELINE_DUP` in every mode, `tonal.mjs:1293` to 1332 at `<base>`, the uptick counter at 1293, the dup counter at 1302 and the three (C6 ii) asserts at 1330 to 1332, under the one name `gateReport` prints); `21` and `23` (revision 10: the `skew-lift-okhsl` (iii b) grid boundary list `GRID_R2_EXCEPTIONS` at `tonal.mjs:986` and the duplicate list `KNOWN_BASELINE_DUP` at 1251, both unchanged from `<base>`, where #739 had already added two Nike tertiary-muted peak keys to the second, its comment at 1249, so 21 and 21 at 505416d7 read 21 and 23 at `<base>`; a unit that must move either re-pins it with the reason in the handoff); then `1` and exactly the number `test/engine/tonal.mjs:1161` (1137 at 505416d7) hard-asserts (`callCount !== 5`; 5 at 326592d2 and at 505416d7: one definition plus the four call sites the assert text itself names, `paletteStopsAnchored`, `paletteStops`, `okhslStopsAnchored`, `okhslStops`). That assertion is a literal, not an inherited value, so a unit that adds a call site reds the gate and must re-pin the literal with a reason in the handoff; a unit that adds none leaves it. The revision-1 grep for `1 + ((controls.dampAmp` is dropped: the string is already absent and `test/engine/tonal.mjs:1162` asserts it. Naming: `src/engine/tonal.js:397` (395 at 505416d7) already declares `const shoulder` for the `dampAmp` term, so U1's neighbourhood term takes a different identifier (the handoff names it). Negative control: a scratch copy that adds a second `export function chromaEnvelope` reds the first grep; a scratch copy that computes the neighbourhood term from `stop` instead of `liftStop(stop, lift)` reds `skew-lift-okhsl` (iii c) (the R2 keying rule); the two list counts read `0` for a list name that does not exist (verified in revision 8: the same `sed` range on `NO_SUCH_LIST` prints 0) and one more than the pinned count (`22`, `24`) for a scratch copy with one entry added. Today at 505416d7: `anchor-ramp monotone: 0` pass, `distinct (25-stop) allow-list: 16 (expected 16)` pass, `skew-lift-okhsl` and `chroma-envelope` pass (22 gates pass in the review's full run), list counts 21 and 21, greps 1 and 5. Today at 282fca8d (revision 10): the same two `anchor-ramp` lines pass, `skew-lift-okhsl` and `chroma-envelope` pass (22 pass lines, 0 FAIL in the full leg), list counts 21 and 23, greps 1 and 5.
- C8 The `chroma-floor` gate's four properties hold, the saturated exception list is re-derived, and contrast holds with the re-pin direction read by a comparator over the FLOORS tables. Command: `grep -E '^  (pass|FAIL)  chroma-floor' "$F/t.log"; node test/engine/semantic.mjs 2>&1 | grep -E '^  (pass|FAIL)  role-contrast'; npm run gate:corpus-contrast 2>&1 | tail -1; B=$(mktemp); git show <base>:test/engine/semantic.mjs > "$B"; node -e 'const fs=require("fs");const P=f=>{const s=fs.readFileSync(f,"utf8"),o={};for(const[,m,b]of s.slice(s.indexOf("const FLOORS = {")).matchAll(/(perceptual|even|peak):\s*\[([\s\S]*?)\n\s*\]/g)){o[m]={};for(const[,n,l,d]of b.matchAll(/\["([^"]+)",\s*([\d.]+),\s*([\d.]+)\]/g))o[m][n]=[+l,+d]}return o};const[a,b]=process.argv.slice(1).map(P);let c=0,d=0;for(const m of ["perceptual","even","peak"]){const na=Object.keys(a[m]||{}).length,nb=Object.keys(b[m]||{}).length;console.log(`${m} entries: base ${na}, branch ${nb}`);if(na!==16||nb!==16){console.log(`FLOORS ${m}: entry count moved`);process.exit(1)}}for(const m in b)for(const n in b[m]){const x=a[m]&&a[m][n],y=b[m][n];if(!x||(x[0]===y[0]&&x[1]===y[1]))continue;c++;const dn=y[0]<x[0]||y[1]<x[1];if(dn)d++;console.log(`${m} ${n}: ${x[0]}/${x[1]} -> ${y[0]}/${y[1]} ${dn?"DOWN":"up"}`)}console.log(`FLOORS changed ${c}, down ${d}`);process.exit(d?1:0)' "$B" test/engine/semantic.mjs; echo "exit $?"`. The comparator parses `const FLOORS = { perceptual: [ ["Neutral", 6.8, 4.8], ... ], even: [...], peak: [...] }` (the entry shape at `test/engine/semantic.mjs:251` onward: an array literal per family, light then dark, 16 per mode), first prints `<mode> entries: base 16, branch 16` for each of the three modes and exits 1 with `FLOORS <mode>: entry count moved` when either file has other than 16 (revision 8: the comparator loops over the branch's entries, so a family DELETED from the branch table used to pass silently, which is exactly how a downward re-pin could be "left OUT" without a trace; the count line closes that), then pairs each family by mode between `<base>`'s file and the branch's, prints one line per changed entry with `up` or `DOWN`, then `FLOORS changed C, down D`, and exits 1 when D is non-zero. Expected: three `entries: base 16, branch 16` lines; `chroma-floor` pass with (a) the muted light stop lifted by more than 2 C, (b) no floored stop above the intended mid, (c) a chroma-0 neutral byte-identical with and without the floor, (d) a vibrant ramp byte-identical except at the stops `SAT_FLOOR_EXCEPT` names, that set re-measured by U2 and listed by stop in the handoff (11 named at 326592d2 and at `<base>`: 100, 125, 150, 175, 200, 250, 300, 875, 900, 925, 950, `tonal.mjs:362`; the gate self-reds on a stale list at line 371); `pass  role-contrast` (revision 10: the gate prints `role-contrast`, the `hpg-` prefix lives only in the source comment at `semantic.mjs:152`, so the revision-9 grep for `hpg-role-contrast` matched nothing on pass or fail; the `chroma-floor` line is read from C3's full-leg capture, since a bare `node test/engine/tonal.mjs` is the sampled leg after #713); `PASS: every measured curated preset's accent clears 4.5:1 against its own on-color`, 0 under, no new carve-out row; the comparator's changed lines are exactly the five downward re-pins owner ruling R44 (2026-09-25, `.sdlc/questions/chroma-floor-U1.md`, answer B) admits, every one `even` (a `perceptual` or `peak` line is a FAIL of C6 as well): Warning light, Warning dark, Data 3 light, Data 5 light and Data 8 light, each floor re-pinned to the table's precision at or just below the value measured at the unit head (U1 measured 9.8756, 5.2819, 6.4562, 5.7748, 5.7955 against floors 9.9, 5.3, 6.5, 5.8, 5.8); the comparator counts rows, and Warning's light and dark floors share one row, so it prints `changed 4, down 4`, and any other downward line is a FAIL; every move named (cell, old floor, new floor, measured before and after) in the handoff and in a comment on #662; AA (4.5:1) held for all 16 families on both schemes (the even accents ARE stops 550 and 450, the two stops a shoulder moves most, so the default kit's even ratios will move). Negative control, measured today: against a scratch copy of the 326592d2 file with even Neutral's light entry lowered 0.1 and even Primary's dark entry raised 0.1, the comparator prints `even Neutral: 7.2/4.6 -> 7.1/4.6 DOWN`, `even Primary: 7.4/4.7 -> 7.4/4.8 up`, `FLOORS changed 2, down 1`, `exit 1`, while `hpg-role-contrast` stays green on that copy (a green gate alone is consistent with a downward re-pin, which is why the comparator exists); run against the same file twice it prints `FLOORS changed 0, down 0`, `exit 0`; against a scratch copy with even `Neutral` deleted it prints `even entries: base 16, branch 15`, `FLOORS even: entry count moved`, `exit 1` (revision 8, verified on the 505416d7 file). A raised entry 0.1 above the measured ratio makes that family's `hpg-role-contrast` line FAIL; the `chroma-floor` (c) control is the gate's own (a floor that tints a neutral changes a hex). Today: `chroma-floor` pass at 326592d2; ratios not re-read by the planner; the comparator verified on the 326592d2 tables (16 families per mode parsed). Today at 282fca8d (revision 10): `pass  chroma-floor` in the full leg; `pass  role-contrast`; contrast `PASS: every measured curated preset's accent clears 4.5:1 against its own on-color`; the comparator on the `<base>` file against itself prints `perceptual entries: base 16, branch 16`, the same for even and peak, `FLOORS changed 0, down 0`, `exit 0`; even Neutral reads 7.2 / 4.6 and even Primary 7.4 / 4.7, so the negative control's figures still apply.
- C9 Q-D unchanged. Command: `grep -E 'anchor-f4 hueSpace' "$F/a.log"; node test/ui/headless-boot.mjs > "$F/hb.log" 2>&1; echo "exit $?"; tail -1 "$F/hb.log"; grep -c '(hs)' test/ui/headless-boot.mjs` (revision 9: read from C2's full-leg capture; after #713 a bare `node test/engine/anchor.mjs` is the SAMPLED leg and its "full corpus + default kit" bound lines would describe the sample. Revision 10: the shim's `ok()` at `headless-boot.mjs:17` records failures only, so a green `(hs)` group prints nothing and the revision-9 grep on the output was vacuous; the evidence is `exit 0`, a last line beginning `HEADLESS BOOT PASS`, and the source count `4` proving the group is still there, with any `(hs)` line in `$F/hb.log` a FAIL). Expected: the perceptual and peak bound lines pass at `max OKLab dE <= 0.01` with the same bound constants as `<base>`, the even line still reads above the JND (live), and the `(hs)` shim group passes (control absent or disabled for anchored perceptual and peak, present for even): `exit 0`, the PASS line, `4`, no `(hs)` line in the log. Negative control: a scratch copy that forces the perceptual solve into the wrong hue space reds the perceptual bound line (Q-D's own). Today: even live at max OKLab dE 0.0486 (want above 0.01), perceptual bound max dE 0.0048 with codes max 2, peak bound max dE 0.0048 with codes max 1, all three pass. Today at 282fca8d (revision 10): the three lines identical, printed as `full corpus + default kit`; the shim run exits 0 ending `HEADLESS BOOT PASS`, source count 4.
- C10 Regenerated assets clean and `docs/` moves by a list fixed here, not in a handoff. Command: `npm run gen:categories && npm run gen:adia-exports && npm run gen:mcp-assets && npm run bundle && npm run gen:figma-ui && git status --short | wc -l; git diff --stat "$(git merge-base HEAD origin/main)" -- docs/ figma/binder/figma-semantic-binder/code.js docs/reference/data/role-table.json | grep -vE '^ [0-9]+ files? changed'` (revision 8: diffing against `<base>` after the units have merged `origin/main` would list every other plan's `docs/` change landed since, a false FAIL; the merge-base is what this branch itself changed). Expected: `0`; the `docs/` paths in the diff are a subset of exactly these six, each with the reason given: `docs/reference/references/knowledge-02-tonal-scale.md` (§5 floor formula and the even shoulder, U2 and U3), `docs/reference/references/glossary.md` (row `chromaFloor`, U3), `docs/reference/references/decision-records.md` (the ADR-025 amendment line, U3), `docs/reference/CHANGELOG.md` (next entry, U3), `docs/reference/rubrics/quality-rubric.md` (only if it states the floor mechanism, U3), `docs/reference/data/adia-oklch-export.css` (regenerated by `gen:adia-exports`; it moves only if the Adia kit's even ramp moves, which U1 measures and states either way in its handoff); `code.js` and `role-table.json` absent from the diff. A seventh `docs/` path, or `code.js` or `role-table.json` present, is a FAIL. Negative control: touch `docs/reference/references/knowledge-01-*.md` in a scratch copy and the diff lists a path outside the six. Today: not applicable before the change.
- C11 All three allow-lists are gone and cannot come back quietly, by the counts the gates assert, not only by grep (re-derived at 505416d7, revision 7: `LONE_SPIKE_ALLOW`, `DEFAULT_KIT_SPIKE_FINDING` and the 90-name `EVEN_DIP_BASELINE`). Command: `grep -c EVEN_DIP_BASELINE test/engine/tonal.mjs; grep -c LONE_SPIKE_ALLOW test/engine/anchor.mjs; grep -c DEFAULT_KIT_SPIKE_FINDING test/engine/anchor.mjs; grep -c 'anchored + default kit 16, no allow-list' test/engine/anchor.mjs; grep -cE 'loneSpikeSorted\.length === 0' test/engine/anchor.mjs; grep -cE 'pending U4|pending owner' test/engine/tonal.mjs; grep -c 'stops other than 500' test/engine/tonal.mjs` (one file per grep, so each prints a bare count). Expected: `0`, `0`, `0`, `1`, `1`, `1` (the one `pending owner` mention is inside `EVEN_DIP_BASELINE`'s retired-53 history comment, `tonal.mjs:1491` at `<base>`, 1416 at 505416d7, which U2 deletes with the list, so the count may read 0 and either is a pass; a rise is a FAIL), `1` or more (the C3 print). What actually holds the line is the pair of assertions, and this row names them: U1 REPLACES the `allowListOk(loneSpikeSorted, LONE_SPIKE_ALLOW)` comparison (line 1068 at `<base>`; at 505416d7 it was `allowListMatches` at 998, #713 wrapped it) and the `allowListMatches(dkSpikeSorted, [...DEFAULT_KIT_SPIKE_FINDING])` comparison (line 1061 at `<base>`, 991 at 505416d7) with the 326592d2 form, `loneSpikeSorted.length === 0` printed as pass or FAIL plus one `FAIL("anchor-ramp", ...)` per surviving name and no suppression in the predicate (the fifth grep is what proves the replacement happened); and the dip gate's even branch reds on any off-anchor dip with no membership test at all (there is no list to be a member of), so a re-add under any identifier, in either file, reds the gate itself. A suppression moved into a helper file or into the corpus builder (filtering palettes before the sweep) would pass every grep; the reviewer checks that the corpus each gate sweeps still counts what it did at `<base>`: the C3 and C4 lines print `3764 palettes + default kit 16` (the `dampAmp`-0 dip sweeps, Adia's 16 skipped by name), the C2 line prints `corpus 3380 anchored` (the lone-spike sweep visits anchored palettes only) and the `anchor-ramp:` line of the same run prints `3370 of 3380` (its exact-pass denominator is the in-window subset); and that the `NOTCH_ALLOW` line still reads `17 (expected 17)` (revision 10: the gate's own printed count at `<base>`; 15 at 505416d7, and #739 added the #ACADAE travel palette under `[peak]` and `[perceptual]` at `anchor.mjs:681` to 682, so the 15 `[even]` entries are the same population; revision 7's "79" was a miscount that would have started a false re-diagnosis; the notch population is #681's and may move under a later #681 fix; a change there is reported, not a FAIL, but a change that coincides with a K drop in C3 is re-diagnosed before U2 is verified, because it would mean the stop-500 class was absorbed into the notch list rather than fixed). Negative control: re-add one name to either gate in a scratch copy: the corresponding grep prints 1 and the gate itself reds on a non-zero count. Today at 505416d7, system grep: `EVEN_DIP_BASELINE` 4, `LONE_SPIKE_ALLOW` 11, `DEFAULT_KIT_SPIKE_FINDING` 6, the exact `no allow-list` string 0, `loneSpikeSorted.length === 0` 0, `pending U4|pending owner` 1, `stops other than 500` 0; control on 730ff941: `anchor.mjs` absent, every count 0. Today at 282fca8d (revision 10): 5, 11, 6, 0, 0, 1, 0; the corpus lines print `3370 of 3380` and the dip sweep visits 3,764 + 16.
- C12 Landing rule after #691 (records-refresh). Command, at pre-land, on the plan branch head rebased onto `origin/main`: `sh .sdlc/checks/baseline-agrees-check.sh; echo "exit $?"`. Expected, keyed to the script's own output at the rebased head rather than to line counts fixed here (revision 8: #713's U6b rewrites this script to add a `time <gate>` row per gate script and re-points the baseline `ref`, and its `TESTS` reads 50, so a count of rows or a test-file figure written here would be stale at `<base>`): every `tests:` and `time` line reads `ok` (the `tests:` figure equals `TESTS.length` at the pre-land head, 50 after #713; if it does not, the pre-land commit repairs `.sdlc/baseline.md`'s figure and says so in `.sdlc/verdicts/chroma-floor-prepr.md`; the `time` rows the script checks are whatever list it carries at `<base>`, and the two gate rows this plan adds must be in that list if U6b's script reads every gate, which the pre-land verifier confirms by reading the script); the `ui.html:` line is the one line allowed to read `STALE` (an engine change grows the bundle); the tree-moved `head:` line is a `note` that counts toward nothing, and the in-history `head:` line reads `ok` because a rebased plan head has the baseline's ref in `origin/main`'s history. So the pass value is `stale total: 1` with `ui.html` the only STALE line, `exit 1` (the script exits 1 for any stale count, never the count itself; revision 5's `exit 3` was never what the script does), or `stale total: 0`, `exit 0` (a baseline rerun happened, or the bundle size did not move). Any other STALE line, in particular `tests:`, a `time` row, or the in-history `head:` line, is a FAIL. The pre-land record quotes every line the script prints (14 at `<base>`: 13 plus `stale total`; 10 at 505416d7). Revision 10: the script's gate list is a LITERAL array (`baseline-agrees-check.sh:24` to 33, one `[cmd, gate, label]` row per gate), not a read of every `gate:*` script, so `gate:even-dips` and `gate:mode-isolation` are checked only if U2 and U1 append `["npm run gate:even-dips", "even-dips", "gate:even-dips"]` and `["npm run gate:mode-isolation", "mode-isolation", "gate:mode-isolation"]` to that array in the same commit as their `.sdlc/baseline.md` timing row and `.sdlc/adapter.md` §1 row (the units say so); the pre-land run then prints 16 lines, two of them `ok    time gate:even-dips` and `ok    time gate:mode-isolation`, and a missing row is a FAIL of C12. Negative control: a scratch `test/run.mjs` with one extra entry prints `STALE tests: baseline N, test/run.mjs TESTS N+1`, `stale total` one higher, `exit 1` (verified in revision 8 on the plan branch: `stale total: 1`, `exit 1`, against `stale total: 0`, `exit 0` unpatched). Today at 505416d7: the review's run printed all `ok` plus one `note`, `stale total: 0`, `exit 0`. Today at 282fca8d (revision 10): 13 `ok` (tests 50, ui.html 4125.3 KB, nine `time` rows including the four `gate:*` rows, the in-history `head:` line) plus one `note` (the tree moved outside `.sdlc/` since a62ec020), `stale total: 0`, `exit 0`.
- C13 The floor is continuous across the tone-window edge and the near-extreme drain is bounded (revision 14, from U2 review pass 1 finding 1). Command: a scratch probe script named in the handoff (the re-diagnosis's `rediag.mjs` shape: hue 250, chroma 50, `DEFAULT_CONTROLS` with `toneMode: "even"`, 19 stops, CAM16 C read from the rendered hex, anchors `#E8EEFA` (L* 93.97, in window), `#ECF1FC` (95.06, clamped), `#1C2030` (12.57), `#161A28` (9.50), and hue-250 anchors generated at 90% of the local ceiling and at C 6 for L* 9.55, 9.99, 10.05, 94.95, 95.13, 95.2; plus the corpus drain count: even 25-stop rendered cells of every anchored curated palette and the kit, `dampAmp` 0, compared with the pre-#701 floor in a data-URL copy). Expected: (i) for each generated pair straddling an edge (9.99 vs 10.05, 94.95 vs 95.13) the largest loss vs the pre-#701 floor over the five far-side stops differs by at most 2 C (the re-diagnosis reads 0.7 and 1.9); (ii) `#E8EEFA` and `#ECF1FC` read within 2 C of each other at stops 700 to 800 (14.1 / 13.9 / 13.8 vs 13.8 / 13.4 / 13.2), `#1C2030` and `#161A28` within 3 C at 300 to 450; (iii) at most 20 corpus cells lose more than 8 C and more than half their chroma vs the pre-#701 floor (16 in the re-diagnosis, 6 anchors, named in the handoff), 0 of them from anchors with L* between 88 and 95.05, and at most 5 anchors below L* 15 (4). Negative control: the same script on pass 1's engine (`floorRef = clamped ? Infinity : maxc500`) reads a 13.5 and 14.4 C jump across the edges and 215 cells in 96 anchors, `FAIL`. Today at U2 pass 1 (fe65e640): the control's figures; on the revision-14 rule in the re-diagnosis's scratch clone: 0.7 / 1.9, 16 cells, 6 anchors, PASS.

## Units

Serial, each in its own `.worktrees/<unit>` off `plan/chroma-floor`, because all three touch `src/engine/tonal.js` or the two gate files.

Start gate (revision 8, decided by command, no unit before it prints green): `git fetch -q origin; git log -1 --format=%h -S'"gate:sweeps"' origin/main -- package.json; git show origin/main:package.json | grep -c '"gate:sweeps"'; git show origin/main:.github/workflows/ci.yml | grep -c '^  sweeps:'; git show origin/main:test/run.mjs | grep -oE '"[a-z]+/[a-z0-9-]+\.mjs"' | wc -l`. Green: a sha (the commit that introduced the script; revision 9 replaced the `(#713)` subject grep, which a record commit could satisfy), `1`, `1`, and the last equal to the figure #713's landing wrote (50 on `plan/gate-split` @ e035f841). Today (2026-09-22): nothing, `0`, `0`, `48`, so no unit starts (control: the same `-S` selector on `plan/gate-split` prints a11e8aff, its U1 commit). When green, the plan branch is reset onto that `origin/main` commit (it becomes `<base>`, recorded in the revision log), every "Today at 505416d7" figure is re-read there, and the units run. Green on 2026-09-24: `a62ec020`, `1`, `1`, `50` (revision 10); the Orchestrator merged `origin/main` @ 282fca8d into the plan branch (4381e860) instead of resetting, keeping the nine pushed revision commits reachable, and `git merge-base plan/chroma-floor origin/main` prints 282fca8d, so `<base>` = 282fca8d.

- [x] U0 (S, the Orchestrator, no builder; done in revision 10 at `<base>` = 282fca8d, the section 'Re-read at 282fca8d', nothing blocks U1) the start gate above, the reset, the re-read, and one revision that records `<base>`, the re-read figures, and every line pin re-read at `<base>`: #713 edits `test/engine/anchor.mjs` and `test/engine/tonal.mjs` (the FULL/SAMPLED split), so the pins the unit text and criteria carry at 505416d7 (`anchor.mjs` 608, 774 to 854, 977 to 998, 1018, 1028, 1188; `tonal.mjs` 32, 322 in `tonal.js`, 350, 359, 967, 1137, 1138, 1205, 1276 to 1281, 1297, 1302, 1314, 1415 to 1532, 1540, 1545, 1563, 1564, 1594, 1643 to 1667; `report-preset-fidelity.mjs` 99, 292; `semantic.mjs` 252) move; U0 re-pins each or replaces it with the identifier it names (`LONE_SPIKE_ALLOW`, `DEFAULT_KIT_SPIKE_FINDING`, `EVEN_DIP_BASELINE`, `findDips`, `dipDocs`, `above100Violators`, `GRID_R2_EXCEPTIONS`, `KNOWN_BASELINE_DUP`, `SAT_FLOOR_EXCEPT`, `FLOORS`), and a pin that no longer resolves at `<base>` is a U0 finding, not a builder's guess (revision 9).

- [x] U1 (M) even envelope shoulder at the anchor stop; both lone-spike lists retired, one count over corpus and kit; the mode-isolation gate script; the report's gate-path flag · grade l4 · reviewer-l2 · verifier-l2
  `src/engine/tonal.js`: a neighbourhood term inside `chromaEnvelope`'s even branch only (`isEven`), keyed on the same `sd` from `liftStop`, zero at the anchor and vanishing by stop 300/700 so the C5 cells do not move, its radius derived from or documented against the sliders (no dead control, no new control); `env(500) = 1` unchanged; perceptual and peak untouched (C6). `test/engine/anchor.mjs` at `<base>` (revision 10 pins; 774 to 854, 989, 1018, 1028 at 505416d7): `LONE_SPIKE_ALLOW` (834 to 914) and `DEFAULT_KIT_SPIKE_FINDING` (line 1059) deleted with their comparisons (1061, 1068 to 1071), echoes (1066, 1073) and the two R10 controls that name them (lines 1093, 1103); the kit loop's names unioned into `loneSpikeNames`; one `lone-spike` line reading `expected 0, corpus A anchored + default kit 16, no allow-list`; the gate's own data-URL negative control against the shoulder term (C2). `test/engine/mode-isolation-gate.mjs` + `test/engine/fixtures/mode-isolation.json` captured at `<base>`, header naming the sha; `package.json` `gate:mode-isolation`, a member of `gate:sweeps`; a `- gate:mode-isolation` leg in `ci.yml`'s `sweeps` matrix; the `.sdlc/adapter.md` §1 gates row and §2 CI-on-a-PR entry, the `.sdlc/baseline.md` timing row under the quiet-host rule, and the `["npm run gate:mode-isolation", "mode-isolation", "gate:mode-isolation"]` row in `.sdlc/checks/baseline-agrees-check.sh`'s literal gate list (C6, C12). `scripts/report-preset-fidelity.mjs`: `--gate-path` (C5). Regenerate assets (C10). Handoff: the probe table re-measured on the real engine (spikes, rendered dips by stop, cells on both readings, moved cells, max dC), the default-kit even ratios before and after, which even FLOORS moved (C8 re-pin only upward), and the movement table of the blast-radius section. Criteria: C1, C2, C5, C6, C7, C8, C9, C10.
- [~] U2 (M) chroma floor monotone in both senses: gamut-relative near white and black, envelope-relative in the interior; even dip gate retired to 0 off-anchor on the rendered path, the gate-path reading as a CI gate script · grade l6 · reviewer-l3 · verifier-l3
  `src/engine/tonal.js`: `evenChroma`'s floor term (line 322 at 505416d7 and at `<base>`) redesigned so the floor cannot cross the damped value in the interior (the 450-stop dips that sit exactly on the floor, 49 at 326592d2, go to 0) while near the extremes it stays proportional to `maxc` (the shape the even 100/900 cells depend on, the reason #681 U3's `max(env, chromaFloor/100)` attempt reopened them: 100 p90 19.8% to 39.0%, 900 median 16.3% to 40.5%); both call sites (`chromaAt` in `paletteStopsAnchored`, the per-stop map and the stop-500 seed in `paletteStops`) share the one function as today; the four `chroma-floor` properties hold and `SAT_FLOOR_EXCEPT` is re-derived. Revision 14 (the re-diagnosis, `.sdlc/plans/chroma-floor-U2-rediagnosis.md`): the floor is `chromaFloor% * min(maxc, floorRef)`, never above `intended`, with ONE reference on both sites and no branch on a clamped anchor: `floorRef = max(maxc500, maxc(hue, tone(450)), maxc(hue, tone(550)))`, the gamut ceiling at the anchor's own tone or at its first display step on either side, whichever is larger (`anchorLerp` tones on the anchored site, `toneAt` on the non-anchored one; `maxc500` stays the reference at stop 500 itself). Pass 1's `clamped ? Infinity : maxc500` is retired: it was a level cap at the anchor stop's ceiling, which near white and black is a few C and drained the far half of the ramp, plus a threshold branch whose only job was #739's skip bound; the first-step reference is continuous in the anchor's L*, is the largest reference that cannot open a dip (a V cannot bottom at the first step, and beyond it the floor is non-increasing outward as in pass 1), and holds #739 at 3 of 30 skipped with no special case. The code comment and `foundations.md` §5 state the guarantee as conditional on the damped value being non-increasing outward (constant `intended`) and state the 0 as the gated measurement on both paths, not as a structural claim under `relChroma` or the anchored basis blend. `test/engine/tonal.mjs` at `<base>` (revision 10 pins; 1415 to 1532 at 505416d7): `EVEN_DIP_BASELINE` (1490 to 1607) and `BASELINE_BY_MODE.even` (line 1639) deleted, the peak and perceptual entries (one name each since #739) left alone; the (iv) gate's even branch prints the unconditional off-anchor / stop-500 line and reds under `dip-gate-even` (C3); both negative controls as C3 states; the peak entry left as #681 lands it. `test/engine/even-dips-gate.mjs`, `package.json` `gate:even-dips` as a member of `gate:sweeps`, a `- gate:even-dips` leg in `ci.yml`'s `sweeps` matrix, the `.sdlc/adapter.md` §1 gates row and §2 CI-on-a-PR entry, the `.sdlc/baseline.md` timing row under the quiet-host rule, and the `["npm run gate:even-dips", "even-dips", "gate:even-dips"]` row in `.sdlc/checks/baseline-agrees-check.sh`'s literal gate list (C4, C12). `docs/reference/references/knowledge-02-tonal-scale.md` §5: the floor formula. Handoff: the depth histogram before and after on both readings, the K stop-500 names, both `--envelope` tables (`--gate-path` and default), the default-kit even ratios per family before and after (the blast-radius section's field; pass 1 omitted it), C13's probe table and drain count, and the `.sdlc/adapter.md:36` `sweeps` row repaired to seven rows and `355 to 475 s` (`86+79+67+57+20+20+26 to 116+100+86+83+23+34+33`, "CI runs the seven as a matrix"). Stop rule: if the redesigned floor cannot hold all four even cells on the gate path AND reach 0 off-anchor rendered dips, U2 stops, no second workaround, and reports which target yields, by how much, with the two tables, for the owner (the shape `pif-u3-yield.md` used). Criteria: C1, C3, C4, C5, C6, C7, C8, C10, C13. Depends on U1 merged into the plan branch.
- [ ] U3 (S) records and the retirement sweep · grade l2 · reviewer-l1 · verifier-l1
  `.claude/skills/color-math/SKILL.md` invariant 1 and `references/foundations.md` §5 (the floor formula and the even shoulder, the two-path warning kept), `docs/reference/references/glossary.md` row `chromaFloor`, `decision-records.md` a dated amendment line under ADR-025 (the even floor is envelope-relative in the interior; the even envelope has a shoulder at the anchor; both allow-lists retired), `docs/reference/CHANGELOG.md` next entry, `docs/reference/rubrics/quality-rubric.md` if it states the floor mechanism. The C11 greps. `node test/repo/branding.mjs` and `test/repo/citations.mjs` green. Criteria: C1, C10, C11. Depends on U2.

Pre-land (adapter §2 step 1): `chroma-floor-prepr-reviewer` (reviewer-l4, the adapter's 2026-09-20 amendment) and `chroma-floor-prepr-verifier` (verifier-l3), verdict `.sdlc/verdicts/chroma-floor-prepr.md` with `verdict: 🟢` and the branch head sha, C12 run there, build and `npm run gate:sweeps` run there (the unit touches the tonal ramp, adapter §1; `gate:sweeps` now includes `gate:even-dips` and `gate:mode-isolation`, C4 and C6). CI on the exact head: every required job green, and the verdict names each by name with its conclusion, read from `gh run view <id> --json jobs --jq '.jobs[] | [.name,.conclusion] | @tsv'`: `build-test`, `panda-smoke`, `corpus-contrast` (already required and named on `origin/main`'s adapter, its 2026-09-19 CI-on-a-PR amendment), and every `sweeps (gate:...)` leg, including `sweeps (gate:even-dips)` and `sweeps (gate:mode-isolation)`. A verdict that does not name each leg, or names one as anything but `success`, is not 🟢 (revision 8: `main` has no branch protection, so a red leg blocks nothing mechanically; this line and the adapter's §2 list are the block, the same shape #713's Landing section rules for its own legs). Then the standing ruling R1 lands it without asking.

## Parity against #681's landed corpus

| What | May it move | Bounded how | Where it is checked |
|---|---|---|---|
| perceptual ramps, 3,780 + 16 | no | byte-identical, fixture hash | C6 |
| peak ramps | no | byte-identical, fixture hash | C6 |
| even ramps, stops 050 to 300 and 700 to 950 | only via U2's floor, only where the floor binds today | the four `--envelope` even cells stay OK on the gate path; 100 and 900 medians within 5 points of the landed head's (16.3 and 10.9 today), reported | C5, U2 handoff |
| even ramps, stops 350 to 650 | yes, U1's shoulder and U2's floor | the U1 probe moved 4,498 of 94,500 even cells at r = 0.2 with max dC 19.1 C; a total above 15% of even 25-stop cells across U1 and U2 together is a stop-and-report to the owner, not a silent pass | U1 and U2 handoffs, blast-radius report; the verifier reruns the builder's movement script itself at `<base>` and the unit head and reads the 15% line from its own run, never from the handoff (revision 8) |
| even stop 500 | no | the anchor pass-through is #681's C3; `env(500) = 1`; 0 anchored in-window palettes off | C5 (`env(500)` sweep), #681's `anchor-ramp` line unchanged |
| the 32 stop-500 dips (anchor under basis, notch class) | not this plan's | printed as K, named, not gated | C3 (revision 8: K moved from C4 to C3 in revision 7), Q-C / `NOTCH_ALLOW` |
| `prime.*` ladders, 7 rungs | no | `prime.mjs` untouched; `test/engine/prime.mjs` green unchanged | C1 |
| default kit even accents (450 dark, 550 light) | yes | `hpg-role-contrast` even FLOORS re-pinned only upward; every downward move named and handed to #662's policy; AA held for all 16 families | C8 |
| exports | even-mode colour cells in every format and the Figma bundle; Adia OKLCH export only if the Adia kit's even ramp moves (it carries `dampAmp` 70, so the cap path differs; report it) | named in C10 | C10 |
| `role-table.json`, binder `code.js`, type, geometry | no | diff empty | C10 |

## Blast radius (what U1 and U2 report, and what the PR body carries)

A movement table against `<base>` (revision 7: `report-preset-fidelity.mjs` has no `--movement` mode, its own header at line 10 says U4 did not build it; U1's handoff measures movement with a scratch script over `projectView` at `<base>` and at the unit head, the technique this plan's probe used, and names the script; no new report flag): presets moved / 343, palettes moved / 3,780, per mode; even-mode max |dL*| (expected 0.00, `toneAt` sets L*) and max dChroma per stop; perceptual and peak rows expected 0 moved (C6 makes anything else a FAIL); the count of even 25-stop cells moved and the 15% line; the lone-spike count before and after (64 + 1 to 0); the rendered dip histogram by stop before and after (450: 57, 500: 32, 550: 1 today); the four even cells on both readings; the default-kit even ratios per family before and after (a required field: U2 pass 1's handoff omitted it, review finding 4); the exports that moved; from revision 14, C13's probe table and drain count.

## Landing rule for plans after #691

Records-refresh (#691) landed at 28c2e8cc and its rule stands for every later plan (k17-rerun and #681 U5 carry the same line): a landing that changes `TESTS.length` repairs `.sdlc/baseline.md`'s test-file figure in its own pre-land commit, and the pre-land pass runs `sh .sdlc/checks/baseline-agrees-check.sh` as a gate. This plan registers no `TESTS` entry, so the figure is inherited from #713's landing (50 on its branch today); C12 states what every line the script prints must read at the rebased head and why the `ui.html` line alone may be STALE on an engine change without failing the landing.

## Not in scope

| Item | Why not here | Owner |
|---|---|---|
| the yielded perceptual 300 / peak 700 p90 cells (79.9 / 97.9 today on perceptual 300), and the full perceptual/peak median and p90 miss across all four stops | ruled yielded, #681 revision 24 ruling 1; C6 here makes perceptual and peak byte-identical, so they cannot move | that ticket now exists: #725 (`kind:bug`, `size:big`, owner-ruled 2026-09-20 at #681's pre-land, `.sdlc/handoffs/pif-prepr-review-p1.md` finding B2), which owns perceptual and peak by its own stated acceptance scope; this plan owns even mode only and claims neither |
| the 32 even dips AT stop 500 (anchor renders under a group basis above it) | a different mechanism (`anchorChromaBasis` blending to a higher group target), ruled under Q-C with `NOTCH_ALLOW` "pending U4"; #681 R2 settles by rule | #681 U4 |
| above-anchor stops on the anchored even path (14,979 stop cells) | the "0 above 100%" clause is gated on the gate path only; C5 reports the anchored path | ruled Q2: report only |
| the peak-mode `DIP_BASELINE` (6 names, 0 observed on the integrated tree) and its negative control | F1's peak-cap trade, ruling 3 of `pif-u3-yield-answer.md`, settled at U4 by rule | #681 U4 |
| the rendered-path C6 cells as a bar (300 p90 113.2%, 900 p90 51.1% on the anchored path) | never a ruled target; the ruled targets were measured and held on the gate path | ruled Q2: report only; a bar would be its own ticket. Note (revision 6): #725's own evidence table cites an `even 300: p90 113.7` reading and an `even 670` above-100%-of-stop-500 count alongside its perceptual/peak figures, but #725's Acceptance section scopes itself to "perceptual and peak cells" only, so this plan reads that as #725 NOT claiming the even rendered-path bar either, leaving it unowned exactly as this row already states; flagged for the owner rather than assumed |
| `dampAmp` semantics, the Adia carve-out, `EVEN_DAMP_FACTOR`'s value | Q7 ruled; a shoulder term is added beside the retune, the retune itself is not revisited | none |
| the default kit in the window, gap, distinct, notch, monotone, order and dupe sweeps, and an identity-control mode for the report | #715 (open, `kind:chore`, `size:small`); this plan's kit coverage is the lone-spike count (C2, already swept at 505416d7, folded into one count here) and the dip gate (C3, already in `dipDocs`), nothing wider; U1 does not push the kit into `anchor.mjs`'s shared loop (C2 says why) | #715 |
| moving #681's existing full-corpus sweeps out of `npm test` (the ~296 s ceiling) | #713 (`plan/gate-split`, U6b pending, lands before this plan starts: the start gate); this plan's two new sweeps join its `sweeps` matrix and `gate:sweeps` (C4, C6) and its reworked gates (C2, C3) live in the files #713 made sampled-by-default, read through their `--full` legs | #713 |
| the `hueSpace` even-mode hue solve and Q-D's inspector rule | unchanged by construction; C9 proves it | #681 U2 |
| `prime.mjs`, the ladder, `EVEN_DAMP_FACTOR` in perceptual or peak | untouched | none |
| a user-facing control for the shoulder radius | F4 forbids a new control; the radius derives from the sliders or is a documented constant | none |

## Risks

| Risk | Handling |
|---|---|
| #681 changes the gate shapes, the `NOTCH_ALLOW` population (15 at 505416d7, 17 at `<base>` after #739, none of the two in even mode), or the FLOORS between `<base>` and its landing on main | every criterion reads `<base>`, re-read at the landed sha before pre-land; U1's handoff quotes what it found there; the plan's revision log records the landed sha when each unit merges `origin/main`. Revision 7 already absorbed one such move (five criteria re-derived); a second is the same procedure, not a surprise |
| a shoulder wide enough to clear the spikes reopens the 300/700 cells | the probe shows r = 0.2 leaves them untouched to one decimal; C5 holds them; the radius is bounded by where the term reaches zero |
| the shoulder lifts 450/550 above the anchor on the anchored path (no cap there today) | the default (rendered) `--envelope` run's `above 100% of stop 500` count (670 at 505416d7) and the C6 (v) even violator count (1,205 / 3,764) are reported under Q2, never barred; U1's handoff states both before and after, and if either rose U1 adds the same `Math.min(chroma, anchorChroma)` cap to `chromaAt` and says so (revision 8: the row used to cite a `--rendered` flag C5 no longer has) |
| U2's floor cannot hold all four even cells and reach 0 rendered off-anchor dips | U2's stop rule: report which yields, by how much, no second workaround; the owner rules with the two tables |
| the floor's reference drains the far half of a ramp anchored near white or black, or a branch on the anchor's tone hides that drain outside the window (U2 review pass 1, finding 1) | revision 14: one continuous reference on both sites (the first-step ceiling), C13's edge probe and drain bound, #739's skip bound read with no special case; a further boundary branch is the stop rule's second workaround and goes to the owner |
| the revision-14 rule re-pins a different set of `tonal-legacy.json` even cells than pass 1's 37 and may move a FLOORS row pass 1 left alone | C8 as written: re-pin only upward, name every downward move, AA is the floor; the cumulative even-cell count against the 15% line falls, since the rule moves fewer cells than pass 1 (6,595 vs 8,342 anchored 25-stop cells in the re-diagnosis) |
| the even accents move enough that a family's even contrast ratio falls | C8: re-pin only upward, name every downward move, AA is the floor (#662), the corpus sweep is the backstop |
| another plan on `lane:color-engine` (an #681 follow-up) touches `tonal.js` at the same time | one active plan per lane (plan-rules); the Orchestrator sequences |
| the dip gate's replacement negative control is vacuous on the new floor | C3 requires a control that produces a non-zero count on the rendered reading, and C4 names today's engine as exactly that control |

## Owner rulings (2026-09-19, via the Conductor, `.sdlc/questions/chroma-floor-approval.md`)

| Q | Question | Ruled | Where it lands |
|---|---|---|---|
| Q1 | Approve the plan: three units, twelve criteria, no unit before #681 lands | Approved | this plan, `status: approved` |
| Q2 | The four even envelope cells were ruled and measured on the gate path; on the rendered path they read 48.0/113.2 at 300 and 22.9/51.1 at 900, with 14,979 stop cells above the anchor's chroma. Bar the rendered path in #701, or report it? | Report only in #701 | C5's default (rendered) `--envelope` table under `REPORTED, NOT BARRED`; the Not-in-scope rows for the rendered-path cells and the above-anchor stops; a bar, if ever wanted, is an envelope ticket of its own |
| Q3 | The 32 even dips exactly at stop 500 (the anchor under its group basis) are the notch class #681 rules on under Q-C. Stay outside #701? | Yes, they stay with #681's notch ruling | C3 prints the stop-500 count K and never asserts it (C4 before revision 7); the Not-in-scope row |
| follow-up 3 (2026-09-20) | Start now off #681's plan tree instead of waiting for it to land? | Yes, a third lane | superseded in revision 8 by the Orchestrator's sequencing after #713 (`depends:`, the start gate); the owner's answer is kept on record and the row says how to restore it |

## Revisions

| Date | Change | Why |
|---|---|---|
| 2026-09-19 | drafted from #701 and its 2026-09-20 scope comment, measured at 326592d2 with the witness table, the two-readings finding (gate path 0 dips, rendered path 90 dips and 64 + 1 spikes), and the plateau probe | standing ruling R3 (plan #701 now, in parallel with #681); planner, read-only on the engine |
| 2026-09-19 | revision 2: the nine checkability yellows folded (`chroma-floor-checkability.md`): C2's default-kit check scoped to the predicate block (the to-end-of-file awk was vacuous), C3/C4 print an unconditional count line under their own gate name (the shared `chroma-envelope` name masks a second message), C5 names READING (a) and pins the perceptual/peak blocks by md5 against the landed head, C7 states the hard-asserted call-count literal (5) and drops the vacuous `dampAmp` grep and names the `shoulder` identifier clash, C8 makes the upward re-pin readable from the FLOORS diff, C10 fixes the `docs/` list to six named paths, C11 names the count assertions that actually hold the line and one file per grep, C12 states `exit 3` as the pass value. Pre-state corrected: `NOTCH_ALLOW` holds 78 names (15 is what the gate observes), and every `dampAmp`-0 gate sweeps 3,764 palettes, not 3,780 (the Adia kit's 16 are skipped), so C2, C3 and C4 carry the real figure with the default kit counted separately. `depends:` now defines the landed head (the `origin/main` commit whose squash subject carries #681's PR number, recorded here at rebase time) and states that it and the lone-spike identifier are undeterminable until #681 has a PR. Labels row notes the issue's intake `size:small` beside the plan's `size:M` | team-lead, checkability review at 7eb7238b (3 green, 9 yellow, 0 red) |
| 2026-09-19 | revision 3: C8's FLOORS diff regex (rejected as an empty subexpression by both greps, and aimed at a key shape the table does not have) replaced by a node comparator over the actual array-literal entries of both files, verified today on the 326592d2 tables and on a scratch copy with one even entry lowered and one raised (`changed 2, down 1`, exit 1); C11's pre-state re-measured with the system grep at both heads (exact string 0, loose phrase on 4 lines at 326592d2) and the lone-spike identifier named as `LONE_SPIKE_ALLOW` from `unit/pif-u4-integration` @ e2df6a42, where U4 pass 2 replaced the `=== 0` assertion with an allow-list comparison that U1 reverses (the fourth grep proves it); C2 carries the same identifier note; the corpus row narrows the default-kit claim to the dip and spike sweeps; the `NOTCH_ALLOW` row records the e2df6a42 count | team-lead, re-check at 66c2ae6f (7 green, C11 yellow, C8 red) |
| 2026-09-19 | revision 4: two literals at e2df6a42 corrected and re-measured: `LONE_SPIKE_ALLOW` is on 9 lines of `test/engine/anchor.mjs`, not 5; `NOTCH_ALLOW` holds 142 entries, not 165 (the earlier count included comment lines that carry a backtick) | team-lead, re-check at ceb1b65c (C8 green, C11 yellow on one literal) |
| 2026-09-19 | revision 5: owner approved the plan through the Conductor (`.sdlc/questions/chroma-floor-approval.md`, copied byte for byte); the two open questions become the rulings table (Q2 report only, Q3 the stop-500 dips stay with #681's notch ruling) and the three rows that cited them now cite the ruling; `status: approved` | owner, via the Conductor |
| 2026-09-20 | revision 6: revalidation against #681's current tip (`plan/preset-intent-fidelity` @ `505bf565`, U1-U6 merged, U7 in flight, not yet landed). Grep-verified against that tip directly (no test run, host load rule honored): #681's own addendum-2 pass has already reworked the exact mechanisms C2, C3, C4 and C11 assume are still stale, and a second lone-spike allow-list (`DEFAULT_KIT_SPIKE_FINDING`, 1 name) now ships beside `LONE_SPIKE_ALLOW` (still 64 names), so C2's "exactly one `lone-spike` line" is provably false today and its awk `defaultDocument()` proxy no longer discriminates; `EVEN_DIP_BASELINE` is no longer the stale 53-name gate-path list but a 90-name, anchor-aware, rendered-path list (57 at 450, 32 at 500, 1 at 550, matching this plan's own earlier rendered-path measurement almost exactly), so C3/C4's "today" premise and expected gate shape are wrong. All four criteria marked STALE inline with what moved and why; re-deriving their exact Command/Expected text against the landed head is left to U1/U2's builder, since that requires the same measured rigor as the rest of this document and the mechanism is still moving under #681's own U7. `NOTCH_ALLOW` moved again, 142 to 102 names, matching a risk this plan already carried (not itself a fault). Confirmed against `.sdlc/handoffs/pif-prepr-review-p1.md` and gh issue #725 that the mode split holds: #725 (`kind:bug`, `size:big`) owns the perceptual and peak median/p90 miss by its own stated acceptance scope, this plan owns even mode's `chromaFloor` dip only and claims neither of the other two; the Not-in-scope table's "a new ticket if the owner wants them" line now names #725. `status: draft`: the four STALE criteria are load-bearing (C2/C3/C4 are U1's and U2's Criteria rows) and their Command/Expected text no longer describes what a builder would find at the landed head, which is more than a number drifting; the owner's 2026-09-19 approval covered a different gate shape and should see this before U1 starts | orchestrator, revalidation per team-lead dispatch |
| 2026-09-22 | revision 7: the four STALE criteria repaired against `plan/preset-intent-fidelity` @ `505416d7` (the branch tip, 47 commits past revision 6's `505bf565`; `tonal.mjs` and the report are byte-identical between the two), each with a command, an exact Expected, a negative control and a "Today at 505416d7" row, plus a fifth found stale by measurement: C5, because `report-preset-fidelity.mjs --envelope` reads the rendered path since fa0264fa and nothing prints the ruled gate-path cells (U1 adds `--gate-path`; the revision-5 `--rendered` flag is dropped since the default run is that table). C2 retires both lone-spike lists to one count; C3 keeps #681's rendered-path dip gate and drops its 90-name baseline; C4 becomes the gate-path reading as a CI gate script and C6's fingerprint becomes one too (#713: new sweeps go in gate scripts, not `npm test`; C1 and C12 stay at 49 test files); C11 greps all three lists. Literal drifts corrected: `TESTS.length` 49, `NOTCH_ALLOW` 79 (revision 6's 102 was a miscount, verified at both shas), the `chromaEnvelope(` assert at `tonal.mjs:1137` and 1138, C12's pass value `stale total: 1`, `exit 1` (the script exits 1 for any stale count and the rebased head's ref is in `origin/main`'s history, so revision 5's `exit 3` never matched the script). The `depends:` row and the Units preamble now carry the owner's 2026-09-20 follow-up (build off #681's tree, merge `origin/main` once after its squash), which revision 5's approval commit recorded in `chroma-floor-approval.md` but never wrote into this file. The blast-radius section no longer cites `--movement` (the report has no such mode). Not-in-scope gains #715 (default kit in the other seven sweeps, no overlap: this plan's kit coverage is already what #681 shipped, folded) and #713. The re-read table above "Feasibility probe" records every value with its control. `status: draft` stays: five criteria and the unit contents changed since the owner's approval | orchestrator, replan per team-lead dispatch |
| 2026-09-22 | revision 8, on the checkability review of f4c556f9 (`chroma-floor-checkability-rev7.md`, 5 green, 12 yellow, 0 red), every yellow folded. Sequencing: the plan now starts after #713 lands (a start gate decided by command, U0; `<base>` is #713's landing commit on `origin/main`; the owner's earlier "start off #681's tree" answer is kept on record as superseded, with the way to restore it), because #713 makes `tonal.mjs` and `anchor.mjs` sampled by default, so C2 and C3 read the `--full` legs `gate:corpus-anchor` / `gate:corpus-tonal` captured once to a file, C1 and C12 key to `TESTS.length` and the check script at the rebased head, and C4 and C6 join the `sweeps` matrix and `gate:sweeps` rather than standing as their own jobs. Landing blockers named: C4 and C6 add their legs to the adapter's §2 CI-on-a-PR list, and the pre-land line names every required job (`build-test`, `panda-smoke`, `corpus-contrast`, every `sweeps` leg) with the `gh run view` read, a verdict without them not being 🟢. C3's control (1) is re-targeted to U2's floor line (the shipped target is the line U2 rewrites), control (2)'s figure is measured at U1's merge head, both sweep `dipDocs`. C6 gains the fixture re-capture rule for #725. C7's vacuous `dup` grep replaced by the `chroma-envelope` pass line and source counts of `GRID_R2_EXCEPTIONS` and `KNOWN_BASELINE_DUP` (21 and 21, controls 0 and 22). C8's comparator asserts 16 entries per mode in both files (a deleted family used to pass silently; verified: `even entries: base 16, branch 15`, exit 1). C10 diffs against the merge-base. C11 reads `NOTCH_ALLOW` as `15 (expected 15)`, the gate's own print (revision 7's 79 and revision 6's 102 were awk ranges that ran into the lone-spike list), and names which line prints which corpus figure. C12 keyed to the script's output, ten lines today. Q3 and the parity K row point at C3. The Risks row for above-anchor no longer cites `--rendered`. The blast-radius 15% line is rerun by the verifier. Flags written literally (zsh). C2's combined grep reads 16, not 17; `const shoulder` is at `tonal.js:395` | orchestrator, checkability review by the team lead |
| 2026-09-22 | revision 9, on the revision-8 recheck (13 green, 4 yellow, 0 red), the four folded: the `<base>` selector and the start gate use `git log -1 -S'"gate:sweeps"' origin/main -- package.json` instead of a `(#713)` subject grep a record commit could satisfy (verified: nothing on `origin/main`, a11e8aff on `plan/gate-split`); the sequencing after #713 is put to the owner under item 1 rather than settled by the plan; C9 reads the hueSpace bound lines from C2's `$F/a.log` (bare `anchor.mjs` is the sampled leg after #713); U1 and U2 name the `sweeps` matrix leg, `gate:sweeps` membership and the adapter's §1 and §2 rows, matching C4 and C6; U0 re-pins every line number #713 moves or replaces it with its identifier. Two cosmetic slips fixed: C2's both-identifiers line is 991, and `corpus-contrast` is already named at `adapter.md:54` | orchestrator, recheck by the team lead |
| 2026-09-24 | revision 10, U0 done: the start gate printed green (`a62ec020`, `1`, `1`, `50`); `<base>` = 282fca8d, the `origin/main` tip the Orchestrator merged into the plan branch at 4381e860 rather than reset it onto (the branch's nine pushed revision commits and its records stay reachable; `git merge-base plan/chroma-floor origin/main` reads 282fca8d, the verifier's check). Every 'Today at 505416d7' figure re-read at 282fca8d in a read-only shared clone (the section 'Re-read at 282fca8d'; every heavy run in the foreground on a host under its core count): the two even populations this plan retires are unchanged (64 + 1 lone spikes, 90 rendered dips with K = 32, gate path 0 with the 1.6x control at 155), the four even cells identical, perceptual identical, peak 900 median 29.3 to 29.4 by #739's achromatic hue branch (the C5 md5 and the C6 fixture key to `<base>`, `6e558839ee9e43217e1e2f7afc898b7b` for the C5 extraction). Every line pin in the U0 list re-pinned or replaced by its identifier (the pin table in that section); none failed to resolve. Criteria moved by the three landings: C1 N = 50; C7 `KNOWN_BASELINE_DUP` 23 (#739's two peak keys); C11 `NOTCH_ALLOW` `17 (expected 17)` (#739's two, peak and perceptual, the 15 even names unchanged); C3 notes the one-name peak and perceptual dip lists U2 leaves alone; C12 reads 14 lines and names the script's literal gate list, so U1 and U2 each add their gate row there. Two vacuous greps found and re-pinned: C8's `hpg-role-contrast` (the gate prints `role-contrast`) and C9's `(hs)` grep on the shim's output (`ok()` records failures only; the PASS line plus the source count are the evidence). Nothing blocks U1 and nothing the owner approved changed shape: every moved count is in a list outside even mode | planner, U0 per the Orchestrator's dispatch |
| 2026-09-25 | revision 11, owner ruling R44 on `.sdlc/questions/chroma-floor-U1.md` (answer B): U1's shoulder at the plan's minimum radius 0.2 moves five even FLOORS cells down, all above AA; C8 now grades those five named re-pins (`down 5` cells, each named in the handoff and on #662) instead of leaving down-moves out of the table, which would have left C1 red | orchestrator |
| 2026-09-25 | revision 12: C8's comparator figure is `changed 4, down 4`, since the comparator counts FLOORS rows and Warning's light and dark cells share one row; the five named cells are unchanged | orchestrator, from U1's run at `585ba1d9` |
| 2026-09-26 | revision 13, from U1's verdict (`.sdlc/verdicts/chroma-floor-U1.md`, 🔴 at `fb85ed0b`, no engine defect): (a) C7 at U1 grades the `chroma-envelope` uptick and duplicate-hex sub-checks, which raise nothing; its dip sub-check stays red at U1 by design, because the shoulder moves the even dips U2 retires: 37 of the 90 cited 450-stop dips close, and 3 new stop-400 dips open (Sapa `secondary|400`, Wadi Rum `primary-muted|400`, Yixing `primary|400`). U2's C3 retirement of `EVEN_DIP_BASELINE` must bring those three to 0 too, and the plan's pre-land reads C7 whole. (b) C10 admits by name the four citation-forced paths U1's line shift in `tonal.js` moves: `docs/reference/SKILL.md`, `docs/reference/rubrics/acceptance-criteria.md`, and the two `docs/reference/reviews/2026-08-20-reactivity/` files; any other path is still a FAIL. (c) C2's `grep -cE 'lone-spike'` Expected is `2`: the gate line and the review-F3 control line | orchestrator, from the verdict's routes |
| 2026-09-26 | revision 14, from U2 review pass 1 (`.sdlc/verdicts/chroma-floor-U2-review.md`, FAIL at 0427d587) and the planner's re-diagnosis (`.sdlc/plans/chroma-floor-U2-rediagnosis.md`): (a) the U2 entry names the floor's reference, `max(maxc500, maxc(hue, tone(450)), maxc(hue, tone(550)))` on both sites, retiring pass 1's `clamped ? Infinity : maxc500` (a level cap at the anchor stop's ceiling that drained the far half of near-white and near-black ramps, masked outside the window by a branch whose only job was #739's skip bound; measured: `#E8EEFA` 5.4 C at stops 750 to 950 against `#ECF1FC` 22.3 one L* away, 215 cells in 96 anchors losing over 8 C and over half their chroma vs the pre-#701 floor). The rule is continuous, keeps 0 off-anchor dips on both paths, leaves the four gate-path cells at one decimal, holds #739 at 3 of 30 with no special case, and reads 16 cells in 6 anchors; the rate-limited and excess-release alternatives are rejected in the record with figures. (b) C3's negative controls are the pre-#701 floor at 1x (24) and 1.6x (381 rendered), replacing the vacuous shipped-floor-at-1.6x member; C4's in-script control figure is 120 at U1's head, not 155. (c) C13 added: the edge probe and the drain bound. (d) The U2 handoff carries the default-kit even ratios per family and repairs `.sdlc/adapter.md:36` (seven `sweeps` rows, 355 to 475 s). (e) The "no off-anchor dip can form" wording becomes a gated measurement conditional on a non-increasing damped value. Two risk rows added. U2 returns to pass 2 on `unit/cf-U2`; no owner call: the change stays inside U2's scope under R54 and the stop rule is not triggered | planner, re-diagnosis measured in a shared clone of `unit/cf-U2` @ b5d8ffd7 |
