# Checkability review: baseline-regex plan (#718), at 562f58ec

Read-only. All plants ran in throwaway `git clone -q --shared` clones under this session's job
tmp dir, never in the tracked repo tree. `git status --short` on the repo confirms no edits leaked
back. No `npm test` was run, per host rule.

| # | Runnable | Control can fail | R10 | State | Evidence |
|---|---|---|---|---|---|
| P1 | yes | yes (not rerun this session, host rule) | clean | 🟢 | `npm test 2>&1 \| tail -1` today prints `` `✓ all 48 test files passed` `` (confirmed via `TESTS.length` in `test/run.mjs` = 48, and the print template at `test/run.mjs:33`). Negative control edits `` `"scrim` `` to `` `"scrimX` `` in `role-table.json`; confirmed `` `scrimWeakest`/`scrimWeaker`/`scrimWeak` `` are real keys consumed by `` `test/engine/semantic.mjs` ``'s `` `refs-canonical` `` deep-equal gate (`test/engine/semantic.mjs:65-71`), so the plant is structurally certain to fail, not vacuous. Not independently rerun by me (host rule: no `npm test` this review), matching the planner's own note that it was "last independently run at `d34b4fb1`". |
| P4 | yes | yes | clean, minor nit | 🟡 | Ran verbatim: `` `branding: clean (590 files scanned)`, exit 0 ``. Plant (`cp decision-records.md docs/x.md`) gave `` `FAIL: 3 branding violation(s) across 520 files`, exit 1 ``. Control discriminates correctly (`FAIL`+exit 1 vs `clean`+exit 0 is the real signal). Nit: the main-path row explicitly disclaims `` `N not pinned` ``, but the negative-control cell pins `` `519 files` `` with no such disclaimer, and I reproduced drift on both counts (589→590, 519→520) purely from unrelated concurrent-seat scratch in the shared root checkout. Not an R10 violation (no line number, and this isn't a programmatic needle, nothing greps for "519") and not vacuous, but worth adding the same "(N not pinned)" qualifier to the negative-control cell for consistency, since a builder/verifier who takes the count literally could pause unnecessarily. **Correction, Orchestrator, 2026-09-21 (#718 critic read, R9):** the two counts above cannot both describe one tree. The plant ADDS a file, so the control's count must exceed the clean one, and 520 is below 590. They were taken minutes apart in the shared root checkout, whose untracked set other seats were changing, so the pair is two trees, not one. Re-measured in the `br-plan` worktree at `880b4a36`, a tree no other seat writes to: clean `` `branding: clean (527 files scanned)` ``, then with the same plant `` `FAIL: 3 branding violation(s) across 528 files` ``, one file more, as the mechanism requires. The verdict on P4 is unchanged: the control discriminates, and neither count is pinned. |
| P5 | yes | yes | clean | 🟢 | Ran in a clean clone off the shared root's current tip: baseline reads `0` (plan file and fixed check script both correctly excluded by the wall regex). All four listed plants (one byte on `adapter.md`; `.sdlc/checks/new.sh`; `.sdlc/verdicts/k17-rerun-baseline-regex.md`, slug not at the start; a second `.sdlc/plans/baseline-regex-extra.md`) each independently bumped the count to `1`. The row's own documented 1→2 instability in the shared root checkout reproduced live during this review (my own reading in the shared checkout was `1`), which is exactly why the row correctly mandates reading P5 from the unit worktree rather than here, this is the right design, not a defect. |
| P6 | yes | yes | clean | 🟢 | Clean tree: `0`. Fixture (em dash outside a backtick span in a `+` line under `.sdlc`): `1`. Fixture (same glyph inside a backtick span): `0`. Both directions confirmed; the strip-then-grep is not vacuous. |
| U1-1 | yes | yes | clean | 🟢 | At `562f58ec` (pre-fix): `` `matchAll(...)` count 0, `.match(...)` count 1 ``, matching the plan's stated "before" row exactly. After applying the plan's own §Texts fix verbatim in a clone: `` `1, 0` ``, matching the "after" row exactly. The needle strings are the exact source literals §Texts requires land ("with exactly:"), not fragile prose or a line number, a legitimate diff-landed check, not an R10 violation. |
| U1-2 | yes | yes, but the control's plant depends on a brittle exact-string `sed` match against `.sdlc/adapter.md`'s CURRENT row text (a file this unit never touches and does not own) | clean | 🟡 | Reproduced both directions exactly as claimed: unfixed script on the planted `test`-row clone stays `` `ok ... exit 0` `` (the #718 defect itself, confirmed live); fixed script on the same shape goes `` `STALE time test: baseline 56 to 60 s, adapter 56 to 60 s, 280 to 550 s`, exit 1 ``. Not vacuous today. Risk: `sed -i '' 's/56 to 60 s \| builder every pass/…/'` silently no-ops if `.sdlc/adapter.md`'s row text drifts even slightly (this file is edited by several other concurrently-active seats visible in this session's roster, e.g. `gate-gaps-*`, `rf-*`, `pif-*`). A silent no-op means no second range is ever planted, so the fixed script correctly still reads `ok`/exit 0 on what the block believes is a planted tree, and the verifier, per step 3's own instruction ("disagrees with the criteria's expected STALE/exit-1 reading … the unit returns to the builder"), would bounce a CORRECT fix back to the builder over an unrelated file drift. Not broken today (verified live), but worth an assertion right after the `sed` line, e.g. `grep -q 'INTERIM ceiling' .sdlc/adapter.md \|\| { echo "PLANT FAILED: adapter.md row text drifted, update the sed pattern"; exit 2; }`, so a silent no-op reads as an obvious plant failure instead of a confusing false red on the fix. |
| U1-3 | yes | yes, same brittle-sed risk as U1-2, on the unrelated `build` gate row | clean | 🟡 | Reproduced both directions exactly as claimed: unfixed script stays `` `ok time build: baseline 1 to 3 s, adapter 1 to 3 s`, exit 0 `` on the planted clone; fixed script goes `` `STALE time build: baseline 1 to 3 s, adapter 1 to 3 s, 9 to 9 s`, exit 1 ``. Confirms the fix is not a `test`-row special case, as claimed. Same fragility as U1-2 applies to the `build` row's exact text (`` `1 to 3 s warm \| builder when the unit touches` ``); same fix suggested. |

## Scope wall and checklist line

- Scope wall lists exactly the files U1 touches for its fix (`.sdlc/checks/baseline-agrees-check.sh`, the time-loop lines only) plus the process artifacts (handoff, verdicts, plan, board). P5's grep regex is a faithful encoding of that wall, verified by direct construction above (4/4 out-of-wall plants correctly counted, 0 in-wall). Nothing is missing: the fix's whole surface is one file, per the plan's own root-cause section, and no other file needs a code change.
- The `- [ ] U1 (S) …` line parses correctly under both regexes: `progress.py`'s `` `^- \[([ ~x!r])\] (U\d+[a-z]?)\s*\((S|M|L)\)\s*(.+?)\s*$` `` and `board.py`'s `` `UNIT = re.compile(r"^- \[([ ~x!r])\] (U\d+[a-z]?)\s*\((?:S|M|L)\)", re.M)` ``, both matched `mark=" "`, `id="U1"`, `size="S"` against the live plan file at `562f58ec`, confirmed by running each regex directly.

## R10 (no line-number or exact-prose-string needles in criteria rows)

No criterion Command/Expected/Negative-control cell pins a line number anywhere. The plan's only
line-number citation (`` `baseline-agrees-check.sh:21` ``) is in the evidence table, which the
team lead's brief already rules is fine, and it's accurate (`grep -n` confirms line 21). U1-1's
grep needles are exact source-code literals tied to §Texts's own "with exactly:" requirement, not
prose descriptions of behavior, so they don't read as an R10 violation.

## Reds

None. No row graded 🔴.

## Would-fix notes for the 🟡 rows (not blocking, but worth folding into the handoff)

- P4: add "(N not pinned)" to the negative control's file-count reading, matching the main-path row's own disclaimer, so a drifted count doesn't read as a failed plant.
- U1-2 / U1-3: add a `grep -q` assertion immediately after each `sed -i` line confirming the planted string actually landed in `.sdlc/adapter.md`, so a future drift in that file's row text fails loud (plant failed) instead of silently producing a spurious red on a correct fix.
