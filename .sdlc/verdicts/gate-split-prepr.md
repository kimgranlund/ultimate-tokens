---
kind: verdict
plan: gate-split
seat: verifier
pass: 1
pr: 743
ticket: "#713"
written: 2026-09-24
---

verdict: 🟢
sha: 7d09989642015812653d96e677fcd20562139338

# Pre-land verdict gate-split · 🟢

Independent grade of `plan/gate-split` at `7d099896` for PR #743. Read-only. Every command below ran in `git clone -q --shared` at `/private/tmp/claude-501/gs-prepr2/clone`, checked out at the head sha, `node_modules` absent, tree clean before and after (`git status --short | wc -l` printed `0`). The clone's `origin/main` is the local `main` (`2890d67a`); the real remote head `d50241ae` was fetched in as `real/main`. Diff base for P8 and P9 is `$(git merge-base real/main HEAD)` = `2890d67a`. Timing rows are graded from the committed records only; my one `npm test` is the independent half P2 asks for and is recorded with its readings, not used as a figure of record. The clone's removal was denied; see Clean-up at the end.

Owner rulings applied: #713 approved with the one CLAUDE.md CI-jobs line; R34 quiet windows; R35 shrinks the sample (U6c); R36 re-time first; R38 changes only the CLAUDE.md `~60 s` mention. Carried, not red, per the Orchestrator: U6-5's head-ref stale line (U6-9 re-points it at close-out), P10's stale count `4` where 5 legs are measured, U5's branding-at-base note.

## Pass 1, at `7d099896`

| # | Criterion | State | Evidence | Negative control |
|---|---|---|---|---|
| 1 | Head and PR identity | 🟢 | `git rev-parse 7d099896` = `7d09989642015812653d96e677fcd20562139338`; `gh pr view 743 --json headRefOid,mergeable,mergeStateStatus` = `7d09989642015812653d96e677fcd20562139338`, `MERGEABLE`, `CLEAN`, `isDraft: true`, base `main` | `origin/plan/gate-split` resolves to the same sha; a stale PR head would print a different `headRefOid` and this row could not cite the CI run below at the same sha |
| 2 | P1 `npm test` green, no `node_modules`, N agrees, tree byte-stable | 🟢 | Clone, foreground: `exit 0`, last line `✓ all 50 test files passed`; `perl` count of `TESTS` prints `50`; `git status --short | wc -l` prints `0`; `NO_NM` before and after; `repo/branding.mjs pass` inside the run | The adapter's own control (`"scrim` to `"scrimX` in `role-table.json`) is the baseline verdict's and reds `engine/semantic.mjs`; here the control that bit is the N check: `TESTS` at `49` would print `STALE tests` in P7's script (run, see row 8) |
| 3 | P2 the ceiling holds in the figures of record | 🟢 | P2's command over `.sdlc/baseline.md` prints `ok 89.1`; the `npm test` row reads `89.10 · 79.93 · 80.07` (U6c-8, window 2), each under 120; adapter §1 test row reads `80 to 89 s` and `ceiling 120 s` | `sed` one figure to `121.00` and the same command prints `OVER 121` |
| 4 | P2 independent half, the verifier's own `npm test` | 🟡 carried (the run passed inside the band; timing rows are graded from the committed records by the Orchestrator's rule, so the loud reading is informative, not a criterion) | `/usr/bin/time -p npm test` in the clone: `real 88.16`, `user 90.10`, exit 0, inside the `80 to 89 s` record and under the 120 s ceiling. Readings before: `hw.ncpu 10`, load `6.13 4.49 4.28`, hot `1`, pgrep showed another seat's `node test/run.mjs` (pid `40206`) running; after: load `6.65 5.40 4.67`, hot `3`. The quiet-host rule (load under 5, pgrep empty) did not hold, so by the plan's own P2 sentence this half grades 🟡, never 🟢, and the figure is informative only | The plan's rule is what makes this yellow: a run that started at load `6.13` with a foreign `test/run.mjs` alive is exactly the row U6-3's awk rejects (`$4+0>=c` is false at 10 cores, but pgrep `!= none` rejects it); the wall still landed inside the band, which is why it is not red |
| 5 | P3 coverage not given back: every gate script sweeps everything | 🟢 | CI run `35999799707` at this sha, leg logs fetched with `gh run view --job <id> --log`: `(FULL: 343 curated documents, 3780 palettes)` count `1` in `sweeps (gate:corpus-tonal)` (job `107633378853`), `sweeps (gate:corpus-anchor)` (`107633378910`), `sweeps (gate:corpus-reset)` (`107633378715`); `(FULL: 2000 determinism cases, 1500 poison renders, hue step 1)` count `1` in `sweeps (gate:sweep-prime)` (`107633378496`); all four legs `success`. Locally the two mutant runs (rows 12, 13) also printed their FULL line once each | `build-test`'s log (job `107633378402`) prints `0` FULL lines; `grep -c -- '--full' test/run.mjs` prints `0`; the clone's `npm test` log prints `0` `(FULL:` lines. Without the `sweeps` job no FULL sweep runs anywhere |
| 6 | P4 CI invokes every gate script, nothing masks a red leg | 🟢 | P4's command: `corpus-tonal 1`, `corpus-anchor 1`, `sweep-prime 1`, `corpus-reset 1`, contrast `2`, `sweeps:` job count `1`, `if:`/`continue-on-error:`/`needs:` inside the block `0`, `fail-fast: false` `1`, `pull_request:` `1`. The job body: matrix of five `gate:` names, `runs-on: ubuntu-latest`, `actions/checkout@v7`, `setup-node@v7` node 24, `run: npm run ${{ matrix.gate }}` | In the clone, the `gate:corpus-anchor` matrix entry commented out: the stripped block prints `0` for it (restored from a backup after) |
| 7 | P5 mutants at the head, FULL leg (rows 4 and 6, the two the Orchestrator asked for) | 🟢 | See rows 12 and 13: M-B `exit 1` with `409/151200 out-of-gamut rungs exceeds the pinned ceiling of 0` and `7/2000 palettes shifted hex by call order`; M-D `exit 1` with `(rst-corpus) 3396 of 3396 anchored palettes failed the exact-snapshot field round trip`. Rows 1, 2, 3, 5, 7 stand as graded in the U2 and U3 verdicts (`.sdlc/verdicts/gate-split-U2.md`, `gate-split-U3.md`, each with `git diff --stat` 1/1) | The unmutated FULL legs at this sha (CI legs, row 5) print `0/2000 palettes shifted`, `same sweep): 114/151200`, no `FAIL` line, and the reset leg no `(rst-corpus)` failure |
| 8 | P7 baseline agrees with `TESTS` and the adapter, by script | 🟡 carried (documented unit-branch exception; U6-9 re-points `ref` at close-out) | `sh .sdlc/checks/baseline-agrees-check.sh`: `ok tests: baseline 50, test/run.mjs TESTS 50`, `ok ui.html`, every `time` line `ok` including `time gate:corpus-tonal 86 to 116 s`, `gate:corpus-anchor 79 to 100 s`, `gate:sweep-prime 67 to 86 s`, `gate:corpus-reset 57 to 83 s`; then `note  head:` and `STALE head: baseline ref c87d98fc`, `stale total: 1`, `exit 1`. The one STALE is the U6-5 head-ref line the Orchestrator carried to close-out; adapter §5 carries U6-9 (`sets .ref. to the squash sha` count `1`). Files moved outside `.sdlc/` since `c87d98fc`: `.claude/CLAUDE.md`, the shipping skill, `ci.yml` comments, `prime.mjs` comments, plus main's `decision-records.md` and `gen-adia-derived-exports.mjs` | Baseline `all 50` retyped to `all 49`: `STALE tests: baseline 49, test/run.mjs TESTS 50`, `stale total: 2` (restored after) |
| 9 | P8 branding clean, no em dash in added lines | 🟢 | `repo/branding.mjs pass` inside the green `npm test`; `git diff 2890d67a | grep '^+' | perl ... /\x{2014}/ | wc -l` prints `0` | A fixture of one added line with U+2014 piped through the same filter prints `1` |
| 10 | P9 scope wall | 🟢 | `git diff --name-only 2890d67a` through the allow-list with `Q2=yes` prints `0`; `git diff --name-only 2890d67a -- src | wc -l` prints `0`; the 44 changed paths are all under `.sdlc/`, `test/`, `.github/workflows/ci.yml`, `package.json`, the shipping skill and `.claude/CLAUDE.md` | The five-name fixture (`.claude/CLAUDE.md`, the skill, `.sdlc/x.md`, `test/engine/tonal.mjs`, `src/engine/hct.js`) prints `1` with `Q2=yes`, the `src/` path alone surviving |
| 11 | P10 CI wall budget at this head | 🟢 | Run `35999799707` (`pull_request`, created `2026-09-24T12:33:26Z`): P10's jq prints `success`, `202`, `198`, `5`. Legs: `sweeps (gate:corpus-tonal)` success 198 s, `(gate:corpus-anchor)` success 125 s, `(gate:corpus-reset)` success 123 s, `(gate:sweep-prime)` success 83 s, `(gate:corpus-contrast)` success 45 s; wall 202 s at or under 300, longest leg 198 s at or under 240. The plan's expected `4` is the stale count the Orchestrator carried; `5` is what U6-2 defines and what is measured | The same jq over run `35517479022` (pre-split) prints `success`, `117`, a blank third line, `0`: a run without the `sweeps` job cannot pass this row |
| 12 | M-B red at the head, FULL prime leg | 🟢 | Clone at `7d099896`, `perl` edit from the plan's mutation section, `git diff --stat` = `1 file changed, 2 insertions(+), 2 deletions(-)`; `npm run -s gate:sweep-prime` `exit 1`; needle `out-of-gamut rungs exceeds the pinned ceiling` count `1`; `grep -cE ' [1-9][0-9]*/2000 palettes shifted'` `1`; lines `7/2000 palettes shifted hex by call order` and `FAIL  gamut-ceiling  — 409/151200 out-of-gamut rungs exceeds the pinned ceiling of 0`; FULL mode line count `1`. `hct.js` restored with `git checkout --`, status `0` | The same command unmutated in CI at this sha (job `107633378496`): `0/2000 palettes shifted` count `1`, `114/151200` control witness count `1`, `26` pass lines, `0` FAIL, `PASS: prime-system clears all AC-050 gates` |
| 13 | M-D red at the head, FULL reset leg | 🟢 | Clone, `perl` edit on `src/ui/sections/color.js`, `git diff --stat` = `1 file changed, 1 insertion(+), 1 deletion(-)`; `npm run -s gate:corpus-reset` `exit 1`; `(rst-corpus)` count `4`, including `✗ (rst-corpus) 3396 of 3396 anchored palettes failed the exact-snapshot field round trip`; FULL mode line count `1`. File restored, status `0` | Unmutated in CI at this sha (job `107633378715`): `(FULL: 343 curated documents, 3780 palettes)` once, `HEADLESS BOOT PASS` once, `stride` count `0` (the stride line prints only under SAMPLED) |
| 14 | U2-1, U3-1, U4-1, U5-1 FULL legs green at the head (pre-land rerun, taken from the CI legs at this sha rather than timed locally) | 🟢 | tonal: `21` pass lines, `0` FAIL, `PASS: tonal-generation clears all [gate] predicates` (U2's amended needle 19 to 21). anchor: `22` pass lines, `0` FAIL, `PASS (FULL): ... window-clamp (10), gap-19 (72), distinct-25 (16) and notch (15, Q3-resolved)` (U3 verdict recorded `19`; the head adds `key-anchor corpus`, `key-anchor rendered path` from U6b's `71cdb960` and `anchor-achromatic` from main). prime: `26` pass lines, `0/2000 palettes shifted` `1`, `114/151200` `1` (U6c-2's revision-14 count). reset: `HEADLESS BOOT PASS` `1`, FULL line `1` | Rows 12 and 13 are the mutated halves of two of these four legs; the U2 and U3 verdicts hold M-A, M-C and M-E red on the other two |
| 15 | P6 the sample is a pure function of the document set and the seed | 🟢 | `node test/engine/corpus-sample.mjs`: last line `PASS: corpus sample is a pure function of the document set and the seed`, `exit 0`; the duplicate-name leg prints `sampleCorpus threw as expected: ... two documents named "Dup"` | The U1 verdict's control (the `.sort()` on `vols` deleted) reds `7 of 7` categories; at this head U1-3 shows one picker only, so no second picker could pass unsorted |
| 16 | U1-2, U1-3, U1-5: sample size, one picker, exact gate scripts | 🟢 | U1-2 probe prints `35 392`; `grep -rlE 'fnv1a|% vols\.length' test/engine test/ui` prints one line, `test/engine/lib/corpus-sample.mjs`; U1-5 prints four `ok` and five `sweeps has ... ok`; `package.json` lines 30 to 35 carry the six `gate:` scripts and `gate:sweeps` as an `&&` chain of five | U1-2 with the corpus returned unsampled prints `343 3780`; a two-line picker fixture prints `2` where U1-3 wants `1` |
| 17 | U6-2 the matrix lists exactly the scripts that exist | 🟢 | Node probe prints `same 5` | A sixth script name appended to the `package.json` side prints `DIFFER 5` |
| 18 | U6-3 and U6-10 the Runs table: 15 counted quiet rows, load under 5 at start | 🟢 | `HF=.sdlc/handoffs/gate-split-U6b.md`: `15`, `0`, `32`; my awk over the counted rows prints `counted rows at load>=5: 0`. The timing verdict (`/private/tmp/claude-501/gs-U6-timing-verdict.md`, 🟡, 12 🟢 2 🟡 0 🔴 at `b2c61512`) placed every window-2 row outside both rg-U4 intervals; its one open item, the window-2 bounds, is fixed at `2101b69e`: the handoff now reads `2026-09-23 21:58 PDT to 2026-09-24 00:21 PDT` at lines 59, 83 and 128 and the old `22:14 PDT to 2026-09-24 00:19` count is `0` in U6b and U6c | A planted counted row with load `11.2` makes the second figure `1` |
| 19 | U6-6 adapter §1 and §2 | 🟢 | Gate rows `1 1 1 1`; ceiling sentence `1`; quiet-host `ps -Ao pcpu=,comm= | awk ...` needle `1`; interim needle `Interim ceiling now, split sweeps into gate scripts` `0` at the head; PR jobs `build-test 1`, `panda-smoke 1`, `corpus-contrast 1`, `sweeps 1`; `deploy. runs only on a main push` `1` | The interim needle on `real/main` prints `1`, proving it is live; the ceiling sentence reworded prints `0` |
| 20 | U6-7 the shipping skill | 🟢 | `1 5 0 0 0 1`; step 6 reads `Watch CI (~265 s wall on a PR, measured on run 35974499577: ...)`; the placeholder `not yet measured on plan/gate-split` count `0` | On `real/main` the same file prints `gate:sweeps` `0` and `all three legs` `1` |
| 21 | U6-9, U6-10, U6-11, U6-12 adapter amendments | 🟢 | §5 `sets .ref. to the squash sha` `1`; `load under 5` `1`; §2.1 `R8|R9|R10` `4`; `clone --shared` `1` | The same four greps on `real/main:.sdlc/adapter.md` print `0`, `0`, `0` for the first three |
| 22 | `.claude/CLAUDE.md` diff is exactly the approved CI-jobs line plus the R38 figure | 🟢 | `git diff origin/main...HEAD -- .claude/CLAUDE.md`: two hunks in the SDLC bullets, line 95 `~60 s` to `80 to 90 s`, line 99 CI jobs gain `` `sweeps` ``; `numstat` `2 2`; `grep -c '60 s'` prints `0` at the head (the file on this branch had one mention, as plan revision 16 states) | A third changed line would show as a third `-`/`+` pair in the same diff; the R38 question's own grep on main printed line 95 only |
| 23 | Adapter §1, `baseline.md` and CLAUDE.md agree on the `npm test` figure | 🟢 | `baseline-agrees-check.sh`: `ok    time test: baseline 80 to 89 s, adapter 80 to 89 s`; baseline row `89.10 · 79.93 · 80.07`; CLAUDE.md `80 to 90 s` is the exact wording R38 ruled for the summary line, the rounded band of the same three readings | Adapter retyped to `80 to 90 s`: the script prints `STALE time test: baseline 80 to 89 s, adapter 80 to 90 s`, so the two machine-read records are pinned to each other and only the owner-ruled summary line carries the rounded form |
| 24 | Merge-tree clean against main | 🟢 | `git merge-tree --write-tree real/main HEAD` prints tree `921bf873...` and `exit 0`, no conflict block; `real/main` = `d50241ae9303d00798e2ba3cf44cd10bc745afab` (one records-only commit past the merge-base `2890d67a`) | Against the clone's local `origin/main` (`2890d67a`) the same command also exits `0`; a conflicting tree would print `CONFLICT` lines and exit 1 |
| 25 | CI at the head sha green on every leg | 🟢 | `gh api .../commits/7d09989642015812653d96e677fcd20562139338/check-runs`: `build-test success`, `panda-smoke success`, `corpus-contrast success`, `sweeps (gate:corpus-tonal)`, `(gate:corpus-anchor)`, `(gate:sweep-prime)`, `(gate:corpus-reset)`, `(gate:corpus-contrast)` all `success`, `deploy skipped` (push-only); run `35999799707` conclusion `success`, later than `35978389659` (`b5383ffa`, also `success`) | Two earlier runs on this branch (`35978135691`, `35978104354`) read `cancelled` when superseded, so the listing does distinguish outcomes; the run cited is the one whose `headSha` equals the PR head |
| 26 | Pre-land review fix-first findings closed (U7, U7b) | 🟢 | `grep -c '#681\|runs red until\|unsplit' .github/workflows/ci.yml` `0`; `grep -c '151,200 every time' test/engine/prime.mjs` `0` and the FULL/SAMPLED comment at lines 37 to 42; U2 handoff line 220 carries the full `Trulli of Alberobello · vernacular · Puglia, Italy|primary|500` name; U7 review record `Verdict | PASS`, addendum `PASS at the final head 2875dcc7`; reviewer round 3 passed under Lane B | `.sdlc/verdicts/gate-split-prepr-review.md` rows 2b and 3b record the same greps printing `107`, `109`, `357` and the placeholder before U7 |
| 27 | No `.claude/docs/other`, no `node_modules` in the diff or the tree | 🟢 | `git diff --name-only 2890d67a | grep -c` for both prefixes `0`; `git ls-tree -r --name-only HEAD | grep -c` for both `0` | `test -e node_modules` in the clone prints `NO_NM` before and after `npm test`, so a tracked `node_modules` would have been visible as a tree entry, and the `ls-tree` grep would print a count |
| 28 | Carried items named, none red | 🟡 carried (owner-ruled: P10 count and U5 branding note) | (a) U6-5 head-ref `STALE` (row 8), re-pointed at close-out by U6-9. (b) P10's plan text still reads `then `4` (`3` if Q1 is no)` (plan grep count `1`) where `5` legs are measured (row 11). (c) U5's branding-at-base note: the U5 verdict row 14 reads `branding: clean (515 files scanned)` at its head; at this head `npm test` shows `repo/branding.mjs pass` | Each has a command above that would flip: `stale total: 0` once `ref` is re-pointed; the plan's `4` retyped to `5` prints `0` on that grep; a branding hit prints `FAIL: N branding violation(s)` and reds `npm test` |

Counts: 🟢 25 · 🟡 3 carried · 🔴 0. No yellow is an open defect: row 4 (my own `npm test` ran on a loud host, 88.16 s, exit 0, inside the band; timing is graded from records), row 8 (U6-5's head-ref STALE, documented, U6-9 re-points it at close-out) and row 28 (the owner-carried set). Under the carry rule (no red, every yellow carried with a ruling or a documented exception, as the #681 record landed) the record grades 🟢. Every FULL leg is named with its conclusion in row 11, as the plan's Landing section requires.

Clean-up: `rm -rf /private/tmp/claude-501/gs-prepr2/clone` was denied by the permission system twice, so the clone is still on disk at `/private/tmp/claude-501/gs-prepr2/clone` (checked out at the head, mutations reverted, `git status --short` empty); logs sit beside it under `/private/tmp/claude-501/gs-prepr2/` (`npmtest.log`, `p5-4.log`, `p5-6.log`, `ci-*.log`).


The review side of this pass: `reviewer-l4` (gs-prepr-reviewer-l4-p1) under Lane B, FIX-FIRST in round 1 (records and comments, closed by U7), PASS in round 2 at `b5383ffa` and round 3 at `7d099896` after U7b (R38); record in Lane B's scratchpad `gs-prepr-review.md`. CI run `35999799707` is green on all 8 legs at `7d099896`.

verdict: 🟢
sha: 7d09989642015812653d96e677fcd20562139338
