verdict: 🟢
sha: e409a061cc879cd0dbdb9950eee4d956bf34e85e

# Verdict aa-U1 · 🟢

Verifier-l2, fresh context, 2026-09-24. Unit branch `unit/aa-U1` (local and `origin/unit/aa-U1` both at `e409a061`). Plan `achromatic-anchor` revision 3, approved in `.sdlc/questions/achromatic-anchor-approval.md` (Q1 palette hue, Q2 OKLab C under 0.002, Q3 #739 first). Diff base `B=625248316db6` (`git merge-base origin/main e409a061`); `origin/main` is `2890d67a`, which differs from `B` in `scripts/gen-adia-derived-exports.mjs` only, so every "origin/main" reading below ran in a shared clone at `2890d67a` with engine files identical to `B`. Clones: `head` (at `e409a061`, no `node_modules`, where `npm test` ran once, foreground), `base` (at `origin/main`), `neg` (at `e409a061`, every edit reverted, tree `0` after each), `build` (at `e409a061`, `node_modules` symlinked read-only, for P2 only), all under `/private/tmp/claude-501/aa-u1-v`. The handoff and review record were read as claims; every row below is my own run.

| # | Criterion | State | Evidence | Negative control |
|---|---|---|---|---|
| G0 | #681 landed, anchored branches and U10 black guard on main, unit built on main | 🟢 | `gh issue view 681` prints `CLOSED COMPLETED`; at `B` the anchored-identifier count is `13` and `grep -c 'if (L <= 0) return'` okhsl.js is `2` (line 169 `okhslToRgb`'s own guard plus line 194, U10's guard); `git merge-base --is-ancestor 625248316 e409a061` exit `0`. The plan's Expected `1` for line 3 is a plan miscount (it forgot line 169), see note 1 | the same G0 lines at pre-#681 `dae67a3a` print `0` and `1`, so the anchored-branch count and the guard count both discriminate |
| P1 | `npm test` green, no `node_modules`, count agrees, tree byte-stable | 🟢 | clone `head`: `exit 0`, `✓ all 49 test files passed`; TESTS length `49` (same as `origin/main`, the unit adds no file); `git status --short \| wc -l` `0` | clone `neg`: `sed 's/"scrim/"scrimX/'` on `role-table.json` then `npm test` prints `exit 1`, `engine/semantic.mjs FAIL`, `refs-canonical ... ordered key set != canonical` |
| P2 | `npm run build` green, tree clean after | 🟢 | clone `build`: `exit 0`, last line `wrote figma/plugin/ui.html 4125.1 KB` (matches `.sdlc/baseline.md`), `git status --short` empty | clone `neg` (no `node_modules`): unedited build `exit 127` and grep `0`; with `export` removed from `rgbToOkhsl`, build `exit 1` and `grep -c "does not provide an export named 'rgbToOkhsl'"` prints `1` |
| P3 | branding clean, no added em dash outside backticks, raw count 0 | 🟢 | `branding: clean (649 files scanned)`; backtick-stripped count `0`; raw count excluding handoffs `0` | clone `neg`: `cp decision-records.md .sdlc/verdicts/x.md` gives `FAIL: 3 branding violation(s)`, `exit 1`; one prose em-dash line in the handoff plus one backticked em-dash line in `tonal.js` give second count `1` and third count `1` |
| P4 | scope wall | 🟢 | first command `0`, second command `0`; 15 files changed, all inside the revision-3 wall (`.sdlc/baseline.md`, `00-synthesis.md` admitted by revision 3) | the three-name fixture (`prime.mjs`, `test/run.mjs`, `tonal.js`) through the first filter prints `2`; an uncommitted `// planted` line on `src/ui/app.js` in clone `neg` makes the second command print `1` |
| U1-1 | `#808080` at palette hue 250 renders the palette hue in all three modes, stop 500 exact | 🟢 | `node "$F/hue.mjs" "#808080" 250 50` at head: `perceptual #808080 d300 5 d700 3`, `peak #808080 d300 0 d700 4`, `even #808080 d300 2 d700 2`; ends are blue-grey (`hex300 #ACB9C6`, `hex700 #444F5B`). `#808081`, `#000000`, `#010101` also at most `5` | same probe on `origin/main`: `d300 151 d700 156` (perceptual), `144 157` (peak), `153 158` (even), khaki ends `#BDB6A5` / `#524D40`: 144 to 158 degrees off, as required |
| U1-2 | `rgbToOkhsl` reads white achromatic, nothing else moves; the guard uses a tolerance | 🟢 | head prints `0 1.56e-5 254,254,254 255,255,255`; `okhsl.js:209` is `if (L >= 1 - 1e-6) return { ..., s: 0, l: 1 }`; `node test/engine/okhsl.mjs` prints `pass  white` | `origin/main` prints `0.5582831888483675` first. In clone `neg`, rewriting the guard as literal `if (L >= 1) return` makes U1-2 print `0.5582831888483675` again and `test/engine/okhsl.mjs` exit `1` with `FAIL  white ... rgbToOkhsl([255,255,255]).s is 0.5582831888483675, want exactly 0` |
| U1-3 | Nike tertiary-muted on the rendered path no longer khaki | 🟢 | head prints `25 33.21 98.38 7 2.77 500:#F1F1F1 650:#B8969F 900:#3B1E27`: field 2 at most field 3, worst hue `7` at most 12, stop 500 C `2.77` at most 3.50; stop 650 CAM16 hue `360`, not 91. See note 3 on what it renders now | `origin/main` prints `25 31.47 98.38 105 8.10 500:#F8F1DE 650:#B89E57 900:#2F2815` (650 at CAM16 hue `91`, C `31.47`): fields 4 and 5 both red the row |
| U1-4 | every chromatic anchor byte-identical; the caught ones named and counted | 🟢 | dump at `origin/main` and head: `10188` lines each; differing anchors `#ACADAE`, `#FFFFFF`, `6` lines (2 x 3 modes); 48 kit lines identical (diff `0`); corpus anchors under 0.002: `2`, under 0.004: `11` (my count over 3,380 dump anchors) | two planted changes in clone `neg`, each re-dumped against `origin/main`: constant set to `0.02` gives `342` anchors and `1415` lines; a `+ 0.5` degree on the chromatic `targetOklchHue` of both anchored branches (non-achromatic anchors only) gives `1705` anchors and `10187` lines, so a change to any non-achromatic anchor shows in the diff |
| U1-5 | synthetic gate pins the hue rule and white guard, in `npm test`, and bites on both sides of the constant | 🟢 | `node test/engine/anchor.mjs` `exit 0`, one line `pass  achromatic-anchor: 27 of 27 ... 3 skipped under CAM16 C 5 ... #808082 (OKLab C 0.0030, above the constant) chromatic and hue-stable ...: true`; `okhsl.mjs` `exit 0`, `pass  white`. The 3 skipped cells are white's stop 300 (`#F5F7F9` C `4.40` hue `225`, and so on), see note 4 | clone `neg`: `achromatic` forced to `< -1` gives `exit 1`, `FAIL  achromatic-anchor: 0 of 26 ...`; constant widened to `0.004` (swallowing `#808082`) gives `exit 1`, `FAIL ... hue-stable across a moved palette hue: false`; white control is U1-2's |
| U1-6 | every allow-list naming Nike tertiary-muted re-measured | 🟢 | Nike name counts at head `anchor.mjs 4`, `prime.mjs 2`, `tonal.mjs 0` (unchanged from `origin/main`); FAIL counts `0`, `0`, `0`; `tonal.mjs` exit `0`. Added entries, none naming Nike: `NOTCH_ALLOW` +2 `#ACADAE`, `KNOWN_BASELINE_DUP` +2 (`peak\|0\|100.00...`), `DIP_BASELINE` and new `PERCEPTUAL_DIP_BASELINE` one `#ACADAE` key each, see note 2 | clone `neg`: Nike line deleted from `RAMP_WINDOW_ALLOW` gives `2` FAIL lines, `FAIL  anchor-ramp allow-list: 10 (expected 9)`. Control (2) does not apply (no Nike entry dropped) |
| U1-7 | prime ladder does not move | 🟢 | prime dump at `origin/main` and head: `3780` lines, `cmp 0` | clone `neg`: `prime.mjs:146` `keyChroma = cam.chroma * 1.01` gives `differ: char 241, line 2`, `cmp 1` |
| U1-8 | §9 states the rule and names the constant | 🟢 | `grep -c ACHROMATIC_ANCHOR_C`: doc `1`, `tonal.js` `3` (both `0` on main); paragraph at `knowledge-02-tonal-scale.md:451` covers the constant, pivot lightness and chroma, the hue per branch, black and white `s = 0`, the 2026-09-23 ruling; audit's last non-empty line `canvasView enumerations: 0` at head and main | clone `neg`: deleting lines 450 to 464 drops the doc count to `0` |

## Notes (none is a red)

1. Plan text, G0 line 3: its Expected `1` and "Today `0`" are both off by one, because `okhslToRgb`'s own `if (L <= 0) return [0, 0, 0];` (okhsl.js:169) matches the same grep. `dae67a3a` prints `1`, `B` prints `2`; the step from 1 to 2 is U10's guard, so the gate's intent holds. Worth a plan revision if G0 is reused.
2. Allow-lists grew. `#ACADAE` (OKLab C 0.0018, the second caught anchor) now notches at stop 500 in peak and perceptual, allow-listed in `NOTCH_ALLOW` and both dip baselines. It follows from Q1 (a): a grey pivot with chroma at the palette hue on either side. `tonal.mjs` gained a `PERCEPTUAL_DIP_BASELINE` set and one `BASELINE_BY_MODE` key, slightly more than the plan's "allow-list entries only". I read the loop (`tonal.mjs:1595-1626`): perceptual was checked against no baseline before and is now checked against one named key with the exact-match `seenBaseline` test, so nothing got looser.
3. Nike tertiary-muted is no longer khaki. It now renders a mauve ramp at its stored `hue 0` (`650:#B8969F`, max CAM16 C `33.21`, above main's `31.47`). That hue is the generator's hueless-sample fallback, which the plan puts out of scope. The owner should look at this swatch before deciding the follow-up.
4. The U1-5 gate skips stops under CAM16 C 5 (floor `skipped <= 3`, today exactly 3: white's stop 300 in each mode, C 3.6 to 4.4, hue 219 to 225). The plan listed `#FFFFFF` under the 12 degree bound without a skip, but U1-3's own row uses the same C 5 filter, and the review's neutral-ramp control showed the floor bites.
5. Handoff accuracy: its U1-1 control quotes perceptual `d700 157`, but `origin/main` prints `156` (the plan says 156). Every other handoff number I re-ran matched: U1-3, U1-4's `342`/`1415`, U1-5's pass line, U1-7's `char 241, line 2`, `4125.1 KB`.

verdict: 🟢
sha: e409a061cc879cd0dbdb9950eee4d956bf34e85e
