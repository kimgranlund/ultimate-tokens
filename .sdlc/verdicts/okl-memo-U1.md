verdict: 🟢
sha: 348cc40252ea1fbbd0561040134d5df2deb41d66

# Verdict okl-U1 · 🟢

Plan okl-memo (#738), revision 5, unit/okl-U1 at 348cc402. Code rows were run at 62ec7e60; `git diff --stat 62ec7e60..348cc402` prints `.sdlc/handoffs/okl-memo-U1.md | 14 +++++---------`, `1 file changed`, and 62ec7e60 is an ancestor, so those runs stand. The handoff change rewords P4 and "Left out" to match revision 5 (fixture control `1`), adds no U+2014 and no brand term. B is `b747c2c8` (dfecee41's origin/main parent and `git merge-base 62ec7e60 origin/main`). Run in `git clone -q --shared` clones under /private/tmp/claude-501, no node_modules except the separate P2 build clone (a symlink, main repo `git status` and `node_modules/.vite` unchanged before and after). Host load 143 to 163 during `npm test`, 64 to 67 on 10 cores during the timing runs. Handoff and review r2 treated as claims; every number below is this seat's own run.

| # | Criterion | State | Evidence | Negative control |
|---|---|---|---|---|
| P1 | `npm test` green, no node_modules, count agrees, tree stable | 🟢 | `npm test` once in the foreground: `exit 0`, `✓ all 50 test files passed`; TESTS perl count `50` at head and `50` at B (no test file added); `git status --short \| wc -l` `0` | clone, `sed 's/"scrim/"scrimX/'` on `role-table.json` (7 lines): `node test/engine/semantic.mjs` exit `1`, `FAIL  refs-canonical`. Ran the owning test file alone, not a second `npm test`, per the run-once rule |
| P2 | `npm run build` green, tree clean after | 🟢 | separate clone at `62ec7e60`: `exit 0`, `wrote figma/plugin/ui.html 4125.2 KB`, status `0` besides the symlink; `npm test`'s own `gen:figma-ui` also left `ui.html` unchanged | same clone, closing paren removed from `okhslLAt`'s body: `exit 1`, `SyntaxError: missing ) after argument list` at `src/engine/tonal.js:929`, last step `gen:categories` |
| P3 | branding clean, no added em dash outside backticks, raw count matches handoff | 🟢 | `branding: clean (714 files scanned)`, stripped count `0`, raw count `0`; the handoff states `0`, `0` and quotes no lines, so it matches | clone, `cp decision-records.md .sdlc/verdicts/x.md`: exit `1`, `FAIL: 3 branding violation(s) across 715 files`; one added prose line carrying U+2014 through the stripped filter prints `1` |
| P4 | scope wall | 🟢 | the three commands print `0`, `0`, `0`; `.sdlc/` changes are the board and `okl-memo*` records only; `00-synthesis.md` numstat `1 1`; hct.js diff is 2 comment lines | fixture `hct.js`, `test/engine/prime.mjs`, `tonal.js` through the filter prints `1` (revision 5's figure); `const __x = 1;` appended to `hct.js` makes the middle command `1` |
| U1-1 | memo gone, one-expression body, no replacement cache | 🟢 | `0`, `0`, `1`, `  return rgbToOkhsl(hctToRgb(0, 0, lstar).rgb).l;`, `0`; `grep 'boundedCache\|new Map\|_okL'` finds only the two per-call `envelopeAt` lookup maps at `:849` and `:1113`, which are in B too | tonal.js at B prints `4`, `1`, `  const k = lstar.toFixed(2);`, `1`, and the broader needle finds `5` (3 `_okL` plus the same 2 maps) |
| U1-2 | worker keeps its old contract for `prime.mjs` | 🟢 | `node test/engine/prime.mjs` exit `0`, `0/200 palettes shifted hex by call order` (`SAMPLED: 200 determinism cases`, the gate-split form the row allows); `git diff B -- test/engine/prime.mjs \| wc -l` `0` | clone, worker's bare-array branch writes `hexes[0]+"X"` when `poison.length`: exit `1`, `1/200 palettes shifted hex by call order` |
| U1-3 | corpus byte-identical to B | 🟢 | the plan's probe: B's `tonal.js` and head both print `11340`, `cmp 0`; `src/ui/categories` has no diff against B | head with `hctToRgb(0, 0, lstar + 0.5)`: `differ: char 61, line 2`, `cmp 1` |
| U1-4 | per-call cost under 5 us, 6 call sites | 🟢 | 7 runs at load 67 on 10 cores: `1.46 1.88 1.76 1.54 1.51 1.58 1.53` us/call, median `1.54`; `grep -c 'okhslLAt('` `6`. Higher than the handoff's `0.70-0.84` at load 13 and than the "under 1us" wording in `foundations.md:139` and the tonal.js comment, which is host load; within budget | 24-step bisection over `lstarFromRgb(okhslToRgb(0, 0, mid))`: `14.18 15.54 16.03 13.97 14.80` us/call, median `14.80`, over `5.00` |
| U1-5 | `okl-order` gate in `npm test`, bites from both directions | 🟢 | `node test/engine/tonal.mjs` exit `0`, pass line count `1`, `okl-order` in test `8`, `prelstars`/`ramps` in worker `8`; own cold-process worker probe on head `0/24 shifted`; in-process pair on head `0.07999402014631897 0.0837536365535359 distinct` | old code (B's `tonal.js` restored, memo back): worker probe `24/24 shifted`; pair `0.07999402014631897 0.07999402014631897 COLLAPSED`; tonal.mjs exit `1`, one FAIL line `render-level: 24/24 ramps shifted hex ... okhslLAt(5.26499)` (needle count `1`); old code with the poisoned run's `prelstars` emptied: exit `1`, `function-level: okhslLAt(5.26499) = 0.07999402014631897, expected 0.0837536365535359`, `24/24` count `0`; head with the same edit stays exit `0` |
| U1-6 | skill lines and synthesis citation no longer claim the memo | 🟢 | `toFixed` counts `0,0,0,0`; `memoized in` `0`; `#738` in SKILL.md `1`; rubric.md:15 reads `a new cache keys on the exact float or does not exist`; foundations.md:50/53/65 read `key EXACT (#686)`; 00-synthesis.md:89 cites `tonal.js:928` (`export function okhslLAt(lstar) {`); `node test/repo/citations.mjs` exit `0`, `STALE 0 across 10 discovered docs` | files at B: `SKILL.md:1`, `best-practices.md:1`, `foundations.md:3`, `rubric.md:1`, `memoized in` `1`, `#738` `0`; 00-synthesis.md reverted to B: citations exit `1`, `00-synthesis.md:89 cites src/engine/tonal.js:926` STALE |
| U1-7 | source diff is the deletion and the comment | 🟢 | `git diff --numstat B -- src/engine/tonal.js` `3 5`, removed lines `5`; A=3 is the body plus 2 comment lines, matching the handoff | B's file re-keyed (`const k = lstar;`): numstat `1 1`, D is not `5` |
| hct | hct.js:276 comment consistent with #738 fixed | 🟢 | `:277` keeps `are pure again` about hct.js's exact-keyed caches; `:278` to `:279` now read `tonal.js kept a bucketed _okL memo ... until #738 deleted it`; comment-only diff (P4 middle `0`) | at B `grep -c 'still keeps a bucketed'` prints `1` (the sentence U1 made false); at head `0` |
| head | head moved, handoff only | 🟢 | `git diff --stat 62ec7e60..348cc402` prints `.sdlc/handoffs/okl-memo-U1.md` and `1 file changed, 5 insertions(+), 9 deletions(-)`; `git merge-base --is-ancestor 62ec7e60 348cc402` exit `0` | a code file in the delta would void the runs: any `src/`, `test/`, `scripts/` or `.claude/skills/` path in that stat would send every code row back for a rerun at 348cc402; the stat names none |

Counts: 13 🟢, 0 🟡, 0 🔴.

Cleanup: rm was denied for `/private/tmp/claude-501/okl-v` (the clone) and `/private/tmp/claude-501/okl-v-f` (scratch holding the neg and build clones); both still exist.

verdict: 🟢
sha: 348cc40252ea1fbbd0561040134d5df2deb41d66
