# Pre-land review: #713 gate-split at 1c958d6b

| Field | Value |
|---|---|
| Reviewer | gs-prepr-reviewer-l4-p1, fresh context, read only |
| Target | `plan/gate-split` @ `1c958d6bdd4a76641b2400d6bc51965d56ba26e1`, diff base `2890d67a` (`git merge-base origin/main HEAD`, which is `origin/main` itself; 40 files, +2732 / -143) |
| Records read | plan revision 14 whole, verdicts U1 to U5, U6a, U6c, U6-timing, the U6b and U6c reviews, `gate-split-review-findings.md`, adapter §1 to §3 and §5, the U6b/U6c handoffs (targeted) |
| Runs | light only, all in the worktree without editing it: `baseline-agrees-check.sh`, `branding.mjs`, `corpus-sample.mjs`, the P4/U6-2/U1-5/U6-6/U6-7/U6-3/P8/P9 commands. No heavy leg run: the host read 4 node test processes and load 6.5 to 9.2 throughout, and the unit verdicts already ran every FULL and SAMPLED leg at heads that differ from this one only by `.sdlc/` and main's own two files (below) |
| Draft PR | #743, run 35974499577 on this head |
| Verdict | FIX-FIRST. Runtime is right and every mechanical row is green; four record and comment items the plan itself routed to "the pre-land fix commit" are still open, plus one placeholder sentence in a shipped skill. One commit, `.sdlc/` + two comments + the skill, no source or test logic |

## Rows

| # | Check | State | Evidence | Control |
|---|---|---|---|---|
| 1a | One flag, one sampler, one substitution point per file | 🟢 | `tonal.mjs`, `anchor.mjs`, `prime.mjs`, `headless-boot.mjs` each read `process.argv.includes("--full")` once and import `lib/corpus-sample.mjs`; `curated-contrast.mjs` lost its private `fnv1a`/`pickVolume` (grep `fnv1a\|% vols\.length` under `test/` hits only `test/engine/lib/corpus-sample.mjs`). Substitution lines: tonal `presets = FULL ? all : sampleCorpus(byCategory)`, anchor `if (FULL) {...} else { for (const doc of sampleCorpus(byCategory)) ... }`, headless-boot `corpusDocs = FULL ? Object.values(byCategory).flat() : sampleCorpus(byCategory)`, prime `DET_CASE_COUNT = FULL ? 2000 : 200`, `POISON_COUNT = FULL ? 1500 : 500`, `hueRange()` for both grids. Every downstream sweep and in-file control reads that one list (`docs`/`dipDocs`, `corpus`/`presetsByCat`/`controlSubjects`/`hueSpaceBoundSubjects` at `anchor.mjs:1217`, `resetEntries`) | P3 (b) and (c) per unit verdict: flag forced false prints no FULL line; FULL forced onto the sample reds the vacuity FAIL (U2, U3, U4, U5 verdicts, each with `git diff --stat` 1/1) |
| 1b | `npm test` is SAMPLED, the gate scripts are FULL, `gate:sweeps` stops at the first red | 🟢 | `test/run.mjs:25` `execFileSync("node", [join(HERE, t)])` passes no argv; U1-5's probe prints four `ok` and five `ok`; `gate:sweeps` is an `&&` chain of the five | U1 verdict neg3: one command changed prints `WRONG`, one leg dropped prints `MISSING` |
| 1c | CI: the `sweeps` matrix runs every FULL leg on every PR and nothing masks a red | 🟢 | P4 at this head: `corpus-tonal 1`, `corpus-anchor 1`, `sweep-prime 1`, `corpus-reset 1`, contrast `2`, one `sweeps:` job, `0` `if:`/`continue-on-error:`/`needs:`, `fail-fast: false` `1`, `pull_request:` `1`. U6-2 prints `same 5`. Run 35974499577 on `1c958d6b`: `sweeps (gate:corpus-tonal)` success 192 s, `(gate:corpus-anchor)` success 170 s, `(gate:sweep-prime)` success 109 s, `(gate:corpus-reset)` success 127 s, `(gate:corpus-contrast)` success 67 s, `corpus-contrast` success, `panda-smoke` success; `build-test` still running at 08:22Z (see row 5c) | U6a verdict: matrix entry commented out prints `0`; `continue-on-error: true` under `sweeps:` prints `1` |
| 1d | Each FULL leg blocks a merge somewhere | 🟢 procedural | `main` has no branch protection (adapter §2, U5 row 13), so the block is procedural and it is written in three places that agree: adapter §2 "CI on a PR" (four jobs, `sweeps` every leg's own conclusion), `shipping-changes` step 6 (every job incl. `sweeps`) and step 7 (gate on `gh run view --json conclusion`), `.claude/CLAUDE.md:99` (four job names). The plan's Landing section adds that the pre-land verdict names every `sweeps` leg with its conclusion; row 1c does that for this head | U6-6 at this head: gate rows `1 1 1 1`, ceiling `1`, quiet-host block `1`, interim needle `0`; U6-7: `1 5 0 0 0 1` |
| 1e | FULL vacuity floors | 🟢 with one note | tonal and anchor FAIL on FULL under 343 documents or 3780 palettes; prime FAILs on FULL under 2000 cases (literal, not read off `DET_CASE_COUNT`); headless-boot's FULL floor is `corpusChecked > 3000` anchored palettes, no document count. That is what plan U5 step 3 ruled ("`corpusChecked > 3000` becomes the FULL floor"), and P3 (c) bites on it (U5 row 8: `316 palettes, want > 3000`), so it is design, not a gap. The mode line in all four files is computed from what ran | U5 verdict row 8 |
| 2a | Dead code, leftover probes | 🟢 | `listOk` in `anchor.mjs` is gone (grep 0); the old `corpusPresets` loop in headless-boot is replaced, not shadowed; no `Date.now`/`hrtime`/`console.error` timestamp probe, `TODO` or `debugger` on any added test line; `curated-contrast.mjs`'s "35 documents x 3 tone modes = 2352 cells" comment is still true (the sorted pick still yields 35) | the U1 verdict's `neg2` control shows the grep bites when a second picker exists |
| 2b | Stale comments the diff invalidates | 🟡 fix-first | (i) `.github/workflows/ci.yml:105-110`: "gate:corpus-anchor needs test/engine/anchor.mjs, which arrives with #681, so that leg runs red until it lands; gate:corpus-tonal, gate:sweep-prime and gate:corpus-reset only read --full once U2, U4 and U5 teach those files to, so until then they run their existing, unsplit suite instead". #681 is on main and U2 to U5 are merged; every leg is green on this head. The sentence now describes a state that no longer exists in the file that ships. (ii) `test/engine/prime.mjs:345` ("= 151,200 rungs") and `:357-359` ("measured, this commit, three runs: 114/151,200 every time", "742/302,400") still describe the FULL leg as the file with no FULL clause; SAMPLED prints `3/30240`. This is finding C1, which plan revision 7 (a) routes to "the pre-land fix commit". (iii) `anchor.mjs`: `allowListOk` is called at `:414`/`:417` and declared at `:969`, hoisting-valid; C5 nit, leave or move | (i) `grep -n '#681\|runs red until\|unsplit' .github/workflows/ci.yml` prints lines 107 and 109; after the fix it prints nothing. (ii) `grep -n '151,200 every time' test/engine/prime.mjs` prints `357`; a comment that names FULL and the SAMPLED denominator changes the line |
| 3a | `.sdlc/baseline.md` and `.sdlc/adapter.md` agree | 🟢 | `sh .sdlc/checks/baseline-agrees-check.sh` at `1c958d6b`: `tests 50/50 ok`, `ui.html 4119.1 ok`, every `time` line `ok` incl. the four `time gate:` lines, `note  head:` and `STALE head: baseline ref c87d98fc is in origin/main's history`, `stale total: 1`. The STALE is P7's documented branch-state and U6-9's close-out (adapter §5 step 5 is present: `sets .ref. to the squash sha` count `1`). The `note` is new and real: `git diff --stat c87d98fc HEAD -- . ':!.sdlc'` lists `docs/reference/references/decision-records.md` (+49) and `scripts/gen-adia-derived-exports.mjs` (+5/-2), both main's own commits brought in by the `2890d67a` merge. The second is a generator `npm test` runs, so the 80 to 89 s set was taken on a tree one generator edit away from this head. Not a blocker: the close-out re-runs the gates of record and re-points `ref` (U6-9), and P2's independent half is the Conductor's verifier's own `npm test` on this head | U6-timing verdict row 13: one adapter range moved by a second prints `STALE time gate:` |
| 3b | `shipping-changes` SKILL.md | 🟡 fix-first | Step 6 reads `Watch CI (~figure pinned at pre-land, once U6-8/P10 measures a completed sweeps matrix run; not yet measured on plan/gate-split)`. That is a placeholder left for exactly this moment, and it ships as prose in a skill every later plan lands through. The figure now exists (row 5c): pin it. The rest is right: four jobs named, a red `sweeps` leg is a red run, `npm run gate:sweeps` named as the local command, `all three legs` and `Three legs must pass` gone | U6-7 needles `1 5 0 0 0 1`; on `origin/main` the same six print `0 1 1 1 1 1` (U6b review 2c) |
| 3c | `.claude/CLAUDE.md` | 🟡 defer to owner | Line 99 names the four CI jobs (the one line Q2 allowed). Line 95 still says `npm test` (no `node_modules`, ~60 s, tree clean after)`; the figure of record is now 80 to 89 s. Adapter X9 needs the owner's yes for a second line there, and the U6b review (M2) already made the builder revert a second-line edit. Not fixable inside this plan without an owner ruling; it is one stale number in a summary line, and adapter §1 (the source it points at) is right | `grep -n '~60' .claude/CLAUDE.md` prints line 95 |
| 3d | README, architecture doc citations, debt | 🟢 | `README.md` makes no CI-job or `npm test`-time claim (line 99 only says what `npm test` does). `.sdlc/architecture.md` K17 cell widened by `engine/lib/corpus-sample.mjs` (the one line U1 was allowed); `.sdlc/debt.md` K17 row updated and names the five helper files that have drifted since (#681, #717), owned by the next unit that touches `architecture.md`. The `test/run.mjs:12-21` citation is stale by two lines but was already so on `origin/main` (TESTS is 12-19 there too), not this plan's | `test/repo/citations.mjs` is in TESTS and `npm test` is green at every unit head |
| 4a | Commit hygiene | 🟢 | 317 commits on the branch, squash-merged as one (`shipping-changes`). `git log origin/main..HEAD --name-only -- node_modules .claude/docs/other` prints nothing. The U6b merge that duplicated 48 lines of `src/ui/sections/color.js` (U6b review H1) is fixed: `git diff --name-only 2890d67a -- src` prints `0` files. P9 with `Q2=yes` prints `0`, `0` | U6b review pass 2: `git diff --quiet 04f95ff0 75a3b876` clean on `color.js`, both docs and `ui.html` |
| 4b | Secrets, config smells, dependencies | 🟢 | `package.json` diff is five `scripts` lines only; `package-lock.json` untouched; no `devDependencies` change; zero runtime deps intact. No token, key or password pattern on any added line. `ci.yml` adds one job with the same action pins (`checkout@v7`, `setup-node@v7`, node 24) and no secret reference | `git diff origin/main...HEAD -- package.json package-lock.json \| grep -E '^[+-] *"' \| grep -v '"gate:'` prints nothing |
| 5a | U6-5, `STALE head` 1 | 🟢 correctly deferred | The baseline `ref` is `c87d98fc`, a branch-only sha that the squash drops; adapter §5 step 5 (U6-9) makes the close-out commit re-run the gates of record and set `ref` to the squash sha. Every other line of the check is `ok` (row 3a) | on a clone of main with `ref` at a pre-squash value the check prints `note  head:` only (U6b review 2d) |
| 5b | U6-7, the CI watch-time figure | 🔴 fix-first | See 3b: the sentence is a placeholder, and the completed run this head needs is the one that pins it. Wall figure in row 5c | |
| 5c | U6-8 / P10, a completed `sweeps` run on CI | 🟡 pending at write time, see the addendum | Run 35974499577 on `1c958d6b`: every `sweeps` leg `success`, the longest leg `gate:corpus-tonal` at 192 s (ceiling 240), 5 legs (expected `4` in the plan's P10 jq is the count of legs whose name starts with `sweeps`, which is 5 here because the matrix carries `gate:corpus-contrast` as U6-2 requires; the plan's own `4` predates U6-2's `same 5` and the U6a verdict records the fifth leg as intended). `build-test` was still running at 08:22Z; wall and conclusion are in the addendum below | run 35517479022 (pre-plan) prints a blank third line and `0` legs, per the planner's control |
| 5d | U5's branding-at-base note (C7) | 🟢 nit, fix with the rest | `.sdlc/handoffs/gate-split-U5.md:53` cites `branding: clean (514 files scanned)` at base `ebddc55d`; the unit head read 515 and this head reads 693. Citation fidelity only; re-quote in the fix commit since it is open anyway | `node test/repo/branding.mjs \| tail -1` at `1c958d6b` prints `branding: clean (693 files scanned)` |
| 5e | C6, the U2 handoff quote | 🟡 fix-first | `.sdlc/handoffs/gate-split-U2.md:220` still quotes `Trulli of Alberobello ...\|primary\|500` as literal program output with no `altered:` marker; the program prints `Trulli of Alberobello · vernacular · Puglia, Italy\|primary\|500` (U2 verdict U2-4 (a) has the full line). Plan revision 10 routes this to the pre-land fix commit | `grep -c 'altered:' .sdlc/handoffs/gate-split-U2.md` prints `0` |
| 6a | Em dash on added lines outside backtick spans | 🟢 | P8 at base `2890d67a`: `0` over the whole diff, `0` over the non-`.sdlc` half | the planner's fixture and every unit verdict's one-line plant print `1` |
| 6b | Bold inline labels | 🟡 low | Added lines in `.sdlc/handoffs/gate-split-U3.md` (three list items, "Corrections" section) and `gate-split-U4.md` (five, incl. `**This branch's N for U4-1 is 21, not the plan's 20**`) carry bold inline labels. The plan's prose rule covers every added line; the U6b review swept the adapter (L1) but the U2 to U5 handoffs were never swept. Records, not code; unbold in the fix commit or leave as history with a line saying so | `git diff 2890d67a -- '*.md' \| grep '^+' \| grep -cE '\*\*[^*]+\*\*'` prints 8; after the sweep `0` |
| 6c | Retired maker brand paraphrased in `.sdlc/` | 🟢 | `branding: clean (693 files scanned)`, exit 0 | U6c verdict row 15: `decision-records.md` copied into `.sdlc/verdicts/x.md` reds it with 3 violations |

## Findings by severity

| Sev | File and symbol | Finding | Fix |
|---|---|---|---|
| High (record) | `.claude/skills/shipping-changes/SKILL.md`, step 6 `Watch CI (~figure pinned at pre-land ...)` | A placeholder sentence in a shipped skill; U6-7 asks for the measured CI wall, rounded | Replace with the wall from run 35974499577 (addendum), e.g. `Watch CI (~N s, build-test is the wall; the sweeps legs run to about 190 s in parallel)` |
| Medium | `test/engine/prime.mjs:345`, `:357-359`, the `151,200` and `114/151,200 every time` sentences | C1, plan revision 7 (a): FULL coverage stated as the file's, SAMPLED prints `3/30240` | Qualify both as FULL and name the SAMPLED denominator (30240 at hue step 5) |
| Medium | `.github/workflows/ci.yml:105-110`, the `sweeps` job comment | Describes #681 as unlanded and U2/U4/U5 as unmerged; every leg is green on this head | Cut the sequencing sentence, keep the shape rationale (own runners, `fail-fast: false`, no `npm ci`, no `if:`/`continue-on-error:`) |
| Medium | `.sdlc/handoffs/gate-split-U2.md:220` | C6, plan revision 10: a FAIL line quoted with the preset name shortened and no `altered:` marker | Restore the full name or mark it `altered:` |
| Low | `.sdlc/handoffs/gate-split-U5.md:53` | C7: branding cited at the base, not the unit head | Re-quote at this head (693) or mark the base |
| Low | `.sdlc/handoffs/gate-split-U3.md`, `gate-split-U4.md` | Eight added lines with bold inline labels | Unbold |
| Low, owner | `.claude/CLAUDE.md:95` `~60 s` | Stale against 80 to 89 s; X9 needs the owner's yes for the line | Ask with the landing message; not this plan's to edit |
| Nit | `test/engine/anchor.mjs:414` / `:969` | C5, `allowListOk` used before its declaration | Leave (hoisting) or move |
| Nit | `test/engine/anchor.mjs`, `seenCats` | C4, the presence assertion skips `brands`, which the 30-document floor and `controlSubjects >= 46` catch only indirectly | Later, with #701 or the next anchor unit |

Deferred correctly, no action in this PR: U6-5's `STALE head` (U6-9 close-out re-points `ref`), the `note  head:` on the two main-side files (same close-out re-run), `.sdlc/debt.md`'s K17 drift list (next `architecture.md` unit), the `symmetry` anchored-corpus leg in prime (Not in scope, owner).

What the fix commit is: five record/comment edits (skill step 6, `prime.mjs` comments, `ci.yml` comment, two handoffs) and the optional unbold. No source, no test logic, no `package.json`, so no FULL leg re-runs; `npm test` once for the tree-clean check (the `ci.yml` and `.mjs` comment edits regenerate nothing) and the P8/P9 greps.

## Addendum: run 35974499577 final reading

Read at 08:24:55Z. P10's jq: `success`, `265`, `192`, `5`. Wall 265 s (budget 300), longest `sweeps` leg 192 s (budget 240), `build-test` 262 s is the wall as the plan projected. Legs by name with conclusion: `build-test` success, `panda-smoke` success, `corpus-contrast` success, `sweeps (gate:corpus-tonal)` success, `sweeps (gate:corpus-anchor)` success, `sweeps (gate:sweep-prime)` success, `sweeps (gate:corpus-reset)` success, `sweeps (gate:corpus-contrast)` success, `deploy` skipped (push-only). Row 5c is 🟢 on this reading; the plan's P10 expected `4` legs is stale text against U6-2's `same 5`, worth one revision line. The figure for `shipping-changes` step 6 is about 265 s wall on a PR (`build-test` about 260 s, the `sweeps` legs 70 to 190 s in parallel).
verdict: 🟡 FIX-FIRST
