# Pre-PR · okl-memo · pass 2, verifier leg

verdict: 🟢
sha: 9d5f6dc383342ef41458d507e53da27ead19dd76

`plan/okl-memo` at `9d5f6dc3` (draft PR #749, ticket #738), graded against `.sdlc/plans/okl-memo.md` revision 7 on that branch. Fresh detached clone at `/private/tmp/claude-501/okl-prepr-v2`, `git rev-parse HEAD` = `9d5f6dc383342ef41458d507e53da27ead19dd76`. `B` is the merge base against `main` fetched from GitHub directly (`git fetch https://github.com/kimgranlund/ultimate-tokens.git main:refs/remotes/github/main`; `git merge-base github/main HEAD`) = `62e48e91159f6626f860a19e27a33729e9af6e8b`, the GitHub `main` tip. A first derivation used the local root checkout's `origin/main`, which was two commits stale at `b747c2c8` (`git diff --stat b747c2c8 62e48e91`: the two Conductor handoff files only, 19 insertions, no code); that reading is retracted, every base-dependent row below was rerun at `62e48e91`, and the rows whose base clone `$F/base` was cut at `b747c2c8` (U1-1, U1-6 controls) are unaffected because neither commit touches those files. Every control ran in `$F/neg`, a `--shared` clone of that scratch clone at the unit head (`git -C $F/neg log -1 --format=%h` = `9d5f6dc3`), with the file reset by `git checkout --` after each control; `$F` = `/Users/kimba/.claude/jobs/ef9dc581/tmp/okl-v2`. The host was loaded throughout (1-minute load 12 to 61, 3 hot processes at the start of `npm test`), so no timing figure below counts under adapter §1's quiet-host rule; each is reported with its load.

No red. The pass 1 red (baseline ui.html figure) is closed: `baseline-agrees-check.sh` prints `stale total: 0`. One yellow, U1-2: the criterion holds, but the plan's own control cell is vacuous at this head and was replaced by the U1 review's control, which the plan text never folded in (a stale plan cell, no defect in the unit).

Smoke is not owed: no hand edit under `src/ui/` (`src/ui/describe-mcp-assets.js` is a generated `*-assets.js` file and `figma/plugin/ui.html` is a build product; both byte-stable after `npm test` and `npm run build`).

| # | Criterion | State | Evidence | Negative control |
|---|---|---|---|---|
| P1 | `npm test` green with no `node_modules`, N agrees, tree byte-stable | 🟢 | scratch clone, no `node_modules` present: exit `0`, last line `✓ all 50 test files passed`, `TESTS` length `50`, `git status --short \| wc -l` = `0`; wall 341 s at load 28.24 before / 61.40 after (not a quiet-host run; adapter §1 says it is recorded and not counted against the 120 s ceiling) | `$F/neg`, `sed -i '' 's/"scrim/"scrimX/' docs/reference/data/role-table.json`: `npm test` exit `1`, `✗ 1/50 test file(s) failed`, the one file `engine/semantic.mjs`, the one gate `FAIL  refs-canonical`, `grep -c FAIL` = `3` (268 s at load 12.64 / 30.87) |
| P2 | `npm run build` green, tree clean after | 🟢 | scratch clone after `npm ci` (exit `0`): `npm run build` exit `0`, last line `wrote figma/plugin/ui.html 4125.3 KB`, status `0` lines; the same `4125.3 KB` line is what `npm test`'s `gen:figma-ui` step printed | `$F/neg` with `node_modules` symlinked, closing parenthesis removed from the new body: exit `1` at the `gen:categories` step, `SyntaxError: missing ) after argument list` naming `.../neg/src/engine/tonal.js:930` (`tsc` never reached) |
| P3 | branding clean, no added em dash outside backticks, raw count matches the handoff | 🟢 | `branding: clean (716 files scanned)`; added-line count outside backticks `0`; raw count excluding handoffs `0`, and the handoff's P3 row states `0`, `0` (no quoted lines owed) | `cp docs/reference/references/decision-records.md .sdlc/verdicts/x.md`: `FAIL: 3 branding violation(s) across 717 files`, exit `1`; one prose line with U+2014 appended to the plan moves the second count to `1` |
| P4 | scope wall | 🟢 | at `B` = `62e48e91`: `0`, `0`, `0`. `git diff --name-only $B` lists 20 paths, all inside the wall; the only `hct.js` hunk is the two-line `// covers hct.js only:` comment; `prime.mjs`, `test/run.mjs`, `scripts`, `.github`, `package.json` untouched. (Against the stale local `origin/main` at `b747c2c8` the first command printed `2`, the two Conductor handoffs that are ancestors of the real `main`; retracted above) | the plan's three-name fixture through the first filter prints `1`; `const __neg = 1;` appended to `hct.js` moves the middle command to `1` |
| U1-1 | memo gone, function is one expression, comment no longer says memoized | 🟢 | `grep -c -e '_okL' -e 'toFixed(2)'` = `0`, `new Map()` = `0`, the declaration `1`, the line after it `  return rgbToOkhsl(hctToRgb(0, 0, lstar).rgb).l;`, `memoized` = `0` | `git show $B:src/engine/tonal.js`: `4`, `1`, `1`, `  const k = lstar.toFixed(2);`, `1` |
| U1-2 | the worker keeps its old contract for `prime.mjs` | 🟡 | `node test/engine/prime.mjs` exit `0` (92 s at load 34), the determinism line in its post-gate-split sampled form `determinism (cold process vs poisoned-before-any-render process, real-palette poison, hex comparison): 0/200 palettes shifted hex by call order` with `(SAMPLED: 200 determinism cases, 500 poison renders, hue step 5)`, which the row accepts; the exact `0/2000` needle count is `0` for that reason; `git diff $B -- test/engine/prime.mjs \| wc -l` = `0`; CI `sweeps (gate:sweep-prime)` (the FULL leg) `success` at this head. Yellow because the plan's own control cell does not bite (next column). The replacement control is a documented exception: the U1 review (`.sdlc/verdicts/okl-memo-U1-review.md`, finding 1, medium) named the plan's control vacuous and prescribed the substitute, the U1 handoff row for U1-2 and the U1 verdict (`.sdlc/verdicts/okl-memo-U1.md`) both run it and cite `1/200`; only the plan text (revisions 4 to 7) never folded it in. A stale plan cell, not a defect in the unit; no landing block | plan's control as written (`process.stdout.write(JSON.stringify({ hexes }))` unconditionally): `prime.mjs` exit `0`, `0/200`, since `runDeterminismWorker` returns `JSON.parse(out)` and `detClean[i] !== detPoisoned[i]` compares `undefined` to `undefined`; the U1 review's replacement control (bare-array branch corrupts hex 0 when `poison.length`): exit `1`, `1/200 palettes shifted hex by call order` |
| U1-3 | behaviour-neutral on the corpus | 🟢 | probe at `$B` (`$F/base` re-cut at `62e48e91`, `git rev-parse --short HEAD` = `62e48e91`; the first run at `b747c2c8` gave the same bytes) and at the head: first line `11340` both times, `cmp $F/before.txt $F/after.txt` prints nothing, `cmp 0` | `$F/neg`, `hctToRgb(0, 0, lstar + 0.5)` in `okhslLAt`: `differ: char 61, line 2`, `cmp 1` |
| U1-4 | cost budget: uncached call under 5 µs, at most three calls per render | 🟢 | three runs `3.80 us/call`, `3.72 us/call`, `1.72 us/call` at load 33.94 (1-minute) / 61.58 (5-minute), each under `5.00`; `grep -c 'okhslLAt(' src/engine/tonal.js` = `6`. The figures sit above the planner's 0.40 to 0.90 because of the host, as the plan's risk row anticipates; the body is the one-line expression (U1-1) | `$F/neg`, body replaced by a 24-step bisection over `lstarFromRgb(okhslToRgb(0, 0, mid))`: `9.55 us/call`, `9.67 us/call` at load 15.36, over the budget |
| U1-5 | `okl-order` gate in `npm test`, bites both directions | 🟢 | `node test/engine/tonal.mjs` exit `0`, pass line `okl-order: okhslLAt is a function of its argument; 0/24 ramps shifted hex by call order` count `1`, `pass  okl-order` in the summary; `grep -c 'okl-order' test/engine/tonal.mjs` = `8`; `grep -c -e prelstars -e ramps` on the worker = `8`; `engine/tonal.mjs` is one of the 50 files `npm test` ran | `$F/neg`, memo restored verbatim (`grep -c _okL` = `3`): exit `1`, one `FAIL  okl-order` line, `render-level: 24/24 ramps shifted hex by call order after a prior okhslLAt(5.26499)`, the `5.26499` and `24/24` needle grep = `1`; second control, memo restored and `runOklOrderWorker([L2])` changed to `runOklOrderWorker([])`: exit `1`, `function-level: okhslLAt(5.26499) = 0.07999402014631897, expected 0.0837536365535359`, `5.26499` count `1`, `24/24` count `0` |
| U1-6 | the seven skill lines are true of the tree | 🟢 | `grep -c toFixed` on `SKILL.md`, `best-practices.md`, `foundations.md`, `rubric.md` = `0` each; `memoized in` in `foundations.md` = `0`; `#738` in `SKILL.md` = `1` | the same four files at `$B` via `git show`: `1`, `1`, `3`, `1`, then `memoized in` = `1`, `#738` = `0` |
| U1-7 | source diff is the deletion plus the comment | 🟢 | `git diff --numstat $B -- src/engine/tonal.js` = `4 5` (A = 4: one body line plus a three-line comment, within the row's 1 to 4; D = 5: the `_okL` line and the four body lines); `grep -c '^-[^-]'` = `5` | `$F/neg`, memo restored and re-keyed (`const k = lstar;`): numstat `4 1`, D is not `5` so the row reds (the plan quotes `1 1` for a head without the comment; this head keeps its three comment lines, so A is `4`) |
| S1 | `baseline-agrees-check.sh` stale total (the pass 1 red) | 🟢 | `sh .sdlc/checks/baseline-agrees-check.sh` last line `stale total: 0`; `.sdlc/baseline.md:26` carries `wrote figma/plugin/ui.html 4125.3 KB` and the 2026-09-24 #738 correction paragraph; the committed `ui.html` measures `4125.3 KB` in both chains above | pass 1 at `59a55d91` read `STALE ui.html: baseline 4125.1 recorded, 4125.3 KB in the tree` (the record this pass supersedes); the check exits non-zero when the figure and the tree disagree |
| S2 | the other `.sdlc/checks/*` scripts, no regression against `$B` | 🟢 | at the head: `card-amendment-check.sh` `stale total: 0`, `card-source-range-check.sh` `range mismatches: 0`, `doc-drift-rows-check.sh` `rows 56 drifted 11 holds 45 undetermined 0 bad 1`, `verdict-frontmatter-check.sh` `verdicts 138 graded 138 bad 10`, `ceiling-counts-check.mjs` `ceiling-counts: 1 failure(s)`; the three non-clean figures are identical at `$B` (`$F/base`: `bad 1`, `bad 10` of `135`, `1 failure(s)`), so none is this plan's | the branding control above moves `verdict-frontmatter-check.sh`'s input set by one file (`x.md`), and P4's fixture control shows the wall filter itself discriminates |
| S3 | CI at this exact head | 🟢 | `gh api .../commits/9d5f6dc383342ef41458d507e53da27ead19dd76/check-runs`: `build-test` `success`, `panda-smoke` `success`, `corpus-contrast` `success`, `sweeps` all five legs (`gate:corpus-anchor`, `gate:corpus-tonal`, `gate:corpus-reset`, `gate:sweep-prime`, `gate:corpus-contrast`) `success`; `deploy` `skipped`. First read at 11:5x showed `build-test` and three `sweeps` legs `in_progress`; the second read, after the local gates, showed all completed | the same query at the pass 1 head is a different commit's run list; a query with a wrong sha returns no rows for this branch's run `36043185818` |

Notes for the Orchestrator, outside the table:

- The timing wording revision 7 owed is in: `00-synthesis.md:89` and `test/engine/tonal.mjs:1985` both read `0.40-0.90 us per uncached call on a quiet host, median 1.54 us at load 67`; `grep -n 'well under\|under 1'` over those two files, `tonal.js` and `foundations.md` prints nothing.
- The P2 control left `src/ui/describe-mcp-assets.js` modified in `$F/neg` (a half-run `gen:mcp-assets` against the broken `tonal.js`); with the source restored, `npm run gen:mcp-assets` exits `0` and the tree is clean, so the file is byte-stable under the real chains.
- Scratch clone left in place at `/private/tmp/claude-501/okl-prepr-v2` (with its `node_modules`); the control clones are under `$F` in this job's tmp directory.
