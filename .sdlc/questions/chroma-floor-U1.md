# Question U1 · from cf-U1-builder-l4

| Field | Value |
|---|---|
| Blocks | U1 P1 (C1 `npm test` green, C8 `pass role-contrast`) |
| Question | The even-mode neighbourhood shoulder (`EVEN_NEIGHBOURHOOD_R = 0.2`, `src/engine/tonal.js`'s `chromaEnvelope`) is the smallest radius the plan's own feasibility probe found that fully closes both lone-spike populations (64 corpus + 1 default kit, both now a true 0 — C2 is green, `gate:corpus-anchor` FULL exit 0). Its unavoidable side effect: it raises the default kit's Warning family's even LIGHT accent (stop 550) chroma enough to move its contrast ratio from the pinned floor's own measured value (9.9148:1, `test/engine/semantic.mjs`'s `FLOORS.even`) down to 9.88:1 — 0.02 under the pinned 9.9 floor, still 5.4:1 above the ruled AA floor (4.5:1). C8's own text anticipates this exact mechanism ("the even accents ARE stops 550 and 450, the two stops a shoulder moves most, so the default kit's even ratios will move") and rules that a downward move is "left OUT of the branch's table [FLOORS] and instead named in the handoff and handed to #662's policy" — but C8 also separately expects the literal print `pass role-contrast`, which this movement reds (`FAIL role-contrast — even Warning LIGHT: accent #603B08 on #FFFFFF = 9.88:1, below its pinned floor 9.9:1`). I have not touched `FLOORS` (no second workaround: lowering the pinned number to re-admit 9.88 would silently hide the erosion, exactly what C8's own comment forbids). This is the one remaining `npm test` failure (1/50 files); everything else measured green (C2, C5, C6, C7, C9, C10; C1 and C8 are the two this blocks). |
| Options | A leave `FLOORS["even"]["Warning"]` untouched, let `role-contrast` red, and land U1 with this ONE named, documented exception (recommended: matches C8's own "hand to #662's policy" instruction literally; #662 owns on-color/floor policy, not this plan) · B re-pin `FLOORS["even"]["Warning"]`'s light floor down to the new measured value (9.88, still far above AA) so `npm test` reads clean, and name the downward move in the handoff instead (a policy call C8's own text discourages, since "any downward move" is supposed to stay OUT of the table) · C hold U1 pending a different, larger-radius-avoiding construction that does not touch Warning's light accent at all (not attempted: the plan's own probe already establishes 0.2 as the minimum radius that closes 0/0 spikes; no smaller radius was found to work, and re-deriving a fourth construction is a second workaround the dispatch explicitly rules out) |
| Default if unanswered | A |

Full measured picture, everything else green:
- C2 (lone spikes): `gate:corpus-anchor --full` exit 0, `anchor-ramp lone-spike ...: 0 (expected 0, corpus 3380 anchored + default kit 16, no allow-list)`.
- C5 (envelope cells + rendered-path report): `--gate-path` reads all four OK, `above 100% of stop 500: 0 OK`; the rendered default run is byte-identical to `<base>` (15.6/37.0, 48.4/113.7, 42.5/80.2, 22.9/52.0, above 100% 670); `--gate-path --damp-amp 55` control reads 1916 FAIL (bites).
- C6 (mode isolation): new `gate:mode-isolation` script + fixture, perceptual/peak fingerprints identical with and without the shoulder patch (`34e544942d500b9e` / `f560f784d8a4883a`), captured at `<base>` 282fca8d.
- C7 (ramp shape / one envelope): `anchor-ramp monotone: 0`, `distinct: 16`, `skew-lift-okhsl`/`chroma-envelope` pass, `chromaEnvelope(` greps 1/5 unchanged (naming: the neighbourhood term uses `t`/`plateau`, never `shoulder`, which stays the `dampAmp` term's own name).
- C8: `chroma-floor` and `role-contrast Q-B floor gate` both pass; `FLOORS` comparator on the branch against `<base>` reads `changed 0, down 0, exit 0` (nothing edited); the ONE live-measurement red is this question's own subject.
- C9 (Q-D): unaffected, even live dE 0.0486 > 0.01, perceptual/peak bound lines unchanged.
- C10: regenerated assets clean; `docs/` diff is 4 paths beyond the named six, all one-line citation-number repairs made necessary by this unit's own line-number shift in `tonal.js`/`test/engine/tonal.mjs` (`SKILL.md:95`, `acceptance-criteria.md:25`, two lines in the dated `2026-08-20-reactivity` review docs) — same repair class already ratified for U1 of plan `okl-memo` (`.sdlc/questions/okl-memo-U1.md`, answer A) for the identical citation (`00-synthesis.md:89`, `_okL`/`okhslLAt`). Not re-asking that half; naming it here so C10's grading knows why a seventh-plus path appears and that it is not new judgment.

Everything is committed to the worktree but not yet committed to git or pushed, pending this answer (A changes nothing further; B needs one line in `test/engine/semantic.mjs`; C is a stop).
