---
kind: verdict
plan: achromatic-anchor
seat: verifier
pass: 2
pr: 746
ticket: "#739"
written: 2026-09-24
---

verdict: 🟢
sha: 52e8981b083581a0e8cabb7d2f17dde6a674fc44

## Pass 1, at `bf1f4e35`

Pre-land verifier, fresh context, read-only. Plan `.sdlc/plans/achromatic-anchor.md` revision 4 at the head, owner rulings applied: Q1 (a) the palette's stored hue, Q2 OKLab C under `0.002`, Q3 #739 before #701; Nike rendering mauve at the fallback hue 0 is #744 and carried 🟡. Every row below is my own run at `bf1f4e3582fe6b2a2093729b34a62ec66d3a1f99`, not a copy of the unit verdict (`.sdlc/verdicts/achromatic-anchor-U1.md`, 13 of 13 🟢 at `e409a061`); where a figure matches that verdict it is because the re-run printed it. Clones, all `git clone -q --shared` under `/private/tmp/claude-501/aapl-bf1f`, none with `node_modules` except `build`: `head` (the plan head, `npm test` ran there once, foreground), `base` (`d50241ae`, the `origin/main` the head merged and the three-dot diff base), `neg`, `neg2`, `neg3`, `neg4` (each edit reverted, tree `0` after), `build` (the repo's `node_modules` symlinked in, P2 only). The probe `hue.mjs` was written verbatim from the plan text. `B` below is `d50241ae9303d00798e2ba3cf44cd10bc745afab`.

The one red is not in the unit's work. While this pass ran, #713 landed on `origin/main` as `a62ec020` (`test: split the corpus sweeps out of npm test (#713) (#743)`), and the plan head no longer merges: `git merge-tree --write-tree origin/main bf1f4e35` exits `1` with content conflicts in `.sdlc/baseline.md` and `test/engine/anchor.mjs`, and PR #746 reads `mergeable=false state=dirty`. At the `origin/main` the head was built on (`d50241ae`) the same command is clean. The Orchestrator merges `a62ec020` into `plan/achromatic-anchor`, resolves the two files, and requests pass 2 at the new head; P1 must be re-run there because #713 changes `test/run.mjs` and the `TESTS` count (`49` here, `50` on the new main).

| # | Criterion | State | Evidence | Negative control |
|---|---|---|---|---|
| P1 | `npm test` green, no `node_modules`, count agrees, tree byte-stable | 🟢 | clone `head`: `exit 0`, `✓ all 49 test files passed`; `TESTS` length `49`; `git status --short \| wc -l` prints `0`; the run's `wrote figma/plugin/ui.html 4125.1 KB` matches `.sdlc/baseline.md` at the head | clone `neg3`: `sed 's/"scrim/"scrimX/'` on `docs/reference/data/role-table.json`, then `npm test` prints `P1 control exit 1`, `engine/semantic.mjs FAIL`, `refs-canonical ... ordered key set != canonical` |
| P2 | `npm run build` green, tree clean after | 🟢 | clone `build`: `build exit 0`, last line `wrote figma/plugin/ui.html 4125.1 KB`, `git status --short \| wc -l` prints `0` | clone `neg4` (no `node_modules`): unedited build `exit 127`, grep `0`; with `export` removed from `rgbToOkhsl`, build `exit 1` and `grep -c "does not provide an export named 'rgbToOkhsl'"` prints `1` |
| P3 | branding clean, no added em dash outside backticks, raw count `0`, no private docs, no `node_modules` | 🟢 | `branding: clean (676 files scanned)`, exit `0`; backtick-stripped count `0`; raw count excluding handoffs `0`; raw count including handoffs `0`; `git diff --name-only B \| grep -c -e '^node_modules/' -e '^\.claude/docs/other/'` prints `0` | clone `neg3`: `cp decision-records.md .sdlc/verdicts/x.md` gives `FAIL: 3 branding violation(s) across 677 files`, exit `1`; one prose em-dash line appended to the handoff and one backticked em-dash line in `tonal.js` make the second count `1` and the third count `1` |
| P4 | scope wall | 🟢 | first filter prints `0`, second command prints `0`; `17` files changed against `B`, every one inside the revision-3 wall (`.sdlc/baseline.md` and `00-synthesis.md` admitted there) | the three-name fixture (`src/engine/prime.mjs`, `test/run.mjs`, `src/engine/tonal.js`) through the first filter prints `2`; an appended `// planted` line on `src/ui/app.js` in clone `neg3` makes the second command print `1` |
| U1-1 | `#808080` at palette hue 250 renders the palette hue in all three modes, stop 500 byte-exact | 🟢 | `node hue.mjs "#808080" 250 50` at head: `perceptual #808080 d300 5 d700 3`, `peak #808080 d300 0 d700 4`, `even #808080 d300 2 d700 2`, every distance at most `5`; the ends' own OKLCH hue reads `248.1` to `251.9` (`#ACB9C6 248.1`, `#444F5B 250.7`, `#BAC5D1 250.4`, `#3A444E 248.4`, `#B8C5D4 251.9`, `#35414D 248.5`), within 2 degrees of the stored 250. `#808081`, `#000000`, `#010101` at most `5`; `#FFFFFF` `d700` at most `4` (its `d300` cells are the sub-C-5 skips U1-5 names) | the same probe in clone `base` (`origin/main` at `d50241ae`): `d300 151 d700 156` (perceptual), `144 157` (peak), `153 158` (even), khaki ends `#BDB6A5` (OKLCH hue `88.3`) and `#524D40` (`89.5`): 144 to 158 degrees off, the yellow ramp in the issue |
| U1-2 | `rgbToOkhsl` reads white as achromatic, nothing else moves | 🟢 | head prints `0 1.56e-5 254,254,254 255,255,255`: white's `s` is exactly `0`, `#FEFEFE` unchanged, both round-trip; `okhsl.js` guard is `if (L >= 1 - 1e-6) return { ..., s: 0, l: 1 }` | clone `base` prints `0.5582831888483675` first. Clone `neg2` with the guard rewritten as a literal `if (L >= 1) return`: white prints `0.5582831888483675` again and `node test/engine/okhsl.mjs` exits `1` with `FAIL  white  — rgbToOkhsl([255,255,255]).s is 0.5582831888483675, want exactly 0` (the em dash is the gate's own output) |
| U1-3 | Nike tertiary-muted on the rendered path no longer khaki | 🟢 | head prints `25 33.21 98.38 7 2.77 500:#F1F1F1 650:#B8969F 900:#3B1E27`: field 2 at most field 3, worst hue `7` at most 12, stop 500 CAM16 C `2.77` at most 3.50; stop 650's CAM16 hue is `360` (the stored hue 0), not 91 | clone `base` prints `25 31.47 98.38 105 8.10 500:#F8F1DE 650:#B89E57 900:#2F2815` with stop 650 at CAM16 hue `91`: fields 4 and 5 both red the row |
| U1-4 | every chromatic anchor byte-identical; the caught ones named and counted | 🟢 | dump in `base` and `head`: `10188` lines each; differing anchors exactly `#ACADAE` and `#FFFFFF`, `6` lines (2 anchors x 3 modes); `48` kit lines, `0` differ; corpus anchored palettes `3380`, under `0.002`: `2` (`#FFFFFF #ACADAE`), under `0.004`: `11` | clone `neg4`, each re-dumped against `base`: constant set to `0.02` gives `342` anchors and `1415` lines; a planted `+ 0.5` degree on the chromatic `rgbToOklchHue(anchor.rgb)` of both anchored branches gives `1705` anchors and `10187` lines, so a change to non-achromatic anchors shows in the diff and the clean run's `6` is a real reading |
| U1-5 | synthetic gate pins the hue rule and the white guard, in `npm test`, bites on both sides of the constant | 🟢 | `node test/engine/anchor.mjs` at head: `exit 0`, one line `pass  achromatic-anchor: 27 of 27 anchored/twin hue distances at most 12deg ... 3 skipped under CAM16 C 5 ... #808082 (OKLab C 0.0030, above the constant) chromatic and hue-stable across a moved palette hue: true`; `node test/engine/okhsl.mjs` `exit 0`, `pass  white`; both files are in `TESTS` | clone `neg2`: `achromatic` forced to `rgbToOklabChroma(rgb) < -1` gives `exit 1`, `FAIL  achromatic-anchor: 0 of 26 ...`; constant widened to `0.004` (swallowing `#808082`) gives `exit 1`, `... hue-stable across a moved palette hue: false`; the white control is U1-2's |
| U1-6 | every allow-list naming Nike tertiary-muted re-measured, not left | 🟢 | Nike name counts at head `anchor.mjs 4`, `prime.mjs 2`, `tonal.mjs 0`, identical at `base`; FAIL counts `0`, `0`, `0` with `prime.mjs` and `tonal.mjs` both `exit 0`; the added entries name `#ACADAE` (`NOTCH_ALLOW` +2, `DIP_BASELINE`/`PERCEPTUAL_DIP_BASELINE` one key each) and two `peak\|0\|100.00...` dup keys, none drops a Nike line | clone `neg2`: the Nike line deleted from `RAMP_WINDOW_ALLOW` gives `exit 1`, `FAIL  anchor-ramp allow-list: 10 (expected 9)`, `2` FAIL lines. Control (2) does not apply: no Nike entry was dropped from any list (name counts unchanged from `base`) |
| U1-7 | the prime ladder does not move | 🟢 | prime dump in `base` and `head`: `3780` lines each, `cmp 0` | clone `neg3`: `prime.mjs:146` `keyChroma = cam.chroma * 1.01` gives `differ: char 241, line 2`, `cmp 1` |
| U1-8 | §9 states the rule and names the constant | 🟢 | `grep -c ACHROMATIC_ANCHOR_C`: doc `1`, `tonal.js` `3` (both `0` in `base`); paragraph at `knowledge-02-tonal-scale.md:451` covers the constant, pivot lightness and chroma, the hue per branch, black and white `s = 0`, the 2026-09-23 ruling; `node scripts/audit-citations.mjs` last line `canvasView enumerations: 0` at head and base, exit `0` both | clone `neg3`: deleting lines 450 to 464 drops the doc count to `0` |
| S1 | smoke, CI's `panda-smoke` at this sha (no local Chrome run) | 🟢 | run `36000752153` has `head_sha bf1f4e3582fe6b2a2093729b34a62ec66d3a1f99`; job `panda-smoke completed success`, `https://github.com/kimgranlund/ultimate-tokens/actions/runs/36000752153/job/107636522831` | the same check-runs query at the unit head `e409a061` (never pushed as a PR head) returns `total_count 0`: the query discriminates by sha, so the success above is this head's own |
| M1 | `git merge-tree --write-tree origin/main <head>` is clean | 🔴 | at `origin/main` = `d50241ae` (the main the head merged, `merge-base --is-ancestor` exit `0`): tree `1d10dcdbea83eef2ab9354d057c261db6e3db45a`, `exit 0`. Re-fetched during the pass, `origin/main` = `a62ec020922b26368ad95e718d2e2bcba2744066` (#713 landed): `exit 1`, `CONFLICT (content)` in `.sdlc/baseline.md` (the `npm test` row, main now says `all 50 test files passed` and adds `gate:corpus-tonal`/`gate:corpus-anchor` rows) and `test/engine/anchor.mjs` (the REPORT tail near line 1454, #713 U3's reworded PASS line against this plan's `notch (17, +2 at #739)` wording); `merge-base --is-ancestor a62ec020 bf1f4e35` exit `1` | clone `neg`: a commit on top of `d50241ae` that edits `tonal.js`'s `okhsl.js` import line (which the head also edits) makes `merge-tree` exit `1` with stage-1/stage-2 `src/engine/tonal.js` entries, so a clean `exit 0` at `d50241ae` is a real reading, not a tool that never conflicts |
| C1 | PR #746's head equals the plan head, mergeable, CI green at that sha | 🔴 | `gh pr view 746`: `headRefOid bf1f4e3582fe6b2a2093729b34a62ec66d3a1f99` equals the plan head; `isDraft true`, base `main`. First poll `mergeable MERGEABLE`, `mergeStateStatus UNSTABLE` (CI in progress); after #713 landed, `gh api pulls/746` reads `mergeable=false state=dirty`. Check-runs at the sha: `build-test completed success`, `panda-smoke completed success`, `corpus-contrast completed success`, `deploy skipped`; no sweeps legs exist in `ci.yml` at this head (they arrive with #713's `ci.yml`, `6` sweep mentions on the new main against `2` comment-only mentions here). CI is green but ran against the pre-#713 workflow; it re-runs at the re-merged head. `https://github.com/kimgranlund/ultimate-tokens/pull/746` | `git rev-parse plan/achromatic-anchor~1` is `d37d89fecbbe2b7359cba8034570f7d1844f8b22`, not the PR head, so the equality reading is between two distinct shas; the `e409a061` query in S1 shows the check-run query returns `0` for a sha CI never ran |
| R40 | Nike tertiary-muted renders mauve at its stored fallback hue 0 | 🟡 | at head the ramp is `650:#B8969F` at CAM16 hue `360`, max CAM16 C `33.21` (above main's `31.47`), because the generator stores `hue 0` for a hueless sample; the plan's Not-in-scope table names it and it is filed as #744. Carried, not a red, per the owner ruling | clone `base` renders the same palette khaki, `650:#B89E57` at CAM16 hue `91`: the probe tells the two renders apart, so the mauve reading is the head's own and not a probe artefact |

Counts: 13 🟢, 1 🟡, 2 🔴. The two reds share one cause (main moved). Next action, Orchestrator: merge `a62ec020` into `plan/achromatic-anchor`, resolve `.sdlc/baseline.md` and `test/engine/anchor.mjs`, push, wait for CI at the new head, request pass 2. The clones under `/private/tmp/claude-501/aapl-bf1f` (`head`, `base`, `neg`, `neg2`, `neg3`, `neg4`, `build`) are still on disk: the `rm -rf` was denied by the permission system, so that path needs removing by hand.

verdict: 🔴
sha: bf1f4e3582fe6b2a2093729b34a62ec66d3a1f99

## Pass 2, at `52e8981b`

verdict: 🟢
sha: 52e8981b083581a0e8cabb7d2f17dde6a674fc44

Delta pass, fresh context, read-only. Graded `bf1f4e35..52e8981b` (one commit, the merge `52e8981b` with parents `bf1f4e35` and `a62ec020`) plus pass 1's two reds. The merge resolution handoff (`.sdlc/handoffs/achromatic-anchor-merge-resolution.md`) was read as claims and every figure below is my own run. Clones, all `git clone -q --shared` under `/private/tmp/claude-501/aap2-52e8`, none with `node_modules`: `head` (`52e8981b`), `main` (`a62ec020`, #713 as landed), `pre` (`bf1f4e35`, pass 1's head), `base` (`d50241ae`, the pre-#713 main both plans were measured against), `neg` (every edit reverted, tree `0` after each). The probe `hue.mjs` was extracted verbatim from the plan's code fence. `origin/main` moved once more during this pass, to `8bb8d875` (the #713 plan-close commit, records only); rows M1 and C1 read against it. Rows not listed below (P2, P4, U1-3, U1-5 to U1-8, S1) were re-covered by `npm test` at the head (branding, both synthetic gates, prime and tonal allow-lists are all inside the 50 files) and by CI at the sha; the delta touches no `src/` file, so their pass-1 readings stand.

Why the notch count moved from 15 to 17. `NOTCH_ALLOW` holds the same 15 names at `d50241ae` and `a62ec020` (#713 did not touch it) and the same 17 at `bf1f4e35` and `52e8981b`; the two added names are both `#ACADAE` (`[peak]`, `[perceptual]`), #739's own re-freeze from pass 1 U1-6. #713 only split the gate into FULL/SAMPLED and reworded the PASS tail. The head's `anchor.mjs` against `a62ec020` is exactly #739's hunks, and against `bf1f4e35` is exactly #713's hunks plus the `15` to `17` tail wording and one four-line comment. So 17 is the union, and the FULL gate measures it (`17 (expected 17)`) rather than reading it as prose.

| # | Criterion | State | Evidence | Negative control |
|---|---|---|---|---|
| D1 | `anchor.mjs` merge resolution correct: 17 is the union of #713 and #739, no allow-list silently widened | 🟢 | `NOTCH_ALLOW` entry counts by sha `d50241ae 15`, `a62ec020 15`, `bf1f4e35 17`, `52e8981b 17`; `diff` main vs head adds only the two `#ACADAE` lines; base vs main and pre vs head both `identical`. Three-way hunks: head vs main equals pre vs base for `anchor.mjs` (#739 content) and `tonal.mjs` (`30` lines identical); head vs pre equals main vs base for `tonal.mjs` (`111` lines identical) and for `anchor.mjs` up to the `notch (15` to `notch (17, Q3-resolved, +2 at #739)` tail and a 4-line comment. FULL pass in clone `head`: `node test/engine/anchor.mjs --full` exit `0`, `pass  anchor-ramp notch allow-list (ruled Q-C, named pending U4): 17 (expected 17)`, tail `PASS (FULL): ... notch (17, Q3-resolved, +2 at #739) ...`; every other allow-list count (`26`, `3`, `10`, `72`, `16`, `64`) identical between `head` and `main`. Clone `main`: `15 (expected 15)`, `PASS (FULL): ... notch (15, Q3-resolved)` | clone `neg` with `ACHROMATIC_ANCHOR_C = -1` (the #739 rule disabled, export kept so the test file loads): `--full` exit `1`, `FAIL  anchor-ramp notch allow-list (ruled Q-C, named pending U4): 15 (expected 17)`, `notch allow-list: expected member missing - travel "67° N ..." secondary-muted #ACADAE [peak]`, `FAIL: 2 gate failure(s)`. Reverting `tonal.js`/`okhsl.js` wholesale to `a62ec020` instead dies at import (`does not provide an export named 'ACHROMATIC_ANCHOR_C'`), so the constant edit is the discriminating control |
| D2 | `baseline.md` keeps #713's table and figures, `stale total: 0` | 🟢 | `git diff a62ec020 52e8981b -- .sdlc/baseline.md`: the gate-split rows (`gate:corpus-tonal`, `gate:corpus-anchor`, `gate:sweep-prime`, `gate:corpus-reset`, `npm test` at `all 50 test files passed`, `89.10 · 79.93 · 80.07`) all kept; only `gate:corpus-anchor`'s summary text (`notch (15` to `notch (17, Q3-resolved, +2 at #739)`), `npm run build`'s KB cell (`4119.1` to `4125.1`, the pass-1 U1 figure), the `ref:` line (`plan/gate-split @ c87d98fc` to `main @ a62ec020`) and two appended `Correction` paragraphs move. Clone `head`: `bash .sdlc/checks/baseline-agrees-check.sh` last lines `ok    head: baseline ref a62ec020 is in origin/main's history`, `stale total: 0`, exit `0`. `npm test` at the head printed `wrote figma/plugin/ui.html 4125.1 KB`, matching the cell | the handoff names the pre-fix reading `STALE head: baseline ref c87d98fc is in origin/main's history`; the `ref:` line is the only line that row of the check reads, and it is the one the resolution changed, so the `0` is the fix's own. Pass 1's M1 row shows the same table in conflict before the resolution |
| U1-1 | `#808080` at palette hue 250 renders the palette hue, stop 500 byte-exact, unchanged by the merge | 🟢 | clone `head`, `node hue.mjs "#808080" 250 50`: `perceptual #808080 d300 5 d700 3` (`#ACB9C6`/`#444F5B`), `peak #808080 d300 0 d700 4` (`#BAC5D1`/`#3A444E`), `even #808080 d300 2 d700 2` (`#B8C5D4`/`#35414D`); every figure and hex equals pass 1 | clone `base`: `d300 151 d700 156`, `144 157`, `153 158`, khaki ends `#BDB6A5`/`#524D40`; clone `neg` with `ACHROMATIC_ANCHOR_C = -1` prints the same `151 156`, `144 157`, `153 158` at the head, so the probe reads the rule and not the tree |
| U1-2 | `rgbToOkhsl` reads white as achromatic, nothing else moves, unchanged by the merge | 🟢 | clone `head`: `0 1.56e-5 254,254,254 255,255,255` (white `s` exactly `0`, `#FEFEFE` unchanged, both round-trip), identical to pass 1 | clone `base`: `0.5582831888483675 1.56e-5 254,254,254 255,255,255` |
| U1-4 | every chromatic anchor byte-identical, caught ones named and counted, unchanged by the merge | 🟢 | dumps in `base` and `head`: `10188` lines each; differing anchors exactly `#ACADAE` and `#FFFFFF`; `6` differing lines; kit lines `0` of `48` differ; identical to pass 1 | clone `neg` with the constant at `0.02`, re-dumped against `base`: `342` anchors, `1415` lines, so the dump discriminates on the threshold and the `6` is a real reading |
| P1 | `npm test` green at the head, foreground, no `node_modules`, tree byte-stable | 🟢 | clone `head`: `ls node_modules` prints `No such file or directory`; `npm test` exit `0`, `✓ all 50 test files passed`, `50` `▶` lines, `wrote figma/plugin/ui.html 4125.1 KB`, `git status --short \| wc -l` prints `0` | clone `neg`: `sed 's/"scrim/"scrimX/'` on `docs/reference/data/role-table.json`, then `node test/engine/semantic.mjs` exit `1`, `FAIL  refs-canonical  — ordered key set != canonical` (the em dash is the gate's own output); D1's control is the anchor gate's own red |
| P3 | delta prose clean: no em dash outside backticks, no private docs, no `node_modules` | 🟢 | lines the head adds against `a62ec020` in the three resolution files (`anchor.mjs`, `baseline.md`, the merge handoff), backticks stripped: `0` em dashes; the handoff's raw count `0`; `git diff --name-only a62ec020 52e8981b \| grep -c -e '^node_modules/' -e '^\.claude/docs/other/'` prints `0`; `test/repo/branding.mjs` ran green inside P1's `npm test` | the pass-1 record itself, through the same raw grep, prints `1` (U1-2's quoted gate output), so the grep finds an em dash when one is present |
| M1 | `git merge-tree --write-tree origin/main 52e8981b` clean | 🟢 | `origin/main` = `8bb8d875b78c20f44aff2bcb225403b510563fe6`; `merge-tree` prints tree `8ac355651a47911cf0f905de106001a77c8756f5`, exit `0`, no conflict lines | pass 1's M1: the same command at `a62ec020` against `bf1f4e35` exited `1` with `CONFLICT (content)` in `.sdlc/baseline.md` and `test/engine/anchor.mjs`; the head is that merge resolved, so the clean reading is the resolution's own |
| C1 | PR #746 head is `52e8981b`, MERGEABLE, CI green at the sha including every sweeps leg | 🟢 | `gh pr view 746`: `headRefOid 52e8981b083581a0e8cabb7d2f17dde6a674fc44`, `mergeable MERGEABLE`, `mergeStateStatus CLEAN` (first poll `UNSTABLE` while CI finished, no sleep needed), `isDraft true`, base `main`. Check-runs at the sha: `total_count 9`; `build-test`, `panda-smoke`, `corpus-contrast`, `sweeps (gate:corpus-tonal)`, `sweeps (gate:corpus-anchor)`, `sweeps (gate:corpus-reset)`, `sweeps (gate:sweep-prime)`, `sweeps (gate:corpus-contrast)` all `completed success`, `deploy completed skipped`; run `36005718870` has `headSha 52e8981b...`, `conclusion success`; its `gate:corpus-anchor` job log (`107653230545`) prints `17 (expected 17)` and the `notch (17, Q3-resolved, +2 at #739)` tail. `https://github.com/kimgranlund/ultimate-tokens/actions/runs/36005718870` | pass 1's C1 at `bf1f4e35`: `mergeable=false state=dirty` and only `4` check-runs (no sweeps legs, pre-#713 `ci.yml`), so the 9-run green set is this sha's own and not a stale reading; `ci.yml` on main lists exactly `build-test`, `panda-smoke`, `corpus-contrast`, `sweeps`, `deploy` |
| R40 | Nike tertiary-muted renders mauve at its stored fallback hue 0 | 🟡 | clone `head`: `650:#B8969F cam16 hue 360`, unchanged from pass 1; `gh issue view 744` reads `OPEN Hueless presets fall back to hue 0 and render mauve`. Carried under the owner ruling, out of this plan's scope | clone `base`: `650:#B89E57 cam16 hue 91` (khaki), so the probe tells the two renders apart |

Counts: 10 🟢, 1 🟡 (R40, carried, #744), 0 🔴. Pass 1's two reds (M1, C1) are cleared at this head. Next action, Orchestrator: land PR #746 per `shipping-changes` (squash, sync), then close the plan. The clone directory `/private/tmp/claude-501/aap2-52e8` was removed; pass 1's `/private/tmp/claude-501/aapl-bf1f` is still on disk and needs removing by hand.


The review side: `reviewer-l4` under the orchestrator, PASS at `bf1f4e35` (record `/private/tmp/claude-501/achromatic-anchor-prepr-review.md`); the delta `bf1f4e35..52e8981b` is the origin/main merge only, resolved per `.sdlc/handoffs/achromatic-anchor-merge-resolution.md`. CI run `36005718870` is green on all legs, including five sweeps, at `52e8981b`.

verdict: 🟢
sha: 52e8981b083581a0e8cabb7d2f17dde6a674fc44
