---
kind: verdict
plan: k17-rerun
seat: verifier
pass: 1
written: 2026-09-20
---

# Pre-PR · k17-rerun · 092d294159ac881eb8217d35dd080f8ee4afc607

verdict: 🟢
sha: 092d294159ac881eb8217d35dd080f8ee4afc607

Graded at `plan/k17-rerun` @ 092d2941, merge base `main` @ d34b4fb1, draft PR #711, ticket #710.

Three independent runs, none of them the seat that wants to land: this Verifier seat's own, a
`reviewer-l4` and a `verifier-l3` dispatched in fresh context per `pre-land-review`, both fable, so
neither shares a family with the sonnet builder or with this opus seat. All three ran the K17 control and
their own plant. They agree on every number. The builder's handoff and the unit verdict were read as
claims, never as evidence. The Orchestrator's own rerun of the two 🟡 unit rows is recorded below as a
claim and regraded here independently.

## Checks

| Check | State | Evidence | Negative control |
|---|---|---|---|
| U1-1 pass 6 exists once, is last, one K row, K17, 🟢, no empty cell | 🟢 | `1`, nothing, `1`, `1`, `0`, measured by all three runs | at d34b4fb1: `0`, nothing, `0`, `0`, `0`. Fixture with the row 🔴: fourth line `0`. A second pass 6 appended: first line `2` |
| U1-2 the newest K17 row quotes the map's filter byte for byte | 🟢 | `1`. The filter is lifted from the map's cell by the block, not retyped, escapes included | `ui/counts.mjs` dropped from the map's cell with the verdict unchanged: `0`, so either file moving alone fails. At d34b4fb1: `0` |
| U1-3 graded at an equivalent head, counts and own plant in the row, grader's rerun agrees | 🟢 | `3600ad6e` as a single value, then `0`, `1`, `1`. `git diff 3600ad6e 092d2941 -- test .sdlc/architecture.md` is empty | intro sha set to `d814500`: `22`. Before the unit: empty line, `no sha`, `0`, `0`. Intro paragraphs rejoined at this head: the extractor returns two shas and git exits `fatal: bad revision` |
| U1-4 the title names the highest pass and the computed 🟢 count | 🟢 | `1`, then `6 18`. Graded here by this seat and by both workers independently. See concern 1 | at d34b4fb1: `0`, `5 17`. Pass 6 present with the old title: `0`. Pass 6 state 🔴: `0`, `6 17` |
| U1-5 the seven counts the sister repo's readiness row reads | 🟢 | `18`, `18`, `19`, `31`, `1`, `7`, `19 19`. Line 6 reads `7`: the word it counts appears on verdict lines 7, 21, 27, 35, 55, 58 and 61, all in passes 1 to 5, never in pass 6 | before the unit: `18 18 18 30 1 7 18 18`. One pass 5 K row deleted: first line `17`. Pass 6 given the counted word once: line 6 `8` while the fold on line 5 still prints `1` |
| U1-6 passes 1 to 5 untouched, the only deletion is the old title | 🟢 | `1`, `0`. The file's whole diff against the merge base is the one title line | at d34b4fb1: `0`, `0`. Pass 5's own K17 state flipped in place: `2`, `1` |
| U1-7 the handoff is a claim with numbers and a recorded hit count | 🟢 | `0`, `1`, `1`, `1`. It says `measured at c2c7a1fe`, whose `test/` and map equal this head's, verified by direct diff, not by the block. See concern 5 | copy naming `d814500`: `22`. Copy mentioning a plant with no count: last line `0`. Copy with `measured at` removed: the guarded block prints `no sha` |
| P1 `npm test`, no `node_modules`, tree byte-stable | 🟢 | `✓ all 48 test files passed`, then `0`, in clean clones, twice independently | role table key renamed: `FAIL refs-canonical`, `✗ 1/48 test file(s) failed`, exit 1; clone back to `0` |
| P4 branding gate | 🟢 | `branding: clean (467 files scanned)`, `exit 0` | records doc copied outside its exempt path: `FAIL: 3 branding violation(s)`, `exit 1` |
| P5 scope wall | 🟢 | `0`. Exactly 7 paths differ, all under `.sdlc/`, all inside the naming rule | five legs: a byte on the map `1`; a new `.sdlc/checks/new.sh` `1`; the slug not at the start of the name `1`; a second plan file `1`; the loop's own later records together, including this one, `0` |
| baseline `npm run build` | 🟢 | green in two clones, once after `npm ci` and once with `node_modules` linked. `git diff --exit-code` after the build: no generated-artifact drift, tree `0` | the drift gate is itself the control: it exits 1 on any regenerated byte |
| baseline `npm run smoke` | 🟢 not required | `git diff --name-only main...HEAD` is 7 paths, none under `src/ui/`, `src/`, `test/` or the build chain. Adapter §1 requires smoke only when `src/ui/` changed | CI's `panda-smoke` covers the PR regardless and reports `success` |
| CI at this exact sha | 🟢 | run 35485151639, `headSha` 092d294159ac881eb8217d35dd080f8ee4afc607: `build-test` success, `panda-smoke` success, `corpus-contrast` success, `deploy` skipped | a red job would print its conclusion here; the conclusion is read by hand per the adapter's watch quirk |
| K17 control and plant, three own runs | 🟢 | framework half 0; registration half 7 before the map's filter, 0 after; the seven names identical across all three runs and to pass 6's row. Plant as the map's bite cell writes it: 2 hits, 1 per half, every clone reset to `0` | the plant is the control's bite: clean baseline 0 and 0, planted 1 and 1 |
| pass 6 committed verbatim | 🟢 | the committed section is byte-identical to the fence in `.sdlc/verdicts/k17-rerun-U1.md`: 1959 bytes both ways, compared programmatically | a single changed byte would fail U1-2 or U1-5 line 6; both were rerun at this head |
| integration across the seven paths | 🟢 | plan, approval, criteria review, handoff, unit verdict, board row and the two A2 edits agree with each other and with the map's K17 cell. Revision 2 folded all three unit findings: the two-paragraph intro, the `no sha` guard, the reworded P1 control | each checked at file and line by the reviewer in fresh context |
| commit hygiene | 🟢 | 10 commits, every one carries a model attribution trailer; the commits that stage `.sdlc/board.md` and the unit merge carry `Seat: orchestrator` | a missing trailer is refused by the repo's commit-msg hook |
| added lines: em dashes, retired maker brand, pre-rename identifier | 🟢 | 0, 0, 0 across the diff, and the branding gate scans `.sdlc/` | the gate's own control above |
| secrets, config, dependencies | 🟢 | no `package*.json`, no CI file, no config, no secret-shaped string in the diff | scope is 7 records under `.sdlc/` |

## Concerns, none blocking

| # | State | Concern | Owner |
|---|---|---|---|
| 1 | 🟡 | U1-4 is the criterion for the defect this plan exists to fix, and the plan's pre-land grading list omits it. Its only 🟢 before this record was the Orchestrator's own rerun, which is the seat that wants to land. Closed here: three independent runs read `1` and `6 18`. The plan's grading map stays short, so a future pass repeating this plan's shape would inherit the hole | Orchestrator, after landing |
| 2 | 🟡 | `.sdlc/architecture.md` line 18, the rerun note, still says pass 5 is K17's current reading and that U3's verdict grades it. Pass 6 now is. The owner's Q1 wall excludes the map so this change could not repair it, but Q-A has no recorded answer and no debt row exists for the pointer or the check script the plan defers | Conductor, Q-A |
| 3 | 🟡 | PR #711 has an empty body. Adapter §2 requires the plan summary plus this verdict table ending in the generated-with line. Legitimate while draft; it must be filled before the PR is marked ready, or the landing breaches the adapter | Orchestrator, before ready |
| 4 | 🟡 | `.sdlc/debt.md` line 92 still describes the K17 control as filtering 3 files by name. It has filtered 7 since `28c2e8cc`. Pre-existing and outside this plan's wall, so this PR inherits it rather than causing it, but it is the same staleness class this plan exists to correct | Conductor, debt row |
| 5 | 🟡 | U1-7's first leg is guarded at this head, and the guard is load-bearing: run unguarded on a handoff with the `measured at` line removed it prints `0` on stdout, byte-identical to the pass value, with `fatal: bad revision` on stderr and git exit 128. Measured independently twice. U1-7 is 🟢 here on a direct diff of `c2c7a1fe` against this head, not on that leg | recorded, no action |
| 6 | 🟡 | `pre-land-review` names `reviewer-l4`, and states the pre-land pair is fable so it never shares a family with the builders. `.sdlc/adapter.md` §2.1 and this plan's own grade paragraph both say `reviewer-l3`. The adapter is declared to win over plugin defaults, so two records now contradict the skill. The stricter cross-family pair actually ran | Conductor, adapter §2.1 |
| 7 | 🟡 | committing this record moves the branch head past the `sha:` it names, so the adapter's gate sees a stale sha. The repo's two prior records resolved this differently: one names a sha at which the record does not exist, the other names a sha at which it does. No convention is written down | Orchestrator, at land |

## One note on the tooling, because the counts leave this repo

`grep` in the seats' shells is a snapshot shell function backed by ugrep, not the system binary. Since
U1-5's seven counts are read by a sister repo, every one of them was rerun through `/usr/bin/grep`
(BSD grep 2.6.0-FreeBSD) by this seat, and independently by the pre-land verifier: `18`, `18`, `19`, `31`,
`1`, `7`, `19 19`, identical. U1-1 to U1-4, U1-2's byte-for-byte filter match, and the K17 control itself
(`7` before the filter, `0` after) were rerun the same way, also identical. The numbers are not an
artifact of either grep.

## What this record does not cover

`.sdlc/verdicts/k17-rerun-prepr-review.md`, the reviewer's own record, is untracked in the worktree and
inside the wall. It and this file are both the Orchestrator's to commit. Neither is graded by anything.
