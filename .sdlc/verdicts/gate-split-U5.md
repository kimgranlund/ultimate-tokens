# Verdict gs-U5 · 🟡

Unit head `d0a61ff7` (code `fe099258`, handoff `d0a61ff7`), fork point `ebddc55d` (`git merge-base plan/gate-split HEAD`), plan `plan/gate-split @ 557b6c0c`, ticket #713. Verifier-l1, read-only. Load at start `3.99 4.94 5.16` (11:48), at end `6.54 6.94 6.46` (12:00), 10 cores. The host was never quiet: another seat's `node test/run.mjs` and hot processes ran throughout, so the one timing below is indicative only.

Counts: 13 🟢, 2 🟡, 0 🔴.

Provenance (revision 11): every control clone is `git clone -q --shared <repo>` then `git checkout --detach d0a61ff7`, the committed unit head. Each printed `d0a61ff7` and `grep -c 'const FULL = process.argv.includes'` = `1`; the same grep on `ebddc55d` prints `0`, so no clone ran the pre-edit file.

| # | Criterion | State | Evidence | Negative control |
|---|---|---|---|---|
| 1 | U5-1 FULL sweeps everything | 🟢 | fresh worktree, no `node_modules`: `npm run -s gate:corpus-reset` exit `0`; `HEADLESS BOOT PASS` count `1`; `(FULL: 343 curated documents, 3780 palettes)` count `1` | M-D clone (diff stat `1 file changed, 1 insertion(+), 1 deletion(-)`), same command: exit `1`, `(rst-corpus) 3396 of 3396 anchored palettes failed the exact-snapshot field round trip` |
| 2 | U5-2 SAMPLED green, says so, M-D still reds it | 🟢 | `node test/ui/headless-boot.mjs \| tail -2` gives `(SAMPLED seed 0: 35 curated documents, 392 palettes)` then `HEADLESS BOOT PASS`, exit `0` | M-D clone, SAMPLED: exit `1`, `grep -c '(rst-corpus)'` = `4`, `(rst-corpus) 316 of 316 anchored palettes failed the exact-snapshot field round trip` |
| 3 | U5-3 only the sweep block changed | 🟢 | `A=3950 Z=4020`, hunks at `13 15 3965 3968 3970 3974 4011 4015`, outside count `2` (the `import { sampleCorpus` line and `const FULL` line), equal to the handoff's `2`; removed lines `4` by `grep -v '^--- ' \| grep -c '^-'`, matching the handoff's 4 deletions | clone: one comment line planted above `(cm-toggle)` in a lettered group, diff stat `1 file changed, 1 insertion(+)`: figure rises to `3` |
| 4 | U5-4 file share at or under 30 s quiet | 🟡 | SAMPLED `real 28.23`; start load `8.25 5.86 5.48` with `2` hot processes, end `6.43 5.65 5.41` with `0`. It fails the quiet-host rule at the start, so indicative only. It is under 30 s even under load | what would red it: the same `/usr/bin/time -p node test/ui/headless-boot.mjs` on a quiet host reading `real` above `30`, or the FULL list leaking into the default leg (as in row 6, clone with `sampleCorpus` bypassed), which runs the 3396-palette sweep and took far longer than 30 s in row 1 |
| 5 | P1 `npm test` green, count agrees, tree byte-stable | 🟢 | scratch worktree: `✓ all 50 test files passed`, `TESTS` perl count `50`, `git status --short \| wc -l` = `0` after; `test/run.mjs:25` passes no args, so the suite runs SAMPLED | clone with `"scrim` changed to `"scrimX` in `role-table.json`: `npm test` exit `1`, `✗ 1/50 test file(s) failed` |
| 6 | P3 (a) gate lost its `--full` | 🟢 | the unmutated `npm run -s gate:corpus-reset` prints `(FULL: 343 curated documents, 3780 palettes)` with count `1` (row 1) | clone, ` --full` removed from `gate:corpus-reset` in `package.json` (diff stat 1/1): exit `0`, FULL needle count `0`, tail shows `(SAMPLED seed 0: ...)` |
| 7 | P3 (b) flag read forced false | 🟢 | the unmutated gate prints `(FULL:` once (row 1) | clone, `process.argv.includes("--full")` changed to `false` (diff stat 1/1): exit `0`, `grep -c '(FULL:'` = `0` |
| 8 | P3 (c) FULL-side vacuity bites | 🟢 | FULL floor `corpusChecked > 3000` passes on `3396` (row 1) | clone, substitution line changed to `FULL ? sampleCorpus(byCategory) : sampleCorpus(byCategory)` (diff stat 1/1): exit `1`, `(rst-corpus-setup) exercised Reset over the FULL anchored corpus ... (316 palettes, want > 3000)` |
| 9 | P5 row 6, M-D reds the FULL leg | 🟢 | M-D clone FULL: exit `1`, `grep -c '(rst-corpus)'` = `4` | the same command unmutated exits `0` with `(rst-corpus)` failure count `0` (row 1) |
| 10 | Coverage: the sampled default catches a planted defect, and the plan calls it a canary | 🟢 | SAMPLED reds M-D on `316 of 316` (row 2). The plan's design says `So the sample is a canary and the full gates are the gates of record` | the unmutated SAMPLED run is green (row 2), so the red comes from the mutation |
| 11 | SAMPLED vacuity floor and the per-category drop gap (handoff item 3) | 🟢 | per-category anchored palettes in the sample, printed by a probe: `{"architecture":40,"brands":20,"cuisine":40,"film":40,"literature":40,"music":40,"nature":40,"travel":40}` plus 16 from the default kit = `316` against a floor of `> 300`. Dropping any one category leaves at most 296, so the floor catches a lost category today. The margin is thin: `16` palettes | clone with `music` removed from the category loop: exit `1`, `(SAMPLED seed 0: 31 curated documents, 348 palettes)`, `(276 palettes, want > 300)`. Clone with the sample cut to `.slice(0, 3)`: exit `1`, `(46 palettes, want > 300)`, mode line `3 curated documents, 33 palettes`, which also shows the mode line is computed, not typed |
| 12 | P4 CI runs the FULL reset sweep on every PR (merged view) | 🟢 | `557b6c0c` merged with `d0a61ff7`: clean, tree `27814cd3` (same as `git merge-tree`). P4 prints `corpus-reset 1`, contrast `2`, sweeps jobs `1`, `if/continue-on-error/needs` `0`, `fail-fast: false` `1`, `pull_request:` `1`. The leg runs `npm run gate:corpus-reset`, the same command row 1 ran green from a fresh tree with no `node_modules` | clone: `gate:corpus-reset` matrix entry commented out, its count prints `0`; `continue-on-error: true` added under `sweeps:`, that count prints `1` |
| 13 | That CI job actually blocks the merge | 🟡 | nothing runs yet: `git ls-remote origin plan/gate-split unit/gs-U5` is empty, `gh pr list --head plan/gate-split` is `[]`, and `gh api .../branches/main/protection` returns `Branch not protected`. The block is procedural (plan Landing: P10 wants conclusion `success` plus every `sweeps` leg named in the pre-land verdict). Not a U5 defect. The proof belongs to P10 at pre-land | what would red it: a P10 read of the first draft-PR run, `gh run view <id> --json jobs --jq '.jobs[] \| select(.name\|startswith("sweeps")) \| [.name,.conclusion] \| @tsv'`, showing the `gate:corpus-reset` leg absent or not `success`, or `continue-on-error` added to `sweeps:` (row 12 control printed `1`) |
| 14 | P8 branding clean, no added em dash | 🟢 | `branding: clean (515 files scanned)` exit `0`; added-line U+2014 count outside backtick spans `0` | clone: `decision-records.md` copied to `.sdlc/verdicts/x.md` gives `FAIL: 3 branding violation(s)` exit `1`; two planted handoff lines with the dash, one starting `+`, count `2` |
| 15 | P9 scope wall | 🟢 | base `ebddc55d`, `Q2=yes`: filter `0`, `src` diff `0`; changed files `.sdlc/handoffs/gate-split-U5.md`, `test/ui/headless-boot.mjs` | fixture of four names: `0` with `Q2=yes`, `1` with `Q2=no`; `src/engine/hct.js` passes the filter, `1` |

Not applicable to this unit: P2 and P10 (pre-land), P6 (U1's file), P7 (expected stale on unit branches until U6b).

Notes for the Orchestrator:
- Handoff item 3 (no per-category presence assertion in U5): the palette floor already catches any single-category drop at today's corpus (row 11). U3 carries the explicit presence assertion in `anchor.mjs`. If the corpus grows, the `300` floor could stop covering a `brands` drop, since the margin is 16.
- Row 13 moves to P10: the pre-land verdict must name the `sweeps (gate:corpus-reset)` leg with conclusion `success`.

Scratch worktree `vU5` removed by exact path. The control clones under `scratchpad/gsv5/` are still there: the permission system denied `rm -rf` on that path. They are plain clones, not registered worktrees.
