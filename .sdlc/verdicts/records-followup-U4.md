# Verdict U4 · 🟢

Plan `records-followup` (#709), unit U4, head `unit/rf-U4` @ `b9e7095011ade90022d2fd97064b6db2cef6e747`, pass 2. Criteria read from `.sdlc/plans/records-followup.md` on `plan/records-followup` @ `07ecb44c` (revision 3). `BASE` `d34b4fb1beefff11c9be53ec039d4265c925da4a`, `UB` `07ecb44c3c885b7856435a56d0f14d1605f3dc99`. Verified in my own detached scratch worktree and two throwaway `--shared` clones, all under my scratchpad, all removed by exact name. Nothing was edited, pushed or fixed.

Both programs were run, not read. The P1 negative control corrupted `role-table.json` in a clone and ran `npm test` with no `node_modules`. `npm run smoke` ran in a second clone after `npm ci`, in real headless Chrome Canary, which `test/smoke/smoke.mjs` auto-detects on this host. Every restored quote was compared to the bytes those runs printed with `od -c` and `grep -Ff`.

| # | Criterion | State | Evidence | Negative control |
|---|---|---|---|---|
| 1 | the rule is written once, in the adapter, and says the three things | 🟢 | `.sdlc/adapter.md`, then `1`, then `1`. The amendment body is byte-identical to the plan's §Texts block: 1480 bytes both sides, `cmp` silent. Opener typed as the file's own shape, `**Amendment (2026-09-19).**` | at `BASE` in a clone the same three commands print an empty line, `0`, `0` |
| 2 | the U1 verdict quotes the gate line as my own control prints it | 🟢 | control: `exit 1`, `grep -c FAIL` prints `3`, last line `✗ 1/48 test file(s) failed`. `line.txt` is one line; `od -c` gives `F A I L` two spaces `r e f s - c a n o n i c a l` two spaces U+2014 space `o r d e r e d   k e y   s e t   ! =   c a n o n i c a l`. Then `1`, `1`, `0`: `grep -cFf` matches `.sdlc/verdicts/records-refresh-U1.md` with no differing byte, and the falsified comma form is gone. The printed line carries two leading spaces and the cell marks their loss | the `wc -l` guard is `1`, so `grep -Ff` is not matching on an empty pattern file. At `BASE` the same three values are `1`, `0`, `1` |
| 3 | every `.sdlc` quote of smoke's summary outside `plans/` and U4's own handoff is the line the source prints | 🟢 | `smoke.txt` is one line. `npm run smoke` exit 0, one `SMOKE PASS` line, and the line it printed is byte-identical to the source-derived line (`cmp` silent, U+2014 and three U+00B7 included): `SMOKE PASS — gallery · category · editor · export dialog all render in a real browser`. `git grep -cFf` against the bytes the browser run printed gives `.sdlc/baseline.md:1`, `.sdlc/handoffs/records-refresh-U1.md:1`, `.sdlc/handoffs/records-refresh-U3.md:4`, `.sdlc/verdicts/records-refresh-U3.md:1`, `.sdlc/verdicts/survey.md:1`, nine occurrences. The four-alternative altered-form grep prints `0` | the handoff exclusion is load-bearing here: the handoff carries `2` lines matching the altered-form alternatives. At `BASE` the widened grep prints five files |
| 4 | each corrected history file says it was corrected | 🟢 | `1` on each of the five paths. The `records-refresh-U3.md` correction carries the extra F6 sentence flagging Note 2, as §Texts requires | at `BASE` in a clone the five counts are `0` |
| 5 | the dash measure exempts the restored quotes and nothing else; branding unchanged | 🟢 | stripped count `0`; raw count against `UB` `9`, and the per-file breakdown is exactly the enumerated list, `.sdlc/baseline.md` 1, `.sdlc/handoffs/records-refresh-U1.md` 1, `.sdlc/handoffs/records-refresh-U3.md` 4, `.sdlc/verdicts/records-refresh-U1.md` 1, `.sdlc/verdicts/records-refresh-U3.md` 1, `.sdlc/verdicts/survey.md` 1, `.sdlc/adapter.md` 0; then `branding: clean (472 files scanned)`, `exit 0` | ran both fixtures in a clone. A dash added to the amendment's prose moves the stripped count off `0`. The same dash inside a backtick span leaves it `0` and moves the raw count off `9` |
| 6 | scope: eight files, history files changed only on the quote lines and the appended correction | 🟢 | the eight expected paths in order, `.sdlc/adapter.md`, `.sdlc/baseline.md`, `.sdlc/handoffs/records-followup-U4.md`, `.sdlc/handoffs/records-refresh-U1.md`, `.sdlc/handoffs/records-refresh-U3.md`, `.sdlc/verdicts/records-refresh-U1.md`, `.sdlc/verdicts/records-refresh-U3.md`, `.sdlc/verdicts/survey.md`; removed-line counts `1,2,4,1,1`; `stale total: 0`. I read the U1 verdict's diff: one line replaced, the span restored with `(altered: two leading spaces dropped)`, two lines appended | a reworded finding raises a file's removed count; at `ba0a66d6` the plan records six paths and `1,2,4,0,0` |
| P1 | `npm test` green with no `node_modules`, tree byte-stable | 🟢 | in the scratch worktree with no `node_modules` present: `exit 0`, `✓ all 48 test files passed`, the `TESTS` length `48`, `git status --short` `0` lines | the U4-2 control is P1's own: `exit 1` and `3` on a corrupted `role-table.json` |
| P2 | `npm run build` prints the size the live baseline row records | ⏳ | pre-land only by the plan's own wording, not a unit row. Not run | not run |
| P3 | `npm run smoke` green | 🟢 | ran it although the plan scopes it pre-land, because U4-3 rests on what smoke prints: `npm run smoke 2>&1 \| grep -c 'SMOKE PASS'` prints `1`, exit 0, in real Chrome Canary | `test/smoke/smoke.mjs:24` is the plan's stated control. It is also the subject of note 2 below |
| P4 | branding gate clean | 🟢 | `branding: clean (472 files scanned)`, `exit 0` | the plan's fixture copies `decision-records.md` into `.sdlc/verdicts/` and the gate fails, so it does scan the files this unit edited |
| P5 | scope wall | 🟢 | `0`, `0`, and nothing from the `CLAUDE.md` numstat, which is the expected shape before U3 merges. U3 is unstarted | `echo "// probe" >> src/engine/motion.mjs` in a clone moves the first number to `1` |
| P6 | no em dash added in prose | 🟢 | `0` against `BASE` over all of `.sdlc` | the same two fixtures as U4-5, both run |
| P7 | the baseline's test-file figure equals `TESTS.length`, by script | 🟢 | seven `ok` lines, `stale total: 0`, `exit 0`. The first reads `ok    tests: baseline 48, test/run.mjs TESTS 48` | records-refresh measured the planted stale figure; my independent check is P1's own `TESTS` count of `48`, which agrees with the script |

## The pass-2 review's seven notes, graded

None is a U4 criterion failure. I re-measured notes 1, 2 and 7 myself rather than accepting them.

| Note | Grade | What I found |
|---|---|---|
| 1 unmarked `engine/semantic.mjs FAIL` in the repaired cell | 🟡 confirmed, plan scope | my control prints `▶ engine/semantic.mjs      FAIL`, a U+25B6 then a space then six spaces before `FAIL`; the cell carries `engine/semantic.mjs FAIL` with no `altered:`. Real, and F11's own defect class sitting beside the repair. §Texts enumerated only the `refs-canonical` span, so no U4 criterion reaches it |
| 2 dash-truncated `smoke: missing dist/ultimate-tokens.html` | 🟡 confirmed, plan scope | `test/smoke/smoke.mjs:24` prints an absolute path and then U+2014 and a clause. The quote is cut before the dash, unmarked, in `.sdlc/verdicts/survey.md:11`, and the same string sits in `verdicts/records-refresh-checkability.md:12`, `plans/archive/records-refresh.md:45` and this plan's own P3 row. Systemic, and the plan itself is one of the carriers, so it cannot be a U4 row |
| 3 the raw-count sentence is incident vocabulary | 🟡 plan text | the builder typed §Texts byte for byte, `cmp` silent at 1480 bytes. The wording is the plan's, not the builder's |
| 4 the exemption control is non-circular only if the number comes from the enumerated list | 🟡 plan text, and the gap is real | I derived the per-file breakdown from the diff and it equalled the enumeration independently, so the row is sound as graded. The amendment never states the requirement, so a later verifier could re-derive the expected number from the diff and get a tautology |
| 5 silent on fenced blocks and on retroactivity | 🟡 plan text | correct. P6 strips inline spans line by line, so a quote in a fenced block has no stated exemption, and notes 1 and 2 show the legacy population the rule does not scope itself against |
| 6 the Correction wording is singular where U3's handoff had four | 🟡 plan text | the five correction lines are §Texts verbatim, including the F6 sentence. Wording is the plan's |
| 7 the builder's own P1 row is not P1 evidence | 🟡 handoff hygiene, unit clean | I ran P1 myself on the committed tree in a worktree that never had `node_modules`: `exit 0`, `48`, clean tree. The unit passes; the handoff row overstates its own conditions |

One further note of my own, not in the review.

| Note | Grade | What I found |
|---|---|---|
| 8 U4-5's handoff exclusion is inert at this head | 🟡 not a failure | the handoff carries `0` em dashes, so the raw count is `9` with the exclusion and `9` without it. The plan justified the exclusion by the rule obliging the handoff to quote both program lines; at pass 2 the handoff paraphrases them instead. The criterion's expected `9` is met either way, and the same exclusion is load-bearing in U4-1 and U4-3 |

## Verdict

🟢. Six of six unit criteria pass on my own evidence, with a live negative control on each. The heart of the unit holds: both programs were run here, and all ten restored spans, one gate line and nine smoke lines, equal the bytes those runs printed. Five of the seven review notes are edits to the plan's own §Texts and belong to a later unit, and the remaining two are a handoff row and an unenumerated legacy quote, neither of which any U4 criterion covers. P2 is pre-land only and stays ⏳.
