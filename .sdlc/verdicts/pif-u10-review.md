---
plan: preset-intent-fidelity
unit: U10
ticket: 681
seat: reviewer-l4
pass: 1
sha: 65b72746e4373b1e2fb8816d55517cb371fca612
reviewed_code_sha: 872e0b7a100c333ffcc35f264ffdbff76e215750
date: 2026-09-23
result: PASS
---

# Review pif-u10 pass 1 (builder-l5) at `872e0b7a`, graded at `65b72746`

Fresh context. Code reviewed at `872e0b7a`; the unit head is now `65b72746`, and `git diff --stat 872e0b7a..65b72746` names only `.sdlc/plans/preset-intent-fidelity.md` (revision 39, owner R27, 7 insertions 1 deletion), so every code and fixture reading below holds at both shas. Diff read as `git diff 8f0e08d6..HEAD -- . ':!.sdlc/plans'` (14 files). Controls ran in four `git clone -q --shared` scratch clones under `$TMPDIR/pif-u10-rev`: `head` (872e0b7a), `nocap` (872e0b7a with the one `sd` cap line reverted to the uncapped form), `pre` (UB `d1f2704c`, the engine before U10), `main` (`origin/main` `dae67a3a`). All four removed after the run. OKLCH readings below are from my own sRGB to OKLab conversion in a probe script, not the engine's.

| Row | State | Evidence | Negative control |
|---|---|---|---|
| U10-1 black anchor renders | 🟢 | `rgbToOkhsl([0,0,0])` prints `{"h":0,"s":0,"l":0}`; `#010101` `s 9.3e-8`, `#808080` `s 1.2e-7`, all finite | `pre` clone with the new `anchor.mjs`: `FAIL  anchor-achromatic: 20 of 24 ramps real` |
| U10-2 guard is in the engine | 🟢 | `git diff --name-only d1f2704c -- src/engine/ src/ui/persist.js` prints `src/engine/hct.js`, `src/engine/okhsl.js`, `src/engine/tonal.js` | `persist.js` absent from that list, and the gate calls `paletteStops` directly |
| U10-3 a test pins it | 🟢 | `head`: `pass  anchor-achromatic: 24 of 24 ramps real (...), planted-NaN control caught` | same gate on the `pre` engine reds (row U10-1); the in-file planted `#NANNANNAN` stop is caught before any ramp runs |
| F1 guard cannot hide a real NaN | 🟢 | `rgbToOkhsl([NaN,0,0])` prints `{"h":null,"s":null,"l":null}` (NaN, not 0): `L <= 0` is false for NaN, so a NaN upstream still passes through; only OKLab `L === 0` (pure black) takes the branch, and `L` is `lab[0]`, which no non-black sRGB input reaches | `#FFFFFF` still reads `s 0.558 h 89.9` (F4, #739): the guard did not touch the other degenerate end |
| U10-4 fact sheet current | 🟢 | `grep -n -e 'OKHSL lightness ladder' -e 'unchanged from the previous release' docs/marketing/fact-sheet.md \| wc -l` = `0`; line 18 now says `CIE L*` ladder and `ramp and role values differ from the release before it` | the same grep at `8f0e08d6` prints line 18 (the diff's `-` line) |
| U10-5 envelope never 0 across a band (R27) | 🟢 | `head` `env lift-36: 0.20 0.20 0.20 0.20 0.59 1.00` at 50/100/150/200/300/500. Rendered, my OKLCH read: Warning `100:#FCFBFA C0.0017 h68`, `150:#F5F1ED C0.0069 h68`, `200:#E9E1D9 C0.0139 h68`; navy (h250 c60 lift +40) `850:#2C3137 C0.0128 h253`, `900:#1C2024 C0.0097 h248`, `950:#0F1114 C0.0070 h258`. `origin/main` same inputs: Warning `100:#FDFBF9 C0.0034 h68`, `150:#F9F1E9 C0.0137 h68`; navy `850:#253240 C0.0308 h251`, `900:#192028 C0.0185 h252`, `950:#0E1115 C0.0096 h256`. Hue matches main within 2 to 6 degrees at every stop; chroma is 0.4 to 0.7 of main's. The rows read tinted, not neutral grey (Warning 150 and navy 850/900 are clearly warm and navy; Warning 100 at C 0.0017 is at the edge of perception, as main's own C 0.0034 is). The chroma gap is not the cap's: uncapped stops differ the same way (navy `700` head `C0.0508` vs main `C0.0812`, `800` `C0.0143` vs `C0.0468`), so it is the U3 envelope's shape, which R26 did not reopen. Revision 39 (owner R27) sets the expected tint to origin/main's hue at about half its chroma, which is what these readings show; the chroma follow-up rides #725 | `nocap` and `pre`: `env lift-36: 0.00 0.00 0.00 0.17 0.59 1.00`, Warning `100:#FBFBFB C0.0000`, `150:#F1F1F1 C0.0000`, navy `850:#313131 900:#202020 950:#111111` all `C0.0000` (neutral greys) |
| Fixtures moved by the cap only | 🟢 | `tonal-legacy.json`: 8 cells, all Warning (perceptual 100 to 200, even 125 to 175). `default-doc-ramps.json`: Success 75 `#ECF0EB` to `#ECF1EB` plus Warning 100 to 200; Success lift -5 puts stop 75 at raw `sd -1.0106`, past the cap, so it moves. `radix-baseline.json`: structural JSON diff prints `changed leaves: 30`, `non-warning: 0`, all `warning/1..5` and `a1..a5` base values in ALL, BRAND_ONLY, COLLIDING; the `a1..a3` leaves moved from `oklch(0 0 0 / x%)` because an alpha decomposition of a neutral step is black at low alpha and of a tinted step is that tint | `nocap` clone: `FAIL  damping-curve  Success default != legacy at stop 75: 2.5145 vs 2.7193`, `FAIL  intensity-legacy  perceptual Warning stop 100: #FBFBFB != fixture #FCFBFA`, `FAIL  radix-refs-values-unchanged  exportRadix(ALL) drifted`, `FAIL  ac003b  drifted from the pinned fixture at palette "Success"`; `head`: all four `pass` |
| Generated assets match their generators | 🟢 | `npm test` in the `head` clone (no `node_modules`) ends `✓ all 49 test files passed`, `6:30.13 total` under other seats' load; `git status --short` afterwards names only my two probe files, so `figma/plugin/ui.html` and `src/ui/describe-mcp-assets.js` are what `gen:figma-ui`/`gen:mcp-assets` write | a stale committed asset would show as `M` after the regeneration step |
| U10-6 hct.js:276 scoped | 🟢 | lines 276 to 279: `this cache and the other two exact-keyed caches in this file (peakC, oklchToCam16Hue) are pure again. That claim covers hct.js only: tonal.js still keeps a bucketed _okL memo on lstar.toFixed(2) ... tracked as #738`; `peakC` keys on `String(hue)` (`:341`), `oklchToCam16Hue` on `target + ":" + cf` (`:372`), `tonal.js:911` reads `const k = lstar.toFixed(2)` | the line at `8f0e08d6` ends `genuinely pure again.` with no scope (diff `-` line) |
| U10-7 no em dash in added tonal.js comments | 🟢 | `git diff origin/main...HEAD -- src/engine/tonal.js \| grep '^+' \| perl -CSD -ne 'print if /\x{2014}/' \| wc -l` = `0` | the same count at `8f0e08d6` is `58` (handoff), and the diff shows 61 U+2014 to hyphen swaps on `-` lines |
| No em dash on any added line | 🟢 | `git diff 8f0e08d6..HEAD -- . ':!.sdlc/plans' \| grep '^+' \| perl -CSD -ne 'print if /\x{2014}/' \| wc -l` = `0` | the fact-sheet `-` line 18 carries four U+2014, so the filter sees the character when present |
| U10-P1 npm test green, tree clean | 🟢 | see the generated-assets row: `✓ all 49 test files passed`, exit 0, clean tree | `pre` engine with the new gate: `FAIL anchor-achromatic`, `1/49` (handoff U10-P1 control, and my `pre` run above) |
| U10-P2 scope wall | 🟢 | `git diff --name-only d1f2704c` prints 14 paths: the three engine files, `anchor.mjs`, the fact sheet, the two generated assets, the handoff, plus six repair-half paths (`.sdlc/baseline.md`, `test/engine/exports.mjs`, `test/engine/tonal.mjs`, three fixtures), each named in the handoff with its cause and a `nocap` control that reds it | a path outside the declared list and the handoff would print as an extra name; none does |
| U10-P3 landing checks, branding | 🟢 | `head` clone: five `.sdlc/checks/*.sh` each exit 0 (`baseline-agrees-check.sh` `stale total: 0`), `ceiling-counts: clean`, `branding: clean (640 files scanned)` | before the baseline repair the check read `STALE ui.html` (handoff); `baseline-agrees-check.sh:16` measures `length / 1024` and the committed file reads `4119.1` there |
| Engines pure and DOM-free | 🟢 | the three engine edits are one guard line, one `Math.max/Math.min` clamp and comments; `grep -n "document\|window" src/engine/okhsl.js src/engine/tonal.js` on the diff's `+` lines = `0` | a DOM reference on an added engine line would print |

## Findings, by severity

1. Resolved. U10-5 first read 🟡 against the revision 37 wording ("each matching the tint origin/main renders": hue met, chroma about half). Owner R27 (revision 39, merged as `65b72746`) accepts origin/main's hue at about half its chroma and routes the chroma to #725, so the row is 🟢 against that wording. The remaining chroma gap also sits on stops the cap never touches (navy 700 and 800), so it is the U3 envelope's shape, consistent with R27 sending it to #725 rather than to this unit.
2. Low. `test/engine/tonal.mjs` damping-curve (a) now caps `uLeg` at 1 to mirror the engine's cap, so the "independent legacy formula" encodes one engine decision. The builder cited it in a comment; the row still reds with the cap reverted (`Success` stop 75), so it discriminates.
3. Note. `chromaEnvelope` now reads a flat `0.20` from stop 50 to 200 under lift -36: every capped stop takes the same edge floor, and the visible chroma gradient across that band comes from `maxChromaInGamut` and the basis blend alone. Expected under R26 ("At the cap a stop takes the 50/950 edge floor"), recorded here so the verifier does not read it as a plateau bug.

verdict: 🟢
