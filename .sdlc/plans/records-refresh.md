---
status: active
ticket: #691
priority: P1
lane: docs
size: S + S + M + S (U1 S = 1 point, U2 S = 1 point, U3 M = 2 points, U4 S = 1 point)
labels: kind:chore · size:S · lane:docs · mode:multi
written: 2026-09-18
amended: 2026-09-18 (U2, C31); 2026-09-19 (U3, re-measure at 20298cc after #706); 2026-09-19 (U4, adapter conflict ids C1 to C13 renamed X1 to X13, approval Q11 second ruling)
head: 746f93d (`plan/records-refresh`, 4 commits over `cf8e61a`, not pushed); rebases onto `origin/main` @ 1ea2f80 before U1
branch: plan/records-refresh
inputs: .sdlc/questions/survey-2026-09-18-approval.md Q2 and Q7, .sdlc/verdicts/survey.md (C9, C11, C13, C15 and its corrections line), .sdlc/survey.md, .sdlc/baseline.md, .sdlc/adapter.md §1 §3, .sdlc/debt.md, .sdlc/architecture.md, .sdlc/verdicts/architecture.md, .sdlc/handoffs/adopt-hygiene-drill-report.md row R18
---

# Make the adoption records true at today's head: rerun the baseline, remove the copied stale numbers, put the two local-only ignore rules in the repo, and mark the two records that were not re-derived

The owner approved the "Small records unit" (approval Q2) and nothing more. `architecture.md` and `debt.md` are not re-derived. The survey, its verdict, and the approval doc are already committed on this branch and land with this plan as one PR.

Amended 2026-09-18 (approval Q7, the Conductor appends it to the approval doc). A second unit, U2, closes the adoption drill's C31 row (`.sdlc/handoffs/adopt-hygiene-drill-report.md` R18, 🟡). The plugin repo's instruction, verbatim: "one unit landed by PR, the A2 map gains a Doc drift section and the A2 verdict grades all 18 conventions per row." U2 does not re-derive the map: §1 to §5 and the recorded cells of §6 stay at `f9e20c5`; it adds one section measured at the rebased head and has the 18 controls rerun and graded one row each. U1 is unchanged by the amendment.

Amended 2026-09-19 (U3, owner ruling relayed by the Conductor). With U1 and U2 merged and 🟢, the branch was rebased from `d814500` onto `origin/main` @ `20298cc` (#706: Node 24, vite 8.3, TypeScript 7). The ruling, in substance: #706 changed the toolchain the baseline describes, so numbers measured at `d814500` are stale on arrival; re-measure §8, Counts and the baseline (three clean `npm test` runs, loads recorded, no concurrent gates) at `20298cc`; keep the `d814500` figures as a labelled prior set; criterion U2-2 then names `20298cc` as both measured head and merge base. A third unit, U3, does that, and also corrects the one §6 row pass 5 graded 🔴 at the rebased head (K17's exception list). U1's and U2's texts are unchanged except the three criterion rows the revision log names.

Amended 2026-09-19 (U4, owner ruling, Q11 in `.sdlc/questions/survey-2026-09-18-approval.md`). The plugin repo's drill-findings U5 adds a `board.py ids` pre-commit check: a `#`-headed table row whose first cell is `C<n>` counts as a criterion definition, and a definition outside `plans/*.md` refuses the commit. `.sdlc/adapter.md` §4 numbers its conflict rows C1 to C13, so once the check is live no commit that touches the adapter (U3's Time cells, a pre-land fix, a non-ff merge that stages it) goes through. U4 renames the thirteen rows to X1 to X13, the prefix plan-rules reserves for an id the adapter mints, and the ten lines in the adapter and three debt notes that cite them; history files keep the old ids and the adapter says so. The four cites in this plan were rewritten by this amendment. U1, U2 and U3 are unchanged except the three criterion rows the revision log names (U1-6, U1-9, U3-7), whose deletion counts the rename would otherwise trip.

Root cause this plan fixes, not only the symptom. The test count went stale because `adapter.md` and `debt.md` each carry a copy of a number that only `baseline.md` measures, and nothing compares the copies. Two test files were added over 16 commits and no record moved. So the copies go, `baseline.md` becomes the one home of measured numbers, and a check script compares what is left against the tree.

Scope wall. Only these paths change: `.sdlc/baseline.md` (U1 whole; U3 whole again, keeping U1's figures as a prior set), `.sdlc/adapter.md` (U1; U3 the three §1 Time cells only; U4 the thirteen §4 row ids, the ten lines that cite them, one appended §4 amendment), `.sdlc/debt.md` (U1: the P3 row and one note; U4: the last cell of rows C5, C6, C7), `.sdlc/architecture.md` (U1: one note; U2: one rerun note, one Counts bullet, one appended `## 8. Doc drift` section; U3: the rerun note, the Counts bullet, §8's head sentence and rows, and the §6 K17 row, all in place), `.sdlc/checks/baseline-agrees-check.sh` (new), `.gitignore`, `.sdlc/verdicts/architecture.md` (U2: one appended `## Pass 5` section, written by the verifier), plus each unit's handoff (`.sdlc/handoffs/records-refresh-U<n>.md`), each unit's verdict (`.sdlc/verdicts/records-refresh-U<n>.md`), and the Orchestrator's board and checklist. Every plant U2 needs under `src/`, `test/`, `figma/`, `mcp/`, `scripts/` happens in a throwaway clone, never in a worktree of this branch. `.claude/CLAUDE.md` does not change: it carries no test count (measured: the stale-count sweep in U1 row 5 finds nothing under `.claude/`), it cites no adapter conflict id (U4-5 measures that), and adapter conflict row X9 (C9 until U4) allows an edit there only inside a unit with a criterion for it. `.sdlc/survey.md` is a dated snapshot, already corrected in 33ee3b1. Verdicts, handoffs, archived plans, tickets, and questions are history and keep their numbers, including the conflict ids they cite in the old form.

Branding rule (adapter conflict row X12, C12 until U4). Nothing this plan writes quotes the retired maker brand or the pre-rename element identifier. No em dash in any added line.

Criteria ids follow the archived plan's shape (P rows for the plan, numbered rows per unit, cited as U1-3), because `C<n>` in this plan already means a survey claim.

## Before U1: rebase (Orchestrator)

`origin/main` moved from `cf8e61a` to `1ea2f80` while the survey was graded. In this worktree: `git fetch origin && git rebase origin/main`. Measured in a throwaway clone on 2026-09-18: clean, 4 commits over `1ea2f80`, every one touching `.sdlc/` only. The branch is not on the remote, so nothing is force-pushed. Then `worktrees.py add U1 --plan records-refresh`. If `origin/main` has moved past `1ea2f80` by then, rebase onto what is there and read every "1ea2f80" below as that sha.

## Criteria (plan-level: the standing gates of adapter §1, run by the builder, again by the verifier, again at pre-land)

Every negative control that edits a file runs in a throwaway clone (`git clone -q --shared . "$CLAUDE_JOB_DIR/tmp/neg"`), never in the unit worktree (the 2026-09-18 survey verdict's practice). In every table cell below `\|` is the table's escape for a plain `|`: type it unescaped. Where a regex needs a literal pipe it is written `[|]`, so no cell depends on which way a backslash is read.

| # | Criterion | Command | Expected | Negative control |
|---|---|---|---|---|
| P1 | `npm test` green with no `node_modules` needed, tree byte-stable | `npm test 2>&1 \| tail -1; git status --short \| wc -l` | `✓ all N test files passed` where N is the `tests:` figure U1-1 prints (46 at plan time), then `0` | in the clone: `sed -i '' 's/"scrim/"scrimX/' docs/reference/data/role-table.json && npm test 2>&1 \| grep -c FAIL` prints a non-zero count (3 in the adopt-hygiene verdicts) |
| P2 | `npm run build` green, prints the size the baseline records, tree clean | `npm ci >/dev/null 2>&1; npm run build > "$CLAUDE_JOB_DIR/tmp/build.log" 2>&1; echo "exit $?"; grep -o 'ui.html [0-9.]* KB' "$CLAUDE_JOB_DIR/tmp/build.log"; grep -o 'ui.html [0-9.]* KB' .sdlc/baseline.md; git status --short \| wc -l` | `exit 0`, then the same `ui.html NNNN.N KB` string twice, then `0` | in the clone with no `node_modules`: exit 127, `tsc: command not found`. At 746f93d the two strings differ (`3753.6` built, `3695.6` recorded) |
| P3 | `npm run smoke` green (the rewritten baseline claims it at this head) | `npm run smoke 2>&1 \| grep -c 'SMOKE PASS'` | `1` | in the clone with no `dist/`: `node test/smoke/smoke.mjs` exits 1 with `smoke: missing dist/ultimate-tokens.html` |
| P4 | branding gate clean | `bash -c 'set -o pipefail; node test/repo/branding.mjs \| tail -1; echo "exit $?"'` | `clean (N files scanned)`, exit 0 | in the clone: `cp docs/reference/references/decision-records.md docs/x.md && node test/repo/branding.mjs \| tail -1` prints FAIL (the RECORDS exemption is by path) |
| P5 | scope wall: nothing outside `.sdlc/` and `.gitignore` differs from the merge base, so `.claude/CLAUDE.md`, `src/`, `test/`, `scripts/`, `mcp/`, `figma/` are untouched. U1's, U2's, U3's and U4's paths all sit under `.sdlc/` (the wall above), so the command is the same for every unit; U2's 18 plants are the case the wall exists for, and they run in the clone, never here | `git diff --name-only $(git merge-base origin/main HEAD) \| grep -vcE '^\.sdlc/\|^\.gitignore$'` | `0` | `echo "// probe" >> src/engine/motion.mjs` in the clone and rerun: `1` (the command reads the working tree, so an uncommitted edit shows; a K1 plant left behind in the worktree shows the same way) |

## Units

- [x] U1 (S) rerun the baseline, remove the copied numbers, add the two ignore rules, state C11's scope, add the two staleness notes · grade l2 · reviewer-l1 · verifier-l1
- [x] U2 (S) the C31 shape: `## 8. Doc drift` measured at the head, a rerun note, the 18 controls rerun by the builder (handoff table) and graded one row each by the verifier (`verdicts/architecture.md` pass 5) · grade l2 · reviewer-l1 · verifier-l1 · after U1 merges
- [x] U3 (M) re-measure at 20298cc after #706: three clean runs each of `npm test`, `npm run build`, `npm run smoke` with loads recorded, the `d814500` figures kept as a labelled prior set, §8 re-checked row by row and the Counts bullet redone at the head, the adapter's time ranges re-derived, the 18 controls rerun (handoff), K17's exception list corrected · grade l2 · reviewer-l1 · verifier-l1 · after U2 merges
- [~] U4 (S) rename the adapter's thirteen conflict ids C1 to C13 to X1 to X13, the ten adapter lines and three debt notes that cite them, one dated §4 amendment carrying the map; the plugin's `board.py ids` check and its pre-commit hook run against the tree as the proof · grade l1 · reviewer-l1 · verifier-l1 · after U3 merges (first, if the plugin's check goes live before U3's merge; see U4 §Dependency)

Grade. The Orchestrator's table puts docs and config at l1, and `debt.md` grades every docs row L1. This unit goes one up, on the table's own evidence rule: it touches six files, its core job is transcribing nine measured runs without error, and three of the four l1 records units in adopt-hygiene needed a second pass. l2 keeps the l1 checker pair (reviewer-l1, verifier-l1). The reviewer dispatch names `.claude/agents/change-reviewer-agent.md` §What to check (adapter conflict row X2, C2 until U4).

Grade, U2. Docs, so l1 by the table; one up to l2 for the same reason as U1: the unit transcribes 18 control runs and 18 plants without error and reads two docs against code, where a plausible guess is the failure mode the cartographer's rules forbid ("unknown stays unknown"). Checker pair stays reviewer-l1, verifier-l1; the verifier is opus, so it does not share a family with the sonnet builder, as the table requires.

Grade, U3. Docs, so l1 by the table; one up to l2 for the reason U1 and U2 give, compounded: the unit is U1's nine transcribed gate runs plus U2's 56 row re-checks and 18 control reruns, with no authoring beyond correcting a state or an exception list, and its failure mode is a sha swapped without a re-check (U3-4's control shows a global substitution passes the sha grep, so the handoff carries one row per DD and the verifier reruns the rows that cite a #706 path). S by the table (one subsystem, `.sdlc/` records, no design judgment), though at the top of S: the nine runs alone are about six minutes of wall time plus `npm ci`. Checker pair stays reviewer-l1, verifier-l1; the verifier reruns the three gates once itself as the sanity read the Risks row describes and grades every U3 row.

Grade, U4. Docs, l1 by the table, and it stays l1: the unit is a thirteen-row id rename plus thirteen cite tokens with no measuring and no authoring beyond one amendment and three note cells, and U4-3 proves completeness by construction (mapping X back to C reproduces the base file), so the failure mode the l2 bump exists for, a transcription error nobody can see, has a mechanical catch here. Checker pair reviewer-l1, verifier-l1 as for the other units.

### U1 (S, grade l2)

Order matters, because the baseline must describe the tree before this unit edits it.

1. In the fresh unit worktree, which has no `node_modules`: note `uptime` and `sysctl -n hw.ncpu`. If the 1-minute load is above the core count (it was 19 on 10 cores when this plan was written), tell the Orchestrator and wait for a quiet slot. Timings taken under load are not a baseline.
2. `npm test` three times in sequence. After each: exit code, wall seconds (`/usr/bin/time -p`), the runner's last line, `git status --short | wc -l`.
3. `npm ci` once, then `npm run build` three times, then `npm run smoke` three times, recording the same four facts plus the `wrote figma/plugin/ui.html NNNN.N KB` line and the `SMOKE PASS` line.
4. Every run made is recorded. No run is dropped or retried away. A red run stops the unit: triage it with `flaky-gates`, write it under Fail or Flaky, and send the Orchestrator a question, because a baseline with a Fail row changes what green means for every later plan.
5. `gh run list --branch main --workflow ci.yml --limit 5 --json databaseId,headSha,conclusion` and take the run whose `headSha` is the `origin/main` sha the branch was rebased onto (35342720776 for `1ea2f80`, measured 2026-09-18).
6. Create `.sdlc/checks/baseline-agrees-check.sh` from the block below, byte for byte. Then make the edits in §Texts. Then run U1 rows 1 to 9 and P1, P4, P5.

The check script (written and measured by the planner on 2026-09-18; outputs are in the U1-1 control cell):

```sh
# The numbers in .sdlc/baseline.md agree with the tree they describe, and the adapter's
# time ranges agree with the baseline. Reads files and git only: no node_modules, no network.
# Usage: sh .sdlc/checks/baseline-agrees-check.sh   (from the repo root)
# A STALE head line on a later commit is expected: it says the tree moved since the baseline ran,
# so the numbers are unproven at that head, not that they are wrong.
node - <<'EOF'
const fs = require("fs"), cp = require("child_process");
const read = (f) => fs.readFileSync(f, "utf8");
const b = read(".sdlc/baseline.md"), a = read(".sdlc/adapter.md");
let stale = 0;
const say = (ok, line) => { if (!ok) stale++; console.log((ok ? "ok    " : "STALE ") + line); };
const row = (txt, head) => (txt.split("\n").find((l) => l.startsWith(head)) || "").split("|").map((s) => s.trim());
const tests = ((read("test/run.mjs").match(/const TESTS = \[([\s\S]*?)\];/) || ["", ""])[1].match(/"[^"]+\.mjs"/g) || []).length;
const bn = (row(b, "| `npm test` |")[5] || "").match(/all (\d+) test files passed/);
say(!!bn && +bn[1] === tests, `tests: baseline ${bn ? bn[1] : "none"}, test/run.mjs TESTS ${tests}`);
const kb = (read("figma/plugin/ui.html").length / 1024).toFixed(1);
const bs = (row(b, "| `npm run build` |")[5] || "").match(/ui\.html ([\d.]+) KB/);
say(!!bs && bs[1] === kb, `ui.html: baseline ${bs ? bs[1] : "none"} KB, tree ${kb} KB`);
for (const [cmd, gate] of [["npm test", "test"], ["npm run build", "build"], ["npm run smoke", "smoke"]]) {
  const t = (row(b, "| `" + cmd + "` |")[4] || "").split("·").map(Number);
  const m = (row(a, "| " + gate + " |")[5] || "").match(/(\d+) to (\d+) s/);
  const lo = Math.round(Math.min(...t)), hi = Math.round(Math.max(...t));
  say(t.length === 3 && t.every(Number.isFinite) && !!m && +m[1] === lo && +m[2] === hi,
    `time ${gate}: baseline ${lo} to ${hi} s, adapter ${m ? m[1] + " to " + m[2] + " s" : "none"}`);
}
const ref = (b.match(/^ref: .*@ ([0-9a-f]{7,40})\b/m) || [])[1];
let same = false, onMain = false;
if (ref) {
  try { cp.execSync(`git diff --quiet ${ref} HEAD -- . ":(exclude).sdlc" ":(exclude).gitignore"`, { stdio: "ignore" }); same = true; } catch (e) {}
  try { cp.execSync(`git merge-base --is-ancestor ${ref} origin/main`, { stdio: "ignore" }); onMain = true; } catch (e) {}
}
say(same, `head: baseline ref ${ref || "none"} has the same tree as HEAD outside .sdlc/ and .gitignore`);
say(onMain, `head: baseline ref ${ref || "none"} is in origin/main's history`);
console.log(`stale total: ${stale}`);
process.exit(stale ? 1 : 0);
EOF
```

#### Texts

`.sdlc/baseline.md` is rewritten whole, same sections and table shape as today. Front matter: `ran:` the run date; `ref: origin/main @ <the sha the branch was rebased onto>`; `host:` as today plus the load figures from step 1; a new line `supersedes: the 2026-09-16 baseline (git show 180eca0:.sdlc/baseline.md)`. The `ref` is a `main` sha on purpose: a plan-branch sha dies at the squash, which is how `architecture.md` and `debt.md` came to cite a head that is not in `main`'s history. The intro sentence says the runs happened in the unit worktree at the rebased plan head, whose tree equals the `ref` outside `.sdlc/` (U1-1 proves it). The Pass table carries the three new timings per command (`a · b · c`, the separator the script splits on), `all N test files passed`, and `wrote figma/plugin/ui.html NNNN.N KB` exactly as printed. The sentence under the table becomes:

```text
`git status --short` empty after every run: every committed asset that `npm test` and `npm run build` regenerate is byte-stable at this head. `src/ui/type-fonts.js` is not in that set. Neither chain runs `gen:type-fonts`, so a clean status says nothing about that file (survey verdict C11 of 2026-09-18, debt G1).
```

The Not-run-here line names the new CI run id and the same sha as `ref`, in the words `CI run <id> on <sha7>`. The old head, the old size, and the old run id appear nowhere in the file.

`.sdlc/adapter.md`, in-place edits in the §1 gate table only (four lines at most):

- Header cell `Time (baseline, main @ 7faf3aa)` becomes ``Time (rounded from `.sdlc/baseline.md`)``.
- The three Time cells become `A to B s` (test, smoke) and `A to B s warm` (build), where A and B are the rounded minimum and maximum of that command's three baseline timings. A cell whose range did not move stays as it is.
- In the test row's Green-means cell, the text from the space before `(N = 44 at baseline` through `(C13, CI drift gate)` becomes (so the cell reads ``passed`, where N is``):

```text
, where N is the length of `TESTS` in `test/run.mjs` (the last measured N is in `.sdlc/baseline.md`; a unit that adds a test file raises N and must register it in TESTS, K17). Then `git status --short` is empty: every asset `npm test` regenerates is byte-stable (survey C11 of 2026-09-18, CI drift gate). `src/ui/type-fonts.js` is outside that set, because `npm test` never runs `gen:type-fonts`; the fonts row below is its gate
```

The adapter carries no literal N afterwards. The verdict's correction asked for "46"; a second copy of the number is what went stale, so the cell names the source instead (measured: with this edit the row still parses and U1-6 prints `0` outside-table deletions).

`.sdlc/adapter.md`, two appended amendments, in the file's own `**Amendment (date).**` shape. After the paragraph that ends "where each fact was proved." and before `## 1. Gates`:

```text
**Amendment (2026-09-18).** `.sdlc/baseline.md` was rerun and is the only record that carries measured gate numbers. This file points at it and copies only the rounded time ranges, which `sh .sdlc/checks/baseline-agrees-check.sh` compares against it. `.sdlc/architecture.md` and `.sdlc/debt.md` were not re-derived and still cite `f9e20c5`; each opens with a staleness note, and a plan that touches a path they cite re-checks the citations it relies on at its own head (the note gives the command). Survey claim ids cited in this file and in `debt.md` (C1 to C14) are the 2026-09-16 grading's, readable with `git show 180eca0:.sdlc/verdicts/survey.md`. The 2026-09-18 grading renumbered them: byte-stability moved from C13 to C11, and C13 is now the local-only ignore rules.
```

At the end of §3, after the second 2026-09-17 amendment:

```text
**Amendment (2026-09-18).** `.gitignore` now carries `.claude/settings.local.json` and `.sdlc/launcher.env`. Before this the first was ignored only by one machine's user-global git ignore file and the second only by one clone's `.git/info/exclude`, so a fresh clone protected neither (survey verdict C13 of 2026-09-18). The local rules stay where they are and are now redundant.
```

`.gitignore`, appended at the end:

```text

# local-only files that must never reach a commit: per-user Claude Code settings, the sdlc launcher environment
.claude/settings.local.json
.sdlc/launcher.env
```

`.sdlc/debt.md` row P3, in place: the item cell becomes ``` `npm test` runs every `TESTS` entry serially as child processes, about a minute per run ``` and the where cell becomes ``` `test/run.mjs:23-25`; count and timings in `.sdlc/baseline.md` Pass ```. No other row changes.

`.sdlc/debt.md`, one paragraph after the intro paragraph and before `## Hot untested`:

```text
Staleness note (graded 2026-09-18). This map was built at `f9e20c5`, the head of `sdlc/adopt`. That commit is not in `main`'s history: the branch landed squashed as `180eca0` (PR #653), and the object is reachable only through `origin/sdlc/adopt`. The survey verdict of 2026-09-18 (`.sdlc/verdicts/survey.md` C15, graded at `cf8e61a`) found 16 of the 96 paths this file cites edited in `180eca0..cf8e61a`, and `main` has moved again since, so a line-anchored citation into an edited path is unverified at any later head. The map was not re-derived (owner ruling, `.sdlc/questions/survey-2026-09-18-approval.md` Q2). Rule: a plan that touches a path this file cites, or sizes a unit from a row here, re-checks each citation it relies on at its own head and records the result in the plan. `git diff --stat f9e20c5 HEAD -- <path>` prints nothing when the cited file is unchanged since this map was written. Where the `f9e20c5` object is absent, `git log --oneline 180eca0..HEAD -- <path>` is the fallback, and a weaker one: `180eca0` already differs from `f9e20c5` in ten files under `src/`, `test/` and `figma/`. Survey claim ids cited here (C5, C6, C8, C13) are the 2026-09-16 grading's, readable with `git show 180eca0:.sdlc/verdicts/survey.md`; the 2026-09-18 grading renumbered them.
```

`.sdlc/architecture.md`, one paragraph after the "Recovered from code at f9e20c5." paragraph and before `## 1.`: the same text with three changes. "This map was built at" becomes "This map was recovered at"; "16 of the 96" becomes "18 of the 72"; "or sizes a unit from a row here" becomes "or relies on a claim or a K control here". The last sentence (survey claim ids) is dropped, because this file cites none.

#### Criteria

| # | Criterion | Command | Expected | Negative control |
|---|---|---|---|---|
| 1 | the baseline's test count, bundle size, and cited head agree with the tree, and the adapter's time ranges agree with the baseline | `sh .sdlc/checks/baseline-agrees-check.sh; echo "exit $?"` | seven `ok` lines, `stale total: 0`, `exit 0` | measured at 746f93d, script run from a scratch copy: `STALE tests: baseline 44, test/run.mjs TESTS 46`, `STALE ui.html: baseline 3695.6 KB, tree 3753.6 KB`, `STALE head`, `stale total: 3`, exit 1. Measured in a clone rebased onto 1ea2f80 with the three numbers fixed (46, 3763.2, ref 1ea2f80): `stale total: 0`; one timing changed to `99.9` prints `STALE time test: baseline 58 to 100 s, adapter 58 to 62 s`; a probe commit under `src/` prints `STALE head`; a size taken from the byte count instead of the build line (3798.0) prints `STALE ui.html` |
| 2 | the script is the plan's text, so a builder cannot pass row 1 by weakening it | `diff <(awk '/^\140\140\140sh$/{f=1;next} /^\140\140\140$/{f=0} f' .sdlc/plans/records-refresh.md) .sdlc/checks/baseline-agrees-check.sh && echo same` (`\140` is awk's octal for a backtick; the plan has exactly one `sh` fence) | `same` | change one character of the script: `diff` prints the hunk and `same` is absent. Before the unit the file does not exist and `diff` errors |
| 3 | the baseline is a full rerun: three runs of three commands, all exit 0, and no figure of the old baseline left | `grep -cE '^[\|] .npm (test\|run build\|run smoke). [\|] 3/3 [\|] 0 [\|]' .sdlc/baseline.md; grep -cE '7faf3aa\|34804556354\|3695\.6' .sdlc/baseline.md` | `3`, then `0` | at 746f93d: `3`, then `4`. A red run cannot produce `3/3`, so it fails the first count and the unit stops with a question (step 4) |
| 4 | the cited CI run is green on the cited head, and that head is the `main` commit the branch sits on | `git merge-base origin/main HEAD \| cut -c1-7; grep -oE '^ref: .*@ [0-9a-f]+' .sdlc/baseline.md \| grep -oE '[0-9a-f]+$' \| cut -c1-7; gh run view "$(grep -oE 'CI run [0-9]+' .sdlc/baseline.md \| head -1 \| grep -oE '[0-9]+')" --json headSha,jobs --jq '[.headSha[0:7], (.jobs[] \| select(.name=="build-test" or .name=="panda-smoke") \| .name + "=" + .conclusion)] \| join(" ")'` | the same seven-character sha three times, the third followed by `build-test=success panda-smoke=success` (for run 35342720776: `1ea2f80 build-test=success panda-smoke=success`, measured) | measured at 746f93d: `cf8e61a`, `7faf3aa`, `7faf3aa build-test=success panda-smoke=success`. The old baseline agrees with its own run, so the first line is what bites: the merge base is not the head the baseline cites. Run 34660177977 prints `build-test=failure`, so the jobs query can report red |
| 5 | no live record outside the baseline carries a test count (the "find them" sweep: `.sdlc`, `.claude`, `README.md`; history dirs and the dated survey excluded) | `git grep -nE '[0-9]+ test files\|runs [0-9]+ files\|N = [0-9]+ at' -- .sdlc .claude README.md ':!.sdlc/verdicts' ':!.sdlc/handoffs' ':!.sdlc/plans' ':!.sdlc/tickets' ':!.sdlc/questions' ':!.sdlc/survey.md' \| cut -d: -f1` | one line, `.sdlc/baseline.md` | measured at 746f93d: three lines, `.sdlc/adapter.md`, `.sdlc/baseline.md`, `.sdlc/debt.md`. Nothing under `.claude/` at either head, which is why CLAUDE.md stays out of scope |
| 6 | adapter: in-place edits stay inside the §1 gate table, no literal N and no old head remain, the two amendments sit where §Texts puts them (amended 2026-09-19 for U4: the first command maps X1 to X13 back to C1 to C13 before counting, so the conflict-id rename is not a deletion; it read `git diff $MB -- .sdlc/adapter.md \| grep -E '^-[^-]' \| grep -vcE '^-[\|] (Gate\|test\|build\|smoke) [\|]'` when U1 was graded and prints `23` on a U4 tree, measured) | `MB=$(git merge-base origin/main HEAD); diff <(git show $MB:.sdlc/adapter.md) <(perl -pe 's/(?<![A-Za-z0-9-])X([1-9]\|1[0-3])(?![0-9a-z])/C$1/g' .sdlc/adapter.md) \| grep -E '^<' \| grep -vcE '^< [\|] (Gate\|test\|build\|smoke) [\|]'; grep -cE 'N = [0-9]+\|7faf3aa' .sdlc/adapter.md; awk '/^# Adapter/,/^## 1\. Gates/' .sdlc/adapter.md \| grep -c 'Amendment (2026-09-18)'; awk '/^## 3\. /,/^## 4\. /' .sdlc/adapter.md \| grep -c 'Amendment (2026-09-18)'` | `0`, `0`, `1`, `1` | at 746f93d the last three print `2`, `0`, `0`. For the first: measured in a clone, one reworded §4 row (C12 "scans" to "walks") prints `1` |
| 7 | C11's real scope is stated where the records claim byte-stability, and the statement is true | `grep -E '^[\|] test [\|]' .sdlc/adapter.md \| grep -c 'gen:type-fonts'; grep -c 'type-fonts' .sdlc/baseline.md; node -e 'const s=require("./package.json").scripts; console.log(/gen:type-fonts/.test(s.test), /gen:type-fonts/.test(s.build), /gen:categories/.test(s.test))'` | `1`, `1` or more, `false false true` | at 746f93d the first two print `0`, `0`. The third `true` shows the same regex shape does see a generator that is in the chain; if a later change adds `gen:type-fonts` to either script the first two values flip and both records become false |
| 8 | both local-only rules come from the repo's `.gitignore`, and neither file is tracked | `for p in .claude/settings.local.json .sdlc/launcher.env; do git check-ignore -v "$p" \| cut -f1 \| cut -d: -f1; done; git ls-files .claude/settings.local.json .sdlc/launcher.env \| wc -l` | `.gitignore` twice, then `0` | at 746f93d in this checkout: the user-global ignore path and the `.git/info/exclude` path. Measured in a fresh clone at 746f93d: the `launcher.env` line is missing altogether. Measured with the rule in both `.gitignore` and `info/exclude`: `.gitignore` wins. `git check-ignore -v README.md` exits 1, and `git ls-files .claude/settings.json \| wc -l` prints `1`, so neither command matches everything |
| 9 | each not-re-derived record opens with one dated staleness note that quotes the verdict faithfully, states true facts, and carries the re-check rule; nothing else in either file changed except debt row P3 (amended 2026-09-19: and the §6 K17 row, U3's one in-place edit to a pre-plan line, excluded the way P3 is; the command read `grep -cE '^-[^-]'` on `architecture.md` when U1 and U2 were graded; amended again 2026-09-19 for U4: the debt count also excludes rows C5, C6, C7, whose last cells U4 rewrites, and read `grep -vc '^-[\|] P3 [\|]'` before) | `for f in architecture debt; do awk '/^## /{exit} /^Staleness note \(graded 2026-09-18\)\./{n++} END{print n+0}' .sdlc/$f.md; grep -c 'git diff --stat f9e20c5 HEAD' .sdlc/$f.md; done; grep -c '18 of the 72' .sdlc/architecture.md; grep -c '16 of the 96' .sdlc/debt.md; grep -c '18 of the 72.*16 of the 96' .sdlc/verdicts/survey.md; git merge-base --is-ancestor f9e20c5 origin/main; echo "ancestor $?"; git diff --name-only f9e20c5 180eca0 -- src test scripts mcp figma \| wc -l; MB=$(git merge-base origin/main HEAD); git diff $MB -- .sdlc/architecture.md \| grep -E '^-[^-]' \| grep -vc '^-[\|] K17 [\|]'; git diff $MB -- .sdlc/debt.md \| grep -E '^-[^-]' \| grep -vcE '^-[\|] (P3\|C[5-7]) [\|]'` | four lines of `1` (note count and rule count, per file), then `1`, `1`, `1`, `ancestor 1`, `10`, `0`, `0` | at 746f93d the four note counts and the two quote counts print `0`. `git merge-base --is-ancestor 180eca0 origin/main` prints `ancestor 0`, so the ancestry test can say yes. The rule's own command is not vacuous, measured: `git diff --stat f9e20c5 HEAD -- src/engine/tonal.js` prints `113 insertions(+), 18 deletions(-)` and the same on `src/engine/hct.js` prints nothing |

### U2 (S, grade l2): the C31 shape

What C31 measures (`sdlc-orchestration/.sdlc/plans/readiness.md` §C31, run against this repo in drill row R18): the map has the cartographer's seven sections (`Module map`, `Runtime topology`, `Persistence`, `Cross-cutting`, `Conventions`, `Load-bearing`, `Doc drift`, matched case-insensitively on `^## .*<word>`), every convention row carries a command, and the verdict has one graded row per convention. R18's Substitutions cell names the three gaps: `Module map` is spelled `Layering and dependency direction` (§1), `Doc drift` is absent (the drift table sits in `debt.md`), and the verdict grades the 18 conventions in one counted row. Measured at cfd0ff4 by the planner: the section grep prints `5`, the convention count prints `18 18`, the pass-5-scoped row count prints `0`. U2 closes the second and third gap. The first stays a recorded substitution: renaming §1 would be a deleted line in `architecture.md`, and U1-9 holds that file to zero deletions against the merge base.

Where the drift table lives, decided: `.sdlc/debt.md` §Records drift stays where it is and is not mirrored. It is the A3 artifact (ADRs, specs, LLDs, plans checked against code, from `decisions.md`'s Human answers), the plugin's own diagnosis calls it "a different artifact" (`readiness-u6-diagnosis.md` §2), and the A7 candidates list keys off its R ids. The cartographer's Doc drift is a narrower thing: claims in `README.md`, `.claude/CLAUDE.md`, `AGENTS.md` or docs that the code contradicts, each with a code citation (`agents/cartographer.md` step 3 and Rules). So §8 is a new table with `DD` ids, measured at the rebased head, and it opens with one sentence pointing at `debt.md` §Records drift for the records-vs-code table. `AGENTS.md` does not exist here (`git ls-files AGENTS.md` is empty), and §8 says so. Nothing is copied from `debt.md`: every row quotes the doc line as it reads at the head, and U2-3 checks the quote against the file.

Division of labour (the verifier writes verdict rows, the builder does not):

| Seat | Produces | Not |
|---|---|---|
| builder (l2) | `## 8. Doc drift` and one Counts bullet in `.sdlc/architecture.md`; the rerun-note paragraph; `.sdlc/handoffs/records-refresh-U2.md` with the 18-row evidence table (its own run of every K control at the head, its own plant per row, the map's recorded cell and the delta) | anything under `.sdlc/verdicts/`; any edit to §1 to §7 beyond the Counts bullet; any fix to a violation a control finds |
| reviewer-l1 | the usual fresh-context read of the diff against rows 1 to 6 | running the 18 controls (the verifier does) |
| verifier-l1 | `.sdlc/verdicts/records-refresh-U2.md` (rows 1 to 6, P1, P4, P5, one negative control each); `## Pass 5` appended to `.sdlc/verdicts/architecture.md`: 18 rows K1 to K18, each its own run at the head and its own plant in a throwaway clone, the handoff table used as the claim under test, never as the evidence | grading row 7 (its own pass 5; pre-land does) |
| Orchestrator | commits pass 5 on `plan/records-refresh` with `Seat: orchestrator` after the U2 merge and before pre-land; the board row; the U2 worktree off the plan branch only after U1's merge commit is on it | |
| pre-land reviewer | row 7 and the C31 block (row 8), because the verifier cannot grade a file it wrote | |

How U1 and U2 compose in `architecture.md`. U1 inserts the staleness note (one paragraph after "Recovered from code at f9e20c5.", before `## 1.`); U2 inserts a second paragraph directly after it, appends `## 8.` at the end of the file and one bullet at the end of §7. Three disjoint regions, so the merge is textual. The rule that composes them: U1's note says the map was not re-derived and that a plan relying on a claim or a K control re-checks it at its own head; U2's rerun note is that rule applied once, for all 18 controls, and names where the result lives. U2 does not edit U1's paragraph (U1-9 counts deletions in this file and expects `0`; a reworded line would count). Where a pass 5 result differs from a §6 cell, pass 5 is the current reading and the cell is history; the note says so.

Steps, in order:

1. Worktree `.worktrees/rr-U2` on `unit/rr-U2` from `plan/records-refresh` only after U1's merge commit is on the branch. First: `sh .sdlc/checks/baseline-agrees-check.sh` prints `stale total: 0` and `awk '/^## /{exit} /^Staleness note \(graded 2026-09-18\)\./{n++} END{print n+0}' .sdlc/architecture.md` prints `1`. Either other value: stop, U1 is not in, or the branch moved and U1-1 must rerun first (Risks).
2. `HEAD7=$(git merge-base origin/main HEAD | cut -c1-7)`. This equals the `ref` sha in `.sdlc/baseline.md` (U1-4 proved it) and is the "measured at" sha for §8, the rerun note, the handoff and pass 5. `841e185` at amendment time.
3. Clone: `git clone -q --shared . "$CLAUDE_JOB_DIR/tmp/u2"`. Run every K control of §6 and every §6.1 script at the head, read-only ones in the worktree, K7 and K9 (they write) and every plant in the clone, `git reset --hard && git clean -fdx` between plants. One handoff row per control: `| K<n> | own HEAD result | map cell (f9e20c5) | delta | plant | hits |`. Planner's read at cfd0ff4 (tree equal to 841e185 outside `.sdlc/`): the 15 read-only controls print what the map recorded (K1 to K6 `0`; K10 6 raw; K11 `0 0 0` and `html:` 6+3+3; K12 `0`, 4, 4; K13 `0`; K14 the three `lc-` lines; K15 `0`, `0`, selftest PASS; K16 `0`; K17 `0`, `0`; K18 silent at 4). K7 will print a KB figure other than `3692.6` (the bundle grew; the baseline's build line has the current number, and the delta cell says so). K8's `test/figma/binder.mjs` and K9 were not run by the planner.
4. A control that prints a hit not covered by its exceptions cell is a real drift at the head. Record it in the handoff, do not fix it (the wall), message the Orchestrator; the pass 5 row is 🔴 and the plan decides whether it is a new debt row or a stop.
5. Sweep the two docs: every checkable claim in `README.md` and `.claude/CLAUDE.md` (a number, a path, a script name, a "never / only / all" statement), each checked against code at the head, one `DD` row each, state `drifted`, `holds` or `undetermined` (with what was looked at). The seven seeds below must each have a row, because §1 to §6 already lean on them; more rows are expected. Quotes are a verbatim fragment of one line, 20 characters or more, cut short of any em dash (the doc has them; the plan's branding rule forbids adding one).
6. Write §Texts, run rows 1 to 6 and P1, P4, P5, hand off.

#### Texts

`.sdlc/architecture.md`, a paragraph directly after U1's staleness note and before `## 1.`:

```text
Rerun note (U2 of plan records-refresh, <run date>). The 18 controls of §6 and §6.1 were rerun at `<HEAD7>` (the `origin/main` head `.sdlc/baseline.md` cites) and graded one row each in `.sdlc/verdicts/architecture.md` pass 5; §8 was measured at the same head. The claims of §1 to §5 and the recorded HEAD and bite cells of §6 stay as written at `f9e20c5`. Where a pass 5 result differs from a §6 cell, pass 5 is the current reading and the cell is history.
```

`.sdlc/architecture.md` §7, one bullet appended: ``- Doc drift (§8, measured at `<HEAD7>`): N rows, D drifted, H hold, U undetermined.`` with the real counts.

`.sdlc/architecture.md`, appended after §7 as the last section:

```text
## 8. Doc drift

Measured at `<HEAD7>` by U2 of plan records-refresh (#691). Claims in `README.md` and `.claude/CLAUDE.md` checked against the code at that head; `AGENTS.md` does not exist (`git ls-files AGENTS.md` is empty). Records-vs-code drift (ADRs, specs, LLDs, plans) is a different table and stays in `.sdlc/debt.md` §Records drift. Each doc cell quotes the doc line verbatim with its line number; each code cell cites the file, with a line where one applies, that confirms or contradicts it; `undetermined` names what was looked at.

| id | doc claim | code | state |
|---|---|---|---|
| DD1 | `<doc>:<line>` "<verbatim fragment>" | `<path>:<line>` <what it says> | drifted / holds / undetermined: <one line> |
```

Seeds (one row each, the doc phrase the row must contain): `gen:type-fonts` (CLAUDE.md Commands, `test`/`build` run all but it; P7 and U1-7), `do not hand-edit` (CLAUDE.md Layout; K9), `DOM-free` (CLAUDE.md Conventions; K1), `12 live attributes` (CLAUDE.md Conventions; K11), `info/exclude` (CLAUDE.md Always; K15, and the C13 shape: a fresh clone has no such line), `interchange-only` (README format list; R3's README half), `53-role` (README; K8).

`.sdlc/handoffs/records-refresh-U2.md`: the unit handoff in the adopt-hygiene shape, plus a table headed `| K | own HEAD result | map cell | delta | plant | hits |` with 18 rows `| K1 |` to `| K18 |` and no empty cell, and a line `measured at <HEAD7>`.

`.sdlc/verdicts/architecture.md`, appended by the verifier:

```text
## Pass 5 (U2 of plan records-refresh, per convention)

Graded <date> at `<HEAD7>` in a throwaway clone; every control and every plant is this verifier's own run. The handoff table (`.sdlc/handoffs/records-refresh-U2.md`) is the claim under test, not evidence.

| # | Convention | State | Evidence | Negative control |
|---|---|---|---|---|
| K1 | Engines touch no DOM or browser globals | 🟢 | own run: 0; map cell: 0 | own plant `const x = document.body;` in tonal.js: 1 |
```

One row per convention, K1 to K18, no empty cell, a state emoji in each. A row whose control printed a hit outside its exceptions is 🔴 with the hit quoted. Pass 5 replaces the single counted row of pass 1 as the reading of criterion "each convention has a negative control run and exceptions listed"; passes 1 to 4 stay as history.

#### Criteria

Rows 1 to 6 are graded by the U2 verifier; rows 7 and 8 by pre-land review (the verifier wrote what they read). `F` is `$CLAUDE_JOB_DIR/tmp`. Values marked "at cfd0ff4" were measured by the planner on 2026-09-18 in the survey-refresh worktree; planted values in a throwaway clone of it.

| # | Criterion | Command | Expected | Negative control |
|---|---|---|---|---|
| 1 | the map has the Doc drift section, once, last, and C31's section grep now counts it | `grep -c '^## 8\. Doc drift' .sdlc/architecture.md; awk '/^## 8\. Doc drift/{f=1;next} f&&/^## /{print "NOT LAST"}' .sdlc/architecture.md; for s in 'Module map' 'Runtime topology' 'Persistence' 'Cross-cutting' 'Conventions' 'Load-bearing' 'Doc drift'; do grep -qi "^## .*$s" .sdlc/architecture.md && echo -n "$s\|"; done \| tr '\|' '\n' \| grep -c .` | `1`, nothing, `6` (`Module map` stays the recorded substitution for §1) | at cfd0ff4: `0`, nothing, `5`. A copy with `## 9. Notes` appended after §8 prints `NOT LAST` |
| 2 | §8, the rerun note and the Counts bullet all name the same head, and it is the merge base and the baseline's `ref` | `H=$(git merge-base origin/main HEAD \| cut -c1-7); echo $H; grep -oE '^ref: .*@ [0-9a-f]+' .sdlc/baseline.md \| grep -oE '[0-9a-f]{7}'; awk '/^## 8\. Doc drift/,0' .sdlc/architecture.md \| grep -oE 'Measured at .[0-9a-f]{7}' \| grep -oE '[0-9a-f]{7}$'; awk '/^## /{exit} /^Rerun note \(/' .sdlc/architecture.md \| grep -oE 'rerun at .[0-9a-f]{7}' \| grep -oE '[0-9a-f]{7}$'; awk '/^## 7\. Counts/,/^## 8\./' .sdlc/architecture.md \| grep -oE 'measured at .[0-9a-f]{7}' \| grep -oE '[0-9a-f]{7}$'` | the same seven-character sha five times. Amended 2026-09-19: that sha is `20298cc`, the merge base after the rebase onto #706 and the head U3 measures at (it was `841e185` at amendment time and `d814500` when U2 was graded 🟢; the row is re-read after U3, not re-graded for U2) | at cfd0ff4 lines 3 to 5 are empty. A copy with the §8 sha changed to `f9e20c5` prints a different third line: the section was measured at a head that is not this tree. Measured at d46ae48a, after the rebase and before U3: `20298cc` then `d814500` four times, red until U3 lands |
| 3 | every §8 row has the shape, its quote is on the cited doc line at HEAD, its cited code paths are tracked, its state is one of three; the seven seeds each have a row | `sh .sdlc/checks/doc-drift-rows-check.sh; echo "exit $?"; S=$(awk '/^## 8\. Doc drift/,0' .sdlc/architecture.md); for p in 'gen:type-fonts' 'do not hand-edit' 'DOM-free' '12 live attributes' 'info/exclude' 'interchange-only' '53-role'; do echo "$S" \| grep -c -- "$p"; done \| grep -vc '^0$'` | `rows N drifted D holds H undetermined U bad 0` with N at least 7, `exit 0`, then `7` | at cfd0ff4 (no §8): `rows 0 ... bad 0`, `exit 1`, then `0`. Planted in a clone, six rows: a quote cited one line off prints `QUOTE DD1: not found at .claude/CLAUDE.md:21`; an untracked `src/engine/nope.mjs:3` prints `PATH DD3`; a quote with one character changed prints `QUOTE DD4`; a blank state prints `STATE DD5`; `bad 5`, `exit 1`. A correct row (`README.md:11` "derives a **53-role semantic layer**", `src/engine/semantic.js`, `holds`) prints nothing. Seeds on that six-row plant: `2` |
| 4 | the check script is the plan's text, so a builder cannot pass row 3 by weakening it | `diff <(awk '/^\140\140\140sh doc-drift$/{f=1;next} /^\140\140\140$/{f=0} f' .sdlc/plans/records-refresh.md) .sdlc/checks/doc-drift-rows-check.sh && echo same` (the plan has exactly one `sh doc-drift` fence; the `sh` fence is U1-2's) | `same` | one character changed: `diff` prints the hunk, `same` absent; before the unit the file does not exist and `diff` errors |
| 5 | the rerun note exists once, U1's note is untouched, `debt.md` is untouched by U2, and `architecture.md` still has zero deletions against the merge base (amended 2026-09-19: zero outside the §6 K17 row, U3's one in-place edit to a pre-plan line; the third command read `grep -cE '^-[^-]'` when U2 was graded) | `awk '/^## /{exit} /^Rerun note \(/{n++} END{print n+0}' .sdlc/architecture.md; awk '/^## /{exit} /^Staleness note \(graded 2026-09-18\)\./{n++} END{print n+0}' .sdlc/architecture.md; MB=$(git merge-base origin/main HEAD); git diff $MB -- .sdlc/architecture.md \| grep -E '^-[^-]' \| grep -vc '^-[\|] K17 [\|]'; git diff $MB -- .sdlc/debt.md \| grep -E '^-[^-]' \| grep -vc '^-[\|] P3 [\|]'; awk '/^## 8\. Doc drift/,0' .sdlc/architecture.md \| grep -c 'debt.md'` | `1`, `1`, `0`, `0`, `1` | at cfd0ff4: `0`, `0`, `0`, `0`, `0`. A copy where the rerun sentence is spliced into U1's paragraph prints `0` first; a copy with one word of U1's note changed prints `1` on the third line |
| 6 | the builder's evidence table: 18 conventions, each with its own HEAD result and plant, no empty cell, measured at the head | `HF=.sdlc/handoffs/records-refresh-U2.md; grep -oE '^[\|] K([1-9]\|1[0-8]) [\|]' $HF \| sort -u \| wc -l; grep -E '^[\|] K([1-9]\|1[0-8]) [\|]' $HF \| awk -F'[\|]' '{for(i=2;i<NF;i++) if($i ~ /^ *$/) c++} END{print c+0}'; grep -c "measured at $(git merge-base origin/main HEAD \| cut -c1-7)" $HF` | `18`, `0`, `1` | before the unit the file is absent: `0`, `0`, `grep` errors. A copy with K7's hits cell blanked: `18`, `1` |
| 7 | (pre-land) the verifier's pass 5: one graded row per convention, K1 to K18, a state in each, no empty cell, the head named | `V=.sdlc/verdicts/architecture.md; grep -c '^## Pass 5' $V; sed -n '/^## Pass 5/,$p' $V > $F/p5.md; grep -oE '^[\|] K([1-9]\|1[0-8]) [\|]' $F/p5.md \| sort -u \| wc -l; grep -E '^[\|] K([1-9]\|1[0-8]) [\|]' $F/p5.md \| grep -vcE '🟢\|🟡\|🔴'; grep -E '^[\|] K([1-9]\|1[0-8]) [\|]' $F/p5.md \| awk -F'[\|]' '{for(i=2;i<NF;i++) if($i ~ /^ *$/) c++} END{print c+0}'; grep -c "at .$(git merge-base origin/main HEAD \| cut -c1-7)" $F/p5.md` | `1`, `18`, `0`, `0`, `1` | at cfd0ff4: `0`, `0`, `0`, `0`, `0`. A copy with one K row's negative-control cell blanked prints `1` on the fourth line; a copy with K9's emoji removed prints `1` on the third |
| 8 | (pre-land) C31's own block, run as the drill ran it, with the substitutions recorded here | `A2=.sdlc/architecture.md; V2=.sdlc/verdicts/architecture.md; for s in 'Module map' 'Runtime topology' 'Persistence' 'Cross-cutting' 'Conventions' 'Load-bearing' 'Doc drift'; do grep -qi "^## .*$s" $A2 && echo -n "$s\|"; done \| tr '\|' '\n' \| grep -c .; conv=$(sed -n '/^## .*Conventions/,/^## /p' $A2 \| grep -c '^[\|] [^-#]'); ctrl=$(sed -n '/^## .*Conventions/,/^## /p' $A2 \| grep '^[\|] [^-#]' \| tail -n +2 \| grep -c '\140'); echo "$((conv-1)) $ctrl"; sed -n '/^## Pass 5/,$p' $V2 \| grep -oE '^[\|] K([1-9]\|1[0-8]) [\|]' \| sort -u \| wc -l; grep -c 'exception' $V2 \| sed 's/^[2-9]/1/'; cp $A2 $F/a.md; perl -pi -e 'if (/^\\\| K1 \\\|/ && !$done) { s/\140[^\140]*\140//g; $done=1 }' $F/a.md; ctrl2=$(sed -n '/^## .*Conventions/,/^## /p' $F/a.md \| grep '^[\|] [^-#]' \| tail -n +2 \| grep -c '\140'); echo "$ctrl2 $ctrl"` | `6`, `18 18`, `18`, `1`, `17 18` | at cfd0ff4: `5`, `18 18`, `0`, `1`, `17 18`. Substitutions to record in the pre-land row, as R18 did: `Module map` is §1 `Layering and dependency direction`; the block's third line counts every table row in the verdict file (`12` at cfd0ff4, passes 1 to 4 kept as history), so the count is scoped to pass 5; the block's mutation as written hits row A1 (the file's first backticked row) and prints `18 18`, so it is moved to the K1 row per the block's own instruction |

The check script for row 3 (written and measured by the planner on 2026-09-18; outputs are in the row 3 control cell):

```sh doc-drift
# Every row of .sdlc/architecture.md §8 Doc drift is a measurement at this head, not a copy:
# its quote sits on the cited doc line, its cited code paths are tracked, its state is one of three.
# Reads files and git only. Usage: sh .sdlc/checks/doc-drift-rows-check.sh   (from the repo root)
node - <<'EOF'
const fs = require("fs"), cp = require("child_process");
const sec = (fs.readFileSync(".sdlc/architecture.md", "utf8").split(/^## 8\. Doc drift/m)[1] || "").split(/^## /m)[0];
const rows = sec.split("\n").filter((l) => /^\| DD\d+ \|/.test(l)).map((l) => l.split("|").map((s) => s.trim()));
const tracked = new Set(cp.execSync("git ls-files", { encoding: "utf8" }).split("\n"));
const isPath = (s) => s.match(/^([\w.\/-]+\.(?:js|mjs|ts|json|css|md|yml|html))(?::\d+(?:-\d+)?)?$/);
let bad = 0, n = { drifted: 0, holds: 0, undetermined: 0 };
const say = (l) => { bad++; console.log(l); };
for (const r of rows) {
  const [, id, doc, code, state] = r;
  const m = (doc || "").match(/^`([^`:]+):(\d+)` "([^"]{20,})"/);
  if (!m) { say(`SHAPE ${id}: doc cell is not \`path:line\` "quote of 20+ chars"`); continue; }
  const [, f, line, q] = m;
  const txt = tracked.has(f) ? (fs.readFileSync(f, "utf8").split("\n")[+line - 1] || "") : "";
  if (!txt.includes(q)) say(`QUOTE ${id}: not found at ${f}:${line}`);
  if (q.includes(String.fromCharCode(0x2014))) say(`EMDASH ${id}`);
  for (const t of (code || "").match(/`[^`]+`/g) || []) { const p = isPath(t.slice(1, -1)); if (p && !tracked.has(p[1])) say(`PATH ${id}: ${p[1]} not tracked`); }
  const st = (state || "").match(/^(drifted|holds|undetermined)\b/);
  if (!st) say(`STATE ${id}`); else n[st[1]]++;
  if (st && st[1] === "drifted" && !(code || "").match(/`[^`]+`/)) say(`CODE ${id}: drifted row cites no code`);
}
console.log(`rows ${rows.length} drifted ${n.drifted} holds ${n.holds} undetermined ${n.undetermined} bad ${bad}`);
process.exit(rows.length && !bad ? 0 : 1);
EOF
```

### U3 (M, grade l2): re-measure at 20298cc after #706

Why. #706 (`d814500..20298cc`, 48 files) landed between U2's verdict and the rebase: `package.json` moves TypeScript `~6.0.2` to `~7.0.2` and vite `^8.0.12` to `^8.3.0`, CI moves to Node 24, and `test/run.mjs` gains `repo/gate-report.mjs` (TESTS is 48). The owner's ruling is in the intro. Measured by the planner at d46ae48a: `sh .sdlc/checks/baseline-agrees-check.sh` prints `STALE tests: baseline 47, test/run.mjs TESTS 48`, `STALE ui.html: baseline 3777.8 KB, tree 3780.5 KB`, `STALE head` (same tree) and `stale total: 3`; U2-2's five lines print `20298cc` then `d814500` four times. So U1-1 and U2-2, both 🟢 at their verdicts, are red at the rebased head, which is what the Risks row "if the branch is rebased anyway, rerun U1-1" predicted. U3 is that rerun, plus the owner's §8 re-check, plus the one correction pass 5 asked for (K17 below).

What the planner read at d46ae48a (leads for the builder, not results):

- 14 of the 56 §8 rows cite a path #706 touched: DD1, DD9, DD19, DD28, DD29, DD30, DD31, DD38, DD39, DD40, DD42, DD45, DD51, DD53 (the loop in row 4 derives the list). The `package.json:16,21,22,27,29` cites (DD1, DD28, DD31, DD40, DD42, DD53) did not move: #706's hunk in that file starts at line 31. DD38's `figma/plugin/code.js:9,12,25` and DD51's `test/engine/type.mjs:181` read the same at `20298cc`; DD51's `test/engine/categories.mjs:307` sits in a file #706 edited by ten lines and needs the builder's read.
- Certain movers: DD19 (`ls test/` now has nine entries, `gate-report.mjs` added, so "four of the eight" is five of nine), DD33 ("6 of `test/`'s 8 entries" is 6 of 9), DD9 (copies the baseline's `npm test` timings and head, both of which this unit replaces). 24 DD rows and three prose lines of `architecture.md` name `d814500`, all U2's. A row's `d814500` becomes `20298cc` only after the row is re-checked; row 4's control shows a global substitution passes the sha grep, which is why the handoff carries one row per DD with the builder's own result.
- K17 (pass 5, 🔴 at `d46ae48`): the registration half of the control prints four hits after its exception filter: `gate-report.mjs`, `repo/fixtures/gate-report-clean.mjs`, `repo/fixtures/gate-report-mismatch.mjs`, `repo/fixtures/gate-report-singlequote.mjs`, all from #699 (`9a44f68`), a shared printer module and three fixtures, the same class as the listed `ui/counts.mjs`. The control bites; §6's exception list is stale. U3 corrects the K17 row in place: the filter's alternation gains `gate-report.mjs` and `repo/fixtures/gate-report-(clean|mismatch|singlequote).mjs` as two more alternatives, written in the cell with the file's own `\|` pipe escape like the three it already has, the HEAD cell reads `0 (7 unlisted files before the exception filter)`, the exceptions cell names the four with their class. Measured: with the widened filter the control prints nothing at `20298cc`; in a clone, a tracked `test/engine/zzz.mjs` still prints `engine/zzz.mjs`. This is one deleted line against the merge base in `architecture.md` (measured in the clone: `1`, and `0` with the K17 exclusion), so U1-9 and U2-5 now exclude the K17 row the way U1-9 excludes debt's P3 row.
- The 18 controls: the rerun note says they were "rerun at" the head U2-2 reads, so for U2-2 (and U3-4, which reads the same sha) to name `20298cc` truthfully the builder reruns all 18 at `20298cc` (U2 step 3's procedure) and records them in the handoff's Controls table, which U3-10 reads; the rerun note may name `20298cc` only once U3-10 is green. Pass 5 is not regraded: it stays the verifier's reading at `d814500`, the rerun note says so, and a U3 rerun that differs from a pass 5 cell is a handoff row and a U3 verdict row, not a pass 6.

Steps, in order:

1. Worktree `.worktrees/rr-U3` on `unit/rr-U3` from `plan/records-refresh` at `d46ae48a` or later, no `node_modules`. First: `git merge-base origin/main HEAD | cut -c1-7` prints `20298cc` and `git diff --stat 20298cc origin/main -- . ':!.sdlc' | wc -l` prints `0` (both measured 2026-09-19). Either other value: stop and ask; the head moved again and the Orchestrator rules whether U3 re-targets (every `20298cc` below, the CI run id and the movers list would change with it).
2. Quiet host, as U1 step 1: `uptime` and `sysctl -n hw.ncpu`; a 1-minute load above the core count (10) means tell the Orchestrator and wait. No other seat's gate in flight: the Orchestrator confirms Lane B idle on the board before dispatch, and before each run the builder runs `pgrep -fl 'test/run.mjs|vite build|smoke.mjs'` and writes its output into the run row's `pgrep before` cell, the word `none` when it printed nothing (a hit means wait, and the row shows the wait happened). `node --version` and, after step 4's `npm ci`, `npm ls typescript vite --depth=0` go in the handoff verbatim.
3. `npm test` three times in sequence. Around each: `uptime` before, `/usr/bin/time -p` wall seconds, exit code, the runner's last line, `git status --short | wc -l`, `uptime` after.
4. `npm ci` once, then `npm run build` three times, then `npm run smoke` three times, the same six facts plus the `wrote figma/plugin/ui.html NNNN.N KB` line and the `SMOKE PASS` line.
5. Every run is recorded; no run is dropped or retried away; a red run stops the unit (U1 step 4).
6. CI run for `20298cc`: `gh run list --branch main --workflow ci.yml --limit 5 --json databaseId,headSha,conclusion`; measured 2026-09-19: run 35455937943, `20298cc panda-smoke=success build-test=success`.
7. Rewrite `.sdlc/baseline.md` per §Texts; re-derive the adapter's three Time cells by U1 §Texts' rule (rounded min and max of the new timings; a cell whose range did not move stays).
8. Rerun the 18 controls at `20298cc` (U2 step 3's procedure, clone `git clone -q --shared . "$CLAUDE_JOB_DIR/tmp/u3"`, read-only controls in the worktree, K7, K9 and every plant in the clone), one handoff row each against the pass 5 cell. Correct the K17 row per the bullet above. Any other hit outside a row's exceptions: record, do not fix (the wall), message the Orchestrator (U2 step 4).
9. Re-check the 56 §8 rows at `20298cc`, one handoff row each; rewrite a row in place where its state, its quoted evidence or a line cite moved; then the head sentence, the rerun note and the Counts bullet per §Texts. `.sdlc/checks/doc-drift-rows-check.sh` is not edited.
10. Run U3 rows 1 to 10, then P1, P4, P5 and, as regression on the files U3 edits, U1-1 to U1-9 and U2-1 to U2-6 as written (with the two amended commands). U2-6 reads U2's handoff, which U3 does not touch, so it is regression only, never U3 coverage; U3-10 is the row that reads U3's own Controls table. Hand off.

#### Texts

`.sdlc/baseline.md`: front matter `ran: <run date>`; `ref: origin/main @ 20298cc`; `host:` as today with the new figures (`load <1m> <5m> <15m> on 10 cores at run start`); `supersedes: the 2026-09-19 baseline at d814500 (kept below as the prior set) and the 2026-09-16 baseline (git show 180eca0:.sdlc/baseline.md)`. The intro sentence names the unit head (`plan/records-refresh` @ `<unit head sha7>`) and the `npm ci` result with the resolved toolchain: `(exit 0, <N> s; resolves typescript <x.y.z> and vite <x.y.z>, npm ls --depth=0)`. The Pass table carries the nine new timings (`a · b · c`), `all 48 test files passed` as the runner prints it, and the `wrote figma/plugin/ui.html <NNNN.N> KB` line as printed (the check script's own measure of the tree at d46ae48a is 3780.5; the build line is what goes in the table). The Not-run-here line reads `CI run 35455937943 on 20298cc`. Nothing above the prior set names `d814500`, `35446265780`, `3777.8`, `63.54` or `47 test files`. Appended as the last section, verbatim except the date:

```text
## Prior set (d814500, superseded <run date>)

The figures of the 2026-09-19 run at `origin/main @ d814500` (U1 of plan records-refresh; load 3.13 6.16 6.24 on 10 cores at run start; CI run 35446265780 green on d814500). Kept because #706 (`d814500..20298cc`: TypeScript 6 to 7, vite 8.0 to 8.3, Node 24 in CI, one test file added to `TESTS`) changed the toolchain the live table describes, so these are history to compare against, not a range to grade against. The rows are headed differently from the live table on purpose: `baseline-agrees-check.sh` reads the first row that starts with the live head.

| command | runs | exit | seconds | summary |
|---|---|---|---|---|
| `npm test` (prior, d814500) | 3/3 | 0 | 63.54 · 65.90 · 59.17 | all 47 test files passed |
| `npm run build` (prior, d814500) | 3/3 | 0 | 2.96 · 1.87 · 2.48 | wrote figma/plugin/ui.html 3777.8 KB |
| `npm run smoke` (prior, d814500) | 3/3 | 0 | 19.96 · 20.23 · 20.45 | SMOKE PASS |
```

`.sdlc/adapter.md` §1: the three Time cells only, by U1 §Texts' rule. No other line.

`.sdlc/architecture.md`, the rerun note rewritten in place (an added line against the merge base, so U1-9 and U2-5 do not count it; the phrase `rerun at` appears once, because U2-2 extracts every match):

```text
Rerun note (U2 of plan records-refresh, 2026-09-19; re-measured by U3, <run date>). The 18 controls of §6 and §6.1 were rerun at `20298cc` (the `origin/main` head `.sdlc/baseline.md` cites) by U3 (`.sdlc/handoffs/records-refresh-U3.md`), after U2 ran them at `d814500` for `.sdlc/verdicts/architecture.md` pass 5, which graded one row each; §8 was measured at `20298cc`. #706 (`d814500..20298cc`) changed the toolchain the baseline describes, which is why U3 re-measured; pass 5 stays graded at `d814500`, and U3 corrected the K17 exception list pass 5 found stale. The claims of §1 to §5 and the recorded HEAD and bite cells of §6 stay as written at `f9e20c5`. Where a pass 5 result differs from a §6 cell, pass 5 is the current reading and the cell is history; where U3's rerun differs from a pass 5 cell, the handoff row says so and the U3 verdict grades it.
```

§7 bullet: ``- Doc drift (§8, measured at `20298cc`): N rows, D drifted, H hold, U undetermined.`` with the counts the check script prints after the re-check. §8's first sentence becomes ``Measured at `20298cc` by U3 of plan records-refresh (#691), re-checking row by row the table U2 first measured at `d814500`, before #706 landed.``; the rest of the paragraph stays. In the DD rows, every `d814500` is gone and each cell that named it names `20298cc` or the re-check's own result.

`.sdlc/handoffs/records-refresh-U3.md`: the adopt-hygiene handoff shape plus three tables and a line `measured at 20298cc`. Runs: `| # | command | load (1 min) before | load (1 min) after | pgrep before | exit | wall (s) | last line | git status lines |`, rows `1` to `9` for the three commands in step order (`npm ci` is a prose line above the table, followed by the `node --version` and `npm ls typescript vite --depth=0` output verbatim, one package per line). Rows: `| DD | cited paths | changed in d814500..20298cc | own result at 20298cc | state before | state after |`, 56 rows, column three `y` or `n` from `git diff --name-only d814500 20298cc` against the row's cells. Controls: `| K | own result at 20298cc | pass 5 cell | delta | plant | hits |`, 18 rows, no empty cell.

#### Criteria

Rows 1 to 10 are graded by the U3 verifier. Values marked "at d46ae48a" were measured by the planner on 2026-09-19 in the survey-refresh worktree; planted values in a throwaway shared clone of it or on a scratch fixture. `F` is `$CLAUDE_JOB_DIR/tmp`. Commands using `<(...)` or `<<<` run under `bash`. The contract of the table: rows 1, 3, 7 and 9, and the check script inside row 4, read the tree, git or GitHub, none of which the unit writes, and are the independent anchors; rows 2, 5, 6, 8 and 10 and the handoff lines of row 4 read records the unit authored and are shape checks, so each of them names the anchor or the verifier's own run that corroborates it. "At d46ae48a" values equal to Expected are the pre-state, recorded so the verifier can tell preservation from change; the bite in those rows is the planted variant.

| # | Criterion | Command | Expected | Negative control |
|---|---|---|---|---|
| 1 | the baseline's test count, bundle size and cited head agree with the tree at `20298cc`, and the adapter's time ranges agree with the baseline (U1-1 at the new head) | `sh .sdlc/checks/baseline-agrees-check.sh; echo "exit $?"; git merge-base origin/main HEAD \| cut -c1-7` | seven `ok` lines, `stale total: 0`, `exit 0`, `20298cc` | at d46ae48a: `STALE tests: baseline 47, test/run.mjs TESTS 48`, `STALE ui.html: baseline 3777.8 KB, tree 3780.5 KB`, `STALE head: baseline ref d814500 has the same tree`, `stale total: 3`, `exit 1`. In the clone with the three numbers fixed by sed (48, 3780.5, `ref` 20298cc): `stale total: 0`; the same clone with one timing set to `99.9`: `STALE time test: baseline 59 to 100 s, adapter 59 to 66 s`; the same clone with a prior row headed `\| \`npm test\` \|` placed above the live table: `STALE tests: baseline 47`, because the script reads the first row with that head, which is why row 2 counts that head |
| 2 | a full rerun at `20298cc`: three runs of three commands, the front matter names the head and the loads, the prior figures live only in the prior set with its three rows, #706 is named once as the reason, and the nine timings are corroborated by the verifier's own runs, not by the transcript | `grep -cE '^[\|] .npm (test\|run build\|run smoke). [\|] 3/3 [\|] 0 [\|]' .sdlc/baseline.md; grep -c '^[\|] .npm test. [\|]' .sdlc/baseline.md; grep -c '^ref: origin/main @ 20298cc$' .sdlc/baseline.md; grep -cE '^host: .*load [0-9.]+ [0-9.]+ [0-9.]+ on 10 cores' .sdlc/baseline.md; awk '/^## Prior set/{exit} {print}' .sdlc/baseline.md \| grep -v '^supersedes:' \| grep -cE 'd814500\|35446265780\|3777\.8\|47 test files\|63\.54'; awk '/^## Prior set \(d814500/,0' .sdlc/baseline.md \| grep -cE '^[\|] .npm (test\|run build\|run smoke). \(prior, d814500\) [\|] 3/3 [\|] 0 [\|]'; grep -c '#706' .sdlc/baseline.md`. Then the verifier's own P1, P2, P3 runs, once each in its own clean tree with no other gate in flight, timed with `/usr/bin/time -p`, and for each command `grep -E '^[\|] .npm (test\|run build\|run smoke). [\|]' .sdlc/baseline.md \| awk -F'[\|]' '{print $2, $5}'` read against the wall seconds it measured | `3`, `1`, `1`, `1`, `0`, `3`, `1`; then, for each of the three commands, the verifier's own wall seconds between half the smallest and double the largest of the three recorded figures, its last line equal to the row's summary (`all 48 test files passed`, the same `ui.html NNNN.N KB`, `SMOKE PASS`) | at d46ae48a: `3`, `1`, `0`, `1`, `4`, `0`, `0`: the head line, the prior-set rows and the reason bite. A copy with the prior section deleted prints `0` on the sixth line; a copy whose live test row still reads `47 test files` prints `1` on the fifth; a copy naming #706 in the front matter as well prints `2` on the last. Measured in the clone: the prior rows' heading keeps the first count at `3` and the second at `1`. The `3/3` cells and the timings are not provable from the file: the verifier's own run is the control. A recorded `npm test` set of `6.1 · 6.3 · 6.0` against a verifier run of about 60 s is outside double and the row is 🔴 (a typed row, or a run that was not `npm test`); a set inside the band but with one figure the verifier's run is more than double of is 🟡 with both figures quoted, never a rerun to a nicer number |
| 3 | the cited CI run is green on `20298cc`, and that head is the merge base (U1-4 at the new head) | U1-4's command, verbatim | `20298cc` three times, the third followed by `build-test=success` and `panda-smoke=success` in either order (run 35455937943 printed `20298cc panda-smoke=success build-test=success`, measured 2026-09-19) | at d46ae48a: `20298cc`, `d814500`, then run 35446265780's line, which opens `d814500`: the merge base is not the head the baseline cites. Run 34660177977 prints `build-test=failure` (U1-4's control), so the jobs query can report red |
| 4 | §8 was re-checked row by row at `20298cc`: no DD row still names `d814500`, the three head mentions read `20298cc` (U2-2 at the new head), the row check exits 0 and is unedited, and the handoff shows every row re-checked with the rows citing a #706-touched path marked | `awk '/^## 8\. Doc drift/,0' .sdlc/architecture.md \| grep -E '^[\|] DD' \| grep -c d814500; H=$(git merge-base origin/main HEAD \| cut -c1-7); echo $H; grep -oE '^ref: .*@ [0-9a-f]+' .sdlc/baseline.md \| grep -oE '[0-9a-f]{7}'; awk '/^## 8\. Doc drift/,0' .sdlc/architecture.md \| grep -oE 'Measured at .[0-9a-f]{7}' \| grep -oE '[0-9a-f]{7}$'; awk '/^## /{exit} /^Rerun note \(/' .sdlc/architecture.md \| grep -oE 'rerun at .[0-9a-f]{7}' \| grep -oE '[0-9a-f]{7}$'; awk '/^## 7\. Counts/,/^## 8\./' .sdlc/architecture.md \| grep -oE 'measured at .[0-9a-f]{7}' \| grep -oE '[0-9a-f]{7}$'; sh .sdlc/checks/doc-drift-rows-check.sh \| tail -1; diff <(awk '/^\140\140\140sh doc-drift$/{f=1;next} /^\140\140\140$/{f=0} f' .sdlc/plans/records-refresh.md) .sdlc/checks/doc-drift-rows-check.sh && echo same; awk '/^## 8\. Doc drift/,0' .sdlc/architecture.md \| grep -cE '^[\|] DD[0-9]+ [\|]'; HF=.sdlc/handoffs/records-refresh-U3.md; grep -oE '^[\|] DD[0-9]+ [\|]' $HF \| sort -u \| wc -l; grep -E '^[\|] DD[0-9]+ [\|]' $HF \| awk -F'[\|]' '{for(i=2;i<NF;i++) if($i ~ /^ *$/) c++} END{print c+0}'; for p in $(git diff --name-only d814500 20298cc); do awk '/^## 8\. Doc drift/,0' .sdlc/architecture.md \| grep -E '^[\|] DD' \| grep -F -- "$p" \| awk -F'[\|]' '{gsub(/ /,"",$2); print $2}'; done \| sort -u \| wc -l; grep -E '^[\|] DD[0-9]+ [\|]' $HF \| grep -c '[\|] y [\|]'` | `0`; `20298cc` five times; `rows N drifted D holds H undetermined U bad 0` with N at least 56 (the floor: U2 wrote 56 rows and U3 deletes none; a row whose claim vanished from the doc becomes `undetermined` with the reason, it is not dropped); `same`; `N` (the §8 row count); `N` (the handoff count, the same N); `0`; `M`; `M` or more (M is 14 at d46ae48a) | at d46ae48a: `24`; `20298cc` then `d814500` four times; `rows 56 drifted 11 holds 45 undetermined 0 bad 0`; `same`; `56`; the handoff is absent, so the last four lines are `0`, `0`, `14`, `0`. A copy of `architecture.md` with the DD19 row deleted prints `rows 55` and `55`: below the floor, so dropping an inconvenient row cannot pass. In the clone, `sed 's/d814500/20298cc/g'` over `architecture.md` alone turns the first line to `0` and the sha lines to five of `20298cc` without any re-check, which is what the handoff's per-row results and the `y` count exist for: the sha can be swapped, a row's own result at `20298cc` cannot. A handoff copy with DD19's third cell `n` prints `13` on the last line |
| 5 | the Counts bullet carries §8's real counts at `20298cc` | `read -r _ N _ D _ H _ U _ B <<<"$(sh .sdlc/checks/doc-drift-rows-check.sh \| tail -1)"; grep -c "measured at .20298cc.): $N rows, $D drifted, $H hold, $U undetermined" .sdlc/architecture.md` | `1` | at d46ae48a: `0` (the bullet names `d814500`). Measured in the clone with `d814500` in the pattern: the unedited file prints `1`, a copy whose bullet reads `12 drifted, 44 hold` prints `0`, so the count part bites on its own |
| 6 | K17's exception list is corrected, the control as the cell now writes it prints nothing at `20298cc`, and that same filter still catches an unregistered test file | `grep -E '^[\|] K17 [\|]' .sdlc/architecture.md \| grep -c 'gate-report-(clean.[\|]mismatch.[\|]singlequote)'; grep -E '^[\|] K17 [\|]' .sdlc/architecture.md \| grep -c '7 unlisted files'; CF=$(grep -E '^[\|] K17 [\|]' .sdlc/architecture.md \| grep -oE 'grep -vxE "[^"]+"' \| tr -d '\134'); echo "$CF"; git ls-files test \| grep "\.mjs$" \| sed "s#^test/##" \| while read f; do grep -q "\"$f\"" test/run.mjs \|\| echo "$f"; done \| eval "$CF" \| wc -l` (`\134` is `tr`'s octal for a backslash: the filter is lifted out of the cell and its `\|` pipe escapes dropped, so what runs is the cell's own filter, not a copy in this plan) | `1`, `1`, then the echoed filter reading `grep -vxE "run.mjs\|smoke/smoke.mjs\|ui/counts.mjs\|gate-report.mjs\|repo/fixtures/gate-report-(clean\|mismatch\|singlequote).mjs"` (with plain pipes), then `0` | at d46ae48a: `0`, `0` (the row reads `3 unlisted files`), the echoed three-name filter, then `4` (the pass 5 🔴, run from the cell itself, measured). The first pattern's `.[\|]` matches the cell's own `\|` pipe escape (measured: the same shape on the row's existing three-name filter prints `1`), so the row is read as written, not as typed. Measured in the clone: a cell widened with the five names prints `0`; the same cell with `ui/counts.mjs` dropped prints `ui/counts.mjs` and `1`, so a cell that gains the new names and loses an old one fails here even though both presence greps pass; a tracked `test/engine/zzz.mjs` prints `engine/zzz.mjs` and `1` through the widened filter, so it still discriminates. A filter widened to `repo/fixtures/.*` instead would swallow a future `repo/fixtures/real-test.mjs`, so the three fixture names are spelled out |
| 7 | adapter: the time ranges follow the new baseline, and the in-place edits stay inside the §1 gate table against the new merge base (U1-6 at `20298cc`; amended 2026-09-19 for U4: the first command is U1-6's amended form, which maps X1 to X13 back before counting; if U4 lands first, U3 is graded with this form, otherwise the two forms print the same `0` on a U3 tree) | `MB=$(git merge-base origin/main HEAD); diff <(git show $MB:.sdlc/adapter.md) <(perl -pe 's/(?<![A-Za-z0-9-])X([1-9]\|1[0-3])(?![0-9a-z])/C$1/g' .sdlc/adapter.md) \| grep -E '^<' \| grep -vcE '^< [\|] (Gate\|test\|build\|smoke) [\|]'; grep -cE '^[\|] (test\|build\|smoke) [\|].*[\|] [0-9]+ to [0-9]+ s( warm)? [\|]' .sdlc/adapter.md; sh .sdlc/checks/baseline-agrees-check.sh \| grep -c '^ok    time'` | `0`, `3`, `3` | pre-state at d46ae48a: `0`, `3`, `3` (the diff against `20298cc` has four deleted lines, all gate-table rows, so the first count is what confines U3). The controls: one timing changed to `99.9` in the clone prints `2` on the third line (row 1's `STALE time test`); a reworded §4 row prints `1` on the first (U1-6's measured control) |
| 8 | the nine runs are recorded with load before and after, neither at or above the core count, no other gate visible before any run, the test runs print the new count, and both resolved packages are on record | `HF=.sdlc/handoffs/records-refresh-U3.md; grep -cE '^[\|] [1-9] [\|] .npm (test\|run build\|run smoke). [\|]' $HF; grep -E '^[\|] [1-9] [\|]' $HF \| awk -F'[\|]' '{for(i=2;i<NF;i++) if($i ~ /^ *$/) c++} END{print c+0}'; grep -E '^[\|] [1-9] [\|] .npm' $HF \| awk -F'[\|]' -v c="$(sysctl -n hw.ncpu)" '{gsub(/ /,"",$4); gsub(/ /,"",$5); if ($4+0 >= c \|\| $5+0 >= c) n++} END{print n+0}'; grep -E '^[\|] [1-9] [\|] .npm' $HF \| awk -F'[\|]' '{gsub(/^ +\| +$/,"",$6); if ($6 != "none") n++} END{print n+0}'; grep -cE '^[\|] [1-9] [\|] .npm test. [\|].*all 48 test files passed' $HF; grep -c 'typescript@7\.' $HF; grep -c 'vite@8\.3\.' $HF; grep -c 'measured at 20298cc' $HF` | `9`, `0`, `0`, `0`, `3`, `1`, `1`, `1` | before the unit the file is absent: every count `0`, then `grep` errors. Measured on a three-row fixture in the §Texts column order: a row whose load-after cell reads `12.3` prints `1` on the third line (load-before `19.20` the same, measured earlier); a row whose `pgrep before` cell reads `812 node test/run.mjs` prints `1` on the fourth; a row with a blank `wall (s)` cell prints `1` on the second; a run row reading `all 47 test files passed` is not counted by the fifth; a fixture with two `typescript@7.0.2` lines and no vite line prints `2` and `0`, which the old single grep counted as "2 or more" and this pair rejects. The load threshold is read from `sysctl -n hw.ncpu` at run time (10 on this host; the fixture prints the same `1` with it read that way, measured), so no reader has to remember to change a copied number |
| 9 | scope wall: nothing outside `.sdlc/` and `.gitignore` differs from `20298cc`, so the K7 and K9 reruns and every plant stayed in the clone | `git diff --name-only $(git merge-base origin/main HEAD) \| grep -vcE '^\.sdlc/\|^\.gitignore$'; git status --short \| wc -l` | `0`, `0` | pre-state at d46ae48a: `0`, `0`. The controls: `echo "// probe" >> src/engine/motion.mjs` in the clone and rerun: `1` (P5's control); a `test/engine/zzz.mjs` plant left tracked in the worktree prints `1` the same way |
| 10 | the 18 controls were rerun at `20298cc` and the evidence is U3's own Controls table: 18 conventions, each with its own result, the pass 5 cell it is read against, a delta and a plant, no empty cell, six columns, K17's row naming the correction; the rerun note may say `20298cc` only when this row is green | `HF=.sdlc/handoffs/records-refresh-U3.md; grep -oE '^[\|] K([1-9]\|1[0-8]) [\|]' $HF \| sort -u \| wc -l; grep -E '^[\|] K([1-9]\|1[0-8]) [\|]' $HF \| awk -F'[\|]' '{for(i=2;i<NF;i++) if($i ~ /^ *$/) c++} END{print c+0}'; grep -E '^[\|] K([1-9]\|1[0-8]) [\|]' $HF \| awk -F'[\|]' 'NF<8{n++} END{print n+0}'; grep -E '^[\|] K17 [\|]' $HF \| grep -c 'exception'; grep -c 'measured at 20298cc' $HF` | `18`, `0`, `0`, `1`, `1` | before the unit the file is absent: `0`, `0`, `0`, `0`, `grep` errors. Measured on an 18-row fixture with the §Texts columns: unedited `18`, `0`, `0`; K7's hits cell blanked `18`, `1`, `0`; K18's row removed and K9's row cut to four columns `17`, `0`, `1`. The verifier's own evidence for this row is its rerun of at least K7, K8, K9, K17 and every control whose cited path #706 touched (§6's `where` cells against `git diff --name-only d814500 20298cc`), in its own clone; the handoff row is the claim under test, as in U2 |

### U4 (S, grade l1): rename the adapter's conflict ids out of the plan namespace

Why. The plugin repo's plan drill-findings, unit U5 (branch `unit/df-ids`, merged into `plan/drill-findings`, not yet in the plugin's `main` at plan time), adds `board.py ids` and wires it into the plugin's `pre-commit` as its third block; `pre-merge-commit` execs the same script, so a `git merge` that stages the file is checked too. This repo's `core.hooksPath` is the plugin cache symlink to the plugin repo's `plugins/sdlc`, so the check is live here the moment drill-findings lands. Its U5 review (pass 2 residual R1) names the consequence for this repo: `adapter.md` cannot be touched at all until its ids are renamed. Measured with the branch's hook in a shared clone of this worktree at 114061a2: a whitespace-only commit touching `.sdlc/adapter.md` prints thirteen `adapter.md:<n>: C<n> defined in a adapter file; owner kind ['plan']` lines and exits 1; a non-ff merge staging the same file exits 1 through `pre-merge-commit`; a commit touching `.sdlc/roadmap.md` alone exits 0.

What the check matches (read from `plugins/sdlc/scripts/board.py` on `unit/df-ids`, function `ids`, and the hook's block 3). A file is scanned only if its kind defines ids: `plans/*.md` outside `plans/archive/`, `baseline.md`, `debt.md`, `readiness.md`, `adapter.md`, `survey.md`, `architecture.md`, `records/`; `verdicts/`, `handoffs/`, `questions/`, `tickets/`, `board.md` are never read. Inside a scanned file a table is defining when its header's first cell is exactly `#`, `Id` or `ID`; a row of such a table whose first cell matches `^\| ([A-Z]-?)(\d+[a-z]?) \|` (with optional spaces) defines that id; the prefix must be owned by the file's kind: `U`, `C`, `A` by plans, `B` by the baseline, `D` by debt, `R` by readiness, `R-` by records. A prefix outside that map is ignored. The hook feeds it the staged copy of each staged `.sdlc/*.md`, never the whole tree, so a landed violation in a file the commit does not touch is not re-checked (the F1 fix in the U5 review). Measured per tracked file under `.sdlc/` at 114061a2, each staged alone: the only file refused is `adapter.md`, 13 lines, rows C1 to C13 at lines 103 to 115 (its §4 table is headed `#`). `debt.md` (rows C1 to C7, D, G, H, K, P, R series), `survey.md` (claims C1 to C15), `architecture.md` (A, K, DD series) and `baseline.md` pass because every one of their tables is headed `id` in lower case or by a word, which the check does not read as a definition header; the three `adopt-hygiene-*` sidecars still under `plans/` and this plan define nothing an owner map covers (this plan's criterion ids are P rows and bare numbers). Verdicts, handoffs and the survey's C1 to C15 claims are citations or unscanned by the check's rule, so nothing there is renamed.

The form, and why the check accepts it. `X<n>`: plan-rules §Id namespaces on the same branch rules that "adapter conflicts (an id the adapter itself mints, distinct from a plan's `C<n>`) get their own prefix, `X<n>`, never a borrowed `C`", and lists `X` among the prefixes `board.py ids` deliberately does not enforce. Mechanically, `| X1 |` matches the row shape and `X` is absent from the owner map, so the row is skipped by design rather than by accident (fixture: `| X1 |` in a scratch `adapter.md` prints nothing; `| C1 |` beside it prints one line). The alternative the Conductor floated, `A5-1` to `A5-13`, also passes, but only because `5-1` falls outside the row regex; it borrows the adoption-unit prefix `A` (owned by plans) and reads as a sub-id of unit A5, and it contradicts the prefix plan-rules names. `X` is what a verifier reading plan-rules will expect. No `X1` to `X13` token exists anywhere in `adapter.md`, this plan, `debt.md` or `roadmap.md` at 114061a2 (measured, `0` each), so the mapping is collision-free in both directions.

Id map: C1 to X1, C2 to X2, C3 to X3, C4 to X4, C5 to X5, C6 to X6, C7 to X7, C8 to X8, C9 to X9, C10 to X10, C11 to X11, C12 to X12, C13 to X13. Same number, same row, same text.

Every live cross-reference (the sweep in U4-5 lists them; 18 lines at 114061a2, of which the five in this plan were rewritten by this amendment):

| File | Lines at 114061a2 | Old token | New token |
|---|---|---|---|
| `.sdlc/adapter.md` | 103 to 115 | `\| C<n> \|` row head | `\| X<n> \|` |
| `.sdlc/adapter.md` | 49 | `see conflict C8 for` | `see conflict X8 for` |
| `.sdlc/adapter.md` | 60 | `is conflict C4 (` | `is conflict X4 (` |
| `.sdlc/adapter.md` | 65 | `pending conflict C3 and C4:` | `pending conflict X3 and X4:` |
| `.sdlc/adapter.md` | 89 | `\| Conflict C6 \|` (the `C8 🟡` on the same line is a survey cite and stays) | `\| Conflict X6 \|` |
| `.sdlc/adapter.md` | 91 | `files (C6) and set` | `files (X6) and set` |
| `.sdlc/adapter.md` | 130 | `may not (C12).` | `may not (X12).` |
| `.sdlc/adapter.md` | 144 | `(conflict C9 rules` | `(conflict X9 rules` |
| `.sdlc/adapter.md` | 179 | `sentence (C5) and the pinned trailer string (C8)` | `sentence (X5) and the pinned trailer string (X8)` |
| `.sdlc/adapter.md` | 180 | `once C3/C4 are ruled` | `once X3/X4 are ruled` |
| `.sdlc/adapter.md` | 181 | `if C6 is approved` | `if X6 is approved` |
| `.sdlc/debt.md` | 59, 60, 61 | the three "distinct from adapter conflict C<n>" notes | the cells in §Texts |
| `.claude/CLAUDE.md`, `.claude/skills/`, `.claude/agents/`, `README.md` | none | (the color-math rubric's C1 to C7 are its own ids) | unchanged |

Dependency. After U3 merges. Both units edit `.sdlc/adapter.md` on disjoint lines (U3: the three Time cells in rows 26 to 28; U4: rows 103 to 115 and the ten lines above), so a textual merge is clean in either order; the sequence exists because U1-6 and U3-7 count deletions in that file and the rename adds 23, so U3 is graded first with the command as written and pre-land grades both rows with the amended command. Exception, checked by the Orchestrator before merging U3: if `grep -c 'board.py" ids' "$(git config core.hooksPath)/pre-commit"` prints `1`, the check is live, the U3 merge would be refused (measured, exit 1), and U4 runs first from `plan/records-refresh` as it stands; U3 then rebases `unit/rr-U3` onto the branch with U4 in it and is graded with U3-7's amended command. In the unit worktree, `B` is resolved once and used by every row: `HP="$(git config core.hooksPath)"; B="$HP/../scripts/board.py"; python3 "$B" ids --help >/dev/null 2>&1 || { PR="$(cd "$(realpath "$HP")/../../.." && pwd)"; git -C "$PR" show unit/df-ids:plugins/sdlc/scripts/board.py > "$F/board.py" 2>/dev/null || git -C "$PR" show plan/drill-findings:plugins/sdlc/scripts/board.py > "$F/board.py"; B="$F/board.py"; }` (the plugin's current `main` `board.py` has no `ids` subcommand and exits 2 on `ids --help`; the branch copy exits 0, measured; `PR` is the plugin repo the hooks symlink resolves into, so no machine path is written here). `GH` for row 2 is `$HP` when the grep above prints `1`, else the `githooks/` of `git -C "$PR" archive unit/df-ids plugins/sdlc \| tar -x -C "$F/plug"`.

Steps, in order:

1. Worktree `.worktrees/rr-U4` on `unit/rr-U4` from `plan/records-refresh` (after U3's merge commit, or before it under the exception). First: `python3 "$B" ids .sdlc \| wc -l` prints `13` and `grep -c '^| C13 |' .sdlc/adapter.md` prints `1`. Any other value: stop and ask; the file moved.
2. In `.sdlc/adapter.md`: the thirteen row heads, the ten cite substitutions in the table above (each old token occurs exactly once, measured), the amendment in §Texts appended after the `X13` row and before `## 5.`. In `.sdlc/debt.md`: the three cells in §Texts. No other line in either file, and no other file.
3. Run rows 1 to 7, then P4 and P5, then U1-6, U1-9 and U3-7 as amended. Hand off: `.sdlc/handoffs/records-refresh-U4.md` in the adopt-hygiene shape with the id map, the cite table with line numbers at the unit head, and the verbatim output of row 1 and row 2.

#### Texts

`.sdlc/adapter.md` §4, appended after the `X13` row, one blank line each side, before `## 5.`:

```text
**Amendment (<date>).** The conflict rows above were numbered C1 to C13 until this date and are X1 to X13 now, same numbers, same rows. `X<n>` is the prefix plan-rules reserves for an id the adapter itself mints; the plugin's `board.py ids` pre-commit check reads a `#`-headed table in this file as definitions and refuses a `C<n>` here, because `C` belongs to plans. A record written before this date that cites conflict C<n> (the A5 ruling in `.sdlc/questions/adopt-a5-conflicts.md`, the adopt-hygiene verdicts and handoffs, `.sdlc/tickets/T-0001.md`) means X<n>; those files are history and were not rewritten. Every `C<n>` still in this file is a survey claim id (the 2026-09-18 amendment above says which grading).
```

`.sdlc/debt.md`, the last cell of rows C5, C6, C7, from "This debt id" to the closing pipe:

```text
This debt id C5 is distinct from adapter conflict X5 in `.sdlc/adapter.md` §4 (the stale "no local git hooks" line; C5 there until records-refresh U4 renamed the conflict rows); the shared prefix that made this a reading hazard is gone |
This debt id C6 is distinct from adapter conflict X6 in `.sdlc/adapter.md` §4 (the tracked `.claude/ops/` files; C6 there until records-refresh U4 renamed the conflict rows); the shared prefix that made this a reading hazard is gone |
This debt id C7 is distinct from adapter conflict X7 in `.sdlc/adapter.md` §4 (commit trailers; C7 there until records-refresh U4); same history as C5 and C6 |
```

#### Criteria

Rows 1 to 7 are graded by the U4 verifier; pre-land reruns rows 1 and 2. `BASE` is `$(git merge-base plan/records-refresh HEAD)` on the unit branch and the U4 merge commit's first parent afterwards; `F` is `$CLAUDE_JOB_DIR/tmp`; `B` and `GH` are resolved as §Dependency says. Values "at 114061a2" were measured by the planner on 2026-09-19 in the survey-refresh worktree; "simulated" values in a shared clone of it with the edits above applied by script. Rows 1 and 2 read the plugin's own code, rows 3 to 6 read the tree, row 7 is regression.

| # | Criterion | Command | Expected | Negative control |
|---|---|---|---|---|
| 1 | the plugin's check accepts every tracked `.sdlc/*.md`, run against the unit tree | `python3 "$B" ids .sdlc; echo "exit $?"` | nothing, then `exit 0` | at 114061a2: thirteen lines from `adapter.md:103: C1 defined in a adapter file; owner kind ['plan']` to `adapter.md:115: C13 ...`, `exit 1`. Simulated with row C13 left unrenamed: one line, `exit 1`. `python3 "$B" ids "$F/nope"` prints `board.py: no such directory`, exit 2, so a mistyped path cannot read as green |
| 2 | the hook as git runs it: a commit touching `.sdlc/adapter.md` is accepted after U4 and refused before it | `git clone -q --shared . "$F/u4hook"; git -C "$F/u4hook" config core.hooksPath "$GH"; echo >> "$F/u4hook/.sdlc/adapter.md"; git -C "$F/u4hook" commit -qam probe; echo "commit $?"` | `commit 0` and no `board.py ids refused` line | the same clone at `git -C "$F/u4hook" reset -q --hard $BASE`, same probe: the thirteen refusal lines, `board.py ids refused an id defined outside its owning file`, `commit 1` (measured at 114061a2). Measured there too: a non-ff `git merge` staging the unrenamed file exits 1 (`pre-merge-commit`), an `echo >> .sdlc/roadmap.md` commit exits 0, so the hook discriminates by file, not by any staged `.sdlc/` path |
| 3 | the rename is the only change to the adapter: mapping X1 to X13 back to C1 to C13 reproduces the base file except the one appended amendment | `diff <(git show $BASE:.sdlc/adapter.md) <(perl -pe 's/(?<![A-Za-z0-9-])X([1-9]\|1[0-3])(?![0-9a-z])/C$1/g' .sdlc/adapter.md) \| grep -E '^[<>]' \| grep -vE '^> $\|^> \*\*Amendment \(2026-09-[0-9]{2}\)\.\*\* The conflict rows above were numbered C1 to C13'; diff <(git show $BASE:.sdlc/adapter.md) <(perl -pe 's/(?<![A-Za-z0-9-])X([1-9]\|1[0-3])(?![0-9a-z])/C$1/g' .sdlc/adapter.md) \| grep -c '^<'` (bash, for `<(...)`) | nothing, then `0` | simulated: nothing, `0`. A reworded resolution cell prints its `<` and `>` pair and `1`; a row renamed `X14` prints its pair, because the map covers 1 to 13 only; `grep -cP '(?<![A-Za-z0-9])X([1-9]\|1[0-3])(?![0-9a-z])' .sdlc/adapter.md` is `0` at 114061a2, so the map folds nothing that predates U4 |
| 4 | §4 is X1 to X13 in order with no C-headed row, the ten cite lines are renamed, and every C1 to C13 token left in the file sits on a survey-cite line | `awk '/^## 4\. /,/^## 5\. /' .sdlc/adapter.md \| grep -oE '^[\|] X[0-9]+ [\|]' \| tr -d '\| ' \| paste -sd, -; awk '/^## 4\. /,/^## 5\. /' .sdlc/adapter.md \| grep -cE '^[\|] C[0-9]+ [\|]'; grep -nP '(?<![A-Za-z0-9-])C([1-9]\|1[0-3])(?![0-9a-z])' .sdlc/adapter.md \| grep -ivcE 'survey\|🟢\|🟡\|🔴'; grep -c 'Conflict C6' .sdlc/adapter.md; grep -cP '(?<![A-Za-z0-9-])X([1-9]\|1[0-3])(?![0-9a-z])' .sdlc/adapter.md; awk '/^## 4\. /,/^## 5\. /' .sdlc/adapter.md \| grep -c '^\*\*Amendment (2026-09-[0-9][0-9]).\*\* The conflict rows'` | `X1,X2,X3,X4,X5,X6,X7,X8,X9,X10,X11,X12,X13`, `0`, `0`, `0`, `24` (13 rows, 10 cite lines, 1 amendment; the line-89 cite shares its line with a survey token and counts once), `1` | at 114061a2: an empty line, `13`, `18`, `1`, `0`, `0`. The `Conflict C6` grep exists because that line also carries `C8 🟡`, so the survey filter alone lets it through (measured: line 89 is not among the 18). Simulated with line 144 left as `conflict C9`: `1` on the third line and `23` on the fifth |
| 5 | the live records cite the new ids and history is byte-identical to the base: the conflict-cite sweep prints nothing outside history, the three debt notes name X5 to X7, nothing else in `debt.md` moved, `.claude/` and `README.md` carry no C1 to C13 token outside the color-math rubric | `git grep -niP '(adapter\|conflict)[^.\|]{0,12} C([1-9]\|1[0-3])(?![0-9a-z])\|\bC([1-9]\|1[0-3])(/C[0-9]+)? (are ruled\|is approved)\|\(C([1-9]\|1[0-3])\)' -- .sdlc .claude README.md ':!.sdlc/verdicts' ':!.sdlc/handoffs' ':!.sdlc/questions' ':!.sdlc/tickets' ':!.sdlc/survey.md' ':!.sdlc/plans/archive' ':!.sdlc/plans/adopt-hygiene-*' ':!.sdlc/plans/records-refresh.md' ':!.sdlc/board.md' ':!.claude/skills/color-math' \| wc -l; grep -c 'adapter conflict X[5-7]' .sdlc/debt.md; grep -ic 'adapter conflict C[0-9]' .sdlc/debt.md; MB=$(git merge-base origin/main HEAD); git diff $MB -- .sdlc/debt.md \| grep -E '^-[^-]' \| grep -vcE '^-[\|] (P3\|C[5-7]) [\|]'; git grep -lP '(?<![A-Za-z0-9-])C([1-9]\|1[0-3])(?![0-9a-z])' -- .claude README.md \| grep -vc color-math; git diff --stat $MB -- .sdlc/verdicts .sdlc/handoffs .sdlc/questions .sdlc/tickets .sdlc/survey.md .sdlc/plans/archive ':!.sdlc/handoffs/records-refresh-U4.md' \| wc -l` | `0`, `3`, `0`, `0`, `0`, `0` | at 114061a2 the sweep prints `13` (adapter lines 49, 60, 65, 89, 91, 130, 144, 179, 180, 181; debt 59 to 61) and the debt counts are `0`, `3`. This plan is excluded from the sweep because its U4 section quotes the old tokens on purpose; its own five cites (lines 25, 27, 53, 385, 396 at 114061a2) were rewritten by the amendment, and the plan is archived at landing. Simulated: `0`, `3`, `0`, `0`. The `.claude` sweep is `0` before and after; its bite: a planted `adapter C9 says so` line in `.claude/CLAUDE.md` in the clone prints `1`. A history file with one cite rewritten makes the last count non-zero |
| 6 | scope: three files, no new em dash, branding clean | `git diff --name-only $BASE \| sort \| paste -sd, -; git diff -U0 $BASE -- .sdlc \| grep '^-' \| LC_ALL=C grep -c $'\xe2\x80\x94'; git diff -U0 $BASE -- .sdlc \| grep '^+' \| LC_ALL=C grep -c $'\xe2\x80\x94'; bash -c 'set -o pipefail; node test/repo/branding.mjs \| tail -1; echo "exit $?"'` | `.sdlc/adapter.md,.sdlc/debt.md,.sdlc/handoffs/records-refresh-U4.md`, then the same count twice (simulated: `1` and `1`; adapter line 130 carries an em dash already and moves with its rename, none is added), `clean (N files scanned)`, `exit 0` | an em dash typed into the amendment makes the second count exceed the first; a fourth path in the list is the wall; P4's control for branding |
| 7 | regression: U1-6, U3-7 and U1-9 as amended, and P5 | U1-6's first command, U3-7's first command, U1-9's last command, P5's command | `0`, `0`, `0`, `0` | U1-6's pre-amendment command on the simulated tree prints `23` (thirteen rows plus ten cite lines), which is what the amendment maps away; a reworded §4 cell prints `1` under the amended form too (U1-6's own measured control still bites through the map) |

## Risks and assumptions

| Risk | Handling |
|---|---|
| A K control prints a hit outside its exceptions at the rebased head (a real drift since `f9e20c5`) | step 4: recorded, not fixed (the wall); pass 5 row 🔴; the Orchestrator decides between a new `debt.md` row in a later plan and a stop. The planner's read of the 15 read-only controls at cfd0ff4 found none, so the exposure is K7, K8 (binder test) and K9 |
| The verifier grades its own pass 5 | rows 7 and 8 are pre-land's, and pre-land's reviewer sits outside the verifier's model family by adapter rule |
| U2 is cut before U1's merge is on the branch, or the branch is rebased between the two units | step 1 stops on `stale total` or a missing staleness note; a rebase sends U1-1 back first (the U1 risk row above) and U2's `HEAD7` follows it |
| C31's verdict-row count, as literally written, counts the whole file | recorded substitution in row 8; the alternative (one live table, passes 1 to 4 moved out) is the owner's call and is listed under Not in scope |
| The host runs many agents (load 19 on 10 cores at plan time), so timings can come out inflated and a gate can go red from contention | step 1 waits for a quiet slot; a red run is recorded and triaged with `flaky-gates`, never retried away. The verifier's own single runs of P1 to P3 are a sanity read on the timings, not a criterion: a run outside half to double the recorded range is a 🟡 note |
| `origin/main` moves again after U1 verifies | the squash does not need another rebase (only `.gitignore` could conflict). If the branch is rebased anyway, rerun U1-1: a `STALE head` line sends the unit back for a baseline rerun, a `STALE ui.html` or `STALE tests` line likewise |
| After landing, U1-1's head line goes STALE at the next feature commit | intended and said in the script's header: it reports that the baseline is behind. It is a criterion only inside a plan that reruns the baseline |
| `f9e20c5` lives only on `origin/sdlc/adopt`; the remote branch sweep (debt P2, a human job) would make the head `architecture.md` and `debt.md` cite unrecoverable in a fresh clone | the notes give the `180eca0` fallback and say it is weaker. Whoever runs the sweep decides whether to keep that branch; not this plan's call |
| Records on this branch cite plan-branch shas (`8120ab4`, `c1c0a32`, `33ee3b1`) that the rebase rewrites and the squash drops | accepted, as for every unit sha on the adopt-hygiene board; the new baseline avoids the pattern by citing a `main` sha |
| The in-place adapter edit breaks the append-only habit the adopt-hygiene plan held the adapter to | the habit was that plan's criterion, not an adapter rule. The owner approved fixing the lines; U1-6 confines deletions to the gate table and both changes are recorded by dated amendments |
| `origin/main` moves past `20298cc` while U3 runs or before pre-land, and the move touches a path outside `.sdlc/` | U3 step 1 stops on it before any run; after U3, pre-land reruns U1-1 and U2-2 as the same guard (`STALE head` or a sha mismatch), and the Orchestrator asks the owner whether to repeat U3 at the new head or land with the baseline one commit behind `main`. A move that touches only `.sdlc/` or `.gitignore` leaves every U3 row green (row 1's head line compares trees outside those paths) |
| U3's gate timings are contaminated by another seat's gate on the same host, the thing U1's first three runs suffered | step 2 waits for a 1-minute load under the core count and an empty `pgrep`, and records load before and after each run (row 8 rejects a run started at 10 or above). The verifier's own single runs of P1 to P3 are the sanity read: a run outside half to double the recorded range is a 🟡 note, not a rerun |
| A rerun K control or a §8 re-check finds a real drift at `20298cc` beyond K17 (#706 edited `src/ui/overlays/apply-gate.js`, `figma/plugin/code.js`, `test/ui/headless-boot.mjs` and nine test files) | step 8 and step 9: recorded in the handoff with the hit quoted, not fixed (P5 and row 9), the Orchestrator told; a K17-class fix (an exception list that is stale, not code that drifted) is in scope only when pass 5 already graded it, as K17 is. Anything else is a new `debt.md` row in a later plan or a stop |
| The builder swaps `d814500` for `20298cc` across `architecture.md` without re-checking the rows | row 4's control shows the sha grep alone passes that; the handoff's 56 rows with the builder's own result and the `y` marks on the 14 rows citing a #706 path are what the verifier reruns, at least those 14 |
| The plugin's `board.py ids` check goes live on this machine (the plugin repo lands drill-findings U5 into `main`, which the `core.hooksPath` symlink follows) before U3's merge commit is on `plan/records-refresh` | measured with the branch's hook in a shared clone: a commit or a non-ff merge that stages the unrenamed `adapter.md` exits 1; a commit touching any other `.sdlc/` file passes. U4 §Dependency: the Orchestrator checks `grep -c 'board.py" ids' "$(git config core.hooksPath)/pre-commit"` before merging U3; `1` means U4 goes first as the unblocker and U3 rebases onto it (disjoint lines in `adapter.md`, so no conflict). `--no-verify` is not a door |
| A later edit re-heads a `debt.md`, `survey.md` or `architecture.md` table with `#`, `Id` or `ID` | their `C<n>`, `R<n>` and `A<n>` rows become definitions to the check and are refused (`C` and `A` belong to plans, `R` to `readiness.md`). Recorded here and under Not in scope; U4 renames only what the check refuses today, which is the adapter's thirteen rows (measured per file at 114061a2) |

## Not in scope, and why

| Item | Why |
|---|---|
| Re-deriving `architecture.md` and `debt.md` | owner ruling Q2; re-checked per plan under the new rule |
| `.claude/CLAUDE.md` | no stale count in it and no conflict-id cite (U4-5); adapter conflict row X9 (C9 until U4) |
| Renaming `debt.md`'s Config smells rows C1 to C7 | the check does not refuse them: `debt.md`'s tables are headed `id` in lower case and the check reads only `#`, `Id`, `ID` as a definition header (measured, U4 §What the check matches). They stay a reading hazard beside the survey's C ids, and `D` is already debt's other series, so a rename is a debt-map decision for the owner, not a U4 side effect |
| Rewriting the conflict-id cites in history files (`.sdlc/questions/adopt-a5-conflicts.md` headings and line 6, the adopt-hygiene verdicts and handoffs, `.sdlc/tickets/T-0001.md`, `.sdlc/plans/archive/adopt-hygiene.md`, the three `adopt-hygiene-*` sidecars under `plans/`) | history by this plan's rule, and the check never reads them (it scans no `verdicts/`, `handoffs/`, `questions/`, `tickets/`; the sidecars and the archive define no owned id, measured). The adapter's §4 amendment carries the map so an old cite still resolves |
| `test/ui/counts.mjs` tracked and registered nowhere (verdict C9), debt K17 | a test-gate change, L2 in the debt map; the first plan that opens `test/` takes it with R12 |
| Rewriting old survey claim ids row by row in `adapter.md` and `debt.md` | one sentence in the amendment and in the note resolves them; a row-by-row rewrite is a re-derivation |
| The five unowned `.git-worktrees/` worktrees (verdict C14), the missing `.sdlc/roadmap.md` (approval Q3) | separate work already routed by the Conductor |
| Renaming §1 of `architecture.md` to `Module map` so C31's section grep prints 7 | a deleted line, and U1-9 holds the file to zero deletions; R18 accepted the substitution, row 8 records it again |
| Moving or mirroring `debt.md` §Records drift into the map | a different artifact (A3 records-vs-code); §8 points at it in one sentence, U2-5 counts the pointer |
| Restructuring `verdicts/architecture.md` into one live table so C31's whole-file row count reads 18 | discards the pass 1 to 4 history from the file the block reads; the owner's call, not this plan's. Pass 5 is appended and row 8 records the scoped count |
| Fixing a violation a rerun control finds at the head | behind the wall (P5); U2 step 4 records it and asks |

## Landing

One PR from `plan/records-refresh` to `main`, carrying the refreshed survey, its verdict, the approval doc, this plan, U1, U2, the verifier's pass 5 (committed by the Orchestrator before pre-land), U3 and U4. Title `chore(sdlc): refresh the survey and the adoption records`. Pre-land runs U4-1 and U4-2 once more at the branch head, because they are the reason the branch can still be committed once the plugin's check is live, and grades U1-6 and U3-7 with their amended commands. Pre-land grades U2 rows 7 and 8 itself and records the three C31 substitutions in its own row, so the plugin repo's re-verify of R18 reads them from one place. Pre-land also reruns U1-1 and U2-2 at the branch head as the moved-main guard (amended 2026-09-19): both are red at d46ae48a and green only after U3, and both go red again if `origin/main` moves outside `.sdlc/` before the squash; that case is the Orchestrator's question to the owner (Risks), not a silent rebase. The pre-land record names `20298cc` as the merge base it graded against. The door is `.sdlc/adapter.md` §2.1 as ruled (conflict row X4, C4 until U4): pre-land review writes `.sdlc/verdicts/records-refresh-prepr.md` with `verdict: 🟢` and the branch head sha; `adapter.py land --branch plan/records-refresh --gate .sdlc/verdicts/records-refresh-prepr.md --dry-run` proves the gate; CI watched to `success` for `build-test` and `panda-smoke`; squash per `shipping-changes` step 7; sync per step 8. Then adapter §5: status `done`, revision row, file moved to `.sdlc/plans/archive/` (U1-2's path gains `archive/` from then on), ticket closed, board row 🟢 under `Seat: orchestrator`.

## Revisions

| Date | Change | Why |
|---|---|---|
| 2026-09-18 | plan written (draft) from approval Q2 and the survey verdict's corrections line; every U1 command was run by the planner at 746f93d (controls) and again in a throwaway clone rebased onto 1ea2f80 with §Texts applied by script (expected values); P1 to P3 were not rerun, their figures come from the 2026-09-18 survey verdict | records-refresh dispatch |
| 2026-09-18 | mobilized: ticket #691 minted; branch rebased onto `origin/main` @ `841e185` (not `1ea2f80`: #673 landed first), so read every `1ea2f80` above as `841e185`; unit worktree is `.worktrees/rr-U1` on `unit/rr-U1` (a plan-scoped name, since `U1` alone collides across plans) | Orchestrator, on the Conductor's mobilize |
| 2026-09-18 | amended (U2, C31): U2 added after U1 with its own criteria, `## 8. Doc drift` decided as a new table (debt §Records drift stays), pass 5 as the verifier's 18 rows, scope wall and P5 text widened to U2's paths, size `S + S`; U1 text unchanged. Planner measured the C31 block and the 15 read-only K controls at cfd0ff4 and the row 3 script's plants in a throwaway clone | owner ruling Q7, relayed by the Conductor |
| 2026-09-19 | P4 command now runs under `set -o pipefail` and prints the gate's exit | P4 exit check read tail's status; pipefail, conductor ruling 2026-09-19 |
| 2026-09-19 | U2 merged into `plan/records-refresh` on a 🟢 verdict (`.sdlc/verdicts/records-refresh-U2.md`, `unit/rr-U2` @ 341be2a). U2 also changed `.sdlc/baseline.md` (`host:` Node 22 to 24.18), a U1-owned file outside every U2 criterion, so nothing in U2's table grades it; the verifier reconfirmed U1 criterion 1 after the fold (`baseline-agrees-check.sh` seven `ok`, `stale total: 0`) | Conductor ruling to fold the host-line fix here rather than reopen U1 |
| 2026-09-19 | U2 criterion 5's stated negative control is unsound: U1's note is itself an added line against the merge base, so editing it yields a changed addition and no deletion (measured twice, prints `0`). Substituted control, recorded in the verdict: removing pre-merge-base lines from `architecture.md` prints `6`, plus a direct `git diff f857ce72 341be2a` proving U1's note byte-identical across U2's base and head | verifier note 2, plan-text defect fixed before the next plan copies the pattern |
| 2026-09-19 | rebased onto `origin/main` @ 20298cc (#706 landed). Two conflicts, both resolved to the plan side: `baseline.md`'s measured block (main carried the copied host line #691 exists to remove) and `adapter.md`'s de-copied `test` row. `adapter.md`'s two `Node 22` gate cells then folded to `Node 24`, matching #706's toolchain and the baseline's measured `Node 24.18` | Conductor ruling to rebase keeping the measured host line |
| 2026-09-19 | amended (U3, #706): U3 added after U2 with nine criteria; the baseline, the adapter's Time cells, §8, the rerun note and the Counts bullet re-measured at `20298cc`, the `d814500` figures kept as a labelled prior set in `baseline.md`, the 18 controls rerun by the builder (handoff, no pass 6), K17's exception list corrected after pass 5 graded it 🔴 at `d46ae48`. Three earlier rows amended in place with a dated note rather than rewritten: U2-2's Expected now names `20298cc` (was `841e185` at amendment time, `d814500` when graded); U1-9's and U2-5's `architecture.md` deletion count excludes the K17 row (was zero deletions). Size `S + S + S`; scope wall and Landing widened to U3. Planner measured at d46ae48a: `baseline-agrees-check.sh` `stale total: 3`, U2-2 `20298cc` then `d814500` four times, the K17 control's four hits, the widened filter's `0` and its `zzz.mjs` plant in a throwaway clone, CI run 35455937943 green on `20298cc`, 14 §8 rows citing a #706 path, `package.json` script lines unmoved (hunk at line 31) | owner ruling relayed by the Conductor: #706 changed the toolchain the baseline describes, so numbers measured at `d814500` are stale on arrival; re-measure at `20298cc`, keep the prior set, criterion 2 names `20298cc` as both measured head and merge base. K17 from the coordinator's addendum on pass 5 |
| 2026-09-19 | U3 revised on the checkability review of 789ff6e (5 🟢 / 4 🟡 / 1 🔴): new row 10 reads U3's own Controls table (18 K ids, no empty cell, six columns, K17's correction named; fixture-measured controls), and the rerun note may name `20298cc` only once it is green; row 2 gains the verifier's own P1 to P3 runs as the corroboration of the timings (half to double band, 🔴 outside it); row 4 pins the §8 floor at 56 rows and ties the §8 count to the handoff count; row 6 runs the filter lifted out of the K17 cell itself (`tr -d '\134'` on the cell's escapes), so a cell that drops `ui/counts.mjs` fails (measured); row 8 gates load-after, adds the `pgrep before` column and counts typescript and vite separately; rows 7 and 9 label their pre-state as such; the criteria intro states which rows are tree checks and which are shape checks; the leads bullet's "criterion 2" reads U2-2. Step 10 says U2-6 is regression, not U3 coverage. Size stays M | coordinator relay of the checkability verdict; controls measured on scratch fixtures and in a throwaway shared clone at 789ff6e |
| 2026-09-19 | U3 sized M, not S, by the Orchestrator's table: it is the union of U1's and U2's measuring steps (three gate triples, the 56-row §8 re-check, the 18 controls rerun, K17's exception list). Only the checklist row, the section heading and the frontmatter changed | Orchestrator, on the planner's own size question |
| 2026-09-19 | amended (U4, conflict ids): U4 added after U3 with seven criteria; the adapter's §4 rows C1 to C13 become X1 to X13, the ten adapter lines and three `debt.md` notes that cite them follow, one dated §4 amendment carries the map; history files stay verbatim. Three criterion rows amended in place with a dated note: U1-6 and U3-7 count adapter deletions after mapping X back to C (the old command prints `23` on a U4 tree), U1-9 excludes debt rows C5 to C7 as it excludes P3. The plan's own four cites rewritten; scope wall, P5, Landing, Risks and Not in scope widened. Planner measured at 114061a2 with the plugin branch's `board.py` and `pre-commit`: 13 refusal lines on `adapter.md` and none on any other tracked `.sdlc/*.md`; the hook refuses a commit (exit 1) and a non-ff merge (exit 1) staging the file and passes an unrelated `.sdlc/` commit; after the simulated rename in a shared clone the check prints nothing, the commit passes, the normalisation diff is empty, `X1` to `X13` in order, the sweep prints 0 of 18, branding clean; a row left as `C13` is refused with one line | owner ruling, Q11 (relayed by the Conductor); the plugin repo's drill-findings U5 review R1 says this repo cannot touch `adapter.md` until the ids are renamed |
