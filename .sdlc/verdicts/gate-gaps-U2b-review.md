# Review: gate-gaps U2b (#715), reviewer-l2 pass 1

Verdict: FIX-FIRST (one 🔴, two 🟡)

| Field | Value |
|---|---|
| Head reviewed | `e6011b01` (`unit/gg-U2b`; moved from 82407c72 through c00f27b9, R53 records commit, then e6011b01, handoff closes the question). Both later commits touch `.sdlc/` only |
| Merge parent | `8bb8d875` (main at step 0); merge `2603690c` parents `9b14c15b` + `8bb8d875` |
| Clones | `git clone --shared` under `scratchpad/ggU2b-rv/`, each `git rev-parse HEAD` printed before use; `refs/remotes/origin/main` set to the real `origin/main` `c5d9346f` so the check script's ancestry read is live |

| # | Check | Result | Evidence | Control |
|---|---|---|---|---|
| 1 | Step 0: tree = main 8bb8d875 + U1/U2 work only; color.js | 🟢 | `git diff 8bb8d875 e6011b01 --stat`: 15 files, all in the plan's Touches (`scripts/report-preset-fidelity.mjs`, `test/engine/{anchor,ramp-identity}.mjs`, `test/run.mjs`, `.sdlc/adapter.md`, `.sdlc/baseline.md`) or `.sdlc/` records. `git diff --quiet 8bb8d875 HEAD -- src/` exits 0. `adapter.md` diff is the one `ramp-identity` row, byte-equal to 9b14c15b's. `board.md` +3 gate-gaps rows only. Non-merge #715 commits in `8bb8d875..9b14c15b` touch 12 paths, none of them `src/`; every commit that touched `src/ui/sections/color.js` there is #681 (`607e8672`, `e0e22ae0`, `bacdf47b`, `a6500f65`), so 75d13c88's revert took a hunk that was not this plan's | Replayed every non-merge #715 commit's `scripts/` + `test/` diff onto a clean 8bb8d875 checkout (`git apply -3`, all clean); `git diff --stat e6011b01 -- scripts test` against that replay printed nothing, so HEAD's code equals main + the plan's own hunks exactly, nothing of main's lost |
| 2 | Step 7: three green runs, non-overlapping spans, load/elapsed, under-load marked; N and KB agree | 🟢 | `baseline.md` row `151.04 · 1057.97 · 452.98`, `✓ all 51 test files passed`. Spans: run 1 2026-09-24 06:47:35 to 06:50:07; run 2 2026-09-25 21:41:48 to 21:59:27; run 3 21:59:53 to 22:07:26: disjoint. Each carries load before/after and seconds; runs 2 and 3 labelled `(under load, R50)`, run 1 `quiet` at 4.98. Run 1's date is backed by `scratchpad/gg-u2b-poll-start.txt` (epoch 1790257193 = 2026-09-24 06:39:53 PDT) and 75d13c88's commit time 06:30; the question doc's stale 21:47 copy was fixed in c00f27b9. TESTS count 51 at head; `ui.html` 4119.1 = tree | Check script lowered to 50 files goes STALE (row 3) |
| 3 | U2-7 / P2 at the head | 🟢 | Clone p2 at `c00f27b9`, rerun at `e6011b01` (h2): `exit 1`; every line `ok` except `STALE time test: baseline 151 to 1058 s, adapter 80 to 89 s`; `note  head: baseline ref a62ec020, ...`; `ok    head: baseline ref a62ec020 is in origin/main's history`; `stale total: 1`. First figure `1`, second `1`: exactly the R53 exception (`.sdlc/runtime/owner-rulings-2026-09-22.md` R53, Chosen `"Keep quiet figure, carry STALE (Recommended)"`, quoted byte for byte in the question doc and handoff). `adapter.md` test row untouched | Clone p2ctl, row's `✓ all 51 test files passed` set to 50: adds `STALE tests: baseline 50, test/run.mjs TESTS 51`, `stale total: 2`, figures `2`, `2`. Matches the handoff's reading (plan's 1/1 + the R53 line) |
| 4 | P1 in a clone at the head, tree clean | 🟢 | Clone p1 at `c00f27b9`, no `node_modules`, started with heavy count `1`: `exit 0`, `✓ all 51 test files passed`, `grep -c FAIL` `0`, `git status --short` 0 lines. Repeated at `e6011b01` (clone h2, heavy count `1` at start): `exit 0`, `✓ all 51 test files passed`, 0 lines after, `▶ repo/branding.mjs        pass`. No timing taken | Clone p1ctl at `c00f27b9`, `"scrim` to `"scrimX`: `exit 1`, `▶ engine/semantic.mjs      FAIL`, `FAIL: 1 gate failure(s)`, `✗ 1/51 test file(s) failed`, `grep -c FAIL` `3` |
| 5 | No em dash outside single-line backtick spans; bold; quotes; branding | 🔴 | P3's own command at `e6011b01` (merge-base `8bb8d875`) prints `1`, plan expects `0`. The line is `.sdlc/handoffs/gate-gaps-U2b.md:109`, the P1 control's `refs-canonical` output line, inside a ``` fenced block, not a single-line backtick span. The quote itself is byte-true to the program's output, so the fix is formatting only: quote that line inside single-line backticks (repo precedent: `.sdlc/handoffs/small-fixes-U1.md:35`, `.sdlc/verdicts/records-followup-U10.md:23`), keeping the bytes. Otherwise clean: `branding: clean (708 files scanned)` exit 0; no `**` bold in added lines (the one `**` hit is inside backticks in the U2 verdict); all quoted script output matches my runs; R53 quote byte-true; commit messages 0 em dashes; P4 wall `0`, `0` | `printf '+plain; dash\n'` through the same perl filter prints `1` |

## Yellows (fix with the red, not blocking on their own)

| # | Finding |
|---|---|
| Y1 | Handoff `Head` cell (line 15) still reads `cfd1edf8`; the head is `e6011b01` after three later records commits. Name the last commit the handoff can cite (`c00f27b9`) or say the handoff commit itself is the head |
| Y2 | Handoff line 56 says "Three runs at `cfd1edf8`", but run 1 (2026-09-24 06:47) predates `c9751d21` and `cfd1edf8`; it ran on the `75d13c88` tree. No test-relevant difference (`git diff --stat 75d13c88 e6011b01 -- . ':(exclude).sdlc'` is empty), but the sentence should name the tree each run was on |

altered: the Orchestrator replaced the em dashes in this record's prose with semicolons when committing it (the repo rule bans the em dash outside code spans); no other change. The reviewer's original is `gg-U2b-review.md` in the Lane B scratchpad.
verdict: 🟡 FIX-FIRST
