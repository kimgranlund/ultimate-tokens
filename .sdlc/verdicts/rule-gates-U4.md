verdict: 🟢
sha: f06609ed1b033ff4892065842894d5721e772ee1

# Verdict rg-U4 pass 4 · 🟢

Unit `unit/rg-U4` at f06609ed. That head holds pass 4, the R41 completion commit ac89ee7c and its mirror regeneration 40d44414, graded against plan revisions 13 and 14 and E1 to E4 of `.sdlc/plans/rule-gates-U4-rediagnosis.md`. Everything ran in the shared clone `/private/tmp/claude-501/rgu4p4` at f06609ed with no `node_modules`. I reverted every control edit, and `git status --short | wc -l` printed `0` after each one. E1 to E4 ran from my own script: Python with U+2014 escaped, the re-diagnosis predicates, E1 in the widened R41 form, and the generated `*-assets.js`, `type-fonts.js` and `src/ui/categories/` excluded. I did not reuse the builder's script. Tally: 17 🟢, 3 🟡, 0 🔴.

| # | Criterion | State | Evidence | Negative control |
|---|---|---|---|---|
| 1 | U4-1 G0 | 🟢 | `CLOSED`, `0`, and `git merge-base --is-ancestor ae4206ac f06609ed` gives `0`. Note: origin/main is now b747c2c8 and adds `41` glyph lines since ae4206ac, so the pre-land re-merge must rerun `--fix` | `git merge-base --is-ancestor origin/main f06609ed` gives `1` against today's main |
| 2 | U4-2 (P1, P2, P3, P9 together) | 🟢 | from rows 3 to 6, the sub-rows are `P1 🟢 P2 🟢 P3 🟢 P9 🟢` | each sub-row's own control below bites |
| 3 | P1 npm test, count, clean tree | 🟢 | one run in the clone, 07:40:15 to 08:04:18 at a load of about 20 to 300, so no timing figure. `npm exit 0`, `✓ all 51 test files passed`, the TESTS count `51`, `▶ repo/em-dash.mjs pass`, and `git status --short \| wc -l` gives `0` after. The harness moved this foreground call to background at its 600 s tool timeout; I did not ask for that, and nothing else ran in the clone until it finished | `sed 's/"scrim/"scrimX/'` on `role-table.json`, then `node test/engine/semantic.mjs` gives `FAIL  refs-canonical, ordered key set != canonical`, `ctl exit 1` |
| 4 | P2 the em-dash gate is clean over all files and reads Unicode | 🟢 | `exit 0`, `1` self-test line, `em-dash: clean (686 files scanned)` | a glyph planted in `src/engine/tonal.js` gives `✗ src/engine/tonal.js:1275`, `FAIL: 1 em dashes outside inline code spans in 1 files`, `ctl exit 1` |
| 5 | P3 `--fix` is deterministic at the head and the rule fixtures bite | 🟢 | `test/repo/em-dash.mjs` is unchanged since 5091460f (it is not in `git diff --name-only 5091460f f06609ed`). `--fix` at the head gives `fix exit 0` and changes `0` files. The full P3 run at 0faca70d is carried from the 5091460f verdict | the row-679 fixture changed to expect `\| a , b \|` gives `self-test: FAIL 1 case(s)`, `✗ R1 empty md cell`, `ctl exit 1` |
| 6 | P9 scope pair check and numstat | 🟢 | the pair check gives `26` at c5f7bb2c (it was `24` at 5091460f) and `33` at b8abddf0, and numstat gives `0` at both. I diffed the pair list against 5091460f's. Exactly 2 lines are new, and both are the authorized U4-6 edits (`body=$(awk "/^## ADR-$num /...` and `head_line=$(grep -nE "^## $id "...`) | the scope loop below flags a planted `.gitignore` edit (`NOGLYPH .gitignore`) |
| 7 | U4-3 gate registered | 🟢 | `grep -c '"repo/em-dash.mjs"' test/run.mjs` gives `1`, and the npm test log shows `▶ repo/em-dash.mjs` `1` time | with the entry removed from `test/run.mjs`, the grep gives `0` |
| 8 | U4-4 pivot regex survives and bites | 🟢 | `grep -c 'u2014 (?:not'` gives `1`, and the four-pivot fixture prints `WARN: 4 em-dash pivot` (count `1`) | with the blind swept regex `/, (?:not` in the clone, the counts are `0` and `0` |
| 9 | U4-5 entry file states the rule and the repair | 🟢 | `2`, `1`, and `git diff --numstat b8abddf0 f06609ed -- .claude/CLAUDE.md` gives `29	27` (the value the 5091460f verdict graded; the file is unchanged since) | carried from 5091460f: with the bullet's two lines deleted, `ctl: 0 0 27	27` |
| 10 | U4-6 record checks read by their last line; the heading match is repaired | 🟢 | each check writes one line only: `rows 56 drifted 11 holds 45 undetermined 0 bad 0`, `range mismatches: 0`, `stale total: 0`, all `exit 0`. The diff is `^## $id[: ]` and `/^## ADR-$num[: ]/`, only the pattern changing | with the c5f7bb2c scripts put back, `range mismatches: 24`. With ADR-011's card shifted to `163-192`: `end ADR-011 says 192, section ends at 191`, `range mismatches: 1`. `Amendment (2026-09-16): planted.` under `## ADR-011:` gives `stale card ADR-011`, `stale index ADR-011`, `stale total: 2` |
| 11 | U4-10 E1 to E4 print exactly the kept list | 🟢 | my run gives hits `E1 18`, `E2 35`, `E3 78`, `E4 26`, and kept `1`, `7`, `20`, `1`. `diff` against the handoff's 29 kept names prints `names equal`. Every other hit reads a colon or a sentence break at the head. Hand reads: E1 `ds-export.js:1495` `## Which variant? Decision tree` is a sentence break, and E3 `color.js:913`'s `on` branch is the kept clause splice | (a) with `geometry.mjs:64` dropped from the list: `E3 run 20 list 19` and the diff names `E3 src/engine/geometry.mjs:64`. (b) with `typography.js:24` and `brand-kit-core.mjs:61` reverted to the comma: `E1 run 2 list 1`, `E3 run 21 list 20`, and the diff names both lines |
| 12 | the widened E1 regex catches the `brand-kit-core.mjs:61` shape | 🟢 | fixture line 1 is c5f7bb2c's `` `# ${kit.name \|\| "Brand Kit"} U+2014 usage\n\n` `` and gives `old=False new=True`. Line 2 is a plain `"## Plain heading U+2014 tail"` and gives `old=True new=True`. Over the tree, E1 is `17` hits with the old regex and `18` with the new | the old regex is the control: it misses fixture line 1, and its tree count is one lower (`E1 hits 17`) |
| 13 | U4-8 the five half-rewritten lines | 🟢 | `ds-export.js:776` to `:777` now read `(a surface that inverts the app's OWN neutral, as toasts and tooltips do, never a brand family's).`, one sentence with the aside kept. The other four were graded 🟢 at 5091460f and are unchanged | revert (b) in row 11 shows the enumeration names a reverted line. At c5f7bb2c the text is `neutral U+2014 toasts, tooltips U+2014` |
| 14 | the verdict's named reds, reread at the head | 🟢 | `geometry.js:733` `"Choose a treatment + base height: icon, font, padding, gap & radius follow..."`, `typography.js:630` `"...body size: fonts, tracking, weight & leading follow."`, `color.js:600` `"Hue × chroma: context + proposed"`, `color.js:1788` `"...organized under: Material, Brand, System, or Data."`, and `brand-kit-core.mjs:61` `# ${...}: usage`. `typography.js:24` to `:27` read `Modular scale: size (px) per step` and the like, and `color.js:1739` reads `"Tune hue · chroma · skew · lift (live)"` plus its two siblings. The export headings at `ds-export.js` 975, 1264, 1386, 1449, 1486, 1525, 1535 and 1545 all take the colon. `store-copy.md:154`, `:155`, `:208`, `:210` and `:212` and `landing.md:77` read `**Free**: the full generator, ...` and the like. `mcp/README.md:8` reads `**three token systems**: **Color**, ...`. All keep their meaning | the same lines at 5091460f read as comma lists (the 5091460f verdict's rows 12, 14 and 15), and row 11's revert (b) shows a reverted line gets named |
| 15 | sample of 20 more rewritten user-visible lines | 🟢 | all 20 read as sentences: `app.js:1238` `Free is limited to ... Upgrade to Pro for unlimited.`, `:1318` `Import failed: not a palette config (.json)`, `apply-gate.js:167` `Applied ... to Figma. Check the Variables & Styles panels`, `:179` `Couldn't apply to Figma. Please try again.`, `color.js:817` `Scrims: the 7 translucent 500 overlays...`, `:1251` `No enabled palettes. Toggle one on...`, `:2210` `Chroma basis: gamut when on, peak when off`, `geometry.js:578` `Centering law: pad = ½(height − glyph)`, `:581`, `typography.js:772` `Custom family: exports as-is; ...`, `:884`, `ds-export.js:894` `- **${what}**: ${why}.`, `:956` `**Roles, then scheme**: pick the semantic role; ...`, `:1437`, `:769`, `derive.mjs:74` `Albers: the primary's complement, muted ...`, `store-copy.md:189` `**5 seats included**: add more anytime...`, `:214`, `brand-kit-mcp.md:44` and `claude-plugin.md:60`. `ds-export.js:1141` has two colons (`Naming standard: **Ultimate Tokens grammar**: ...`), round 3's nit, still there | at c5f7bb2c each of these carried the glyph (all are E1 to E4 hits in my run). The mechanical-comma test tags `0` of them `COMMA` |
| 16 | no code behavior change beyond strings and comments | 🟢 | my skeleton compare (strings blanked except `${...}`, `//` comments dropped) covers `70` changed code line pairs in `5091460f..f06609ed`, with `0` unbalanced hunks. Only `2` differ: the `test/engine/exports.mjs` needles that follow `ds-export.js:1059` and `:1089` (`design-system-for-google-stitch: Stitch profile export`). The two `.sh` changes are the U4-6 heading pattern only | the same tool flags those 2 needle regex edits, which shows it detects a non-string change |
| 17 | scope, branding, private paths | 🟢 | every file changed in `5091460f..f06609ed` carried the glyph at c5f7bb2c, except the two authorized `.sdlc/checks/*.sh` and the handoff. `branding: clean (678 files scanned)`. `.claude/docs/other` and `node_modules` appear `0` times in the diff and `0` times in `git ls-files`. R37: `git grep -E 'Ultimate Tokens, (Pro\|Studio)'` finds `0` outside `.sdlc`, and `PRODUCT_NAME = { 1182548: "Ultimate Tokens Pro", 1182535: "Ultimate Tokens Studio" }` | a copy of `decision-records.md` at `.sdlc/verdicts/zz-ctl.md` gives `FAIL: 3 branding violation(s) across 679 files`. A planted `.gitignore` edit gives `NOGLYPH .gitignore`. The R37 grep at e55a2ce9 gives `3` |
| 18 | E2 over `.md` under `mcp/` (round 3's gap) | 🟡 | `:8` is fixed. Outside the handoff's code-only E2, `mcp/README.md:47` `**Resources**, \`brand://kit\` (full), ...` and `:64` `**Prompt**, \`apply_brand\`: ...` still read as label commas. `README-describe.md:4` is an apposition and reads fine | `--md-under-code` run: `E2 hits 39`, `COMMA mcp/README.md:47`, `COMMA mcp/README.md:64`. At c5f7bb2c they read `**Resources** U+2014` and `**Prompt** U+2014` |
| 19 | a fifth-predicate candidate: paragraph-start bold labels in `.md` | 🟡 | `70` removed `.md` lines outside `.sdlc` match `^\s*\*\*[^*]+\*\* U+2014` with no bullet or `> `, which is neither R3 nor E4. At the head, `76` lines read `^\s*\*\*[^*]+\*\*, `. `32` of them are `store-copy.md` field-label instructions, and one is user-visible: `README.md:6` `**▶ Try it live**, the dependency-free,`. The re-diagnosis grades a construct outside E1 to E4 🟡, not 🔴 | `git grep -nE '^\s*\*\*[^*]+\*\*, ' -- '*.md' ':!.sdlc'` gives `76` at the head. Each hit carried `** U+2014` at c5f7bb2c |
| 20 | carried yellows from 5091460f | 🟡 | these are unchanged and outside R41. `decomposition.md:77` still reads as a list (`at \`src/engine/\`, \`hct.js\`, ...`). `voice-platform.md:121` still says `em-dash form` but gives a comma example. That is marketing wording, #748 | at c5f7bb2c `decomposition.md:77` read `` `src/engine/` U+2014 `hct.js` ``, which shows these are sweep output, not new text |

## Yellows

1. Rows 18 and 19: a fifth predicate, paragraph-start bold labels in `.md` (`^\s*\*\*[^*]+\*\* U+2014` at line start, no bullet), about 70 lines. The customer-visible ones are `README.md:6` and `mcp/README.md:47` and `:64`. The rest are internal docs and `store-copy.md` field labels. This is a follow-up ticket or #748, not U4 under R39's bound.
2. Row 15 nit: `ds-export.js:1141` has a double colon, and round 3's `color.js:2214` has one too.
3. Row 20: `decomposition.md:77`, `voice-platform.md:121`.
4. Pre-land: origin/main adds `41` glyph lines since ae4206ac, so the re-merge must rerun `--fix` and the hand pass.

verdict: 🟢
sha: f06609ed1b033ff4892065842894d5721e772ee1
