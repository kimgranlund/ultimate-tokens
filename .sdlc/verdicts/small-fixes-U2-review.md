# Review U2 · reviewer-l4 → orchestrator

| Field | Value |
|---|---|
| Unit | small-fixes U2 (#717), builder pass 1 |
| Reviewed at | `unit/sf-U2` @ 310a58a8 (code at 7ac1377e), diff against `plan/small-fixes` @ 262ae996 |
| Result | FIX-FIRST on F1, one bounded change in `launcher.mjs`; everything else green |
| Scratch | clones under `$TMPDIR/sfU2rev-R7JE` (`mut`, `old`, `nospawn`), removed after; `pgrep -f fake-chrome-grandchild` `0`, `pgrep -f fixtures/fake-chrome.mjs` `0`, no `ultimate-tokens-smoke-*`, `launcher-legE-*` or `probe-*` dir left under `$TMPDIR`; `git status --short` in the worktree showed nothing but this file at review time; a second clone `sfU2rev2-*` for the U2-6 control, removed after |

## Findings, ranked

| # | Severity | Finding | State | Evidence | Negative control |
|---|---|---|---|---|---|
| F1 | 🟡 fix first | Legs (e) and (f) poll the pid file on existence (`launcher.mjs:68` in `waitForGrandchild`, `launcher.mjs:240` in leg (e)) and then read it once. The fixture creates the file with `writeFileSync` (`fake-chrome.mjs:34`), which opens before it writes, so a poll that lands between the two reads `""`, `readGrandchild` throws `grandchild pid file reads ""`, and the leg reds on a healthy launcher with a message that blames the fixture. Legs (b), (c) and (d) cannot hit it: they read after `ready`, and the fixture closes the pid file before it writes `DevToolsActivePort`. The window is microseconds at idle, so the 100 ms poll hits it rarely, but a fixture preempted between open and write on a loaded host or a CI runner widens it to a scheduler quantum, which is exactly the flake class U1 spent two passes on | 🟡 | A probe (`$CLAUDE_JOB_DIR/tmp/pidfile-probe.mjs`) that spawns the fixture 150 times and reads the pid file the instant `existsSync` is true: `ok=130 empty-or-unparseable=20 missing=0`. Fix: poll for a non-empty parseable pid rather than existence in both places (`try { return /^\d+\s*$/.test(readFileSync(f, "utf8")); } catch { return false; }`), or have the fixture write a temp name and `renameSync` it into place. Either keeps the no-spawn control red (the bounded wait still ends in `ENOENT`) | The same probe at the launcher's own cadence is what the unit runs: 3 sequential head runs and 8 concurrent head runs all `exit 0`, `6` pass, `0` FAIL, so the race is not visible at 100 ms on this host today; the 130 `ok` reads show a completed write parses every time, so the empty reads are the open-to-write gap, not a fixture bug |
| F2 | 🟡 note, non-blocking | The new asserts add legs (b) and (e) to the legs that need a reaping PID 1. Under the group kill the grandchild's parent dies with it, so the grandchild is reparented; a PID 1 that never calls `wait` leaves it a zombie and `kill(pid, 0)` reads it as alive. Legs (c), (d) and (f) already had this dependence at the plan base through the fake itself. CI runs on a VM with a real init, and the plan's U1-8 row runs Docker through `sh -c`, so no criterion changes; the handoff's "Zombie risk on Linux" line should say the Docker row needs a reaping PID 1 (`sh -c` or `--init`) rather than only that it passed | 🟡 | Docker `node:24`, head, `node test/smoke/launcher.mjs` as PID 1: `exit 1`, `5` FAIL, of which `leg (b): grandchild pid 38 still alive 2s after close()` and `leg (e): grandchild pid 97 still alive 2s after the deadline` are new. Same image, head, via `sh -c`: `exit 0`, `6` pass. Same image, head, `--init`: `exit 0`, `6` pass | Plan base 262ae996 unmutated, `node` as PID 1: `exit 1`, `3` FAIL, all `fake pid <n> still alive 2s after <signal>` on (c), (d), (f), none on (b) or (e); so the base already carried the dependence and U2 extends it, not introduces it |
| F3 | 🟡 note, non-blocking | Leg (e) now depends on fixture start-up: the pid file must appear within its 1500 ms deadline, where before U2 the leg waited for nothing from the fixture (MUTE never writes). A slow start reds on `pid file missing`, never passes, so it is a flake risk, not a vacuity risk. Node start-up is 20 ms on this host under load 4 to 5 and the margin is 75x | 🟡 | `/usr/bin/time -p node -e 0`: `real 0.02` three times at load `3.94 5.38 5.50`; 8 concurrent head runs `0` FAIL | Docker `node:24` via `sh -c` at the head: `exit 0`, `6` pass, so a cold Linux start also lands inside 1500 ms |

## Criteria

| Row | State | Evidence | Negative control |
|---|---|---|---|
| U2-1 | 🟢 | Worktree head, 3 sequential runs: each `exit 0 pass=6 FAIL=0`; last line `PASS: launcher discovers its port from DevToolsActivePort and leaves no process on close, SIGTERM, SIGINT or deadline`, byte for byte the 262ae996 line (`diff` of the two `grep 'PASS: launcher'` outputs empty); `legs: 6` | U2-2's mutant: `exit 1`, `5` FAIL |
| U2-2 | 🟢 | Clone `mut` at 310a58a8, `chrome.mjs:66` `try { proc.kill("SIGKILL"); } catch {`, `git diff --stat` `1 file changed, 1 insertion(+), 1 deletion(-)`: `exit 1`, `FAIL count: 5  grandchild FAILs: 5`, one per leg (b) to (f): `leg (b): grandchild pid 96932 still alive 2s after close()`, `leg (c): ... after SIGTERM`, `leg (d): ... after SIGINT`, `leg (e): ... after the deadline`, `leg (f): ... after SIGTERM` | Clone `old` at 262ae996 with the same one-line mutant (`1 file changed`): `exit 0`, `6` pass, `0` FAIL, the critic's finding reproduced |
| U2-3 | 🟢 | Right after the mutant run: `pgrep -f fake-chrome-grandchild \| wc -l` `6` (five asserting legs plus leg (a)), `fakes: 0`; after the head run: `0` | Clone `nospawn` with `fake-chrome.mjs:34` deleted (`1 file changed, 1 deletion(-)`): `exit 1`, `5` FAIL, each `leg (<x>): grandchild pid file missing in <dir> (ENOENT)`; `0` grandchildren, `0` fakes, `0` smoke dirs newer than 5 min after it, so the failed read leaves nothing behind (7ac1377e's leg (b) fix holds) |
| U2-4 | 🟢 | `env -u TMPDIR node test/smoke/launcher.mjs`: `exit 0 pass=6 FAIL=0`; Docker `node:24` `sh -c`: `exit 0  pass=6 FAIL=0` | U2-2's mutant in Docker `node:24` `sh -c`: `exit 1  pass=1 FAIL=5 grandchildFAIL=5` (pids 39, 61, 83, 98, 121, matching the handoff) |
| U2-5 | 🟢 | `git fetch -q origin; git diff --stat origin/plan/small-fixes...HEAD -- . ':!.sdlc'`: `test/smoke/fixtures/fake-chrome.mjs \| 12`, `test/smoke/launcher.mjs \| 76`, `2 files changed, 74 insertions(+), 14 deletions(-)`; `.sdlc` side is the handoff only | Clone `mut` working tree: `git diff --stat` shows `test/smoke/chrome.mjs` as the changed file, which the scope filter would list as a third |
| U2-6 | 🟢 partial | `npm test` in the worktree at 310a58a8: `npm test exit 0`, `✓ all 48 test files passed`; `git status --short` after shows only this review file and the builder's own in-progress F1 edit to `launcher.mjs`, nothing regenerated. `npm run smoke` not rerun (needs `node_modules` and Chrome; the builder's handoff records exit 0 with `SMOKE PASS`, and the verifier's row confirms it) | The smoke script's post-build chain `sh -c 'node test/smoke/launcher.mjs && node test/smoke/smoke.mjs'` in a fresh mutant clone (`1 file changed, 1 insertion(+), 1 deletion(-)`): `exit 1`, `grep -c '^SMOKE PASS'` `0`, `smoke.mjs` never runs (`grep -c smoke.mjs` `0`), last line `FAIL: launcher (5/6 legs failed)`; so a parent-only `chrome.mjs` cannot reach `SMOKE PASS` through the `smoke` script |

## Leak and vacuity audit of the asserts

| Path | Read |
|---|---|
| Vacuous pass | Not possible: `readGrandchild` throws on a missing file, an unparseable pid, or a pid already dead before the cleanup; `assertGrandchildGone` is reached only with a pid that was alive. The no-spawn control (U2-3) reds all five legs on `ENOENT` |
| Does the assert bite on the mutant | Yes on every leg (b) to (f), on macOS and on Linux (Docker). The grandchild is spawned non-detached inside a `detached: true` fake, so it is in the fake's group and only a `-pid` kill reaches it; `stdio: "ignore"` means a parent death sends it nothing |
| Leak on a leg's failure path at the head | Leg (b): `try { readGrandchild } finally { close() }`, close group-kills. Legs (c), (d), (f): a failed read still signals the child, whose `onExit(close)` group-kills; the 5 s timeout path now kills `-fakePid` then `grandPid` then `rmSync(dir)`. Leg (e): `await ready` always runs, the deadline's `close()` group-kills, `finally` removes the scratch. Verified empty by the U2-3 control run and by the post-run counts above |
| Leak under the mutant | Deliberate: five grandchildren survive so U2-3 can count them; each is `node -e "setTimeout(() => {}, 60000)"` and exits on its own. The builder's 62 s recount `0` is consistent with the design |
| Prose rules | One em dash in the diff, inside a backtick span quoting the `SMOKE PASS` line in the handoff, which the rule allows; `**` count `0` |

## Not rerun here

`npm run smoke` (U2-6): needs `node_modules` and Chrome; `npm test` was rerun, see the row.

Round 1 verdict was 🟡, FIX-FIRST on F1; superseded by Round 2 below.

## Round 2 · e78efd73 (F1 fix at b5b8dc47)

Delta reviewed: `git diff 4c6782d4..HEAD`, `launcher.mjs` only. `grandchildWritten(dir)` reads the pid file and tests `^\d+\s*$`; `waitForGrandchild` (legs c, d, f) and leg (e)'s pending-launch poll wait on it instead of `existsSync`. `readGrandchild` still throws on `ENOENT`, an unparseable pid, or a dead pid, so the bounded wait cannot turn a missing grandchild into a pass. Scratch: clones under `$TMPDIR/sfU2rev3-XZ53` (`gap`, `mut`, `nospawn`), removed after; `0` grandchildren, `0` fakes, `0` `ultimate-tokens-smoke-*`, `launcher-legE-*` or `probe-*` dirs left; worktree `git status --short` shows only this file.

| Row | State | Evidence | Negative control |
|---|---|---|---|
| F1 closed | 🟢 | Clone `gap` at e78efd73 whose fixture creates the pid file empty and writes the pid 300 ms later (`1 file changed, 1 insertion(+), 1 deletion(-)`), the preempted-fixture case: new launcher `exit 0`, `pass=6 FAIL=0`, `0` grandchildren, `0` fakes, `0` dirs left. Tight probe (`pidfile-probe2.mjs`, same 150 spawns as round 1 but polling on the `^\d+\s*$` predicate): `ok=150 empty-or-unparseable=0 missing=0` | The 4c6782d4 launcher (`git show 4c6782d4:test/smoke/launcher.mjs`) against the same `gap` fixture: `exit 1`, `pass=4 FAIL=2`, `leg (e): grandchild pid file reads ""`, `leg (f): grandchild pid file reads ""`; round 1's probe on the existence predicate read `empty-or-unparseable=20` of 150 |
| U2-1 | 🟢 | Worktree head, 3 sequential: each `exit 0 pass=6 FAIL=0`; `env -u TMPDIR`: `exit 0 pass=6 FAIL=0`; 8 concurrent head runs: all `exit 0 FAIL=0`; last line `PASS: launcher discovers its port from DevToolsActivePort and leaves no process on close, SIGTERM, SIGINT or deadline`, `diff` against the 262ae996 line empty | U2-2's mutant below: `exit 1`, `5` FAIL |
| U2-2 | 🟢 | Clone `mut` at e78efd73, `chrome.mjs:66` parent-only (`1 file changed, 1 insertion(+), 1 deletion(-)`): `exit 1`, `pass=1 FAIL=5 grandchildFAIL=5`: `leg (b): grandchild pid 38948 still alive 2s after close()`, `leg (c): ... after SIGTERM`, `leg (d): ... after SIGINT`, `leg (e): ... after the deadline`, `leg (f): ... after SIGTERM` | Round 1's `old` clone, the same mutant at 262ae996: `exit 0`, `6` pass (not rerun in round 2; the base did not move) |
| U2-3 | 🟢 | `pgrep -f fake-chrome-grandchild \| wc -l` right after the mutant run: `6`; after every head run: `0` | Clone `nospawn` at e78efd73 (`1 file changed, 1 deletion(-)`): `exit 1`, `5` FAIL, each `leg (<x>): grandchild pid file missing in <dir>`; `0` grandchildren, `0` fakes, `0` dirs left, so the whole-pid wait still ends in `ENOENT` and still cleans up |
| U2-5 | 🟢 | `git diff --stat origin/plan/small-fixes...HEAD -- . ':!.sdlc'`: `2 files changed, 78 insertions(+), 14 deletions(-)`, names `test/smoke/fixtures/fake-chrome.mjs`, `test/smoke/launcher.mjs`; delta em dashes `0`, `**` `0` | Round 1's `mut` working tree listed `chrome.mjs` as a third file |
| Leaks on the new path | 🟢 | `gap` with the old launcher (the red that F1 described): `0` grandchildren, `0` fakes, `0` dirs after the two FAILs, so even the pre-fix red leaked nothing; every round 2 run above ends with the same three zeros | The mutant run leaves `6` grandchildren by design (U2-3 reads them), reaped by `pkill` here and by the fixture's 60 s cap otherwise |
| F2, F3 | 🟢 | Handoff Rework answers match round 1's evidence: F2 names the reaping PID 1 requirement with the three shapes that satisfy it (`sh -c`, `docker run --init`, a VM init), F3 keeps `deadlineMs: 1500` and says where to raise it; `git diff 4c6782d4..HEAD -- test/smoke/chrome.mjs test/smoke/fixtures/fake-chrome.mjs` is empty, so no code moved for either. None needed | Round 1's Docker rows: `node` as PID 1 `exit 1` at base and head, `sh -c` and `--init` `exit 0` |

U2-4 (Docker) and U2-6 (`npm test`, `npm run smoke`) were not rerun in round 2: the delta is a poll predicate inside `launcher.mjs`, covered by the rows above on macOS with `TMPDIR` set and unset; the builder's Rework table records both green at b5b8dc47 and the verifier's grading runs them again.

verdict: 🟢
