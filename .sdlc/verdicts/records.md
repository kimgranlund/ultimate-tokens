# Verdict A3 records · 🟡
verdict: 🟡

Graded by sdlc-verifier on 2026-09-16 against `.sdlc/records/decisions.md`, its cards, and the code at `sdlc/adopt` @ f9e20c5. Mutation controls ran in a `git archive HEAD` copy (`/tmp/ut-a3.8VL1`) and a throwaway worktree (since removed), never the root checkout.
Key: 🟢 implemented · 🟡 drifted (code and record disagree) · 🔴 never done. A superseded record is 🟢 when the superseding state is what the code does.
Tally: 34 rows. 🟢 19 · 🟡 12 · 🔴 3. Gaps: G1-G6 all confirmed; 2 new gaps, G7 and G8.

## ADRs

| # | Decision | State | Code evidence | Negative control |
|---|---|---|---|---|
| ADR-001 | HCT engine, OKLCH input | 🟢 | `hct.js:233 cam16FromRgb`, `:237 lstarFromRgb`; `tonal.js:44 hueSpace: "oklch"` default | default flipped to `"cam16"` in worktree: `tonal.mjs` FAIL intensity-legacy |
| ADR-002 | resolved semantic export, `rawColl` opt-in aliasData | 🟢 | `exports.js:385` aliasData only when `alias` passed; `:579 rawColl` gates it | `EXPORT_SCHEMA_VERSION` 2 to 1 made `exports.mjs` exit 1, so the export gate catches changed output (indirect) |
| ADR-003 | on-colors fixed 050/200, opt-in contrast | 🟢 | `semantic.js:141-142` on 50/50, variant 200/200; `:267 applyOnColorContrast`; `exports.js:324` takes `onColorMode` | `semantic.js:141` set to 950: FAIL oncolors + refs-canonical |
| ADR-004 | scrims on 750 (superseded by 500 ramp) | 🟢 | `semantic.js:223-226` every scrim ref is `500-{pad3}`; `750-` refs: 0 | same grep returns the `500-` sites, so a surviving 750 ref would show |
| ADR-005 | flat raw + `light-dark()` semantic | 🟢 | `exports.js:445` raw has no wrapper, `:460` semantic `light-dark(var, var)` | a `light-dark(` grep on raw emit lines finds none, on semantic lines finds hits |
| ADR-006 | 3-digit padding, `--c-` prefix | 🟢 | `semantic.js:83 refKey`, `exports.js:115 pad3`; retired `--c_` prefix: 0 hits. Panda/Radix unpadded is fenced by SPEC-panda N-1 | `--c_` returns 0 while `--c-` returns many, so the query tells them apart |
| ADR-007 | UI3 schema is interchange-only, not native | 🟡 | schema still emitted (`exports.js:722`, now `v${EXPORT_SCHEMA_VERSION}` = v2). The caution isn't on any surface: drawer labels it "Figma UI3" (`drawer.js:38`), README:56 lists "Figma UI3 (Material)" next to Figma variables, and "interchange" appears 0 times in either | the same grep finds `mergeModeInterchanges` in drawer.js, so the word would be found if present |
| ADR-008 | sampled hue mapping (superseded by 011) | 🟢 | fixed L=0.72/C=0.1 sample survives only as a comment `hct.js:316`; `:307` is the Newton inverse | see ADR-011 |
| ADR-009 | fixed viewing conditions | 🟢 | `hct.js:82 makeVC`, `:110 const VC = makeVC()`; viewing-condition controls in `src/ui`: 0 | `makeVC` query hits engine (2), so a UI copy would also hit |
| ADR-010 | single-file, dep-free, offline | 🟡 | `package.json` dependencies `{}`; hand-rolled zip `zip.mjs:20 crc32`, `:43 zipStore`. Drift: the `window.storage -> localStorage -> in-memory` chain exists only as a comment (`persist.js:31`), with no `window.storage` code; `makeZip` is now `zipStore`; the vite toolchain sits in `dev`/`build` (G3) | `window.storage` grep finds the comment, so a code use would show |
| ADR-011 | OKLCH-native hue, Newton inverse | 🟢 | `hct.js:307 oklchToCam16Hue`, `:216 hctToOklch`; gate `hct-oklch-inverse` `test/engine/hct.mjs:88` | `oklchToCam16Hue` short-circuited to `return h`: FAIL hct-oklch-inverse (Δ3.04°) |
| ADR-012 | hue anchored in render space | 🟢 | `tonal.js:119 solveOkhslHue`, `:138 solveCam16Hue` (module-private, used `:226`); gate `oklch-hue-anchor` `tonal.mjs:317` | `solveOkhslHue` short-circuited in worktree: `tonal.mjs` FAIL |
| ADR-013 | editorial voices | 🟡 | code has 15 voices (`typeScale` categories: Display, Headline, Sub-heading, Title, Sub-title, Lead, Body, Body-mono, Label, Label-mono, Kicker, Tiny, Tiny-mono, UI-control, UI-widget); `makeVoices` `type.mjs:94`, `siblingWeightDefaults` `:402`. ADR-013 text stops at thirteen (`decision-records.md:260`); fifteen first appears in ADR-022 (`:634`). G4 confirmed | voice count read from the live engine, not grep |
| ADR-014 | rename orphans pluginData | 🟢 | `LEGACY_CONFIG_KEY`/`hct-config`: 0; old-brand element `define(…)`: 0; `LEGACY_STORAGE_PREFIXES` `app-helpers.mjs:85`; `config` gate `test/figma/plugin.mjs:413-424` | planted `LEGACY_CONFIG_KEY` in scratch code.js: grep count 1 |
| ADR-015 | product unattributed | 🟢 | `repo/branding.mjs` in TESTS (`run.mjs:18`); clean at HEAD (341 files). Remaining mentions are the allowlisted storage chain + comments | gate bit on real content: root checkout FAILS 5 violations, all in `.sdlc/records/` (see G7) |
| ADR-016 | kebab grammar, collection "Breakpoints" | 🟡 | kebab + homonym renames hold (`geometry.mjs:433-438` icon-gap, pill-radius). Collection set overturned by #491 (2026-09-02, `collections.js:6-8`): "Color Roles" (not "Color Semantic"), "Type Primitives" (not "Font Primitives"), "Geometry" (revert of "Breakpoints"). ADR-016's heading still says "Breakpoints" and no amendment exists. The card and decisions.md miss it too (G8) | `collparity` gate (`binder.mjs:593`) pins the names, so the code side is gated; the record side is not |
| ADR-017 | tickets on GitHub Issues | 🟢 | labels kind:bug/feature, size:small/big exist; issues #638-641 labelled; `docs/tickets` files added after 2026-07-17: 0 | same log query before the cutoff returns 11 added files |
| ADR-018 | role-table.json hand-kept | 🟢 | no `*role*` npm script; `refs-canonical` `semantic.mjs:41-48` | on-role mutation above: FAIL refs-canonical |
| ADR-019 | data-driven theme axis | 🟢 | `semantic.js:104 DEFAULT_THEMES`; `exports.js:580` reads `opts.themes`. Its documented gaps still stand: binder `code.js:789 addMode("Dark")`; `exportUI3` `modes: ["Light","Dark"]` | `opts.themes` grep hits exportDTCG (3), exportUI3 (0) |
| ADR-020 | bundle.mjs stays the inliner | 🟢 | `bundle.mjs:79 preflight`. Split confirmed: Pages deploys `dist/ultimate-tokens.html` (ci.yml:58); vite output (`dist/index.html`, `assets/`) is built but never deployed | planted unregistered `planted.mjs` import: preflight threw `registry problem` |
| ADR-021 | hosted describe-palette exception | 🟢 | `describePalette` in `FLAG_KEYS` `flags.js:14`; `mcp-hosting-spec.md:35` cites ADR-021; no Worker/wrangler files (hosted build not started, as ruled) | `wrangler\|worker` file query returns 0 here; the same query shape found `.claude/ops` files in the survey |
| ADR-022 | type registers, not slots | 🟢 | 339 `"registers"` / 0 `"slots"` in categories JSON; `registersToTypeConfig` `gen-categories.mjs:236`; schema gate `categories.mjs:102`; migrate script kept | `registers` to `slots` in travel.json: `categories.mjs` exit 1 |

## Specs, LLDs, plans

| # | Decision | State | Code evidence | Negative control |
|---|---|---|---|---|
| SITE-licensing | email-bound license, Phase 1 | 🔴 | Phase 1 never done: `email`/`emailMatch`/`customer_email` in `flags.js` + `main.ts`: 0; no email input in `settings.js` | same files do answer to `lemonEntitlement` (`flags.js:189`), so the grep reaches them |
| SITE-mcp-hosting | Phase A shared core | 🟡 | Phase A built: `mcp/brand-kit-core.mjs` + `test/mcp/core.mjs`. B-F unbuilt (no Worker). Drift: spec says `hostedMcp` "stays unwired in TIER_FLAGS until Phase E"; it is wired (`flags.js:17-18`, pro `hostedMcp: true`), so a tier flip would grant a feature with no server | `TIERS_ENFORCED` flipped: `flags.mjs` exit 1, so tier config is gated |
| SITE-storage-sync | local-first sync | 🔴 | never done, as expected for a "for later" draft: `DocEnvelope`/outbox/`sync/push`/serviceWorker/indexedDB in src: 0 | query excludes generated assets only; a src use would hit |
| SITE-runbook | hard launch by flipping `TIERS_ENFORCED` | 🟡 | `TIERS_ENFORCED = false` (`flags.js:24`). Drift: runbook step 0 says no surface consumes `flagOf()`. In fact most gates are wired: maxSets `app.js:1259`, proExport `:1270` + `drawer.js:338,470`, advancedTreatments `:1289`, describePalette `:2491`. Only `hostedMcp` has no consumer | flipping the constant made `flags.mjs` exit 1 |
| SITE-describe-palette | two-step `generate_kit` | 🟡 | built: `MIN_HUE_SEP = 25`, `BRAND_NUDGE = 8` (`describe-kit-core.mjs:74-75`), `STATUS_BANDS` `:64`; single `generate_kit` tool `describe-mcp-core.mjs:118`; Pro-gated download `app.js:2491`; weekly eval workflow. Concern: the 376×528 swatch board size has no literal in `mcp/`, so it went unchecked. §12 key custody still open (G6) | `downloadDescribePaletteMcp` gate grep finds the flag check; removing it would drop the hit |
| OD-004 | plugin-free Figma import test | 🔴 | never done: no result recorded; the named results file `docs/spec/CHANGELOG.md` does not exist; ADR-002 still calls it a "gated spike" (`decision-records.md:50`) | `ls` on the path errors, so an existing file would list |
| LLD-app-shell | as-built shell | 🟢 | `styles.css:372 290px 1fr 300px`, collapse `:383-384`; `app.js:72 view gallery\|editor`; `_liveRefreshNow` skips non-color, falls back to full `render()` in `colorMode === "both"`; LLD and app.js last touched in the same commit 7faf3aa | collapse grep matches only the two zeroed-track rules; a changed track would miss |
| SPEC-muted-base + LLD | prime system, groups, schema v4 | 🟡 | `PRIME_STEP = 0.09` `prime.mjs:23`; prime imports hct/okhsl/tonal only; schema 4; `defaultDocument()` 16 palettes with `Data 1..8`, groups material 30/60, brand/system 100/100, data locked; `Color Prime` `collections.js:18`; `collparity`; `intensityAt`: 0; `deriveDataHues` `data-hues.mjs:31`. Drift: the one shared resolver lives in `src/engine/resolve.mjs:16,25` (imported by `exports.js:36`), with thin wrappers in `model.mjs:419-425`. The LLD says it lives in model.mjs. Still one copy, so the rule holds; the location is stale | `PRIME_STEP` 0.10: `prime.mjs` exit 1 |
| SPEC-panda-park | Panda + Radix exports | 🟡 | `exportPanda` `exports.js:934`, `exportRadix` `:1199`, `pickDrivers` `:806`; `radix-projection.json`; `scripts/smoke-panda.mjs` + CI `panda-smoke`; "10 formats" in CLAUDE.md:27 and skill; `exportParkUi`: 0. Concern: `flattenOver` still exported (`:1084`) with 0 callers in src/test/mcp/scripts, and no recorded decision on its fate is visible in code | caller grep returns only the definition file, so a caller would list |
| PLAN-export-schema | group metadata + schema stamp | 🟡 | E1-E6 show in code: gates `hpg-export-group-metadata` (21 refs), `-json-meta` (14), `-design-system-prime` (1), `-shadcn-chart-6-8` (1), `-schema-stamp` (4); `chart-6..8` `exports.js:796`; binder Color Prime line `code.js:757`; E7 bump rule in `adding-export-formats/SKILL.md:95`. Drift: all 7 step statuses read "todo", frontmatter `status: active`, no `docs/plan/archive/` (G5) | `EXPORT_SCHEMA_VERSION` 1: `exports.mjs` exit 1 |
| PLAN-adia-exports | pinned Adia artifacts | 🟡 | executed: both artifacts, `test/engine/adia-derived-exports.mjs`, `gen:adia-exports` in test/build, 3 tags on origin. Drift: all 8 steps "todo", `status: active` (G5) | +1 byte on radix artifact: FAIL bytes |
| PLAN-overhaul | estate renames | 🟡 | Wave 1 present: 3 `-agent` files, `figma-file-migration` skill. 4 Phase 4 closeout items unticked (`overhaul-plan:74-77`), plan says "nothing here is executed" (G5) | old names `migrating-figma-files`/`ultimate-tokens-reviewer` in tracked `.claude/`: 0 |

## Gaps

| # | Gap | State | Evidence |
|---|---|---|---|
| G1 | no PRD, yet PRD-G* cited | 🟢 confirmed | no tracked `*prd*` file; `PRD-G` cited in `docs/reference/app-shell-patterns.md`, `docs/site/storage-and-sync-spec.md` |
| G2 | 500-ramp scrim model only a note in ADR-004 | 🟢 confirmed | live model `semantic.js:223-226`; no ADR of its own |
| G3 | vite vs "no build step" unruled | 🟢 confirmed, sharpened | vite runs in `dev`, `preview`, `build` (tsc + `vite build`) but its output is not deployed (ci.yml:57-60 ships the bundle.mjs file); `vite.config.js` only limits dep-scan entries. The ruling would be: vite is dev server + type/build check, bundle.mjs is the artifact |
| G4 | voices 13 vs 15 | 🟢 confirmed | 15 in engine; ADR-013 ends at thirteen (`decision-records.md:260`) |
| G5 | plan statuses never flipped | 🟢 confirmed | all three plans, see rows above |
| G6 | `ANTHROPIC_API_KEY` custody | 🟢 confirmed | `gh secret list`: only `NPM_TOKEN`; `describe-eval.yml:28` reads the missing secret; scheduled runs pass in 11-13 s (behavior without a key not inspected) |
| G7 (new) | `.sdlc/` records fail the ADR-015 branding gate | 🔴 | `node test/repo/branding.mjs` in root checkout: 5 violations, all in `.sdlc/records/cards/ADR-014.md`, `ADR-015.md`, `decisions.md` (old brand name, maker domain, pre-rename id). Clean on a HEAD copy. `npm test` goes red in any checkout carrying these files, and committing `.sdlc/` as-is would break CI |
| G8 (new) | #491 collection renames (2026-09-02) overturn ADR-016's collection set with no amendment | 🟡 | `collections.js:6-8` vs `decision-records.md:354,373` |
