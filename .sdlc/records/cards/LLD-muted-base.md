# LLD-muted-base · LLD: Palette groups with an absolute base chroma, a per-palette prime system, data hue derivation · 2026-09-11 (v0.3.0) · approved
| Field | Value |
|---|---|
| Decision | Fixes where each SPEC 0.3.0 concern lives: ramp chroma resolution (`rampChromaOf`) and prime chroma resolution (`primeChromaOf`) live in `src/ui/model.mjs` and are the ONE shared resolver called by both `projectView` and `exports.js` `derivePalette`, so `tonal.js` and the new pure `src/engine/prime.mjs` stay group-unaware; `prime.mjs` imports only `hct.js`/`okhsl.js`/`tonal.js` helpers and never `semantic.js`; the document key is `paletteGroups`; `CURRENT_SCHEMA_VERSION = 4` with `RENAME_MAPS` v3 (`keyIntensity` → `primeChroma`) and v4 (drop `palette.intensity`, default-fill `paletteGroups`); `COLLECTIONS.colorPrime = "Color Prime"` with single mode `Base`, mirrored in both sandbox literals under the `collparity` gate. Build units P1..P8 (spike retirement, prime engine, persist+model, emitters, Figma apply, DS bundle+MCP, UI strip, docs sweep) plus G1 (#556, PR #561) and G2 (#559, stacked on #561); data-palette units U5..U9 from 0.1.0 stand unchanged. Ordering: P1 → P2 → P3 → P4 → P5 ∥ P6; P7 after P3; G1 before G2; G2 after P3; P8 last. |
| Forces | engines DOM-free and acyclic, `intensity-legacy` byte fixture retained, exports must equal the canvas (double-application risk: a group at 30 renders at 9 if the `b` multiplier survives), `story.groups` name clash, the P2..P3 rename window, `collparity` needs all three literal sites in one PR, Figma duplicate-collection risk on re-apply (#494 precedent), yellow-cusp compression near `l 0.9`, Abney residual on outer swatches (~2° worst case) |
| Rejected | two copies of the resolver (one shared import, never two); a per-swatch hue re-solve (ruled out on #537: oscillates at low saturation, shares `tonal.js`); a `FIGMA_MIGRATIONS` entry for `Color Prime` (new collection, no rename); a top-level `groups` key |
| Consequences | Token counts: 16 × 96 = 1,536 default; Figma Color Primitives 576, Color Roles 848 × 2 modes, Color Prime 112 × 1 mode. `role-table.json` untouched (prime tokens are not roles; their answer key is `prime.mjs`'s verifier re-derivation). New instruments: `test/engine/prime.mjs`, `intensity-uniform` tonal group, export/plugin/MCP gate extensions, a headless-boot lettered group; retired: `intensity-spike`, `identity-stops`. Sizes: P1 small, P2 big, P3 small, P4 big, P5–P8 small, G1/G2 small. Issues named: #503, #533, #537, #494, #508 (U5 shipped), #509 (U1 shipped), #552, #556, #559, PR #561; TKT-0455. Risks 0..9 each carry a detection and fallback. No ADR ids cited (references the SPEC's R1..R5, G1..G4 rulings only). |
| Supersedes / amended by | supersedes its own 0.1.0 (chroma spike on identity stops, shipped as U1/U2) and 0.2.0; tracks SPEC 0.3.0; amended by Amendment (2026-09-16) (the shared resolver this LLD's risk table and Agent verification section refer to lives at `src/engine/resolve.mjs`, the group-chroma resolvers imported by both the canvas and `exports.js`'s `derivePalette`) |
| Source | docs/lld/lld-muted-base-key-spikes.md |
