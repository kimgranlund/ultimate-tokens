verdict: 🟡
sha: 008e50fb87a1daffd393c3d5c754c8d755fb87cf

# Verdict gs-U6c · 🟡

Plan `gate-split` revision 13 at `dc6bbd56` (`### U6c`), unit `unit/gs-U6c` at `008e50fb`, cut from U6b `d40cc5ce`. Every run was in a `git clone -q --shared` under `/private/tmp/claude-501/gs-u6c-v/`, `git rev-parse HEAD` printed `008e50fb87a1daffd393c3d5c754c8d755fb87cf` in each clone before any edit; mutations were uncommitted edits on that head, `git diff --stat | tail -1` quoted per row. No `node_modules`. One heavy run at a time, foreground. No timing taken.

| # | Criterion | State | Evidence | Negative control |
|---|---|---|---|---|
| 1 | U6c-1 prime SAMPLED at 200 cases and 500 poison renders, hue grid unmoved | 🟢 | `node test/engine/prime.mjs`: `exit 0`, counts `1`, `1`, `1`; lines `(SAMPLED: 200 determinism cases, 500 poison renders, hue step 5)`, `0/200 palettes shifted hex by call order`, `same sweep): 3/30240` | clone c1, `HUE_MULT` to `FULL ? 1 : 10` (`1 file changed, 1 insertion(+), 1 deletion(-)`): `exit 1`, a `FAIL  gamut-ceiling` row reading `negative control: the vulnerable shared-cache reconstruction measured 0 violations on this sweep`, `FAIL: 1 gate failure(s)` |
| 2 | U6c-2 prime FULL unchanged, graded on what I measured | 🟢 | `npm run -s gate:sweep-prime`: `exit 0`, pass count `26`, `1`, `1`, `1`; last line `PASS: prime-system clears all AC-050 gates`. Diff `d40cc5ce..HEAD` keeps the FULL side byte-equal: `FULL ? 2000 : 200`, `FULL ? 1500 : 500` replacing literal `1500` | the same `^  pass  ` needle on the M-B SAMPLED log reads `24` against `26` unmutated, so the count tracks gate state and is not constant |
| 3 | U6c-2 plan text: expected pass count | 🟡 | plan row reads `21 at c8823976`; measured `26` here. The pass-1 review traced it: 22 declared gates after #681 plus 4 self-prefixed symmetry and ladder-span lines. Plan text is stale, the unit is right | measured `26` against the plan's `21`: the needle as written would print a mismatch, which is how the drift was caught |
| 4 | U6c-3 M-B still reds SAMPLED at 200 | 🟢 | M-B clone (`1 file changed, 2 insertions(+), 2 deletions(-)` on `hct.js`): `exit 1`, counts `1`, `1`; `2/200 palettes shifted hex by call order`, `73/30240 out-of-gamut rungs exceeds the pinned ceiling of 0`, `FAIL: 2 gate failure(s)` | row 1 is the unmutated run: `0/200`, `exit 0`, no ceiling FAIL |
| 5 | U6c-4 stride 4, default kit whole, 35 documents | 🟢 | `node test/ui/headless-boot.mjs`: `exit 0`, counts `1`, `1`, `1`; `(rst-corpus SAMPLED: stride 4, 91 anchored palettes checked of 316, default kit whole)`. Probe clone: `300` corpus anchored entries, `75` kept, plus 16 kit | clone c4a, stride over corpus and kit together keyed on `preset.name === "default kit"`: line reads `79 anchored palettes checked of 316`, needle count `0`. Clone c4b, `corpusFloor` back to `300`: `exit 1`, `(rst-corpus-setup) ... (91 palettes, want > 300)` |
| 6 | U6c-5 M-D still reds the reset sweep at the stride | 🟢 | M-D clone (`1 file changed, 1 insertion(+), 1 deletion(-)` on `color.js`): `exit 1`, `(rst-corpus)` count `4`, `(rst-corpus) 91 of 91 anchored palettes failed the exact-snapshot field round trip` count `1` | row 5 is the unmutated run: `exit 0`, `HEADLESS BOOT PASS` |
| 7 | U6c-6 FULL reset unchanged, and U5-3's line range | 🟢 | `npm run -s gate:corpus-reset`: `exit 0`, `1`, `stride` count `0`, `1`. U5-3 with `A=4051 Z=4157`: outside count `0` against `unit/gs-U6b`, `3` against plan fork `e035f841` (U6b's main merge, per M5) | clone ctl, one comment line inserted at line 200 (a lettered group): outside count against `unit/gs-U6b` rises to `1`. FULL detection: clone rd below, `1 of 3396 anchored palettes failed` |
| 8 | a regression in a sampled preset still reds; what the stride gives up | 🟢 | clone rk, `resetAnchor` lifts to 0 only for `sourceAnchor === "#E0DEDC"` (a stride-kept palette, `Trulli of Alberobello ... secondary`): `exit 1`, `(rst-corpus) 1 of 91 anchored palettes failed` | clone rd, same edit on `#3876AE` (a stride-dropped palette, `Oia ... secondary-muted`): SAMPLED `exit 0` and `HEADLESS BOOT PASS`, the stated coverage given up; FULL `gate:corpus-reset` `exit 1`, `(rst-corpus) 1 of 3396 anchored palettes failed`, so the dropped palette is FULL-only, as designed |
| 9 | stated coverage given up: plan step 2's hue-step comment | 🟡 | plan step 2: the comment above `DET_CASE_COUNT` says 200, 500 `and why the hue step stayed`. Added lines in `prime.mjs` mentioning hue: `0`. The handoff claims `Comments above both consts state the new sizes and why the hue step (HUE_MULT, HUE_OFFSET) did not move`, which is false. Sizes and prefix semantics are stated; the reset print lines state `91 ... of 316` and `0 of the rest lost every anchored palette to the stride` | clone ctl, one comment line naming the hue step added above `DET_CASE_COUNT`: the same count prints `1` |
| 10 | review F1 fixed: the `RESET_STRIDE` comment names the right mechanism | 🟢 | comment now reads `replaces that name with defaultDocument().name (\"Default\")`; measured `node -e` on `model.mjs`: `"Default" "Default"` for the doc and the spread kit | clone c4a (name-keyed skip) strides the kit and reads `79`, which is exactly the failure the comment explains |
| 11 | review F2 fixed: the none-contributing print and the line-range cite | 🟢 | log line `(rst-corpus SAMPLED: 5 of 35 sampled documents contribute no anchored palette in either mode, 0 of the rest lost every anchored palette to the stride)` count `1`; handoff U6c-6 row cites `0` and `3`, matching row 7 | `gate:corpus-reset` FULL log: `stride` count `0`, so neither print leaks into FULL |
| 12 | the review record | 🟡 | nothing under `.sdlc/verdicts/` on the branch names U6c (`git ls-tree` lists `gate-split-U1` to `gate-split-U6a` only). The record is `gs-U6c-review.md` in a seat scratchpad under `/private/tmp/claude-501/`, pass 1 at `cc540b6c`; no pass-2 re-review record exists, so the F1 and F2 fixes are graded here only | the same `git ls-tree -r --name-only 008e50fb .sdlc/verdicts` does list `gate-split-U6a.md`, so the query would find a U6c record if one were committed |
| 13 | U6b fix `71cdb960`: key-anchor named presets read `byCategory` | 🟢 | at head, `node test/engine/anchor.mjs`: `exit 0`, `key-anchor rendered path: 46 of 46 ... 4 subjects (... plus 3 named corpus presets), 0 off` | clone ka, `71cdb960` reverted with `git apply -R` (`1 file changed, 3 insertions(+), 6 deletions(-)`): `exit 1`, `rendered leg: no travel preset matching "Hidaka coast" in the corpus - the named subject moved`, `2 subjects`, `FAIL: 1 gate failure(s)` |
| 14 | scope matches the plan lists | 🟢 | `git diff --name-only unit/gs-U6b HEAD`: `.sdlc/handoffs/gate-split-U6c.md`, `test/engine/prime.mjs`, `test/ui/headless-boot.mjs`, all on U6c's list. P9 with `BASE=04f95ff0` and `Q2=yes`: `0`, `-- src` `0`; the only off-list path before the Q2 filter is `.claude/CLAUDE.md`, inherited from U6b | P9 fixture of five names with `src/engine/hct.js` through the same filter: `1` |
| 15 | P8 branding and em dash | 🟢 | `branding: clean (665 files scanned)`; em-dash count on added lines against `04f95ff0`: `0` | clone ctl, `decision-records.md` copied to `.sdlc/verdicts/x.md`: `FAIL: 3 branding violation(s) across 666 files`, `exit 1`; one added em-dash line: count `1` |
| 16 | `npm test` at the unit head | 🟢 | foreground, once, clone base, no `node_modules`: `exit 0`, `all 50 test files passed`, `git status --short` count `0` after | rows 4, 6 and 13 are the same suites red under a mutation, so the green run is not vacuous |
| 17 | U6-5 baseline agrees (U6b) | 🟡 | `baseline-agrees-check.sh`: `exit 1`, `^ok    time gate:` count `1` of expected `4`, `stale total: 4`. Three are `STALE time gate:corpus-anchor` / `sweep-prime` / `corpus-reset` with `adapter none`, the gate-script figures owed by the deferred quiet window; the fourth is `STALE head: baseline ref 71cdb960 is in origin/main's history`, which U6-9's close-out re-points at landing. Not a U6c defect; cannot go green before the window | clone ctl, adapter corpus-tonal range `86 to 116 s` to `87 to 116 s`: `STALE time gate:corpus-tonal`, ok count `0` |
| 18 | U6-6 adapter §1 and §2 (U6b) | 🟢 | gates `1 1 1 1`, ceiling `1`, quiet-host `1`, interim `0`, jobs `build-test 1`, `panda-smoke 1`, `corpus-contrast 1`, `sweeps 1`, deploy `1` | same needles on `origin/main:.sdlc/adapter.md`: gates `0 0 0 0`, `Interim ceiling now, split sweeps into gate scripts` count `1` |
| 19 | U6-7 shipping skill (U6b) | 🟡 | counts `1`, `5`, `0`, `0`, `0`, `1`; line 37 says a red `sweeps` leg is a red run and names `npm run gate:sweeps`. The watch-time figure reads `~figure pinned at pre-land, once U6-8/P10 measures a completed sweeps matrix run`, not yet the measured CI wall the row asks for; pending P10 | on `origin/main` the same six counts read `0 1 1 1 1 1` |
| 20 | U6-9 close-out re-points `ref` (U6b) | 🟢 | §5 `sets .ref. to the squash sha` count `1` | `origin/main` adapter: `0` |
| 21 | U6-10 load under 5 written (U6b), grep half | 🟢 | `grep -c 'load under 5'`: `1`. The Runs-table half is timing and deferred | `origin/main` adapter: `0` |
| 22 | U6-11 R8 to R10 in §2.1 (U6b) | 🟢 | `R8\|R9\|R10` in §2.1: `4` | `origin/main` adapter: `0` |
| 23 | U6-12 clone provenance rule (U6b), applied by this unit | 🟢 | `grep -c 'clone --shared'`: `1`; the U6c handoff states `git rev-parse HEAD` printed `8fc8abf5...` and `46dc9b77...` in its clones | `origin/main` adapter: `0` |

Deferred, not graded and not red: U6c-7 (per-file SAMPLED share), U6c-8 (the re-time, P2 after U6c), and U6b's six rejected gate-script timing rows with U6-3's Runs-table count and U6-4 (P2). They need the coordinated quiet window.

Findings for the Orchestrator:

| Sev | Row | Finding | Owner |
|---|---|---|---|
| 🟡 | 9 | plan step 2's hue-step reason is missing from the `prime.mjs` comment, and the handoff says it is there. One comment line plus a handoff correction | builder |
| 🟡 | 3 | plan U6c-2 still says `21`; measured `26` | planner, next revision |
| 🟡 | 12 | the U6c review is not a committed record under `.sdlc/verdicts/`, and pass 2 has no re-review | Orchestrator |
| 🟡 | 17, 19 | U6-5 and U6-7's figure wait on the quiet window and P10 | window, pre-land |

Clones left on disk: `rm -rf` was denied. Path to remove: `/private/tmp/claude-501/gs-u6c-v` (clones base, probe, c1, mb, md, c4a, c4b, rk, rd, ka, ctl; ctl and base are clean, the rest carry the uncommitted mutations named above).

verdict: 🟡
sha: 008e50fb87a1daffd393c3d5c754c8d755fb87cf
