# Handoff pif-u10 · #681 preset-intent-fidelity, pre-land F1 and F2

Builder, grade l5, pass 1. Written 2026-09-23.

## Branch

`unit/pif-u10`, worktree `.worktrees/pif-u10`, base `8f0e08d6` (UB = `git merge-base plan/preset-intent-fidelity HEAD` = `8f0e08d65db288dd0e112b8152bca50d3d9bbd13`). Controls ran in two `git clone -q --shared` scratch clones under `$TMPDIR` (`pif-u10-scratch` at `30bfaeeb`, `pif-u10-head` at `7d4c2b44`), never in the repo.

## Files

| File | Change |
|---|---|
| `src/engine/okhsl.js` | `rgbToOkhsl` returns `s = 0` at `L <= 0`. Pure black has a zero-width gamut, so `getCs` gives zero chroma bounds and the saturation formula divided 0 by 0. Every other input takes the old path unchanged (the regenerated corpus and ADIA exports did not move). Hue was already finite (`0`). |
| `test/engine/anchor.mjs` | new `anchor-achromatic` gate: `#000000`, `#FFFFFF`, `#010101`, `#808080` anchors x 3 tone modes x 2 hue spaces, called on `paletteStops` directly; every stop must be `^#[0-9A-F]{6}$` with a finite tone, and stop 500 must equal an anchor whose L* is inside `RAMP_L_MIN..RAMP_L_MAX` (the `#808080` rows). A planted `#NANNANNAN` stop proves the predicate bites on every run; `rgbToOkhsl([0,0,0]).s` is asserted finite. |
| `docs/marketing/fact-sheet.md` | line 18: "OKHSL lightness ladder" becomes a CIE L* ladder around the key colour (the source anchor verbatim when there is one); "every ramp and role value is unchanged from the previous release" is replaced by the ladder moving to CIE L* in #681, the release that also moved the ramps. |
| `figma/plugin/ui.html`, `src/ui/describe-mcp-assets.js` | regenerated by `npm test` (both inline the `okhsl.js` source as text; git diffs them as binary). |
| `.sdlc/baseline.md` | ui.html KB cell 4117.5 to 4118.2 plus a named correction paragraph (see Left out). |

## Ran

| # | Command | Output | Control output |
|---|---|---|---|
| U10-1 | `paletteStops({hue:30,chroma:50,skew:0,lift:0,anchor:A}, {...DEFAULT_CONTROLS, toneMode}, STOPS)` for A in `#000000`, `#FFFFFF`, `#010101` | 🟢 all 9 rows `nan=0`; `#000000 perceptual 500=#1B1B1B 100:#E9E1E3 ... 950:#141011`, `peak 500=#1B1B1B`, `even 500=#1B1B1B`; `#FFFFFF` 500 `#F8F1DE`/`#F8F1DE`/`#F3F1EB`; `#010101` 500 `#1B1B1B` in all three. All three anchors sit outside the 9.95..95.05 L* window, so stop 500 is the clamped edge by the anchor rule, not the anchor hex; the in-window byte-exact case is `#808080` inside the U10-3 gate (stop 500 `#808080` in all 6 mode/space pairs) | same probe at `30bfaeeb`: `#000000 perceptual nan=19 500=#NANNANNAN 100:#NANNANNAN 150:#NANNANNAN ...`, `peak nan=19` likewise, `even nan=0` |
| U10-2 | `git diff --name-only $UB -- src/engine/ src/ui/persist.js` | 🟢 `src/engine/okhsl.js` | U10-1 calls the engine directly, so a `persist.js`-only filter could not pass it |
| U10-3 | `node test/engine/anchor.mjs` | 🟢 `pass  anchor-achromatic: 24 of 24 ramps real (#000000, #FFFFFF, #010101, #808080 x 3 tone modes x 2 hue spaces), planted-NaN control caught`, exit 0 | new `anchor.mjs` copied into the `30bfaeeb` clone: `FAIL  anchor-achromatic: 20 of 24 ramps real ...`, `FAIL: 1 gate failure(s)`, exit 1 |
| U10-4 | `grep -n -e 'OKHSL lightness ladder' -e 'unchanged from the previous release' docs/marketing/fact-sheet.md` | 🟢 no match, exit 1 | same grep at `30bfaeeb` prints `18:\| Prime swatches per palette \| ...` |
| U10-P1 | `npm test` in a fresh shared clone of `unit/pif-u10` at `7d4c2b44`, no `node_modules` | 🟢 `✓ all 49 test files passed`, `npm test  304.52s user 5.11s system 103% cpu 4:57.87 total`; `git status --short \| wc -l` = `0` | same clone with `okhsl.js` restored from UB, `node test/run.mjs`: `▶ engine/anchor.mjs        FAIL`, `✗ 1/49 test file(s) failed`, exit 1 |
| U10-P2 | `git diff --name-only $UB` | 🟡 `.sdlc/baseline.md`, `docs/marketing/fact-sheet.md`, `figma/plugin/ui.html`, `src/engine/okhsl.js`, `src/ui/describe-mcp-assets.js`, `test/engine/anchor.mjs`, plus this handoff. `.sdlc/baseline.md` is outside the declared list; see Left out | a stray edit would print an extra name |
| U10-P3 | the five `.sdlc/checks/*.sh`, `ceiling-counts-check.mjs`, `branding.mjs`, em dash count | 🟡 every check exit 0 (`stale total: 0`, `ceiling-counts: clean`, `branding: clean (639 files scanned)`); the dash count prints `1`, see Left out | before the baseline repair, `baseline-agrees-check.sh` printed `STALE ui.html: baseline 4117.5 KB, tree 4118.2 KB`, `stale total: 1`, exit 1; the dash count with one planted added line carrying U+2014 prints `2` |

## Left out

- The em dash count is `1` and it is not an added dash. It is `fact-sheet.md:18`, a line this unit edits that already carried 4 em dashes: removed lines hold 4, added lines hold 4. Removing them would rewrite the voice of text F2 does not name. Team-lead's call whether the row reads added dashes or added lines.
- `.sdlc/baseline.md` sits outside U10-P2's list. The okhsl guard grows the inlined `figma/plugin/ui.html` by 0.7 KB and `baseline-agrees-check.sh` went red, which U10-P3 requires green. Repaired in this unit on the U7 precedent recorded in `baseline.md` (the unit that moves the figure repairs it); the figure is `gen:figma-ui`'s own `wrote figma/plugin/ui.html 4118.2 KB` from two runs.
- `#FFFFFF` reads `rgbToOkhsl` s = 0.558 at h 89.9 (tiny chroma over a near-zero gamut near L = 1), which is F4's arbitrary-hue class. It renders real colours and was left alone, per the brief.
- F3, F4, F5 untouched. `npm run build` and `npm run smoke` not run (no build-chain file touched).
