# Verdict survey · 🔴

Graded by sdlc-verifier on 2026-09-16 at `sdlc/adopt` @ f9e20c5, in a scratch worktree (`/tmp/ut-verify`, detached, no `node_modules` until C2).
Tally: 🟢 7 · 🟡 3 · 🔴 4. The 🔴 rows are wrong survey facts, not repo defects.

| # | Claim | State | Evidence | Negative control |
|---|---|---|---|---|
| C1 | `npm test` runs without node_modules, exit 0 | 🟢 | no node_modules in worktree, parents, or NODE_PATH; `npm test` exit 0, "all 44 test files passed" | corrupted `docs/reference/data/role-table.json`, `node test/run.mjs`: exit 1, 17 FAIL |
| C2 | `npm run build` needs node_modules, exit 0 | 🟢 | `npm ci` exit 0, `npm run build` exit 0, wrote ui.html 3695.6 KB, tree clean after | build before `npm ci`: exit 127, `sh: tsc: command not found` |
| C3 | `npm run smoke` boots headless Chrome, exit 0 | 🟢 | exit 0, "SMOKE PASS", screenshots in smoke-out/ (local macOS Chrome, auto-detected) | replaced `dist/ultimate-tokens.html` with a stub, `node test/smoke/smoke.mjs`: exit 1, "smoke threw" |
| C4 | CI build-test green on latest main | 🟢 | run 34804556354, headSha 7faf3aa = origin/main; jobs build-test, panda-smoke, deploy all success. f9e20c5 itself is not on main and has no CI run | `--status failure` query returns run 34620527115 on main, so the query can surface red |
| C5 | main has no branch protection | 🟢 | protection API 404 "Branch not protected"; `branches/main .protected` false; rulesets 0 | same `.protected` query on cli/cli trunk returns true |
| C6 | PRs land by squash-merge | 🟡 | 48/50 subjects end `(#NNN)`, 0 multi-parent commits in last 50; the 2 exceptions are the direct sdlc commits f9e20c5 and 34dfc7d. Concern: repo allows squash, merge commit, AND rebase (all true), so squash is practice, not enforced | the subject grep excludes the 2 direct commits; an older merge commit 86f3e1f (PR #1) exists beyond the window |
| C7 | root CLAUDE.md and .claude/CLAUDE.md identical | 🔴 | root `CLAUDE.md` does not exist at HEAD or in the working tree; moved under .claude/ in d4f5e80 (2026-07-29). Survey Harnesses row for root CLAUDE.md is wrong | `cmp` on the missing path fails; `git show HEAD~1:CLAUDE.md` fatal |
| C8 | .gitignore ignores .claude/ops/ while ops files tracked | 🟡 | `check-ignore --no-index` matches `.gitignore:14 .claude/ops/`. Concern: 7 ops files are tracked, not 4 (3 more under `.claude/ops/reports/`) | `check-ignore --no-index src/ui/app.js`: no match, exit 1 |
| C9 | docs/reference holds 5 files | 🔴 | `git ls-files docs/reference`: 51 files; 4 at top level, rest in colors/ (3), colors/categories/ (8), data/ (6), geometry/ (2), references/ (14), reviews/ (6), rubrics/ (4), typography/ (4) | same count on a subdir differs per dir (4 vs 51), so the count is sensitive to scope |
| C10 | No ADR files, though CLAUDE.md cites ADR-017 | 🔴 | ADRs exist as sections in one file: `docs/reference/references/decision-records.md` has 22 `## ADR-NNN` headings through ADR-022, incl. `## ADR-017` at line 386. Scout searched filenames and `# ADR` only | `git grep 'ADR-017'` returns the heading, so the check detects presence |
| C11 | 3 agents, 14 skills, 1 hook are the complete harness | 🟡 | counts confirmed: 3 agent files, 14 SKILL.md (49 files under skills/), 1 hook. Concern: the table also omits 3 tracked `.claude/ops/reports/*.md` and shows no root CLAUDE.md fix (see C7) | a count of all tracked `.claude/` paths grouped by top dir shows the extra ops/reports entries, so omissions are detectable |
| C12 | test/run.mjs is sole entrypoint; 47 test .mjs | 🔴 | 47 .mjs confirmed. "Sole" is false: run.mjs runs 44 files; `test/smoke/smoke.mjs` is a separate entrypoint (`npm run smoke`); `test/ui/counts.mjs` is a helper; CI also runs `scripts/smoke-panda.mjs` and `mcp/describe-eval-runner.mjs`. Also: test/ has 50 tracked files (47 mjs, 2 json, 1 css), not 49 | diffed every tracked test .mjs against the TESTS list: smoke.mjs and counts.mjs not listed |
| C13 | generated files byte-identical after `npm test` | 🟡 | `npm test` then `git status --short`: empty, twice; after `npm run build` also empty. Concern: `src/ui/type-fonts.js` is NOT regenerated by `npm test` (manual `gen:type-fonts`), so it is identical only because nothing touches it | appended junk to ui.html, mcp-assets.js, categories/travel.js, type-fonts.js, ran `npm test`: first three restored, type-fonts.js stayed dirty (and leaked into regenerated ui.html) |
| C14 | no secrets in tracked files | 🟢 | grep of all tracked files for sk-, AKIA, ghp_, github_pat_, xox*, PEM private keys: 0 hits; no tracked .env/.pem/.key/id_rsa; `api_key\|token = '<16+ chars>'` literals: 0 hits. History not scanned | same ghp_ pattern on a planted test file: 1 hit |

Survey corrections for the Conductor: root `CLAUDE.md` row removed (C7); ops tracked count 7 (C8); docs/reference 51 files (C9); ADRs live in decision-records.md, 22 of them (C10); test file total 50 (C12).
