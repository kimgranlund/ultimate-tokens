verdict: 🔴
sha: 5091460f61a8c9626dd2cfbfc53ef76348f6e780

# Verdict rg-U4 · 🔴

Unit `unit/rg-U4` at 5091460f, plan revision 12, recorded base b8abddf0, the merge of origin/main at c5f7bb2c. I read the unit's own work as `c5f7bb2c..5091460f`. I checked everything in the shared clone `/private/tmp/claude-501/rg-u4-v` with no `node_modules`. Every control edit was reverted, and `git status --short | wc -l` printed `0` after each one. The one `npm test` run took 01:03:03 to 01:11:46 at a load of about 22, so it gives no timing figure. Tally: 12 🟢, 2 🟡, 4 🔴.

| # | Criterion | State | Evidence | Negative control |
|---|---|---|---|---|
| 1 | U4-1 G0 | 🟢 | `CLOSED`, `0`, and `git merge-base --is-ancestor ae4206ac 5091460f` gives `0`. Note: origin/main is now 2890d67a and adds 1 line carrying the glyph, so the pre-land re-merge must rerun `--fix` | `git merge-base --is-ancestor origin/main 5091460f` against today's main prints `1` |
| 2 | U4-2 (P1, P2, P3, P9 together) | 🟡 | follows rows 3 to 6, sub-row states `P1 🟢 P2 🟢 P3 🟢 P9 🟡` | each sub-row's own control below bites |
| 3 | P1 npm test, count, clean tree | 🟢 | `✓ all 51 test files passed`, exit `0`, the TESTS count `51`, and `git status --short \| wc -l` gives `0` after the run; `▶ repo/em-dash.mjs pass` | `sed 's/"scrim/"scrimX/'` on `role-table.json`, then `node test/engine/semantic.mjs` prints `FAIL  refs-canonical, ordered key set != canonical`, `ctl exit 1` |
| 4 | P2 gate clean over 686 files, reads Unicode, not exempt from itself | 🟢 | `exit 0`, `1` self-test line, `em-dash: clean (686 files scanned)`, `4` escapes, `0` raw glyphs in the gate. My own count of non-binary tracked files is also `686`. The 43 files that still hold a raw glyph are all `.md` files with the glyph inside a code span, and 4 of those spans wrap across a line (`tkt-0031.md:37`, `:82`, `geometry-system/SKILL.md:180`, `best-practices.md:19`) | (a) one glyph planted in `src/engine/tonal.js` gives `✗ src/engine/tonal.js:1275`, `FAIL: 1 em dashes`, `a exit 1`. (b) a code span in `README.md` stays `clean`, `b exit 0`. (c) a template literal in `test/run.mjs` gives `c exit 1`. (d) a `"latin1"` reader at line 419 gives `self-test: FAIL 1 case(s)`, `✗ reader: readText() found 0 dashes, expected 1`, `d exit 1` |
| 5 | P3 `--fix` applies exactly the rule table | 🟢 | at 0faca70d, the diff stat is `339 files changed, 9743 insertions(+), 9743 deletions(-)` on both runs. Numstat gives `0`. The rule counts are `R0 92 R1 27 R2 573 R3 462 R4 0 R5 0 R6 300 R7 31 R8 8623`. The refused list has `92` lines and all `92` carry a habitat. The `to` counts are `7` and `7`, double punctuation is `121` and `121`, and lone tokens are `0`. The output equals e55a2ce9 except `palette-preview.svg` and `type-fonts.js`, and those equal c90402b2. A `--fix` run at the head changes `0` files | the row-679 fixture changed to expect `\| a , b \|` gives `self-test: FAIL 1 case(s)`, `✗ R1 empty md cell: fix produced "\| a \| none \| b \|"`, `exit 1` |
| 6 | P9 scope pair check and numstat | 🟡 | the pair check gives `24` at c5f7bb2c and `31` at the recorded base b8abddf0, where `0` was expected. Numstat gives `0` at both. I read all 24: they are `// —` comment-marker pairs, their copies in the `ui.html` mirror, 2 `architecture.md` re-pins, cross-file stream artifacts, and the hand rejoin in `figma-styles-hard-constraints.md` (lines 18, 20, 101, 115 and 116, which close parentheses). None is an unrelated edit. The handoff still says `20` | one extra edit planted in `package.json` line 1 moves the pair check from `24` to `25` |
| 7 | U4-3 gate registered | 🟢 | `grep -c '"repo/em-dash.mjs"' test/run.mjs` gives `1`, and the npm test log shows `▶ repo/em-dash.mjs` once | with the entry removed from `test/run.mjs`, the same grep gives `0` (`1 file changed`) |
| 8 | U4-4 pivot regex survives and bites | 🟢 | `grep -c 'u2014 (?:not'` gives `1`. The four-pivot fixture prints `WARN: 4 em-dash pivot` (count `1`) | with the blind swept regex `/, (?:not` in place, the two counts are `0` and `0` |
| 9 | U4-5 entry file states the rule and the repair | 🟢 | `2`, `1`, and numstat `29	27	.claude/CLAUDE.md`: one bullet wrapped over lines 113 and 114, with the wording that step 3 prescribes | with the two bullet lines deleted: `ctl: 0 0 27	27` |
| 10 | U4-6 the three record checks on the swept records | 🔴 | all three exit `0`, but the zero is vacuous for two of them. `card-source-range-check.sh` prints `range mismatches: 24` (`no heading ADR-001` and 23 more). R2 turned `## ADR-001 — title` into `## ADR-001: title`, and the check greps `"^## $id "` with a trailing space. It also never exits non-zero on its count. `card-amendment-check.sh` uses `awk "/^## ADR-$num /,..."`, which now matches nothing, so no ADR body is read (`empty-body 24 amended-seen 0`). Fix: match `^## ADR-NNN[: ]` in both scripts, and have the range check exit 1 when its count is above 0 | at c5f7bb2c the same scripts print `range mismatches: 0` and `empty-body 0 amended-seen 3`, so the drop is the sweep's doing |
| 11 | U4-7 every refused line rewritten by hand | 🟢 | the gate prints `em-dash: clean (686 files scanned)`. My sample of the refused-list rewrites (rows 12 and 13 name them) reads as sentences, apart from the U4-8 line in row 12 | c5f7bb2c's text of `src/ui/app.js:690` put back gives `✗ src/ui/app.js:690`, `FAIL: 2 em dashes ... in 1 files`, `exit 1` |
| 12 | U4-8 the five half-rewritten lines | 🔴 | `src/engine/ds-export.js:776` is Make export output and now reads `(a surface that inverts the app's OWN neutral, toasts, tooltips, never a brand family's)`. The pause became a list, so toasts and tooltips read as things the surface inverts. `decomposition.md:77` has a similar list-like reading, a 🟡 (`at \`src/engine/\`, \`hct.js\`, \`tonal.js\`, ...`). `rubric.md:3`, `app.js:690` and `brands.json:1960` read as one sentence | c5f7bb2c's text is `neutral — toasts, tooltips —`, and all five appear in the 0faca70d refused list (`grep -c -F` gives `1` each) |
| 13 | U4-9 over-refusals after `/`, `>`, `%` | 🟢 | `styles.css:6` reads `<style id="ultimate-tokens-theme">, exportCSS() over the 8 default`. The lines `:106`, `marketing-manager-agent.md:4`, `project-docs/SKILL.md:44`, `CHANGELOG.md:420`, `tonal.mjs:243` and `ds-gates.js:14` are all rewritten and no glyph is left | each of those lines appears in the 0faca70d refused list (`grep -c -F` gives `1` for all 12 I checked) |
| 14 | U4-10 user-visible program strings | 🔴 | about 50 labels and toasts are fixed. The following still read as lists: `geometry.js:733` `"Choose a treatment + base height, icon, font, padding, gap & radius follow by the centering law."`, `typography.js:630` `"Choose a treatment + body size, fonts, tracking, weight & leading follow."`, `color.js:1788` `"...organized under, Material, Brand, System, or Data."` and `color.js:600` `"Hue × chroma, context + proposed"`. Export headings: `ds-export.js:975` `# ${name}, Design System`, `:1264` `## Hard rules, IMPORTANT`, `:1486` `# Components, Overview`, `:1525` `## Variants, shadcn's own \`<Button>\`, mapped`, `:1535` `## States, Tailwind modifiers, not separate tokens`, and `mcp/brand-kit-core.mjs:61` `# ${kit.name}, usage`. The handoff lists only the rewritten lines one per line; the kept ones are still grouped as "about 80 lines" | the handoff has `1` hit each for `geometry.js:346`, the fixed label, and `0` for `geometry.js:733`, `typography.js:630`, `color.js:1788` and `color.js:600` |
| 15 | user-visible string sample (src/, docs/marketing/, store copy; 70+ lines read) | 🔴 | about 55 read fine, for example `"Couldn't apply to Figma, please try again."`, `"No legacy styles found, this file is clean."` and `"Contrast: on-colors vs fills (≥4.5:1)"`. Store copy that gets pasted into product descriptions now reads as lists: `store-copy.md:208` `• Three composing systems, Color · Typography · Geometry`, `:210` `• Your own naming, Ultimate, Material 3-style ...`, `:212` `• Brand-Kit MCP, feed your exact tokens...`, `:154` `**Unlimited brand kits**, every client...` and `:155` `**The complete export suite**, every target, every format`. The same holds for `landing.md:77` `**Free**, the full generator, 2 brand kits`. These are 🟡s: `voice-platform.md:121` still says "em-dash form" but its example is now a comma, and the WARN text in `voice-check.mjs` reads `("…, not …")`. Note that the plan's Not-in-scope table sends marketing hand-polish to its owning seat, so the Conductor rules whether these five store lines are U4's | at c5f7bb2c the same lines read `Three composing systems — Color · Typography · Geometry` and `**The complete export suite** — every target`, and R3 missed them because they sit inside a `> ` blockquote |
| 16 | no code behavior changed | 🟢 | I compared the code skeleton (comments dropped, string text blanked) across 138 changed `.js`/`.mjs`/`.ts` files. Only three kinds of difference remain: the pivot regex escape, which matches the same text; two `test/engine/exports.mjs` regexes that follow the export heading text; and the one `TESTS` entry. All three `.css` diffs are in comments. All 12 `.json` files have the same structure with only string values changed. `npm test` and its drift gate are green | the same skeleton tool flags `test/run.mjs` (`constTESTS=[...S,]` has one `S` more), which shows it detects a code change |
| 17 | R37 applied in store-drift-check.mjs and store-copy.md | 🟢 | `PRODUCT_NAME = { 1182548: "Ultimate Tokens Pro", 1182535: "Ultimate Tokens Studio" }`, `DESC_PROBES` `"derived, not guessed"`, `store-copy.md:117` `Ultimate Tokens Pro` and `:171` `Ultimate Tokens Studio`. `git grep -E 'Ultimate Tokens, (Pro\|Studio)'` finds nothing outside the handoff | the same grep at e55a2ce9, the mechanical sweep, gives `store-drift-check.mjs:1` and `store-copy.md:2` |
| 18 | diff inside the declared scope, branding clean, private paths untouched | 🟢 | 355 files changed since c5f7bb2c. Every one carried the glyph at c5f7bb2c except `.sdlc/architecture.md`, a departure the handoff declares, and the handoff itself. `branding: clean (678 files scanned)`. `.claude/docs/other` and `node_modules` appear `0` times in the diff and `0` times in `git ls-files`. The `package.json` and `.github` changes are comment or description text only, with no new script or job | a copy of `decision-records.md` placed at `.sdlc/verdicts/zz-ctl.md` gives `FAIL: 3 branding violation(s) across 679 files`, `exit 1`, and the scope loop prints `NOGLYPH` for the 2 files that lacked the glyph |

## Reds to fix

1. U4-6: fix the heading match in `.sdlc/checks/card-source-range-check.sh` and `card-amendment-check.sh` so it accepts `## ADR-NNN:`, make the range check exit 1 on mismatches, and rerun both. Expect `range mismatches: 0` and 3 amended bodies seen.
2. U4-8 and U4-10: rewrite with a colon or a sentence break `ds-export.js:776`, `geometry.js:733`, `typography.js:630`, `color.js:600`, `color.js:1788`, the `ds-export.js` export headings at 975, 1264, 1386, 1449, 1486, 1525, 1535 and 1545, and `brand-kit-core.mjs:61`. Update the two `exports.mjs` regexes if the headings they match change. Then run `npm test` to regenerate the mirrors. List every kept string one per line, as the U4-10 command asks.
3. Store copy: the Conductor rules whether `store-copy.md:154`, `:155`, `:208`, `:210`, `:212` and `landing.md:77` are U4's or `marketing-manager-agent`'s. They read as lists in text customers see.

## Yellows

P9: correct the handoff to `24` and name the rejoin lines. Also fix the wording in `voice-platform.md:121` to `:124` and the `voice-check.mjs` WARN example, and `decomposition.md:77`.

My `rm -rf` of the clone was denied. It is still at `/private/tmp/claude-501/rg-u4-v`.

verdict: 🔴
sha: 5091460f61a8c9626dd2cfbfc53ef76348f6e780
