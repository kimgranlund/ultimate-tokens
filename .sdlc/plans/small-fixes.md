---
status: approved
ticket: #717
priority: P3
lane: tooling
size: S (U1 S = 1 point; 1 point)
labels: kind:defect · size:small · severity:minor · lane:tooling · P3 (as minted on #717)
written: 2026-09-22
amended: 2026-09-22 (revision 2: the checkability review of adf9e5a9 folded, 9 green, 7 yellow, 0 red; #731 split out; see Revisions)
depends: none
head: 88905891 (`plan/small-fixes` is cut from it, local only, never pushed by the planner; `origin/main` is a4675242 at revision 2, 6 commits on, none touching `test/smoke/` or `package.json`)
measured-at: 88905891 in a scratch worktree with `node_modules` symlinked from the root checkout, 2026-09-22
branch: plan/small-fixes
inputs: ticket #717; the issue triage of 2026-09-22 (Lane scratchpad, `issue-triage-2026-09-22.md`, plan group 4); the checkability review of adf9e5a9 (`policy-plans-checkability.md`, Plan C); `test/smoke/smoke.mjs`, `package.json`, `.github/workflows/ci.yml`, `.sdlc/adapter.md` §1 and §2.1, `.sdlc/baseline.md`, `.sdlc/checks/baseline-agrees-check.sh`, `.sdlc/questions/records-followup-repoint.md`
---

# The smoke script owns its Chrome and its port (#717)

One S unit. `npm run smoke` spawns headless Chrome on the fixed CDP port 9333 and kills it only in a `finally` block, so a signalled run leaves the browser behind and the next run drives whatever is listening. The fix gives every run its own profile directory and an OS-chosen port, read back from Chrome's `DevToolsActivePort` file, and one idempotent cleanup that runs on `finally`, on `exit`, and on `SIGINT`, `SIGTERM` and `SIGHUP`.

#731 (`model.mjs` duplicates its own hex-to-OKLCH conversion) was the second unit of revision 1. It exists only on `plan/preset-intent-fidelity` and cannot start before #681 lands, so it becomes its own small plan then (owner ruling 2026-09-22); this plan lands alone.

What the measurement found that the ticket did not say. It changes the criteria, so it comes first.

1. A normal `npm run smoke` run does not leak. The `finally` block's `SIGKILL` reaches the browser process, and its helper processes exit on their own within 2 s (3 alive at t+0, 0 at t+2 s, measured twice). The leak is the signal path: a run stopped with `SIGTERM` at 5 s exits 143 and leaves 9 Chrome processes (11 in the checkability rerun), the listener holding port 9333. So the criterion is "no Chrome after a signalled run", with a 5 s grace for helpers, and a plain "no Chrome after a green run" would pass today and prove nothing.
2. The fixed port does more than orphan: it attaches to strangers. With a decoy HTTP server on 9333, `node test/smoke/smoke.mjs` sent the decoy 113 requests, waited the full 45 s deadline and failed with `smoke threw: Chrome CDP did not come up within 45s (last: HTTP 404)`, while the Chrome it had spawned sat unused. With a stale smoke Chrome on the port instead of a decoy, the run drives that browser and passes, which is what #717 reports for the `20298cc` figures.
3. Chrome Canary on this host (`Google Chrome 156.0.8068.0 canary`, at the path `smoke.mjs` already lists; `CHROME_BIN` is unset) writes `DevToolsActivePort` under `--remote-debugging-port=0` with a real temp `--user-data-dir`: two lines, the port then the browser websocket path, present after about 8.5 s, and `/json/version` answers on that port. That is the mechanism the fix rests on, verified on the one local Chrome; CI's `google-chrome` verifies it on the first CI run of the plan branch.

Scope wall. Paths this plan may change: `test/smoke/smoke.mjs`, `test/smoke/chrome.mjs` (new), `test/smoke/launcher.mjs` (new), `test/smoke/fixtures/fake-chrome.mjs` (new), `package.json` (`scripts.smoke` only), this plan, its own handoffs, verdicts and questions (`.sdlc/{plans,handoffs,verdicts,questions}/small-fixes*`), and `.sdlc/board.md` (Orchestrator). Nothing under `src/`, `scripts/`, `.github/`, `test/run.mjs`, any other `test/` file, or any other `.sdlc/` record. No generated file moves: nothing bundled changes.

Prose rules for every line this plan adds. No em dash outside an inline backtick span that quotes program output. No bold inline labels. The retired maker brand is paraphrased, never quoted. `grep -P` is absent on this host: PCRE runs through `perl`.

Criteria ids: P rows for the plan, numbered rows for the unit, cited as U1-3. Diffs are taken against `origin/main` after `git fetch -q origin`, never against a merge base: at the plan tip `git merge-base plan/small-fixes HEAD` equals `HEAD`, so a merge-base diff is empty and proves nothing (checkability finding, cross-cutting).

## Measured by the planner on 2026-09-22 at 88905891

Every "today" value below comes from this section unless a row says otherwise. Timing was taken on a loud host and is indicative only; counts are load-independent.

| Fact | Value |
|---|---|
| Chrome on this host | Chrome Canary only, `/Applications/Google Chrome Canary.app/Contents/MacOS/Google Chrome Canary` (in the script's own search list). `CHROME_BIN` unset. No `/usr/bin/google-chrome`, no `chromium` |
| `node test/smoke/smoke.mjs` after `npm run build`, normal run | exit 0, 41 check-mark lines, `real 23.75` loud (`real 20.25` in the checkability rerun). Chrome processes matching `remote-debugging-port=9333`: 3 at t+0 s, 0 at t+2 s, 0 at t+5 s (helpers of the killed browser, exiting on their own) |
| the same, `SIGTERM` sent at 5 s | exit 143. 9 Chrome processes alive 1 s later (11 in the checkability rerun), the listener on 9333 among them (`lsof -nP -iTCP:9333 -sTCP:LISTEN`: one `Google` row). Reaped by exact pattern right after |
| the same, with a decoy `node` HTTP server holding 9333 (answers 404, counts requests) | exit 1 after the 45 s deadline, 0 check-mark lines, `smoke threw: Chrome CDP did not come up within 45s (last: HTTP 404)`; the decoy counted 113 requests; the spawned Chrome was killed by `finally` (0 left) |
| Canary, `--headless=new --no-sandbox --disable-gpu --remote-debugging-port=0 --user-data-dir=<mktemp -d> about:blank` | `DevToolsActivePort` present after 34 polls of 250 ms: line 1 `60568`, line 2 `/devtools/browser/<uuid>`; `curl /json/version` on that port answers `"Browser": "Chrome/156.0.8068.0"`. `SIGKILL` of the browser pid: 0 processes with that `user-data-dir` 0.5 s later |
| `pgrep -f 'remote-debugging-port=9333' \| wc -l` before any run | `0` (the 2026-09-20 reap holds) |
| `test/run.mjs` `TESTS` length | 48 on 88905891 and on a4675242 |
| `sh .sdlc/checks/baseline-agrees-check.sh` | `stale total: 0`, exit 0; `tests: baseline 48, test/run.mjs TESTS 48` |
| `node test/repo/branding.mjs \| tail -1` | `branding: clean (533 files scanned)` in the scratch worktree (602 in the root checkout, which carries untracked files; the number is the file count, not a verdict) |
| records citing `smoke.mjs` by line or the port | `.sdlc/plans/records-followup.md:43` cites `test/smoke/smoke.mjs:293` (the PASS line); `.sdlc/questions/records-followup-repoint.md:12,18,29` cite `:44` and the port. Both are #709 records, history once #720 lands (see Not in scope and Risks) |

## The design, stated once

One launcher, `test/smoke/chrome.mjs`, exporting two functions and nothing else:

- `launchChrome(bin, extraArgs, { deadlineMs })`. Makes a profile directory with `mkdtempSync(join(tmpdir(), "ultimate-tokens-smoke-"))`, spawns `bin` with `[...extraArgs, "--headless=new", "--no-sandbox", "--disable-gpu", "--disable-dev-shm-usage", "--remote-debugging-port=0", "--hide-scrollbars", "--window-size=1440,900", "--user-data-dir=<dir>", "about:blank"]` (the flags `smoke.mjs` passes today, the port at `0`), polls `<dir>/DevToolsActivePort` every 400 ms until its first line parses as a port or `deadlineMs` passes, and resolves `{ proc, port, dir, close }`. On the deadline it calls `close()` and rejects with the message `Chrome CDP did not come up within <s>s (last: <reason>)`, the shape the script prints today, so the CI diagnosis path is unchanged. `close()` is idempotent: `proc.kill("SIGKILL")`, then `rmSync(dir, { recursive: true, force: true })`; a second call is a no-op. `proc` and `dir` are assigned in closure state at spawn time, before the poll, so `close()` reaches a browser that was spawned and not yet discovered.
- `onExit(fn)`. Registers `fn` on `process.on("exit")` and on `SIGINT`, `SIGTERM` and `SIGHUP`; a signal handler runs `fn()` then `process.exit(128 + signal number)` (130, 143, 129), the exit code the shell would have reported for an unhandled signal, so a CI `timeout` or a `kill` reads the same. The exit code is therefore not evidence that the handler ran (an unhandled `SIGTERM` also reports 143); the evidence is the browser pid gone and the directory gone, and every criterion below reads those.

`smoke.mjs` calls `onExit` before `launchChrome`, so a signal that lands during the 45 s start-up wait is also cleaned up; its `finally` calls `close()`; the `cdp()` helper reads `port` from the launch result; the `PORT` constant and the `spawn` are deleted. The `/json/version` and `/json/new` flow stays as it is: the discovered port replaces the fixed one, nothing else about the drive changes.

The profile directory is the process needle, and it is scoped to one run, not to the host. Every real-browser criterion below sets `TMPDIR` to a directory the seat made for that run (`T=$(mktemp -d "$F/tmp-XXXX")`), so `pgrep -f "user-data-dir=$T/ultimate-tokens-smoke-"` and `ls -d "$T"/ultimate-tokens-smoke-*` match that run's Chrome and nothing another seat's concurrent smoke run started (checkability finding on P5). `os.tmpdir()` honours `TMPDIR` on macOS and Linux, so the launcher needs no flag for this. Today's needle (`remote-debugging-port=9333`) stops matching once the fix lands, so the "today" column uses the old one.

The unit test, `test/smoke/launcher.mjs`, runs without Chrome and without a build. `test/smoke/fixtures/fake-chrome.mjs` stands in for the browser: started as `process.execPath` with the fixture path as `extraArgs`, it reads `--user-data-dir=` and `--remote-debugging-port=` from its argv, exits 2 if the port flag is anything but `0` (so a launcher that regresses to a fixed port reds leg (a) instead of passing by accident), listens on `127.0.0.1:0`, answers `/json/version` with `{"Browser":"fake-chrome/1"}`, writes `DevToolsActivePort` (port, newline, `/devtools/browser/fake`) into the profile directory, and stays alive on a timer until killed. With `FAKE_CHROME_MUTE=1` it never writes the file. Legs, each printed as one `pass`/`FAIL` line, and the file exits 1 on any FAIL:

| leg | asserts |
|---|---|
| (a) discovery | `launchChrome(process.execPath, [fixture])` resolves; `port` equals line 1 of `<dir>/DevToolsActivePort`; `fetch("http://127.0.0.1:<port>/json/version")` returns the fake's JSON |
| (b) close | after `close()`, `process.kill(proc.pid, 0)` throws `ESRCH` within 2 s and `existsSync(dir)` is false; a second `close()` does not throw |
| (c) SIGTERM | a child `node test/smoke/launcher.mjs --child` installs `onExit`, launches the fake, prints `<fake pid> <dir>` and waits. The parent sends `SIGTERM`: the fake pid gone within 2 s (`process.kill(pid, 0)` throws `ESRCH`), `dir` gone; the child's exit code is printed, not asserted |
| (d) SIGINT | as (c) with `SIGINT` |
| (e) deadline | `FAKE_CHROME_MUTE=1` and `deadlineMs: 1500`: the launch rejects with a message starting `Chrome CDP did not come up within`, the fake pid is gone within 2 s, `dir` gone |
| (f) signal before discovery | `FAKE_CHROME_DELAY_MS=3000` (the fake writes `DevToolsActivePort` only after the delay): a child as in (c) installs `onExit`, starts the launch, prints `<fake pid> <dir>` as soon as the process is spawned, before discovery. The parent sends `SIGTERM` at 1 s, while the launch is still pending: the fake pid gone within 2 s, `dir` gone. Added by revision 4 |

The test is not registered in `test/run.mjs`. It runs from the `smoke` script, `npm run build && node test/smoke/launcher.mjs && node test/smoke/smoke.mjs`, so CI's smoke step and every local `npm run smoke` run it, and it can be run alone on any host by `node test/smoke/launcher.mjs`. Registering it in `TESTS` instead would raise N and, under the post-#691 landing rule (`.sdlc/checks/baseline-agrees-check.sh`, `tests:` row), oblige a three-run baseline rerun on a quiet host for a 1 s test; that is Q1 for the owner, default no.

## Criteria (plan-level: the builder runs them, the verifier reruns them, pre-land runs them all)

Every negative control that edits a file runs in a throwaway clone (`git clone -q --shared . "$F/neg"`), never in the unit worktree, and the clone is made from the unit's commit, not its working tree (adapter control rule of 2026-09-21: `git -C "$F/neg" log -1 --format=%h` must print the unit head). `F` is a directory the seat makes under its own scratchpad with a name no other seat would pick, and removes by that exact name. `T` is `$(mktemp -d "$F/tmp-XXXX")`, made fresh for each real-browser row and exported as `TMPDIR` for that row's command only. In table cells `\|` is the escape for a plain `|`: type it unescaped. `N` is the length of `TESTS` on the branch, 48 on a4675242; the plan registers no test file, so N is the base's N. Every diff runs after `git fetch -q origin` and reads `origin/main`.

| # | Criterion | Command | Expected | Negative control | Today |
|---|---|---|---|---|---|
| P1 | `npm test` green with no `node_modules`, the count agrees, the tree is byte-stable | `npm test 2>&1 \| tail -1; perl -0ne 'my ($b) = /const TESTS = \[(.*?)\];/s; my @m = $b =~ /"[^"]+\.mjs"/g; print scalar(@m), "\n"' test/run.mjs; git status --short \| wc -l` | the runner's pass line naming N files, then N, then `0` | in the clone: `sed -i '' 's/"scrim/"scrimX/' docs/reference/data/role-table.json && npm test > "$F/neg.log" 2>&1; echo "exit $?"` prints `exit 1` (the gate-split plan's P1 control, reused; the checkability review ran it: `exit 1`, `FAIL refs-canonical`) | `✓ all 48 test files passed`, `48`, `0` (checkability run at adf9e5a9) |
| P2 | `npm run build` green, tree clean after (the unit edits `package.json`) | `npm run build > "$F/b.log" 2>&1; echo "exit $?"; tail -1 "$F/b.log"; git status --short \| wc -l` | `exit 0`, a line starting `wrote figma/plugin/ui.html`, `0` | in the clone, `perl -pi -e 's/"smoke": "npm run build && /"smoke": "npm run build && node test\/smoke\/missing.mjs && /' package.json; npm run smoke > "$F/s.log" 2>&1; echo "exit $?"; grep -c 'Cannot find module' "$F/s.log"` prints `exit 1`, `1`: a script that names a missing file reds the smoke run, so the script line is live (checkability review: ran, bites) | `exit 0`, `wrote figma/plugin/ui.html 3780.5 KB`, `0` |
| P3 | branding clean, and no added prose line carries an em dash (U+2014) | `bash -c 'set -o pipefail; node test/repo/branding.mjs \| tail -1'; git diff origin/main \| grep -v '^+++ ' \| grep '^+' \| perl -CSD -ne 's/`[^`]*`//g; print if /\x{2014}/' \| wc -l; git diff origin/main -- . ':(exclude).sdlc/handoffs' \| grep -v '^+++ ' \| grep '^+' \| perl -CSD -ne 'print if /\x{2014}/' \| wc -l` | `branding: clean (N files scanned)`, `0`, then a raw count the verifier derives itself: it lists every added line the third command matches, and each must be a backtick span quoting a line a program printed (the `SMOKE PASS` line of `smoke.mjs:293` is the one expected, if the handoff quotes it); a matched line that is not such a quote is a red, whatever number the handoff states | in the clone: `cp docs/reference/references/decision-records.md .sdlc/verdicts/small-fixes-x.md` reds the first; one added prose line with the dash makes the second `1` and adds one non-quote line to the third's listing | clean at 533 files in the scratch worktree; the plan's own added lines: `0` and `0` against 88905891 (checkability run) |
| P4 | scope wall: the unit's files, the plan's own records, the board, nothing else | `git diff --name-only origin/main \| grep -v -E -e '^test/smoke/smoke\.mjs$' -e '^test/smoke/chrome\.mjs$' -e '^test/smoke/launcher\.mjs$' -e '^test/smoke/fixtures/fake-chrome\.mjs$' -e '^package\.json$' -e '^\.sdlc/(plans\|handoffs\|verdicts\|questions)/small-fixes[^/]*\.md$' -e '^\.sdlc/board\.md$' \| wc -l; git diff origin/main --stat -- package.json \| tail -1` | `0`, then a line ending `1 file changed, 1 insertion(+), 1 deletion(-)` (the `smoke` script line only) | a fixture of four names piped through the filter, `printf 'src/engine/prime.mjs\n.sdlc/plans/records-followup.md\n.sdlc/verdicts/small-fixes-U1.md\ntest/smoke/chrome.mjs\n'`, prints `2` (the engine file and the other lane's plan are refused; the plan's own verdict and the unit file pass) | fixture prints `2` (run by the planner, revision 2). The plan branch is 6 commits behind a4675242, so `git diff --name-only origin/main` lists 13 files of main's own until the Orchestrator rebases; this row reads `0` only on a rebased branch, which is why the rebase precedes every criterion run |
| P5 | `npm run smoke` green where Chrome exists, the launcher test ran, and this run's Chrome and profile are gone. Split by revision 6: P5a is this row run locally; P5b is `build-test` green on the graded sha, cited by run id, graded at pre-land | `TMPDIR="$T" npm run smoke > "$F/sm.log" 2>&1; echo "exit $?"; grep -c '^SMOKE PASS' "$F/sm.log"; grep -c '^PASS: launcher' "$F/sm.log"; sleep 5; pgrep -f "user-data-dir=$T/ultimate-tokens-smoke-" \| wc -l; ls -d "$T"/ultimate-tokens-smoke-* 2>/dev/null \| wc -l` | `exit 0`, `1`, `1`, `0`, `0`. Locally on this Mac (Chrome Canary) by builder and verifier; in CI by the `build-test` job's smoke step, where the last two figures are not read (the runner is discarded) and U1-2 leg (b) is the launcher-level proof that runs there | none for a green run: helpers exit on their own today, so the process count cannot red before the fix. U1-3 is the discriminating row; U1-2 clone (1) reds the directory figure at launcher level | `SMOKE PASS` and 0 processes 5 s after a normal run today (old needle); no directory exists today because the script makes none; `PASS: launcher` `0` |

## Units

Checklist (the Orchestrator ticks it; the table below carries grades and paths):

- [x] U1 (S) the smoke launcher: per-run profile and port, cleanup on every exit path, launcher test · builder-l2 · reviewer-l1 · verifier-l1
- [x] U2 (S) the fake Chrome spawns a grandchild, and every launcher leg proves the whole process group dies · builder-l5 · reviewer-l4 · verifier-l3

Grades follow the Orchestrator's rule: an L2 builder gets reviewer-l1 and verifier-l1. U1 is L2 because process lifecycle is easy to get subtly wrong (a handler that runs `close()` but forgets to exit hangs the run; an `exit` handler that awaits does nothing).

| Unit | Size | Builder | Reviewer | Verifier | Touches |
|---|---|---|---|---|---|
| U1 the smoke launcher | S | builder-l2 | reviewer-l1 | verifier-l1 | `test/smoke/smoke.mjs`, `test/smoke/chrome.mjs`, `test/smoke/launcher.mjs`, `test/smoke/fixtures/fake-chrome.mjs`, `package.json` (`scripts.smoke`) |
| U2 the grandchild leg | S | builder-l5 | reviewer-l4 | verifier-l3 | `test/smoke/fixtures/fake-chrome.mjs`, `test/smoke/launcher.mjs` |

### U1: the smoke launcher (#717)

Steps. (1) Write `test/smoke/chrome.mjs` as the design states. (2) Write the fixture and `test/smoke/launcher.mjs` with legs (a) to (f); run it until green. (3) Rewrite `smoke.mjs`'s launch, `cdp()` port and `finally` onto the launcher; delete `PORT`. (4) `package.json` `smoke` script gains `node test/smoke/launcher.mjs &&` between the build and the smoke. (5) `npm run smoke` locally under a fresh `TMPDIR` (Chrome Canary is found by the existing search list), then U1-3's signalled run, then U1-5's decoy run. (6) The handoff quotes the `SMOKE PASS` line byte for byte inside a backtick span and names the new line number of that line for the #709 owner (Not in scope).

Rows U1-3 and U1-5 need a Chrome binary and run locally (this Mac has Canary at the listed path). Rows U1-1, U1-2 and U1-6 run on any host and in CI. Row U1-2 runs in CI through the `smoke` script.

| # | Criterion | Command | Expected | Negative control | Today |
|---|---|---|---|---|---|
| U1-1 | no fixed port, discovery from the profile file, every exit path handled | `grep -c -e 'remote-debugging-port=9333' -e 'const PORT = 9333' test/smoke/smoke.mjs test/smoke/chrome.mjs; grep -c 'remote-debugging-port=0' test/smoke/chrome.mjs; grep -c 'DevToolsActivePort' test/smoke/chrome.mjs; grep -c -e '"SIGINT"' -e '"SIGTERM"' -e '"SIGHUP"' -e '"exit"' test/smoke/chrome.mjs; grep -c 'ultimate-tokens-smoke-' test/smoke/chrome.mjs` | `test/smoke/smoke.mjs:0`, `test/smoke/chrome.mjs:0`, `1`, `1` or more, `4` or more (one line per registration, or fewer lines if one `for` loop registers them: then the builder states the loop line and the verifier reads it), `1` | the file as it stands today: the first grep prints `test/smoke/smoke.mjs:1` and `chrome.mjs` does not exist. A renamed fixed port would pass this row and is caught by U1-5 by behaviour | `test/smoke/smoke.mjs:1` (`const PORT = 9333`, line 44); the others `0` or `No such file` (checkability run) |
| U1-2 | the launcher test is green and every leg ran | `node test/smoke/launcher.mjs > "$F/l.log" 2>&1; echo "exit $?"; grep -c '^  pass  ' "$F/l.log"; grep -c '^  FAIL' "$F/l.log"; tail -1 "$F/l.log"` | `exit 0`, `6`, `0`, `PASS: launcher discovers its port from DevToolsActivePort and leaves no process on close, SIGTERM, SIGINT or deadline` | three clones, one edit each, each `exit 1` with at least one `FAIL` line naming the leg, and each clone's `git diff --stat` reading `1 file changed`: (1) delete the `rmSync` call in `close()`: legs (b), (c), (d) and (e) red on `dir` still present; (2) delete the `SIGTERM` registration (or its loop entry): leg (c) red on the fake pid still alive 2 s after the signal and `dir` still present (the child's exit code is 143 either way and proves nothing, which is why the leg does not read it); (3) make `close()` skip `proc.kill`: legs (b), (c), (d) and (e) red on the fake pid still alive | file absent |
| U1-3 | a signalled real-browser run leaves no Chrome and no profile, and holds no fixed port | after `npm run build`: `TMPDIR="$T" node test/smoke/smoke.mjs > "$F/t.log" 2>&1 & N=$!; sleep 5; kill -TERM $N; wait $N; echo "exit $?"; sleep 5; pgrep -f "user-data-dir=$T/ultimate-tokens-smoke-" \| wc -l; ls -d "$T"/ultimate-tokens-smoke-* 2>/dev/null \| wc -l` | `exit 143`, `0`, `0` | the same run against `smoke.mjs` at `origin/main` (a clone: `git clone -q --shared . "$F/old" && git -C "$F/old" checkout -q origin/main`, built there), with the old needle `pgrep -f 'remote-debugging-port=9333' \| wc -l`: `exit 143` and a count above `0` (9 and 11 measured). Run it only when `pgrep -f 'remote-debugging-port=9333' \| wc -l` prints `0` first, and reap the clone's Chrome by the exact pattern `pkill -9 -f 'remote-debugging-port=9333'` right after, recording the count reaped | `exit 143`, 9 processes (11 in the checkability rerun), the listener on 9333 |
| U1-4 | a green run leaves no Chrome and no profile directory | P5's last two figures | `0`, `0` | U1-2's clone (1): the launcher test reds on the directory; the real-browser run is not repeated for it | 0 processes 5 s after a green run today (helpers exit on their own); no directory exists today because the script makes none |
| U1-5 | a stranger on 9333 is never consulted | only when `pgrep -f 'remote-debugging-port=9333' \| wc -l` prints `0` (the decoy holds the port while it runs): `node -e 'let n=0;const s=require("http").createServer((q,r)=>{n++;r.writeHead(404);r.end()});s.listen(9333,"127.0.0.1");process.on("SIGTERM",()=>{console.log("decoy requests: "+n);process.exit(0)})' > "$F/decoy.log" 2>&1 & D=$!; sleep 1; TMPDIR="$T" node test/smoke/smoke.mjs > "$F/d.log" 2>&1; echo "exit $?"; kill -TERM $D; wait $D; cat "$F/decoy.log"; grep -c '^SMOKE PASS' "$F/d.log"` | `exit 0`, `decoy requests: 0`, `1` | the same against `smoke.mjs` at `origin/main` (the U1-3 clone): `exit 1`, `decoy requests: 113` (or any number above 0), `0`, and the log's FAIL line reads `smoke threw: Chrome CDP did not come up within 45s (last: HTTP 404)` | `exit 1`, `decoy requests: 113`, `0` (planner, and reproduced byte for byte by the checkability review) |
| U1-6 | CI runs the launcher test before the browser | `grep -c '"smoke": "npm run build && node test/smoke/launcher.mjs && node test/smoke/smoke.mjs"' package.json; grep -c 'npm run smoke' .github/workflows/ci.yml` | `1`, `1` (the workflow is not edited; it already calls the script) | P2's control | `0`, `1` |
| U1-7 | the launcher is green with `TMPDIR` unset, and leaves nothing under the default temp root | `env -u TMPDIR node test/smoke/launcher.mjs > "$S/u.log" 2>&1; echo "exit $?"; grep -c '^  pass  ' "$S/u.log"; ls -d "$(env -u TMPDIR node -p 'require("os").tmpdir()')"/ultimate-tokens-smoke-* "$(env -u TMPDIR node -p 'require("os").tmpdir()')"/launcher-legE-* 2>/dev/null \| wc -l` | `exit 0`, `6`, `0` | in `C`, `launcher.mjs:153` reverted to `process.env.TMPDIR = prevTmpdir;` (the pass-1 line): `exit 1`, `5`, and the log's FAIL line names leg (f) | `exit 1`, `5` (RD4) |
| U1-8 | the launcher is green on Linux, `TMPDIR` unset | `docker run --rm -v "$PWD:/w" -w /w node:24 sh -c 'node test/smoke/launcher.mjs; echo "exit $?"' > "$S/lx.log" 2>&1; tail -1 "$S/lx.log"; grep -c '^  pass  ' "$S/lx.log"` (Docker Desktop must be up: `docker info >/dev/null && echo up`) | `exit 0`, `6` | the same clone as U1-7's control: `exit 1`, `5` | `exit 1`, `5` (RD6) |
| U1-9 | a signal-leg red names the child's own error, not the symptom | in `C`, with `TMPDIR=/nonexistent` exported for the child only (edit `legF`'s `env` to add `TMPDIR: "/nonexistent"`): `node test/smoke/launcher.mjs 2>&1 \| grep -c 'ENOENT'` | `1` or more (the FAIL line carries the child's stderr) | the head as it stands prints `0` and `child never printed a pid/dir line` | `0` (RD7 shows the text exists only on the child's swallowed stderr) |
| U1-10 | leg (e) cannot pass by `pgrep` being absent | `PATH=/usr/bin:/bin:/nonexistent` is not enough on a host that has `pgrep`; run in Docker: `docker run --rm -v "$PWD:/w" -w /w node:24 sh -c 'mv /usr/bin/pgrep /usr/bin/pgrep.h; node test/smoke/launcher.mjs 2>&1 \| grep -E "deadline"'` | a `FAIL` line naming leg (e) with `pgrep` in its text | the head prints `pass  leaves no process or directory after a deadline with no DevToolsActivePort` with `pgrep` hidden (4.1) | `pass` (vacuous) |
| U1-11 | close() leaves no profile directory and no Chrome process, every time, not most times (revisions 7 and 8) | after `npm run build`, twenty consecutive real-Chrome runs of U1-4's green path, each with its own `T=$(mktemp -d "$F/tmp-XXXX")`: 5 s after each run, count the runs where `ls -d "$T"/ultimate-tokens-smoke-* 2>/dev/null \| wc -l` or `pgrep -f "user-data-dir=$T/ultimate-tokens-smoke-" \| wc -l` prints anything but `0` | `0` of `20` | the same twenty against the pass-2 head `1e25556d`: `1` or more of `20` on either count (the review measured `5` of `17` directories in round 1, and `0` of `30` directories but `3` of `30` processes in round 2) | `5` of `17` at `1e25556d` |
| U1-12 | a signal leg that times out leaves no fake Chrome behind (revision 7) | in a clone with leg (c)'s child made to hang past its timeout, run the launcher, then `pgrep -f fake-chrome.mjs \| wc -l` | `0` | the same against `1e25556d`: `1` or more | `1` at `1e25556d` (review F2) |

### U2: the fake Chrome spawns a grandchild (#717, critic CHANGES at 262ae996)

The critic on PR #735 (issuecomment-5787113504) showed that no leg bites on the process-group kill at `chrome.mjs:66`: with `close()` changed to kill only the parent pid, `launcher.mjs` passes 6 of 6, because `fixtures/fake-chrome.mjs` spawns no child. On macOS real Chrome hides it too; on Linux, where CI runs smoke, a parent-only kill leaves renderer and GPU children behind. U2 changes the test, not the launcher.

Steps. (1) At startup, before any `DevToolsActivePort` write and under every `FAKE_CHROME_*` mode, the fixture spawns one long-lived grandchild (not detached, so it stays in the fake's process group) and writes its pid to a file in the profile directory. (2) Every leg that ends in `close()`, a signal or the deadline (b, c, d, e, f) reads that pid before the cleanup and fails, naming the leg and the pid, if the grandchild is alive 2 s after it. The legs stay six and the last line of U1-2 stays byte for byte. (3) `chrome.mjs` and `smoke.mjs` are not edited. Grade L5: process-group lifetime is the concurrency case the grade rule names, and U1 needed a second pass on the same file.

| # | Criterion | Command | Expected | Negative control | Today |
|---|---|---|---|---|---|
| U2-1 | the launcher stays green with the grandchild asserts | U1-2's command | `exit 0`, `6`, `0`, U1-2's `PASS:` line | the parent-only mutant of U2-2 | `exit 0`, `6`, `0` at 262ae996 |
| U2-2 | the parent-only kill reds | in a clone, `chrome.mjs:66` `process.kill(-proc.pid, "SIGKILL")` replaced by `proc.kill("SIGKILL")`, `git diff --stat` `1 file changed`: `node test/smoke/launcher.mjs 2>&1 \| grep -c '^  FAIL'; ... \| grep -c grandchild` | `exit 1`; legs (b), (c), (d), (e) and (f) each FAIL with `grandchild` in the line (5 and 5) | the same mutant at 262ae996: `exit 0`, 6 passes (the critic's finding) | 6 of 6 pass under the mutant |
| U2-3 | the grandchild is really spawned and really in the group | in the clone of U2-2, `pgrep -f` on the grandchild's own marker right after the launcher exits | `1` or more under the mutant, `0` at the U2 head | the fixture's spawn line deleted: U2-1 reds on the pid file missing, never passes vacuously | no grandchild |
| U2-4 | U1-7 and U1-8 still hold (TMPDIR unset; Linux in Docker `node:24`) | U1-7's and U1-8's commands | `exit 0`, `6`, `0`; `exit 0`, `6` | U2-2's mutant in Docker: `exit 1` | `exit 0`, `6` |
| U2-5 | scope | `git fetch -q origin; git diff --stat origin/plan/small-fixes...HEAD -- . ':!.sdlc'` | only `test/smoke/fixtures/fake-chrome.mjs` and `test/smoke/launcher.mjs` | a stray edit to `chrome.mjs` shows as a third file | no unit diff yet |
| U2-6 | `npm test` and `npm run smoke` green on the unit head, tree clean after | `npm test; npm run smoke; git status --short \| wc -l` | exit 0, exit 0 with `SMOKE PASS`, `0` | P2's control | green at 262ae996 |

## Not in scope

| Item | Why | Where it goes |
|---|---|---|
| #731, the duplicate hex-to-OKLCH conversion in `src/ui/model.mjs` | Exists only on `plan/preset-intent-fidelity`; it cannot start before #681 lands and would hold this PR open for it. Revision 1 carried it as U2 with a start gate; the owner ruled 2026-09-22 to split it out | its own small plan once #681 lands; the revision 1 design (delete `anchorRgbOf` and `rgbToOklchLocal`, `deriveKeyColor` calls `hexToOklch`, an agreement assertion in `test/ui/model.mjs`, a before/after dump diff over the anchored corpus) is readable at `git show adf9e5a9:.sdlc/plans/small-fixes.md` |
| A wall-clock deadline on the whole smoke run (a CDP `send` whose reply never comes waits forever) | Not in the ticket. With the fix, a hung run killed by CI's job timeout or a local `kill` now cleans up, which removes the harm | none planned |
| The parent killed with `SIGKILL` | No handler runs on `SIGKILL`; the browser survives as it does today. `SIGKILL` is not what CI, `timeout` or a terminal send | Risks |
| Editing `.sdlc/plans/records-followup.md:43` (cites `smoke.mjs:293`) and `.sdlc/questions/records-followup-repoint.md` (cites `:44` and the port) | Both are #709 records another lane owns and is landing through #720; the question file is history by its own class. U1 moves the PASS line's number. P4 refuses both paths | the U1 handoff names the new line number; the #709 owner decides whether the live plan's row is amended before #720 lands or archived as written |
| `.github/workflows/ci.yml` | The `smoke` script is what CI calls; the change rides inside it | none |

## Risks

| Risk | What this plan does about it |
|---|---|
| CI's `google-chrome` does not write `DevToolsActivePort` under port `0` the way Canary does | It does (Chromium has written the file for every `--remote-debugging-port` since 2017), but the plan does not rely on the sentence: the first CI run of the draft PR is U1-2's and P5's CI leg, and the 45 s deadline message names the last error if it fails |
| A signal during the start-up wait, before `launchChrome` resolves | `onExit` is installed before the launch and `close()` reads `proc` and `dir` from closure state set at spawn time; leg (e) covers the deadline half of that path and the builder's handoff states where `proc` is assigned |
| Helper processes outlive the `SIGKILL` on the CI runner longer than 2 s | The criteria allow 5 s and count by the run's own profile-dir needle; if a runner shows survivors, the builder switches `close()` to `SIGTERM` first then `SIGKILL` after 1 s and says so in the handoff |
| The parent is `SIGKILL`ed | Not covered; stated in Not in scope. The per-run profile directory still means the next run never attaches to the survivor, so the harm shrinks to one idle process, not a wrong verdict |
| Another seat runs smoke on this host during a criterion run | Every count is scoped by `TMPDIR` to the run under test. The old-needle controls (U1-3, U1-5) are host-global by nature, so they run only when the 9333 count is `0` first, and the decoy row states the same guard |
| `origin/main` moves under the branch before landing | The six commits since 88905891 touch neither `test/smoke/` nor `package.json` (checked at a4675242); the Orchestrator rebases before the draft PR and every diff row reads `origin/main`, so a later move shows up as a scope-wall hit rather than a silent merge |

## Landing

One PR from `plan/small-fixes` to `main`, title `fix(tooling): smoke owns its Chrome and its port (#717)`. The PR body says `Closes #717`. Draft at the verified unit so CI runs the new smoke script. Pre-land per adapter §2.1: P1 to P5 and U1-1 to U1-6 at the branch head, record at `.sdlc/verdicts/small-fixes-prepr.md` with the head sha; smoke is a pre-land gate here because `test/smoke/` changed. Then the squash and sync steps of `shipping-changes`, and the plan-closing rule of adapter §5 (status `done`, the unit ticked, the file to `.sdlc/plans/archive/`, `adapter.py close 717`).

## Open questions for the owner

| # | Question | Default if unanswered |
|---|---|---|
| Q1 | Register `test/smoke/launcher.mjs` in `test/run.mjs` `TESTS` (it needs no Chrome), at the cost of a three-run baseline rerun under the post-#691 landing rule? | no: it runs from the `smoke` script, so CI and every `npm run smoke` run it, and `node test/smoke/launcher.mjs` runs it alone anywhere |

## Revisions

| date | what changed |
|---|---|
| 2026-09-22 | written, status proposed, two units (#717 now, #731 gated on #681). Every "today" value measured by the planner at 88905891 in a scratch worktree or read off 505416d7 with `git show` |
| 2026-09-22 | revision 2, on the checkability review of adf9e5a9 (`policy-plans-checkability.md` Plan C; 9 🟢, 7 🟡, 0 🔴) and the owner ruling that #731 leaves this plan. Yellows folded: every diff reads `origin/main`, not a merge base that equals `HEAD` at the plan tip (P3, P4, U1-3, U1-5 controls); P3's third figure is no longer a number the handoff states about itself, the verifier lists the matched lines and each must be a program quote; P4 admits only this plan's own records and the board, with a four-name fixture that prints `2`; P5, U1-3 and U1-4 scope their process and directory counts to the run's own `TMPDIR`, since the host-global needle would count another seat's smoke; U1-2 control (2) and leg (c) read the fake pid and the directory, not the child's exit code, which is 143 with or without the handler; U1-5 runs only when 9333 is free. G0, U2-1 to U2-4 and Q2 removed with #731 (their fixes, `no shared full-triple` as the needle and `6` as the count, go with it to its own plan). Today values that the review re-measured are noted beside the planner's. The head line records `origin/main` at a4675242 |
| 2026-09-22 | revision 3, activation. `origin/main` was brought in by a merge at activation (`2941e16a`), not by a rebase before the draft PR as the risk row says: the unit's sha will be cited by its handoff and verdict, and a rebase would orphan it. The risk row's intent holds, since every diff row still reads `origin/main`. U1 dispatched at builder-l2 in `.worktrees/sf-U1` |
| 2026-09-22 | revision 4, from the U1 review (`.sdlc/verdicts/small-fixes-U1-review.md`, FIX-FIRST at `b8c204e4`), before any verdict. A signal delivered while `launchChrome` is still waiting for `DevToolsActivePort` leaked the fake Chrome and its profile: `smoke.mjs` installed `onExit` early, but the close it called was a no-op until the launch resolved. The risk row already required `close()` to read `proc` and `dir` from state set at spawn time, so the implementation missed the design; no criterion exercised that window, which is why the builder's run could not see it. Leg (f) is added to exercise it, and U1-2's pass count moves from `5` to `6`. Negative control for (f): in a clone, make the pending launch's cleanup a no-op again, and leg (f) reds on the fake pid still alive and `dir` still present. Every other row is unchanged |
| 2026-09-22 | revision 5, U1 reopened at pass 2. The pre-land at `a1a4ffc6` is 🔴 (`.sdlc/verdicts/small-fixes-prepr.md`): CI `build-test` fails launcher leg (f) on Linux because `test/smoke/launcher.mjs:153` restores an unset `TMPDIR` as the string `"undefined"`, which no Mac run could see. The Verifier appended a correction to `small-fixes-U1.md`: pass 1 graded P5 🟢 without its CI leg, and that verdict is now 🔴. Pass 2 goes through a planner re-diagnosis first, then a builder at L7 per the grade rule; the unit branch is `unit/sf-U1p2` off `plan/small-fixes` |
| 2026-09-22 | revision 6, U1 pass 2, from the planner's re-diagnosis (`.sdlc/plans/small-fixes-U1-rediagnosis.md`). Root cause reproduced on a Mac with `env -u TMPDIR` and on Linux in Docker `node:24`: `launcher.mjs:153` restores an unset `TMPDIR` as the string `"undefined"`, leg (f)'s child inherits it and dies in `mkdtemp` with its stderr unread. The criteria gap: every row ran on a Mac with `TMPDIR` set, and P5 named a CI leg that had no run, since `ci.yml` triggers only on pull requests and `main`. Rows U1-7 (`TMPDIR` unset), U1-8 (Linux in Docker), U1-9 (a signal-leg red carries the child's stderr) and U1-10 (leg (e) cannot pass with `pgrep` absent) are added, copied from the re-diagnosis. P5 splits: P5a is the local run as before; P5b, `build-test` green on the graded sha with its run id, is graded at pre-land on `plan/small-fixes` (PR #735), since the loop merges a unit only after its verdict and no unit branch triggers CI. The unit verdict carries Linux evidence through U1-8 instead. The re-diagnosis's option O1, merging before the verdict, is not taken. Its repro ids `R1` to `R8` are renamed `RD1` to `RD8` in both files, since `R<n>` belongs to the readiness namespace and `board.py ids` refuses them outside it |
| 2026-09-22 | revision 7, from the U1 pass-2 review at `1e25556d` (FIX-FIRST, `.sdlc/verdicts/small-fixes-U1-p2-review.md`), before any pass-2 verdict. F1: `close()` runs `rmSync` right after `SIGKILL`, while Chrome's helper processes can still be writing, so the profile directory survives on `5` of `17` real-Chrome runs, at the pass-2 head and in pass 1 alike. U1-3, U1-4 and P5a each ran once and could not see a one-in-four leak. Row U1-11 runs the green path twenty times and requires zero leaks; the fix retries the removal, synchronously, while the directory exists, with a bounded wait. F2: a signal leg that times out kills its child with `SIGKILL` and leaves the child's fake Chrome alive; row U1-12. This is review rework inside builder pass 2, not a pass 3 |
| 2026-09-22 | revision 8, from U1 pass-2 review round 2 at `73d5f6f4` (PASS, finding F4), before any pass-2 verdict. U1-11's control read directories only, and the race it targets did not leave one in thirty runs at `1e25556d` in round 2, while Chrome processes still alive 5 s after the run did show, in `3` of `30`. A control that fires on one round and not the next cannot carry the row alone, so U1-11 now counts a run as leaking on either a directory or a process. F5 (a bare `rmSync` in the launcher's timeout handler, `launcher.mjs:126`) is carried to the verifier as a non-blocking note |
| 2026-09-22 | revision 9, from the critic on PR #735 at `262ae996` (CHANGES, issuecomment-5787113504), after pre-land pass 3 🟢. No leg bites on the process-group kill: a parent-only `close()` passes 6 of 6 because the fake spawns no child. U2 adds a grandchild to the fixture and a grandchild-dead assert to legs (b) to (f), graded at builder-l5, reviewer-l4, verifier-l3 on `unit/sf-U2` off `plan/small-fixes`. The pre-land record at `262ae996` is superseded by the U2 merge; pre-land reruns on the new head |
