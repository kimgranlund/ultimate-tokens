# Verdict rg-U6 · 🟢 (pass 3 at ffe2e1b2; pass 2 at 715a5f5c 🟢; pass 1 at 6eead0eb 🔴)

Unit `unit/rg-U6` @ `6eead0eb`, base `b3961aa9`, plan `plan/rule-gates` @ `43ccd53c` (revision 7). Read-only. Every run in a detached scratch worktree at `6eead0eb` (`wt-rgU6`) or a throwaway `git clone --shared` of it (`rgU6-verif-neg/neg`), HEAD confirmed `6eead0eb2cfe8fa3767c702ff12e929ef2089525` before each. `D` is the glyph from `printf '\xe2\x80\x94'`.

Next action for the Orchestrator: send U6 back for one small pass (rows 10 and 11). Every plan row U6-1 to U6-3 is green.

| # | Criterion | State | Evidence | Negative control |
|---|---|---|---|---|
| 1 | U6-1: no lone-glyph token in `src mcp figma/plugin/code.js`, 20 or more `"n/a"` | 🟢 | lone-token grep `0`; `"n/a"` count `20` split `color.js:7 geometry.js:4 typography.js:5 app.js:1 app-helpers.mjs:1 ds-export.js:1 brand-kit-core.mjs:1`; the same grep at base prints nothing for `"n/a"`, so all 20 are new | reverting `color.js` alone to base: `node test/ui/headless-boot.mjs` reds `✗ (na) color.js .an-empty reads "n/a" (got —)` and the token grep would count the 7 restored tokens |
| 2 | U6-1: the 20 vs 16 count (plan step 1 says 16 `an-empty`; builder says 11 `an-empty` + 9 others) | 🟢 | base enumeration `git grep -n -E "('\|\"\|\x60)$D('\|\"\|\x60)" b3961aa9 -- . ':!*.md'` lists exactly 20 source lines: 11 `an-empty` (color.js 35/74/96/130/607, geometry.js 592/625/659, typography.js 35/60/91), 2 `tok-na` (geometry.js 113, typography.js 475), `app.js:765`, `app-helpers.mjs:275`, `typography.js:122`, `color.js:1337`, `color.js:2149`, `ds-export.js:1433`, `brand-kit-core.mjs:63`; plus `Binary file b3961aa9:figma/plugin/ui.html matches` (a mirror). U6-1's Expected is `20` or more and its Today cell is `20`; the step text's "16" is a planner typo, the builder's split is right | if any of the 20 were missed, the HEAD run of the same grep would print a nonzero line; it prints nothing |
| 3 | U6-1: `npm test` green, tree clean after | 🟢 | worktree, no `node_modules`: `✓ all 48 test files passed`, `npm test  150.53s user 5.49s system 92% cpu 2:48.94 total`, exit `0`, `git status --porcelain \| wc -l` `0` | any of the five surface reverts below reds one test file, which fails the runner's pass line |
| 4 | U6-2: one anchored assertion per surface | 🟢 | `grep -c 'an-empty.*n/a' test/ui/headless-boot.mjs` `3`; `grep -c '\| n/a \|' test/engine/exports.mjs` `1`; `grep -c 'Systems in this kit: n/a' test/mcp/core.mjs` `1` | deleting any one assertion drops its figure to `0` |
| 5 | U6-2: revert one surface, exactly that file's test reds (all three tests run per revert) | 🟢 | `color.js`: only headless `rc=1` `✗ (na) color.js .an-empty reads "n/a" (got —)`; `typography.js`: only headless `✗ (na) typography.js .an-empty reads "n/a" (got —)`; `geometry.js`: only headless `✗ (na) geometry.js .an-empty reads "n/a" (got —)`; `ds-export.js`: only exports `FAIL  design-system-make  — color.md's text-muted-foreground row lost its n/a placeholder`; lines 1461 to 1462 alone: `spacing.md's space/radius rows lost their n/a placeholder (want 2, one per row)`; `brand-kit-core.mjs`: only core `(U6) an empty kit's guide reads "Systems in this kit: n/a." (got: Systems in this kit: —.)`. Clone clean after `0` | these are the controls; a test that stayed `rc=0` under its own surface's revert would be the red |
| 6 | Informational: tokens with no pinning test | 🟡 | reverting `src/ui/app.js` (volume label) or `src/ui/app-helpers.mjs` (`GENERIC_FONTS` sentinel) leaves all three tests `rc=0`; `ds-export.js:1433` set back to the glyph alone prints `PASS: export-formats clears all [gate] predicates` (the dead branch the handoff discloses). Also the `color.js:1337` drift mark, `color.js:2149` suffix, `typography.js:122` family fallback and the two `tok-na` cells are pinned by no test. Plan asks one test per surface (UI, Make, MCP), which holds; U4's em-dash gate will catch a reintroduction tree-wide | a reintroduced glyph at any of these lines reds `test/repo/em-dash.mjs` once U3/U4 land; today nothing reds, which is this row's point |
| 7 | U6-3: doc line and mirrors | 🟢 | `grep -c 'degrades to "n/a"' ...foundations.md` `1`; escaped `\\"—\\"` count outside `.md` at HEAD `0`, at base `figma/plugin/ui.html:3 describe-mcp-assets.js:2 mcp-assets.js:1` (6) | the base column is the control: skipping regeneration leaves `6` |
| 8 | Generated mirrors regenerate byte-identical | 🟢 | in the clone, the three mirrors checked out at base, then the `npm test` generator chain (`gen:figma-assets`, `gen:mcp-assets`, `gen:categories`, `gen:adia-exports`, `bundle`, `gen:figma-ui`) run: `git diff HEAD --name-only \| wc -l` `0`; hashes equal HEAD's blobs: `ui.html 44de5c14`, `mcp-assets.js 6f299891`, `describe-mcp-assets.js 37e8f1e9` | before regeneration the three base copies showed `3 dirty` against HEAD; a mirror hand-edited or stale would stay dirty after the chain |
| 9 | Scope wall | 🟢 | `git diff --name-only b3961aa9 HEAD` lists 15 files, all in the plan's U6 Touches row (revision 6 adds the three mirrors) plus `.sdlc/handoffs/rule-gates-U6.md` | a file outside the Touches row in that list would red this row |
| 10 | No stale record quotes the old placeholder (CLAUDE.md: a change that invalidates a record repairs it in the same change) | 🔴 | two Markdown lines still name the glyph as the empty value, both in backtick spans so U3's gate exempts them and will never catch them: `.claude/skills/maintaining-brand-kit-mcp/references/foundations.md:87` `` `Systems in this kit:` lists the present ones (or `—`). `` (the MCP summary U6 changed, same file U6 edited nine lines below) and `docs/lld/app-shell.md:270` `` cards render `—` empties (`an-empty`) `` (the inspectors U6 changed). The plan's R0 (c) only looked for the double-quoted form, so the planner missed both | after the fix, `git grep -n "$D" -- .claude/skills/maintaining-brand-kit-mcp/references/foundations.md docs/lld/app-shell.md \| grep -E 'or .—.\|render .—. empties'` prints nothing; today it prints 2 lines. `app-shell.md` is outside U6's Touches row, so the Orchestrator widens it |
| 11 | Plan section 4: code that names the glyph writes the `u2014` escape, never the character | 🟡 | the only glyph added in non-generated unit code: `test/engine/exports.mjs` `if (/\|\s*—\s*\|/.test(colorMd) \|\| /\|\s*—\s*\|/.test(spacingMd) \|\| /\|\s*—\s*\|/.test(makeTypo)) FAIL(...)` (3 glyphs on one line). The review ruled it exempt; the plan's own rule says otherwise, and U3's gate masks nothing in `.mjs`, so U4 inherits it as an unplanned hit outside its 15-line R0 list. Rewrite as `/\|\s*—\s*\|/` | the extraction `git diff --text b3961aa9 HEAD -- . ':!figma/plugin/ui.html' ':!src/ui/*-assets.js' ':!.sdlc' \| grep '^+' \| grep -F "$D"` prints 1 line today; after the escape it prints 0 while the regex still reds a `\| — \|` cell |
| 12 | No lone token left anywhere in the tree outside Markdown | 🟢 | the same lone-token grep over `.` at HEAD (not only `src mcp`) prints nothing; `git grep -i 'u2014\|&mdash;\|&#8212;' -- src mcp figma` (escape-written placeholders) prints nothing | any placeholder spelled as an escape or entity would print here |
| 13 | `npm run smoke` (src/ui changed) | 🟢 | clone after `npm ci`, clean tree at `6eead0eb`: `SMOKE PASS — gallery · category · editor · export dialog all render in a real browser`, `smoke rc=0`, 43 `✓`, 0 `✗`; tree clean after | a smoke assertion failure prints `✗` and a nonzero rc |
| 14 | No Chrome left on port 9333 | 🟢 | `lsof -nP -iTCP:9333 -sTCP:LISTEN \| wc -l` `0` before and after smoke; `pgrep -fl 'Chrome.*9333' \| wc -l` `0` | a leaked smoke Chrome would count `1` |
| 15 | Host load | 🟡 | start `load averages: 38.73 28.20 24.58`, end `load averages: 9.50 38.19 36.25`. Every gate was green, so load cost time, not trust; the `npm test` time is not a baseline figure | a red under this load would need an isolation rerun per `flaky-gates` before it counted |

Cleanup: the scratch worktree `wt-rgU6` and the clone directory `rgU6-verif-neg` removed by exact name after this record.

## Pass 2 · 🟢 at `715a5f5c` (delta from `6eead0eb`, code commit `14efd2c5`, plan revision 8 at `98f8ef63`)

Checks ran in a new detached worktree `wt-rgU6p2` at `715a5f5c` and a shared clone `rgU6-verif-neg2/neg`. HEAD was `715a5f5c07ef0d4a8c8e5b45cce96aeed789df9f` for every run. Both were removed by exact name afterwards. Load was `26.61` at start and `34.13` at end. All gates were green, so the load does not affect the verdict.

| # | Criterion | State | Evidence | Negative control |
|---|---|---|---|---|
| P2-1 | Nothing outside the stated delta changed | 🟢 | `git diff --name-only 6eead0eb 715a5f5c` lists 5 files: `foundations.md`, `.sdlc/handoffs/rule-gates-U6.md`, `docs/lld/app-shell.md`, `test/engine/exports.mjs`, `test/ui/headless-boot.mjs`. `src mcp figma` changed files: `0`. `app-shell.md` is in the revision 8 Touches row | Any `src/` or mirror file in that list would need a new regeneration and smoke check, and this row would go red |
| P2-2 | Row 10 (was 🔴), now U6-4: no doc quotes the old glyph as the empty value | 🟢 | the plan's U6-4 grep with the literal glyph: `0`. New text: `lists the present ones (or `n/a`)` and `cards render `n/a` empties (`an-empty`)` | In the clone, restoring either line from `6eead0eb` makes the grep print `1` for `foundations.md` and `1` for `app-shell.md` |
| P2-3 | Row 6 (was 🟡): `app.js:765` is pinned | 🟢 | headless-boot asserts `Vol n/a` through `buildPresetTiles` | The glyph put back at line 765: only headless-boot reds, with `✗ (na) app.js buildPresetTiles falls back to "Vol n/a" (got Vol —)`. exports and core stay `rc=0` |
| P2-4 | Row 6: `app-helpers.mjs:275` is pinned | 🟢 | headless-boot asserts that `GENERIC_FONTS` has `n/a` and not U+2014 | The glyph put back at line 275: only headless-boot reds, with `✗ (na) app-helpers.mjs GENERIC_FONTS carries "n/a", not the glyph` |
| P2-5 | Row 6: `ds-export.js:1433` is pinned | 🟢 | exports.mjs pins the source text `DS_MAKE_TYPE_USE[key] \|\| "n/a"` (the branch is unreachable, so the pin checks source text, not behaviour). A reformat of that expression would also red it, which errs on the safe side | The glyph put back at line 1433: only exports reds, with `FAIL  design-system-make  — ds-export.js:1433's DS_MAKE_TYPE_USE fallback reverted to the glyph` |
| P2-6 | Row 6 remainder (informational) | 🟡 | Still pinned by no test: `color.js:1337` (drift mark), `color.js:2149` (suffix), `typography.js:122` (family fallback) and the two `tok-na` cells. The plan asks for one test per surface, and these lines are covered once U3/U4's `em-dash.mjs` counts every lone token | Any of those lines set back to the glyph passes all three tests today. After U4 lands, `node test/repo/em-dash.mjs` names the line |
| P2-7 | Row 11 (was 🟡): no literal glyph in unit code | 🟢 | The regex is now built from `String.fromCharCode(0x2014)`. The glyph count on lines added in `b3961aa9..715a5f5c` (excluding generated files and `.sdlc`): `0` | A bare `\| — \|` cell injected at `ds-export.js:1374` (the n/a cells kept, so no earlier check fires first) reds with `FAIL  design-system-make  — a Make export table reintroduced the glyph as a bare table cell in place of n/a`. Note that `FAIL` keeps only the first message per gate, which is why the injection has to leave the n/a cells alone |
| P2-8 | `npm test` passes and leaves the tree clean | 🟢 | worktree, no `node_modules`: `✓ all 48 test files passed`, `npm test  143.69s user 5.20s system 97% cpu 2:32.72 total`, exit `0`, then `git status --porcelain \| wc -l` gives `0` | Each revert in P2-3 to P2-5 reds one test file, which would fail the runner's pass line |
| P2-9 | `npm run smoke` | 🟢 | After `npm ci` in the clone: `SMOKE PASS — gallery · category · editor · export dialog all render in a real browser`, rc `0`, `43` checks passed, `0` failed, tree clean after | A failed smoke assertion prints `✗` and exits nonzero |
| P2-10 | Nothing left listening on port 9333 | 🟢 | `lsof -nP -iTCP:9333 -sTCP:LISTEN \| wc -l` gives `0` before and after smoke. `pgrep -fl 'Chrome.*9333' \| wc -l` gives `0` | A smoke Chrome left running would count `1` |

## Pass 3 · 🟢 at `ffe2e1b2` (delta from `715a5f5c`)

| # | Criterion | State | Evidence | Negative control |
|---|---|---|---|---|
| P3-1 | The delta changes only the handoff, and P8's scan finds no em dash outside a span in its added lines | 🟢 | `git diff --stat 715a5f5c ffe2e1b2` prints `1 file changed, 3 insertions(+), 3 deletions(-)` for `.sdlc/handoffs/rule-gates-U6.md` only. P8's scan (the perl span strip, then U+2014) prints `0` on the delta and `0` on the whole handoff from `b3961aa9`. `branding: clean (568 files scanned)`. The scan over the whole unit from `b3961aa9` prints `6`, and every one is a line in a regenerated mirror (`core: "// brand-kit-core.mjs — the PURE...`, `"data": "// ds-export.js — the DS-bundle...`). Those files re-serialize whole source files, so the dashes come from source comments that existed before this unit, not from a record. Pass 1 row 8 shows the mirrors regenerate byte-identical, so the pass 2 gates still hold because no code changed | A line containing the glyph planted in the handoff in a scratch worktree makes the scan print `1` |
verdict: 🟢
