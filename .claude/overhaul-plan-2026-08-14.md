# Overhaul plan — ultimate-tokens estate (.claude/) — 2026-08-14

Generated by `authorkit:overhaul-planning` (plan-only; nothing here is executed). Authored
directly per PLAN-TEMPLATE rather than through `docs:make-doc`'s LLD pipeline — a 4-move
rename plan; the template's sections are the contract. Human-confirmed verdicts (session
2026-08-14): the 2 high-radius `adding-*`/`building-*` kills were questioned and upheld.

## Phase 0 — Measurements

| Instrument | Available? | Finding summary |
|---|---|---|
| naming-audit | yes (2026-08-14, twice) | errors=0, warnings=0, exemptions=10 (3 agents missing `-agent`, 7 gerund skills) |
| bloat-audit | yes (2026-08-14, agent sweep) | 14/17 flagged mechanically, ~2.8k chars judged recoverable — all applied and shipped in #436; no member overhaul-worthy on bloat grounds |
| check-routing (harness) | no | unavailable — repo-local estate, no trigger-eval suites; routing evidence is anecdotal (no observed misfires) |
| plan-plugin-split surface_map (harness) | no | unavailable — no plugin surface; blast-radius rows below are repo-wide reference greps, not verified closures |

## Phase 1 — Per-member kill-switch

| Member | Where it lives | Species | Blast radius (files referencing) | Merge/Split candidate? | Knowledge tier | Verdict |
|---|---|---|---|---|---|---|
| marketing-manager | correct — owns docs/marketing corpus | agent | 5 | NO — sole owner of its corpus | PROCEDURE — keep-inline | **MOVE** → `marketing-agent` |
| palette-researcher | correct — curated-palette research seat | agent | 4 | NO — no overlapping seat | PROCEDURE — keep-inline | **MOVE** → `palette-research-agent` |
| ultimate-tokens-reviewer | correct — repo-invariant review seat | agent | 2 | NO | PROCEDURE — keep-inline | **MOVE** → `change-review-agent` (also sheds the brand token) |
| migrating-figma-files | correct | skill | 1 | NO | PROCEDURE — keep-inline | **MOVE** → `figma-file-migration` (`migration` ∈ ProcessLex; equally accurate name, radius 1) |
| adding-semantic-roles | correct — owns the 53-role lockstep parity | skill | 12 | NO — the general/narrow test passes; peers cite, don't duplicate (post-#436) | PROCEDURE — keep-inline | **NO MOVE** — nearest ProcessLex terminal (`authoring`) misdescribes a parity procedure; 12-file radius buys no routing improvement (Phase 0: no misfire evidence) |
| adding-export-formats | correct — owns the 8-format emitter surface | skill | 16 | NO — sole ShadCN-exception owner post-#436 | PROCEDURE — keep-inline | **NO MOVE** — same class, largest radius in the estate (16) |
| building-editor-sections | correct — owns the section pattern + shim limits | skill | 7 | NO | PROCEDURE — keep-inline | **NO MOVE** — same class |
| maintaining-brand-kit-mcp | correct — owns mcp/ surface | skill | 3 | NO | PROCEDURE — keep-inline | **NO MOVE** — no fitting ProcessLex terminal; editing the closed lexicon for one name inverts the tail and the dog |
| maintaining-figma-plugins | correct — owns figma/ plugins | skill | 7 | NO | PROCEDURE — keep-inline | **NO MOVE** — same lexicon gap, radius 7 |
| shipping-changes | correct — owns the release workflow | skill | 14 | NO | PROCEDURE — keep-inline | **NO MOVE** — no fitting terminal (`release`/`ship` in no lexicon), radius 14, the estate's most-invoked working name |

Every NO MOVE cites Phase 0: zero routing/bloat defects attributable to the name, so the
rename's only payoff is lexical conformance, which the D8 ratchet already bounds.

## Phase 2 — Waved ticket seeds (not yet minted)

### Wave 0 — merge/split nominations
None — no merge/split candidate survived Phase 1.

### Wave 1 — mechanically-clean moves
- [ ] W1-A: `marketing-manager` → `marketing-agent` (Blocked-by: none)
- [ ] W1-B: `palette-researcher` → `palette-research-agent` (Blocked-by: none)
- [ ] W1-C: `ultimate-tokens-reviewer` → `change-review-agent` (Blocked-by: none)
- [x] W1-D: `migrating-figma-files` → `figma-file-migration` (Blocked-by: none) — executed 2026-08-14 (#442; vocab `figma`+`file` registered, exemption retired)

All four are serial through `naming.manifest.json` (each retires its own exemption entry) —
mint as one batch ticket or execute W1-A→D in sequence, never two in parallel against the
manifest. Each move routes through `authorkit:rename-planning` (blast-radius enumeration)
then `authorkit:rename-execute`.

### Wave 2 — species changes
None.

### Wave 3 — contested
None.

### Grandfathered (ADR-0011 D8 ratchet — exemption retained, shrink-only)
- adding-semantic-roles — no accurate conforming target; radius 12
- adding-export-formats — no accurate conforming target; radius 16
- building-editor-sections — no accurate conforming target; radius 7
- maintaining-brand-kit-mcp — lexicon gap (`maintenance` ∉ ProcessLex); radius 3
- maintaining-figma-plugins — lexicon gap; radius 7
- shipping-changes — lexicon gap (`release`/`ship`); radius 14

## Phase 3 — Execution contract (per ticket, once approved)

claim → worktree → `git mv` (history preserved) → supersession note or `renames.json` entry
→ gates + critics → PR → human merge → verified close. Serial through shared ledgers
(`naming.manifest.json`, `renames.json`) — two tickets never touch the same ledger entry in
parallel.

## Phase 4 — Closeout (per wave, once tickets land)

- [ ] `/check-routing` clean for every touched plugin (n/a here — repo-local estate; substitute: a session smoke that each renamed member still triggers/dispatches)
- [ ] `fix-old-names` sweep run against consumer repos (here: this repo + the user memory index at `~/.claude/projects/-Users-kimba-Projects-nonoun-ultimate-tokens/memory/`)
- [ ] Records amended with dated supersession notes (never rewritten)
- [ ] naming-audit re-run: exemptions 10 → 6, errors still 0

## The five respect invariants

1. Evidence can veto the plan.
2. History preserved, never replaced.
3. Consumers degrade gracefully.
4. The old design's intent is read before it is judged.
5. Nothing semantic rides hidden in a move.

## Next step

This plan and its ticket seeds are generated only — nothing above has been executed. A human
reviews the seed list and approves each row; approved rows are minted through their owning
intake skill (`file-task`, per ADR-017 → GitHub Issues), never auto-created here.
