# Verdict gate-split U2 · 🟢 at cf024615

Fresh verifier pass, no prior verdict inherited. Worked only in `/tmp/gs-u2-verify-p2` (worktree of cf024615, unit/gs-U2). Control clones under `/tmp/gs-u2-verify-ctl-p2` (left in place), each `git clone --shared` from the repo then checked out to cf024615. No source file in the repo itself was edited.

Clone provenance, proved once and applied to every control clone below: each clone's `git log -1 --format=%h` reads `cf024615`, and `grep -c "GRID_HUES = FULL ? HUES_G" test/engine/tonal.mjs` reads `1` (the unit's own thinning line, absent before this unit). Both checks were run in every clone before its mutation, confirming it carries the unit's committed change, not a pre-fix tree.

Host timing note: the 1-minute load at session start was 2.97, under the plan's load-under-5 threshold, so the timing row was taken first. `pgrep -f 'node .*test/(run|engine|ui)'` read 0 at that moment. Later in the session other seats' runs pushed the concurrent-process count above 2 twice; both times the verifier blocked on a single polling command until the count dropped back under 2 before starting its own run, per the host rule. One early control run (P3 control a) started without that poll and briefly overlapped 3 concurrent processes; the affected row is a correctness check (exit code and grep counts), not a timing row, so the result stands, but it is disclosed here rather than silently.

## U2 rows

| # | Criterion | State | Evidence | Negative control |
|---|---|---|---|---|
| U2-1 | FULL is the old file, same gates and verdicts, full corpus | 🟢 | own run: `npm run -s gate:corpus-tonal` exit 0, `grep -c '^  pass  '` 21, `grep -c '^  FAIL'` 0, `grep -c '(FULL: 343 curated documents, 3780 palettes)'` 1. Matches the handoff's amended needle (19 to 21, re-observed at ebddc55d) | P5 rows 1 and 3 below, run fresh by this verifier, both red the FULL leg |
| U2-2 | SAMPLED is green, says so, no sweep dropped | 🟢 | own run: `node test/engine/tonal.mjs` exit 0, `(SAMPLED seed 0: 35 curated documents, 392 palettes)` count 1, pass-line count 21 | own clone, `sampleCorpus` patched to `return []`: exit 1, `FAIL  chroma-envelope , (vacuity) SAMPLED measured only 0 curated documents, expected at least 30` |
| U2-3 | sample catches the broad M-A regression; the sparse M-C one is recorded, not graded | 🟢 | own clone, M-A applied (`git diff --stat` `1 file changed, 1 insertion(+), 1 deletion(-)`), SAMPLED run: exit 1, `(iv dip gate) peak:` count 1. Own clone, M-C applied, SAMPLED run: exit 1, `(C6 i)` count 1 (same outcome the handoff recorded; not required to be caught, and was) | U2-2's own unmutated run |
| U2-4 (a) | subset semantics: deleting an observed name reds SAMPLED | 🟢 | own clone: deleted `Trulli of Alberobello · vernacular · Puglia, Italy\|primary\|500` from `EVEN_DIP_BASELINE` (`git diff --stat` `1 file changed, 1 deletion(-)`), SAMPLED run exit 1, `FAIL  chroma-envelope , (iv dip gate) even: 1 dip instance(s) beyond the cited baseline, e.g. Trulli of Alberobello · vernacular · Puglia, Italy\|primary\|500` | the deletion itself proves the pick was observed (a name that was not observed would not red) |
| U2-4 (b) | subset semantics: a fictitious cited name is silent under SAMPLED, loud under FULL | 🟢 | own clone: added `zz-not-in-corpus\|primary\|500` to `EVEN_DIP_BASELINE` (`1 file changed, 1 insertion(+)`). SAMPLED exit 0. FULL (`gate:corpus-tonal`) exit 1, `1 of the 91 cited baseline dips were not observed this run (zz-not-in-corpus\|primary\|500)` | the two halves are each other's control, as the plan states |
| U2-5 | the file's share of `npm test`, quiet-host reading | 🟢 | load before 2.81, `pgrep -f 'node .*test/(run|engine|ui)'` 0. `node test/engine/tonal.mjs` real 12.77s (well under the 20s ceiling). Load after 2.90. This supersedes the handoff's own honestly-recorded loud reading (46.72s, correctly graded yellow there under a load of 393/225/152) | timing row, no control applies |
| U2-6 | per-category presence assertion (U1 verdict follow-up item) | 🟢 | own run: `node test/engine/corpus-sample.mjs` exit 0, all 8 categories present, duplicate-name leg throws as expected | own clone: `pickVolume` forced to return `undefined` for `"architecture"` (`1 insertion(+)`): exit 1, `category architecture contributed no document to the sample` |

## P-rows the plan applies (P1 to P9; G0 waived, base ebddc55d)

| # | Criterion | State | Evidence | Negative control |
|---|---|---|---|---|
| P1 | `npm test` green, N agrees, tree clean | 🟢 | own run (backgrounded past the 120s foreground cap, completed exit 0): `✓ all 50 test files passed`; `TESTS.length` via the perl one-liner reads `50`; `git status --short \| wc -l` reads `0` | not run again here; P1's own clone control is a role-table edit, out of this unit's scope to re-demonstrate |
| P2 | ceiling holds in the figures of record | 🟡 recorded, not gradable yet | own run of the plan's grep: `ok 59.83`. That figure is `.sdlc/baseline.md`'s pre-#681 row (from an unrelated, already-landed plan), not a post-split measurement; U6b (still open) is the unit that re-measures and writes the real figure. This matches the plan's own "Today" note verbatim ("proves only that the command parses") | not exercised: the row is not yet meaningful at this stage, same as the plan states it will read until U6b lands |
| P3 (a)(b)(c) | `gate:corpus-tonal` full-sweep coverage, scoped to this unit | 🟢 | own runs, all fresh: main exit 0 with `(FULL: 343 curated documents, 3780 palettes)` count 1; (a) `--full` dropped from the script: exit 0, count 0; (b) `FULL` forced `false` in-file: exit 0, `(FULL:` count 0; (c) FULL branch forced onto `sampleCorpus(...)`: exit 1, `FAIL` count 2, `35` count 2 | each sub-row is itself the negative control against the main run |
| P4 | CI invokes every gate script, no bypasses | 🟢 | own run against this branch's `.github/workflows/ci.yml` (U6a already merged here): each of the four `GATES` names 1 in the `sweeps:` block, `gate:corpus-contrast` 2, one `sweeps:` job, 0 `if:`/`continue-on-error:`/`needs:`, 1 `fail-fast: false`, 1 `pull_request:` | not re-run; this unit does not touch `ci.yml` |
| P5 row 1 | M-A reds the FULL leg | 🟢 | own clone, M-A applied, `npm run -s gate:corpus-tonal`: exit 1, `(iv dip gate) peak:` count 1 | the unmutated main run (P3) needles 0 for the same greps |
| P5 row 3 | M-C reds the FULL leg | 🟢 | own clone, M-C applied, `npm run -s gate:corpus-tonal`: exit 1, `(C6 i)` count 1 | the unmutated main run needles 0 for the same grep |
| P6 | the sampler is a pure function of the document set and seed | 🟢 | own run: `node test/engine/corpus-sample.mjs` exit 0, `PASS: corpus sample is a pure function of the document set and the seed` | own clone: `.sort()` removed from `vols` in the picker: exit 1, `the pick moved with load order in categories architecture, cuisine, film, literature, music, nature, travel` (7 of 7, matching the plan's measured figure) |
| P7 | baseline agrees with `TESTS.length` at this head | 🟡 expected-stale | own run: `sh .sdlc/checks/baseline-agrees-check.sh` exits 1, `STALE tests: baseline 48, test/run.mjs TESTS 50`, `stale total: 3`. This matches the plan's own stated expectation for a unit branch ("STALE the moment U1 registers its test file, and stays so until U6 reruns the baseline; expected on unit branches and a blocker at pre-land") | not exercised; the row is defined to be red here by design |
| P8 | branding clean, no added em dash outside a backtick span | 🟢 | own run: `node test/repo/branding.mjs` `branding: clean (515 files scanned)`; em-dash grep on the diff against merge-base ebddc55d: `0` | own clone: copied `docs/reference/references/decision-records.md` into `.sdlc/verdicts/`: `FAIL: 3 branding violation(s)`. Fixture diff line with a real em dash: grep count `1` |
| P9 | scope wall, diff against merge-base ebddc55d | 🟢 | own run: merge-base resolves to `ebddc55d`; filtered diff outside the allow-list `0`; diff touching `src` `0` | fixture of four names run through the same filter: `0` with `Q2=yes`, `1` with `Q2=no`, matching the plan's stated fixture behavior |

## Carried finding, not graded against this unit

C6 (🟢 nit, from `.sdlc/handoffs/gate-split-review-findings.md`, already logged in the plan's revision 10): the U2 handoff quotes a FAIL line with a preset name shortened to `Trulli of Alberobello ...` instead of the full `Trulli of Alberobello · vernacular · Puglia, Italy`, without an `altered:` marker. Confirmed present in `.sdlc/handoffs/gate-split-U2.md:220`. Per the team lead's brief this is noted, not counted against this verdict; the plan already routes its fix to the pre-land pass.

## Summary

Overall 🟢. No red rows. Every U2-n row and every applicable P-row (P1, P3, P4, P5 rows 1 and 3, P6, P8, P9) reproduced green with this verifier's own fresh evidence and a negative control that actually fails. P2 and P7 are correctly not-yet-green by the plan's own design at this stage of the plan (P2 needs U6b's re-measured baseline; P7 is defined to read STALE until U6b runs), not defects introduced by U2. Loads recorded: 2.97 at session start (1-min), 2.81 immediately before U2-5's timing run, 2.90 immediately after; both readings under the load-under-5 threshold, so U2-5 is graded on this verifier's own quiet reading (12.77s) rather than the host's earlier loud one.
