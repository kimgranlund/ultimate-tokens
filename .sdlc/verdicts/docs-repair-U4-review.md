PASS

Reviewed unit/dr-U4 @ 6bba8bf7 (source commit a6ed8e0a plus the handoff commit) against
`.sdlc/plans/docs-repair.md`'s U4 section, the plan-level rows, the scope wall, and "The design,
stated once" (docs-repair.md:83).

## Criteria checked

- U4-1 (file exists, names both plugins, both manifests, the generator, the skill): reran the
  handoff's exact command in a scratch clone at 6bba8bf7: `ok` then `1 2 2 3 1 1`, matching. Confirmed
  by hand that `figma/README.md` names `figma/plugin/manifest.json` and
  `figma/binder/figma-semantic-binder/manifest.json` (figma/README.md:4-16), `gen:figma-ui`
  (figma/README.md:7), and `maintaining-figma-plugins` (figma/README.md:3).
- U4-2 (every named module exists, every `binder/*.mjs` named): reran in the clone, no output
  (pass). Verified by hand: all six `binder/*.mjs` headers (bind-plan.mjs, live-diff.mjs,
  migrations.mjs, mode-apply-plan.mjs, splice-utils.mjs, style-plan.mjs) are named in
  figma/README.md:18-29, and each one-line description matches that file's own first-line header
  comment (checked against the actual file heads).
- U4-3 (unit changes one file): `git diff --name-only 6cf18c8c..a6ed8e0a` produced `figma/README.md`
  only. The handoff commit (`6bba8bf7`) then adds only `.sdlc/handoffs/docs-repair-U4.md`, inside the
  wall's allowance for `.sdlc/(plans|handoffs|verdicts|reviews|questions)/docs-repair` (correctly
  called out in the handoff's own scope note).
- U4-4 / P3 (branding, dash rules): reran `node test/repo/branding.mjs` in the clone, got
  `branding: clean (723 files scanned)` (the handoff's worktree run measured 722; the count differs
  only because a clone vs. worktree tree differs by one file elsewhere in the repo, not this unit's
  diff). `perl -CSD -ne 's/\x60[^\x60]*\x60//g; print if /\x{2014}/' figma/README.md` printed
  nothing, so no em dash outside backtick spans. Grepped `figma/README.md` for the retired brand and
  its old title case-insensitively: no hits.

## Claims fact-checked against the cited source

- "raw colors under a **Color Primitives** collection, the semantic Light/Dark tokens under
  **Color Roles**, aliased to the primitives" (figma/README.md:11-12): confirmed against
  figma/plugin/code.js:9-26 (`RAW_COLLECTION = "Color Primitives"`, `SEMANTIC_COLLECTION = "Color
  Roles"`).
- "embeds the parametric config in the file's root `pluginData` for a lossless round-trip"
  (figma/README.md:12-13): confirmed against figma/plugin/code.js:84 (`writeConfig`/`readConfig`,
  "the file-embedded parametric config").
- "`code.js` itself is generated by `npm run gen:figma-assets`
  (`scripts/gen-figma-binder-code.mjs`), which splices executable bodies verbatim... (the flagship
  `figma/plugin/code.js` float-executors, `src/engine/semantic.js`'s `semanticRoles()` for the role
  table)" (figma/README.md:19-23): confirmed against scripts/gen-figma-binder-code.mjs:1-19 and
  package.json:18 (`gen:figma-assets` runs `gen-figma-binder-code.mjs` then `gen-figma-assets.mjs`).
- "`npm test` and `npm run build` both regenerate `ui.html`" (figma/README.md:9-10): confirmed
  against package.json:27,29 (both scripts run `gen:figma-ui`).
- Test-file claim, "Each planner has a matching verifier under `test/figma/`... plus `plugin.mjs`
  for the app-as-plugin path" (figma/README.md:31-32): `ls test/figma/` lists exactly those six
  files; `test/figma/plugin.mjs`'s own header confirms "the figma-plugin-app validation adapter".
- "It cites no line numbers" (design intent, docs-repair.md:83): `grep -nE ':[0-9]+'
  figma/README.md` found nothing.

No stale or invented claim found. Nothing outside `figma/README.md` and the handoff needed fixing
in this section.

## Minor, non-blocking observation

The design paragraph (docs-repair.md:83) says the README should cover "how to import either
manifest in Figma"; the file gives the *Plugins > Development > Import plugin from manifest...*
steps once, under `figma/plugin/`, and states the binder manifest's path without repeating the
same three-step instruction there. U4-1..4 don't grade this (no criterion checks for a second
import walkthrough), so it doesn't affect the verdict, but a future edit could make the binder
bullet say "same Import plugin from manifest flow" explicitly if that ambiguity ever needs
tightening.

## Scope and branding

No file outside the allowed U4 diff (`figma/README.md`) or the handoff's own allowed path was
touched. `test/repo/branding.mjs` is clean. No em dash outside a backtick span in the added prose.

verdict: PASS
