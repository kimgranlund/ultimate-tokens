# Verdict baseline-regex U1 · 🟢 at f57f99d3

| # | Criterion | State | Evidence | Negative control |
|---|---|---|---|---|
| U1-1 | matchAll present, old non-global match gone | 🟢 | `1`, `0` at f57f99d3 (`grep -cF 'matchAll(/(\d+) to (\d+) s/g)' .sdlc/checks/baseline-agrees-check.sh` then the `.match(...)` grep) | at merge base `bcb7ce02`: `0`, `1` (before the fix, own run, confirms the flip) |
| U1-2 | #718 reproduction: clean tree unaffected, `test` gate planted with a second disagreeing range goes STALE | 🟢 | clone provenance `git -C /tmp/br-u1-verify-ctl/u1-neg-test log -1 --format=%h` = `f57f99d3`, and `grep -cF 'matchAll(...)/g)' .sdlc/checks/baseline-agrees-check.sh` = `1` in that clone. Plant fidelity: `md5 -q .sdlc/adapter.md` went `19ad535100a19a0cc4fb3537edb15da1` -> `c7f429fc74905091a7de440e9c3498a6`, `grep -c 'INTERIM ceiling: 280 to 550 s wall on a host at load'` = `1`. Clean tree: `clean tree exit: 0`. Planted, fixed script: `` `STALE time test: baseline 56 to 60 s, adapter 56 to 60 s, 280 to 550 s` ``, `planted tree exit: 1` | same plant, pre-fix script (`git show bcb7ce02:.sdlc/checks/baseline-agrees-check.sh`) run against the identical planted clone: `` `ok    time test: baseline 56 to 60 s, adapter 56 to 60 s` ``, `pre-fix planted tree exit: 0`. Control fires: the unfixed script misses the second range on the exact same bytes the fixed script catches |
| U1-3 | not a test-row special case: same shape planted on `build` also goes STALE | 🟢 | clone `/tmp/br-u1-verify-ctl/u1-neg-build` provenance `f57f99d3`, matchAll grep `1`. Plant fidelity: `md5 -q .sdlc/adapter.md` `19ad535100a19a0cc4fb3537edb15da1` -> `76f85c117b5b5fa2875c78bb24e61dbc`, `grep -c '9 to 9 s cold'` = `1`. Fixed: `` `STALE time build: baseline 1 to 3 s, adapter 1 to 3 s, 9 to 9 s` ``, `exit: 1` | same clone, pre-fix script: `` `ok    time build: baseline 1 to 3 s, adapter 1 to 3 s` ``, `pre-fix exit: 0` |
| edge: no range at all stays STALE | own verification, not in the plan's own U1 blocks | 🟢 | clone `/tmp/br-u1-verify-ctl/u1-edge-norange` provenance `f57f99d3`, matchAll grep `1`. Plant: `test` row's `56 to 60 s` text replaced with `no timing recorded`, hash changed `19ad535100a19a0cc4fb3537edb15da1` -> `f53bc5198a40be4d546de1b4729cb4d5`. Fixed script: `` `STALE time test: baseline 56 to 60 s, adapter none` ``, `exit: 1`. So the `ms.length > 0` guard is doing real work, not a vacuous pass | ablation control: a copy of the shipped script with the `ms.length > 0 \|\| ` guard clause stripped (`perl -pi -e 's/ms\.length > 0 && //'`), run against the identical no-range plant: `` `ok    time test: baseline 56 to 60 s, adapter none` ``, `no-guard exit: 0`. This proves the guard is load-bearing: without it, `[].every(...)` vacuously returns true and a missing range would incorrectly pass. Control fires |
| edge: two agreeing ranges pass | own verification, not in the plan's own U1 blocks | 🟢 | clone `/tmp/br-u1-verify-ctl/u1-edge-agree` provenance `f57f99d3`, matchAll grep `1`. Plant: `test` row's cell changed to `56 to 60 s baseline; confirmed 56 to 60 s again`, hash `19ad535100a19a0cc4fb3537edb15da1` -> `0d9a77e36994eca83dee752163664518`. Fixed script: `` `ok    time test: baseline 56 to 60 s, adapter 56 to 60 s, 56 to 60 s` ``, `exit: 0` | the negative control is U1-2/U1-3 above: the identical mechanism (two ranges in one cell) with one range disagreeing reds the same script on the same file. Two agreeing ranges pass, two disagreeing ranges fail: the predicate discriminates on agreement, not on range count |
| P1 | `npm test` green, no `node_modules`, tree byte-stable | 🟢 | fresh run in `/tmp/br-u1-verify` (own worktree, host at 0 test-shaped processes when started): `` `✓ all 48 test files passed` ``, `git status --short \| wc -l` = `0` before and after. Timing: no reading taken, 1-min load was 11.58 at start (host rule requires load 5 at or under 5 for a gradable timing reading; this run was well above that both times attempted, at 11.58 and 18.56) | clone `/tmp/br-u1-verify-ctl/p1-neg` provenance `f57f99d3`. Plant: `sed -i '' 's/"scrim/"scrimX/'` on `docs/reference/data/role-table.json`, hash `3772b50b4a90fde5ea892710b8fcf2c7` -> `64fec93579e586fef59aec303146e284`, `grep -c '"scrimX'` = `7`. Rerun: `✗ 1/48 test file(s) failed`, `FAIL  refs-canonical`, `exit: 1`. Control fires |
| P4 | branding gate clean | 🟢 | `/tmp/br-u1-verify`: `` `branding: clean (522 files scanned)` ``, `exit 0` (N not pinned per the plan's own hardening; today's count differs from the handoff's own `521` reading, expected drift from concurrent seats, not a defect in this unit) | clone `/tmp/br-u1-verify-ctl/p4-neg` provenance `f57f99d3`. Plant: `cp docs/reference/references/decision-records.md docs/x.md`. Rerun: `` `FAIL: 3 branding violation(s) across 523 files` ``, `exit 1`. Control fires |
| P5 | scope wall: only the wall's paths differ from the merge base, tracked or untracked, read from the unit worktree | 🟢 | in `/tmp/br-u1-verify` itself: the wall-exclusion grep count is `0` against the 5 files that actually differ (`.sdlc/checks/baseline-agrees-check.sh`, `.sdlc/handoffs/baseline-regex-checkability.md`, `.sdlc/handoffs/baseline-regex-U1.md`, `.sdlc/plans/baseline-regex.md`, `.sdlc/questions/baseline-regex-approval.md`), all matched by the wall pattern | clone `/tmp/br-u1-verify-ctl/p5-neg` provenance `f57f99d3`. Planted 4 off-wall files/edits (one byte on `.sdlc/adapter.md`, new `.sdlc/checks/new.sh`, `.sdlc/verdicts/k17-rerun-baseline-regex.md`, `.sdlc/plans/baseline-regex-extra.md`): count reads `4`. Control fires |
| P6 | no em dash added in prose | 🟢 | `/tmp/br-u1-verify`: `0` | in clone `/tmp/br-u1-verify-ctl/p5-neg`: an em dash appended outside a backtick span to `.sdlc/plans/baseline-regex.md` reads `1`; the same byte inside a backtick span, on the same file, reads `0` (the strip is deliberate and does not mask a real violation elsewhere). Both readings match the plan's own fixture exactly |

## Overall

🟢. Every U1 row, every P row, and both edge cases the reviewer claimed (no-range stays STALE, two agreeing ranges pass) reproduced independently in freshly cloned worktrees with proven provenance (commit `f57f99d3` plus a grep for the fix's own `matchAll` call in every clone used for evidence). The full clean-tree run of the shipped script at `f57f99d3` (not just the `test`/`build` rows) also reads all-`ok`, `stale total: 0`, `exit 0`.

No red rows. P1's timing sub-reading is the only ungraded cell: the host stayed at 1-minute load well above 5 (11.58, then 18.56) on both attempts, so no timing figure is reported for it; correctness (all 48 files passed, tree byte-stable, negative control fires) is fully graded and green.
